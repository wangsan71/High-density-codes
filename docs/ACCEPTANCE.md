# PSKT v3 · 自验收账（ACCEPTANCE）

> 判据原文在 `docs/PLAN.md`；本轮实测状态的历史叙述在 `docs/STATUS.md`。
> **这一页只回答三个问题**：这一条现在是什么判决、**用什么命令能自己复现**、差多少。
> 任何没有对应命令的数字都不该被相信，包括写在这一页里的。
> 最后更新：第 26 轮（G9 行由"未开始/零证据"更正为 🟡 产物级全绿、真机未验证 ✓ 另把"一句话结论"里对 G9 的低估一并更正 ✓ 台账虚高与虚低同样是错 ✓ 前次为第 22 轮：判决口径与 `docs/STATUS.md` 的"现况表"对齐 ✓）。

## 怎么读这份账

| 标记 | 含义 |
|---|---|
| ✅ **通过** | 判据全部满足，且证据命令能独立复现 |
| 🟡 **部分** | 判据的某一部分满足，**未满足的部分逐条列出**（不折算成百分比当成绩 ✓） |
| ⬜ **未开始** | 一行证据都没有 |
| ❌ **已知缺陷** | 有证据表明不满足，且原因已定界 |

贯穿全表的一条底线：**误接受是唯一不可原谅的失败**（帧 CRC16 + 页头 magic/version/CRC16 + 明文 SHA-256 摘要 + 原子写）。目前所有失败案例都是"拒绝且不写文件"，**没有一例静默误接受**。

## 逐条判决

### ✅ G0 · 规格可独立实现
`docs/PLAN.md` 是绑定契约；独立实现只需文本即可产出一致结果。
复现：`python ref/decode.py` → `conformance.json: PASS with 4 spec gap(s)`（309 项检查）
4 个 GAP 是 `geometry-*`：闭合它们要把决策 6 的整套 EW 量化搜索搬进规格文本，代价不划算；改由 G8/G10 用实物几何证据兜住（理由记在 `ref/README.md`）。

### ✅ G1 · 渲染—读回零误读
纸面/板材/双色/单色全剖面的渲染图经**同一份**读格代码回读。
复现：`node cli/pskit.mjs verify --gate G1` → 2 692 800 格 0 误读（历史最硬数字）；`--gate all` 全通过。

### 🟡 G2 · 纸面 300/600 dpi 扫描 100% 还原（判据：200 seed × ≤1 MB）⇒ **200/200 已跑通，但测的是"只有码区、没有纸张"的源页（D45 ✗ ⇒ **第 57 轮：这条保留意见对 300 dpi 侧已经解除**，改测"整张 A4 交付形状"后仍 200/200 ✓ 见下面第 57 轮那段）⇒ **第 58 轮：600 dpi 侧判决已出 ⇒ `FAIL G2 corpus: 162/200 byte-exact`（页级直接读出只有 258/600 = 43%）✗ 不达标** ⇒ **G2 整体：300 dpi 侧 ✓ 达标（200/200 = 100%）、600 dpi 侧 ✗ 不达标（162/200 = 81%）⇒ 未通过、维持 🟡、不上调**（判据要两侧都 200 seed × 100% ✓ 见下面第 58 轮那段）

**第 57 轮实测（(c) 的 300 dpi 侧判决出来了 ⇒ 前提已是"用户真的会打印/拍到的那张纸"）**：`node cli/pskit.mjs verify --gate G2 --root .tmp --match 'sc-scan300-*'` ⇒ **`PASS G2 corpus: 200/200 byte-exact in 1113.7s`**，门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200`，**`ALL GATES PASS`**、**`GATE exit=0`**（日志 `.tmp/g2-a4-300.log`：生成 15:02:40 收工、判决 15:21:14 收工）。

- **前提（与第 51 轮那次的唯一差别，也正是关键差别）**：源页 `.tmp/g2src-a4-300` 是**整张 A4 sheet `2480×3508`**（码区按标称物理尺寸居中 + 留边里的四角裁切标记与四边套准十字），不再是"只有码区"的 `2260×3290` ⇒ **D45 修好之后的交付形状** ✓ 载荷同一份 `.tmp/payload.bin`（sha256 `91ab3f7310da452b`）⇒ 每份 3 页 × 200 份 = **600 页盲解**；生成命令 `python sim/channel.py --in .tmp/g2src-a4-300 --out .tmp/sc-scan300-N --seed N --preset scan300 --modifier nocrop`，**200/200 生成 exit 0**、信道自报 `crop=0.0`（平板扫描仪的物理正确模型：纸贴定位边、视野大于纸面 ⇒ crop 本该为 0 ✓ 不是"把裁角关掉来凑通过率"）
- **页级明细（判据是载荷级，但边际在哪必须如实写）**：**13/200 份各有 1 页读不出、全部由页间 RS 重建**，其余 187 份三页直读 ⇒ **页级直接读出 587/600 = 97.83%**、**载荷级 200/200 = 100% 逐字节、每份都 `digest verified`、含 FAIL 的行数 = 0** ✓ 那 13 页的原因分布：`readout/echo-header-crc` × 10、`markers/no-hollow-corner` × 2、`readout/echo-bad-magic` × 1 ⇒ 冗余正是为此而存在（一页读不出 ≠ 整批失败 ✓ 协议设计意图第一次在全样本量下兑现）。**这不是把失败算成通过**：判据比的是最终落盘字节与原文件的 SHA-256，摘要由 `tools/g2-corpus.mjs` **在进程内重算**、不 grep CLI 措辞 ✓
- **不许被引用的部分**：这次运行**只评估了 G2**，门限自己打印 `not evaluated by this run: G0 G1 G3 G4 G5 G6 G7 G8 G9 G10`（并说明 `G4 G9 need a real phone/browser, G6 is the long soak, G10 needs a printer`）⇒ **`ALL GATES PASS` 不得被引用成"全部门限通过"** ✗
- **G2 整体仍 🟡、不上调**：判据要求 300 **与** 600 两侧都做到 200 seed × 100% ⇒ 600 dpi 侧（`pwsh-14` → `.tmp/sc-scan600-*`，源页 `4961×7016`）判决时仍在生成（52/200、77.5 s/份 ⇒ 还要约 3 h）⇒ **本节不写 600 dpi 的任何比率** ✗ 另外：历史上 600 dpi 那 2 例失败曾被归因于"夹具没有纸边 ⇒ 角标紧贴图像边缘、任何姿态都出画"，**该归因本轮既未被证实也未被推翻**，等 `sc-scan600-*` 的判决（**第 58 轮判决已出 ⇒ 该归因被推翻**：交付形状 + `nocrop` 下 `markers/no-hollow-corner` 仍有 **41 页**、语料级 **38/200 失败** ⇒ 详见下一段）；而 300 dpi 侧这 2 页 `markers/no-hollow-corner` **也不许被读成"那个问题消失了"**——它们是被 RS 兜住的页级失败，确实发生了 ✓

**第 58 轮实测（(c) 的 600 dpi 侧判决出来了 ⇒ **不达标 ✗**）**：`node cli/pskit.mjs verify --gate G2 --root .tmp --match 'sc-scan600-*'` ⇒ **`FAIL G2 corpus: 162/200 byte-exact in 4359.7s`**，门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200`，**`GATE FAILURE`** · **`GATE exit=1`** · `failure classes: short 0/1 x37, no-page-header x1`（日志 `.tmp/g2-a4-600.log`：生成 18:05:26 收工、判决 19:18:08 收工）。

- **前提与 300 dpi 侧完全对称 ⇒ 差别只在 dpi/档**：源页 `.tmp/g2src-a4-600` = **整张 A4 sheet `4961×7016`**（D45 修好后的交付形状：码区按标称物理尺寸居中 + 留边里的四角裁切标记与四边套准十字），**同一载荷** `.tmp/payload.bin`（sha256 `91ab3f7310da452b`）⇒ 每份 3 页 × 200 份 = **600 页盲解**；生成命令 `python sim/channel.py --in .tmp/g2src-a4-600 --out .tmp/sc-scan600-N --seed N --preset scan600 --modifier nocrop`，**200/200 生成 `exit 0`** ⇒ 失败**不是**语料生成失败、**不是**夹具形状问题、也不是两侧口径不一致（`nocrop` 两侧都用 ✓）
- **页级明细（这才是真画面，别只看 162/200）**：**342 条页级失败 / 600 页 ⇒ 页级直接读出只有 258/600 = 43%**（300 dpi 侧同口径 **97.83%**）；**162 份 OK 里有 145 份也含页级失败**（被页间 RS 兜住 ⇒ 冗余在超负荷工作），38 份 FAIL 全部含页级失败。原因分布：`assemble/intra-fail` **× 210**、`readout/echo-bad-magic` × 78、`markers/no-hollow-corner` × 41、`readout/echo-header-crc` × 12、`readout/echo-version-3` × 1
- **历史归因被推翻（必须改口 ✓ 台账虚高与虚低同样是错）**：第 49–52 轮我曾把 600 dpi 那 2 例失败归因于"夹具只有码区、没有纸边 ⇒ 角标紧贴图像边缘、任何姿态都出画"。**本轮在交付形状（整张 A4 + `nocrop`）下重测：`markers/no-hollow-corner` 仍有 41 页、语料级仍有 38/200 失败** ⇒ 那个归因**不成立** ✗（不变的那半边：这**不是**解码器把可读页误判成不可读——3 角 6 方程 < 单应 8 自由度 ⇒ 拒绝是正确的 ✓ 见本页"已知物理边界"段与 `sim/README.md`）
- **~~主因指向"页内 ECC 余量不足"~~（**第 61 轮实测推翻 ✗** 详见本段末的第 61 轮块与 `DEFECTS.md` D51：能读出的 600 dpi 页只花 **0.0–0.2%** 预算 ⇒ 余量充裕；坏页是**电平判读整体塌陷**），但当时根因未定、也没写成已解**：210 页 `intra-fail` 与本页历史 A 类结论一致（第 19 轮逐格实测：`0.23%` 误码 = 669 格就已超每块 RS 预算 ⇒ 当时记为 `P-M1-600@600dpi` 的**容量事实**、不是 bug ✓ `sim/README.md` 的 G2 行也早写明"10 px 格距下 `ew_mm`/`mtf_mm` 哪怕 0.02 mm 就破页内 ECC"）⇒ 这一档在**标称信道模型**下就是余量不足（**← 第 61 轮推翻 ✗** 实测：能读出的 600 dpi 页只花 **0.0–0.2%** 预算、300 dpi 侧 9 页全 **0.0%** ⇒ 余量不是约束；坏页是**逐格电平判读塌陷到单一电平** ⇒ D51）；但"为什么 600 dpi 的原始误码率比 300 dpi 高这么多"仍属本页待办第 4 项（B 类根因未定）与第 1 项（`pskit calibrate` 未实现 ⇒ 无法排除"差的是能力而不是样本量"）⇒ **本轮不下根因结论** ✗
- **⇒ G2 整体判决：300 dpi 侧 ✓ 达标（200/200 = 100% 逐字节）、600 dpi 侧 ✗ 不达标（162/200 = 81%）⇒ G2 未通过、维持 🟡、不上调** ✓ 判据要两侧都 200 seed × 100%；**没有**用"只报通过的那一档"来凑数 ✗，也**没有**为了让 600 dpi 变绿而改信道参数、或把正确拒绝剔出分母 ✗ 误接受仍为 **0**（38 份失败全部是**拒绝/缺页**，没有一份交出错误字节 ⇒ 硬约束未被破坏 ✓）
- **对用户的直接影响（本轮已修文档 ⇒ 记 D49）**：`docs/USE.md` §3 原先写"扫描：300 或 600 dpi"⇒ 把两档并列成等价选择 ✗ 实测 600 dpi 只有 81% ⇒ 已改为**推荐 300 dpi（实测 200/200）**并明示 **600 dpi 暂不可靠（162/200 ⇒ D49 OPEN）**；`--profile P-M1-600` 仍在 CLI 的 `--profile` 帮助里与其它档平等列出（`cli/pskit.mjs` L24），而**~~网页发送端不提供该档~~（第 60 轮撤回 ✗ 错判）**（当时的"证据"是实测 grep：`web/` 内无 `P-M1-600` ⇒ 照手册走网页的用户不会踩到 ✓ **← 这个结论第 60 轮撤回 ✗** `web/sender.js` 的下拉框是 `for (const [id, p] of Object.entries(PROFILES))` **动态枚举全部档位**建的 ⇒ 档位 id 从不以字面量出现 ⇒ **grep 不到 ≠ 不提供**（文本扫描能证明"某串在"，证明不了"动态列表里没有某项"）⇒ 真实暴露面更大：网页用户也能一键选到会失败 19% 的那一档 ✗ **第 60 轮已修**：标签经 `profileOptionLabel()` 渲染并对"非板材且 dpi ≥ 600"加 `⚠ 实测未达标 (D49)`（**不隐藏该档**）、CLI 给 stdout + manifest note 双警示、守卫测试 `tests/unit/profile-picker-warning.test.mjs` **4/4 绿**（含反空转对照）⇒ 详见 `DEFECTS.md` D49/D50 与 STATUS 第 60 轮），但这只是"我看过一眼"，**没有测试盯着它不被加回去** ✗）⇒ CLI 侧的警示与那条守卫测试都留到下一轮（要动 `cli/` + 跑套件；两个 G2 批本轮都已收工 ⇒ D28 解除 ✓）

**第 61 轮（`pskit calibrate` 落地 ⇒ G2 600 dpi 侧的根因**测到了**：不是余量、是电平判读塌陷 ⇒ 记 D51）**：**判决不变** —— 300 dpi ✓ 200/200、600 dpi ✗ 162/200 ⇒ **G2 整体未通过、维持 🟡、不上调**；本轮**没有重跑任何门限**，因为 `core/` **一行未动** ⇒ G2 的前提与第 57/58 轮完全相同 ✓ 新增的是**分辨能力**：

- **量具**：`node cli/pskit.mjs calibrate <dir|file>` ⇒ 逐页打印 readout 结果、页内 RS **预算占用** `(2·errors + erasures)/(blocks·nsym)`（失败块按"耗尽整块预算"计 ⇒ 明写 **LOWER BOUND**，不做讨好剖面的数字）、阈值无关的着墨面积比（仅健康检查）。它**复用接收端同一条路径**（`decodePage` + `TransferAssembler.feed`；后者本来就在成功路径 `core/protocol.js:489` 与拒绝路径 `:484` 返回 `intraDecode` 的 stats ⇒ 所以 `core/` 无需改动），**不是门限**（exit code 不是判决）、**不派生阈值**、**不改任何解码判决** ⇒ 不需要重判 G5/G2 ✓ **PLAN §M7 的 calibrate（MTF 校准板 → 推荐喷嘴/格距）仍未实现** ✗ 全量套件 **301/301 · 0 fail**、`SUITE_EXIT=0`（条数与第 60 轮相同 ⇒ calibrate 未带新测试，如实记：`cli/` 仍无测试挂点，本轮以可执行证据结案 ✓）
- **测量（对照齐全 ⇒ 不是孤证）**：300 dpi 三份语料 9 页 ⇒ **全部 `errors 0 · erasures 0 · blocks clean 102/102 · budget 0.0%`**；600 dpi 侧 `sc-scan600-12` p0 = **0.0%**、p1 = **5 errors ⇒ 0.2%**、`sc-scan600-11` p0 = **0.0%** ⇒ **能读出的 600 dpi 页与 300 dpi 一样干净**；而坏页（`sc-scan600-11` p1/p2、`sc-scan600-10` p1）是 **`blocks 141 · clean 0 · errors 0 · erasures 0 · failedBlocks 141`** ⇒ 分布**双峰**，不是"贴着余量悬崖"；`sc-scan600-105` 三页在 readout 之前就死（`echo-bad-magic` ×2、`markers/no-hollow-corner` ×1）⇒ 那是另一条通路，**不许与塌陷一并归因** ✗
- **根因（逐格对拍，对拍工具 `tools/level-diff.mjs`（第 61 轮由 gitignored 探针 `.tmp/lvl61.mjs` 提升入库并参数化 ⇒ 台账不再指向不入库的文件 ✓）；真值 = `.tmp/g2src-a4-600` 的原始渲染，即 `sim/channel.py` 的输入）**：`sc-scan600-11` page-001 的信道页被读成**内部 283155 格全部 = 电平 1**，而原始渲染是 `0=256118 1=27037` ⇒ 失配 **90.452%**、混淆**只有 `0->1` 一种**、行/列带**平坦**（行带 `51549 51571 51706 51767 51692`）、48 个 ±3 格平移里最好的仍 **90.4%** ⇒ **不是噪声**（噪声会有多种混淆对且空间聚集）、**不是局部损伤**（平坦）、**不是格构偏移**（平移无效）⇒ 是**电平判读整体塌陷**；同语料 page-000 作对照失配 **0.000%**（两侧直方图完全一致 `0=256501 1=26654`）、`sc-scan600-12` p1 的 5 个 RS 错误全部落在内部窗口外的 2 格边带（行带 `2 1 0 0 2`、列带 `3 0 0 0 0`）⇒ **量具与 RS 报告互相印证** ✓
- **因此被推翻/更正的**：① 第 19 轮记为"容量事实、不是 bug"的 A 类 ⇒ **是 bug（D51）** ✗ ② 第 58 轮"主因指向页内 ECC 余量不足" ⇒ **推翻**（预算占用 0.0–0.2%）✗ ③ 挂了四十多轮的"600 dpi 差的是能力还是样本量"⇒ **答案：两者都不是**，是判读缺陷 ⇒ **加冗余与加样本量都救不了这类页** ✗
- **仍然不许写成结论的**：塌陷的**机制**（"dpi 翻倍 ⇒ 每像素峰值覆盖率约减半 ⇒ 固定阈值落到 ρ 分布之外"）**尚未直接量过** ⇒ 下一步用 `core/decode/ideal.js#analyseCell` 取塌陷页与健康页的 **ρ 分布**对比（第 15 轮只对**角标二值化**证伪过 `otsu/peak` 0.54 vs 0.57；**电平判读是另一条通路，还没量过** ✗）
- **修法与其护栏（下一轮）**：按页重测 ρ 表再定阈——构件已在仓内（`core/render/raster.js#measureTargets`、`ideal.js#shapeThresholds` 接受 `{targets}`、`ideal.js#expectedRho`、`ideal.js#analyseCell`、参考实现 `sim/selfcheck.py` 的 `calibrated_targets`）⇒ 但它会**让接受更容易** ⇒ **必须回 G5 重验误接受**（0 是不可原谅线），且判据必须是**跨 seed 的稳定谷底**而不是"这一页能过"（第 11 轮拒绝把 `0.65` 钉进代码就是防这个 ✗）⇒ **本轮刻意不做**：夹带一个会改变判决的解码器改动、却不同时重判 G5/G2，等于制造新的虚高 ✗
- **误接受仍为 0** ✓ 而且这轮看清了**是谁在拦**：塌陷页产出了一张"看起来完整"的电平网格（**readout 没有拒绝它** ✗），拦住它的是页内 RS 找不到自洽错误图样 + 最后的 SHA-256 摘要门 ⇒ **硬约束正在干活**，代价是整页白印 ✗（附带可改：这类页本该在 readout 就被点名拒绝，像只有 3 个角标时那样 ⇒ 但它不提高通过率、且会改变 reason 码分布 ⇒ 要重跑 G2 两侧才能记账 ⇒ 本轮不做，记在 D51）

**第 62 轮（D51 的机制量到 ⇒ 判决仍不变；下一轮改判决时的验收计划先写死）**：**本轮没有跑任何门限判决**（`core/` 一行未动 ⇒ G2/G5 前提与第 57/58/61 轮相同）⇒ **G2 仍是 300 dpi ✓ 200/200 · 600 dpi ✗ 162/200 ⇒ 整体未通过、维持 🟡、不上调**；**误接受仍为 0** ✓ 新增的是"改判决要按什么验收"的依据：

- **量具**：`tools/rho-report.mjs <pristineDir> <channelDir> <pages>`（ρ 分布与同一页原始渲染对拍；**镜像对照** `0 differing level(s) of 287507` ⇒ 量的是产品路径；**零退化对照** ⇒ 当场纠正并**撤回**了本轮中途"ρ 口径不一致、比值路径每页都坏"的错判 ✗）；只量不改、不是门限
- **机制**：信道使 ρ 抬 **4–20 倍**（pristine `0.0021/0.3741` → 300 dpi 完美解码页 `0.5939/1.5750` → 600 dpi 塌陷页 `1.2684/1.6824`）⇒ 固定切点 `mid[0]=0.1850` 在**任何**信道页错 **60.1–90.6%**、pristine 错 **0%** ⇒ **塌陷发生在匹配滤波**（模板形状固定、自由增益吸收不了渗墨造成的形状变化），而 **ρ 信息仍在**（分离度 3.61–17.67）
- **可修性**：**无监督** Otsu 切点在 9 个信道页上把逐格错误压到 **≤0.072%**（含 3 个塌陷页：208 / 1 / 1 格），RS 上限 **6.30%** ⇒ 这些页救得回来；**但朴素 Otsu 在退化直方图上给 60.102% 错误** ⇒ 切点须落谷底 / 并列平台中点，否则会打破 pristine/fast 路径 ✗
- **下一轮的验收计划（先写死，免得事后挪门柱）**：① 修法 = **RS 仲裁的重读**（MF 路径不变；仅 `intra-fail` 时按页标定重读，且**只有 RS / 帧 CRC / SHA-256 通过才接受** ⇒ 判例 `core/decode/echo.js:40-51`）② **不得放宽任何判据**：G2 仍按 100% byte-exact、G5 仍按误接受 0 ③ 必须**重跑 G2 两侧**（300 dpi 侧必须仍是 **200/200**，否则就是回归 ✗）与 **G5**（误接受必须仍是 **0**）④ 600 dpi 侧的**预期不许先写进台账当结论**：`intra-fail` 那 210 页有望被救，但 `echo-bad-magic` 78 页 / `no-hollow-corner` 41 页 / `echo-header-crc` 12 页是**别的通路**、本修法**不覆盖** ⇒ 通过率能到多少**由重跑决定** ✗ ⑤ 套件必须仍 0 fail，且新增单测须覆盖"退化直方图"这个陷阱（否则修完就会在 pristine/fast 路径上悄悄回归 ✗）⑥ 必须同时改门限 harness `tools/g2-corpus.mjs`，否则产品与判据分家 ✗
- **另记 D52**：`inkBalance.ratio` 被自己的判决污染（塌陷页 `2.71–3.52` 反而低于健康页 `11.57–20.70`）、`ratioDisagreements` 对塌陷**失明**（塌陷页 0、健康页 4 万～26 万）⇒ 两者都不参与判决（**误接受不受影响** ✓），但 `ideal.js` 的注释正把它们当机制诊断量 ⇒ 会误导下一个修 D51 的人，须在 G2/G5 重跑**之前**定稿

**第 63 轮（D51 的修法落地 ⇒ G2 300 dpi 侧重跑 PASS、G5 重验 PASS 并覆盖新接受路径；600 dpi 侧判决待跑完，本轮不记）**：

- **G2 300 dpi ✅ `PASS G2 corpus: 200/200 byte-exact in 1245.4s`、`exit 0`**（`node cli/pskit.mjs verify --gate G2 --root .tmp --match 'sc-scan300-*'`）⇒ 验收计划第③条的非回归要求**达标**：判据一字未改（100% byte-exact、200 个固定 seed）、语料未重生、失败页仍全进分母 ⇒ 修法没有靠挪门柱换来绿色
- **G2 600 dpi：最终判决没有取 —— 重跑由用户中止，不是跑绿了 ✗** 进行到 **52/200（51 OK / 1 FAIL）**时用户第 63 轮明示「其实没必要 600dpi，只要能够读到就行」⇒ 我据此 `job_kill` 停掉该次重跑（还要约 3 h）。**这是产品负责人对范围的判定，不是放宽判据**：G2 仍按 100% byte-exact 判、失败页仍全进分母、语料与信道参数未动、三角点拒绝未动。已见事实照记：页级失败原因分布 `echo-bad-magic 19`、`no-hollow-corner 12`、`echo-header-crc 5`，而**旧跑里 210 次的 `assemble/intra-fail` 在这 52 份里 0 次** ⇒ 修法命中的正是它声称要命中的那一类，且没有把读出前的别的通路一起"变好"（那本来也不可能）。**600 dpi 在新读法下没有门限判决**；谁要用它，得自己重跑 `verify --gate G2 --root .tmp --match 'sc-scan600-*'`（约 3 h）取判决 ⇒ **D49 因此降为"不阻塞可用（用户判定非必要路径）"，但历史实测 `162/200 = 81%` 保留、不改写成达标**
- **G5 ✅ `G5 false acceptance: PASS`、`exit 0`，两半都跑**：① 原 **10000** 次篡改试验的**配比与 PRNG 序列一字未动** ⇒ `9200 recovered exactly / 800 refused / **0 FALSE ACCEPTS**`，守卫分布 `accepted-original=9200`、`feed:intra-fail=3408`、`feed:header:header-crc=1324`、`refused:digest-mismatch=800`、`feed:header:bad-magic=84`、`feed:header:version-5=7` 与第 57/58 轮**逐字相同** ⇒ 可比，且证明核心改动没扰动协议守卫 ② **新增 2000 次接缝试验**（跑在 P-M1-300 上、ρ 描述"第三种读法"⇒ 给重读最大的作恶机会，并断言 `shapeLevelsOf === 2` 防退化、断言 `seamRetried > 0` 防空跑）：**0 FALSE ACCEPTS**、重读实际触发 **2000** 次 ⇒ **误接受仍为 0，而且这条保证现在覆盖了新的接受路径**，不再只是"没碰所以没问题" ✓ **如实注**：那 2000 次全部"精确恢复"是**跨页奇偶**兜住了被页内 RS 拒掉的重读页，**不是**重读把 2000 页都修好了 ✗ 别那么读
- **判据未放宽的证据（逐条对第 62 轮写下的验收计划）**：G2 仍按 100% byte-exact 判 ✓ G5 仍按误接受 0 判 ✓ 失败页仍全进分母 ✓ 三角点拒绝（`no-hollow-corner`）一字未动 ✓ 信道参数与语料未重生 ✓ 新接受路径的接受者是**页自身的 RS + 帧 CRC + 全载荷 SHA-256**、不是那个切点 ✓ 门限 harness `tools/g2-corpus.mjs` 与产品同轮改（第⑥条）✓ 退化直方图陷阱有单测守着（第⑤条）✓
- 其余执行证据（套件 `307 · 307 · 0`、usability 冒烟全过 331 s 且字节相同、`build-web` 29 模块、`check-dist` 13 PASS 含 G9 12 断言、产物内含 `feedPageWithRecalibration`）见 STATUS 第 63 轮块与 D51

**第 262 轮（G2 300 dpi 侧的复核，不是新判决）：`node cli/pskit.mjs verify --gate G2 --root .tmp --match 'sc-scan300-*'` ⇒ `PASS G2 corpus: 200/200 byte-exact in 1271.1s`**（门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200`）—— 语料是第 57/63 轮那一批（`.tmp/sc-scan300-1..200`，每种子 3 页、载荷 204,800 B），**未重生；判据、信道参数、失败页分母一字未动**。目的只有一个：确认第 237–261 轮对解码侧的改动（`advice.js` 的文案、`calibrate.js` 的死代码删除、`glyphs.js` 取消导出的常量）**没有回归**这条主线 ⇒ **无回归** ✓（本轮 200 份全部逐字节一致 ⇒ 没有可归类的失败页）。**判决不变**：300 dpi 侧 ✓ 达标、600 dpi 侧维持产品负责人第 63 轮的判定（不必做）⇒ **G2 整体维持 🟡、不上调**；**误接受仍为 0**。这不是新的样本量主张 —— 复用的是同一批固定 seed ✓

**第 37 轮起这是门限、不再只是手工命令**：`node cli/pskit.mjs verify --gate G2 --root .tmp --match 'nc-scan*'`（或 `--corpus DIR`）✓ 门限 `import` 同一个 `tools/g2-corpus.mjs` ⇒ 摘要比较、失败分类、"100% 否则 FAIL"的算术只有一份 ✓
**第 51 轮实测（300 dpi 侧补满样本 ⇒ 通过）**：`node cli/pskit.mjs verify --gate G2 --root .tmp --match 'nc-scan300-*'` ⇒ **`PASS G2 corpus: 200/200 byte-exact in 1085.4s` · `ALL GATES PASS` · exit 0**，且门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200` ⇒ **样本量对齐 PLAN、判据未放宽** ✓ 语料 `.tmp/nc-scan300-{1..200}`（200 KB 载荷 ⇒ 每份 3 页 ⇒ 共 600 页盲解），生成命令 `python sim/channel.py --in .tmp/g2src --out .tmp/nc-scan300-N --seed N --preset scan300 --modifier nocrop`，**200/200 生成 exit 0、0 失败** ✓ 判决过程里若干份带**逐页提示**（`markers/no-hollow-corner [fiducial-out-of-frame] 2 of 4 corners`、`readout/echo-header-crc`）而**该份仍 `OK 204800 bytes, digest verified`** ⇒ 提示是页间/页内冗余在起作用，**不是失败** ✓（历史记录：第 37–50 轮按 32 份**混样**记账为 `FAIL G2 corpus: 30/32 byte-exact in 392.3s` ⇒ 那 2 例**都在 600 dpi 侧**，与本节下面的定界一致 ✓）
**已证（分侧记、别混）**：**300 dpi 侧 = 200 seeds × 200 KB × 3 页 ⇒ 200/200 = 100% 逐字节**（第 51 轮 ✓ 摘要由工具**进程内重算**、不是 grep CLI 的措辞 ✓）；**600 dpi 侧仍只有 seed 1–16**（30/32 那次混样里的 2 例失败即在此侧，已定界为信道前提 ✓ 见下）。**⇒ G2 整体尚未达成**：判据要求 300 **与** 600 两侧都做到 200 seed × 100% ✗ 本节标题因此维持 🟡、**不上调** ✓
**防伪绿两道（不变）**：干净渲染语料会被 `corpusProvenance` 拒绝（实测 119 个目录：干净 10–18 灰阶 vs 过信道 192–256 ⇒ 地板 64 ✓ 阳性对照 `.tmp/g2src` ⇒ FAIL exit 1 ✓），以及"没有语料"记为 **skipped 并点名**、不算通过 ✓
**失败已分类（第 16 轮定界完成 ✓ 结论是"不是解码器缺陷"）**：`30/32` 里的 2 例失败页经像素级查明是**角标出画**——失败页四边着墨率 `top 4.7% / left 89.5% / right 80.8% / bottom 100%`（健康页四边均 100% ✓）⇒ 顶边被切出成像区 ✓ 只剩 3 个角标 ✓ 而 **3 点只提供 6 个方程、单应有 8 个自由度 ⇒ 定不出单应 ⇒ 拒绝在数学上正确** ✓ 仿真器侧也按自己的文档行动（`--modifier nocrop` 确实归零 `crop_frac` ✓ 打印的 `crop=0.00281` 是"实际出画面积" `1-visible_frac` ✓ 来自姿态/透视/`fill` ✓）。A4 放在几乎等大的平板上带 0.x° 旋转时角扫出画外是真实物理 ⇒ **这 2 例属信道前提、不属解码器** ✓ **【第 51 轮更正 ⇒ 我第 49 轮那句"A4 放在几乎等大的平板上带 0.x° 旋转时角扫出画外是真实物理"**撤回** ✗】** 实测夹具形状：语料源页 `.tmp/g2src` 的 PNG 是 **2260×3290 px @300dpi = 191.35×278.55 mm**（600 dpi 侧 `.tmp/sw-scan600-src` 是 4530×6590、manifest 里 `sheetMm=null`），**都不是 A4**（A4@300dpi 应为 2480×3508、@600dpi 应为 4960×7016），源页 mtime `2026-09-07 08:36` 也早于第 48 轮那次"页=纸张"的提交（`2026-09-08 11:50`）；而且**今天重跑 `pskit send .tmp/payload.bin --profile P-M1-300 --sheet A4 --format png` 仍然只输出码区**（`send exit=0`、`render 2260x3290px @ 300dpi`、`sourceSha256=91ab3f7310da452b` 与旧语料同一个载荷 ⇒ **`--sheet` 只作用于 PDF 路径** ⇒ 记为 **D45**）⇒ 所以被仿真的是"一张**没有纸边、角标紧贴图像边缘**的页"，任何姿态或透视都会把角标推出画外 ⇒ **那 2 例失败很可能是夹具形状造成的、不是真实平板扫 A4 的物理** ✗ **"不是解码器缺陷"这个结论不变**（3 点只给 6 个方程、单应有 8 个自由度 ⇒ 拒绝在数学上正确 ✓），但**归因错了**，而且**200/200 那个数字只覆盖"码区页"这个前提**，覆盖不到用户今天真正会打印/拍摄的"整张 A4 带留边与裁切标记" ⇒ 修法与重测顺序见本节末与 D45 ✓
**判决不上调**：仍记 `30/32 = 93.8%` ✓ 把正确拒绝从分母里剔掉等于重定义判据 ✗ 不做 ✓ 待做的是**记账口径**：语料工具需标注 `fiducial-out-of-frame` 与解码器失败两类（下一步 ✓）✓ 两条出路各自代价写明：① 明示 G2 前提"纸张完整位于成像区内"（平板有定位边 ⇒ 合理 ✓ 但旋转仍可切角 ✗）② **3 角标 + 已知页面宽高 ⇒ 仿射矫正**（放弃透视项 ⇒ 平板近乎无害、手机有害 ✓ **属能力扩展、不是 bug 修复** ✓ 且它会让"接受更容易" ⇒ 必须按 G5 的误接受口径重新验收 ✓）
**本轮我连续三次自我否证**（笔画核 → 纸纹桥接 → nocrop 说谎 ✓ 全错 ✓）⇒ 根因是只用计数猜结构而未看像素 ✓ 新增 `tools/diag-mask.mjs`（ASCII 着墨图 + 四边/内部着墨率 ✓ 一次就终结了三轮猜测 ✓）
复现：`node tools/diag-mask.mjs .tmp/nc-scan600-3/page-001.png` ✓ `python sim/channel.py --in .tmp/sw-scan600-src --out .tmp/nc-probe-3 --seed 3 --preset scan600 --modifier nocrop`

**600 dpi 侧的下一步（第 51 轮定：先诊断源页前提，再决定跑不跑那 2.3 小时的批）**：现有 16 份语料的源页 `.tmp/sw-scan600-src` 很可能是**第 48 轮那次修正之前**渲染的——那一轮才把产物改成"页 = 纸张 A4、码区按标称尺寸居中、四周 ~9.3 mm 留边 + 裁切/套准标记"（D44 闭合 ✓）。**若源页本身没有纸边，`--modifier nocrop` 下角标就贴着成像区边缘、稍有姿态就出画** ⇒ 那 2 例失败可能测的是**旧产物**而不是用户今天会打印的东西 ✗ 所以：**① 先比对 `.tmp/sw-scan600-src/manifest.json` 与当前 `pageLayout` 的 `sheetMm`/`physicalMm`（以及页 PNG 的像素尺寸是否等于 A4@600dpi）**；② 若确属旧前提，就用**当前渲染器**重出 600 dpi 源页（`pskit send --sheet A4 --dpi 600`）再跑 `seed 1..200` ⇒ 这才是"用户今天打印的东西过信道"；③ **不许**为了让比率好看而改信道参数或把正确拒绝剔出分母（那两条都是重定义判据 ✗）。300 dpi 侧的 200 份语料是**同一套源页**渲染的（`.tmp/g2src` ⇒ 也需要同样核一遍前提；它 200/200 通过 ⇒ 即便源页是旧的，结论对旧产物成立、对当前产物**待重验** ✗ 这一点必须写明，不能含糊 ✓）。

**诊断结果（第 51 轮已做 ⇒ 上面 ①②③ 的顺序作废，按这里走）**：① 的比对结论是**源页确实没有纸张**（2260×3290@300dpi、4530×6590@600dpi、manifest `sheetMm=null`），**但这不是"语料过期"——今天的渲染器加 `--sheet A4` 出 PNG 仍然只给码区** ⇒ 病根在**栅格路径不认 `sheet`**（**D45** ✗），不在夹具老旧 ⇒ ② 因此**不能**靠"重出源页"解决（重出一百次也是同一个形状 ✗ 我实测过一次：`.tmp/g2src-sheet` 与 `.tmp/g2src` **同形** ⇒ 所以那个约 85 分钟的 200-seed 批**我没有起**——起了就是用同一前提再测一遍、白花机器时间 ✗）。**正确顺序**：**(a) 先让栅格路径认 `--sheet`**——`pageLayout`/`renderPageBitmap` 已经算得出 `sheetMm` ✓、PDF 路径也已用它做居中与标记 ✓ ⇒ 栅格侧要做的是：按 sheet 尺寸开位图、填纸白、把码区贴到居中偏移、并按 **PDF 里同一套 mm 几何**画四角裁切标记与四边套准十字；配单测，且必须含"**不传 sheet 时逐字节不变**"的回归保护（`web/sender.js` 与 CLI 的 PNG 输出都被现有测试盯着 ✓）；**(b) 用修好的渲染器重出 300/600 两侧源页**（同一载荷 `.tmp/payload.bin`、sha256 `91ab3f7310da452b` ⇒ 与历史语料可直接对比 ✓）；**(c) 再跑 seed 1..200 × 两侧**（300 dpi 约 1 h、600 dpi 约 2.5 h ⇒ 后台分批）⇒ **只有 (c) 的结果才配写进"G2 达标/不达标"**。在此之前，300 dpi 的 **200/200 只能记作"码区前提下 100%"**、本节标题维持 🟡 ✓ ③ 不变：**不许**为了让比率好看而改信道参数、也不许把正确拒绝剔出分母 ✗

**第 52 轮：(a) 与 (b) 已做完、(c) 正在跑 ⇒ 本节上面的数字暂不变、也不新增比率**。(a) `--sheet` 现在**对栅格也生效**：新增 `core/render/sheet.js` 作**唯一几何真源**（mm 单位，PDF 与栅格都调它 ⇒ 两条路径不可能各自漂移），`core/render/raster.js` 的 `renderSheetBitmap()` 把码区居中贴到纸上、按同一套几何画裁切/套准标记 ⇒ `pskit send .tmp/payload.bin --profile P-M1-300 --sheet A4 --format png` 实测输出 **2480×3508**（600 dpi 侧 `--profile P-M1-600` ⇒ **4961×7016**；4961 是 4960.63 px 取整、偏差 0.01% ⇒ 上一轮我写的"应为 4960"才是 naive 期望 ✗），PDF 侧**几何不变**（`pdf-truesize` 5 项仍绿、MediaBox 仍 `595.28 841.89` pt）⇒ **D45 CLOSED**；新单测 `tests/unit/raster-sheet.test.mjs` **7 项**，其中两条是"**标记绝不碰码区**"的像素级与算术级判定（含阳性对照：把纸张尺寸改 1 mm 必须不匹配）⇒ 全量套件 **296/296 · 0 fail**。(b) 两侧源页已用修好的渲染器重出：`.tmp/g2src-a4-300`（3 页 2480×3508）与 `.tmp/g2src-a4-600`（3 页 4961×7016），**同一载荷** `.tmp/payload.bin`、sha256 `91ab3f7310da452b` ⇒ 与历史语料可直接对比 ✓ (c) 两个后台批已起（`pwsh-13` = 300 dpi、`pwsh-14` = 600 dpi，各 seed 1..200，语料名 **`sc-scan300-*` / `sc-scan600-*`**，与旧的 `nc-*` **分开命名 ⇒ 两种前提永远不会混进同一个数字** ✓）⇒ **判决未出 ⇒ 本节不写任何新比率** ✗ **端到端旁证（不是 G2 判决、别混为一谈）**：`& .\tools\usability.ps1` **exit 0 / 285 s / 11 项全 PASS**，其中信道仿真与 `pskit receive --photo` 吃的正是**带留边与裁切标记的整张 A4**、落盘字节与原文件相同（`sha256 32ec4805…` 进出一致）⇒ 至少证明"整张纸过信道再解码"这条路通、且没有误接受 ✓ 另：**纸面档在 CLI 里本来就默认 A4** ⇒ 修好之后纸面 PNG **一律是整张纸**（想要裸码区只能走码牌路径或自己裁）⇒ 这是修复的后果、不是回归 ✓

**第 19 轮把 `intra-fail` 拆成两种病（用 `tools/diag-intra.mjs` 逐格对照同页的 pristine 读出 ✓ 9 页全测 ✓）**

| 类 | 页 | 实测格失配 | 定性 |
|---|---|---|---|
| **A · 页内 ECC 余量薄** | `600-2/000` `600-3/000` `600-5/001` `600-14/000` `600-15/001` | `6.27%` `7.22%` **`0.23%`** `0.52%` `1.41%` | **不是缺陷 ✓** `0.23%`（669 格）就已超每块 RS 预算 ⇒ 这是 `P-M1-600@600dpi` 的**容量事实** ✓ 也解释了为何语料仍逐字节复原（页间奇偶从别页补回 ✓ 与 G3 设计一致 ✓） |
| **B · 整页读出崩塌** | `600-7/002` `600-11/001` `600-9/002` `600-3/002` | `90.51%` `88.18%` `66.94%` `47.87%` | `7/002` 是 **`287 507/287 507` 全读成同一符号** ✓✓ 属页级判决散架、不是逐格光度错 ✓ |

两个假设已被测量否证（记下防复发 ✓）：quad 边长在失败页/健康页/**pristine 页**同样偏 `1.1%/0.8%` ⇒ 尺度不是判据 ✓；整格相位扫描 `88.17–88.76%` ⇒ 不是相位错位 ✓
**并修正我第 19 轮说过的一句过头话**："符号分布不可能 ⇒ 不需真值就能抓到" ✗ **不成立** ✓ 因为测试载荷是可压缩的重复图案 ⇒ deflate 后**健康页本身就 90% 是同一个符号** ✓ 所以任何从这 4 个坏页倒推的阈值都会误杀 ✓ ⇒ 先量健康总体（`tools/diag-symbol-population.mjs` ✓ 全 32 份语料 × 每页的最大符号占比、不同符号数、**次多符号占比** ✓ 常量崩塌没有尾巴 ✓）再定闸值 ✓ **若总体与坏页分布重叠，诚实结论就是这个判据不成立、得换别的** ✓
**差的量**：32 → 200 seed ✓ **25 倍**
**差的原因（已定界，不是"再跑跑就好"）**：
- ❌ **600 dpi 阈值标定缺失**：物理 σ 固定时 dpi 翻倍 ⇒ 每像素峰值覆盖率约减半 ⇒ Otsu 只剩笔画核 ⇒ 60 px 角标量成 30 px ✓ 而 `otsu/peak` 四页都是 0.56–0.59 **完全不判别** ✓ 可用的信号是**实测着墨率 ÷ 期望着墨率**（失败页 3.34% 对健康页 6.5–9.7% ✓ 期望值可由页内自描述的 `cols×rows×字母表` 算出 ⇒ 尺度无关、不需 manifest、不需 dpi ✓）⇒ 待实现 `pskit calibrate`（M7）
- ❌ **阈值阶梯是调参不是标定**：现为双向 `[1,0.85,0.7,0.55,1.35,1.7]` ✓ 困难页最坏 6 次全图连通域 ≈ 4.9 s/页 ✓ 与 G6 记的 430 ms 差一个量级 ⇒ 正解是先由直方图统计判方向（一次 O(N) ✓）
复现：`python sim/channel.py --in .tmp/sw-scan300-src --out .tmp/nc-scan300-9 --seed 9 --preset scan300 --modifier nocrop` 然后 `node tools/g2-corpus.mjs --root .tmp --match "nc-scan*"`（不足 100% 即 exit 1 ✓）

### ✅ G3 · 缺页/乱序/重复：≤parity 全复原，超出干净拒绝
- **协议层** ✅：`verify --gate G3`（3 档 × 丢 0..parity 全数精确还原；丢 parity+1 → `result=null` 且不写文件；每页喂两次仅计重复；异会话页 → `other-session` 拒绝）
- **图像层** ✅（第 12 轮闭合 ✓ 真删 PNG）：**只留一张校验页**（= 缺 2 页 = 正好等于 parity 预算）也必须重建整份文件 ⇒ 6/6 逐字节 ✓ 复现：`node tools/g2-corpus.mjs .tmp/g3k-000 .tmp/g3k-001 .tmp/g3k-002 .tmp/g3d-0 .tmp/g3d-1 .tmp/g3d-2 --fast`
- **分层记账纪律**（写下来防止两层互相顶包 ✓）：结构测试必须用**干净页**做；拿被信道打坏的语料做同一实验会死在 `echo-bad-magic`/`no-hollow-corner`，看着像结构缺陷其实不是。

### ⬜ G4 · 手机压力档 ≥99% + 失败分类
信道预设 `phone40`/`phone-hard` 已就绪 ✓ 失败分类侧 `core/decode/advice.js` 已就绪（**每个 reason 必须有 advice，机器强制**：`tests/unit/advice-coverage.test.mjs` 扫全 `core/` 并对"扫不到任何 reason"本身设了下限断言 ✓）✓ 但 500 seed × 8 种压力**一次都没跑过** ⇒ 判据完全没有证据。
已知物理边界：平板扫描有纸定位边 ⇒ `crop_frac≈0`；角裁属手机模型 ⇒ 这类失败**归 G4 不归 G2** ✓ PSKT 确实无法矫正"角标空心角被裁掉"的页（朝向就住在那儿）✓ 该拒绝是正确的 ⇒ **不许为过 G2 而削弱它** ✓

### ✅ G5 · 误接受实测 0
复现：`node cli/pskit.mjs verify --gate G5 --trials 10000` → 9200 次被 ECC 纠正回原值、800 次拒绝、**0 误接受**；7 种损坏模式混合。
两项变异检验让"通过"不空转：① 试验必须真的触发帧 CRC/magic 闸；② **摘要是承重的**——伪造页带正确 sessionId + 新鲜合法 CRC + 合法码字，全部结构闸放行，只有摘要拦得住（第一版此测试因"一页就装完全部数据"假通过，已修 ✓）。
本轮追加确认：echo 条带改成 CRC 仲裁候选阈值后，**接受条件未放松**（仍需 magic+version+CRC16 ✓）✓ `G5` 复跑 PASS。

### 🟡 G6 · 性能 + soak
**判决（第 65–66 轮，三部分分开记，权威源就是这一段）**：
- ① **编码 ✅**：1 MB → 168 页 **195 ms**（soak 里实测；第 65 轮量具 197 ms）对判据 **≤5 s** ✓ 另报不判：render+PNG **202 ms/页**（判据只说编码，而渲染 168 张 A4 要几分钟、会把一个 PLAN 没要求的数字变成主角）。
- ② **解码 🟡（300 dpi ✅ / 600 dpi ✗）**：判据 **≤2 s/页**。修 D54 后真实信道 300 dpi 页 **1467 ms**（pristine **952 ms**）⇒ **`docs/USE.md` 指给用户的那一档进预算** ✓；**600 dpi 页 31786 ms ⇒ 超预算 15.9 倍 ✗**（同 dpi 的 `P-C4-600` 与 `P-M1-600` 码区尺寸相同 ⇒ 第 65 轮那个尺寸信号分不开、稳定排序回落字母序；~~再降需要第二个廉价信号~~ ⇒ **第 72 轮划掉：这个推断被实测推翻，见下面 ②bis**）。用户第 63 轮判过"没必要 600 dpi、只要能读到就行"，但**判据本身没分档**，而收窄一条判据的适用范围是产品负责人的决定 ⇒ **我不在台账里替它把这条记绿**。
- ②bis **600 dpi 的地板已实测（第 72 轮）——这条替换上面 ② 里被划掉的那个推断**：量具是 `tools/g6-perf-probe.mjs`，本轮给它加了提示旗标 `--profile / --dpi / --palette / --only-hints`（**不传旗标时行为与历史逐字节相同**；未知 `--flag` 直接 exit 2）。口径与 `tools/soak.mjs` L92-101 一致：`decodePNG` + `bootstrapDecode` 两段相加，`readFileSync` 不计。同一张 600 dpi 真实信道页（`.tmp/sc-scan600-12/page-000.png`，**5190×7240 = 37.6 MP、PNG 40.0 MB**）三个数：**无提示 24743 ms**（`1547 + 23196`，6 次尝试 ⇒ 5 次整页白读 = 18.5 s）；**几何已知 13520 ms**（`--profile P-M1-600 --dpi 600 --only-hints`，3 次尝试、2 次白读 = 6.9 s）；**地板 = 恰好 1 个候选 6192 ms**（再加 `--palette PAPER1`：`1547 + 4645`、`attempts=1`、`ok=true`、第一次就是赢家 `P-M1-600@600/PAPER1`）⇒ **完美排序也只到判据（≤2 s/页）的 3.1 倍**，而**光 PNG 解码 1547 ms 就吃掉预算的 77%**（该页像素数是 300 dpi 同纸页的 4 倍：`.tmp/sc-scan300-1/page-000.png` 2565×3609 = 9.3 MP）。**同页对照（阳性对照，证明提示旗标真的在起作用、也证明 300 dpi 不需要它）**：300 dpi 无提示 **1373 ms**（`363 + 1010`，**第一次尝试就赢**、0 次白读），加同样提示 **1367 ms** ⇒ **提示买不到任何东西**；与已记的 1467 ms 及 soak worst 1783 ms ≤ 2 s ✅ 一致。**结论**：600 dpi 这一档的代价由**像素数**主导、不由候选顺序主导 ⇒ 第二个廉价信号最多把 24.7 s 买到 ~6.2 s，**仍然超预算**。**本轮因此决定不造那个信号**（它无法让 ② 达标；用户第 63 轮已判"没必要 600 dpi、只要能读到就行"，口径「刚好就行」），并把 ② 的出路如实写成**只有产品负责人能走的三条**：① 把 ② 限定在 300 dpi（`docs/USE.md` 指给用户的正是这一档，实测 1373 ms ✅）；② 为 600 dpi 放宽 2 s 预算；③ 投资一种**根本上更便宜的 600 dpi 读法**（先降采样/分块粗定位、再对定位到的区域精读 —— 唯一可能接近 2 s 的技术路线，本轮未做、也不主张它一定可行）。**G6 整条仍 🟡；本轮没有把任何判决改绿，也没有为过关收窄任何判据。**
- ③ **soak ✅（第 68 轮收：三项第一次同源于同一次 30.2 min 跑，`PASS G6` / `ok:true` / exit 0）**：`node tools/soak.mjs --minutes 30`（= `verify --gate G6`）。**修前代码那次**（`.tmp/g6-30.json`，30.7 min、25 cycles、257 页、200 次腐蚀试验）：RSS 634.5 → 650.3 MB = **+2.50%**、**误接受 0 / 摘要不符 0 / 抛错 0 / 拒绝 0**，而解码 8850 ms ❌（属修前代码）⇒ `ok:false` 原样保留。**修后代码那次**（`.tmp/g6-30-postfix.json`，30.2 min、**141 cycles、1401 页解码、1128 次腐蚀试验**）：**编码 194 ms ✅**、**解码 worst 1681 ms / mean 1587 ms ≤ 2 s ✅**、**完整性 ✅（误接受 0 / 摘要不符 0 / 抛错 0 / 页拒绝 0，仲裁再读触发 0 次）**，但 **RSS 报 `+13.98% > 10%` ❌** ⇒ 查清是**仪器分辨率**、不是泄漏（**D59**）：把日志里 29 个每分钟样本按窗口取中位数 ⇒ 0–3 min **581.9** / 3–8 min **510.6** / 8–20 min **512.8** / 20–31 min **515.45** MB，min/max **449.3 / 641.4** ⇒ **第 3–31 分钟几乎全平（+0.95% / 28 min），而且头 3 分钟反而比平台高** ⇒ 既不是泄漏、也不是"热身不足"（本轮先猜的正是热身不足，**被自己的数据推翻** ✗）；真因是"前/后 10% 样本"的窗口在 D54 修好后只覆盖 ~3 min（一个循环从 ~80 s 变成 ~13 s），**短于一次 GC 锯齿**，而 ±100 MB 的锯齿叠在 ~515 MB 基线上（±20%）远大于 10% 的判据余量 ⇒ 修法：**判据一个字不动**，窗口改**四分位**（每侧 ≈7.5 min），并**同时算/打印/写进 JSON 旧窗口的统计量**（`narrowGrowthPct`，标注 `printed, not judged`）+ **Q1..Q4 中位数**与 **peak/floor**（泄漏 = 这四个数单调爬升，GC 噪声不是）⇒ **仪器修好后重跑，第 68 轮收判决**（`.tmp/g6-30-r67.json`，30.2 min、**141 cycles / 1401 页解码 / 1128 次腐蚀试验**）：**编码 203 ms ✅**、**解码 worst 1663 ms / mean 1569 ms ≤ 2 s ✅**、**RSS 507.3 → 516.3 MB = `+1.78%` ≤ 10% ✅**（peak 640.7 / floor 436.1 MB、141 个样本）、**完整性 ✅（误接受 0 / 摘要不符 0 / 抛错 0 / 页拒绝 0）**、30.2 min ≥ 30 ✅ ⇒ **`PASS G6`、`ok:true`、`SOAK_R67_EXIT=0`** ⇒ **G6 三项第一次同源于同一次跑**，第 66 轮记的"三项不同源"缺口至此闭合。**旧统计量不稳的旁证**：同一次跑里旧窗口（10% 样本）算出 `narrowGrowthPct = +2.96%`，而上一轮**同一份代码、同一把旧尺子**算出 **+13.98%** ⇒ 差 11 个百分点，正是 D59 说的相位抽样。**如实两点**：① **Q1..Q4 中位数 507.3 / 511.7 / 515.8 / 516.5 MB 是单调爬升的**（本工具自己的注释就写着"泄漏长这样"）⇒ **不能只凭"过了判据"就说"无泄漏"**；增量在收窄（**+4.4 / +4.1 / +0.7 MB**）⇒ 形状像收敛到工作集、不像线性泄漏，但**形状判断不是证明** ⇒ 已起 **60 min 跑**（PLAN 允许 30–60 min，`.tmp/g6-60-r68.json`）把"收敛"与"慢泄漏"分开，**结果出来再写**。② **G6 整条仍 🟡**：600 dpi 解码 **31786 ms/页**（超预算 15.9 倍）⇒ **不因用户说 600 dpi 非必要就记绿**（收窄判据范围是产品负责人的决定，不是我在门限表里能默默做的）。
- ③bis **60 min 复跑判决（第 70 轮收，`.tmp/g6-60-r68.json`，`ok:true`、`SOAK60_EXIT=0`）——这条兑现上面 ③ 结尾那句"结果出来再写"**：那次 60 min 跑是第 68 轮为**把"收敛到工作集"与"慢泄漏"分开**起的（③ 里 Q1..Q4 单调爬升、增量收窄，但"形状判断不是证明"）。**结果**：60.02 min、**281 cycles / 2782 页解码 / 2248 次腐蚀试验**；**编码 199.4 ms ≤ 5 s ✅**；**解码 worst 1783.0 ms / mean 1599.9 ms ≤ 2 s ✅**（源仍是三份真实 300 dpi 信道语料 `sc-scan300-1/-10/-100`）；**完整性 ✅：误接受 0 / 摘要不符 0 / 抛错 0 / 页拒绝 0 / 仲裁再读触发 0**；**RSS 判据 `growthPct = +3.16% ≤ 10% ✅`**（基线 532.31 MB = Q1 中位数 → 终值 549.14 MB；peak 681.37 / floor 462.87 MB ⇒ ±100 MB 锯齿再次确认，也再次说明窗口为什么必须是四分位）；旧窗口统计量同时算/打印/写盘但**不判**：`narrowGrowthPct = +3.93%`。**吞吐没退化**：`cycleMsEarly 12867.5 ms` vs `cycleMsLate 12581.5 ms` ⇒ 后期反而**快 2.2%**。**但"无泄漏"仍然不主张，理由如下（如实）**：Q1..Q4 中位数 **532.31 / 538.63 / 542.84 / 549.28 MB**，增量 **+6.32 / +4.21 / +6.44 MB** ⇒ 60 min 里**没有收敛**；而 30 min 那次的增量是 **+4.4 / +4.1 / +0.7**（看着像收敛）⇒ **两次形状不一致 ⇒ "像收敛"这个读法本身不可靠**。按 ~+5 MB/15 min 线性外推，约 **2.5–3 h** 会撞上 10% 预算 ⇒ **判据在它规定的时长（PLAN 30–60 min）内通过，而"长时间会话不长"没有被证明**。**两个平凡解释已排除（读代码，不是猜）**：① 不是 soak 自己攒记录——留存是千字节级（`firstErrors` 上限 5 条、281 个 cycle 时间、281 个 RSS 样本、~2782 个 per-page 计时）；② 不是语料缓存边跑边填——`readCorpus` 在 **L124 一次性读入全部 PNG**、`corpora` 在 **L140 循环开始前**就建好，页位图是每次用时 `decodePNG`（L93）现解、用完即弃 ⇒ 残差只剩两种可能：**V8 在 ~350 MB/cycle 的分配速率下高水位漂移**，或 **core 里真有 ~6 KB/页 的留存**（17 MB / 2782 页）⇒ **60 min 分不开这两者，我不主张任何一个**。**决定不再拉长到 3 h**：PLAN 的判据是 30 min 那一档、30 与 60 min 都过，为一个判据已经回答的问题再花 3 小时机器时间属于过度设计（用户第 63 轮口径"刚好就行"）⇒ 记为**观察**并留下线索：若将来有人报告多小时会话内存上涨，从这里查。**G6 整条仍 🟡**：② 的 600 dpi 解码 **31786 ms/页**（超预算 15.9 倍）未变，本条只涉及 ③ 的 soak。
- **另一条必须写在这里的坦白（D60）**：`tools/soak.mjs` 原本判 `minutesOk = elapsedMin >= Math.min(minutes, 30) * 0.98` ⇒ **文档可达的命令** `verify --gate G6 --minutes 1` 能用 **72 秒**打印 `PASS G6`（还打出自相矛盾的一行 `1.2 min >= 30 min`）✗ 已修成常量下限 `MINUTES_REQUIRED = 30`（**`--minutes` 只能加长、不能缩短判据**），1 分钟试跑复验为 `FAIL G6: … 1.2 min < 30 min` + `note: a short run cannot pass G6, only fail it`、`SHORTSOAK_EXIT=1` ✓ **本页所有 G6 数字都来自 30 min 跑，没有一条来自短跑** ⇒ 那条假绿没有被引用过，但它可达，所以按缺陷记、不按"理论问题"放过。

**历史（划线保留，未删）**：~~已测：SHA-256 269 MB/s、deflate 1.1 MB/35 ms、600 dpi 整页渲染 192 ms + 读回 430 ms、PNG 617 ms/TIFF 181 ms、单应矫正 362 ms、50 次连跑 heap Δ 0.0 MiB。未做：30–60 min soak（需 `--gate G6` 长跑，从未执行）；且上面 430 ms 是理想路径，真实信道困难页最坏 4.9 s/页 ⇒ 性能预算含未知量~~ ⇒ 第 65–66 轮把这两个"未知量"量掉了：真实信道页不是 4.9 s 而是 **8713 ms（修前）/ 1467 ms（修后）**，而"理想路径 430 ms"与真实路径差一个量级的原因是**候选搜索在错的几何上白跑整页读**（D54）✓ 另：`50 次连跑 heap Δ 0.0 MiB` 这类只看 JS 堆的证据**不足以证明不漏**——第 66 轮的 D56 正是在 `heapUsed` 恒为 5 MB 的同时留住 1.3 GB 外部内存（`arrayBuffers`）⇒ soak 现在按 **RSS** 判，并打印 arrayBuffers。

**这条判据不覆盖**：`sim/channel.py` 的光学信道模型（Python）、真墨真纸、真手机摄像头、浏览器 ⇒ 那些是 G2/G4/G9 的证据；`tools/soak.mjs` 自己每次也打印 `covers:` 与 `does NOT cover:` 两行，不许被引用成覆盖了它们 ✗

### ~~✅ G7 · 色道塌陷下单色兜底 100%~~ **RETIRED（第 110 轮）**
> **~~RETIRED~~ · 第 110 轮 · 产品负责人第 109 轮取消 3D 板材线**：本门限验的是3D 板材（PL-D2/D3）在色道塌陷时的单色兜底，属已取消的 3D 线 ⇒ **不再作绿/红依据**，`verify --gate all` 也不再把 it 计入总数。相应档位已冻结（`core/profiles.js` 的 `retired: true`：仍可解码、不再在 CLI/网页提供）。历史判决原文**保留不改写**（撤回＝划线 + 标注）。依据：`docs/PLAN-V5.md` §3。

`verify --gate G7`：单色渲染图由数据自身判为 `colourAlive=false`（**不用边信道** ✓）⇒ 整条色道作擦除、由形状道 + 奇偶复原；`monoSafe:'off'` 档 → **拒绝而非猜** ✓

### ~~🟡 G8 · 3MF/STL 被独立解析~~ **RETIRED（第 110 轮）**
> **~~RETIRED~~ · 第 110 轮 · 产品负责人第 109 轮取消 3D 板材线**：本门限验的是3D 码牌产物（3MF/STL）能否被独立解析，属已取消的 3D 线 ⇒ **不再作绿/红依据**，`verify --gate all` 也不再把 it 计入总数。相应档位已冻结（`core/profiles.js` 的 `retired: true`：仍可解码、不再在 CLI/网页提供）。历史判决原文**保留不改写**（撤回＝划线 + 标注）。依据：`docs/PLAN-V5.md` §3。

- **STL ✅**：`python ref/verify_stl.py --selftest .tmp/plate-page0.stl` → 真实 13.1 MB / 262 080 三角形板上 **13/13 PASS**（`84+50n` 逐字节 ✓ 头部自报数与二进制数一致 ✓ units=mm ✓ 零退化 ✓ 每条法向与顶点右手序 cos>0.99 ✓ 包围盒有限且 mm 量级 ✓ **有向体积 +373 mm³** ⇒ 壳体序一致 ✓）+ `--selftest` 造坏副本**必须失败** ⇒ 检查是承重的 ✓
  附带一条反向收获：**无向边 393 120 条中 100% 恰被两个三角形共用 ⇒ 网格其实是水密的**，而 `core/mesh/stl.js` 一路保守报 `watertightHint:false` ⇒ 独立侧测得比自报更好 ✓ "保守误报"不等于真相 ✓
- **3MF 🟡**：`core/mesh/threeMF.js` 已写出（`encode3MF`/`buildZip`/`readZip`/`parseModelXml`/`manifoldReport`/固定 DOS 时间戳 ✓）但**尚未被第三方解析验证过**，且它是在提交 `3502db8` 之后仍在改的中途状态 ⇒ 不判决 ✓
- ✅ **G8 判据第 3 条（投影对拍）第 14 轮真过了**（我亲自重跑，不接受转述 ✓）：`send --format stl,3mf` 打印 `projection G8 §6.3: max 0.76% mean 0.69% off the raster mask, 0/1764 cells >= 8%` ✓ Python 侧从**文件里的**朝上三角形独立重量 ⇒ `PASS g8-3/projection-area-error max 1.208% mean 1.189% over 196 cells` ✓ 第三个不依赖光栅的纯解析式参考也给 `max 1.138%` ✓✓ **关键是两条算路互相独立**：期望掩码来自光栅侧（`buildCoverageTiles` + `layout.originPx` + 像素量化格宽 ✓ `shape=0` 仍算外环 ✓），不再是"拿网格跟自己比" ⇒ 我上一版那个 `mismatch=0/1764` 的空转已修 ✓ 且有 3 条承重反例（单格半径 ×1.3 → 红并命中该格 ✓ 删一格 → 被"每格都得有料"抓住 ✓ Python 侧改 facts 档 → 红 ✓）
- ✅ **确定性实测**：同一输入连跑两次 ⇒ **STL 5 个文件 + 3MF 5 个文件逐字节相同** ✓（DOS 时间戳钉在 1980-01-01 ✓ 部件顺序固定 ✓）
- ✅ **纸面档正确拒绝**（我实跑）：`--profile P-M1-300 --format stl` → exit 1 ✓ 消息引用 `MESH-CONTRACT §3` ✓ **输出目录 0 个文件** ✓
- 🟡 **第 38 轮：XSD 子集校验器已写、进程内可跑**（`node cli/pskit.mjs verify --gate G8` ✓ 实现 `tools/check-3mf.mjs` ✓ 22 条单测含 18 个篡改用例）⇒ **它对我们每一份 3MF 都 PASS**（本轮造的 `PL-D2@0.4` 3 objects/377964 triangles ✓ M5 历史产物 `.tmp/m5det1/page-000.3mf` 426444 tri ✓ `.tmp/m5verify/page-000.3mf` 45132 tri ✓）而它的价值以另一种方式当场兑现：**第一版误报** `object/@name` 违规 ⇒ 因为我引的是网上旧变体，而权威 `ref/3mf-core-1.4.0.xsd` **L97** 明确允许 ⇒ 记 **D40 撤回 + D41 OPEN** ✓ 另：`selfCheck3MF` 与它**互不覆盖**——前者问"写出的是不是我想写的"、后者问"合规读取器会不会收下"⇒ 两条独立算路都要 ✓ 仍**不是** schema conformance：本机没有 XSD 引擎 ⇒ 规则**逐条抄自本仓库 vendored 的 `ref/3mf-core-1.4.0.xsd`**（行号写进代码注释与 `tests/unit/threeMF-subset.test.mjs` ✓）并按 `xs:anyAttribute namespace="##other"` 放行**带前缀的扩展属性**、按 `xs:any namespace="##other"` 放行**扩展元素**（无限定的未知属性/元素仍判违规 ✓ 那才是手改或错模板会产生的形态）✓ G8 的进程内半**绿 ⇒ 已并入 `--gate all`**（本轮曾短暂按"红就不并入"处理，理由是并入会让例行基线永远红、掩盖其余门限的回归 ✓ 绿了就进例行 ✓）✓ 且 `cmdVerify` 每次**打印"本次未评估哪些门限"** ⇒ `ALL GATES PASS` 不能被引用成覆盖了 G4/G6/G9/G10 ✓ 另**更正本页一句错话**：原先写"本机无 XSD、**无网络**"⇒ 无网络是错的，**Node 的 `fetch` 能出网**（本轮已用它取到 schema 原文 ✓；不行的是 PowerShell 的 `Invoke-WebRequest`）⇒ 目前"schema 正确"的实际含义是"**被第三方 stdlib 解析器读通且结构自洽**" ✓ 不等于过 schema ✓ 另：`docs/MESH-CONTRACT.md` §5 与实现有两处文本冲突待我修订（"单个 `<object>`"在"每条无向边恰两次"下做不到 ⇒ 实现发多 object ✓；契约里写的 `<unit millimeter=…>`/`zUp` 不是合法 3MF Core 1.4 属性 ⇒ 实现写 `unit="millimeter"` ✓ **是实现比我的契约更对** ✓）；也没有进程内 `gateG8`（独立那一半天生需要 Python ✓ 沙箱禁 spawn ⇒ 与 G2 同一模式：门限跑 JS 侧 + 打印必须手跑的 python 命令 ✓）

### 🟡 G9 · Web 扫描端（GitHub Pages）
**判据**：`web/` 可部署到 GitHub Pages ✓ 零外部 origin ✓ 离线可用 ✓ `?selftest=1` 全绿 ✓
**已有证据（产物级 ✓ 第 22–25 轮 ✓ 第 31 轮补至 10 条 ✓ 第 32 轮补至 11 条）**：`node tools/build-web.mjs && node tools/check-dist.mjs --pages .tmp/g2src` ⇒ **11 条断言全通过** ✓ 具体覆盖：构建自报文件数与磁盘**双向相等** ✓ 页面每个 `src`/`href` 都指向已构建产物 ✓ **"内联脚本必须能被解析"（把两个单文件页里的 `<script>` 抠出来 `new Function` 只解析不执行 ✓ 语法坏/bundle 被截断都会在这里死 ✓ 而"浏览器是否强制 `file://` 的 `<meta>` CSP"本机验不了 ⇒ 只验验得了的那一半 ✓）** ✓ **"该在的引用也确实在"（站点版 `index.html` 必须含指向 `send.html` 的导航 ✓ 单文件版必须没有 ✓ 与构建的剥离规则配对 ✓ 第 31 轮补 ✓ 并做过负向测试：删掉产物里的链接 ⇒ 该断言红 ✓）** ✓ 构建出的 JS 里每个 `getElementById` 都能在某页面找到 id ✓ precache 清单哈希与磁盘逐字节对上且无重复 url ✓ dist 内 selftest **13/13**（含真语料）✓ 打包后的 bundle 对 `.tmp/g2src` **无提示自识别解码**、摘要与 `manifest.sourceSha256` 相等 ✓ 另有 Node 侧冒烟三条路：接收端 bundle ✓ 发送端 `buildArtifacts`（纸 3 页 / 板 8 页、8 份浮雕模型全过 `projectionReport`+`stlSelfCheck`+`selfCheck3MF`、往返摘要相等 ✓ `tools/smoke-sender.mjs` ✓）✓ 连拍外壳（真 3 页逐页解回、去重、两道质量门控、页数冲突拒绝、裁半页拒收 ✓ `tools/smoke-capture.mjs` **12/12** ✓）
**仍缺（所以是 🟡 不是 ✅）**：真浏览器与真手机各一次人工执行 ✓ 本机没有浏览器也无摄像头 ✓ `?selftest=1` 与 `file://` 路径只由 Node 侧等价物证明 ✓ 记为 **D18**；GitHub Pages 的仓库推送与开关是**用户的动作**（`docs/PLAN.md` L172 ✓ 本轮仍未推送 ✓ `gh` 未安装 ✓ 需要仓库 URL 与交互凭据 ✓）
**第 249–250 轮新增的真浏览器证据（判决不变，仍是 🟡）**：用户装了 DSH 浏览器插件后，第一次有真浏览器可用（`HeadlessChrome/152`）。① `?selftest=1` **以前在浏览器里根本不会跑**（内联 loader 被该页自己的 CSP `script-src 'self'` 拦掉 ⇒ **D89**，已修并新增第 14 条产物断言）⇒ 修后新鲜加载**自检自己跑出 `13 passed / 0 failed / 0 skipped -> SELFTEST GREEN`**；② **接收页整条链**：灌入三张 300 dpi 页图（204,800 B 载荷）点「开始还原」⇒ 页面自报 **`已逐字节还原：204,800 字节 · SHA-256 32ec480521da27d2… · 3 页被接受`**，日志显示逐候选搜索后**第 10 次**命中 `P-M1-300@300/INK2`（与 CLI `--profile auto` 的 10 个候选互相印证）；③ **发送页整条链**：3,000 B 输入 ⇒ `编好 3 页 · 1072 ms · PAPER1 · 300 dpi`、明文 SHA-256、纸面档**正确禁用** STL/3MF；④ `sw.js` 在 **localhost** 注册成功（页面自报「离线缓存已就绪」）；**第 251 轮补**：在页面里用 `caches.match()` 取回缓存实体并与磁盘对拍 —— `pskt-bundle.js` / `index.html` / `capture.js` **两侧 SHA-256 完全相同**（399,862 / 9,529 / 18,939 B），缓存共 **71 条**（与 `check-dist` 的 70 条预缓存 + 页面本身相符），`sw.js` **NOT CACHED**（SW 从不预缓存自己，阴性对照）⇒ **「离线访客实际拿到的字节」第一次被直接验证**（此前只由 Node 侧重算清单间接推得）；**未真的断网**（工具没有网络开关）⇒「断网后仍能还原」仍归用户。**第 255 轮补：加密传输的完整口令流程也在真浏览器里验过**（同一批 3 页、6,000 B、口令 `pskt-r73-pass`）：**不填口令** ⇒ 具名拒绝且把「缺口令」与「缺页」分开 ✓；**口令正确** ⇒ `已逐字节还原：6,000 字节 · SHA-256 4491cd04246c48c8…`，与磁盘载荷的 sha256 一字不差 ✓；**口令错误** ⇒ 拒绝（`transform-failed: … bad magic`）且**一个字节都不写**、上次成功的横幅实测未留在屏幕上（`#out` 为 `hidden`/`display:none`）✓。**第 256 轮补：单文件版的**两个产物**也验了**（这是另一套内联 bundle，此前从未在浏览器里跑过）。用 http 打开**同一个单文件产物**（`file://` 页面 `fetch` 不到暂存图；而 `file://` 协议只额外影响 SW 与摄像头，那两条第 250/254 轮已实测）：`pskt-file.html` 灌入三张 300 dpi 页图 ⇒ **`已逐字节还原：204,800 字节 · SHA-256 32ec480521da27d2… · 3 页被接受`**；`pskt-send-file.html` 编 3,000 B ⇒ `编好 3 页 · 1142 ms · PAPER1 · 300 dpi`，明文 SHA-256 与**服务版发送页**编同一份数据时**一字不差**（两套 bundle 互证）✓。**仍未覆盖**：可见浏览器（无真人点击）、**Edge / Safari**（本机装了 Edge，但插件的引擎设置不在我可改的一侧）、`file://` **协议本身**（双击打开；只影响 SW/摄像头，不影响解码器）、手机 ⇒ 所以这条门限**不因这些证据改判**。
**第 254–256 轮（真浏览器补四格，判决不变）**：加密三态（无口令 ⇒ 具名拒绝；对口令 ⇒ 6,000 B 逐字节还原 `4491cd04246c48c8…`；**错口令 ⇒ 拒绝且不写盘**）· 命名规范化（`../../evil.png` ⇒ 摘要名）· **单文件版两个产物**（`pskt-file.html` / `pskt-send-file.html`）在 http 下各跑通一次、其内联 bundle 与磁盘产物互证 · 摄像头被拒文案 ✓
**第 264 轮（`file://` 这一格补上了，判决不变仍是 🟡）**：以 **`file://`** 打开**发布产物** `web/dist/pskt-file.html`，喂入第 0/2 数据页（243,190 B）⇒ **`已逐字节还原：1,500 字节 · SHA-256 cc7605e78942548a… · 1 页被接受`**，日志 `P-M1-300@300dpi INK2 第 0/2 页（数据）· 第 10 次命中 · 6535ms`、`完成：1500 字节，摘要核对通过` ✓。**摘要由另一份实现独立复核**：`node:crypto` ⇒ `cc7605e78942548a557cb1dd20fbd926a078c29cab60dddf890574ddff5082d0` ✓。**边界写明**：插件不能驱动操作系统的文件选择框，且 `file://` 页面 fetch 网络被 Chrome 拒（实测 `TypeError: Failed to fetch` —— 即使那个一次性服务器带 `Access-Control-Allow-Origin: *`、直接访问返回 200）⇒ 夹具由**页面副本上追加的内联脚本**塞入（该页 CSP 是 `script-src 'self' 'unsafe-inline'` ⇒ 允许；**发布产物一字未改**）⇒ 这一格证明的是「`file://` 下的解码路径 + 单文件 bundle」，**不证明「人点选择框」那一下**（那是浏览器行为）✓
**复现**：`node tools/build-web.mjs && node tools/check-dist.mjs --pages .tmp/g2src && node tools/smoke-sender.mjs && node tools/smoke-capture.mjs`（真浏览器那四条另见 `docs/STATUS.md` 第 249–250 轮块）

### ~~🟡 G10 · 喷嘴 × 参数矩阵~~ **RETIRED（第 110 轮）**
> **~~RETIRED~~ · 第 110 轮 · 产品负责人第 109 轮取消 3D 板材线**：本门限验的是3D 打印机的喷嘴 × 参数矩阵，属已取消的 3D 线 ⇒ **不再作绿/红依据**，`verify --gate all` 也不再把 it 计入总数。相应档位已冻结（`core/profiles.js` 的 `retired: true`：仍可解码、不再在 CLI/网页提供）。历史判决原文**保留不改写**（撤回＝划线 + 标注）。依据：`docs/PLAN-V5.md` §3。

**已有真材料**：`PL-G` 在 0.6 mm（3.6 mm 格 = 6 EW）与 0.8 mm（4.5 EW）喷嘴下被 `glyphGeometry` **拒绝**（"2 级形状字母表需 ≥8 EW/格" ✓）⇒ 这是"网格与光栅共用一个形状真值源"的直接后果 ✓ 属正确行为 ✓ 也是矩阵里第一个真实边界点 ✓
**未做**：0.2/0.4/0.6/0.8 × 各剖面的完整矩阵实跑 ✓ 未标定 MTF 前不判决 ✓

## 会改变判决性质的开放缺陷（第 22 轮重排为五条 ✓ 原"三条"已不足以描述现状）

| # | 缺陷 | 为什么重要 |
|---|---|---|
| 1 | 600 dpi 标定缺失（`pskit calibrate` **第 61 轮落地了"测量"半；"从实测定阈"半仍未做** ⇒ 部分兑现、仍 OPEN） | 它决定 G2 是"差样本量"还是"差能力" ✓ 现在无法排除后者 ✗ 第 15 轮已证"声明墨面积是可用真值 ✓ 但用二值化 cut 反推该面积不是可用执行器" ⇒ 修法不能是调参 ✓ 第 21 轮量到 `inkRatio` 其实**是渗墨量度**（pristine `1.065` vs 600 dpi 信道页 `3.6–21.7` ✓）⇒ 标定要处理的是"随 dpi 变化的模糊宽度" ✓ **第 61 轮更新**：`pskit calibrate` 已实现**测量半**（每页 readout 结果 + 页内 RS 预算占用 `(2·errors + erasures)/(blocks·nsym)` + 阈值无关的着墨比；**不是门限、不派生阈值、不改解码判决** ⇒ 不必重判 G5/G2）⇒ 用它量出的答案是：**既不是"差样本量"、也不是"页内 ECC 余量不足"** —— 600 dpi 的坏页是**逐格电平判读整体塌陷到单一电平**（`sc-scan600-11` page-001：内部 283155 格全读成电平 1，原始渲染 `0=256118 1=27037`；混淆只有 `0->1`；空间平坦；48 个 ±3 格平移无效），而能读出的页只花 **0.0–0.2%** 预算（300 dpi 侧 9 页全 **0.0%**）⇒ **是解码器缺陷（D51）**；修法是按页重测 ρ 表再定阈（构件已在仓内 ⇒ 见 D51），且必须回 G5 重验误接受、判据须是跨 seed 的稳定谷底 ✗ **定阈那一半仍未做** ⇒ 本项**部分兑现、仍 OPEN** ✓ |
| 2 | `core/decode/page.js` 让 448 bit、**零纠错**的回显条带一票否决整页 | 有 manifest 时应允许走数据区、由页间/页内 RS + 末端摘要定夺 ✓ 挂账多轮未做 |
| 3 | **已闭合（第 36 轮 ✓）** `no-hollow-corner` 语义过载（"凑不齐四角"与"角标被糊住"共用一个 reason）· **第 33 轮：拆它的显然做法被测量否证 ✓ 并且现在有可复现的一行铁证——`node --test tests/unit/fiducial-corner-damage.test.mjs` 打印 `第四角被遮=no-hollow-corner(hollow=0,cand=158)` 与 `角标在而孔不可读=no-hollow-corner(hollow=0,cand=159)` ✓ 两种含义相反的失败同一个字符串 ✓ 两者都被拒收（无误接受 ✓）⇒ 记为 D36 ✓ sound 路线是把第四角预测在剖面自己的几何上 ✓ 而非任意三元组 ✓** | 它**连续误导了两次根因分析** ✓ 拆开后分类才可用于 G4 的"失败分类"判据 ✓ |
| 4 | **G2 的 B 类失败（整页读出崩塌）根因未定** ✓ 第 19 轮确证存在（`287507/287507` 单符号 ✓）· 第 20 轮"符号分布闸"被总体数据否证（健康页 `maxFrac p90=0.9057` ✓ 只差 7 个百分点 ⇒ 必误杀 ✓ 未实现 ✓）· 第 21 轮给的"渗墨推过中点 ⇒ 整页翻 1" **第 22 轮由我自己撤回**（口径错两处 + 一个能逐字节复原的健康页在该探针里同样偏斜 ⇒ 因果未建立 ✓） | 这是 G2 从 93.8% 走到 100% 的**唯一未知项** ✓ 若最终发现是形状电平判定对渗墨不鲁棒 ⇒ 属**能力缺陷**（须改 `readPageIdeal` ✓ 而任何"让接受更容易"的改法必须回 G5 重验误接受 ✓）；若发现是 `P-M1-600` 几何本身在 600 dpi 不成立 ⇒ 属**剖面表缺陷**（须改容量/字母表 ✓ 影响 G10）✓ 现在两种都排除不了 ✗ |
| 5 | 3MF 的 **XSD 校验器根本没写**（`ref/3mf-core-1.4.0.xsd` 已 vendored ✓ 但 `ref/verify_model.py` 只有 `--dir/--stl/--3mf/--facts/--selftest` ⇒ **没有 `--xsd` 开关** ✓） | "schema 正确"这一判据目前**零证据** ✓ 只能主张"被第三方 stdlib 读通且结构自洽" ✓ 第 22 轮我一度在本页写下 `verify_model.py --xsd …` 这条**不存在的命令**——正是本页开头那句"任何没有对应命令的数字都不该被相信"要防的事 ✓ 已改回真实命令 ✓ |

## 一句话结论

**"能印能扫能逐字节复原"在纸面 300/600 dpi 两档平板扫描模型上已被证明 `30/32 = 93.8%` ✓ 判据是工具自己在进程内重算 SHA-256、不靠措辞 ✓ 但"通过 G0–G10 全部门限"没有被主张、在 22 轮预算内也确实没有做到** ✓ 拆开说：`G4`（手机压力档 500×8）**仍零证据** ✓ `G9`（`web/` Pages 扫描端）**不再是零证据、但也没闭合**：产物级 9 条判据全绿（含 bundle 无提示自识别解码并摘要对上 ✓ dist selftest 13/13 ✓ 发送端与连拍的 Node 侧冒烟 ✓）✓ 缺的是真浏览器/真手机各一次（D18 ⇒ 维持 🟡 ✓ 上一页曾写"G9 未开始/完全零证据"**是低估 ✓ 本轮更正 ✓ 台账虚高与虚低同样是错 ✓） `G2` 差 **25 倍样本量**（现 32 seed ✓ 判据要 200 ✓）并含一条**未解的 B 类根因**与一条未实现的标定 ✓ `G6` 的 soak 未跑 ✓ `G8` 的 3MF **未过 XSD**（校验器未写 ✓ 而投影对拍已 PASS ✓ 三条独立算路互证 ✓）✓ `G10` 只测到矩阵里一个真实边界点 ✓ 全程**没有为了让任何一条变绿而降低判据** ✓
