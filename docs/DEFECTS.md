# 缺陷台账（按用户指示：错误先记录、后修复；本文件只增不删，修好才划掉）

> 规则：每条必须能被一条命令复现。**不写"应该没问题"**。性能问题一律不修（用户明示先不管）。
> 状态标记：`OPEN` 待修 · `CLOSED` 已修并复验 · `NOTABUG` 记录用，非缺陷。

### 第 254 轮新增（D91 · CLOSED · 真浏览器量渲染尺寸查出）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| **D91** | **`hidden` 属性在「有 class 设定 display」的元素上完全无效 ⇒ 网页上两个长期可见的错误**：① **接收页的结果区 `section.card.result#out` 从一开始就显示着**（标记里本来写着 `hidden`，作者意图是解码成功后才出现）；② **发送页的「喷嘴」`label.field#s3d`、「打印幅面」`#splate` 与两个 3D 按钮 `#dl3mf`/`#dlstl`（3D 线第 109 轮已取消、永远只能禁用）一直显示在屏幕上**。根因：`hidden` 只带来 UA 的 `display: none`，**任何** class 里的 `display`（`label.field{display:flex}`、`.btn{display:inline-flex}`）都盖过它；项目里本来有 `.hide{display:none!important}` 的约定，但 JS 用的是**属性** ⇒ 静默失效 | 修前在发送页量 `getBoundingClientRect()`：`s3d`/`splate`/`dl3mf`/`dlstl` **全部有非零尺寸**（而 `.hidden` 读回是 `true`）；修后在**真浏览器**量同一个量：`s3d:false · splatebox:false · dl3mf:false · dlstl:false`，而 `dlpng:true · dlpdf:true · doprint:true`（阳性对照：没把该显示的也藏掉） | **CLOSED**（修法：`web/app.css` 加一条 `[hidden] { display: none !important; }`；同时把发送页的 3D 家具按退役现实隐藏、lede 不再承诺 `.3mf/.stl`、「改用粗喷嘴剖面」的提示改成纸面档措辞）。**复验**：接收页解码前 `#out`/`#outname` **隐藏**、点「开始还原」后**出现**且打出 `已逐字节还原：204,800 字节 · SHA-256 32ec480521da27d2… · 3 页被接受`（真浏览器，第 254 轮） |
### 第 249 轮新增（D90 · CLOSED · 新检查第一次运行就抓到）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| **D90** | **`core/decode/calibrate.js` 从第 154 轮起语法就是坏的 —— 第 154 轮删死代码时把 `calibrateInkCut()` 的闭合 `*/`、函数签名与函数体一起删掉了，只留下悬空的文档注释**（`2cbee43` 的 diff 显示删了 48 行；文件里 `function maxOf(a)` 直接接在一段没有 `*/` 的注释后面）。**7 轮无人发现**：没有任何测试 import 这个文件（`pskit calibrate` 才动态加载它），构建只拼文本不解析，`check-dist` 也不解析服务版脚本 ⇒ **`pskit calibrate` 这条「只量不改」的读数路径从第 154 轮起一直是崩的**（D51 的诊断量具就在这条路上） | `node --check core/decode/calibrate.js`（修前 **exit 1**、报 `SyntaxError: Invalid or unexpected token`）；`node tools/check-dist.mjs`（修前第 15 条断言点名 `core\decode\calibrate.js: does not parse`）| **CLOSED**（从 `2cbee43^` 取回 `calibrateInkCut()` 原文并原样恢复 —— 43 行；**修后复验**：`node --check` ⇒ **exit 0**、`check-dist` ⇒ **61 file(s) parsed / all 15 assertions pass**、`node cli/pskit.mjs calibrate .tmp/usability/pages-scan` ⇒ **exit 0**，逐页打出 ECC 与油墨比 0.90x/1.19x/1.25x）|
### 第 249 轮新增（D89 · CLOSED · 真浏览器查出）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| **D89** | **服务版接收页的 `?selftest=1` 入口是一个内联 `<script type="module">`，而该页自己的 CSP 是 `script-src 'self'`（不含 `'unsafe-inline'`）⇒ 浏览器静默拦掉它，自检在真浏览器里从来没跑起来过**（`tools/build-web.mjs` 里早有同一条护栏，但**只覆盖它自己重写的单文件版**；服务版从没被检查过，`check-dist` 也只查「CSP 存在」）。**实测（headless Chrome 152）**：新鲜加载 `?selftest=1` ⇒ `#selftest-wrap` 保持隐藏、`performance` 里根本没有 `selftest-page.js`；在页面里注入一个内联 module 脚本 ⇒ `window.__inlineRan` 保持 `undefined`（被拦）；手动 `import('./selftest-page.js')` ⇒ 立刻 `13 passed / 0 failed / 0 skipped -> SELFTEST GREEN` | 修前 `node tools/check-dist.mjs` ⇒ 第 14 条断言变红并点名 `index.html: an inline <script> (95 chars) is not permitted by its own CSP`；**修后复验**：`node tools/check-dist.mjs` ⇒ **`G9 CHECK: all 14 assertions pass`**，且真浏览器新鲜加载 `?selftest=1` ⇒ 依次加载 `selftest-loader.js → selftest-page.js → selftest.js` 并**自己跑出 `SELFTEST GREEN`**（不带参数则三者一个都不加载，阴性对照 ✓） | **CLOSED**（修法：loader 落成 `web/selftest-loader.js` + `<script type="module" src=…>`，保持 CSP 严格；`build-web.mjs` 补 `writeDist`；新增 `check-dist` 第 14 条断言防复发） |

### 第 244 轮新增（D88 · OPEN · 门限偶发且未定位）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| **D88** | **`verify --gate all` 在一次「与其它重活并发」的运行里打印了 `GATE FAILURE`，但隔离复跑不通**（复跑：`VERIFY_EXIT=0`、`ALL GATES PASS -- 4/5 evaluated, 1 skipped`）。触发时的并发：同一程序里同时跑着 `build-web`（它会 `rmSync` 旧 `web/dist` 再 `renameSync` 新目录 ⇒ **存在一个 `web/dist` 不存在的窗口**）、`check-dist`、单测与 `usability.ps1`。**未能定位是哪一条腿失败 —— 这是操作错误不是产品证据**：我把 job 输出**先按关键字过滤读掉**，缓冲区随之消费且没有落盘。**缓解**：门限输出一律 `> .tmp/verify.log 2>&1` 落盘后再看；若再现，日志会点名那条腿 | `node cli/pskit.mjs verify --gate all > .tmp/verify.log 2>&1; $LASTEXITCODE`（**先落盘再看**） | **OPEN**（未复现；疑似并发/时序，非确定性） |
### 第 190 轮新增（D87 · OPEN · 测试偶发）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| ~~D87~~ | **【第 260 轮根因已定 + 已修】** **`encrypt/decrypt round-trip on random data of assorted lengths` 偶发失败的真因是「测试断言本身的统计错误」，不是产品缺陷**：该用例断言「**密文必须不等于明文**」，而**长度为 1 时**密文那一个字节在均匀 keystream 下有 **1/256** 概率恰好等于明文 ⇒ 短样本上这条断言**按构造就会偶发**。**阳性对照（`.tmp/d87-probe.mjs`）**：5000 个 1 字节样本 ⇒ **11 次 `ct == pt`（0.22%，理论 0.39%）**，同一批**往返 5000/5000 全部正确** ✓ ⇒ 加密没问题。**修法**：`len >= 8` 逐步断言（碰撞需 2^-64）、短长度只用整轮聚合断言 `differed > 0`（pass-through 仍会红） | 复验：**修后 `node --test --test-isolation=none "tests/unit/chacha20.test.mjs"` 隔离重跑 250 次 ⇒ 0 失败**；旧断言会红的原文见 `.tmp/verify-260.log`（`FAIL … ciphertext == plaintext at len 1`，该次 `verify --gate all` 的 G0 就是它）| **CLOSED**（第 260 轮；先前第 245 轮那句「200 次重跑 0 失败 ⇒ 不是输入相关」**已被推翻并划线** —— 200 次对 1/256 只是一次抛硬币）|
### 第 178 轮闭掉的（D86）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| ~~D86~~ | **"自动路径把严重损坏的页读成功了"的真相：既不是假拒绝，也不是误接受，而是我自己写的 `readTilePage()` 会在读不动的瓦片上 `missing.push(t); continue;` —— 静默跳过后拼出一份"带洞的载荷"并返回成功。** 前者（假拒绝/误接受）都会误导后续设计，真正的病是"**把读不到说成读到了**"，正是本项目红线那一条的另一副面孔。**证据（一条命令，`.tmp/d86-probe.mjs`）**：毁掉第 5 块 40 个数据格后，三条路径**全部拒绝** —— 直读 `could not be corrected (chien-mismatch, 8 error(s))`、`findTileOffset` 同样拒绝、`detectTileRotation` 同样拒绝；而只毁 8 个格时直读**成功且载荷逐字节正确**（RS 修好了） | 第 177 轮的改法 + 跑 `tests/unit/tile-read.test.mjs` ⇒ `Missing expected exception`（因为异常被 `continue` 吞了，函数返回了"带洞"的载荷） | **CLOSED（第 178 轮）** ✓ **结论写进设计规则**：整页读取遇到任一块读不出，**必须整页拒绝**（`readTilePage` 不得用 `missing` 掩盖失败）；要支持"只读到一部分"，必须由调用方**显式**要求并自己承担后果。⇒ 也**推翻了我第 177 轮的误接受概率担忧**：那条 3.8% 的估算针对的是"在偏移窗内搜 CRC"，而实测里 `findTileOffset` 的**头域 + CRC 双约束**并没有让损坏的瓦片通过 |
### 第 177 轮新增（D86 · **OPEN** · 优先：可能与"误接受"红线有关）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| **D86** | **`readTilePage()` 一旦改成"先直读、失败则搜索偏移、再试四个方向"的自动路径，一个**严重损坏**（人为毁掉 40 个数据格，远超 RS 的 8 字节纠错能力）的瓦片**竟然读成功了** —— 原来的直接路径对这种页是**具名拒绝**的。两种可能，都必须查清：① 原来的拒绝是**假拒绝**（瓦片其实在某个偏移下确实完好，那么自动路径是改进）② 自动路径**误接受**（把错的读成对的）—— 后者触及本项目红线，必须最优先查 | 第 177 轮的改法：`readTileAuto()`（直读 → `findTileOffset` → `detectTileRotation`）接进 `readTilePage()`，然后跑 `tests/unit/tile-read.test.mjs` 里那条"RS 修复与拒绝"测试 ⇒ 它报 `Missing expected exception`（本该拒绝的页读成功了）。**改动已撤回**（`git checkout`），当前代码仍会拒绝 | **OPEN**（撤回后未修）。待查：把那条测试的**载荷是否真的正确**打出来（若正确 ⇒ 假拒绝；若错误 ⇒ 误接受，红线）；并核算"在 ±12 px 偏移窗 × 4 个方向里搜 CRC"的**误接受概率上界**（625×4 次尝试 × 1/65536 ≈ 3.8%/瓦片，一页 48 块 ⇒ 期望 >1 块/页 —— **这个量级本身就要求"头 + CRC"双约束，甚至要纳入页级摘要**） |
### 第 131 轮新增（D85 · 环境 / 夹具）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| **D85** | **沙箱给被测进程的会话级临时目录一旦不可用，用 `os.tmpdir()` 的测试会 `EPERM: mkdtemp` 直接红，在汇总里长得像产品坏了**：`tests/unit/render-writers.test.mjs` 里两条（pillow 对拍、`.raw` sidecar）用 `fs.mkdtempSync(os.tmpdir(), ...)` 建暂存目录；harness 把沙箱进程的 TEMP 指向会话级目录 `%TEMP%\dsh-XXXX`，该目录被删或被 ACL 受限令牌拒写之后 mkdtemp 就 EPERM，**两条测试在 0.8 ms 内红掉**（根本没跑到 python 那一步、也没碰到任何被测代码） | `node --test --test-isolation=none "tests/unit/render-writers.test.mjs"` 修前 ⇒ `PNG+TIFF: pillow (ref/verify_raster.py) agrees on every dumped sample` 与 `sidecar: dump-sample-raster writes a self-describing .raw` 两条红，错误原文 `EPERM: operation not permitted, mkdtemp 'C:\Users\ASUS\AppData\Local\Temp\dsh-8nDSLJ\pskit-raster-XXXXXX'`；**阳性对照**：同一条命令、只把暂存目录改到仓库内 `.tmp/` 之后 ⇒ `tests 14 · pass 14 · fail 0` | **CLOSED（第 131 轮）** ✓ 修法 = 该测试文件新增 `makeScratch()`，在 `REPO_ROOT/.tmp` 下 `mkdtempSync`（工作区是每次运行都保证可写的地方）；**只改夹具落地位置，不动任何判据、不改断言** |
### 第 128 轮新增（D84 · 环境 / 工具）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| **D84** | **把 `usability.ps1` 中途打断之后，8123 会暂时绑不上，于是"服务器那两步"整体 FAIL，看起来像产品回归**：步骤 7 用 `Start-Process node tools/serve.mjs --port 8123`（**端口是显式传的 ⇒ 绑不上就拒绝，不会自己换端口**），而被打断的那次冒烟留下的一次性出站连接会把这个端口当成自己的**临时本地端口**占住 ⇒ 下一次 `listen EADDRINUSE`。**这不是产品缺陷**：`serve.mjs` 自己把两种成因都打印出来了（含 `netstat -ano \| findstr :8123` 该看什么），`check-serve` 里 4 条"期望被拒绝"的检查还会**假绿**（拿到连接错误也算拒绝） | 在步骤 7 之前打断一次（例如外层工具 600 s 墙钟到点被杀），紧接着再跑：`& .\tools\usability.ps1` ⇒ 汇总行 **`USABILITY: 2 step(s) FAILED in …`**、`USABILITY_EXIT=1`；证据在 **`.tmp/usability/step7-serve.err.log` 首行**：`serve: cannot listen on 0.0.0.0:8123 -- listen EADDRINUSE: address already in use`；而 `Get-NetTCPConnection -LocalPort 8123` **看不到任何监听者**（选(b)：临时端口占用）。**阳性对照**：同一份代码、端口释放后原样复跑 ⇒ `USABILITY_EXIT=0` 全腿 PASS | **NOTABUG（环境行为）** ✓ 做法：等到端口释放再跑，或 `& .\tools\usability.ps1 -ServePort 8124`；**判回归前先看 `step7-serve.err.log`** |
## 第 23 轮闭掉的

| # | 缺陷 | 复验命令 | 状态 |
|---|---|---|---|
| ~~D1~~ | 发送端未进 dist 拷贝清单（G9 绿 ≠ 手机那页真的发布） | `node tools/build-web.mjs && node tools/check-dist.mjs` ⇒ 80 文件、78 条 precache 全对字节 | CLOSED |
| ~~D2~~ | `encodeTransfer` 选项形状与 `sheetMm` 类型未经执行验证（我曾传布尔 ✓ 真 bug） | `node tools/smoke-sender.mjs --bytes 2048` ⇒ 纸面 3 页 / 盘面 8 页全部**编好→自识几何→解回原摘要** | CLOSED |
| ~~D3~~ | 单测红：新登记的 bootstrap reason 破了 advice 契约 | `node --test --test-isolation=none "tests/unit/**/*.test.mjs"` ⇒ 231/231 | CLOSED（修法是把成功路径的字段改名，见 D12 ✓ 未动判据） |
| ~~D6~~ | PWA 清单与图标缺失 ⇒ 手机"添加到主屏幕"不完整 | `Test-Path web/dist/manifest.webmanifest, web/dist/icon-page.png` ⇒ True True（图标由 `core/render/png.js` 真渲染一页生成 ✓ 零新依赖） | CLOSED（遗留见 D13） |

## OPEN

| # | 缺陷 | 复现 | 性质 |
|---|---|---|---|
| ~~D4~~ | `index.html` 里没有指向 `send.html` 的入口：接收端用户在站内点不到发送端 | 打开 `web/dist/index.html` 目视（只能手输 `/send.html`） | 客户端可用性 |
| ~~D5~~ | 发送端未进 `pskt-file.html` 单文件变体 ⇒ `file://` 下只能"收"不能"发" | `Select-String web/dist/pskt-file.html -Pattern sender` ⇒ 无 | PLAN 只要求接收端 file:// 可用，故列为待办非违约 |
| ~~D7~~ | ~~手机摄像头连拍取页未接线~~ → 第 24 轮接线、第 25 轮结案，见下方"闭掉的"；实机部分另立 D18 | `node tools/smoke-capture.mjs` ⇒ 12/12 ✓ | CLOSED |

### 第 98 轮闭掉的（D83）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| ~~D83~~ | **一张纯白/空白扫描图，在默认调色板下被诊断成"失焦、擦镜头"，而它其实是空白页**：`findMarkers` 的 `blank-image` 判据是 `base.inkCount < 32`，而 `inkCount` 来自**相对底色的墨度**；`receive` 在没有 manifest 时默认 `--palette INK2`，INK2 的底色是**纸色 `[246,242,234]`**（不是纯白）⇒ 一张 255 的空白页在每一格上都"比纸亮" ⇒ 墨度处处非零 ⇒ `inkCount` 很大 ⇒ 空白判据不触发，最后落到 `no-square-candidates` 的文案（"靠近、2× 变焦、擦镜头"）。**这是 D69/D71/D76 同一族**：不交出错误数据（照旧拒绝 ✓），但**把用户指向错误的补救动作**（去擦镜头，而真正的事实是"这张图没有墨"） | **两条命令**（不需要任何探针文件）：`python -c "from PIL import Image; Image.new('RGB',(2480,3508),(255,255,255)).save('.tmp/blank/page-000.png')"` ⇒ `node cli/pskit.mjs receive .tmp/blank --photo --profile P-M1-300 --out .tmp/a.bin` 修前打印 **`FAIL markers/no-square-candidates`** + `do: move closer or use 2x optical zoom, hold still, and wipe the lens`；**阳性对照**同一条命令加 `--palette PAPER1`（底色纯白）⇒ 修前打印 **`FAIL markers/blank-image`** + `cause: the image is (almost) uniform -- nothing was printed, the lens was covered, or the scan was empty`（47 ms vs 1100 ms）| **CLOSED ✓（第 98 轮）** ~~原记"未实施、未验证"~~ **已实施并验证**：`findMarkers` 用 `inkRangeRatio()` 的 **p1/p99 动态范围 + 峰值尾部比例**判 `blank-image`，两者都低于 `0.02` 时提前返回；该判据与 substrate 无关，同时保留尾部条件防住"少量稀疏墨块被当成空白"。阳性对照：纯白页带 INK2 底色判 `blank-image`、D69 平光图仍判 `no-contrast`、正常页照旧可读；CLI 默认 INK2 与 PAPER1 都打印 `markers/blank-image`、exit 2、不写盘。单测 366 项中 365 pass / 1 本机 Python 对拍 skip / 0 fail，`verify --gate all` 通过 |

### 第 96 轮新增（D82 ⇒ **本轮已闭** ✓）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| ~~D82~~ | **TIFF 是平板扫描仪的另一个默认输出，而接收端只会说"这个格式读不了"**：`receive` 的输入过滤只收 `.png`，其余格式里 TIFF 被点名 `TIFF read-back is not wired` 并让用户去装 ImageMagick/PIL —— **在气隙环境里"先装个工具"不是答案**，而这个仓库连 PNG 编解码都是自己写的（`core/render/tiff.js` 一直写得出一份 TIFF，只是读不回来）。扫描仪"输出 TIFF"与"输出 PNG"同样常见，多页 TIFF（一次扫多页存一个文件）更是常态 | **一条命令**（已入库为 `tools/usability.ps1` 的 **4h 腿**）：用 PIL 从我们自己的页造出扫描仪会写的十种 TIFF（未压缩 / LZW / Adobe Deflate / PackBits / 8-bit 灰度 / 1-bit 二值 / 调色板 / 16-bit 灰度 + 一个 **3 页多页 TIFF**），逐个 `receive --photo` ⇒ 修前 `found 1 file(s) ... none in a format this build reads (.tif)`、exit 1；修后**十个全部 exit 0 且 sha256 与载荷逐位相同**，多页那份打印 `pages.tif: 3 pages in one file` 后逐页读出。单测 **`tests/unit/tiff-read.test.mjs`（9 例）**自己按字节造 TIFF（不依赖第三方编码器）：**II 与 MM** 两种字节序 · `WhiteIsZero`/`BlackIsZero` 语义相反 · **FillOrder 2（LSB 优先）** · 4/16-bit 灰度定标与取高字节 · `ColorMap` 三块布局 · **Predictor 2**（8-bit 与 16-bit）· LZW / PackBits / raw Deflate / **zlib 包裹的 Adobe Deflate** 四种压缩解出同一批像素 · 第二个 IFD 即第二页 · 以及八条具名拒绝（压缩 3、PlanarConfiguration 2、orientation 3、32-bit、4 samples、SampleFormat 3、坏字节序、空文件）| **FIXED ✓（第 96 轮）** 新增 `core/decode/tiff-read.js`（基线 TIFF 读取器，**零依赖、自写 LZW/PackBits**）：II/MM · 多 IFD 多页 · 条带式 chunky 布局 · 光度 0/1/2/3 · 位深 1/4/8/16 · 1 或 3 samples · 压缩 1/5/8/32946/32773 · FillOrder 1/2 · Predictor 1/2 · orientation 1 · XResolution+ResolutionUnit ⇒ dpi；**其余一律具名抛错**（瓦片、分离平面、CMYK/YCbCr、浮点、JPEG-in-TIFF、其它朝向）。CLI 的 `receive` 与 `calibrate` 都接上：`.tif/.tiff` 与 PNG 并列，**多页文件展开成多页**（日志用 `文件 [page N]` 区分）。**如实记一处我自己的错**：写 FillOrder 测试时把 `BlackIsZero` 的语义写反了（我按"1=黑"写预期），是测试把我纠回来的 —— 规范里 1 是**白**（纸），0 才是墨 |

### 第 95 轮新增（D80 / D81 ⇒ **两条都已闭** ✓）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| ~~D80~~ | **扫描仪的默认档是"黑白 / 线稿"，而我们的 PNG 读者只吃 8-bit ⇒ 用户被自己扫描仪最干净的一种输出挡在门外**：`core/decode/png-read.js` 里 `if (depth !== 8) throw` 与 `palette PNG unsupported` 把 **1-bit / 2-bit / 4-bit / 16-bit 与调色板 PNG 全部拒绝**。实测（PIL 从我们自己的页转出、单页单目录、`receive --photo`）：`bw1`（1-bit 无抖动）与 `pal8`（16 色调色板）**exit 2、不写盘**，用户看到的是 `page-000.png: not a readable PNG (decodePNG: bit depth 1 unsupported (need 8))` —— 一句工具内部的话，而且**把最干净的输入当成坏文件**（1-bit 扫描没有半色调，墨点边界比彩色扫描更锐利） | **一条命令**（已入库为 `tools/usability.ps1` 的 **4g 腿**）：用 PIL 把一页转成 8-bit 灰度 / 1-bit / 16 色调色板 / 16-bit 灰度四种，各跑一次 `receive --photo` ⇒ 修前 **1-bit 与调色板 exit 2、不写盘**；修后**四种全部 exit 0 且 sha256 与载荷逐位相同**。单测 **`tests/unit/png-read-depths.test.mjs`（8 例）**自己按字节造 PNG（不依赖第三方编码器，CI 里也跑）：1/2/4-bit 灰度的 MSB 解包与 `0/85/170/255` 定标 · 调色板 + `tRNS`（越界条目保持不透明）· 16-bit 取高字节（灰与 RGB）· 灰+alpha · **亚字节行上滤波的像素偏移 bpp=1**（`Sub`/`Up` 各一例）· 五条"坏头必须具名拒绝"（隔行、palette 16-bit、RGB 4-bit、无 PLTE、调色板下标越界） | **FIXED ✓（第 95 轮）** 读者按规范支持 **colour type 0/2/3/4/6 × 允许的位深**：行字节数 = `ceil(width × depth × channels / 8)`、滤波偏移 `bpp = max(1, ceil(depth × channels / 8))`、亚字节按 MSB 优先解包、低于 8 位的灰度按 `255/(2^depth−1)` 定标、16-bit 取高字节、`PLTE`+`tRNS` 展开（缺 PLTE、下标越界、位深与 colour type 不匹配一律具名抛错）。仍然拒绝：隔行 PNG、非 0 压缩/滤波方法 |
| ~~D81~~ | **`decodePNG` 从来没有读出过 dpi**：pHYs 的布局是"4 字节 X 密度 + 4 字节 Y 密度 + **1 字节单位**"，而代码写的是 `u32(phy, 8) === 1` —— 从第 8 字节起读了**四个**字节（其中三个越过 9 字节的块 ⇒ 读到 0），这个条件**永远不成立** ⇒ `dpi` 恒为 `null`。后果不止"少一个字段"：`core/decode/page.js` 的 `Math.abs((bitmap.dpi \|\| layout.dpi) - layout.dpi) < 0.5` 是"这张位图真的是标称画布吗"的**第二道守卫**，`bitmap.dpi` 恒 null ⇒ 它退化成 `0 < 0.5` ⇒ **守卫永远通过**（第一道守卫"宽高相等"仍然有效，所以没有因此误收过页） | 复现：读我们写出的 PNG 的 pHYs（实测 `11811 11811 1` = 300 dpi）再 `decodePNG` ⇒ 修前 `dpi null`、修后 `300`；新单测最后一条把这个断言钉住（`assert.equal(back.dpi, 300)`）。**它是写这组深度测试时被抓出来的**（我原本只想验证自己的读取器）⇒ 回归：`node cli/pskit.mjs receive .tmp/sc-scan300-10 --photo --profile P-M1-300 --dpi 300` ⇒ **exit 0、摘要与 manifest 相符**（语料 pHYs 实测 11811 = 300 dpi ⇒ 守卫现在真的在比 300 对 300，而不是空过） | **FIXED ✓（第 95 轮）** 改成 `phy.length >= 9 && phy[8] === 1`。修复方向是**更保守**：dpi 与版面不符时跳过快路、改走标记几何路 ⇒ 不会因为这次修复而误收一页 |

### 第 93 轮新增（D79 ⇒ **本轮已闭** ✓）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| ~~D79~~ | **"扫描成 PDF"是最常见的扫描仪默认输出，而 `receive` 对一目录 PDF 说的是 `no pages found`** —— 把一个"格式读不了"说成"没有页"，用户会去重扫、去查文件名、去怀疑自己没扫上（与 D74 同一形状：那次是 JPEG/TIFF）。本 build **写得出一份 PDF（那就是打印路径）却光栅化不了一份**，而 PDF 恰好不在 `UNREADABLE` 名单里 ⇒ 目录里只有 `scan.pdf` 时直接落到兜底那句。**同一轮还查到一处会印在每条错误上的重复前缀**：`pskit <cmd>: ` 之后原样拼 `e.message`，而很多消息自己就带 `receive: ` ⇒ 用户看到 `pskit receive: receive: found ...` | **一条命令**（已入库为 `tools/usability.ps1` 的 **4f 腿**，`-Skip3D` 可跳）：`node cli/pskit.mjs send <载荷> --profile P-M1-300 --format pdf --out <目录>` 后 `node cli/pskit.mjs receive <目录> --photo --profile P-M1-300 --out <不该存在的文件>` ⇒ 修前 `receive: no pages found in ...`（exit 1、不写盘）；修后点名 PDF + 给出导出路径（`found 1 PDF file(s) ... does not rasterize PDF pages -- it only writes them. Export the scanned pages as PNG ...`）、exit 1、不写盘、且**不再出现 `receive: receive:`**。**阳性对照**：同一目录再放进同一次传输的 PNG 页 ⇒ 逐字节还原、exit 0（证明这条拒绝没有把能读的挡掉） | **FIXED ✓（第 93 轮）** ① `cli/pskit.mjs` 把 `pdf` 单独识别并给一句**自己的**话（写得出、读不了 ⇒ 导出而不是换阅读器；并提醒**别截屏**，截屏会重采样墨点、移动角标 —— D78 的教训）② 混合目录的提示行同时报出 PDF 与图片格式的计数 ③ 顶层 catch 只剥掉一层重复的命令前缀（不逐条改消息，测试与工具仍按原措辞匹配）④ usability 新增 4f 腿（拒绝 + 阳性对照两条断言）⇒ `USABILITY_EXIT=0`、211 s 全腿通过 |

### 第 91 轮新增（D78 ⇒ **本轮已闭** ✓）

| # | 缺陷 | 复现 | 状态 |
|---|---|---|---|
| ~~D78~~ | **我们写出的 PDF 在任何主流阅读器里都是「斜的」**：图像字典写的是 `/DecodeParms << /Predictor 15 /Colors 3 /Columns ${width * 3} >>`，而流里每行只有 `width*3 = 6780` 字节。阅读器**只**按 `rowBytes = Columns × Colors × BitsPerComponent / 8` 解释 `/Columns`（pdf.js `PredictorStream`、PDFium `CPDF_Predictor`、mupdf `fz_open_predict`、poppler `StreamPredictor`、Ghostscript `zpredict` 都是这一条）⇒ 它按 `20340` 字节取行 ⇒ **每 3 行漂移 2 字节 = 0.222 px/行（300 dpi）**，整页被剪成平行四边形：下半页的套准十字与角标被挤出纸边，**印出来的纸页解不了**。PNG 产物一直是好的（PNG 的 IHDR 没有这个歧义）⇒ 用户看到的「PDF 斜、PNG 直」正是这一条。**不是用户转换工具的锅**：第 90 轮只证到「两个产物在**我们自己的行距**下逐像素相同」（等于拿自己的尺子量自己），据此推出的「转换工具剪切了页内容」本轮作废 | **一条命令**（已入库：`tests/unit/render-pdf.test.mjs` 的 D78 用例，自带阳性对照）：把写出的图像流按阅读器公式解一遍、与输入像素逐字节比 —— 修前 **5682462/7435400 像素不同（76%）**、修后 **0/7435400**。本机另做过一次不共用我们代码的独立复算（一次性探针 `.tmp/pdfcheck/reader_check.py`）：`FIXED writer … no /DecodeParms: rowBytes = Width*Colors*bpc/8 = 6780 ⇒ 0/7435400`；`OLD writer … /Columns 6780 ⇒ rowBytes 20340 (true row is 6780) ⇒ 5682462/7435400`。**形变指纹**：对 `scans/pack.pdf` 的 obj 4 按阅读器公式重排后，局部周期向量由 `(dy,dx)=(10,0)` 变成 `(10,2)`，与用户那三张 `pack_pages-to-jpg-000{1,2,3}.png` 实测**逐位一致**（它们的底部角标因此出现在 `x≈794`、右下角标被推出纸面，左侧斜边即每行 0.2 px 的漂移） | **FIXED ✓（第 91 轮）** ① 图像流改为**裸 RGB 行**（`width*3` 字节/行、无每行滤波字节），字典里**不再写 `/DecodeParms`** ⇒ 行距只剩 `Width × Colors × bpc / 8` 这一个无歧义算法。**为什么不只是把 `/Columns` 改成像素宽**：两种读者约定（`Columns` = 像素宽 vs = 采样数）必有一方算错，只要留着这个参数就仍有读者会错；而每行滤波类型恒为 0（`filter type bytes: {0: 3290}`）⇒ predictor 一分钱不省，删掉最干净 ② 单测把 `/DecodeParms`、`/Predictor`、`/Columns` 三个参数名断言为**不存在**、流长度恰为 `height*width*3`、按阅读器公式解码后与输入像素逐字节相等，**并重建旧格式字节断言同一公式下第 1 行即错**（阳性对照 ⇒ 这条测试真的会红）③ 修后 `pack.pdf` 反而**小 15 KB**（820853 → 805662 B），三张 PNG 与修前**逐字节相同** ⇒ 只有 PDF 这一条路径变了 |

### 第 24 轮新增

| # | 缺陷 | 复现 | 性质 |
|---|---|---|---|
| ~~D17~~ | **连拍的跨会话守卫未按预期触发**：构造"另一会话的页"时收集器返回 `kind page` 而非 `other-session`（`have 1/3`）⇒ 要么 `locate()` 读到的 sessionId 与我假设的形状不同，要么夹具仍错，二者之一，**尚未定论** | `node tools/smoke-capture.mjs --bytes 1024` ⇒ `CAPTURE SMOKE: 1 FAILED` | **只降级提示语，不降级安全**：混批在下游由 `TransferAssembler` 的 `other-session` 拒绝兜住（G0/G5 已测 ✓ 10000 次篡改 0 误接受 ✓）。禁止用"删掉这条断言"变绿 |
| D18 | 连拍的**实机部分本机无法验证**：`getUserMedia` 授权、`facingMode/continuous` 对焦、`getImageData` 帧率、iOS Safari 的 canvas  tainted 行为 | 需真机 https 访问 | 与 D9 同类：浏览器内未验证，G9 维持 🟡 的核心理由 |
| ~~D19~~ | `build-web` 的闭包写循环未跳过 `web/capture.js` ⇒ precache 清单里 `./capture.js` 出现两次 | 第 24 轮内即修（与 selftest/sender 同处加跳过 ✓ `node tools/build-web.mjs && node tools/check-dist.mjs` 仍 8/8） | CLOSED |
| ~~D20~~ | `web/dist` 里 `capture.js` 的 DOM 半边引用的 id（`burst`/`video`/`burst-log`/`burstprog`/`burststop`）来自本轮新加的 section，**没有判据保证二者不脱钩**（改 HTML 忘改 JS ⇒ 静默失效，因入口是 `getElementById('burst')` 的守卫） | 删掉 section 后 `node tools/check-dist.mjs` 仍全绿 | 需要一个"页面引用的 id 必须存在于对应脚本守卫里"的检查，或至少 selftest 里加一条 DOM 契约 |

### 第 47 轮新增（OPEN）

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| D51 | **600 dpi 部分页的"逐格电平判读"整体塌陷到单一电平 ⇒ 整页符号全错、页内 RS 141 块全崩却报告 `errors 0 / erasures 0`** ✗ 第 61 轮实测（`pskit calibrate` + 与原始渲染逐格对拍，对拍工具 `tools/level-diff.mjs` ⇒ 已入库、新克隆也复现得了）：`.tmp/sc-scan600-11` 的 page-001 被读成**内部 283155 格全部 = 电平 1**（同一页的原始渲染是 `0=256118 1=27037`）⇒ 失配 **90.452%**、混淆**只有 `0->1` 一种**、行/列带分布**完全平坦**（行带 `51549 51571 51706 51767 51692`）⇒ 排除噪声（噪声会有多种混淆对且有空间聚集）、排除局部损伤（平坦）、排除格构偏移（48 个 ±3 格平移里最好的仍是 90.4%）；同语料 page-000 作对照失配 **0.000%**（两侧直方图完全一致 `0=256501 1=26654`）⇒ **量具本身可信** ✓ 后果链：每符号皆错 ⇒ RS 找不到自洽的错误图样 ⇒ `errors 0 erasures 0 failedBlocks 141` ⇒ `assemble/intra-fail`（G2 600 dpi 侧这一类共 **210 页**）⇒ 页丢够多就 `short 0/1`（38 份失败语料）⇒ **这就是 D49 的 B 类根因：解码器/光度缺陷，不是"容量事实"** ✗ 并**推翻**第 19 轮"容量事实"与第 58 轮"主因指向页内 ECC 余量不足"：能读出的 600 dpi 页只花掉预算 **0.0–0.2%**（`sc-scan600-12` p0 = 0.0%、p1 = 5 errors = 0.2%；`sc-scan600-11` p0 = 0.0%），与 300 dpi 侧（9 页全 **0.0%**）同级 ⇒ **余量不是约束** ✓ 唯一拦住它变成错误字节的是页内 RS + SHA-256 摘要门 ⇒ **误接受仍为 0**（硬约束未破 ✓ 但整页数据白印了 ✗） | ① `node cli/pskit.mjs calibrate .tmp/sc-scan600-11` ⇒ page-001/002 打印 `blocks 141 clean 0 errors 0 erasures 0 failedBlocks 141`、`budget 4512/4512 units = 100.0% LOWER BOUND`；对照 `node cli/pskit.mjs calibrate .tmp/sc-scan600-12` 与 `.tmp/sc-scan300-10`（或 -11 / -12）⇒ `budget 0.0–0.2%`、`errors 0`。② 逐格对拍 **`node tools/level-diff.mjs .tmp/g2src-a4-600 .tmp/sc-scan600-11 0,1`**（第 61 轮把 gitignored 探针 `.tmp/lvl61.mjs` 提升为入库工具并把目录/页号参数化 ⇒ **台账不再指向不入库的文件**、新克隆也复现得了 ✓）⇒ 打印两侧电平直方图、混淆对与**混淆对种类数**、`COLLAPSE` 标记、48 个平移的失配率、行/列带分布；缺参数或缺 manifest ⇒ **`exit 1` 并点名**（不假装成功 ✓），比对跑成 ⇒ `exit 0`（**它不是门限**：exit 0 只表示"比对跑成了"、不表示这页好） ⇒ **修好后同一脚本在失败页上必须给出接近 0 的失配率，或至少不再是"单一电平"** | **CLOSED ✓（第 63 轮修法落地；第 94 轮用本条自己点名的命令复验 ⇒ 见末尾 ⑧）** ~~OPEN ✗（第 61 轮量到根因，未修）~~ **当时的修法方向（被本轮测量指向、且 PLAN/sim 早就写明）**：电平判读不要钉死阈值，而是**在采集页上重测 ρ 表**再判决——构件都已在仓内：`core/render/raster.js#measureTargets` + `core/decode/ideal.js#shapeThresholds`（接受 `{targets}`）、`ideal.js#expectedRho`（注释写明 for calibration reports）、`ideal.js#analyseCell`（逐格 ρ）、参考实现 `sim/selfcheck.py` 的 `calibrated_targets`（自称 pskit calibrate in miniature）⇒ 即 PLAN §M7 的 calibrate 与 `sim/README.md` L306-307 那段 recalibrated decision ✓ **两条护栏（第 11 轮写下、不许绕过）**：① 按页实测会**让接受更容易** ⇒ 必须回 **G5** 按误接受口径重验（误接受 0 是不可原谅线 ✗）；② 判据必须是**跨 seed 的稳定谷底**、不是"这一页能过"（第 11 轮拒绝把 `0.65` 钉进代码就是防这种过拟合 ✓）。**下一步测量（尚未做 ⇒ 不许写成结论）**：用 `analyseCell` 取塌陷页与健康页的 **ρ 分布**，检验"dpi 翻倍 ⇒ 每像素峰值覆盖率约减半 ⇒ 固定阈值落到分布之外"这条机制（第 10 轮的假设；第 15 轮只对**角标二值化**证伪过 `otsu/peak` 0.54 vs 0.57 ⇒ **电平判读是另一条通路，还没量过** ✗）。**一个附带发现（本轮未做，先说清代价）**：整页读成单一电平的页本该在 readout 阶段就被**拒绝并点名**（像只有 3 个角标时那样），而不是交给 RS 兜 ⇒ 用户会看到"这页判读塌陷、请重拍"而不是笼统的 `intra-fail` ✓ 但它**不会提高 600 dpi 通过率**（页照样丢），且会改变 reason 码分布 ⇒ 要重跑 G2 两侧才能记账 ✗ **第 62 轮：机制量到了、修法形态也被数据定了（仍未修）** 新增量具 `tools/rho-report.mjs`（把信道页的 ρ 分布与**同一页原始渲染**对拍；自带**镜像对照**：探针走的 `findMarkers → rectifyPage → readPageIdeal` 与 `decodePage` 的 levels 逐格相同，实测 `0 differing level(s) of 287507` ⇒ 量的是产品路径、不是平行实现 ✓）：① **零退化对照**（把原始渲染当作自己的"信道页"）⇒ ρ 两簇 **0.0021 / 0.3741**、`targets [0, 0.3699]`、比值路径 **0 错**、`ratioDisagreements 0`、`inkBalance.ratio 1.0657`（600 dpi）/`1.0197`（300 dpi）⇒ **`measureTargets` 与 `analyseCell` 口径一致**、比值判决在 pristine 上是**对的** ✓（据此**撤回**本轮中途我说的"两者口径不一致、比值路径每页都坏"✗ 那是没做零退化对照就下的判断）② **一遇信道退化 ρ 就被抬 4–20 倍**：300 dpi **完美解码页** 0-like p50 `0.5939`/1-like `1.5750`、600 dpi 健康页 `0.6164`/`1.7289`、600 dpi **塌陷页** `1.2684`/`1.6824`（pristine 是 `0.0021`/`0.3741`）⇒ 固定切点 `mid[0]=0.1850` 在**任何**信道页上错 **60.1%（300 dpi）～90.6%（600 dpi）**、在 pristine 上错 **0%** ⇒ **今天产品完全靠匹配滤波覆盖比值判决才能工作** ✗ ③ **塌陷发生在匹配滤波、不在 ρ**：塌陷页 `final levels = 1 × 287507`（MF 判每格 level 1），而 ρ 两簇**仍然分开**（`separation 3.61–5.53`、oracle 最优切点 **0.000–0.007%** 错误）⇒ 机制 = 渗墨把**每格内部**填满 ⇒ "环+点"模板对每格都比"仅环"更贴合（MF 的自由增益只吸收**幅度**、吸收不了**形状**变化）⇒ 与 `sim/selfcheck.py:836`"ρ 是穿过固定窗口量的、而墨并不待在那儿"一致 ✓ ④ **可修性（关键数字）**：**无监督** Otsu 切点（"低簇 = level 0"是字母表性质、不需真值）在**全部 9 个信道页**上把逐格错误压到 **0–208 格 / 68904–287507 格（≤0.072%）**，含三个塌陷页（`sc-scan600-11` p1 = 208、p2 = 1、`sc-scan600-10` p1 = 1）⇒ 远低于 RS 上限 **6.30%**（`(nsym/2)/(k+nsym) = 16/254`）⇒ **这些页本来救得回来** ✓ ⑤ **量到一个陷阱（不许照抄朴素 Otsu）**：在**退化的 pristine 直方图**上（两簇 sd = 0.0000 的 δ 尖峰）"取第一个最大类间方差"的朴素 Otsu 把切点放到 `t=0.0018`（**低于** 0-like 尖峰 0.0021）⇒ **60.102% 错误** ✗ ⇒ 估计器必须把切点放在**谷底 / 并列平台的中点**（或两簇均值之间），否则会**打破今天完美的 pristine/fast 路径** ⇒ 这正是第 11 轮"判据须跨 seed 稳定、不能只对自己挑的页成立"的实例 ✓ ⑥ **修法形态（下一轮实现）**：**RS 仲裁的重读** —— 先走现有 MF 路径（今天能解的页**逐格不变** ⇒ 300 dpi 侧按构造保持 200/200 ✓），**仅当**页内 RS 判 `intra-fail` 时用"按页标定的 ρ 切点"重读一次，且**只有 RS / 帧 CRC / SHA-256 通过才接受** ⇒ 仓内已有同构判例：`core/decode/echo.js:40-51` 把硬编码 `>6000` 换成"枚举候选切点、由条自身 CRC16 裁决"，并写明"**这不是放宽接受**……G5 保证与从前相同，只是阈值由证据选、不由我们选"✓ 而页级仲裁者比 CRC16 强得多（141 块页内 RS + 帧 CRC + 全载荷摘要）⇒ **无新魔数、不引入未经验证的接受** ✓ 必须同时改的调用点（漏改门限 harness 就会让产品与判据分家 ✗）：`cli/pskit.mjs` receive(L483/507) 与 calibrate(L1239/1258)、**`tools/g2-corpus.mjs`(L232/245) = G2 门限 harness**、`web/app.js`、以及相关 smoke/diag ⇒ 且**必须回 G5 重验误接受、重跑 G2 两侧**（600 dpi 侧约 50 min 解码 ⇒ 后台跑）✗ **本轮仍不实现**：夹带会改判决的解码器改动而不同时重判 G5/G2 = 制造新的虚高 ✗ ⑦ **另记 D52**：两个被注释当作"机制诊断量"的读出价（`inkBalance.ratio`、`ratioDisagreements`）实测**自证 / 失明**，会把下一个修 D51 的人引到错的因果上 **第 63 轮：修法落地 = RS 仲裁的重读（严格按第 62 轮写死的形态与验收计划）** 新模块 `core/decode/recalibrate.js`：`estimateRhoCut`（在本页 ρ 直方图上取**并列平台中点**作两簇切点，低簇 = level 0 是字母表性质、不需真值；`levels ≠ 2`、ρ 恒定、可测格 <64 一律**拒绝并说明理由**，退回今天的读法）+ `recalibrateLevels`（**按通道名**继承原值、只重判 shape ⇒ 第三通道不会被静默清零；ρ 不可测的格**保留匹配滤波的答案**、不猜）+ `feedPageWithRecalibration`（先走原路径；**仅当** `intra-fail` 才重读；重读要先过页内 RS，后面还有帧 CRC 与全载荷 SHA-256 ⇒ 无新魔数、不引入未经验证的接受）。**调用点六处全改**（漏一处产品与判据就分家）：`cli/pskit.mjs` receive + calibrate、`tools/g2-corpus.mjs`（**G2 门限 harness**，并把救回数记进 `pageClasses` 与 `rescued`）、`web/app.js`（桌面/文件接收）、**`web/capture.js`（手机连拍：`feed(page,bmp)` 不传几何，而 `addFrame` 内总是先 decode 再 feed ⇒ 暂存本帧 `boot.geom`，不改契约、不加机制）**、`tools/smoke-capture.mjs`（两处真路径闭包）。**执行到的证据**：① 新单测 `tests/unit/recalibrate.test.mjs` **6 条全过**，含**朴素 Otsu 阳性对照**（第 62 轮的陷阱被复现：naive 切点 < 0.0021、错 >50%，而估计器 **0 错**）、渗墨位移页恢复到 RS 上限内、守卫拒绝、**塌陷页 → 被拒 → 救回 → 载荷逐字节相同**、以及 **ρ 无信息时不许救**（拒绝被记录；若传输仍闭合，字节必须与原文件相同）② **G5 重验（验收计划第③条）**：原 10000 试验的**配比与 PRNG 序列一字未动** ⇒ 数字与守卫分布与第 57/58 轮**逐字相同**（`9200 精确恢复 / 800 拒绝 / 0 误接受`、`feed:intra-fail=3408`、`refused:digest-mismatch=800`）⇒ 核心改动没扰动协议守卫 ✓ 另加**独立接缝试验 2000 次**（跑在 P-M1-300 上、ρ 描述"第三种读法"⇒ 给重读最大的作恶机会，并断言 `shapeLevelsOf === 2` 防退化）：**0 误接受**、**重读被触发 2000 次**（反空跑守卫 `seamRetried > 0`）⇒ 结局只有"精确原字节"或"拒绝" ✓ **如实说明**：这 2000 次全部"精确恢复"是因为**跨页奇偶**兜住了被 RS 拒掉的重读页，**不是**重读把 2000 页都修好了 ✗ 别那么读 ③ **页级直证**（`pskit calibrate`，与第 62 轮同一批语料）：`sc-scan600-11` 三页**全部读出**（p1 救回 cut 1.4725、簇 1.260/1.686、sep 3.54 ↔ 第 62 轮 3.61；p2 救回 cut 1.2862、簇 0.905/1.562、sep **14.82** ↔ 第 62 轮 **14.82**），页内预算 0.0%/8.6%/0.0%；`sc-scan600-10` p1 救回（cut 1.5244、sep **5.53** ↔ 第 62 轮 **5.53**、259991 格改判），p0/p2 仍 `echo-bad-magic`（**别的通路、本修法不覆盖**，与第 62 轮一致、未做预测 ✓）；`sc-scan300-10` **三页全干净、零救回** ⇒ 300 dpi 侧页级无扰动 ✓ ④ 端到端：usability 冒烟**全过**（331 s；**针对最终代码重跑亦全过：11 PASS、326 s、exit 0**）（`bytes came back identical 204800 B sha256 32ec4805…`、`CAPTURE SMOKE: all assertions pass`）、全量套件 **307 测试 · 0 失败**（新增 6）、`build-web` exit 0（**29 模块**、单文件 301.5 KiB、precache 53、原子换站）、`check-dist` **13 PASS / 0 fail** 含 `G9 CHECK: all 12 assertions pass` 与"bundle 无提示解码磁盘页、摘要与 manifest 相符" ✓ ⑤ **产物核对**（用户打开的是产物、不是源码）：`web/dist/core/decode/recalibrate.js` 12389 B、离线单文件页 `pskt-file.html` 内含 `feedPageWithRecalibration`/`estimateRhoCut`/`UNIMODAL`/`plateauHi`、`web/dist/capture.js` 含 `feedPageWithRecalibration`/`frameGeom` ✓ ⑥ **本轮自造并已修**：救回提示原本打印在本页状态行**之前**又不带页名 ⇒ 读起来像在评论**上一页**（**它当场把我骗了**：我一度以为 `echo-bad-magic` 的页被"救回"了 ✗）⇒ 改成自报 `文件名 (page N): RESCUED` 并重跑 `calibrate` 核对归属 ✓ 教训：**歧义的成功输出就是错的输出**，它会顺着人写进台账 ⑦ G2 两侧按验收计划**同轮重跑**，判决与数字以 `docs/ACCEPTANCE.md` 第 63 轮块为准（300 dpi 侧必须仍 200/200，否则就是回归）⑧ **第 94 轮复验（用本条自己点名的命令，判据一字未改）**：`node tools/level-diff.mjs .tmp/g2src-a4-600 .tmp/sc-scan600-11 0,1` ⇒ **首次直读仍然塌陷**（page 1 `mismatch 256118/283155 = 90.452%`、`COLLAPSE`、混淆只有 `0->1`）—— **这不是回归**：修法是"被本页的页内码拒绝后，按**本页实测**切点重读一次"，首读那一步一行未改；而 `node cli/pskit.mjs receive .tmp/sc-scan600-11 --photo --profile P-M1-600 --dpi 600 --out .tmp/d51/got.bin` ⇒ page-001/002 各打印 `RESCUED -- shape levels re-decided against a cut measured on this page`（切点 1.4725 / 1.2862、改判 259796 / 250222 格）⇒ **`received 204800 bytes`、sha256 `91ab3f7310da452b…` 与 manifest 逐位相同、exit 0** ⇒ 本条描述的用户可见后果（整页丢 ⇒ `assemble/intra-fail` ⇒ `short`）**已不存在**；常驻守卫是 `tests/unit/recalibrate.test.mjs` 的两条（塌陷页被救回且逐字节相同 / ρ 无信息的页**不被**救回）。**首读塌陷按设计保留**，并作为救回的触发条件记录在案 —— 所以本条正文那句"修好后 `level-diff` 必须给出接近 0 的失配率"**是当时写错的验收形态**，实际验收形态是"整条传输逐字节还原"（已实测）|
| D66 | **core 早就算出"这批是加密传输、缺的是口令"，三个接收端却全部把它丢掉，还一致告诉用户"缺页"**：`core/protocol.js` 的 `finish()` 在 CIPHER 位为 1 而既无口令也无钥匙时执行 `this.needPassphrase = true; return null`，而**改前那一支不设 `this.error`**（`git show HEAD:core/protocol.js` 可验），可所有消费者在 `result` 为 null 时读的正是 `error` ⇒ 各自落到通用兜底：**CLI** 打 `receive: INCOMPLETE (1/1 data pages) -- still short`、**桌面接收页** 打「未完成（数据页 1/1）：仍缺料」、**手机连拍** 打「组装失败：未知原因（页收齐但内容不完整）」⇒ **进度明明写着 N/N，用户却被指向重印重扫**，而真正缺的是口令。更糟的是**手机连拍那一节连口令输入框都没有**（`web/index.html` 的 `#burst-wrap` 只有 `#burstname`，`web/capture.js` 是 `new TransferAssembler({})`），而**发送页是提供加密的**（`send.html` 的 `#spw` → `sender.js` L507）、桌面接收页也有 `#pass` ⇒ **手机端造得出、收不了，而且没有任何补救入口**。`tests/unit/protocol.test.mjs` L472-475 早就钉住了这个布尔（注释写着 receiver must ask for the passphrase, not fail silently）⇒ **core 履行了契约，是 UI 没接住**。**这不是误接受**：拒绝是对的、摘要门没被碰 ⇒ 判据③未破；它是**一个把用户指向错误补救动作的死路** —— 不交出错误数据，但交出错误诊断。顺带在同一页查到另一处**无来由的分歧**：手机连拍落盘走 `data:` + `btoa`（分块 `String.fromCharCode` 拼整串），而**同一页**的桌面路径走 `Blob` + `URL.createObjectURL`（`web/app.js` L114-116）⇒ 手机是下载限制更多的平台，却用了两种机制里较不标准的那一种，而这恰是判据②的最后一步「取回文件」 | 一条命令链（真跑，不是读代码；已入库为 `tools/usability.ps1` 的 **4c 腿**，`-SkipCrypto` 可跳）：`node cli/pskit.mjs send F --profile P-M1-300 --format png --passphrase PW --out .tmp/e-src`（6000 B ⇒ 1 数据页 + 2 校验页）→ `python sim/channel.py --in .tmp/e-src --out .tmp/e-scan --seed 207 --preset scan300 --modifier nocrop` → **不带口令** `node cli/pskit.mjs receive .tmp/e-scan --photo --out .tmp/e-nopw.bin`：修前是 `INCOMPLETE (1/1 data pages) -- still short`（exit 2、无产出 ⇒ 诊断错），修后是 `receive: NEEDS PASSPHRASE -- all 1 data page(s) arrived, but this transfer is encrypted and no key was given` + `this is not a missing-page problem: re-run the same command with --passphrase` + `the page images are still on disk` + `nothing was written`，**exit 2、`Test-Path` = False** → **错口令** `--passphrase not-the-key` ⇒ `INCOMPLETE (1/1 data pages) -- digest-mismatch`、exit 2、无产出（**摘要门照旧 ⇒ 判据③未被碰**）→ **正确口令** ⇒ `received 6000 bytes`、`MATCHES manifest`、exit 0，再由 `.tmp/sj-help.mjs cmp` 用 **node:crypto（独立于 `core/hash.js` 的另一套实现）**判 `IDENTICAL` ⇒ **这就是阳性对照：页与信道都没问题，两次拒绝的唯一原因就是钥匙** | **FIXED ✓（第 73 轮）** ① **core 只加一行**：那一支同时设 `this.error = 'need-passphrase'`（消费者都在 `result` 为 null 时读 `error`；单测只钉 `needPassphrase` 与 `result`、**不钉 `error`** ⇒ 不动任何判据。G5 的 `refused:` 分类按 `error` 的首个词取标签，而 G5 不加密 ⇒ CIPHER 位为 0 ⇒ 这一支根本不进；万一进了，标签也只会是准确的 `need-passphrase`，不会变成假绿）② **三处各给一句人话诊断**：CLI `NEEDS PASSPHRASE` + 「不是缺页问题」+「页图还在磁盘上，不必重印重扫」+「没写任何文件：没有钥匙就没有明文可与声明摘要核对」；桌面页「缺的不是页、是口令：填口令后再按一次『开始还原』，已选文件还在」；手机连拍「填口令后重来一次」并**如实说出代价**（连拍不留已解出的页 ⇒ 这次要重拍；**不做"留页重试"**，因为在手机上常驻每页判读结果的内存与复杂度都不划算）③ **`web/index.html` 连拍节新增 `#burstpass`**，`capture.js` 在连拍开始时读它构造 `TransferAssembler` ⇒ 手机端从"造得出收不了"变成能收；`check-dist` 两条**现成**断言正好把这块接住（`every getElementById in built JS exists in some built page` 与 `no built page declares the same id twice`，即 D20 当年要的那个契约）⇒ 本轮 `CHECKDIST_EXIT=0`、**13 pass / 0 fail**、`G9 CHECK: all 13 assertions pass` ④ **落盘统一到 `Blob` + `URL.createObjectURL`**（删掉 `btoa` 与分块 `String.fromCharCode`），与同一页的桌面路径一致；**这不是把 D63 的决定翻过来** —— D63 记的是 `sender.js` 的大产物出口（最大 ~63 MB、`send.html` 的 CSP），仍原样留在 G9 批次未动；本轮这一处受协议上限约束（≤~1.52 MB），且换成的是**同一页已经在出货的机制**、不是新机制。**两种机制都没在真浏览器里验过** ⇒ 不主张"blob 在每台手机都行"，只主张"同一页不再两种存法"，并把"真机上验一次落盘与扩展名"写进 G4/G9 清单 ⑤ 文案一律不含 markdown（`log()`/`say()` 赋的是 `textContent`，星号会原样显示）。**仍未做（如实）**：桌面接收页 `#outname` 的 `input` 监听器注册在 `run()` 内部 ⇒ 每跑一次多挂一个（`applyName` 幂等，输出名字与提示都不受影响 ⇒ 属泄漏、不属错误；本轮不动，理由是不在无浏览器可验的情况下去改 DOM 生命周期）|
| D65 | **网页发送端把 core 的英文 `RangeError` 原样丢给用户，而"一次传输最多 255 页 ≈ 1.52 MB"这个产品最重要的实用限制没人说清**：一次传输的天花板不是浏览器给的、是**协议**给的 —— 页头 `totalPages` 只有**一个字节**（`core/frame.js:19`：`totalPages (u8), <= 255`）⇒ 最多 **255 页**，而 `core/protocol.js:318-320` 先扣页间校验页、扣到没位置就抛 `too many pages: N data pages leave no room for parity`。**发送页把这句英文原样显示**（`buildArtifacts` 的 `stage:'encode'` 分支），再附一条**不含任何数字**的通用提示（"换更大剖面、调低校验页 %，或先分割文件"）⇒ 用户不知道自己差多少、上限在哪、哪个旋钮管用；而 **CLI 自第 47 轮就有可操作的拒绝语**（`needs 1120 pages > 255 (inter-page RS limit): shrink payload or use a denser profile`）⇒ 缺口只在网页路径。`docs/USE.md` L91 也只含糊写"最大 payload ≈ 每页净字节 × 255"，**而这句高估了**：校验页要占位置，20% 校验页下真实上限是 **212 个数据页**。**同轮我自己先写错一次**（如实记）：第一版加的是 `MAX_WEB_PAGES = 600` 的**渲染预算**闸门，理由听着对（渲染同步、每页 PNG 常驻 ⇒ 大文件压死标签页），但 ① **它永远不可能触发** —— core 在 255 页就拒了，600 页的闸门在默认档下是**死代码**；② 它的提示写着"改用 CLI……**页数不限**"⇒ **假话**（CLI 走同一个 `encodeTransfer`，同样撞 255）| 两个**只读**探针（一次性、未入库；永久版是 `tools/smoke-sender.mjs` 的三条断言，把数字钉在**编码器自己的输出**上）：`.tmp/probe-capacity.mjs` ⇒ `P-M1-300` 几何 **319×216 格 × 1 bit = 8613 B 原始容量**、`parityPct 20`；`.tmp/probe-encode-limit.mjs` ⇒ **0.5 MiB → 84 页 ✓ / 1 MiB → 168 页 ✓（177 ms）/ 2 MiB → 48 ms 抛（280 数据页）/ 3 MiB → 抛（419）/ 4 MiB → 93 ms 抛（559）** ⇒ 每数据页净 **7514 B**（`geom.ecc.dataBytes`，与第 47 轮 STATUS 记的数字一致 ✓），默认档一次最多 **1,592,968 B ≈ 1.52 MB**、校验页 0% 时 **1,901,042 B ≈ 1.81 MB**。**冒烟两次 FAIL 都是我的错、不是产品的错**，而且两次都值回票价：① 第一版断言预期 `stage=budget`，实得 `stage=encode` ⇒ 才暴露上面那个死闸门与假承诺；② 它得 `encode` 的直接原因是我拿 `new Uint8Array(4 MiB)`（**全零**）当"大文件"⇒ 被本项目自己的 deflate 压成 **6 页**（探针另测 8 MiB 全零 = 10 页）⇒ **载荷错**。同一个事实还推翻第一版的第二处设计：我原想在**读入文件之前**按字节数拒绝，理由是"按每页原始容量算出来的是页数下界、不会误拒"⇒ **压缩比没有上界 ⇒ 那根本不是下界** ⇒ 会把一个 20 MB 但压得动的日志**误拒**（本仓库禁止的失败方向：把能用的挡掉）| **FIXED ✓（第 71 轮）** ① **删掉**不可能触发的 600 页闸门与那句假的"CLI 页数不限"；② 新增纯函数 **`transferBudget(byteLength, geom, parityPct)`**，**逐项镜像** `core/protocol.js:307-321`（`dataPages = ceil(n / geom.ecc.dataBytes)`、`parity = max(2, ceil(dataPages*pct/100))`、总量夹在 u8 上限内），并**反解**当前配置下的最大 payload（从 253 往下走，问编码器会问的同一个问题）；几何未知 ⇒ 返回 `null`（**不给句子，而不是猜一个**）；③ **`pageLimitHint()`** 把那句英文翻成中文并**带数字**：上限来历（`core/frame.js:19` 的 u8）、当前档每页净 7514 B、这个文件需要几页（数据页 + 校验页）、当前配置最多约 1.52 MB、三条出路（**调低校验页 %** ⇒ 0% 时约 1.81 MB，代价是丢页恢复能力下降 / **换每页装得更多的档** / **切成几份分别传**，每份各有自己的摘要校验），并**明说"CLI 受同一个 255 页限制，改用 CLI 解决不了这一条"**；④ **`earlySizePlan()`** 在**读文件之前**只**警告、绝不拒绝**（读入是一次性同步分配、渲染也同步 ⇒ 先把数字给出来；≥64 MB 另提醒读入本身可能卡死页面 —— 该阈值是**判断不是实测**，注释写明本机没有浏览器可测，且它只产生句子）；⑤ `stage:'encode'` 分支只在错误文本匹配 `too many pages` 时换提示，**`error` 原文一字不动**（解释失败不能顶掉真错误），且 `planPage` 在失败路径上再抛就**退回通用提示**；⑥ `docs/USE.md` 三处更正（发送页那句改成"一次传输最多 255 页（默认约 1.52 MB 不可压数据）"、L91 的"× 255"改成"× **能留给数据页的页数**"并给实测数字、故障排查新增一条含 CLI 拒绝语原文）。**验证**：`smoke-sender` 新增 **3 条断言、每条带阳性对照**，且**镜像用编码器自己的输出钉住**（不是测试里另算一遍）：1 MiB xorshift ⇒ 编码器 **168 页** = 预算 **168**（140+28）；2 MiB ⇒ 编码器抛 **"too many pages: 280 data pages"** = 预算 **280 数据页 / 336 总页**超 255；20% 允许 **1,592,968 B** < 0% 的 **1,901,042 B**（证明"调低校验页"是真旋钮）；几何未知 ⇒ `null`；4 MiB ⇒ 警告含 255 / `core/frame.js:19` / 每页净而**不含"不渲染"、不含"改用 CLI"**，**4096 B ⇒ 必须安静**（阳性对照：每次都警告 = 教会用户无视日志）；真实调用 4 MiB xorshift ⇒ `stage=encode`、**98 ms**、hint 含 **"CLI 受同一个 255 页限制"** 与"解决不了"且**不含"页数不限"**，**4 MiB 全零 ⇒ 仍编出 6 页**（不误拒压得动的大文件）⇒ **`SENDER SMOKE: all assertions pass`、13 PASS / 0 FAIL（原 10 + 新 3）、`SMOKE_EXIT=0`**；`BUILD_EXIT=0`（`pskt-file.html` **318.4 KiB**、precache **54** 未变 ⇒ 没新增 core 文件）；`CHECKDIST_EXIT=0`、**13 pass / 0 skipped / 0 fail**、`every id unique within its own page, across 4 built page(s)`、**`G9 CHECK: all 13 assertions pass`**；全套单测 **`tests 315 · pass 315 · fail 0 · duration_ms 132415`**、`SUITE_EXIT=0`；`smoke-capture` `CAPTURE_EXIT=0`（stats 不变）；**端到端 usability 11 PASS / 0 FAIL / 124 s**、字节相同（`32ec480521da27d2` vs `32ec480521da27d2`）、`USABILITY_EXIT=0`（dist 已重建 ⇒ `check-serve` / `check-lan` 查的是新站，即手机视角）。**仍未做（如实）**：255 页这个上限本身没动（它是协议字段宽度，改它要动帧格式与已印出的纸 ⇒ 不是本轮该碰的）；发送页也**没有**分片上传/多传输串联的 UI（切成几份目前要用户自己做 ⇒ **第 72 轮把这条死路补通了**：`core/splitjoin.js`（纯函数、无 `node:` 内建，Node 与浏览器同一份）+ `pskit split` / `pskit join`；join 逐份校验摘要、再校验整文件摘要，缺一片/短一截/错一位都 exit 1 且**一个字节不落盘**，`tools/usability.ps1` 新增 4b 腿把整条链路做成入库 keeper 并带阴性对照）|
| D64 | **D55 的根因：`nearestInk` 返回的是"palette 里的下标"，`joinCellLevels` 按"geometry 声明的档数"校验，两个数从无人核对** ⇒ 候选把一个 2 档 colour 通道的 profile 配上 4 墨 palette 时，量得离第 3/4 种墨更近的格子会产出 `colourLevel = 2` 或 `3` ⇒ `RangeError: joinCellLevels: colour level N out of range 0..1`。这是 D55"抛不是拒绝"的**来源**（第 65 轮只兜住边界：`bootstrapDecode` 现在捕获并记为一次失败尝试）。**而且这一组合是用户可达的、不是只有"错档"才会撞上**：`cli/pskit.mjs` L40 的帮助文本明写 `--palette INK2 INK4 PAPER1`、L131 显式参数优先；`web/sender.js` L366 有色板下拉框、L167 `pickPalette(profileId, opts.palette)` ⇒ 用户可以印一张 `P-C4-600 + INK4` 的页（编码器只会用到 ink 0/1），**这时抛异常的那个候选是正确档**，只要一个格子因渗墨/模糊/光照量偏就中招 ⇒ 真实扫描/照片正是它会出现的地方 | 三条代码事实钉死机制：① `core/decode/ideal.js` L240-242（`pal = getPalette(palette)`、`colourChannel = geom.channels.find(name==='colour')`）与 L317（`colourLevel = nearestInk(a.ink, pal)`），而 `nearestInk`（L417-428）返回 `0..pal.inks.length-1` 的**下标**；② `core/protocol.js` L63-73 校验用的是 `geom.channels[].levels`；③ `planPage`（`core/profiles.js` L107-164）**不吃 palette**，`levels` 直接来自 profile 声明（L159-162），而 `candidatePlans`（`bootstrap.js` L40-82）把 profile × `['INK2','INK4','PAPER1']` **无核对地叉乘** ⇒ 写了只读探针 `.tmp/probe-d55.mjs`（**一次性、未入库**：它数的是矩阵现状，而**永久版是 `tests/unit/nearest-ink.test.mjs` 的第三条断言**，把"不一致的组合恰好是那 2 个"钉住了 ⇒ 不必像 `tools/sender-zip-check.mjs` 那样留一个 keeper）数了一遍：**30 个 profile×palette 组合里 2 个真的不一致**——`P-C4-600` 配 INK4（2 档 < 4 墨，正是第 65 轮那条消息里的候选）与 `PL-D2` 配 INK4（2 档 < 4 墨）；192 个候选里约 11 个落在这两组 ⇒ **探针第一版把"没有 colour 通道"也算成"0 档"、报出 20 个 MISMATCH，是我自己的假阳性**（`P-M1-300`/`P-M1-600`/`P-M2-600`/`PL-G`/`PL-M1`/`REL-H1` 根本没有 colour 通道 ⇒ `ideal.js` L317 的 `if (colourChannel)` 不成立 ⇒ **300 dpi 纸面主路走不到这条抛出**）✗ 如实记 | **FIXED ✓（第 70 轮）** ① **修法不是删候选**：编码器用同一份 geom + palette、印的是 `pal.inks[level % inks.length]` ⇒ 2 档通道配 INK4 时只用 ink 0/1，**这样的页用户真的能造出来** ⇒ 删候选会**改判、丢真页** ✗ ② 改成**让判读只在"该通道能寻址的墨"里找最近**：`nearestInk(rgb, pal, limit = pal.inks.length)`（默认值保持旧语义），L317 传 `colourChannel.levels` ⇒ `min(inks.length, levels)` 在**墨数 ≤ 档数时是空操作**（所有本来不会抛的组合**一个判决都不改**），在墨数 > 档数时与编码器**严格对称** ③ 顺带发现 `recalibrate.js` L210 把 `colourLevels[i]` 直接喂给 L211 的 `joinCellLevels`，而 `ideal.js` L319-323 在 `shapeLevel === null` 时**跳过 join 却已写下 `colourArr[i]`** ⇒ 那条共用仲裁路径（桌面接收 / 手机连拍 / G2 都走）**也能抛同一个异常**、而第 65 轮的兜底没包住它 ⇒ 根因修在源头后两处一起消失（不在两处各加钳位：钳位会造出"看起来成功但是错"的色阶）④ `inkSpread` 同样参数化（它也调 `nearestInk`）⇒ 报告不会与判决各说一套；并发现它**全仓无调用点**、而注释自称 "kept and exercised" ⇒ 新测试把它真正 exercise 了，那句注释重新变成真的 ⑤ `bootstrap.js` 的 try/catch **保留**，注释改成"不再为这一例承重"（猜错档该花时间、不该花会话，这与"今天的代码不会抛"是两件事）⑥ **回归测试 `tests/unit/nearest-ink.test.mjs` 4/4 全过、`NEARESTINK_EXIT=0`**，含**阳性对照**：`nearestInk([20,20,20], INK4) === 3` 且 `joinCellLevels({shape:1,colour:3}, planPage('P-C4-600'))` 抛 `/colour level 3 out of range 0\.\.1/` ⇒ **先证明缺陷真实存在**，再证明限制后 **5832 个采样色**全部落在 0..1 且 `doesNotThrow`；另两条断言：在**真实候选表**上"墨数 ≤ 档数"的组合限制前后**逐个相同**（并断言比较过的组合 **> 20 个**，防这个测试变成空跑）、以及"不一致的组合**恰好是那 2 个**"（将来新增 profile/palette 再引入不一致会**当场红**）⑦ 全套 **315/315 · 0 fail**（311 + 4）、`SUITE_EXIT=0`；`BUILD_EXIT=0`（precache 54）、`CHECKDIST_EXIT=0`（13 pass / 0 fail、`G9 CHECK: all 13 assertions pass`）；端到端 usability **11 PASS / 0 FAIL / 123 s**、字节相同 ⇒ **可达面如实**：抛出只在有 colour 通道的档上（`P-C4-600` / `PL-D2` + INK4），**不是 300 dpi 纸面主路的阻塞**；修它的理由是根因该闭、共用仲裁路径也暴露在同一条异常下、以及第 65 轮的兜底不该继续承重 |
| D63 | **发送页的大产物下载会静默失败**：`give()` 是页面上**唯一**的下载出口（zip / pack.pdf / 3mf / stl 三处调用都走它），它先把整个产物变成 JS 字符串、再 `btoa`、再拼进 `data:` URL ⇒ 一个 168 页的 zip（约 63 MB）意味着 **~295 MB 的瞬时字符串**（UTF-16 中间串 2n + base64 两份 2×4n/3 ≈ 产物的 4.7 倍），而**页面无法知道下载有没有成功**（浏览器不给"文件已落地"这个事件）⇒ 失败模式就是"什么都没发生"= **静默失败**，与 D58 同一形状（那次是日志声称下载了 N 张、浏览器悄悄丢掉大部分）。第 68 轮把这条记在"仍未修"里，但只记了体量、**没记它会静默** | 第 68 轮记账时量到 168 页 zip ≈ 63 MB ⇒ base64 后是单个 ~84 MB 的 data: URL；本轮读 `web/sender.js` L281-293（`b64` + `give`）把**中间字符串**也算进来 ⇒ 瞬时占用约为产物的 4.7 倍；并 grep 确认 `give()` 是唯一出口（L410 zip、L415 pdf、L420 models 三处调用）⇒ 一处即可覆盖全部产物。**为什么不直接改成 `blob:`**：`URL.createObjectURL` 更稳，但 `send.html` 的 CSP 与真机行为**本机无浏览器可验** ⇒ 盲改可能把现在能用的弄坏（第 68 轮已按这个判断记账）⇒ **不动下载机制，只把风险如实说出来** | **FIXED ✓（第 70 轮）** ① 新增纯函数 `downloadPlan(name, byteLength)` + 导出阈值 `DATA_URL_RISK_BYTES = 32 MB`（与 D61 的 `previewPlan`/`printPlan` 同一套做法：DOM 段进程内不可达 ⇒ 策略抽成纯函数才验得到）；**阈值是判断、不是实测**（本机没有浏览器可测），注释写明它设在"瞬时字符串达到几百 MB、手机标签页可能死掉"的地方，且**低于阈值一律保持安静**（每次三页传输都警告 = 教会用户无视日志）② `give()` 在**尝试之前**打印提示：报出文件名与 MB 数、明说"页面无法知道下载有没有成功（浏览器不给这个事件）"、给出**不经浏览器**的出路（`node cli/pskit.mjs send 你的文件 --profile P-M1-300 --format png,pdf --out 目录`）与"把文件切小、分几次传"③ 提示文本不含 markdown（`say()` 写的是 `textContent`，L277 实测；星号会原样显示）④ `smoke-sender` 新增断言**带阈值两侧的阳性对照**：802 KB 与 `DATA_URL_RISK_BYTES - 1` 必须**不留提示**、63 MB 必须留且**点到文件名 / MB 数 / CLI 命令**、阈值本身**含等号** ⇒ **`PASS sender says when a data: URL download is too big to trust, and stays quiet when it is not`**、`SENDER SMOKE: all assertions pass`、`SMOKE_EXIT=0`；`BUILD_EXIT=0`（precache 54）、`CHECKDIST_EXIT=0`（13 pass / 0 fail、ids unique、**all 13 assertions pass**）✓ **仍未修（如实）**：下载机制本身还是 data: URL ⇒ 换 `blob:` + `createObjectURL` 要动 CSP 且需真浏览器验证，归入 **G9** 那批；在那之前，大产物的正确出路是 CLI（提示里已给出原文命令）|
| D62 | **网页接收端把还原出的文件硬命名成 `.bin` ⇒ 手机用户拿回一个打不开的文件**：`web/app.js` L116 与 `web/capture.js` L248 都写死 `pskt-${len}B-${digest12}.bin`。页头**确实没有文件名字段**（`core/frame.js` 只有 magic..length..pages..digest..crc16），所以"默认名从字节导出"**是对的、而且代码注释写明了理由**（`app.js` L113-115 原文：`the only honest default is a name derived from the bytes themselves. **The user renames it after.**`）⇒ 缺陷不在默认名，而在**"用户之后自己改名"这半句在手机上不成立**：iOS/Android **靠扩展名决定用什么打开**，`.bin` 没有对应处理器 ⇒ 用户把一份 PDF 拍回来、摘要核对通过、字节一致，**然后打不开**，还得进系统文件管理器改名（多数人不知道扩展名才是关键）⇒ 目标里的路径 ②（手机端取回文件）走到最后一步卡住。**CLI 没这个问题**：`receive --out <file>` 本来就让用户指定落盘名（`cli/pskit.mjs` L47）⇒ 缺口只在两条网页路径 | 第 68 轮末尾查 `core/protocol.js` 无 filename 字段时注意到（grep 只命中 channel 名），本轮读 `web/app.js` L100-125 与 `web/capture.js` L232-261 确认两处硬编码与那条"用户自己改名"的注释。**动手前先确认了三件决定成败的事实**（否则会踩构建与沙箱的坑）：① `app.js` 在源码树里就写 `./core/…`（L11-19，bundler 的特例解析）② `capture.js` 写 `../core/…`，`tools/build-web.mjs` L378 有专门改写 `'../core/` → `'./core/`、且 L277 把它**排除在 bundle 之外**（原样拷贝）③ **`tools/smoke-capture.mjs` L27 会从 Node 加载 `web/capture.js`** ⇒ 它的**静态** import 必须在源码树里可解析 ⇒ 只能写 `'../core/naming.js'`，不能学它内部那些只在 dist 里跑得通的动态 `'./core/…'`（两种拼法都是承重的，注释里已写明别"顺手统一"） | **FIXED ✓（第 69 轮）** ① 新增**纯 core 模块** `core/naming.js`（Node 与浏览器同一份、零依赖）：`digestName` / `sanitizeFileName` / `downloadName` / `MAX_NAME_LEN`，把"人类输入 → 一个安全的单段文件名"集中在一处（放 core 而不是每页各写一份，是因为两份规则一定会漂移到其中一份放出穿越路径）② **默认名逐字节不变**（留空仍是 `pskt-<len>B-<digest12>.bin`），两条网页路径都改成调 `downloadName({byteLength, sha256Hex, userText})` ③ `web/index.html` 两处各加一个**可选**名字输入（`#outname` + 会显示"将保存为：X"的 `#outname-note`；连拍节 `#burstname`），并写清"名字只用于本机命名下载、不发送、不进页码"④ 规则如实：路径分隔符与 Windows 保留字符/控制字符剥掉；**任何以点开头的输入一律回退到摘要名**（`.`、`..`、`../../etc/passwd` 去掉分隔符后仍以点开头 ⇒ 回退，而不是"清洗成一个看着像的名字"）；Windows 保留设备名（`NUL`/`COM1`/`lpt9`…）回退；超长**截词干、保扩展名**（吃掉 `.pdf` 正是本模块要防的失败）；**只填一个扩展名**（`.pdf`，手机用户最可能干的事）读作"把默认名的 `.bin` 换成它" ⇒ 词干仍由摘要导出、不发明任何东西 ⑤ **单测 `tests/unit/naming.test.mjs` 4 个 test 全过、`NAMING_EXIT=0`**：第一条是**阳性对照**（留空时必须与两个页面原先硬编码的字符串逐字节相同——页面在进程内加载不了，默认名悄悄变了别处不会报警），另一条拿 **18 个恶意输入**断言结果**非空 / 不含分隔符 / 不含 `..` / 不以点开头 / ≤120 字符 / 不含非法字符**，外加截断保扩展名与 `.pdf` 语义 ⑥ 构建侧实测：precache **53 → 54**、单文件页 **306.4 → 315.2 KiB**（naming 已内联）、`web/dist/core/naming.js` 6554 B、`dist/capture.js` 的 import 已正确改写成 `'./core/naming.js'`、`check-dist` **13 pass / 0 fail**（新 id 存在于构建页且各页唯一）、`smoke-capture` 全过 ⇒ **协议、页图、摘要、判据一律未动：不改信道一个比特**。（插曲：第一次跑单测忘了 `--test-isolation=none` ⇒ `spawn EPERM`，`AGENTS.md` 早写着这条；是调用方式错，不是测试错）|
| D61 | **发送页给每一页都建一张 base64 预览 ⇒ 大传输把第 66 轮从 pack.pdf 移走的开销原样搬回 DOM**：编码成功后 `web/sender.js` 对 `r.pages` 逐页 `createElement('img')` + `src = data:image/png;base64,…` ⇒ 每页 ~0.47 MB 的 base64 **字符串**常驻 DOM，而且浏览器会把 `<img>` **解码成位图**（A4/300dpi = 2480×3508×4 B ≈ **34.8 MB/页**；第 66 轮 `tools/sender-memory-probe.mjs` 在 Node 侧对同一批位图实测 **30.6 MB/页 RSS**）⇒ 168 页的预览是 **~79 MB 字符串 + GB 级解码位图**（`loading="lazy"` 只是提示、不是承诺），而 `docs/USE.md` 明说发送页在手机上"也能开" ⇒ 手机发送端首当其冲。**同轮还查出我上一轮自己写的修法有个次序错**：`doprint` 的"页数多会很慢"提醒排在 `window.open` + `document.write` **之后** ⇒ **钱花完了才提醒**；而 168 页 ≈ **5.8 GB** 解码位图塞进一个窗口时，那个标签页很可能根本回不来提醒任何人 = 静默失败 | 第 67 轮把这条挂在"实测但未修"（当时口径只算字符串：168 页 ≈ 79 MB base64 + 59 MB PNG）；第 68 轮读 `web/sender.js` L296-343 时把**解码那一半**也算进来 ⇒ 量级从"字符串"变成"位图"，同时看见 L340 的提醒在 L326 的 `document.write` 之后。**进程内可验的部分抽成纯函数**（`previewPlan` / `printPlan`）：DOM handler 在 `document` 守卫后面不可达 ⇒ 这是唯一能在进程内测到这两条策略的办法，也正是本文件头部写明的分工原则（`buildArtifacts` 当初就是这么来的）。`tools/smoke-sender.mjs` 加两条断言，**都带阳性对照**（3 页必须全预览且不留提示、页数正好等于上限必须仍可打印、0 页也要可打印），否则一个"什么都拒绝"的策略也能把断言蒙过去 | **FIXED ✓（第 68 轮）** ① 预览上限 `PREVIEW_CAP = 8`（≈3.8 MB 字符串、最坏 ~280 MB 解码），超出的页数**如实说明**（"预览只画了前 8 页，还有 160 页没有预览……**所有页都在 pack.pdf 和 PNG（zip）里，一页不缺**；要逐页看请下载它们"），并加 `loading="lazy"` + `decoding="async"` 当提示（注释写明**不指望它**）② `doprint` 改成**花之前先决定**：`printPlan(n)` 返回 `write:false` 就**直接拒绝**，给出 GB 级数字与 pack.pdf 出路，不再写完窗口才提醒（`PRINT_WINDOW_PAGE_CAP = 8`）⇒ 拒绝不是放宽判据：pack.pdf 页数不限、且本来就是按真实物理尺寸放置的那个产物，`docs/USE.md` 也叫用户印它 ③ 两条策略都是纯函数 ⇒ `smoke-sender` 实测 **`PASS sender previews are capped, and the cap says what it skipped`** 与 **`PASS sender refuses the browser-print window before spending, not after`**、`SENDER SMOKE: all assertions pass`、`SMOKE_EXIT=0`；`BUILD_EXIT=0`（`pskt-file.html` 306.4 KiB / precache 53）、`CHECKDIST_EXIT=0`（13 pass / 0 fail、ids unique）✓ **仍未修（如实记账）**：`give()` 用 data: URL 下载，168 页的 zip ≈ 63 MB ⇒ base64 后是**单个 ~84 MB 的 data: URL**；`blob:` + `URL.createObjectURL` 更稳，但要改页面 CSP 且**本机没有浏览器可验** ⇒ 不动（改错了比不改更糟），留作需要真浏览器的那批验收（G9）一起处理 |
| D60 | **G6 的门限可以被"少跑时间"骗绿**：`tools/soak.mjs` 原判 `minutesOk = elapsedMin >= Math.min(minutes, 30) * 0.98` ⇒ 传 `--minutes 1` 时阈值变成 **0.98 min**，于是 1.2 min 的跑打印 **`PASS G6: encode 196 ms <= 5000; worst decode 1752 ms/page <= 2000; rss +0.00% <= 10%; … 1.2 min >= 30 min`** ——**一句自相矛盾的假绿**（同一行里 1.2 与 30 并列却判 `>=`）；而且这条路是**文档里的门限命令**可达的：`node cli/pskit.mjs verify --gate G6 --minutes 1` ⇒ `docs/PLAN.md` 明写 30–60 min，工具却允许用 **72 秒**满足它 ✗ 旁边注释还写着 `a 30 min soak must actually be 30 min` ⇒ **注释是对的、代码没做**（与 D54 同一族：文件头声称的与代码实际做的不是一回事） | 第 67 轮改完 RSS 窗口后跑 1 分钟试跑（只为验证不崩），它没崩、却**报了 PASS** ⇒ 顺带暴露。同一次试跑还暴露了我本轮自己写的一个错：两处编辑的变量名对不上（定义的是 `narrowPct`、JSON 里写 `narrowGrowthPct`）⇒ `ReferenceError: narrowGrowthPct is not defined`，而且它发生在**最后写 JSON 那一步** ⇒ 若没先短跑，30 min 的跑会**跑满之后才崩、白跑半小时** ✗ 这就是"改完先短跑一遍"的价值 | **FIXED ✓（第 67 轮）** ① 提出 `MINUTES_REQUIRED = 30` 常量、`minutesOk = elapsedMin >= MINUTES_REQUIRED * 0.98` ⇒ **`--minutes` 只能加长、不能缩短判据**，并把注释里"为什么"与那条假绿原文一起写进代码 ② 汇总行改成打印常量、不再硬写 30 ③ 变量名对齐（JSON 里写 `narrowGrowthPct: narrowPct`）④ 复验（1 分钟试跑）：**`FAIL G6: … 1.2 min < 30 min`** + `note: the run was shorter than the criterion asks (1.2 min); a short run cannot pass G6, only fail it`、JSON 正常写出、`SHORTSOAK_EXIT=1` ✓ **如实**：这个假绿**从未被当成判决引用过**（第 65–66 轮的 G6 数字都来自 30 min 跑，`ACCEPTANCE` 也写着短跑不算），但它在文档命令里可达 ⇒ 是缺陷、不是理论问题 |
| D59 | **G6 的 RSS 统计量窗口短于 GC 锯齿 ⇒ 同一份代码、同一个工具，两次跑出互相矛盾的判决**：修后 30 min soak 报 **`rss +13.98% > 10%` ⇒ `FAIL G6`**（基线 503.9 MB → 终值 574.3 MB），而修前那次报 **+2.50% ⇒ 通过**。判据（`docs/PLAN.md` L106：RSS 增长 ≤10%）一个字没变，变的是**采样窗口在时间轴上的长度**：工具按"前/后 10% 的样本"取中位数，而 D54 修好后一个循环从 ~80 s 变成 ~13 s ⇒ 同样是 10% 的样本，修前覆盖 ~4 min、修后只覆盖 **~3 min**，与本进程 RSS 的锯齿周期同量级 ⇒ 测出来的"增长"其实是**两个窗口各自落在 GC 的哪个相位** | 把日志里 29 个每分钟样本按窗口取中位数：0–3 min **581.9 MB**、3–8 min **510.6**、8–20 min **512.8**、20–31 min **515.45**，最小/最大 **449.3 / 641.4 MB** ⇒ 第 3 到第 31 分钟**几乎全平（+0.95% / 28 min）**，且**头 3 分钟反而比平台高** ⇒ 既不是泄漏、也不是"热身不足"（本轮最先猜的正是热身不足，**被自己的数据推翻** ✗ 如实记）⇒ 真机制是**窗口分辨率不够**：±100 MB 的锯齿叠在 ~515 MB 的基线上（±20%），而判据只给 10% 的余量 ⇒ 3 分钟的窗口不可能稳定 | **FIXED ✓（第 67 轮）** 判据不动（仍 ≤10%），改**仪器**：窗口从"10% 的样本"改成**四分位**（每侧 ≈7.5 min ⇒ 至少平均掉一次锯齿），并**同时算、同时打印、同时写进 JSON** 旧的那个 10% 窗口统计量（`narrowGrowthPct`，明确标注 `printed, not judged`）⇒ 换窗口不藏任何数、旧数字仍可跨运行比较；另加 **Q1..Q4 四个中位数**与 **peak/floor**（泄漏表现为这四个数**单调爬升**，GC 噪声不是）⇒ 1 分钟试跑实测新输出：`rss baseline … (median of first 8 samples = first quartile)`、`rss quartile medians Q1..Q4 581.8 / 494.3 / 441.8 / 443.6 MB -- a leak climbs monotonically across these four, GC noise does not`、`(the pre-round-67 window, 10% of samples = 3, would report -10.29%; printed, not judged: with a 441.8-581.8 MB sawtooth that window is shorter than one swing)` ✓ 完整性那半与窗口无关、**修后代码上已实测通过**：141 cycles / 1401 页 / 1128 次腐蚀试验 ⇒ **误接受 0 / 摘要不符 0 / 抛错 0 / 拒绝 0**，且 **worst decode 1681 ms ≤ 2000 ✅**、encode 194 ms ✅ ⇒ **30 min 重跑已起（`.tmp/g6-30-r67.json`），**第 68 轮收：`PASS G6`、RSS `+1.78%` ≤ 10% ✅**（`.tmp/g6-30-r67.json`，30.2 min / 141 cycles / 1401 页 / 1128 次腐蚀试验；507.3 → 516.3 MB、peak 640.7 / floor 436.1）⇒ 仪器修好后判决稳定，且**同一次跑里旧窗口算出 `+2.96%`**，而上一轮**同代码同旧尺子**是 **+13.98%** ⇒ 差 11 个百分点，正是本行说的相位抽样。**如实（不许被引用成"无泄漏"）**：**Q1..Q4 中位数 507.3 / 511.7 / 515.8 / 516.5 MB 单调爬升**，而本工具的注释就写着"泄漏表现为这四个数单调爬升"⇒ 增量收窄（+4.4 / +4.1 / +0.7 MB）⇒ 形状像收敛到工作集、不像线性泄漏，**但形状判断不是证明** ⇒ 已起 **60 min 跑**（PLAN 允许 30–60 min，`.tmp/g6-60-r68.json`）分开两者，**结果第 70 轮收了**（`.tmp/g6-60-r68.json`，60.02 min / `ok:true` / `SOAK60_EXIT=0`；**281 cycles / 2782 页解码 / 2248 次腐蚀试验**；编码 199.4 ms ✅、解码 worst 1783.0 / mean 1599.9 ms ≤ 2 s ✅、**误接受 0 / 摘要不符 0 / 抛错 0 / 页拒绝 0 / 仲裁再读 0** ✅、**RSS `growthPct +3.16%` ≤ 10% ✅**，peak 681.4 / floor 462.9 MB、旧窗口同时打印不判 `narrowGrowthPct +3.93%`、`cycleMsEarly 12867.5` vs `cycleMsLate 12581.5` ⇒ 吞吐没退化）⇒ **判据在 PLAN 规定的时长内通过**；但 **"无泄漏"仍不主张**：Q1..Q4 中位数 **532.31 / 538.63 / 542.84 / 549.28 MB**（增量 **+6.32 / +4.21 / +6.44 MB**）⇒ 60 min 内**没有收敛**，而 30 min 那次是 +4.4 / +4.1 / +0.7 ⇒ **两次形状不一致，所以"像收敛"这个读法本身就不可靠**；线性外推 ~+5 MB/15 min ⇒ 约 **2.5–3 h** 撞 10% 预算 ⇒ **"长时间会话不长"没有被证明**。**两个平凡解释已排除（读代码，不是猜）**：① 不是 soak 自己攒记录（留存千字节级：`firstErrors` ≤5、281 个 cycle 时间、281 个 RSS 样本、~2782 个 per-page 计时）② 不是语料缓存边跑边填（`readCorpus` **L124 一次性读入全部 PNG**、`corpora` **L140 在循环前**建好，页位图每次用时 `decodePNG`（L93）现解即弃）⇒ 残差只剩"V8 在 ~350 MB/cycle 分配速率下高水位漂移"或"core 里真有 ~6 KB/页留存（17 MB / 2782 页）"⇒ **60 min 分不开这两者，两个都不主张**。**决定不拉长到 3 h**：判据那一档（30 min）在 30 与 60 min 都过，为一个判据已回答的问题再花 3 小时机器时间属于过度设计（用户口径"刚好就行"）⇒ 记为**观察**并留线索：将来若有人报告多小时会话内存上涨，从这里查 |
| D58 | **网页发送端的「PNG」按钮对每页发一次自动下载 ⇒ 浏览器会拦掉批量下载，用户很可能只拿到前一两个文件，而日志却声称全部下载了**：`web/sender.js` 的 `dlpng` 处理原本是 `for (const p of state.pages) give(p.png, …)`，即**一次点击触发 N 个 `data:` URL 自动下载**；浏览器（Chrome/Edge/Safari）在头几个之后就会拦下并弹"允许此站点下载多个文件？"⇒ 一次 168 页的传输可能只落地 1–2 个 PNG，而页面日志写的是"**N 张 PNG 逐个下载**"——**一句本代码无法核实的陈述** ✗ 这正是本项目最不允许的"静默的部分成功"：用户看着 Downloads 里两个文件，无从知道另外 166 个被丢了。原注释只说"浏览器会问很多次"，**没说可能被拦掉**。同页还有一处相关的量级问题：`doprint` 把**每页**当 `data:` URL `<img>` 写进一个新窗口再 `window.print()` ⇒ 每页 ~0.5 MB base64、解码后 ~30 MB 位图，168 页就是 D56 那个量级的开销**从打印这条路回来**，而 `window.print()` 没有惰性路径可用 | 第 67 轮修完 D57 后顺着"桌面发送端用户第一眼会碰的东西"读完 `web/sender.js` 的 DOM 段（L236-352）发现。**独立读取器对拍**（那个 handler 在 `document` 守卫后面、进程内点不到 ⇒ 这就是可自动化范围内的等价证据，用的是 G8 验 3MF/STL 的同一套纪律：交给第三方实现读）：`tools/sender-zip-check.mjs`（**入库的只读量具**，不是门限；它跑的正是 handler 里那条表达式）造归档 ⇒ `pages 5; zip 1832839 B; magic PK`，我方逐条 sha256；`python -c "import zipfile,hashlib; …"` ⇒ **`entries 5`、`testzip() = None`（每条 CRC 都过）、`compress_type=0`（store ✓ 对已压缩的 PNG 是正确选择）、5 个 sha256 与我方逐个一致**（如 `page-000.png 373254 B a4c6b3aa40ddb02f…`）⇒ 独立实现认可这个归档 ✓ **另：构建守卫当场抓到我一次** —— 第一版图省事写了 `await import('./core/mesh/threeMF.js')`（照 `web/capture.js` 的样子）⇒ `tools/build-web.mjs:73` 直接红：`web/sender.js: dynamic import() is not supported by the web bundler (make it a static import)`、`BUILD_EXIT=1` ⇒ 守卫是对的（而且那个 `'./core/…'` 说明符在源码树里也是错的，源码树里是 `../core/`）。**差点被绿字骗过去**：构建红的那一次 `check-dist` 仍报 `13 pass / 0 fail`、`G9 CHECK: all 13 assertions pass`，因为它跑在**上一次成功构建的 dist** 上 ⇒ 那份绿不是本次改动的证据（"只看 exit code"这条规矩这次救了我） | **FIXED ✓（第 67 轮）** ① `dlpng` 改成**一个 zip、一次下载**：`buildZip(state.pages.map(p => ({name: p.tag+'.png', data: p.png, method:'store'})))` ⇒ zip 写器是**本仓库自己的**（`core/mesh/threeMF.js`，3MF 就是它打的、且早已被第三方 stdlib 解析器读通）⇒ **零新依赖**；日志改成**能核实的陈述**（条数 + 字节数），不再声称发生了它无法观察的下载 ② `web/send.html` 的按钮标签 `PNG` → **`PNG（zip）`**（界面第一眼就说实话）③ `buildZip` 走**顶部静态 import**（L33 本来就在从同一模块导入 `encode3MF, selfCheck3MF`），并在 handler 注释里写明**别改回惰性导入**、以及那次构建守卫是怎么红的 ④ `doprint` **不拒绝**（几页时它是对的、按 CSS 声明的物理尺寸打印），但**超过 8 页（≈240 MB 解码位图）就明说代价并指向 pack.pdf**（`send.html` 本来就有"打印请优先用 pack.pdf"的 notice ⇒ 两处口径一致）⇒ 最终版重跑：`BUILD_EXIT=0`（`pskt-file.html` 306.4 KiB、precache 53）、`CHECKDIST_EXIT=0`、`13 pass / 0 fail`、**`G9 CHECK: all 13 assertions pass`**、`SMOKE_EXIT=0`（`SENDER SMOKE: all assertions pass`）、`SUITE_EXIT=0`（**`tests 307 · pass 307 · fail 0 · duration_ms 131458`**）✓ **同轮查出但未修（如实挂账，带口径）**：编码完成后 DOM 仍给**每页**建一个 `<img src="data:image/png;base64,…">` 预览（`sender.js` L297-306）⇒ 168 页 ≈ 79 MB base64 串 + `state` 里的 PNG ≈ 59 MB；桌面浏览器能承受、**手机发送端会难受**（`docs/USE.md` 说发送端在手机上"也能开"）⇒ 它**不是静默失败**（页都在、字节都对），本轮没动；下一轮的最小修法 = 预览截到前 N 页并明说"还有 M 页未预览"，不改任何产物 |
| D57 | **手机连拍节的取景画面接错了元素：`web/index.html` 声明了两个 `id="video"`** —— 第 1 节单拍的 `<video id="video" playsinline hidden>`（L33）与最后一节连拍的 `<video id="video" playsinline muted>`（L82）⇒ 同一文档里重复 id，而 `document.getElementById('video')` 只返回**文档顺序中的第一个** ⇒ `web/capture.js` 的连拍路径拿到的是**那个隐藏的单拍元素**：**用户在连拍节看得见的那块画面永远黑屏**，而帧是从一个 `display:none` 的 video 上 `drawImage` 取的 ⇒ 这是浏览器相关行为（**iOS Safari 对不渲染的 video 是否继续解码并不可靠**）⇒ 手机路径（目标 ②）最坏情况是"画面全黑、一页也收不到"，而**在桌面上永远看不出来** ✗ 重复 id 同时存在于**单文件版** `pskt-file.html`（构建时由 `index.html` 生成）⇒ 两条交付路径都带着它 | 第 67 轮读 `web/app.js`（查 D56 那一类内存问题在接收端是否存在 ⇒ 结论：**不存在**，`run()` 的 `bmp` 是循环局部、`files` 是磁盘惰性的 File 句柄、单拍路径还先 `files.length = 0`）时顺手读到 `index.html` 的两个 `id="video"` ⇒ **阳性对照是免费的**：当时的 `web/dist` 还是修前构建的，于是**先不重建**直接跑检查 ⇒ `CHECKDIST_PRE_EXIT=1`、`FAIL no built page declares the same id twice`、detail **`./index.html: video x2, ./pskt-file.html: video x2`**、`G9 CHECK: 1 of 13 FAILED` ⇒ 缺陷确实在**出厂产物**里、且新检查抓得住（**不是空跑**）✓ 此前**没有任何检查扫重复 id**：既有那条只查"JS 里每个 `getElementById` 都能在**某个**页面找到"，它把各页 id 混进一个 Set、**只看存在不看唯一** ⇒ 这类"元素在、但取到的是另一个"的静默失败无人管，而唯一能暴露它的证据（真手机跑连拍）**正是本项目在进程内造不出来的那一种** | **FIXED ✓（第 67 轮）** ① 连拍节的元素改名 `id="burst-video"`，`web/capture.js` 改取它，并在代码里写明**为什么两个 id 不能被"顺手统一"**（防止被当冗余改回去）② `tools/check-dist.mjs` 新增**第 13 条**断言 `no built page declares the same id twice`（**逐页**统计 id 出现次数、>1 就红并报出页名与次数）⇒ 重复 id 从此是**构建期强制**，与图标那条同档次 ③ 重建 dist 后复验：`every id unique within its own page, across 4 built page(s)`、`13 pass / 0 skipped / 0 fail`、**`G9 CHECK: all 13 assertions pass`**、`CHECKDIST_EXIT=0`、`BUILD_EXIT=0`（precache 53）✓ 单测 **`tests 307 · pass 307 · fail 0 · duration_ms 130716`**、`SUITE_EXIT=0` ✓ `smoke-capture` **all assertions pass**（4 帧 / 3 收 / 1 重复 / 0 拒）✓ ④ `docs/USE.md` §5 的 G4 行点名该按哪个按钮（拉到最下面「手机连拍（自动挑帧）」按「**开始连拍**」，**不是**第 1 节那个「用摄像头拍一页」——那是一次一页的手动快门，500 页要按 500 次 ✗）⇒ 用户照手册走 G4 不会走错路 ✓ **仍未验证的部分（如实）**：真手机上的取景与自动挑帧仍只有你能验（G4）——本轮修掉的是"接错元素"这个**确定的错**，**不等于连拍在真机上已达标** ✗ |
| D56 | **网页发送端把每页 RGBA 位图全程留在内存里，代价随页数线性增长 ⇒ 手册宣称支持的传输量在浏览器里装不下**：`web/sender.js` 的渲染循环 `pages.push({tag, bitmap, png, …})` 留住每页码区位图（A4@300 dpi ≈ **31 MB/页**），随后 `encodePDFDocument(pages.map(p => p.bitmap))` 又要**一次性拿到全部位图**；而 `core/render/pdf.js` 内部先 `list.map(...)` 把每页 `pixels` 的引用存进 `pages[]`、再 `pages.forEach(...)` 编码 ⇒ 峰值 = 所有位图 + 所有已编码流。而 `docs/USE.md` 告诉用户纸面档最多 **255 页**（≈1.9 MB），`docs/PLAN.md` L106 的 G6 判据自己把"编码 1MB"当普通用例（**168 页**）⇒ 文档宣称的容量与网页端的实际容量差两个数量级：一个 **256 KiB 的普通文档就要 1.34 GB**，1 MB 要 ~5 GB，255 页要 ~8 GB ⇒ 标签页必死、手机更早死 ✗ 更隐蔽的是**只盯 JS 堆永远发现不了**：位图在 `arrayBuffers`（外部内存）里，而 `heapUsed` 全程只有 **5 MB** | 本轮新增只读量具 `tools/sender-memory-probe.mjs`（同一命令修前修后各跑一次 ⇒ 结论是量出来的，不是照注释算的）：`--bytes 65536` ⇒ 11 页、rss **436 MB**、arrayBuffers **420 MB**、`rasters still referenced by the returned result: 11 of 11`、`heapUsed 5.0 MB`；`--bytes 262144` ⇒ 42 页、rss **1343 MB**、arrayBuffers 1300 MB、**42 of 42**、**每页 30.6 MB** ⇒ 按实测每页代价线性外推：168 页 ≈ 5.2 GB、255 页 ≈ 7.9 GB。顺带查出 `tools/smoke-sender.mjs` L61 仍在直接调 `asm.feed`（**第 63 轮仲裁喂页漏掉的第八个调用点**），且它的盲解码喂的是 `p.bitmap`（裸码区），不是用户真正会打印的那张 PNG | **FIXED ✓（第 66 轮）** ① `core/render/pdf.js`：`encodePDFDocument` 改成**单趟消费惰性序列**（数组或任何可迭代对象都收），每页对象发出后**不再引用它的 pixels**，`/Kids` 由轻量 `kids[]` 构建 ⇒ 发出对象的顺序不变、**产物逐字节相同**（对拍 `pack.pdf` sha256：64 KiB `b6908642072c1ec4`、256 KiB `a61aa12fc4121bf0`；page-000 PNG sha256 `4c81684db5c2862f` / `b64aea45b9790cf2` ⇒ **修前修后完全一致** ✓）；空列表的拒绝由"编码前"移到"序列取完后"，错误与消息不变、仍然不出文件 ② `web/sender.js`：改为**边 yield 边把 `p.bitmap` 置 null**（位图只是中间产物，用户下载的是 PNG 与 pack.pdf）⇒ 峰值 = 一页位图 + 已编码流 ③ `tools/smoke-sender.mjs`：盲解码改解 **`p.png`**（用户真正打印的产物，页边距与套准/裁切标记一起进判据 ⇒ **更强、不是放宽**），并补上 `feedPageWithRecalibration`（含 `{geom}`）⇒ 实测同一命令：64 KiB rss **436 → 181 MB**（结果里留住的位图 11 → **0**）、256 KiB rss **1343 → 160 MB**、arrayBuffers **1300 → 92 MB**、每页 **30.6 → 2.4 MB**（剩下的主要是 PNG/PDF 产物本身，那是用户要下载的东西）⇒ **内存不再随页数膨胀** ✓ 重建 dist 后 `check-dist` 13 pass + `all 12 assertions pass`、exit 0 ✓ |
| D54 | **G6 的性能判据实测超预算 4.4 倍，而原因是候选几何按字母序试、不是按可能性**：`docs/PLAN.md` L106 要求"解码 ≤2s/页"，实测真实信道 300 dpi 页 **8713 ms/页**（pristine 7180 ms）⇒ 168 页的一次传输要 ~24 min ✗ 用户端影响具体：网页接收端与手机连拍走的都是**无 manifest 提示**的 `bootstrapDecode`，所以这就是"用户拖一张扫描进去"与"手机每取一帧"要等的时间 | 本轮新增只读分解量具 `tools/g6-perf-probe.mjs`：`node tools/g6-perf-probe.mjs .tmp/sc-scan300-10/page-000.png .tmp/g2src-a4-300/page-000.png` ⇒ `readFileSync 4 ms + decodePNG 393 ms + bootstrapDecode 8316 ms`、`attempts=4`，三次白跑的整页读各 **2365/2407/2391 ms**、全部死在 `stage=readout reason=echo-bad-magic`，候选是 **`P-C4-600@600/{INK2,INK4,PAPER1}`**，而赢家是 `P-M1-300@300/INK2` ⇒ `candidatePlans()` 先按 medium(paper) 再**按字母**排 ⇒ `'P-C4' < 'P-M1'` ⇒ 一个 4 色 600 dpi 档排在常用 300 dpi 档前面，而 `core/decode/bootstrap.js` 文件头声称 "candidates are ordered by likelihood" ✗ 另测得：**PNG 解析不是瓶颈**（`decodePNG` 只占 393 ms / 9.8 MP ⇒ 别去优化 inflate） | **FIXED ✓（第 65 轮）** 修法 = **用位图自身隐含的码区尺寸给候选打分再排序**（`abs(ln(bitmap.w/layout.w)) + abs(ln(bitmap.h/layout.h))`，稳定排序、原次序作 tie-break），**只重排、不删除任何候选**（这是该文件自己的规矩）⇒ **判决不可能变**：候选要赢必须让页头自报的 profile/nozzle 与该候选一致（交叉校验 L143-152），所以无论先试谁，"第一个可能被接受的候选"是同一个 ⇒ 变的只有毫秒 ✓ 实测：真实信道 300 dpi 页 **8713 → 1467 ms**（`bootstrapDecode` 8316 → **1070 ms**、尝试 **4 → 1**）⇒ **进 PLAN 的 2 s 预算** ✓ pristine 页 **7180 → 952 ms** ✓ 出厂 bundle 侧独立确认（重建 dist 后 `check-dist` 的无提示解码）**candidate tries 12 → 3**、摘要仍对、`all 12 assertions pass`、exit 0 ✓ **600 dpi 侧仍 31786 ms/页、仍 3 次白跑（22.2 s）**：同 dpi 的 `P-C4-600` 与 `P-M1-600` **码区尺寸相同 ⇒ 尺寸信号分不开**、稳定排序回落字母序 ⇒ 再降需要第二个廉价信号（如按画面色彩数排序），而 600 dpi 是用户第 63 轮判定**非必要**的路径 ⇒ 本轮不做 ✓ **未跑**：G2 的 200 份语料重跑（≈20 min）⇒ "重排不改判决"目前是**论证 + 6 页实证**、不是 200 份语料的实证 ⇒ 列为下一轮第一件事 ✓ 量具里一并修了我自造的歧义输出（原先把最后一条**失败**尝试标成 `<== ACCEPTED`，与同屏 `winner=P-M1-300@300/INK2` 自相矛盾 ⇒ 第 63 轮同类错**第 2 次**：歧义的成功输出就是错的输出） |
| D55 | **产品解码路径会抛未捕获异常，而"抛"不是"拒绝"**：`bootstrapDecode` 里每个候选的 `planPage`/`pageLayout`/`getPalette` 都被 try/catch 包住并记成失败尝试，**唯独 `decodePage`（原 L128）没有** ⇒ 某个候选几何让 readout 抛 `RangeError: joinCellLevels: colour level 2 out of range 0..1`（`core/decode/ideal.js:353`）时，异常**逃出 `bootstrapDecode` 落到调用方** ✗ 在浏览器与手机连拍里这等于**接收过程中崩掉**（不是干净拒绝），而本项目契约是"解码或干净拒绝"⇒ 猜错一个档只该花时间、不该花掉整次会话 | 修前跑 `node tools/g6-perf-probe.mjs .tmp/sc-scan600-11/page-001.png` ⇒ 进程以未捕获 `RangeError` 退出（`PROBE_EXIT=1`），栈：`joinCellLevels ← readPageIdeal(ideal.js:353) ← readFast(page.js:63) ← decodePage(page.js:47) ← bootstrapDecode(bootstrap.js:128)` ⇒ 触发者是候选 **`P-C4-600@600/INK4`**（错档错色板：在该候选的色板下量出的色阶超出它自己通道的级数）；300 dpi 页不触发（候选组合不同 ⇒ 这条一直在，只是没被走到） | **边界已修 ✓（第 65 轮）· 根因已查清并修 ✓（第 70 轮 ⇒ D64）** 修法 = 把 `decodePage` 也纳入每候选的 try/catch，异常记成一次失败尝试后**继续搜索**；`stage:'decode'`、`reason:'fail'` **复用已有字面量**、消息放独立字段 `threw` ⇒ 复用是有意的：`tests/unit/advice.test.mjs` 把 core 里每个 reason 字面量当成"必须能告知操作者的条件"，而"搜索中某个候选抛了、搜索继续"不是新的操作者条件（第 63 轮为两个**真新**条件加过 ADVICE 条目，这次不该加）✓ 实测同一页现在**不崩、正常判定为 `P-M1-600@600/INK2`**，抛出的候选被如实记录：`[1] P-C4-600@600/INK4 stage=decode ms=6335 reason=fail THREW: joinCellLevels: colour level 2 out of range 0..1` ⇒ 端到端 31786 ms、`ok=true` ✓ ~~**根因仍 OPEN**~~ ⇒ **第 70 轮已查清并修，见 D64**。当时那三个问题现在都有答案：① "`readPageIdeal` 为何算出越界色阶" = `nearestInk`（`ideal.js` L417-428）返回的是 **palette 里的下标**（`0..pal.inks.length-1`），而 `joinCellLevels`（`protocol.js` L63-73）按 **geom 的通道档数**校验，`planPage`（`profiles.js` L107-164）又**根本不吃 palette**（opts 只有 nozzle/sheet/plateMm/marginMm/parityPct，`levels` 直接来自 profile 声明 L159-162）⇒ **两个数从无人核对**；② "该钳位？该按不可测处理？" = **都不对**：钳位会把"量到第 4 种墨"谎报成"第 2 档"（正是本仓库禁止的"看起来成功但是错"），按不可测处理会丢掉一个本来读得出的格子 ⇒ 正解是**只在"该通道能寻址的墨"里找最近**（与编码器 `pal.inks[level % inks.length]` 严格对称：`palette.js` L68、`render/raster.js` L144、`mesh/plate.js` L300；且墨数 ≤ 档数时是**空操作**）；③ "需要一条能在正确候选下触发它的正例" = **当时没有、现在有了**：`--palette` 是**用户可达**的（`cli/pskit.mjs` L40 帮助文本明写 `INK2 INK4 PAPER1 (default chosen by profile)`、L131 显式参数优先；`web/sender.js` L366 有色板下拉框、L167 `pickPalette(profileId, opts.palette)`）⇒ 用户完全可以印一张 `P-C4-600 + INK4`（2 档 colour × 4 墨）的页，编码器只会用到 ink 0/1 ⇒ **这时那个候选不是"错档"、是正确档**，而只要有一个格子因渗墨/模糊/光照量得离 ink 2 或 ink 3 更近，就会抛同一个异常 ⇒ 第 65 轮"不猜着改判决路径"在当时是对的（确实没有证据），第 70 轮改它也是有据的（有了一条合法且用户可达的组合）；**当时那句"目前只在错候选下出现"因此是低估，如实更正** ✗ |
| D53 | **照 `docs/USE.md` §0 从全新检出走，第二条命令就红**：`node tools/check-dist.mjs` 的最后一项断言依赖 `.tmp/g2src/manifest.json` —— 那是**只有我这棵树才有的历年轮次产物**（`.tmp/` 被 `.gitignore` 忽略）⇒ 真用户 clone 完、构建成功（`build-web` **exit 0**），却在"校验产物"这步拿到 **exit 1** 与 `G9 CHECK: 1 of 12 FAILED` ✗ 而手册同一段正写着"判'构建好没好'**只看 exit code**" ⇒ 用户会得到"**我的构建坏了**"的错误结论（产物其实是好的）。同一条断言还**漏走了第 63 轮的仲裁喂页**（它直接调 `asm.feed`）⇒ 这条检查量的是产品的**表亲**、不是产品本身 ✗ 而上轮的调用点清单里 `tools/check-dist.mjs` **就在名单上、我没改到** | ① 用 `git ls-files` 把 **123 个被跟踪文件**复制成 `.tmp/fresh64`（等价全新 clone：无 `web/dist`、无 `.tmp`；`git clone` 本地路径在本沙箱会因 git spawn `sh.exe` 报 `couldn't create signal pipe, Win32 error 5` ⇒ 改用不 spawn sh 的等价做法，不绕沙箱）② 在该树里按 §0 字面跑 `node tools/build-web.mjs` ⇒ **exit 0**（55 文件、29 模块、precache 53）③ `node tools/check-dist.mjs` ⇒ **exit 1**、`FAIL bundle decodes on-disk pages unhinted, digest matches manifest`、detail `.tmp/g2src has no manifest.json (make one with pskit send)`、`G9 CHECK: 1 of 12 FAILED` ✓ ④ **对照**：同一棵 fresh 树按 §4 跑 `& .\tools\usability.ps1` ⇒ **11 项全过、297 s、exit 0、字节相同**（sha256 `32ec4805…` 两侧一致）⇒ 坏的只是这一条断言的**夹具来源**，不是产物、也不是文档的其余步骤 | **FIXED ✓（第 64 轮）** 修法**不是"跳过"**（那会掏空唯一一条"出厂 bundle 真能读盘上 PNG"的断言，而本文件头部注释明写"全跳过的自检不算通过"），而是**让断言自带夹具**：manifest 缺失时用本仓库自己的编码器进程内生成（`encodeTransfer` 20480 B **确定性**填充（不用 `Math.random`，否则判决不可重复）→ `pageLayout(geom,300)` → `renderPageBitmap` + `renderSheetBitmap`（整张纸、与打印产物同形）→ `encodePNG` 写 PNG → 写 `manifest.json` 含 `sourceSha256`），并在记录行**明说来源**（`[fixture generated in-process this run: a fresh clone has no .tmp]`）⇒ 不让人误以为用的是既有语料 ✓ 夹具用 **core** 造、解码仍用 **bundle**（`R(...)`）⇒ 不让被测产物自造输入；core 与 bundle 若不一致，正好在这条断言上暴露 ✓ 同时把喂页换成**从 bundle 取的** `feedPageWithRecalibration`（遵守该断言自己的自律条款"只用产物暴露的东西"，顺带证明产物里真有这个模块 ⇒ 少了它就红）✓ **三次验证**：① fresh 树夹具缺失 ⇒ `G9 CHECK: all 12 assertions pass`、**exit 0**、记录行带 generated 标注、`3 page(s), 12 candidate tries, geometry self-identified as P-M1-300@300/INK2, 20480 B, digest matches` ② fresh 树夹具已在 ⇒ 同样全过、**不带** generated 标注（两条分支都验过）③ 本树（`.tmp/g2src` 原有 204800 B 语料）⇒ 仍 `all 12 assertions pass`、exit 0、`204800 B` ⇒ **老路径无回归** ✓ **教训**：`tools/` 里任何"读 `.tmp/…` 当输入"的检查，都是在把**我这棵树的历史**当成用户环境的一部分 ⇒ 判据的夹具必须自带，否则"绿"只在我这台机器上成立（**D42 同族**：把此处的偶然条件当成通用前提）；而发现它的方式是**照用户手册字面走一遍全新检出**，不是读代码 ✗ 另：本轮同时更正了 `STATUS.md` 里三处过期记账（里程碑表 M4/M5/M7/M8、门限表 G8 行仍写"D41 OPEN"而 D41 第 39 轮已闭）⇒ 过期台账会让下一轮去做已经做过的事 |
| D52 | **两个被注释当作"机制诊断量"的读出价实测是自证 / 失明的 ⇒ 会把下一个修 D51 的人引到错的因果上** ✗ 第 62 轮实测（`tools/rho-report.mjs`）：① `readPageIdeal` 返回的 `inkBalance.ratio = rhoSum/targetSum`，**分母用的是"本格被选中的电平"的 target** ⇒ 它被自己的判决污染：**塌陷页反而最低**（`sc-scan600-11` p1 = `3.5173`、p2 = `2.7096`、`sc-scan600-10` p1 = `3.2929`），健康页高得多（`sc-scan600-11` p0 = `20.6970`、`sc-scan600-12` p0/p1 = `11.5710`/`14.0481`、300 dpi 侧 `4.2302`–`7.4930`）、pristine = `1.0197`–`1.0657` ⇒ **跨页不可比**；而 `core/decode/ideal.js:300-305` 的注释正把它当"bleed 诊断量"、并据此断言机制 ✗ ② `ratioDisagreements`（比值路径与匹配滤波的分歧计数）**看不见塌陷**：`ideal.js:275` 只在两条路径**都表态**时才计数，而塌陷页的比值路径已把 0-like 说成 `1`、把 1-like 说成 `null`（ρ 超 blob ⇒ null）⇒ 塌陷页是 **0**，健康页反而是 `260405`/`259154`/`41698` 这种大数 ⇒ **数值大 = 正常、数值 0 = 可能已经塌了**，与直觉相反 ✗ | `node tools/rho-report.mjs .tmp/g2src-a4-600 .tmp/sc-scan600-11 0,1,2 --no-cross-check` ⇒ 把 `inkBalance.ratio`、`ratioDisagreements` 两列与 `final level error`（p0 = 0.000% / p1 = 90.429% / p2 = 87.032%）并排看 ⇒ 两个"诊断量"与真实成败**反相关或无关** ✓ 零退化对照 `node tools/rho-report.mjs .tmp/g2src-a4-600 .tmp/g2src-a4-600 0 --no-cross-check` ⇒ ratio `1.0657`、disagreements `0`、error `0.000%` ⇒ 即 `disagreements = 0` 在 pristine 上是**好**的、在塌陷页上也是 `0` ⇒ **该量无法区分两者** ✓ 修好后要求：`ratio` 的分母与本页判决无关（或改名并在注释里写明"跨页不可比"），且存在一个**不依赖双方都表态**的塌陷探测量（塌陷页上必须显著、健康页与 pristine 上必须近 0） | **FIXED ✓（第 63 轮：只动报告量与注释，判决一行未动）** ① `core/decode/ideal.js` 新增**与判决无关**的量：`inkBalance.rhoMedian`（本页可测格的 ρ 中位数；单调于渗墨、**跨页可比**：pristine `0.0021` → 300 dpi 信道 `0.59` → 600 dpi 健康 `0.62` → 600 dpi 塌陷 `1.27`）、`shapeMonoFrac`（最终 shape 电平的众数占比 ⇒ **塌陷页 = 1.0**，这才是塌陷探测器）、`ratioNulls`（比值路径判 null 的格数）、`nanCells`、`shapeCounts` ② `ratio` **不改名**（改名会打断 `tools/rho-report.mjs` 与既有台账的引用），但注释改成实测结论：分母用"被选中电平"的 target ⇒ **跨页不可比**、塌陷页反而最低 ⇒ 该看 `rhoMedian`/`shapeMonoFrac`；原注释里"bleed 使整页读成 level 1"的**机制结论保留**（它是对的），另注明"但这个 ratio 分辨不出哪一页塌了" ③ 每格 ρ 与色彩判决以**紧凑数组**留下（`rho: Float32Array` + `colourLevels: Uint8Array`，600 dpi ≈1.4 MB；而 `cells` 对象 ~260 MB ⇒ 只有数组离开这个函数）；色彩判决**提前到 shape 判决之前**做（重读要给每个它重判的格一个色彩、不能凭空造），但 `inks.push`（`colourAlive` 的证据）**范围一字未动** ⇒ 因为 `colourAlive` 是判决不是报告，扩大范围就等于改判决 ✗ ④ `core/decode/page.js` 透传 `rho`/`colourLevels`/`ratioNulls`/`shapeMonoFrac` ⑤ "判决未动"不靠自称：**G5 原 10000 试验的配比与 PRNG 序列没碰** ⇒ 数字与守卫分布与第 57/58 轮**逐字相同**（`9200/800/0`）✓；全量套件 **307 · 0 失败** ✓；usability 冒烟全过（字节相同）✓；`check-dist` 13 PASS 含"bundle 无提示解码磁盘页、摘要与 manifest 相符"✓ ⑥ 定稿时点在 G2/G5 重跑**之前**（本条要求的顺序 ✓）⇒ 门限数字与诊断量没有一起变。**以下为第 62 轮的 OPEN 原记录**： **不直接挡用户**：两者都只是"报告量"、不参与判决 ⇒ 不会造成误接受 ✓ 也不会丢数据；但它**挡下一个修 D51 的人**——照注释把 `inkBalance.ratio` 当健康度阈值、或把 `ratioDisagreements` 当塌陷探测器，都会做出错的判据（而且方向是反的 ✗）⇒ 修法（小、且**不动判决**）：① `ratio` 的分母换成与判决无关的参考（例如本页 ρ 两簇的实测均值 —— `tools/rho-report.mjs` 已经在算），或改名 `ratioVsChosenTarget` 并在注释写明"跨页不可比"；② 给 `ratioDisagreements` 补一个**不依赖双方都表态**的量（例如"比值路径判 null 的格数"与"MF 判为单一电平的格数占比"，后者才是塌陷探测器）；③ 同步改 `core/decode/ideal.js:294-305` 的注释（它现在说"bleed 使整页读成 level 1"⇒ **方向对、量不对**：bleed 确实是机制，但这个 ratio 分辨不出哪一页塌了）⇒ 因为①②③只动"报告量与注释"、不动判决，可与 D51 的修法同轮做，但**必须在 G2/G5 重跑之前定稿**，否则门限数字与诊断量一起变、事后分不清是谁的功劳 ✗ |
| ~~D50~~ | **CLI 的 `render` 行报码区尺寸，而盘上的 PNG/TIFF 是整张纸** ✗ 与 D46 同族（工具自报的形状 ≠ 交付的形状），第 60 轮修 D46 时顺手量到：`send .tmp/d46-tiny.bin --profile P-M1-300 --format png` 打印 `render 2260x3290px @ 300dpi`，而 `.tmp/d50-300/page-000.png` 的 IHDR 是 **2480×3508** ⇒ 差 220×218 px；600 dpi 侧打印 `4530x6590`、盘上是 **4961×7016** ⇒ 差 431×426 px ⇒ 用户拿 stdout 的数字去核对打印件，会以为文件坏了或按码区尺寸裁纸 ⇒ 与 D45 之后的交付形状不一致 | `node cli/pskit.mjs send <小载荷> --profile P-M1-300 --format png --out DIR`，比对 stdout 的 `render` 行与 `DIR/page-000.png` 的 IHDR 宽高（字节 16..23、大端）⇒ 修好后两者必须**逐位相同**；600 dpi 侧同法（`--profile P-M1-600` ⇒ `4961×7016`）；板材档（无 sheet）该行必须**逐字不变** | **CLOSED ✓（第 60 轮，与 D46 一起修）** `cli/pskit.mjs`：`render` 行同时报两者 ⇒ `render 2260x3290px code area on a 2480x3508px sheet (210x297mm) @ 300dpi, printed area 24.2%`；sheet 像素由 `round(sheetMm/25.4*dpi)` 算出（与 `core/render/raster.js` 同一套取整规则 ⇒ 600 dpi 侧得 **4961** 而不是 4960 ✓）；无 `sheetMm`（板材档）⇒ 该行与旧输出逐字相同 ✓ **实测（阳性对照：不看它自己怎么说，去读盘上的文件）**：300 dpi ⇒ stdout `2480x3508px sheet`、盘上 PNG IHDR **2480×3508** ✓ 相同；600 dpi ⇒ stdout `4961x7016px sheet`、盘上 **4961×7016** ✓ 相同；两次 `exit 0` ✓ 另：全量套件 **301/301 · 0 fail**、`build-web` `exit 0`、`check-dist` **13 PASS / 13 pass · 0 skipped · 0 fail / G9 CHECK all 12** 均 `exit 0` ✓ **为什么没有单测**：`cmdSend` 未导出、`cli/` 全仓没有测试挂点 ⇒ 本轮以可执行证据（stdout ⇄ IHDR 对拍）结案，**没有假装它已被测试覆盖** ✗ 若将来给 CLI 加挂点，这条应升级为自动断言（记在 STATUS 第 60 轮的结构性欠账里 ✓） |
| D49 | **600 dpi 纸面档实测不达标（162/200），而文档与 CLI 帮助把它和 300 dpi 并列成等价选择** ✗ 第 58 轮 G2 判决（前提与 300 dpi 侧完全对称：整张 A4 交付形状 `4961×7016`、同一载荷 sha256 `91ab3f7310da452b`、`--preset scan600 --modifier nocrop`、200/200 生成 `exit 0`）：`verify --gate G2 --root .tmp --match 'sc-scan600-*'` ⇒ **`FAIL G2 corpus: 162/200 byte-exact in 4359.7s`** · `GATE FAILURE` · **exit 1** · `failure classes: short 0/1 x37, no-page-header x1`；**页级失败 342/600 ⇒ 直读仅 43%**（300 dpi 侧 97.83%），162 份 OK 里 **145 份也靠页间 RS 兜页**；原因分布 `assemble/intra-fail` × 210、`readout/echo-bad-magic` × 78、`markers/no-hollow-corner` × 41、`readout/echo-header-crc` × 12、`readout/echo-version-3` × 1 ⇒ **两处历史结论被推翻/改写**：① "600 dpi 那 2 例失败是夹具没有纸边造成的"**不成立**（交付形状 + `nocrop` 下照样 41 页 `no-hollow-corner`）✗ ② 第 19 轮记为"容量事实、不是 bug"的 A 类（页内 ECC 余量薄），**在全样本量下的代价是 19% 的载荷拿不回来** ⇒ 它确实不是"静默出错"（误接受仍为 **0** ✓ 38 份全是拒绝/缺页、没有一份交出错误字节），但**是可用性缺陷** ✗ 而 `docs/USE.md` §3 此前写"扫描：300 或 600 dpi"、`cli/pskit.mjs` L24 的 `--profile` 帮助把 `P-M1-600` 与其它档平等列出 ⇒ **用户照文档选 600 dpi 就有约 1/5 的概率拿不回文件** | `node cli/pskit.mjs verify --gate G2 --root .tmp --match 'sc-scan600-*'` ⇒ **exit 1**、打印 `162/200`；对照 `--match 'sc-scan300-*'` ⇒ **exit 0**、`200/200` ⇒ 两侧同载荷、同信道口径、同渲染器，差别只在 dpi/档 ⇒ **判据没放宽**（门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200` ✓）。文档侧复验：`docs/USE.md` §3 现在必须写"用 300 dpi"、并明示 600 dpi 实测 81% ⇒ 否则用户仍会被引向会失败的那一档 | **OPEN ✗（第 58 轮发现；第 60 轮修掉"用户暴露面"；第 61 轮量到根因 ⇒ 修根因仍 OPEN）** **第 63 轮更新（D51 修法落地 + 用户判定范围）**：① 根因（电平判读塌陷）已按第 62 轮写死的形态修掉 ⇒ 见 **D51 = FIXED ✓**；G2 **300 dpi 侧重跑 `PASS 200/200 byte-exact in 1245.4s`、`exit 0`**，G5 重验 **0 误接受**且新增接缝试验 2000 次 **0 误接受** ② **600 dpi 侧的最终判决没有取**：重跑进行到 **52/200（51 OK / 1 FAIL）**时**由用户中止** —— 用户第 63 轮明示「其实没必要 600dpi，只要能够读到就行」⇒ 这是**产品负责人对范围的判定，不是我放宽判据** ✗ 判据一字未改（G2 仍按 100% byte-exact、失败页仍全进分母、三角点拒绝未动）③ 中间普查值得记：旧跑 210 次的 `assemble/intra-fail` 在这 52 份语料里出现 **0 次**，只剩读出前的 `echo-bad-magic 19` / `no-hollow-corner 12` / `echo-header-crc 5` ⇒ 修法命中的正是塌陷类，那三类是别的通路、本修法不覆盖 ④ 因此本条改为**不再阻塞"可用"（用户判定 600 dpi 非必要路径）**，但**实测记录保留、不改写成"达标"**：第 58 轮的 `162/200 = 81%` 仍是 600 dpi 在旧读法下的真实判决，而新读法下**没有判决** ⇒ 谁要用 600 dpi，得自己重跑 `verify --gate G2 --root .tmp --match 'sc-scan600-*'` 取判决（约 3 h，本轮实测速率）⑤ 用户暴露面维持第 60 轮的样子：`P-M1-600` **仍可选**、标签仍带 **`⚠ 实测未达标 (D49)`**（`tests/unit/profile-picker-warning.test.mjs` 守着），`docs/USE.md` §3 仍指引用 **300 dpi** ✓ **第 61 轮（根因已测到 ⇒ 详见新建的 D51）**：① **新增量具** `pskit calibrate <dir\|file>`（`cli/pskit.mjs`；`core/` **一行未动**，因为 `TransferAssembler.feed` 本来就在成功路径 `core/protocol.js:489` 与拒绝路径 `:484` 都返回 `intraDecode` 的 stats ⇒ calibrate 只是消费者）⇒ 每页打印 readout 结果、**页内 RS 预算占用** `(2·errors + erasures)/(blocks·nsym)`、阈值无关的着墨面积比（仅健康检查）；**它不是门限**（exit code 不是判决）、**不派生任何阈值**、**不改任何解码判决** ⇒ 因此不必重判 G5/G2 ✓ 帮助文本与函数头都写明了这两条界限，也写明 **PLAN §M7 的 calibrate（MTF 校准板 → 推荐喷嘴/格距）仍未实现** ✗ ② **量出的结论（对照齐全 ⇒ 不是孤证）**：600 dpi 的失败是**双峰**的，不是"贴着余量悬崖"——能读出的页预算占用 **0.0–0.2%**（与 300 dpi 的 9 页全 **0.0%** 同级），坏页则是**整页 141 块全崩且 `errors 0 / erasures 0`** ⇒ **"页内 ECC 余量不足"这个主因假设被推翻** ✗（第 19 轮的"容量事实"、第 58 轮的"主因指向余量"都按此更正 ✓ 我当时标注了"根因仍未定、本轮不下结论"⇒ 现在结论来了：**不是余量**）③ **根因** = 逐格电平判读在部分 600 dpi 页上**整体塌陷到单一电平**（实测：信道页内部 283155 格全为电平 1，原始渲染为 `0=256118 1=27037`；混淆只有 `0->1`；空间平坦；48 个平移都救不回来）⇒ 记 **D51**（含修法方向与第 11 轮那两条护栏）⇒ **加冗余、加样本量都救不了这一类页** ✗ ④ 因此 `ACCEPTANCE` 待办第 1 项（标定）**部分兑现**：量具已落地并产出根因，但**"从实测定阈"那一半仍未做**（且必须过 G5）；待办第 4 项（B 类根因）对 600 dpi 的 `intra-fail` 类**已测到** ✓ 以下为第 60 轮与第 58 轮的原文（其中"主因指向页内 ECC 余量不足"已被 ②③ 推翻 ⇒ 别当现状读）：**OPEN ✗（第 58 轮发现；第 60 轮修掉"用户暴露面"那一半 ⇒ 根因那一半仍 OPEN）** **第 60 轮进展（含一次自我撤回 ✗）**：① **撤回**"网页发送端不提供 600 dpi 纸面档"这句（第 58 轮写进了 `USE.md` §3、`ACCEPTANCE.md`、`STATUS.md` 与本行下面的"未做 ②"）——它的"证据"是 `grep P-M1-600 web/` 无命中，而 `web/sender.js` 的下拉框是 `for (const [id, p] of Object.entries(PROFILES))` **动态枚举全部档位**建的 ⇒ 档位 id 从不以字面量出现 ⇒ **文本扫描能证明"某个串在"，证明不了"动态生成的列表里没有某项"**（正是本项目反复警惕的空转判据，这次栽在自己的台账上 ✗）⇒ **真实暴露面比第 58 轮记的更大：网页用户也能一键选到会失败 19% 的那一档**。② **已修（用户暴露面 = 原"未做 ①②"）**：`cli/pskit.mjs` 对 600 dpi 纸面档在 **stdout 与 manifest note 各给一条警示**；`web/sender.js` 导出纯函数 `isUnqualifiedPaper()`（非板材且 dpi ≥ 600）与 `profileOptionLabel()`，下拉标签加 `⚠ 实测未达标 (D49)` ⇒ **不隐藏该档**（隐藏等于悄悄削能力；且 `pskit calibrate` 未实现前排除不了"差的是能力而不是样本量"），**数字不进 UI 字符串**（与 CLI note 同一原则：将来重测不会让 UI 烂掉）；`web/app.js` 是**接收端**外壳、那个下拉是解码提示而非产出选择 ⇒ **刻意不标注**（标了是噪音 ✓）。③ **守卫测试** `tests/unit/profile-picker-warning.test.mjs` **4/4 绿**（全量套件 **301/301 · 0 fail**、`build-web` 与 `check-dist` 均 `exit 0` ⇒ 已重建 dist、修复真的出货 ✓），含**反空转对照**：flagged 集必须非空、点名 `P-M1-600` 必须在集内、合格档必须**不带**警示（否则警示沦为噪音）、标签里不许硬编码比率、源码必须经 `profileOptionLabel(id, p)` 渲染、警示文案全仓只许出现一次 ⇒ 谁把标签改回手写就会红 ✓ ④ **仍未做（根因侧 ⇒ 本条继续 OPEN）**：`pskit calibrate`（`ACCEPTANCE` 待办第 1 项）与 B 类根因（第 4 项）⇒ 600 dpi 侧真正达标之后才有资格摘掉警示；**不许**为了摘牌而改信道参数、放宽判据或把正确拒绝剔出分母 ✗ 以下为第 58 轮原文（其中"未做 ①②"已被上面 ②③ 兑现、"未做 ②"的**前提本身是错的** ⇒ 别当现状读）：**OPEN ✗（第 58 轮发现；本轮已修文档、未修代码）** **已做**：① `docs/USE.md` §3 改为**推荐 300 dpi（实测 200/200）**、明示 600 dpi 实测 **162/200 = 81%** 与页级直读 43%，并说明在 `pskit calibrate` 落地或该档页内 ECC 余量重做之前**不是可靠路径**；② `docs/ACCEPTANCE.md` G2 段新增第 58 轮全文（页级明细、原因分布、被推翻的归因、以及"根因仍未定 ⇒ 本轮不下根因结论"）；③ `docs/STATUS.md` 现况表 G2 行改为"300 dpi ✓ 100% / 600 dpi ✗ 81% ⇒ 整体未通过、维持 🟡、不上调"（总账的 🟡 计数不变 ⇒ **没有**因为半边绿了就上调 ✓）。**未做（下一轮，与 D46 一起，因为都要动 `cli/`；两个 G2 批本轮都已收工 ⇒ D28 解除 ✓）**：① CLI 在用户选 `P-M1-600` 时给出**实测不达标**的警示（或把该档标成 experimental）；② 加一条**守卫测试**盯着"网页发送端不提供 600 dpi 纸面档"——现在只有我 grep 过一眼、**没有测试**，将来谁把它加回下拉框不会红 ✗；③ 根因侧：`pskit calibrate`（`ACCEPTANCE` 待办第 1 项）与 B 类根因（第 4 项）⇒ **不许**为了让 600 dpi 变绿而改信道参数、也**不许**把正确拒绝剔出分母 ✗ |
| ~~D48~~ | **`STATUS.md` 的门限表被散文段从中间打断 ⇒ 后半张表没有表头** ✗ 第 53 轮由新工具 `tools/check-docs-tables.mjs` 实测：`docs/STATUS.md` 第 52–53 行是叙述段落（不以 `\|` 开头），把第 48–51 行与第 54–63 行切成两个块；第二块**没有 `---\|---` 分隔行** ⇒ 按 GFM 它的首行不是表头，`\| G2 分界（第 9 轮·墙已拆） \| …` 连同 G3–G10 那八行在页面上**根本不是表格** ⇒ 用户去读"门限状态"看到的是散掉的竖线文本 ✗ 同一批检查还发现第 48–55 行散着**未转义的 `\|`**（`--format stl\|3mf` 一类代码跨度）⇒ 其中几行只是**恰好凑够表头格数**才没被"格数相等"这条判据抓到 ⇒ 所以判据必须改成"表格行内代码跨度不许有裸 `\|`"，光数格子会漏 ✓ | `node tools/check-docs-tables.mjs` ⇒ 打印 `NOTE docs/STATUS.md:54: … no ---\|--- separator …`（外加若干 `FAIL … bare pipe inside a code span …`）并 exit 1；修好后裸 `\|` 一类消失、exit code 只剩 D48 那条 NOTE 需要处置 ⇒ **NOTE 不是通过**，它记录的是"这张表在页面上不成表" ✗ | **CLOSED（第 55 轮 ✓ 成因已修掉、检查器 exit 0、判据未放宽）** 第 55 轮做的：① **没有**照下面"未做"里写的去搬散文——搬走会把第 7–8 轮的叙述从它所属的行旁边挪开、打乱阅读顺序 ✗ 改为**给第二块补回与第一块同款的表头与分隔行**（纯新增 2 行）⇒ 两块都成为合法表格、散文留在原地、**已记录的结论一个字没动** ✓ ② `docs/DEFECTS.md` 第 218 行**夹在 D7 与 D8 之间的那个空行**删掉（markdown 里空行即断表 ⇒ 那张表也重新连续）✓ ③ 两处**需要人工判断**的转义：我写本行时自己又塞了裸竖线（引用了门限表某行原文 ⇒ 本行一度变成 **13 格** ✗ 被检查器当场抓住，正好证明它承重）；以及 `STATUS.md` 第 9 轮那行**叙述里当分隔符用的两个字面竖线**（@1326、@2178，**不在代码跨度里** ⇒ `--write` 正确地没碰，必须由人判它们是正文还是列边界）⇒ 都转义后那行回到 3 格 ✓ **实测**：`node tools/check-docs-tables.mjs` **exit 0**，打印 clean、235 行 / 44 张表全部与表头一致且无裸竖线（机械转义 10 处、人工 2 处）；`git diff --numstat` = DEFECTS +1/-2、STATUS +4/-2 ⇒ 与"只插反斜杠 + 新增 2 行表头 + 删 1 个空行"完全吻合、无其他改动 ✓ **判据没有为了变绿而放宽**：任何一行格数与表头不符、或代码跨度里有裸竖线，它仍然 exit 1（本轮就是它先抓到我自己 ✗）；NOTE（无分隔行的块）不计入 exit code 但会打印 ⇒ 现在 **0 条** ✓ 下面保留第 53 轮的原文作为当时的状态、不改写历史：**OPEN ✗（第 53 轮发现；本轮只做到"能被机器发现"+"裸 `\|` 全部修掉"）** 已做：判据做成常驻工具 `tools/check-docs-tables.mjs`——**真表** = 第二行是 `---\|---`；对真表查两类错（代码跨度里的裸 `\|`、格数与表头不符）；`--write` **只往代码跨度里插反斜杠、绝不改一个字的正文** ⇒ 格数问题留给人，因为"那一列本来该写什么"只有人知道 ✓ 本轮已用它把裸 `\|` 一类**全部修掉**（含我自己写 D47 时那处）并把这一步写进 `AGENTS.md` 的每轮收尾 ✓ **未做**：把 STATUS 第 52–53 行的散文移出表格、给第 54–63 行补回表头与分隔行 ⇒ 这是重排整张门限表的版式，且**不得改动任何一行已记录的实测结论**（挪版式时最容易顺手改字 ✗）⇒ 单独一轮做、逐字 diff 复核 ⇒ **不因为工具变绿就当它不存在** ✓（第 55 轮：这条已兑现 ⇒ 证据在本格开头） |
| ~~D47~~ | **D45 修好之后，三处"进程内证据"仍在喂裸码区 ⇒ 手机侧的绿灯不再描述用户拍到的东西** ✗ 第 53 轮读原文实测：① `tools/smoke-capture.mjs` L44 虽然传了 `sheetMm`，但 L45/L57 送进 `bootstrapDecode` 与连拍收集器的是 `renderPageBitmap` 的**码区**位图（2260×3290），而 `pskit send` 落盘的 `page-NNN.png` 自第 52 轮起已是**整张纸**（2480×3508）；② `web/selftest.js` 三处 `pageLayout(t.geom, prof.dpi \|\| 300, {})` ⇒ **连纸张都没传**，浏览器自检解码的是一个不再存在的产物；③ `tests/unit/warp.test.mjs`（斜拍→找角标→校正→读格的唯一进程内证据）**全是 `PL-*` 码牌档**，纸面档一张没测 ⇒ 更硬的问题是：**纸面斜拍时，留边里那 16 段裁切/套准标记是检测器从没被告知过的黑墨**，会不会被当成角标？没人知道 ✗ 与 D45 同一类病：**夹具 ≠ 交付物** ⇒ 绿灯证明的不是用户走的那条路 | 读 `tools/smoke-capture.mjs`：只有 `renderPageBitmap(...)` 与 `collector.addFrame(b)`、**没有 `renderSheetBitmap`**；读 `web/selftest.js`：`pageLayout(..., {})` ×3；看 `tests/unit/warp.test.mjs` 用的 profile ⇒ 全是 `PL-G`/`PL-D2`/`PL-M1`。修好后：`node tools/smoke-capture.mjs` 打印 `3 sheet(s), 2480x3508px (code area 2260x3290px)` 与 `16 mark segments, margin 9.22mm`；`node tools/check-dist.mjs` 的 `dist selftest green (executed here, not claimed for a browser)` 一行是**在构建产物上执行**的自检；`node --test --test-isolation=none "tests/unit/warp.test.mjs"` 打印 `P-M1-300 sheet: 3 tilted A4 photos (marks in the margin) recovered` ✓ | **CLOSED（第 53 轮 ✓ 三处都改成交付形状，并补上纸面斜拍这块空白）**：① `smoke-capture` 改为 `code.map(b => b.sheetMm ? renderSheetBitmap(b) : b)` 喂**整张纸**，码区帧**保留**为第二组合法输入（用户只扫/只裁码区、以及所有码牌档 ⇒ 两条都必须能过），并新增断言"**帧必须是交付形状**"（sheet 比码区大、`markSegments > 0`、且不带 `sheetMm`）⇒ 实测 **exit 0**：3 页全收、`sha ea26ac9cc04c825d` 与输入一致；裸码区对照组也还原一致；重复/裁切/跨会话/`totalPages` 冲突/不可解码五道门限仍各自正确拒绝 ✓ ② `selftest.js` 三处补 `{ sheetMm: t.geom.sheetMm }` 并把 `encodePNG(bmp)` 改为 `encodePNG(bmp.sheetMm ? renderSheetBitmap(bmp) : bmp)` ⇒ 重建 `web/dist`（build `d7eaa575e9f75fbb`、54 文件、precache 52）+ `check-dist` **exit 0**（13 pass / 0 fail、G9 CHECK 12 assertions），其中 `dist selftest green (executed here, not claimed for a browser)` ⇒ **是执行构建后的自检、不是声称** ✓ ③ `warp.test.mjs` 新增纸面用例：`P-M1-300` 的整张 A4 经 `photoTransform(scale 1.1, rot 0.05, kx/ky 微翘)` + `makePhoto(noise 8, blur 1)` 斜拍 ⇒ `findMarkers` → `rectifyPage` → `readEcho` → `readPageIdeal` → 重组，3 页 900 B **逐字节还原** ⇒ **结论：留边里的裁切/套准标记不会骗过角标检测** ✓ 且加了**阳性对照**：quad 必须等于"码区坐标 + `contentOffsetPx`"经同一变换后的位置、误差 <2.5 px ⇒ 排除"锁到标记上、校正出垃圾却碰巧解码"这种假绿 ✓ `warp exit 0`，码牌三档与 90° 旋转用例不受影响 ✓ **边界（别越界读）**：这条证明的是**几何与检测链在交付形状上成立**，**不等于** G4/G9/G10——真手机 500×8、真浏览器安装提示、真摄像头连拍仍待用户执行（清单在 `docs/USE.md` §5）✗ 进程内证据不冒充真机证据 ✓ |
| ~~D46~~ | **纸面产物的 `manifest.json` note 仍是码牌口径** ✗ 实测（第 52 轮）：`pskit send .tmp/payload.bin --profile P-M1-300 --sheet A4 --format png` 写出的 note 是 `Print at 100% scale (no "fit to page"). Verify the plate fits: 191.3x278.6mm` ⇒ 对**纸面**产物说 "plate（码牌）"、给的尺寸还是**码区**而不是纸张 ⇒ 用户被叫去"检查码牌放不放得下"，而实际该告诉他的是"整张 A4、码区 191.35×278.55 mm 居中、四周约 9.3 mm 留边带裁切/套准标记、按 100% 缩放打印" ✗ 第 52 轮修 D45 时发现（PNG 现在**也是整张 A4** ⇒ 这句 note 与产物的不一致更刺眼了）⇒ **当轮未改**：改它要动 `cli/pskit.mjs`，而那一刻两个 G2 后台批（`pwsh-13`/`pwsh-14`）正在用同一个文件跑判决 ⇒ **按 D28 的规矩不得编辑正在被测量的代码** ✓ 不是忘了、是规矩 ✓ | `node cli/pskit.mjs send .tmp/payload.bin --profile P-M1-300 --sheet A4 --format png --out .tmp/note-check` 然后读 `.tmp/note-check/manifest.json` 的 `note` 字段 ⇒ 现在含 `Verify the plate fits`；修好后**纸面档必须说纸张与留边、只有码牌档才说 plate** ✓ | **CLOSED ✓（第 52 轮发现，因 D28 当轮不改；第 60 轮 D28 解除后修完）** **实测证据（`cli/` 没有单测挂点 ⇒ 直接跑 CLI 读产物；250 B 载荷，板材档喂 200 KB 会被 CLI 正确拒绝）**：① 纸面 300 dpi ⇒ note 变成 `Print at 100% scale (no "fit to page"). Sheet 210.0x297.0mm carries the 191.3x278.6mm code area centred, with crop marks and registration crosses in the margin; scan colour at 300 dpi with auto-crop OFF.` ⇒ **不再出现 "plate"** ✓ ② 纸面 600 dpi ⇒ 同上 + `WARNING: 600 dpi paper profiles are NOT qualified -- the G2 gate measured this side below its 100% criterion (docs/DEFECTS.md D49); prefer a 300 dpi paper profile unless you need this one.`（= D49 的 CLI 半 ✓）③ 板材 `PL-D2 --nozzle 0.4 --format 3mf` ⇒ note 仍是 `Print at 100% scale (no "fit to page"). Verify the plate fits: 189.3x189.3mm` ⇒ **码牌口径原样保留** ✓ 三次 `exit 0` ✓ **实现**：note 三态分叉 `layout.sheetMm ? 纸张口径 : (modelReports.length ? 码牌口径 : 无纸张的码区口径)`；**第三态是新加的**——不传 `--sheet` 时 PNG 只有码区，就老实说"打印机自己决定摆哪儿、没有裁切标记，要可打印的纸请传 `--sheet A4`"（这正是 D45 修之前的默认处境，不能装作不存在 ✓）。所有尺寸数字都由 `layout.physicalMm` / `layout.sheetMm` + `dpi` **算出、没有硬编码** ⇒ 换档不会烂 ✓ **陷阱（已写进代码注释）**：`layout.sheetMm` 是 `{w,h}`，而 `renderPageBitmap` 返回的 `bitmap.sheetMm` 是**数组** `[w,h]`（`core/render/raster.js` L179）⇒ 混用会在用户的 manifest 里打出 `NaN x NaN mm` ✗ **顺带修掉同族的 D50**（stdout 的 `render` 行也只报码区尺寸）✓ **D49 的 CLI 警示刻意不写数字**（`162/200` 留在台账，不进代码字符串 ⇒ 不会因为将来重测而烂掉）✓ 原计划照做：note 按有无 `sheetMm` 分叉——有纸张 ⇒ `Print at 100% scale on A4 (or Letter): the code area is 191.4x278.6mm, centred, with ~9.3mm margins carrying crop and registration marks`；无纸张（码牌路径）⇒ 保留现在的 plate 口径 ✓ 顺带让 `docs/USE.md` 的打印段与它对齐 ✓ |
| ~~D45~~ | **`--sheet` 只对 PDF 生效：栅格（PNG）路径始终只输出"码区"⇒ 用户拿到的 PNG 没有纸张、没有留边、没有裁切/套准标记；而 G2 的全部语料正是拿这种 PNG 喂信道的** ✗ 实测（第 51 轮）：`node cli/pskit.mjs send .tmp/payload.bin --profile P-M1-300 --sheet A4 --format png --out .tmp/g2src-sheet` ⇒ **exit 0**、`render 2260x3290px @ 300dpi`、`page-000.png = 2260×3290 px = 191.35×278.55 mm`（A4@300dpi 应为 **2480×3508 = 210×297 mm**）⇒ 与**不加** `--sheet` 的旧源页 `.tmp/g2src` **同形**；600 dpi 侧 `.tmp/sw-scan600-src` 同样（`4530×6590`、manifest `sheetMm=null`，A4@600dpi 应为 `4960×7016`）✗ 后果两层：**① 用户侧**——照手册打印 PNG 的人拿到的是"一张只有码区的图"，100% 缩放时由**打印机自己**决定把它摆在纸上哪里 ⇒ **没有裁切标记可对刀、没有留边保证角标不出画** ⇒ 这正是 D44 在 PDF 侧修掉、PNG 侧**仍然敞着**的同一个病（`pack.pdf` 现在是对的 ✓ 手册也已把用户引向 PDF ✓ 但 CLI 与网页仍然会给 PNG，且 manifest 的 note 还写着 `Verify the plate fits: 191.3x278.6mm` 这种**码牌口径**的话 ✗）；**② 门限侧**——G2 的 300 dpi `200/200`（第 51 轮）与 600 dpi 那 2 例失败，**都是在"码区页"这个前提下测出来的** ⇒ 600 dpi 的"角标出画"很可能是**夹具形状**造成的（角标紧贴图像边缘 ⇒ 任何姿态都出画）⇒ 我第 49 轮把它归因为"A4 在平板上带 0.x° 旋转的真实物理"，**那句已撤回** ✗（"不是解码器缺陷"的结论不变 ✓ 3 点 6 方程 < 单应 8 自由度 ⇒ 拒绝正确 ✓） | `node cli/pskit.mjs send .tmp/payload.bin --profile P-M1-300 --sheet A4 --format png --out .tmp/g2src-sheet` 然后读 PNG 的 IHDR 宽高（字节 16..23，大端）⇒ 现在打印 `2260x3290`，对照 A4@300dpi 应为 `2480x3508`。**修好后同一命令必须给 `2480x3508`，且不传 `--sheet` 时输出逐字节不变**（回归保护）⇒ 复验：`node --test --test-isolation=none "tests/unit/raster-sheet.test.mjs"`（**待建**）+ 上面那条 IHDR 对比 ✓ | **CLOSED（第 52 轮 ✓ 就是按下面这个设计做的、并且已实测 ⇒ 实测证据在本格末尾）** 修法：`core/render/raster.js` 的 `renderPageBitmap` 已经能拿到 `layout.sheetMm`（第 48 轮为 PDF 加的 ✓）⇒ 栅格侧按 sheet 开位图（`round(sheetMm.w/25.4*dpi)`）、填纸白、把码区贴到**居中偏移**——与 `encodePDFDocument` 里 `txNum/tyNum` **同一套算术**（两条路径必须给出同一个几何 ⇒ 最好把这套算术抽成一个共享函数让 PDF 与栅格都调它，否则两边会各自漂移 ✗）——并按 PDF 里同一套 mm 几何（`gap = margin*0.25`、`arm = margin*0.55`、`cross = margin*0.30`）画四角裁切标记与四边套准十字；然后重出 G2 两侧源页、重跑 seed 1..200（顺序见 `ACCEPTANCE.md` G2 段末的 (a)(b)(c) ✓）。**不许**只在 PDF 侧继续修就把这条关掉、也不许把"码区 PNG"当合格产物交给用户 ✗ 顺带把 manifest 的 note 按纸面/码牌分开措辞 ✓ **【第 52 轮实测 ⇒ 已按此做完】**：新增 `core/render/sheet.js` 作**唯一几何真源**（全用 mm：`sheetPlacement` 给居中与留边、`sheetMarks` 给 16 段标记、`MARK_MIN_MARGIN_MM = 0.5/PT_PER_MM` 与 `MARK_STROKE_MM = 1/PT_PER_MM` 由 PDF 既有常数直接换算 ⇒ 栅格不再自创阈值 ✓）；`core/render/pdf.js` 改为**调它**（保留自己的 `cannot carry` 拒绝文案 ⇒ 既有断言不动）；`core/render/raster.js` 新增 `renderSheetBitmap()`：按 sheet 开位图、填纸白、码区居中 blit、按同一套 mm 几何画标记（**y 轴翻转只在这一处发生** ✓），线宽取 `min(1pt, 0.8×gap)` 且**不细于 2 px**（1 px@300dpi = 0.08 mm 会被扫描仪 MTF 抹掉 ✗），返回值**就是纸**：`pageMm = sheet 尺寸`、**不带 `sheetMm`** ⇒ 交给 `encodePDFDocument` 也不会二次居中 ✓ 出口接线：`cli/pskit.mjs` 的 PNG/TIFF 走 `bitmap.sheetMm ? renderSheetBitmap(bitmap) : bitmap`（**PDF 仍拿码区位图**自己做矢量居中与标记 ✓）、`web/sender.js` 同理 ⇒ 浏览器给用户的 PNG 现在是整张纸 ✓ **实测**：`pskit send .tmp/payload.bin --profile P-M1-300 --sheet A4 --format png` ⇒ `page-000.png = 2480×3508`（209.97×297.01 mm；原来是 2260×3290）；600 dpi 侧 `--profile P-M1-600 --sheet A4` ⇒ `4961×7016`（210.02×297.01 mm；**4961 而不是 4960 才是对的**——210 mm@600dpi = 4960.63 px、栅格必须取整像素 ⇒ 偏差 0.01%，而我第 51 轮写的"应为 4960"是 naive 期望 ✗ PDF 侧仍是精确 mm ✓）；新单测 `tests/unit/raster-sheet.test.mjs` **7 项**：sheet 尺寸/dpi/居中且**码区逐像素等于源**、**标记墨点全部落在码区之外**（逐像素判定、不信注释）、**笔宽碰不到码区**（算术判定 × 4 种纸张尺寸 × 不被纸边裁掉）、退化 sheet（无留边 ⇒ 0 段标记且逐字节等于码区）、PDF 的 MediaBox 与共享几何一致（含**阳性对照**：+1 mm 必须不匹配）、PNG 的 IHDR 自带 sheet 尺寸、坏输入全拒（含"纸装不下码区"⇒ `cannot carry`）；全量套件 **296/296 · 0 fail · 0 todo** ✓ `build-web` exit 0（precache 51→**52**）+ `check-dist` exit 0（13 pass / 0 fail）✓ **端到端**：`& .\tools\usability.ps1` **exit 0 / 285 s / 11 项全 PASS**，其中信道仿真与 `pskit receive --photo` 吃的正是**带留边与裁切标记的整张 A4**，落盘 `sha256 32ec4805… vs 32ec4805…` **逐字节相同** ⇒ 修复没打断主路径，而且"被扫描的东西"终于等于"用户打印的东西" ✓ **一处我自己的口径错要更正**：第 52 轮我先用"不加 `--sheet` 应与旧 PNG 逐字节相同"当回归判据，实测**两者都是 2480×3508** ⇒ 因为**纸面档在 CLI 里本来就默认 A4**，"纸面 PNG 变成整张纸"**正是这次修复本身**、不是回归 ✗ 真正必须不变的是**码牌/3D 路径**（无 `sheetMm` ⇒ 走原分支、字节不动 ✓ 冒烟第 5-6 步实测通过）与 **PDF 几何**（`pdf-truesize` 5 项仍绿 ✓）⇒ 判据已按此改正、没有为了"绿"而放宽 ✓ **未做（不在本行判据内）**：manifest 的 note 仍是码牌口径 ⇒ 另记 **D46**（当轮不得改 `cli/pskit.mjs`，D28 ✓）；(c) 的两侧 200 seeds 已用修好的渲染器重出源页（`.tmp/g2src-a4-300` / `.tmp/g2src-a4-600`，载荷 sha 仍 `91ab3f7310da452b` ⇒ 与历史语料可比）并起后台批 `pwsh-13`/`pwsh-14`，语料命名 `sc-scan300-*` / `sc-scan600-*` ⇒ **与旧 `nc-*` 分开、两种前提永不混进同一个数字** ✓ **判决未出 ⇒ 不声称比率** ✗ |
| ~~D44~~ | **`pack.pdf` 的页不是"纸"、是"码区"；而且 2D 纸面全链路根本没有裁切/套准标记** ✗ 实测：`buildArtifacts(payload, { profile: 'P-M1-300' })` 产出的 `pack.pdf`，每页 `/MediaBox [0 0 542.4 789.6]` = **191.35 × 278.55 mm**，而同一 profile 的 `geom.sheetMm` 是 **A4 210 × 297** ⇒ 差 **18.65 mm × 18.45 mm**；`core/render/pdf.js` 的绘图 CTM 与 MediaBox 同源（`W 0 0 H 0 0 cm /Im0 Do`）⇒ **图确实铺满它自己那一页、页内没有二次缩放** ✓ 所以问题不在"内容被缩小"，而在**页尺寸取的是 `layout.physicalMm`（码区）而不是纸张** ⇒ 后果两条：① **没有页边距 ⇒ 没地方放裁切/套准标记**，而 objective ① 点名要"含套准/裁切标记"——grep `core/render/*.js` + `web/*.js` 的 `crop\|registration\|裁切\|套准\|trim` **零命中**（只有两处无关文字）⇒ 标记这件事**压根没实现** ✗ ② 用户装的是 A4、打印对话框里却是 191×279 的页 ⇒ **更容易顺手点"适合页面"**，而那正是 D8 的失败模式（缩放 ⇒ 几何错 ⇒ 帧头拒收 ⇒ 整批重扫）。**如实说清危害的边界**：100% 打印时几何仍然正确（解码器从基准块自识别几何 ⇒ 这**不是**数据正确性缺陷 ✓），D44 伤的是**可用性**与 objective ① 的明文要求 | 新检查 `tests/unit/pdf-truesize.test.mjs`（= D8 里**可自动化的那一半**：产物侧对不对，不需要浏览器）：期望尺寸走**另一条独立算路**（`encodeTransfer` → `geom.sheetMm`，`PT_PER_MM` 在测试里重算 **72/25.4**、**不 import** ⇒ 不会自己跟自己同意 ✓）；实际值解析**用户真会拿到的那份 PDF**（`buildArtifacts` → `r.pdf`）的每个 `/MediaBox` 与每个绘图 CTM，并断言 **CTM 字串 == MediaBox 字串**（两侧出自同一格式化器 ⇒ 比容差更严、且不会漂移）⇒ **两条阳性对照都通过**：把 MediaBox 缩到 **94%**（"适合页面"的形状）⇒ 被点名报出 ✓；**只缩 CTM、MediaBox 不动**（只查页尺寸会漏掉的失败模式）⇒ 被 `painted at … inside a MediaBox of …` 报出 ✓ ⇒ **判据本身可信，失败的是产物** ✗ 当前这条以 `{ todo: 'D44 …' }` 标记 ⇒ 套件 **282 项 / 281 pass / 0 fail / 1 todo / exit 0**（**判据没有放宽**：todo 失败仍如实计数、差值按 mm 打印 ✓ 修好那天它自己变成真通过、测试文件不用改 ✓ node:test 的 todo 语义已实测：失败不拉红套件 ✓） | **CLOSED（第 48 轮 ✓）** 照 ①–⑥ 落地，而且**判据没有为了变绿被放宽、反而变严**：从"页尺寸对不对"升级到"页 = 纸张 + 码区按标称尺寸绘制 + **真居中** + 标记全在码区之外"。**改法（三处小改，都在 `core/`）**：`core/render/layout.js` 的 `pageLayout` **把纸张记进返回值**（`sheetMm`；板材路径为 `null`——板材的产物是 3MF/STL，在板材 PDF 上画标记只是噪声 ✓）；`core/render/raster.js` 的 `renderPageBitmap` **把 `sheetMm` 挂到 bitmap 上**（**故意不挂 `pageMm`** ⇒ 内容框仍按 `width/dpi` 推 ⇒ **不传纸张的调用方逐字节不变**：`tests/unit/render-pdf.test.mjs` 那些 byte-for-byte 断言原样绿 ⇒ 这是证据不是声明 ✓；两个调用方 `web/sender.js` L109、`cli/pskit.mjs` L325 **零改动**就拿到了修复 ✓）；`core/render/pdf.js` 新增 `sheetMm`（`checkSheetMm`，与 `pageMm` 同形校验）、**页 = 纸张**、码区**居中**（CTM 由 `W 0 0 H 0 0 cm` 变 `W 0 0 H TX TY cm`）、substrate 铺满整张纸、页边距里画**矢量裁切标记（四角 L）+ 套准十字（四边中点）**，`0 0 0 RG` + `1 w` ⇒ **与打印分辨率无关** ✓ 标记尺寸**由页边距自身推出**（gap 0.25 / arm 0.55 / cross 0.30 ⇒ 合计 0.8 ⇒ 永远放得下**且够不到码区**——固定 5 mm 的标记在 3 mm 边距上要么被打印机裁掉、要么落在码区里，而**解码器不知道的墨，正是页面开始解不出来的地方** ✗）⇒ 实测：`pack.pdf` **3 页 · 页 = 210×297 mm = `595.28 841.89` pt · 码区 191.35×278.55 mm 绘在 `(26.44, 26.14)` pt · 每页 16 段标记 · 706980 B** ✓ 另加**拒绝**：纸张小于页面时 `encodePDFDocument` **抛 `cannot carry`** 而不是悄悄裁墨（`pageLayout` 本来就拒，写盘侧再拒一次 ⇒ **不信任调用方的写入器，才不会某天裁了墨还一声不吭** ✓）⇒ 判据侧：`tests/unit/pdf-truesize.test.mjs` 的 `todo` **摘掉**，加 3 条阳性对照（纸张缩到 **94%**、码区缩到 **90%**、**码区不居中**）+ 1 条合成幅面（50×40 mm，用**同一个带容差的判据**，因为写盘按两位小数四舍五入，拿 `toFixed` 的结果做严格相等是在为 0.01 pt 吵架 ✗ 这条我第一版就写错了、跑之前改回容差 ✓）⇒ **套件 284/284、0 fail、0 todo** ✓ **哪些字节变了（如实记）**：只有**传 `sheetMm` 的调用方**的 `pack.pdf`（`web/sender.js` 与 `cli/pskit.mjs`）——页数不变、图像 XObject 不变；变的是 `MediaBox`（`542.4 789.6` → `595.28 841.89`）、内容流多出标记块与居中平移、有 substrate 时填充矩形改为整张纸 ⇒ **仓库里没有冻结的 PDF 字节 fixture**（`render-pdf.test.mjs` 的 `eqBytes` 全是"同输入重编码"自比）⇒ 无需更新任何 fixture ✓ 仍然 OPEN 的是 **D8 本身**（浏览器打印对话框的缩放 ⇒ 需真打印机/真浏览器 ⇒ G9/D18）✓ |

### 第 75 轮新增（OPEN）

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D68~~ | **数据板的网格没有角标 ⇒ 真机打印出来的码牌照片无法登记**：`core/mesh/plate.js` 的 `facts.notModelled` 明写"corner fiducials and the echo strip … the plate carries the data lattice only"，理由是角标的线宽在 `core/render` 里是像素量、而 `MESH-CONTRACT.md` §3 不许凭空发明半径。后果：用户按 `docs/USE.md` §3 印一块 `PL-D2` 码牌、拍照回来，`receive --photo` 的 `findMarkers` 找不到四角标记 ⇒ **G10 的实机那一半根本走不通**（第 75 轮做 MTF 板时才发现：那块板必须能被 `findMarkers` 定位，于是给它建了角标；数据板同一根因仍在）。**第 76 轮不改它**：改数据板要同时动 `MESH-CONTRACT.md` §2/§3、`ref/verify_model.py` 的部件数与投影对拍、以及 `check-3mf` 的断言 ⇒ 单独一轮做，且必须先给 `MESH-CONTRACT` 补一条"角标几何从 `layout.fiducials` 的像素换算、环宽 = 1 个 cell"（这正是 MTF 板用的那条规则，已有实物验证）。 | `node cli/pskit.mjs send F --profile PL-D2 --nozzle 0.4 --format 3mf --out .tmp/d68` ⇒ 用切片软件打开可见**只有底板与浮雕、四角空空**；`ref/verify_model.py --dir …` 报的部件数是"底板+色A+色B"、不含标记。对照：`calibrate --make-mtf --format 3mf` 写出的板**带四个角标**（`marker-tl/tr/bl/br` 各一个墨区，投影对拍里可见） | **CLOSED ✓（第 77 轮）**：角标几何**不发明** —— 全部取自 `layout.fiducials`（像素）按 `mmPerPx` 换算，环宽 = 该标记自己的 `ringPx`（= `max(cellPx, ewPx)` ≥ 1 EW；薄于一格 EW 直接拒绝）；空心角标 = 外框减内框，用 `rectilinear.js` 挤出；四个标记进 `relief-ink0` 桶 ⇒ **object 划分不变**。判据跟着长：`facts.markers` 声明位置与期望面积，投影对拍**只排除声明过的标记**且**每个声明的标记必须真的在、面积对得上**（JS `projectTopToCells({markers})` 与 `ref/verify_model.py` 的 `g8-3/marker-material`；单测带「不声明就必红」的反例）。实测 `verify --gate G8` ⇒ `378064` 三角形（+100 = 3×12 + 64）、`python ref/verify_model.py --dir .tmp/m5verify77` ⇒ `PASS (62 checks, 0 failed)`、`--selftest` ⇒ `PASS (11 checks)`、`mesh-3mf.test.mjs` 30/30。顺带修掉两处**实测暴露**的问题：标记框不能存 4 位小数（焊接网格顶点会落框外 ⇒ 两个标记读成 0mm²）、`cross/same-triangle-multiset` 的贪心匹配会漏配（56 个配不上、报 169.8mm 假偏差）⇒ 换成分箱匹配，判据一字未改。**仍未验**：真照片能不能锁住浮雕角标（单色靠阴影、双色靠料色）| 
| ~~D67~~ | **MTF 校准板只有外观光栅，没有可打印的 3MF/STL 网格 ⇒ 板子的用途（量 FDM 喷嘴）目前没有对应的实物**：第 75 轮落地的 `pskit calibrate --make-mtf` 写出 `mtf-plate.png` + `mtf-plate.json`，而 PNG 是**这块板被相机/扫描仪从上方看到的样子**（`sim/channel.py` 的 `plate-matte`/`plate-glossy` 吃的正是它，所有板材档测试也一直这么用）；FDM 路径要的是几何 —— 而板上的"实心块挖孔"在网格里必须**分解成互不重叠的墨矩形**（本仓 `core/mesh/solids.js` 明令不做 CSG，`prismFromMask` 的矩形并集会让"每条无向边恰用两次"结构性不成立 ⇒ 只能走"孔四周的框 = 4 个不相交矩形"这种分解），这一层没写。**所以现在的诚实状态是：测量半边（读者 + 推荐 + 探针）已完成并经真信道验证；可打印半边未完成 ⇒ 用户拿不到实物板。** 纸面打印 `mtf-plate.png` 也能走通"特征 ladder → 喷嘴推断"这条链（渗墨在纸面与 FDM 上都是限制），但那是**用纸面模拟喷嘴**，不是真板材 | `node cli/pskit.mjs calibrate --make-mtf --out .tmp/mtf-75` ⇒ 只写出 `mtf-plate.png` 与 `mtf-plate.json`（`Get-ChildItem .tmp/mtf-75` 里没有 `.3mf`/`.stl`）；而 `pskit send --format 3mf,stl` 的板材路径只吃**数据页**（`buildPlateModel` 要求 `geom.medium==='plate'` 且每格是 glyph 环+点，板子的孔阵不是这个形状） | **CLOSED ✓（第 76 轮）**：新增 `core/mesh/rectilinear.js`（**共形网格挤出**：并集先按所有矩形边界切网格，格子作为四边形，只有"邻居在外面"的边生成墙 ⇒ 没有内部面，水密是构造出来的）+ `core/mesh/mtfplate.js`（同一份 `spec.inkRegions` 驱动光栅与网格 ⇒ 不可能漂移；`projectionReport()` 拿三角形投影面积与**规格的矩形算术**对拍，实测 17 个墨区全部 `maxPct 0`；角标**也建了模**，否则印出来没人认得出）+ CLI `--format 3mf,stl`（写之前先判水密与投影，不合格就拒绝写）+ 6 个单测 + 探针加一条 mesh 腿。实测 `calibrate --make-mtf --out … --format 3mf,stl,png` ⇒ 561180 三角形、3 objects、每 object `manifoldReport` 通过、`verify --gate G8 --file` 通过、投影 `6122.346178mm²` 对 `6122.346109mm²`（**最差墨区 0%**）|

### 第 79 轮新增（OPEN / 已闭各一）

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D69~~ | **手机压力档下的失败诊断把「这张照片根本没有墨/纸分离」说成「四角没拍进画面」** ⇒ 用户被指向**重新取景**，而真正的补救是**降曝光/避反光/靠近**：`findMarkers` 失败时只会报 `no-square-candidates`/`no-rectangular-quad`，`tools/g2-corpus.mjs` 的像素分类器再把它们一律标成 `fiducial-out-of-frame`。第 79 轮实测（`sim/channel.py --preset phone-hard`，8 seed × 2 条件 × 2 profile = 96 页）：**81/96 页**的 `findMarkers` 失败且墨量统计为「无分离」（median/peak 0.67–0.79），而**信道自己的报告写着 `marker_visible: [1,1,1,1]`、`corners_visible: 4`** ⇒ 角标确实在画面里，是照片本身没有对比度（`clipped_frac` 0.49–0.97、`capture_ppi` 2.75–3.9）。**不是误接受**：页面照旧被拒（判据③未破），但诊断错。 | 复现：`python sim/channel.py --in <pages> --out .tmp/g4d --seed 4 --preset phone-hard --modifier nocrop` ⇒ `node tools/g2-corpus.mjs .tmp/g4d` 修前打 `markers/no-rectangular-quad [fiducial-out-of-frame] 4 of 4 corners carry no marker`，修后打 `markers/no-contrast [no-contrast]`。阳性对照：`tests/unit/fiducial-corner-damage.test.mjs` 的「健康页/裁到码区的页**不得**被判 no-contrast」；判别量按实测选：健康页 0.063、300dpi 扫描 0.060、模糊 4px 0.241、phone 系列 0.668–0.793 ⇒ 切点 0.5 落在空隙里（先用 Otsu 墨量占比 0.55 试过，**被实测否决**：裁到码区的锐利页就到 0.560） | **FIXED ✓（第 79 轮）**：`core/decode/fiducial.js` 新增 `medianInknessRatio()` + `FLAT_CAPTURE_RATIO = 0.5`，`findMarkers` 在**所有尝试都失败之后**（且峰值 > 24，否则那是 `blank-image`）报新 reason `no-contrast` 并带上实测数字；`core/decode/advice.js` 给出对应措辞（降曝光/避反光/靠近，并明说「重新取景没用」）；`tools/g2-corpus.mjs` 把这一类单独标成 `no-contrast`。**判据未动**：没有任何页面因此被接受（G5 复跑 0 误接受 ✓），只是理由不再指向错的补救动作。 |
| ~~D71~~ | **手机拍整页时，失败原因被说成「头字段校验失败/条带损坏」而不是「这一档拍得太远」** ⇒ 用户会去重印或怀疑格式，而正确动作是拍近/换粗档：`phone40`（较温和的手机档）下角标**找得到**（6.3px）、校正 100%，但 `readEcho` 只报 `bad-magic`，而 `advice.js` 对 `echo-bad-magic` 的解释是「条带损坏，或这是另一套码」——**字节层面的陈述是真的，成因层面的陈述是错的**。第 80 轮实测（同一张 P-M1-300 页）：可读的条带两电平分离度 **1.000（原始渲染）/ 0.790（300dpi 扫描）/ 0.705（3× 降采样）/ 0.530（4× 降采样）**，而 `phone40` 只有 **0.148**（条带的墨与纸糊成一条灰带）。 | 复现：`python sim/channel.py --in <pages> --out .tmp/p40 --seed 5 --preset phone40 --modifier nocrop` ⇒ `node tools/g2-corpus.mjs .tmp/p40` 修前打 `readout/echo-bad-magic`，修后打 `readout/echo-no-contrast`。阳性对照：`tests/unit/warp.test.mjs` 的「同一页锐利 ⇒ 仍能读」「轻微模糊（分离度 0.546）**不得**被判 no-contrast」 | **FIXED ✓（第 80 轮）**：`core/decode/echo.js` 新增 `ECHO_SEPARATION_FLOOR = 0.35`（= (p75−p25)/max，切点落在实测空隙里），`readEcho` 在**所有阈值都失败之后**若分离度低于它，改报 `no-contrast`（页面层自动变成 `echo-no-contrast`）；`advice.js` 给出「拍近/对焦/换粗档/改用 300dpi 扫描」。**判据未动**：没有任何页面因此被接受。 |
| ~~D72~~ | **手机连拍界面把解码失败的原因整个丢掉，`bootstrapDecode` 又把具体成因包成 `no-geometry-matched`** ⇒ 前两轮新加的两条诊断（`no-contrast` / `echo-no-contrast`，正是写给手机用户的）**一个字都到不了手机**：`web/capture.js` 的 DOM 半面对 `kind === 'no-page'` 只打一句「画面里没有本工具的页（或太糊/太暗）」，而 `bootstrapDecode` 在所有候选失败后统一报 `no-geometry-matched`，于是连 `reason` 都只剩一个包装名。实测（第 81 轮）：一张平/糊的帧让 24 个候选**全部**以 `no-contrast` 失败，用户看到的却是「所有候选几何都读不出这一页」。 | 复现：`node tools/smoke-capture.mjs`（第 81 轮新增两条断言）—— 修前 `kind no-page · reason no-geometry-matched`，修后 `reason no-contrast` 且 `advice.zh` 点名「过曝/反光/太远」并明说「重新取景没用」；阳性对照：好帧必须被接受且**不带** advice。 | **FIXED ✓（第 81 轮）**：① `core/decode/bootstrap.js` 在**所有候选失败且原因一致**时把这个原因提升为页面级 reason（`wrappedReason` 记下包装名），原因不一致仍报 `no-geometry-matched`（此时没有任何单一成因被确立）；② `web/capture.js` 的收集器给失败帧带上 `advise()` 结果，DOM 半面优先显示中文一句话；③ `core/decode/advice.js` 新增 `ZH` 表（16 条，与英文同表 ⇒ 不会两处漂移）并导出 `localizedReasons()`。**判据未动**：没有任何帧因此被接受（smoke 的阳性对照钉住）。 |
| ~~D73~~ | **接收页的 `#outname` 监听器注册在 `run()` 里 ⇒ 每完成一次传输就多挂一个**（每个都闭包住那一整份装配好的载荷）：用户可见效果是**泄漏**而不是错名字 —— 监听器按注册顺序触发，最新的那个最后跑、总是赢 —— 但把整份 payload 留在闭包里不是免费的；同页的**档位下拉框还是手搓的标签**，于是**接收端既没有 D49 警告、也没有第 80 轮加的「手机拍摄首选」提示**，同一个人在两页上做同一个选择、看到的信息却不一样。 | 复现：`node --test --test-isolation=none "tests/unit/profile-picker-warning.test.mjs"` ⇒ 第 82 轮新增两条断言：`#outname` 的 `input` 监听器**全文件恰好一个**且**不在 `run()` 体内**（反例：同一段切片仍能找得到 `run` 作用域里的 click 监听器 ⇒ 不是空跑）；两页都必须走 `profileOptionLabel`、且 `未达标` 字样在 `web/` 里出现 **0** 次（只在 `core/profiles.js` 出现 1 次）。 | **FIXED ✓（第 82 轮）**：① `web/app.js` 把 `applyName` 与监听器提到模块加载时注册一次，当前传输的命名输入放进 `currentNaming`；② 标签策略从 `web/sender.js` 移进 **`core/profiles.js`**（`isUnqualifiedPaper` + `profileOptionLabel`），两页共用（sender 重新导出以保持它自己的对外面）；③ 接收页改用它 ⇒ 现在两页显示同一份警告与提示。**判据未动**。 |
| ~~D74~~ | **目录里全是 JPEG/TIFF 时，`receive` 说「no pages found」——把用户送去查一个不存在的问题**：CLI 的输入过滤只收 `.png`（`.tif/.tiff` 收进来再静默跳过、只留一句末尾 note），`.jpg` 连列表都进不去 ⇒ 手机拍的 JPEG 拷到电脑上跑 `receive`，得到的是「没有找到页」而不是「这个格式我读不了」。手机相机默认 JPEG、平板扫描仪默认 TIFF ⇒ 这是**现实里第一秒就会撞上**的路径（浏览器接收端不受此限，它自己解 JPEG）。 | 复现：`python -c "from PIL import Image; Image.open('.tmp/…/page-000.png').convert('RGB').save('.tmp/fmt-jpg/page-000.jpg', quality=92)"` ⇒ `node cli/pskit.mjs receive .tmp/fmt-jpg --photo --profile P-M1-300 --out x.bin` ⇒ 修前 `receive: no pages found in …`（exit 1），修后点名 `.jpg`、说明只解码 PNG、给出两条出路（转换命令 / 浏览器接收端）、**一个字节都不写**。已入库为 `tools/usability.ps1` 的 **4d 腿**（含阳性对照：同一目录里放回 PNG 必须照常解码）。 | **FIXED ✓（第 83 轮）**：`cli/pskit.mjs` 的 `receive` 改为「按能力分类输入」：`.png` 解码、已知不可读的扩展名（tiff/jpeg/webp/gif/bmp/heic）**点名统计**并给转换命令，全不可读时以**具名错误**退出（不再是「没找到页」）；`calibrate` 同步。`docs/USE.md` §3 与验收包 README 补「存成 PNG」一句。**判据未动**。 |
| ~~D75~~ | **用户的第一条命令就是坑：`pskit send FILE` 不写 `--profile` 时默认用 `PL-D2`（板材档，~220 B/页）⇒ 任何正常文件都撞 `needs 1120 pages > 255`，而这句话只让人「换更密的档」、不说是哪个**。`defaultProfileFor(ext)` 一直收着扩展名却从不看它、对所有输入都返回 `PL-D2`（`cli/pskit.mjs:143`），而 `docs/USE.md` 教的是纸面 `P-M1-300`（~7514 B/页）—— 两个数量级的差距，忘一个 flag 就得到一句无法执行的拒绝。 | 复现：`node cli/pskit.mjs send <204800 B 文件> --format png --out .tmp/x` ⇒ 修前 `pskit send: needs 1120 pages > 255 (inter-page RS limit): shrink payload or use a denser profile`（exit 1），修后 `profile P-M1-300`、3 页、exit 0；阳性/阴性对照已入库为 `usability.ps1` 的 **1b 腿**：不带 `--profile` 必须出纸面页（日志含 `P-M1-300`），而**显式** `--profile PL-D2` 喂同样载荷必须拒绝**并点名** `P-M1-300` 与 `split`（exit 2）。 | **FIXED ✓（第 84 轮）**：① `defaultProfileFor(ext)` 按输入类型选档 —— `.stl/.3mf/.obj` 仍给 `PL-D2`（发模型给板材是用户的本意），其余给 `P-M1-300`；② 页数上限拒绝时 CLI 补一行可执行的 hint（本次载荷多大、两个档各自每页多少字节、纸面档约需多少页、或 `pskit split` 切分）。**判据未动**（core 的拒绝原样保留）。 |
| ~~D76~~ | **两条用户可见的建议把人指向更坏的结果：「拍近一点，让码区填满画面」** —— 四个角标在纸的**四角**，拍近就会把它们挤出画面 ⇒ 页面**完全定位不了**（单应需要四个点对），比「整页入画、格子糊」还糟。这句话同时出现在 `docs/USE.md` §2/§6 与 `core/decode/advice.js`（英文 `do` + 第 81 轮加的中文 `ZH`）里。 | 复现：`node tools/smoke-capture.mjs` ⇒ `PASS a cropped page is not accepted as a page`（把整页裁到只剩码区 ⇒ `kind no-page · reason no-rectangular-quad`）；`tests/unit/warp.test.mjs` 的切边实验（左边切 16mm ⇒ 正确拒绝）。 | **FIXED ✓（第 87 轮）**：三处文案改成「**四个角标必须都在画面里**（即整页）；拍近到角标出画就彻底定位不了」，并把「拍近」从纸面手机路径的出路里删掉（只剩两条：换粗档 / 用扫描仪 300 dpi）。**判据未动**。 |
| ~~D77~~ | **我第 83 轮删掉 `skippedTiff` 变量时漏了一处引用 ⇒ `receive` 在「一页头都没读出来」这条分支上抛 `ReferenceError: skippedTiff is not defined`，整条诊断没打出来**（用户实测撞到，第 90 轮）。触发条件：目录里有图、但**没有一页**能被解码 ⇒ 走到 `noSession` 分支 ⇒ 崩在拼消息那一行。第 83 轮之后我自己的 `usability` 腿没覆盖这个分支（4d 腿的目录里有一页可读），所以本地一直绿。 | 复现：`node cli/pskit.mjs receive scans --photo --out x.bin`（`scans/` 是用户那三张转换图）⇒ 修前最后一行是 `pskit receive: skippedTiff is not defined`、exit 1；修后打印 `receive: INCOMPLETE -- not one page header could be read … (0 distinct page(s) read out of 3 image(s) offered)`、exit 2、不写文件。 | **FIXED ✓（第 90 轮）**：改用 `names.length + unreadable.length`（第 83 轮把「可读」与「格式不可读」拆成两个列表），并在有不可读文件时点名格式。**判据未动**。 |
| D70 | **G4 的判据没有写明「四个角标必须都在画面内」这个前提，而实测显示这是 phone-hard 档下的主导失败原因**：`docs/PLAN.md` 的 G4 要求「35°/失焦/运动模糊/JPEG45/指头遮挡/卷曲/暗光/裁切 各 500 seed ≥99%」，但 `phone-hard` 的 `crop_frac` 上限 0.08 + 旋转上限 25° 会让**角标本身出画**；一个单应需要 4 个点对 ⇒ 3 个角标时**任何**解码器都只能拒绝。第 79 轮实测（8 seed × 2 条件 × 2 profile）：`markers/no-contrast` 与 `no-rectangular-quad` 之外，`crop active` 条件下「画面内角标」只有 68–73/96（`framing ok` 下 94/96）。**这不是缺陷、是判据的前提缺失**（与 G2 侧早已记下的「判据未明示『纸张完整在成像区内』」同源），但**必须由产品负责人决定**：是给 G4 补上前提，还是要求产品在缺角标时也能工作（后者物理上不可能）。 | 复现：`& .\tools\g4-probe.ps1 -Seeds 8` ⇒ 每行末尾的 `channel report: ... markers in frame 68/96`（信道自己的 ground truth）与解码器的 `fiducial-out-of-frame` 对照 | **OPEN（需产品负责人决定）** |

### 第 43 轮新增（OPEN）


| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| D43 | **手机侧 PWA 装不上：两个各自独立的阻塞，本轮把第二个量出来了** ✗ ① 局域网 http 源**不是 secure context** ⇒ 浏览器拒绝注册 SW 与安装（代码照实处理：`pskt-file.html` 只在 `secure` 时 `navigator.serviceWorker.register('./sw.js')` ✓ 我在 dist 里读到那一行才敢这么写 ✓）⇒ 需 https 托管，而 `.github/workflows/pages.yml` 不存在、GitHub 推送还等用户的仓库与凭据 ② **`manifest.webmanifest` 只有一个图标 `./icon-page.png`、`sizes` 写着 `3290x3290`**——那是**整页渲染被拿来当图标** ✗ 安装性要求 **192×192 与 512×512** ⇒ 即使有了 https，这个 manifest 也过不了安装检查 | 本轮实测（新工具 `tools/check-lan.mjs`，**真起** `python -m http.server 8123 --directory web/dist` 后逐条 fetch）：**48/48** 条预缓存资源全部 200 且 **sha256 与构建清单一致**（用我们自己的 `core/hash.js` 算 ⇒ 浏览器 `cache.add()` 无从失败 ✓）· 4 个页面**无任何外部 http(s) 引用**（扫 `src=`/`href=`/`fetch()`/`import()`，只豁免 `xmlns` 命名空间串 ⇒ **气隙契约 ③ 在 http 源上同样成立** ✓）· 4 个页面**都带 CSP meta** ✓ · 瘦页模块图（`index.html`→`app.js`/`capture.js`/manifest；`send.html`→`sender.js`）在 http 上全部 200 ✓ · manifest 解析成功、`start_url ./index.html` 可取 ✓ ⇒ **exit 0**；两个安装阻塞被工具**打印但不计入 exit code**（那不是它声称要验的东西 ⇒ **报出来 ≠ 通过** ✓） | **部分闭合（第 44 轮 ✓）**：图标半**已修**、https 半**仍 OPEN** ✗ 实际做法与 ① 的设想不同、且更省：**没有新建 `tools/make-icons.mjs`**——`build-web.mjs` 里本来就有一块"用 `renderPageBitmap`+`encodePNG` 画真页图当图标"的代码（D13 的方形补丁 ⇒ `3290x3290` 正是这么来的）⇒ 直接在那块里加**盒式降采样 + 亮度阈值**：先平均再阈值，因为 6× 缩小时单纯平均会把码页糊成灰泥，而阈值化保住角上的基准块与格子纹理 ✓ maskable 版把同一张画进 **80% 安全区**——自适应启动器会从中间裁圆/裁方角，裁掉的正好是**基准块**，也就是这张图里唯一有意义的部分 ✓ ②③④ 全部落地：**重建 dist**（53 文件、预缓存 **51** 条、build `e5ce1d897e9134d3`）· `check-dist` **exit 0** · `check-lan` 对新起的 http 服务 **exit 0、51/51 字节一致** · 并加了**构建期强制断言**（manifest 的 icons 必须含 `192x192` 与 `512x512`、必须有 `purpose: maskable`，否则构建就红 ⇒ 这条性质不会烂回去 ✓）· 产物 `icon-192.png` 1279 B、`icon-512.png` 6093 B、`icon-maskable-512.png` 6084 B；`icon-page.png` 283808 B 保留，给想要更大图的启动器 ✓ ⇒ **⑤ https 第 90 轮已提供** ✓：`.github/workflows/pages.yml` 落地 + Pages 启用（`build_type=workflow`）⇒ **https://wangsan71.github.io/High-density-codes/** 已上线；实测 `index.html` 与本地 `web/dist/index.html` **逐字节相同**（sha256 `1759ccb1…`）、`manifest.webmanifest` / `sw.js` / `pskt-file.html` / `pskt-send-file.html` / `icon-192.png` 全部 200 ⇒ **secure context 这条阻塞解除**；**仍差真浏览器里点一次「添加到主屏幕」确认**（G9 的那一半，本机无浏览器）。原文保留（下方划线处为历史）：~~需用户的仓库 URL + 交互凭据 + `.github/workflows/pages.yml`（未写）~~⇒ 在它之前：**手机端可用、SW 预缓存的一切检查都过，但浏览器仍不会弹安装**（非 secure context ✗）⇒ `docs/USE.md` 与 `check-lan` 的输出都改成**逐条说"哪条已满足、哪条仍阻塞"**（修之前 `check-lan` 硬编码打印"neither is fixed"⇒ 图标修好后那句就成了假话 ✗ 已改 ✓） |

### 第 40 轮新增（OPEN）

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| D42 | **我把"node 里不能 spawn 子进程"错误推广成"语料不能自己造"⇒ 数轮把 G2 按 32/200 记账，并把"200 seeds 需用户带外手跑"写进 `docs/STATUS.md` 与 `docs/ACCEPTANCE.md`** ✗ 实际：**pwsh 工具跑 python 一直是通的**（`AGENTS.md`"环境事实"里的 Python 3.10.9 / numpy / cv2 本来就是这么探测出来的 ✓）⇒ 被禁的只是 **node 进程内的管道 stdio**，不是宿主 shell ⇒ 一个作用域记错，让门限的样本量长期停在判据的 1/6，还让"下一步"里排着一件**根本不需要用户**的事 | 实测（第 40 轮）：`python sim/channel.py --in .tmp/g2src --out .tmp/nc-scan300-17 --seed 17 --preset scan300 --modifier nocrop` ⇒ **15 s / 26.5 MB / 3 页**，`verify --gate G2 --corpus .tmp/nc-scan300-17` ⇒ exit 0、`204800 bytes, digest verified` **字节相同** ✓ 批量 seeds 18–29 ⇒ **12 份 / 174 s** ✓ `nc-scan*` 由 32 → **45** ✓ 再判 `--root .tmp --match 'nc-scan300-*'` ⇒ **exit 0、29/29 byte-exact、134.6 s**（4.6 s/份）⇒ 200 seeds 的代价可算：scan300 侧 ≈ **50 min / 5.3 GB**，600 dpi 侧像素约 4× ⇒ 合计数小时 / ~25 GB（D 盘 2 TB ✓）· 另测得操作事实：判**全 45 份**超工具 **600 s** 上限被杀（`exit 1` = 终止 ✓ 600 dpi 那 16 份占大头）⇒ **按 dpi 分开判**，且 `timeoutMs` 传更大值**无效**（执行器封顶）✗ | **OPEN ✗（实质只剩 ② 的语料量）** 第 49 轮**逐条核对原文**后的真实状态：**① 已完成**——grep 全 `docs/` 的"带外 / 由用户生成 / 只能由用户"⇒ **`ACCEPTANCE.md` 零命中**（它的 G2 段现在写的是"第 37 轮起这是门限、不再只是手工命令"，并给出 `verify --gate G2 --root .tmp --match 'nc-scan*'` ✓）⇒ 命中只剩本行与 `STATUS.md` 的历史交接记录（**历史记录按原样保留、不改写** ✓）；**③ 已完成**（D41 行那句旧口径已就地标注"已被同轮的对拍测试取代"⇒ 原文保留、只加标注 ✓）；**④ 已完成**（`AGENTS.md` 陷阱表现在明写"作用域仅限 node 进程内的管道 stdio；宿主 shell 跑 python/git/node 是通的"，并附已验证命令与 `timeoutMs` **600 s 封顶** ✓）；**② 进行中**：scan300 侧原为 **seed 1–29**（29/29 逐字节 ✓），第 49 轮已起后台批 seed 30–200（`python sim/channel.py --in .tmp/g2src --out .tmp/nc-scan300-N --seed N --preset scan300 --modifier nocrop`，本轮**判决已出（第 51 轮）**：`PASS G2 corpus: 200/200 byte-exact in 1085.4s` · `ALL GATES PASS` · `GATE exit=0`（生成 200/200 exit 0；判决 judged=200 ok=200 fail=0 ⇒ **样本量已对齐 PLAN 的 200 seeds、判据未放宽** ✓ 门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200` ✓ 判决中若干份带**逐页提示**如 `markers/no-hollow-corner [fiducial-out-of-frame] 2 of 4 corners` 而该份仍 `OK 204800 bytes, digest verified` ⇒ 那是冗余在起作用、不是失败 ✓）**但这 200 份语料的源页是"只有码区、没有纸张"的 PNG ⇒ 只能记作"码区前提下 100%"，不是"用户今天打印的那张 A4 100%"** ✗ 病根见 **D45**（`--sheet` 只对 PDF 生效）⇒ 栅格路径修好后必须重出源页、重跑两侧 seed 1..200（顺序见 `ACCEPTANCE.md` G2 段末 (a)(b)(c)）；scan600 侧仍是 **seed 1–16**，其 2 例失败很可能是同一夹具形状造成的（第 49 轮"A4 在平板上旋转的真实物理"那句归因**已撤回** ✗，"不是解码器缺陷"不变 ✓）⇒ **② 维持 OPEN 且依赖 D45** ✗ 不再按"下一轮补齐 scan600"排期（那是用同一前提白跑约 2.5 h ✗ 我因此**没有**起那个批 ✓） ⇒ ② 里"诊断两类失败"这一子项**其实早已完成**：`ACCEPTANCE.md` 的 G2 段已像素级定界（失败页四边着墨率 `top 4.7% / left 89.5% / right 80.8% / bottom 100%`，健康页四边均 100% ⇒ **角标出画 ⇒ 属信道前提、不属解码器**，且 3 点只提供 6 个方程、单应有 8 个自由度 ⇒ **拒绝在数学上正确** ✓ 判决**不上调**、仍记 30/32 ⇒ 把正确拒绝从分母里剔掉等于重定义判据 ✗）⇒ 我第 49 轮之前的上下文摘要把它当"待诊断"是**过期信息** ✗ 已核对原文纠正 ✓ **判据不动**：G2 仍要求 **200 固定 seed × ≤1 MB → 100% 逐字节**；样本量不足时门限自己打印 `PLAN asks for 200 fixed seeds, this run had N` ⇒ 不会被读成达标 ✓ |

### 第 38 轮新增（D41 ⇒ **第 39 轮已闭** ✓）

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| D41 | **我拿一份从网上取来的 schema 变体当权威，判了自己发射器"违规"——而权威文件 `ref/3mf-core-1.4.0.xsd` 一直在盘上、`docs/STATUS.md` L38 还点名了它** ✗ 后果具体：新写的 `tools/check-3mf.mjs` 第一版把 `object/@name` 判为违规 ⇒ `verify --gate G8` 对我们**每一份** 3MF（含 M5 那轮的历史产物）报 FAIL ⇒ **若我信了它，下一轮就会去"修"一个不存在的缺陷**：删发射器的 `name`、改 `parseModelXml` 的 object 正则（它把 `name` 当必需且在固定位置 ⇒ 删了就一个对象都读不出）、动 mesh 测试、并因 `core/` 改动重建 `web/dist` ⇒ 一次纯粹的自我破坏 ✓ 根因不是"取错文件"，而是**没有先查工作区**（AGENTS.md 与 goal 提示都写着 inspect instead of assuming ✗ 我 grep 了 `*.mjs` 却没看 `ref/`）⇒ **D29 同族（引用未核实/非权威证据）第 3 次** | 权威原文（本地 vendored ✓ 逐条读过）：`ref/3mf-core-1.4.0.xsd` **L84-101** `CT_Object` = `id/type/thumbnail/partnumber/`**`name`**`/pid/pindex` + `xs:anyAttribute namespace="##other"` ⇒ **`name` 合法**；**L211-217** `ST_ObjectType` 有 **5** 个枚举（网上变体只有 2 个）；**L201-208** `ST_ResourceID/Index` = positiveInteger/nonNegativeInteger 且 `maxExclusive 2^31`；**L110-125** `CT_Vertices` minOccurs=3、`CT_Triangles` minOccurs=1；**L221-236** core 元素只有 **16** 个（**无** `colorgroup`/`color`/`texture2d` ⇒ 属扩展 schema ⇒ 我表里那两个是凭记忆多加的 ✗）⇒ 按权威改表后实测：`node cli/pskit.mjs verify --gate G8` **PASS / exit 0** —— 本轮造的 `PL-D2@0.4`（3 objects / 377964 triangles / 4.1 MB）✓ 历史产物 `.tmp/m5det1/page-000.3mf`（426444 tri）✓ `.tmp/m5verify/page-000.3mf`（45132 tri）✓ 且 `tests/unit/threeMF-subset.test.mjs` **25/25**：21 条篡改用例每条按预期原因触发 ✓ 4 组正例含 **"D40 回归守卫"（`name`+`partnumber` 必须一直合法）**、`p:UUID` 这类**带前缀的扩展属性必须合法**、**5 个 `type` 枚举必须合法**、**命名空间用前缀绑定也必须通过**（证明它解析命名空间而非匹配字符串 ✓） | **CLOSED（第 39 轮 ✓）** 修法（按计划做完 ✓）：**加一条"表 ⇄ 权威 XSD 对拍"的测试**——用 `check-3mf.mjs` 自己的 `scanXml` 读 `ref/3mf-core-1.4.0.xsd`，抽出每个 `CT_*` 的属性名集合与 16 个元素名，与 `ELEMENTS` 逐项比对 ⇒ 表再抄错/漂移就红 ✓ **当时的**保护只有"篡改用例 + 正例"：它防得住**已知规则失效**，防不住**表本身抄错**（本轮正是抄错 ✗）——**这半句已被同一轮新增的对拍测试取代**（现在表抄错或漂移会直接红 ✓）⇒ 原文按原样保留、只加这句标注，是为了记录**当时的缺口**，别再把它当现状读 ✗（**第 49 轮标注**，即 D42 的剩余动作 ③）⇒ 另把"先查 `ref/` 再上网"写进 `AGENTS.md` ✓ |

### 第 38 轮**撤回**的（D40 **不是缺陷** ✗ 下面那行保留为历史、**别信它**）

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| D40 | **〔第 38 轮**撤回**：本行结论经权威 XSD 复核为**错** ⇒ `ref/3mf-core-1.4.0.xsd` **L97** 明确声明 `<xs:attribute name="name" type="xs:string"/>` 于 `CT_Object`、且 **L100** 有 `xs:anyAttribute namespace="##other"` ⇒ `object/@name` **合法**；我引的是网上那份旧变体（无 `pid`、`materials`/`materialtype`、只有 2 个 `type` 枚举）✗ 真缺陷是"引非权威来源 + 没先查工作区"⇒ 见上方 **D41** ✓ 本行保留为历史 ✗ 别照着它去改发射器〕** **我们发出的每一份 `.3mf` 都在 `<object>` 上写 `name="…"`，而 3MF Core schema 的 `CT_Object` 没有这个属性、也没有 `xs:anyAttribute` 兜底** ⇒ 严格校验器/部分切片机会拒收 ✗ 证据是**取来的原文、不是我记忆里的**：3MF Consortium 自己的 schema `https://raw.githubusercontent.com/3MFConsortium/3mf-samples/master/verifier/3mf.xsd`（命名空间与我们发射的 `core/2015/02` 一致 ✓）里 `CT_Object` 只声明 `id/type/materialtype/materialid`（该变体）· Core 1.4 官方是 `id/type/pid/pindex/thumbnail` ⇒ **两个来源都没有 `name`** ✓ 且 schema 头写着 `blockDefault="#all"`、`attributeFormDefault="unqualified"` ⇒ 未声明属性即违规 ✓ 影响面：**不是本轮回归，是我们发过的每一份**（历史产物 `.tmp/m5det1/page-000.3mf`、`page-004.3mf` 实测同样命中 ✓）⇒ 而 `selfCheck3MF` 对同一份文件报 `ok` ✗——因为它的解析器"只认自己写出来的那种形状"⇒ **自检在原理上无法发现"这个形状本身不合 schema"** ✓ 本轮写独立校验器的理由被当场兑现 | `node cli/pskit.mjs verify --gate G8`（进程内造 PL-D2@0.4 板材 3MF：3 objects / 377964 triangles / 4.1 MB）⇒ **FAIL / exit 1**，且**唯一**一类问题就是 `attribute "name" is not allowed on <object>`（包结构、部件名、内容类型覆盖、关系与起始部件、unit、id 唯一性与正整数、三角形索引范围、退化三角形、DTD/实体、命名空间**全部通过** ✓）· `--gate G8 --file .tmp/m5det1/page-000.3mf,.tmp/m5det1/page-004.3mf` 对历史产物同样 FAIL ✓ 规则**可失败**由 `tests/unit/threeMF-subset.test.mjs` 保证：**18 个篡改用例（每条规则一个，且断言失败原因匹配）+ 4 个正例**，正例含"命名空间用前缀 `m:` 绑定也必须通过"⇒ 证明它**解析命名空间而不是匹配字符串** ✓ 22/22 绿 ✓ | **OPEN ✗** 修法（**需一整轮**）：把对象名移到 schema 允许的位置（Core 1.4 的 `CT_Object` 允许 `<metadata>` 子元素 ⇒ 写 `<metadata name="pskt:object">`，或只留在包级 metadata）⇒ **牵连**：`core/mesh/threeMF.js` 的发射器 + `parseModelXml` 的 object 正则（它把 `name="…"` 当**必需且在固定位置** ⇒ 删属性会直接读不出对象 ✗）+ `tests/unit/mesh-3mf.test.mjs` 若干断言 + **`core/` 改动 ⇒ 必须重建 `web/dist` 再跑 check-dist** ✓ **不做"把校验规则放宽到允许 name"这种修法** ✗ 那是改判据让红变绿 ✓ |

### 第 37 轮闭掉的

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D38~~ | **G2 存在一条"假绿"通道**：盘上没有任何东西能区分"干净渲染的语料"与"过了信道的语料" ⇒ 实测 `.tmp/g2src`（干净）与 `.tmp/g2scan`（过信道）**文件清单完全相同**（`manifest.json` + 3 张 PNG + `pskt-received.out`）✗ 也没有信道报告存在页旁 ✗ 而干净渲染**必然逐字节还原** ⇒ 把 `--gate G2` 指向 `.tmp/g2src` 就会打印 `100% byte-exact` 而**什么都没测**（G2 测的是"印出来、扫回来还对不对"✓）⇒ 这正是本仓库唯一不可原谅的输出形态 | 新增 `corpusProvenance(dir)`（`tools/g2-corpus.mjs` 导出 ✓）：**量灰阶数**——我们的渲染器只发少数电平（300dpi 页 **10 阶** = 调色板+抗锯齿），而模糊/噪声/光照把扫描铺满值域（同一页 **249–256 阶**）✓ **在盘上 119 个语料目录上实测分离：干净渲染 10–18 阶 · 过了信道的 192–256 阶 ⇒ 地板取 64，两边各留 10 倍余量 ⇒ 不是调出来的旋钮 ✓** 阳性对照：`node cli/pskit.mjs verify --gate G2 --corpus .tmp/g2src` ⇒ **`FAIL G2: .tmp/g2src looks pristine (10 grey levels in page-000.png, floor 64)` 且 exit 1** ✓ 真语料 `--corpus .tmp/g2scan` ⇒ 正常评估、`PASS ... 1/1 byte-exact` ✓ | CLOSED（**只每语料量第一页**：这一问是"这批页怎么来的"、不是"每页质量如何"✓ 后者本来就是摘要判据的事 ✓） |
| ~~D39~~ | **`cmdVerify` 的 `allOk &&= ok` 会把任何真值当成通过** ⇒ 一个返回对象的 runner（例如"我没法评估"的 `{skipped:...}`）会被算成**绿** ✗ 接 G2 时必然踩到：G2 在没有语料时**不该算通过、也不该让其余门限的绿作废** ⇒ 需要第三种状态，而旧循环只有两种 | 循环现在显式识别 skip ⇒ 汇总行**点名**未评估的门限：`ALL GATES PASS -- 0/1 evaluated, 1 skipped` + 一行给出"要怎么才能评估它"（含生成语料的 python 命令 ✓）⇒ `ALL GATES PASS` 前缀保持可 grep ✓ 但**不能再被引用成"覆盖了比实际更多的门限"** ✓ 复现：`node cli/pskit.mjs verify --gate G2`（不给 `--corpus`）⇒ exit 0 + 点名跳过 ✓ 而 `verify --gate all --seeds 2` ⇒ 仍 `ALL GATES PASS`、G0 `247 passed, 0 failed`、并多出 G2 的跳过说明 ✓ | CLOSED |



### 第 36 轮闭掉的

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D36~~ | `no-hollow-corner` 语义过载（"第四角没拍进画面"与"角标在但孔读不出"共用一个 reason ✓ 曾连续误导两次根因分析 ✓）· 第 33 轮的拆法被否证 ✓ **但那条否证记录的机制本身也是错的**（见下）✗ | **真机制（实测 ✓ 非推理）**：`clustersTried=[{clusterPx:30,...},{clusterPx:5,...}]` 而最终报告 `maxBlobSide=5` ⇒ 失败是从 **5px 的数据格点簇**报出来的（`progressScore` 平手时 `+candidates` ⇒ 152 成员的格点簇压过 4 成员的角标簇 ✓ 即 **D37**）⇒ 我那个"任意三元组 + 附近有没有 blob"的判别器是**在格点上**算的 ⇒ 格点里任取三个都能拼直角 ⇒ 必然误报 ✗ 而我当时写的"top-14 按面积把真角标挤出去"是**编的**：实测 30px 簇只有 4 个成员、面积 `900/900/900/841`、**我画的方块就在 top-14 里** ✗（我引用的 `cand=159` 是 detectIn 覆写的 `squares.length`、不是枚举池大小 ⇒ **又一次引用未核实的证据 ⇒ D29 同族 ✓**）**sound 拆法**：三个同尺寸方块若构成页尺度直角 ⇒ 它们确定的矩形有第四角 ⇒ **该点是否落在图像内是一次边界检查、没有容差可调** ✓ 越界 ⇒ `fourth-corner-out-of-frame`（新 reason ✓ rank 4 ✓ advice 已配 ✓）· 在界内 ⇒ 仍是 `no-hollow-corner`（"角标读不出/被遮"✓ 建议清污翻面重印 ✓ 语义正确）✓ **几何事实**：轴对齐时第四角必在前三点的包围盒内 ⇒ 该分支**只在旋转/透视下触发**——而那正是手持手机的情形 ✓ **判据**：`tests/unit/fiducial-corner-damage.test.mjs` 5 条全绿 ✓ 含"臂长 609px、隐含角 (1400,920) 越界 ⇒ 必须报 out-of-frame"✓ 与"隐含角 (1000,1000) 在界内 ⇒ 不许甩锅给取景"✓ 与"四点全实心仍走原路径"✓ 与"三点共线 ⇒ `no-rectangular-quad`（分支不能被垃圾触发）"✓ 全套 244/244 ✓ | CLOSED（**并修正记录**：第 33 轮那条否证保留为历史 ✓ 但其机制描述已被本行取代 ✗ 另注：第 33 轮的夹具"在全尺寸图里把角涂白"**根本不是在模拟没拍全** ✓ 那是"角在画面内但读不出"⇒ 夹具本身测错了对象 ✓ 已在新测试的注释里写明 ✓） |
| ~~D37~~ | **`progressScore` 的平手裁决在 detectIn 里选错簇**：`rank*1000 + candidates` 用于"同一幅图的多个尺寸簇"时，**候选数多者胜** ⇒ 152 成员的数据格点簇压过 4 成员的角标簇 ⇒ 用户看到的 `maxBlobSide=5`（一个格点）而角标其实是 30px ✗ 潜在影响更大：**任何在该簇上计算的判别器都是在格点上计算** ⇒ 第 24/26 轮关于"分辨率地板"的两次分析所依据的数字可能一直是格点产物 ✗ | 新增 `reasonRank()` + `betterFailure()`（rank 优先 ⇒ 更深入的失败永远赢 ✓ 平手时**取最大 anchor、再取最少成员**——"四个角就是四个角标"✓）✓ 实测同一夹具：修前 `maxBlobSide=5`、修后 **`blob=30px, cluster=30px`** ✓ 失败返回新增 `clusterPx`/`clusterMembers`/`clustersTried[].members` 便于复核 ✓ **未动 `progressScore` 本身**：它在 findMarkers 的阈值梯子里用"候选更多=更进步"是对的 ✓ 只在簇间比较时换成新裁决 ✓ 簇门槛 `>=4` 放宽为 `>=3`（真·少拍一角时只剩三个角标 ⇒ 原本连诊断都进不去、只会说"没有四个同尺寸的"✗）⇒ **buildQuad 在 3 个成员时永不返回 ok ⇒ 只扩大诊断、不扩大接受面** ✓ 由 G5 误接受门限与 244/244 单测复核 ✓ | CLOSED |



### 第 33 轮新增（OPEN）

| # | 缺陷 | 复现 | 性质 |
|---|---|---|---|
| ~~D36~~ | **〔第 36 轮修正：本行写的失败机制经实测为错 ⇒ 真机制与 sound 拆法见上方"第 36 轮闭掉的"表 ✓ 本行保留为历史 ✗〕** `no-hollow-corner` 的**语义过载仍未拆开**（ACCEPTANCE 开放缺陷 #3 ✓ 它曾连续误导两次根因分析 ✓）✓ 本轮做了一个**看起来显然、实则不成立**的拆法并把它否证掉：拿同尺寸的三个方块拼出页尺度直角 L ⇒ 用 L 推出第四角应有位置 ⇒ 看那儿有没有东西 ✗ **159 个方形候选里最大的那些是合并的数据格点** ✓ 而 `buildQuad` 的枚举池是**按面积取前 14** ⇒ 真角标挤不进去 ✓ 于是假 L 推出的 `q` 落在别处 ✓ 一个"角标在、只是孔不可读"的页被报成"没拍全" ✗ 换成全集候选则反向失衡（任意点附近更容易有东西 ⇒ 遮角情形被吞进"糊住"分支 ✓）**两个夹具互相拉扯 ⇒ 不是参数没调好 ✓ 是路子不对 ✓** | `node --test --test-isolation=none "tests/unit/fiducial-corner-damage.test.mjs"` ⇒ 打印 `D36 现状 · 第四角被遮=no-hollow-corner(hollow=0,cand=158) · 角标在而孔不可读=no-hollow-corner(hollow=0,cand=159)` ✓ **两种含义相反的失败 ✓ 同一个字符串 ✓** 且两者都被拒收（**无误接受 ✓**） | 新文件 `tests/unit/fiducial-corner-damage.test.mjs` 把这件事变成**可复现的一行输出**（此前只有散文描述 ✓）✓ **不交付会说谎的 reason** ⇒ 已撤回 ✓ 真正的拆法必须把"第四角该在哪"锚在**剖面自己的几何**上（用 layout 声明的页尺寸预测角位 ✓ 而不是靠任意三元组 ✓）⇒ 需要失败路径能拿到角标簇而不是 top-N ✓ 归 G4 待办 ✓ |



### 第 32 轮闭掉的

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D5~~ | 发送端未进单文件变体 ⇒ `file://` 下只能"收"不能"发" ⇒ 气隙场景真正缺的那一半 | 新增产物 **`web/dist/pskt-send-file.html`（306.7 KiB ✓）** ✓ 与接收端单文件同一条流水线派生（内联 CSS ✓ 内联 bundle ✓ **剥掉 `<nav class="tabs">` 回链** ✓ 标题标注单文件版 ✓）✓ 复验：`node tools/build-web.mjs && node tools/check-dist.mjs --pages .tmp/g2src` ⇒ **11/11 PASS** ✓ 其中两条新判据直接压在这件事上（"内联脚本必须能解析 ✓ 2 blocks parsed" ✓ 与"单文件除 `data:` 外不引用任何东西" ✓）✓ 另注：**真正的拦路石不是页面而是三行死代码**（见 D35 ✓） | CLOSED |
| ~~D35~~ | **三行 `export default` 死代码让 `core/render/pdf.js` 永远进不了浏览器 bundle** ⇒ 而发送端的 `pack.pdf` 是真功能（`send.html:12` ✓ 非可选）⇒ D5 一直被它挡住 ✗ 全仓库**没有任何一处** import 这三个 default（唯一的 default-import 是 tools/tests 里的 `node:` 内建 ✓ 逐一查过 ✓） | 删掉 `pdf.js/png.js/tiff.js` 三行 default ✓ 改为注释说明约定 ✓ **并加判据防复发**：新 `tests/unit/export-conventions.test.mjs`（3 条 ✓ 含"目录确实走到了"的防空跑守卫 ✓ 与"core/ 不得 import `node:` 内建"这条 AGENTS.md 早写着、此前**无判据** ✓ 的规则）✓ 负向证明：把 HEAD 版旧内容按真实编码读出来喂给同一正则 ⇒ **命中 true** ✓ 工作版 false ✓ 具名导出仍在 ✓ ⇒ 判据不是空跑 ✓ | CLOSED（两条"有约定无判据"的规则从此有判据 ✓） |

### 第 32 轮新增（OPEN）

| # | 缺陷 | 复现 | 性质 |
|---|---|---|---|
| ~~D32~~ | **PowerShell 的 `>` 重定向把 `git show` 的输出写成 UTF-16LE**（首字节 255 = `0xFF` BOM ✓）⇒ 我用 UTF-8 读它 ⇒ `indexOf('export default')` 返回 -1 ⇒ **差点得出"我的判据是空跑的"这个错结论** ✗ 靠**阳性对照**（同一正则测已知含 default 的字符串 ⇒ true ✓）才分清"文件坏了"与"正则坏了" | `node -e "console.log(require('fs').readFileSync('.tmp/oldpdf.js')[0])"` ⇒ `255` | AGENTS.md 已记着同族坑（`Get-Content`+`Set-Content` 走 CP1252 ✓）✓ 这次是**重定向写 UTF-16LE** 的新变体 ⇒ 补进本机陷阱表：**跨工具取文本一律用 `node -e` 直读真文件 ✓ 不要用 `>` 落地后再读 ✓** |
| ~~D33~~ | 交付过一段时间的**单文件页带着 `script-src 'self'` 却内联了 `<script>`** ⇒ 按规范浏览器会拒绝执行自己页面的内联脚本（[GH issue](https://github.com/mshirel/song-history/issues/247) ✓ [SO](https://stackoverflow.com/questions/76347766/content-security-policy-csp-blocking-my-local-script) ✓）✓ 而 `file://` 下 `<meta>` CSP 是否强制执行**本机无浏览器无法验证**（D18 同族 ✓）⇒ 我不声称"它一直是坏的" ✗ 只说：**它把可用性押在一个我在这里验不了的问题上** | `Select-String web\dist\pskt-file.html -Pattern "Content-Security"`（第 31 轮产物：`script-src 'self';` ✓ 同文件里有内联 `<script>` ✓） | 已改为**不依赖那个答案**：两个单文件页的 CSP 都加 `'unsafe-inline'` ✓ 并加构建断言"内联脚本若不被本页 CSP 允许 ⇒ 拒绝出产物"✓ 未选 CSP hash：文件被任何一次重新保存（哪怕只改行尾）就会静默失效 ⇒ **一个下载后不启动的工具比允许自身代码的工具更糟** ✓ 真浏览器里仍需一次人工确认 ✓ 归 D18 |
| ~~D34~~ | `build-web.mjs` 的 `bundle(entry)` 把**自动运行守卫写死成接收端的 id `"files"`** ⇒ 我造第二个 bundle（发送端）时它会在错误的页面上启动错误的入口 ✓ 属于"一个产物能跑只是因为还没有第二个"的潜在缺陷 | `git show HEAD:tools/build-web.mjs` 里 `document.getElementById("files")` 与 `__R("web/app.js")` 均为字面量 | 已参数化 `bundle(entry, autoRunGuardId)` ✓ 接收端 `('web/app.js','files')` ✓ 发送端 `('web/sender.js','sfile')` ✓（`sfile` 这个 id 在 `send.html:16` ✓ 读过的 ✓ 不是猜的 ✓） |

### 第 31 轮闭掉的

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D24~~ | 手机端"角标分辨率地板"（150dpi 等效 24 个几何候选全灭在 `no-hollow-corner` ✓ 当时据此以为手机必须靠近） | **由第 30 轮的 D26 修复顺带闭掉 ✓ 同一条命令、同一夹具重测**：`node tools/probe-curl-tolerance.mjs --down 2 --rot 0 --bend 0,4,12,24` ⇒ `CONTROL ... decode P-M1-300@300/INK2` ✓ `bend 0px DECODED 符号一致 68904/68904 = 100.00%` ✓ ⇒ **地板不存在** ✓ 那是那 1 个像素的取整 bug ✓ 本行原写的"像素预算"判断（第 28 轮已撤回 ✗）现在彻底没有残余 | CLOSED |
| ~~D4~~ | `index.html` 里没有指向 `send.html` 的入口 ⇒ 接收端用户点不到发送端 | `web/index.html` 加 `<p class="nav"><a href="./send.html">`（链接文字明说"单文件版需从站点打开才有这一页"✓）✓ `node tools/build-web.mjs && node tools/check-dist.mjs` ⇒ **9/9 PASS**（含"每条 markup 引用都落在产物里"✓ 与"单文件版除 `data:` 外不引用任何东西"✓）✓ 实测：`web/dist/pskt-file.html` 里 `send.html` 出现 **0 次** ✓ `web/dist/index.html` 里导航 **1 条** ✓ | CLOSED |
| ~~D14~~ | `send.html` 的"纸张 A4/Letter"下拉框是惰性的 ⇒ UI 说了谎 | **删控件、不接线**：`<select id="ssheet">` 换成只读 `<span id="ssheetinfo">` + 标签写"纸张（由剖面决定）· 编好后显示在下方日志" ✓ 真值本来就在日志里（`sender.js:260` 打印 `纸 W×Hmm` ✓ 取自 `t.geom.sheetMm` ✓ **没有新造数字** ✓）✓ `check-dist` 的 DOM-id 契约断言（48→仍全绿 ✓）证明没有 JS 再去抓那个被删掉的 id | CLOSED（"改成能用的纸张选择器"属于新能力 ⇒ 若要做需连编码器一起改 ✓ 不在本轮范围 ✓） |

### 第 31 轮新增（OPEN）

| # | 缺陷 | 复现 | 性质 |
|---|---|---|---|
| ~~D30~~ | 我给 `send.html` 加"回链到接收端"时**没有先看那页已有没有** ⇒ 那里本来就有 `<nav class="tabs"><a href="./index.html">→ 接收端（扫描/解码）</a></nav>` ✓ 我加出了**第二条重复回链** ✗ 由 `check-dist` 的引用计数目视复核抓获（不是我推理出来的 ✓ 是打印产物内容看到的 ✓）⇒ 撤掉我加的那条 ✓ 复验：站点 `send.html` 里 `href="./index.html"` 恰好 **1 条** | `Select-String web\dist\send.html -Pattern "href=.\./index\.html" \| Measure-Object` | 教训与 D4/D5 同源：**改 UI 前先读那页现有的东西** ✓ 本轮 index.html 也犯过一次同类（我把 `<p class="sub">` 那句"不联网、不上传、不需要 URL"**整段替换成了导航链接** ✓ 差点删掉项目最重要的那句承诺 ✗ 立刻补回 ✓）⇒ 两处都属"用 edit 时 old_string 圈大了" ✓ 圈定应只圈要改的那个元素 ✓ |
| ~~D31~~ | 我在 `tools/build-web.mjs` 加的单文件剥离规则是 `/\s*<p class="nav">[\s\S]*?<\/p>/` ✓ 它**按 class 名**匹配 ⇒ 若将来别处也出现 `p.nav`、或站点版那条链接被误删 ✓ 现有判据**全都不会报**（它们只验"剩下的引用都能落到产物上"✓ 从不验"该在的引用还在"）✗ | 补了配对断言后：`node tools/check-dist.mjs` ⇒ 新增一条 `the served index.html keeps its link to the sender` ✓ 它同时断言**站点版必须有、单文件版必须没有** ✓ 去掉任一侧都会红 | CLOSED（同轮补断言 ✓ 没推到下轮：**"一行的事留给下轮"是这个台账里最常见的自欺 ✓**） |

### 第 30 轮闭掉的

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D26~~ | 角标检测对尺度**不单调**（105/120dpi 能找到四角 ✓ 150dpi 找不到 ✓） | 根因：`hasHole` 用 `round(comp.cx)` 取点 ✓ 而 `components()` 的 `cx = (x0+x1+1)/2` ⇒ 奇数边长时与"向下取整的 bbox 中心"**正好差 1 px** ✓ 15px 角标 + 半径 2 的探针 ⇒ 这 1px 把窗口从孔里推进环墨（**实测 0.36 vs 0.00 ✓ 判据 0.35** ✓）⇒ 旧行为的"随机"其实是**角标奇偶性决定边界落在哪边** ✓ 改为 `floor((x0+x1)/2)` ✓ 复验：`node tools/probe-marker-scale.mjs` ⇒ **300/210/150/120/105/90/75 dpi 全部 FOUND** ✓ `exit=0` ✓ 单测 **233/233**（含"90° 旋转页仍能解出 ✓ 朝向来自空心角标"✓）✓ `node cli/pskit.mjs verify --gate all --seeds 2` ⇒ **`ALL GATES PASS`** ✓ | CLOSED（**不是放松判据** ✓ 实心角标在新取样点处处仍读 1.00 ✓ 只是取样点错了一个像素 ✓） |

### 第 30 轮新增（OPEN）

| # | 缺陷 | 复现 | 性质 |
|---|---|---|---|
| D28 | **后台验证与我的编辑赛跑**：第一次编辑后启动"gates + build + check-dist + 两条冒烟" ✓ 随后又改了同一个 `core/` 文件（重写注释时 `old_string` 只圈注释块 ✓ 而 `new_string` 又带了一遍 `const cx/cy` ⇒ **重复声明** ✗）✓ 于是同一次运行里**门限跑在好文件上（PASS ✓）而 check-dist 与两条冒烟跑在坏文件上（三条红 ✗）** ⇒ 一半结论作废 ✓ 且差点让我误判"修法错了" | `Select-String .tmp\cd30.txt -Pattern FAIL` ⇒ `Identifier 'cx' has already been declared` | **流程缺陷**：改 `core/` 后启动的后台验证在跑完前**不得再改 `core/`** ✓ 本轮起改为"文档先写完 → 再跑全套 → 最后提交" ✓ 副产品：语法错只在 Node 侧暴露 ⇒ **`check-dist` 那两条执行型判据确实抓住了纯 grep 抓不到的东西** ✓ |
| D29 | 我的 `--explain` 把 `round(comp.cx)` 那一路**标成"质心"** ✓ 而 `cx` 其实是 bbox 中心 ⇒ 这个我自己起的名字生成了一个**假根因**（"质心被环墨拉偏" ✓ 还被我写进代码注释 ✗）✓ 靠真去读 `fiducial.js:150` 才拆穿 | `core/decode/fiducial.js` L150-151 | **第 14 次"引用未证实的东西"** ✓ 且这次是**给别人的字段起名**（比猜字段名更隐蔽 ✓）⇒ 探针输出里每个标签都必须来自被读过的代码 ✓ |

### 第 29 轮（D26 收窄到"角簇/尝试的选择环节" ✓ 并撤回我自己的第二个猜想）

| # | 缺陷 | 复现 | 性质 |
|---|---|---|---|
| D27 | `tools/probe-marker-scale.mjs --explain` 需要读 `cropMask`/`components` 的真实形状才能写 ✓ 我第一次把 `cropMask` 的返回当 bin 传（它返回**裸 `Uint8Array`** ✓ 还副作用写 `region.cropInset` ✓）⇒ `TypeError: Cannot read properties of undefined (reading 'length')` ✓ **第 13 次"引用未证实的名字"** ✓ 由执行当场抓获 | `node tools/probe-marker-scale.mjs --explain --scales 0.5` | 探针侧 ✓ 未影响产物 ✓ 模式仍未断根（见 D23 ✓） |

**D26 的诊断进展（不改行为 ✓）**：`findMarkers` 自带 `clustersTried`（`fiducial.js:390/406` ✓ 每个角簇假设一条记录 ✓）⇒ 150dpi 失败时它**只试了一个簇**：`cluster 15px -> no-hollow-corner hollow=0` ✓ 而在**同一尺度、同一份裁剪后掩模**上手工走它自己的判据：15×15 且 `area=200 < 225` 的那个 blob（即带孔的那个 ✓）**中心 5×5 窗口着墨率 = 0.00** ✓ 远低于空心判据 0.35 ✓ ⇒ **空心角标明明在候选集里** ✓ 结论：丢失发生在**角簇/尝试的选择环节**（`fiducial.js` L360–400 一带 ✓ 聚簇只产出一个假设、且它取自孔被填掉的那次阈值尝试）✓ **不是二值化本身 ✓ 也不是格点几何 ✓**

**撤回我的第二个猜想** ✗ 本轮我先假设"探针半径 `round(min(w,h)*0.12)` 随尺寸跳档 ⇒ 5×5 把环墨也量进去 ⇒ 空心被判成实心" ✓ 上面那张 r1/r2/r3 表把它否证了：150dpi 处 `probe=2`（5×5 ✓）而 5×5 的着墨率是 **0.00** ✓ 孔完好 ✓ 该解释不成立 ✓ 记下来防止我以后又拿它当结论 ✓（同一轮里它还顺带否证了"角标像素预算地板"✓ 见 D24 更正 ✓）

**修法暂不下注**：要动的是 `findMarkers` 的角簇枚举/阈值尝试取舍（L360–400 ✓）✓ 那是角标朝向判定的所在地 ⇒ **改完必须重跑 `tests/unit`（含"90 度旋转页仍能解出 ✓ 朝向来自空心角标"这条）+ 全套门限** ✓ 未做 ✓ 下一轮首位 ✓



### 第 28 轮（一次"测量否决了自己的修复" ✓ 并抓出更锋利的 D26）

| # | 缺陷 | 复现 | 性质 |
|---|---|---|---|
| ~~D26~~ | **角标检测对尺度不单调**：同一张干净降采样的页（无模糊、无噪声、无 JPEG），**105dpi 与 120dpi 能找到四角 ✓ 150dpi 却找不到** ✓ 90dpi 又失败 ✓ 即"越清晰越认不出"的反复翻转 ✓ 三次翻转 | `node tools/probe-marker-scale.mjs` ⇒ `exit=2` + `NON-MONOTONIC` 三行翻转记录 | **这才是手机端真正的地板问题**（不是我上一轮说的"像素预算"✗ 已撤回 ✓）✓ 失败形态统一是 `hollow=0` 而候选很多（150dpi 时 cand=10 ✓）⇒ 像二值化阈值阶梯/候选选择与该尺度的相互作用 ✓ 定位它才能给出"该靠近还是该重拍"的分类 ✓ |

**本轮撤回我自己的两句话（都是我自己上一轮写的）** ✗
1. **D24 的"角标像素预算是地板"说过头了** ✓ 真凭据是 D26 的非单调：105/120dpi 能解、150dpi 不能 ✓ 若"像素不够"是唯一原因 ⇒ 更高分辨率只会更好 ✓ 数据直接否证 ✓
2. **"用一个像素阈值拆 `no-hollow-corner`"这个修复方案作废** ✓ 因为**现在没有合法信号可用**：我加的读数原本叫 `maxMarkerSide` ✓ 实测在两个失败例里分别读到 **15 与 9**（失败得更"轻"的那个反而更大 ✓）⇒ 它量的是**最大连通块**（很可能是粘连的数据格）✓ **不是角标尺寸** ✓ 拿它定阈值＝给假信号立法 ✓ 该字段已改名 `maxBlobSide` 并在注释里写明它不是什么、不许用它推像素地板 ✓

**D24 后半（拆 reason）因此从"本轮就做"变成"阻塞在 D26"**：先定位非单调的成因 ✓ 有了真信号再拆 ✓ 不硬造分类 ✗

**顺带的事实**：`core/decode/fiducial.js` 只新增失败返回里的诊断字段（`maxBlobSide` / `candidates` ✓ 不改任何判定）✓ 全套单测 **233/233** 通过 ✓ 门限重跑见 `docs/STATUS.md` 第 28 轮 ✓


### 第 27 轮闭掉的（第一次由测量决定特性优先级 ✓）

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D25~~ | 卷曲在页级可造成大面积错读，**传输级未验证** | `node tools/probe-curl-tolerance.mjs --down 1 --rot 0 --bend 0,4,12 --transfer` ⇒ 控制档 `bend=0` **ACCEPTED-CORRECT**（1024 B vs 1024 B ✓ `fed×3` ✓）⇒ 表算数；`bend=4px`（页级曾 47.9% 错读）在传输级被 **`assemble-rejected/intra-fail×3` → REFUSED（`have 0/1`）** ✓✓ 页间/页内 RS + 末端 SHA-256 **精确兜住 ⇒ 零误接受** ✓；`bend=12px` 直接解码拒绝 ✓ | CLOSED：**误接受为 0 的契约在该形变下成立**（合成信道 ✓ 不替代 G4 真实照片档 ✓） |
| ~~D21~~ | PLAN L77 的"四边双频对齐梳 / 亚像素格点相位"无实现 | 同上一条命令（`--transfer` ✓ 传输级只拒不误收 ✓） | CLOSED-为判决、**非闭为已实现**：梳子仍未实现 ✓ 但**不足以成为改动采样路径（G3/G5/G7 全压其上）的理由** ⇒ 降级为"G4 真机数据出来后按实测定去留" ✓ PLAN 里该项仍标未实现 ✓ 不改契约文本 ✓ |

**本轮也撤回我自己写的一句过重的话**：我先在探针注释里写"连拍冒烟的 `feed` 是桩 ⇒ 手机真接线从未执行"✓ 读 `tools/smoke-capture.mjs:49-52` 后确认那里用的**就是真 `TransferAssembler`**（`asm.feed({levels, header: headerBytes, channelMissing})` ✓ 与 `cli/pskit.mjs:448` 同形状 ✓）且 63-64 行断言 `asm.result` 摘要 ✓ 桩只出现在门控子用例（恰当 ✓）⇒ **手机那条链其实已被 12/12 那轮证明** ✓ 注释已改回事实 ✓ 记此一条：**虚假的自我批评同样是台账污染**（与虚高、虚低同一类错 ✓）



### 第 26 轮新增（OPEN）

| # | 缺陷 | 证据 / 复现 | 性质 |
|---|---|---|---|
| ~~D24~~ | **角标是手机端的分辨率地板，而 reason 会说谎**：同一页降到 1/4 面积（2260×3290 → 1130×1645 ✓ 等效 150dpi ✓ 仍是清晰降采样、无模糊噪声）⇒ **24 个几何候选全部死在 `stage:markers / reason:no-hollow-corner`** ✓ 根本没走到读出。也就是说 `rectifyPage(bitmap, layout, found.quad)` 在原理上是跨尺度的（把实测四角映到 layout 画布 ✓ 我上一轮"几何搜索不跨尺度"的说法**说过头了，撤回 ✗**）✓ 真正卡住手机的是**第四空心角在低分辨率下测不出来** | `node tools/probe-marker-scale.mjs` ⇒ 见 D26 ✓（本行原写的"角标像素预算地板"**已撤回** ✓ 该探针证明失败与尺度非单调 ✓） | 原判断"手机端卡在角标像素预算"**不成立** ✓ 剩下的真问题是 D26 的非单调 + `no-hollow-corner` 语义过载（`ACCEPTANCE.md` 开放缺陷 #3 ✓ 本轮又一次把我引偏 ✓）⇒ 拆 reason 阻塞在 D26 定位 ✓ |
| ~~D25~~ | **卷曲在页级可造成大面积错读，传输级尚未验证**：native dpi 下 `bend=4px`（页中部相对四角 4 像素起伏 ✓ 真实卷纸远大于此）⇒ `bootstrapDecode` 返回 `ok:true` ✓ 而 68904 格里 **33021 格与真值不符（52.08% 一致）**；`bend≥12px` 则干脆拒绝（`no-geometry-matched` ✓ 安全）✓ **这不是误接受**——页级 ok 之上还有页间/页内 RS + 末端 SHA-256 ✓ 误接受与否由那条链定夺（G5：10000 次篡改 0 误接受 ✓）✓ 但**本探针没有替该链说话**：必须把整盘页送进组装/校验路径重测 | `node tools/probe-curl-tolerance.mjs --down 1 --rot 0 --bend 0,4,12,24,48` | **D21（对齐梳）的判决依据**：若传输级也能拒 ⇒ 梳子是鲁棒性收益、不该为此改动采样路径；若传输级**接受**了 ⇒ 属误接受家族 ✓ 优先级立刻高于 G4 ✓ 本轮未做（预算）⇒ 下一轮第一件 |

### 第 26 轮闭掉的

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D16~~ | `docs/ACCEPTANCE.md` 的 G9 行仍写"未开始/零证据" | G9 行改为 🟡 并列出 9 条产物级判据与复现命令 ✓ "一句话结论"里对 G9 的**低估**一并更正 ✓ 页头"最后更新"升至第 26 轮 | CLOSED（本轮同时说明：台账虚高与虚低同样是错 ✓） |

### D21 重新定标（第 26 轮 · 由意见改为量测）

原记"对齐梳无实现"✓ 仍然成立 ✓ 但本轮把它从"缺一项 PLAN 特性"改成可判定的命题：**四角单应能表达平移/旋转/均匀与微分缩放/剪切/透视（仿射 6 + 射影 2 自由度）✓ 逐格另有 ±1px 整数搜索 ✓ 所以梳子唯一能补的是单应表达不了的离面形变（卷曲/起伏）** ⇒ 于是要量的不是"要不要忠于 PLAN"而是"**卷曲在什么量级开始造成什么后果**" ✓ 已有页级数据（D25）✓ 待传输级数据 ⇒ 有了它才谈得上"值不值得为此动 G3/G5/G7 压着的那条采样路径" ✓



### 第 25 轮闭掉的

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D11~~ | 构建中途失败会在 `web/dist` 留半套产物（Pages 可发布出破损站） | 把 `web/send.html` 移走 ⇒ `build exit=1` ✓ 而 `web/dist` 文件数 **49→49**、`sw.js` 仍在 ✓ 恢复后无 `dist.tmp` 残留 | CLOSED（改为 `web/dist.tmp` 写完后换入 ✓） |
| ~~D17~~ | 连拍跨会话守卫"疑似"失效 | `node tools/smoke-capture.mjs` ⇒ **12/12 全通过** | CLOSED — **产品从来没错，是我的测试夹具错**：夹具第一帧喂了 `addFrame({})`（空对象）⇒ 被 `no-header` 拒 ✓ `current` 一直为 null ⇒ 第二帧自成新会话 ✓ 症状全部对上 ✓ 教训记在下方 D23 |
| ~~D19~~ | `./capture.js` 在 precache 清单中重复 | 见 D22（已泛化为通用的清单紧度判据） | CLOSED |
| ~~D20~~ | HTML 的 id 与 JS 的 `getElementById` 无判据保护 | `node tools/check-dist.mjs` ⇒ 新增第 9 条 `every getElementById in built JS exists in some built page`（48 markup ids ✓） | CLOSED |

### 第 25 轮新增（OPEN）

| # | 缺陷 | 复现 | 性质 |
|---|---|---|---|
| ~~D21~~ | **PLAN §几何 L77 的"四边双频对齐梳 / 亚像素格点相位"在 `core/` 里没有实现**：fiducial 有（`layout.js:105` 三角实心 + 第四角空心 ⇒ 透视与朝向 ✓ 与 PLAN 等价 ✓），但全仓搜 `comb/梳/gridPhase` 只命中 `ideal.js:159` 的"小范围**整数**对齐搜索"，那不是梳 | `Select-String -Path core\**\*.js -Pattern "comb\|梳\|gridPhase"` ⇒ 无实现点 | **objective 第 (2) 条里唯一没证据的一块**：打印机的缩放/走纸造成的**亚像素格点相位偏移**目前只能靠整数搜索 + ECC 吸收 ⇒ 影响 G4 手机档与纸面鲁棒性，不是性能问题 |
| D22 | **构建自报的文件数与磁盘不符**（曾报 "81 files / 78 precache entries" ✓ 而磁盘只有 49）：`writeDist` 对同一文件写两次（import 闭包与"整份 core"两份清单重叠 ✓ 我第 24 轮为修 D 类问题加的 allCore）就**再记一条账** | 对比 `node tools/build-web.mjs` 与 `Get-ChildItem -Recurse -File web\dist \| Measure-Object` | 已修（去重 + 新增双向紧度判据：清单条目数必须等于磁盘产物数、且不得有重复 url ✓ 修后 49=49 ✓ precache 47 = 49−`sw.js`−`build-manifest.json` ✓）**但我上一轮把"78 条哈希逐字节核对"当成成绩写进了 STATUS ⇒ 那个数字当时是虚的 ✓ 已在 STATUS 更正** |
| D23 | 我这一族的错的**元缺陷**：连续 12 次"引用未证实的东西"，全部由执行抓获、**零次靠阅读发现**；而 D17 更说明**测试写错时我会先怀疑产品** ⇒ 需要一条习惯：断言红时**先打印夹具自身的中间量**（本轮就是加了 `frame1 …` 一行才看清） | 见 `docs/STATUS.md` 第 23–25 轮"我的错"段 | 流程改进，非代码缺陷 |



### 第 24 轮闭掉的

| # | 缺陷 | 复验 | 状态 |
|---|---|---|---|
| ~~D7~~ | 手机连拍取页 | `node tools/smoke-capture.mjs` ⇒ 11/12：真实 3 页**逐页收下并解回原摘要** ✓ 重复页判为 `duplicate` 不算进度 ✓ "角标 6px / 覆盖率 40%" 各出一条可执行提示 ✓ 页数声明冲突拒绝 ✓ **裁掉的页不被接受** ✓ | CLOSED（连拍外壳已接线并验证；实机部分转 D18，跨会话守卫转 D17） |
| D8 | 浏览器打印服从用户的缩放选择；缩放错则几何错（帧头会拒收 ⇒ 不静默出错，但要重扫）—— **第 85 轮量到一半答案**：`tests/unit/warp.test.mjs` 实测「均匀/非均匀仿射缩放**照样逐格零错、逐字节还原**（95%、110%、95%×100% 三例），因为几何是从四角标反算的；真正致命的是**裁掉角标**（16mm 切边 ⇒ 正确拒绝）或非仿射形变」。**剩下那一半仍是物理问题**（印出来的实际尺寸是否等于标称，需尺子量，见 `docs/USE.md` §5）。 | 打印后以 300 dpi 扫描，比较 `cellPx` | 已知限制；UI 与 `pack.pdf` 是正解 ⇒ **第 48 轮：产物侧已经机器化，并且修好了**（D44 闭合 ⇒ `pack.pdf` 现在页 = 纸张 A4、码区按标称尺寸**居中**、四角裁切标记 + 四边套准十字；判据 `tests/unit/pdf-truesize.test.mjs` 用**三条互不共享代码的算路**对拍（协议几何 / 版面权威 / PDF 实字节）+ 3 条阳性对照 ⇒ 284/284 绿）⇒ **剩下的只有用户打印对话框里的缩放选择**，那需要真打印机与真浏览器 ⇒ **本条继续 OPEN**（G9/D18），不因产物侧修好就宣布闭合 ✓ |
| ~~D11~~ | **构建失败会在 `web/dist` 留下半套产物**（本轮 PWA 段抛异常时 `sw.js` 缺失、其余文件仍在），Pages 若被配成"推什么发什么"就会发出破损站点 | `node -e "..."` 使 build 中途抛错，再看 `web/dist` 存在但缺 `sw.js` | 原子性：写 `dist.tmp` 再改名换入 |
| ~~D12~~ | `tests/unit/advice-coverage.test.mjs` 的正则 `/reason:\s*'(...)'/g` **会读注释里的文字**：我为解释"把成功路径的 reason 改名"而在注释里写下那个字面量，测试立刻要求给一条**成功**配建议 | `node --test tests/unit/advice-coverage.test.mjs`（改注释前后各跑一次即见） | **CLOSED（第 46 轮 ✓）** 照原计划落地、且**没有减弱判据**：扫描前用一个**"字符串 / 模板 / 正则字面量 / 注释"四态扫描器**剥注释——**不是**拿正则删 `/*…*/` 与 `//…`，因为那会吃掉真代码，而**吃掉一条真 reason 就是悄悄把要求变少**，正是这个测试唯一不可接受的失败方向 ⇒ `tests/unit/advice-coverage.test.mjs` 重写：① `stripComments` **8 条正例**（块注释、行注释、文档注释里引用整行代码、字符串里的 `//`、正则里的 `\/\/`、`return /…/` 之后的正则字面量、字符串里的转义引号、**字符串里不闭合的 `/*`**——naive 剥离器在这里会把后面整段吃掉）② 原有两条**灵敏度钉子原样保留**（`seen.size >= 20`、点名 `intra-fail`/`other-session`）⇒ **剥过头会红、不会静默** ③ 新增**漂移守卫**：同一棵树跑"剥 / 不剥"两遍并断言两者**完全一致**（今天的树上没有只活在注释里的 reason ⇒ 将来谁写出一个，这条会**点名它**，而不是让要求集悄悄变化）⇒ 实测 **4/4 绿**、全量 **279/279**（277 + 2 新）✓ **未做的（如实记）**：没有在 `core/` 里做一次**活的复现**（为演示而改 `core/` 要重建 `web/dist`、且有留残留的风险 ✗）⇒ 用合成源正例 + 真树双向对拍顶上 ✓ 另：写这轮时我自己先写出一条**空转断言**（把 `reason` 放在 `//` 之前 ⇒ 剥离对错都会过）⇒ 已换成上面那个真能失败的形状 ✓ |
| ~~D13~~ | PWA 图标是一张 2260×3290 的**非方形**页图；`sizes` 走的是 `${w}x${h}` 分支，iOS 对非方形/`any` 支持差 ⇒ 主屏图标可能被忽略 | `node tools/build-web.mjs` 打印 `icon 211384 B (2260x3290)` | 需要在构建期裁成方形（自家编码器可重画 ✓ 不引依赖） |
| ~~D14~~ | `send.html` 上"纸张 A4/Letter"下拉框**是惰性的**：纸面尺寸实际来自 `t.geom.sheetMm`（编码器决定 ✓ 与 CLI 一致），改这个框不影响输出 | 切 A4→Letter 再编码，看日志里 `纸 W×Hmm` 不变 | UI 说了谎：应删除该控件或改为"仅提示" |
| ~~D15~~ | `sender.js` 里 `parityPages` 用 `header.kind === 'parity'` 判校验页数，`kind` 的**取值域我未证实**（不影响产物 ✓ 只影响那一句计数） | `node -e` 打印 `t.pages[0].header` | 又一个"未证实字段"实例（第 9 次 ✓ 模式未断根） |
| ~~D16~~ | `docs/ACCEPTANCE.md` 的 G9 行仍是旧结论（写"未开始"），本轮 G9 已 8/8 ✓ 台账与判据页不一致 | `Select-String docs/ACCEPTANCE.md -Pattern G9` | 记账欠账，下一轮第一件事（连同 STATUS 现况表 G9 行一起改） |

## 判读提示（避免误读别人的结论）

- `pwsh` 里用 `... | Select-Object -First N` 截断 node 的输出流会让 node 被杀 ⇒ **进程退出码 1**，而门限本身可能全绿。要判绿黑请看**末行**（`ALL GATES PASS`）或不要用 `-First`。本轮我差点把这条当成一次门限失败记进台账 ✓
