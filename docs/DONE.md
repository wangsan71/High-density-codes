# DONE · 已完成目标台账（**动工前先读这一页**）

> **为什么有这一页**：`docs/STATUS.md` 有 240+ 个轮次块，上下文一压缩就丢；这一页只记三件事 —— **已经做成的事**、**已经否掉的做法**、**只有用户能验证的事**。目的就是用户要的那句：避免重复执行、避免压缩丢记忆。
> **更新节奏**：每 10 轮更新一次（首次写于第 242 轮末，第 263 轮第二次、第 273 轮第三次、**第 283 轮第四次**）。死代码复查每 5 轮一次（最近一次：**第 282 轮 ⇒ core 导出 329 / `never` 0、模块级产品 0、孤儿工具 0）**。
> **权威源不变**：判据 `docs/PLAN.md` / `docs/PLAN-V5.md`；判决 `docs/ACCEPTANCE.md`；缺陷 `docs/DEFECTS.md`；逐轮进度 `docs/STATUS.md`；接手 `docs/HANDOVER.md`；用户手册 `docs/USE.md`。**本页只是导航与「别重做」清单，与上面冲突时以上面为准。**

---

## A. 已经做成的（每条都指出证据在哪）

| 能力 | 现状 | 证据 / 复现 |
|---|---|---|
| 编码核心（gf256 / rs / hash / chacha20 / crc / pack / deflate / frame / profiles） | 完成 | M1；`node cli/pskit.mjs verify --gate all`（G0 段） |
| 版面渲染 + PNG/TIFF 读写 + CLI `send`/`receive` | 完成 | M2；G1 **725,913 格 0 误读**（`--gate G1`） |
| 解码全链路（标记检测 → 单应 → 透视矫正 → 读格） | 完成（合成图像层） | M3；`tests/unit/warp.test.mjs` |
| 图片路：`send --image-mode lossy` + `.psk` + `tools/unpsk.mjs` | 完成，**有端到端腿** | 冒烟 §4k：图片→页→信道→载荷与 manifest 相符→可看，**PSNR 31.74 dB**；文档 5 页写法实测 **33.88 dB 未降采样**（第 241 轮） |
| 手机/无 manifest 接收：`receive --profile auto` | 完成 | 第 239 轮：无 `manifest.json` 的 3 页目录 → 10 个候选后认出 `P-M1-300@300`、逐字节还原；冒烟 §4j |
| 瓦片页（P4）：写 / 读 / 平移容错 / 剪切 40–80 px / **照片入口** | 完成（工具级） | 第 240 轮：A5 4×6、纸占画面宽 62%、转 2.5° 带梯形 ⇒ **24/24 瓦片、0 孔洞、逐字节一致**（模块 5.45 px 与 9.09 px 两档）；`node tools/make-tile-page.mjs --read photo.png --photo --sheet A5 --out payload.bin` |
| CLI 具名拒绝 | 完成 | 第 238 轮：`send <目录>` 不再抛 `EISDIR`；失败批量汇总成一行 + 主导类 `do`；冒烟 §4i |
| 单色兜底 / soak | 见 B 节门限 | `--gate G7`（**已退役**，仍可按需跑）、`node tools/soak.mjs --minutes 30` |
| Web 产物（PWA + 两个单文件页） | 产物级全绿 | `node tools/build-web.mjs`（72 文件）· `node tools/check-dist.mjs` ⇒ **顶层 13 项 + `G9 CHECK` 15 条，0 fail**（两个数字不是一回事：13 数的是顶层 record，15 数的是 G9 块内断言 —— 第 262 轮量清） |
| **真浏览器端到端**（第 249–251 轮，`HeadlessChrome/152`） | 完成（**服务版 http**） | 接收页：三张 300 dpi 页图 → **`已逐字节还原：204,800 字节 · SHA-256 32ec4805… · 3 页被接受`**，逐候选搜索**第 10 次**命中 `P-M1-300@300/INK2`（与 CLI `--profile auto` 的 10 个候选互相印证）· 发送页：3,000 B → `编好 3 页 · 1072 ms · PAPER1 · 300 dpi` + 明文摘要 + 纸面档正确禁用 3D/STL · `?selftest=1` **13/0 → GREEN** · SW 缓存 **71 条**，`pskt-bundle.js`/`index.html`/`capture.js` 缓存字节与磁盘**逐字节相同**（`sw.js` NOT CACHED = 阴性对照） |
| **真浏览器 · 补充四格**（第 254–256 轮） | 完成 | ① **加密路径三态**：无口令 ⇒ 具名拒绝；对口令 ⇒ **6,000 B 逐字节还原**（`4491cd04246c48c8…`）；**错口令 ⇒ 拒绝且什么都没写** ② **命名规范化**：`../../evil.png` 被换成摘要名（不回退到目录穿越）③ **单文件版两个产物**（`pskt-file.html` / `pskt-send-file.html`）也在浏览器里各跑通一次，其内联 bundle 与磁盘产物**互证**（同一份 3,000 B 编出的明文摘要与第 250 轮服务版一字不差）④ 摄像头被拒时的用户文案实测到位 |
| **两条新产物断言**（第 249 轮） | 完成，**都先证明能红** | ① 服务版页面不得带自己 CSP 不允许的内联脚本（修前点名那个 95 字符 loader）② 每个产物 JS 必须能 `node --check` 通过（**第一次运行就抓出两个真缺陷**：D89、D90） |
| `pskit calibrate` 读数路径 | 复活（第 249 轮） | **D90**：该文件自第 154 轮起语法就是坏的（删死代码时连函数签名一起删），7 轮无人发现 ⇒ 修后 `calibrate` **exit 0**、逐页 ECC + 油墨比 0.90x/1.19x/1.25x |
| **偶发断言普查 + 四条缺陷闭合**（第 258–261 轮） | 完成 | ① **D87 闭合**：`ciphertext == plaintext` 在 1 字节上**按构造**就有 1/256 概率相等（实测 5,000 次里 11 次，而 5,000 次往返全对）⇒ 拆成「逐样本断言（len ≥ 8）+ 聚合循环断言」② 普查全仓同类断言 ⇒ **只有 D87 一例**（另两条余量：`sha256(1 MB)` 中位 3.6 ms vs 200 ms 预算）③ **D90 闭合**：`core/decode/calibrate.js` 自第 154 轮起语法就是坏的 ⇒ **完成那次没做完的删除** ④ **D91 闭合**：`hidden` 属性被 class 的 `display` 压掉 ⇒ `web/app.css` 加 `[hidden] { display: none !important; }` ⑤ **D89 闭合**：CSP `script-src 'self'` 下的内联 loader ⇒ 改成 `web/selftest-loader.js` 用 `src` |
| 文档体检（用户手册每条命令真跑） | 完成 | 第 241 轮：USE.md 里所有本机可跑的命令逐条执行，**全部与文档一致**（含两种具名拒绝） |
| **密度梯（用户验收第 2 步）** | 完成，**有进程内等价腿** | 第 246 轮：`--make` → 模拟扫描 → `--read` 全流程进冒烟（§4l）；实测 A4@300 四档 BER 0/0/0/**1.26e-3**、净 1,878/5,272/7,574/11,882 B 每页 ⇒ **独立复现第 117 轮的 4 px 悬崖** |
| **真浏览器 · TIFF（第 276 轮，D94）** | 完成 | 浏览器接收端**此前读不了 TIFF**（`decodePNG: not a PNG` ✗，而它的提示语却说「PNG/TIFF 由内核直读」✗）⇒ 按魔数派发容器 + **多页逐页展开** ⇒ 单页（26 MB）与 **3 页合成（78 MB）** 都**逐字节还原** ✓（`3 页被接受` ✓），与 CLI 行为一致 ✓ |
| **真浏览器 · 手机路线的四格**（第 264–272 轮） | 完成 | ① **`file://` 单文件接收页**：以 `file://` 打开**发布产物** ⇒ **逐字节还原 1,500 B**（摘要另用 `node:crypto` 复核 ✓）② **手机真正会喂进来的格式 JPEG**：页面内 canvas 编出 **3,782,945 B / 2480×3508** 的 JPEG ⇒ **逐字节还原 ✓** ③ **不安全上下文（局域网 http）**：`isSecureContext=false`、摄像头按钮禁用、连拍按钮按下给可操作提示 ⇒ 「选择文件」照样**逐字节还原 ✓** ④ **自动档（三个下拉框全留默认）** 喂 clean 手机量级照片（6200×4650）⇒ `P-M1-300@300dpi INK2 第 19 次命中 · 31.8 s` ⇒ **逐字节还原**，且**候选数与 CLI 完全一致** ✓ |
| **手机档照片：定性与修复**（第 268–271 轮） | 完成（一条闭合、一条仍 OPEN） | **D93 已闭**：自动档在手机照片上读不出的真因是**候选排序把「纸面档优先」整组盖掉** ⇒ 64 次尝试全花在 600 dpi 板材档、`P-M1-300@300` 一次没被试 ✗；修法是**只加一条分组键、不删候选**（排序改不了判决 ✓）⇒ clean 11.2 px/格 照片现在自动档 **19 次 / 32.9 s** 读出 ✓；**G2 300 dpi 复核 `200/200 in 1200.9s`** 无回归 ✓。**D92 仍 OPEN**：带退化的照片卡在**回显条对比度**（已排除分辨率/模糊/噪声/JPEG/姿态/光度学/背景 ✓）⇒ 性质归属待定 ✓ |
| **验收包产物自证**（第 275 轮） | 完成（进程内等价） | `& .\tools\acceptance-kit.ps1` ⇒ **7 条腿全 PASS** ✓；把它**自己产出的**页过信道（`scan300 --modifier nocrop`）再读回 ⇒ **`paper/`（204,800 B）、`module-6/`、最密的 `module-4/` 三套全部逐字节相同** ✓，paper 的摘要与 `README.txt` 里写的一字不差 ✓。**边界**：信道是模型 ⇒ 真墨真纸仍要用户验 ✓ |
| **真浏览器 · 离线可用（第 279 轮）** | 完成（**真断网**） | `localhost` 上让 SW 接管后**关掉服务器**：同一 URL 仍 `status=200` ✓、**JS 真的执行**（`#profile` 由 `PROFILES` 动态填充出 7 项 ✓）、连**没被预缓存**的 `?selftest=1` 也由 SW 兜底送出并跑出 **`13 passed / 0 failed / 0 skipped -> SELFTEST GREEN`** ✓ ⇒「断网也能继续还原」是实测 ✓（离线**安装**那一半仍要 https ✓）|
| **真浏览器 · 手机连拍管线（第 280 轮）** | 完成（进程内等价，**不是真机**） | `canvas.captureStream()` 造的**真 MediaStream** 假摄像头 ⇒ 页面按真实约束请求（`facingMode: environment` + `focusMode: continuous` ✓）、进度走到 **`已收 3/3 页 · 缺 无`**、每页 `photo · 角标 30px`（照片路径 ✓）、状态 `完成`、结果 **`完成：1,500 B · SHA-256 cc7605e78942548a… · 已按「pskt-1500B-cc7605e78942.bin」下载`** ✓ ⇒ **软件管线无断点**（真机要过的仍是光学/AE 那层 ✗）|
| **真浏览器 · 图片载荷 → PNG（第 278 轮，D96）** | 完成 | `.psk` 图片载荷在接收页里被还原成 PNG：`这是图片载荷：已用同一份解码器还原为 PNG（96×64，q95，19578 B）` ✓，打桩截获 blob ⇒ **`type: image/png`、19,578 B** ✓；修 D96 后默认名也从 `….bin` 变成 **`….png`** ✓ |
| **验收包第 2 步 + 一键核对（第 281 轮，D97）** | 完成 | 用它**自己的**梯页过信道再 `--read` ⇒ 四档 **BER 全 0**、净容量与第 246 轮一字不差 ✓；修 D97（README 叫 `scans-module-6/5/4`、工具只认 `module-6/5/4` ⇒ 照文档做只会看到 `0/3` ✗）⇒ 两种命名都认、失败原因必打印 ✓，复验 **`3/3 profiles byte-exact in 13.4s`** ✓ |
| **Windows 退路：JPEG → PNG → 接收（第 282 轮）** | 完成 | 手机式 JPEG（6200×4650、q=88、**5,068,435 B**）⇒ `& .\tools\jpeg-to-png.ps1`（System.Drawing，另一个编码器 ✓）⇒ `receive --photo --profile P-M1-300` ⇒ **`received 1500 bytes`**、摘要 `cc7605e78942548a…` ✓ |
| 死代码复查（每 5 轮） | 完成，**现在是三条腿的命令** | 导出侧 **19 → 3**（第 242 轮起；3 个是「文档即接口」）；**第 262 轮**补上另两条腿并**提升为常驻命令** `node tools/find-dead-locals.mjs` ⇒ 模块级非导出孤儿（产品代码）**0**、**孤儿工具文件 0 / 42**；阳性对照 `--selftest` **3/3** + `tests/unit/dead-locals.test.mjs` **5 条**（双向 + 两条仓库棘轮） |

**当前基线数字（第 283 轮更新，全部本机实测）**：单测 **443/443** · `verify --gate all` ⇒ `ALL GATES PASS -- 4/5 evaluated, 1 skipped`（列出未评估 G4 G6 G9）· `check-dist` **顶层 13 项 + `G9 CHECK` 15 条，0 fail** · 单文件页 **420.1 KiB** · `usability` 全腿 PASS · **G2 `200/200 in 1200.9s`** · 死代码 core **329 / `never` 0**、非 core `never` **4**、模块级 **0**、孤儿工具 **0/42** · **缺陷**：D92 **OPEN**（退化照片卡在回显条对比度），D93/D94/D95/D96/D97 已闭 ✓。

**~~当前基线数字（第 273 轮更新）~~（已被上一行取代，原文保留）**：单测 **442/442** · `verify --gate all` ⇒ `ALL GATES PASS -- 4/5 evaluated, 1 skipped`（列出未评估 G4 G6 G9；另有 `retired: G7 G8 G10`）· `check-dist` **顶层 13 项 + `G9 CHECK` 15 条，0 fail** · `build-web` 单文件页 **420.1 KiB**（72 文件）· `usability` 全腿 PASS（约 390–436 s）· **G2 300 dpi `200/200 in 1200.9s`**（第 271 轮、改动之后 ✓）· 死代码：core 导出 **329 / `never` 0**、非 core `never` **4**（都已查证在自己文件内被调用 ⇒ 只是导出多余）、模块级产品 **0**、孤儿工具 **0/42**。

**~~当前基线数字（第 263 轮更新，全部本机实测）~~（已被上一行取代，原文保留）**：单测 **438/438** · `verify --gate all` ⇒ `ALL GATES PASS -- 4/5 evaluated, 1 skipped`（列出未评估 G4 G6 G9；另有 `retired: G7 G8 G10`）· `check-dist` **顶层 13 项 + `G9 CHECK` 15 条，0 fail** · `build-web` bundle **389.6 KiB** · `usability` 全腿 PASS（**390–436 s**）· 死代码：导出 **3**、模块级 **0**、孤儿工具 **0/42**。

**~~当前基线数字（第 253 轮更新，全部本机实测）~~（已被上一行取代，原文保留）**：单测 **433/433** · `verify --gate all` ⇒ `ALL GATES PASS -- 4/5 evaluated, 1 skipped`（列出未评估 G4 G6 G9；另有 `retired: G7 G8 G10`）· `check-dist` **15 条断言 0 fail**（61 个产物 JS 全部可解析）· `build-web` bundle **389.6 KiB** · `usability` 全腿 PASS（约 360–370 s）· 死代码复查 **3**（终点）。（上一版基线写于第 242 轮末，此处按实测更新；`verify` 的 6/7 改成 4/5 是因为第 244 轮把退役的 G7/G8 从 `all` 里摘掉了 —— **判据未放宽，是计数口径修正**。）原始行：`verify --gate all` ⇒ `ALL GATES PASS -- 6/7 evaluated, 1 skipped`（列出未评估 G4 G6 G9 G10）· `check-dist` 13/0 · `build-web` bundle **389.6 KiB**（单文件页 415.1 / 385.0 KiB）· `usability.ps1` 全腿 PASS（约 330–360 s）。

---

## B. 门限判决（权威源是 `docs/ACCEPTANCE.md`，本表只作导航）

| Gate | 判决 | 差什么 |
|---|---|---|
| G0 规格可独立实现 | ✅ | — （`python ref/decode.py` → PASS） |
| G1 渲染—读回零误读 | ✅ | — （7 档 × 3 次 = 725,913 格 0 误读） |
| G2 纸面 200 seed | 🟡 | **300 dpi ✅ `PASS 200/200`**（第 63 轮 **1245.4 s**；**第 262 轮在改动之后复核 `200/200 in 1271.1s`，第 271 轮改了解码引导逻辑（D93）之后再复核 `200/200 in 1200.9s`**，语料未重生、判据未动、失败页分母未剔 ⇒ **无回归**）；**600 dpi 无最终判决**，且用户已明示 600 dpi 非必要 |
| G3 缺页/乱序/重复 | ✅ | — |
| G4 手机压力 ≥99% | ⬜ | **500 页 × 8 轮仍未跑**（需真手机）⇒ 见 C 节；**第 280 轮**把连拍**管线**在真浏览器里用假摄像头（真 `MediaStream`）跑通 ✓ —— 缺的只有**真机光学/AE 那一层** ✗ |
| G5 误接受 0 | ✅ | — （10,000 次篡改试验 + 2,000 次接缝试验：**0 误接受**） |
| G6 性能 + soak | 🟡 | ① 编码 ✅ ≤5 s；② 解码 **300 dpi ✅ 1373 ms** / **600 dpi ✗ 地板 6192 ms**（判据 ≤2 s/页 ⇒ 待产品负责人决定）；③ soak ✅ 30 min 与 60 min 双过 |
| ~~G7 单色兜底 100%~~ | **RETIRED（第 110 轮）** | ~~✅~~ —— 验的是 3D 板材（PL-D2/D3），产品的负责人第 109 轮**取消了 3D 线** ⇒ **不再作绿/红依据** |
| ~~G8 3MF/STL 独立解析~~ | **RETIRED（第 110 轮）** | ~~🟡~~ —— 同上（3D 码牌产物）；历史判决原文保留不改写 |
| G9 Web 扫描端 | 🟡 | **第 274–280 轮再补五格**：手机四要素**同时**成立（不安全 http + 真 JPEG + 自动档 + 手机量级照片 ⇒ 逐字节 ✓）、**TIFF（含多页）** ✓、**图片载荷 → PNG** ✓、**真断网仍可用（关掉服务器后自检 13/0 GREEN）** ✓、**连拍管线**（假摄像头，真 `MediaStream`）✓。**第 264–272 轮补四格：`file://` 单文件接收页逐字节还原 ✓、真 JPEG（3.6 MB）逐字节还原 ✓、不安全上下文（局域网 http）下「选择文件」逐字节还原 ✓、自动档喂手机量级照片 19 次候选读出 ✓（Node 与浏览器候选数一致）**；产物级全绿 + **第 249–251 轮真浏览器（服务版 http）已跑通发送/接收两条链、`?selftest=1` 13/0、SW 缓存字节与磁盘一致**（第 254–256 轮补：加密三态、命名规范化、**单文件版两个产物**也各跑通一次、摄像头被拒文案）；**仍差**：可见浏览器（无真人点击）、Edge / Safari、手机；**`file://` 那一格第 264 轮已由真浏览器验过**（`file://` 打开发布产物 `pskt-file.html` ⇒ **逐字节还原 1,500 字节**，摘要由 `node:crypto` 另一份实现复核一致；夹具是注入进去的 ⇒ 只证明解码路径与 bundle，不证明「人点选择框」那一下） |
| ~~G10 喷嘴 × 参数矩阵~~ | **RETIRED（第 110 轮）** | ~~🟡~~ —— 同上（3D 打印机喷嘴矩阵） |

**总账（不主张完成）**：**在册** ✅ 4（G0 G1 G3 G5）· 🟡 3（G2 G6 G9）· ⬜ 1（G4）· **已退役 3（G7 G8 G10）**；`verify --gate all` 打印的 `ALL GATES PASS` **每次都会列出本次未评估的门限**，**不得引用成「全部门限通过」**。（`docs/ACCEPTANCE.md` 第 110 轮把 G7/G8/G10 划为 **RETIRED**，依据 `docs/PLAN-V5.md` §3；相应板材档位在 `core/profiles.js` 里已 `retired: true` —— 仍可解码、不再在 CLI/网页提供。）

**✅ 两条「退役动作」（`docs/STATUS.md` 第 110 轮自己写明、一直没做完）第 244 轮已收尾**：

- **① 门限计数**：`verify --gate all` 现在只跑 **G0 G1 G2 G3 G5**（第 244 轮前还在跑退役的 G7/G8），输出 `ALL GATES PASS -- 4/5 evaluated, 1 skipped`，并**单独打一行** `retired: G7 G8 G10 (the 3D plate line was cancelled; docs/ACCEPTANCE.md records the retirement). Any of them still runs on request, e.g. --gate G7`；`not evaluated` 那一行不再把退役门限列成「欠着的活」（实测 `--gate G7` 仍可单独跑、仍然 PASS）。
- **② 验收包**：腿早就退了（第 110 轮之后某轮），但**残留没清** —— 脚本仍在建空的 `plates/` 与 `mtf/` 目录、仍在写 `payload-plate.bin`，用户手册模板里**还列着** `payload-plate.bin` 与 `mtf/`（和它自己第 47 行「3D 线已取消」自相矛盾），步骤号还跳过了第 3 步。第 244 轮清干净并**重跑生成验证**：`KIT_EXIT=0`、README **0 个未替换占位符**、README 的文件清单与包内实际内容**逐条对上**、步骤号 1 / 1b / 2 / 3 / 4 连续。

---

## C. 只有用户能验证的（**我做不了，也不冒充**）

| 项 | 怎么做 | 门限 |
|---|---|---|
| **G4 手机压力** | **第 282 轮补（进程内等价）**：Windows 退路逐字可用 —— 手机 JPEG ⇒ `tools\jpeg-to-png.ps1` ⇒ `receive --photo --profile P-M1-300` ⇒ 逐字节还原 ✓（**不需要站点、浏览器或口令** ✓）。**⚠ 第 264–272 轮补：不用部署站也能走完** —— 局域网 http 下摄像头禁用但「**选择文件**」可用（系统相机拍照 → 相册选进来，已实测逐字节还原 ✓）；**知道打印档位就显式选**（自动档第 271 轮已修好，但显式档快 30 倍以上 ✓）。⚠ **摄像头只在安全上下文可用**（`https://` 或 `localhost`）：`http://192.168.x.x:8123` **不是**，页面会**自己禁用**摄像头按钮（这**不是权限设置问题**，iOS/Android 都没有开关）。三条路：① 手机打开**已部署的 https 接收页** `https://wangsan71.github.io/High-density-codes/pskt-file.html` → 最下面「手机连拍（自动挑帧）」；② **没网**：系统相机拍照 → 接收页「选择文件」，或把照片传回电脑走 `node cli/pskit.mjs receive 目录 --photo --profile auto --out 回来的文件` | G4 |
| **G9 浏览器** | Chrome / Edge / Safari 各开一次发送页与接收页（`file://` 单文件版 + http 版各一次；**自检只能用服务版**：单文件版按设计不带 loader）；本机若把浏览器插件指向 Edge 就能补第二个引擎 | G9（**服务版 http 与 `file://` 单文件版两格第 264 轮都已由真浏览器验过**，见 A 节；仍差真人点击与 Edge / Safari） |
| **D8 打印缩放** | 打一页量实际尺寸与 PDF 标称是否一致；被缩放就改 100% 再量 | D8 |
| ~~**G10 喷嘴矩阵**~~ | **RETIRED（第 110 轮：产品负责人取消 3D 板材线）⇒ 这条不用做**；工具仍在，想跑随时可以：`node tools/mtf-matrix.mjs --dir 目录 --provenance real-print` | — |
| ~~**G8 切片软件**~~ | ~~用切片软件打开 `pskit send --format 3mf,stl` 写出的 `.3mf`~~ ⇒ **RETIRED（第 110 轮，3D 线已取消）⇒ 不用做** | — |
| **三条密度梯纸** | 打 `acceptance-kit` 里的 `density-a4-300` / `density-a5-600` / `density-a6-1200` 并扫描回来 | 密度数字目前**只有仿真** |

**⚠ 第 283 轮更新：已部署的 https 站仍然落后 —— 本轮实测 `git status -sb` 是 **`ahead 185`**（第 263 轮实测 165）****（第 263 轮实测 165 个提交，此后本地又推进了十余轮 ⇒ 现在约 **175**）（`origin/master` 停在 `bea7007`；实测 `app.css` −640、`app.js` −2,774、`pskt-bundle.js` −45,334 字符，且站上**没有**第 254 轮的退役档过滤、也**没有**第 261 轮的 `[hidden]` 修复）⇒ **手机上验到的是旧代码** ✗。推送失败的原因**不是沙箱**而是**缺 GitHub 凭据**（`credential.helper=manager` 在非交互环境里跑不了提示脚本；本机无 `~/.git-credentials`）⇒ 两条出路：① 你推一次（普通终端 `git push origin master`）；② 走**不需要站点**的路：手机系统相机拍照 → 电脑 `node cli/pskit.mjs receive 目录 --photo --profile auto`。，此后本地又推进了十余轮 ⇒ 现在约 **175**）（`origin/master` 停在 `bea7007`；实测 `app.css` −640、`app.js` −2,774、`pskt-bundle.js` −45,334 字符，且站上**没有**第 254 轮的退役档过滤、也**没有**第 261 轮的 `[hidden]` 修复）⇒ **手机上验到的是旧代码** ✗。推送失败的原因**不是沙箱**而是**缺 GitHub 凭据**（`credential.helper=manager` 在非交互环境里跑不了提示脚本；本机无 `~/.git-credentials`）⇒ 两条出路：① 你推一次（普通终端 `git push origin master`）；② 走**不需要站点**的路：手机系统相机拍照 → 电脑 `node cli/pskit.mjs receive 目录 --photo --profile auto`。

准备这些的命令：`& .\tools\acceptance-kit.ps1`（**第 244 轮起生成**：纸面页 `paper\pack.pdf`、三档模块页 `module-6/5/4`、三张密度阶梯页 `density-a4-300`/`a5-600`/`a6-1200`、以及 `README.txt`；**板材码牌与 MTF 板已随 3D 线退役移除**）。

---

## D. 已经否掉 / 别重做（**最省时间的一节**）

1. **600 dpi 不是必要路径**（用户第 63 轮明示）⇒ 不要再为它优化、也不要把它记绿；连「候选排序」也已量到地板（完美排序仍需 6192 ms vs 判据 2000 ms，其中 `decodePNG` 就占 1547 ms）。
2. **不要跑 3 h soak**：判据只要求 30–60 min，已双过。
3. **不要用括号配对脚本删死代码**：第 154/155 轮两次因此把仍在用的共享常量一起删掉、19 个测试文件整套导入失败。逐个 read 精确删。
4. **不要用未拟合的粗模型给瓦片身份打分**（第 216/217 轮）；**编号必须由块头决定**（晶格周期性使任何几何打分对整数块平移全盲，第 209 轮）。
5. **不要再追「瓦片格式的密度优势」——它不存在**：A4/30 mm 瓦片的模块 0.909 mm ≈ `P-M1-300` 的 0.85 mm；它买到的是「不需要角标/回显条/manifest 也能自己定位」。
6. **合成夹具必须按照片自己的尺度渲染**：先按 300 dpi 渲染再压缩会烤出**非刚性**模块栅格，读不出是夹具的锅（第 240 轮第一版夹具踩过）。
7. **不要改判据来让台账好看**；判据收窄是产品负责人的决定（AGENTS 硬约束 6）。
8. **不要为通过而删候选／夹读断电平**；「误接受」是唯一不可原谅的失败。
9. **死代码复查是三条腿，且现在是命令**（第 262 轮定型；第 267 轮删掉 `glyphMask`；第 272/277/282 轮复核三角度全清）：① 导出侧 `node tools/find-dead-exports.mjs` ⇒ core **329 个导出、`never` 0**（那三个「文档即接口」的名字现在被单测点名，落在 tests-only 桶 ✓），`expectedRho` / `decodePages` / `DEFAULT_PROFILE`，全是「文档即接口」）⇒ **不要再当待办**；② 模块级非导出孤儿、③ **孤儿工具文件** —— ②③ 都在 `node tools/find-dead-locals.mjs`（`--selftest` 是阳性对照）；导出侧另加 `--scope all` 才看得到 `tools/` `cli/` `web/` 的导出（第 267 轮补，此前从未查过）⇒ **不要再用 `.tmp/` 里的探针做复查**（第 257 轮那个探针既违反 AGENTS §6.8、又会随会话消失，第 262 轮才补成命令）。**注意词边界**（两个方向都踩过）：用 `\b` 会数不到 `$(...)` 这类调用（`$` 不是单词字符）⇒ `web/app.js` 的 `$` 被误报过一次；而**不用边界**则会把长名字算成短名字的提及 ⇒ `glyphMask` 被 `glyphMaskForLevel` 藏了 25+ 轮，直到第 267 轮把边界收紧才抓出来（已删）。
10. **不要重复发现已闭合的缺陷**：`docs/DEFECTS.md` **只增不删**。判读要**两处一起看** —— **划线行** = 已闭（CLOSED）或记录用（NOTABUG）；但**有些条目状态格写着 FIXED/CLOSED 却没划线**（`D41` 第 39 轮、`D43` 第 90 轮 https 半边、`D51` 第 63 轮、`D52`–`D66` 各轮、`D84` NOTABUG、`D85` 第 131 轮）⇒ **只按划线读会漏掉这批已修项**。仍未闭合的：`D8 D9 D10 D18 D22 D23 D27 D28 D29 D42 D49 D70 **D88**`。**第 260 轮 D87 已闭**（根因是那条断言在 1 字节上按构造 1/256 概率相等；实测 11/5000 ⇒ 已拆成逐样本 + 聚合两条）⇒ **D87 不要再查**。仍 OPEN 的 **D88** 是「一次并发跑 `verify --gate all` 打印过 `GATE FAILURE` 且**未能复现**」（我当时只留了筛过的几行 ⇒ 现场没留住）⇒ **门限要单独跑、输出整段留住**，见第 27 条。
12. **G7 / G8 / G10 已 RETIRED（第 110 轮）**：产品负责人第 109 轮取消 3D 板材线（依据 `docs/PLAN-V5.md` §3）⇒ 这 3 条**不再作绿/红依据**、板材档位 `retired: true`（仍可解码、不再在 CLI/网页提供）。**不要**再把它们当待办、也不要因为 `verify --gate all` 还打印它们而困惑（那是尚未做完的退役动作，见 B 节末）。
13. **归因撤回别重蹈**：① 「600 dpi 失败是因为夹具没纸边 ⇒ 角标出画」（第 49–52 轮）**已被第 58 轮推翻**（交付形状下 `markers/no-hollow-corner` 仍 41 页）；② 「页内 ECC 余量不足」（第 19/58 轮）**已被第 61 轮推翻**（能读出的 600 dpi 页只花 0.0–0.2% 预算）⇒ 真因是 D51（逐格电平判读整体塌陷）；③ 「600 dpi 差的是能力还是样本量」**已无意义**（两者都不是）。
14. **`tools/soak.mjs` 的假绿已修**：以前允许 `--minutes 1` 用 72 秒打印 `PASS G6`；现在是常量下限 `MINUTES_REQUIRED = 30`（`--minutes` 只能加长、不能缩短判据）⇒ **所有 G6 数字都来自 ≥30 min 的跑**。
15. **`grep 不到 ≠ 不提供`**（第 60 轮撤回的错判）：网页发送端的档位下拉框是**动态枚举** `PROFILES` 的，grep 静态列表查不到任何档 ⇒ 判「某档未提供」前必须看渲染逻辑。
16. **同一个门限的两种数字要并列如实写**：G1 的「零误读」在 `docs/ACCEPTANCE.md` 记 **2,692,800 格**、在 `STATUS`/`HANDOVER` 记 **725,913 格**（7 档 × 3 次）—— 两处口径不同，**不要抹平成一个数**。
17. **摄像头只在安全上下文里可用**（`https://` 或 `localhost`）：局域网 `http://192.168.x.x` 上**没有开关可开** —— 页面会自己禁用摄像头按钮并说明（第 248 轮实测；iOS 与 Android 都没有等价设置）。
18. **服务版页面的内联 `<script>` 会被它自己的 CSP 拦掉**（`script-src 'self'` 不含 `'unsafe-inline'`）⇒ **加脚本要用 `src` 指到文件**（第 249 轮 D89：`?selftest=1` 的入口就是这么静默失效的）。
19. **改完 JS 立刻 `node --check`**：第 249 轮我把分号打成逗号，`build-web`/单测/`check-dist` 全绿（那个文件不在任何单文件产物里）⇒ 现在由 `check-dist` 第 15 条「每个产物 JS 必须能解析」盯着。
20. **「恢复一个函数」不等于「修复一个文件」**：第 249 轮我从历史取回原文去修 D90，实际是把第 154 轮**故意删掉的死代码复活**了；正确做法是**完成那次没做完的删除**（第 252 轮扫描器抓出来的）。
21. **孤儿 import 与孤儿导出是同一根绳的两头**：删 import 之后要回头查那个导出还有没有人用（第 242 轮只记了前一半，第 252 轮补上 `ANNULUS_AREA`）。
22. **`& .\tools\x.ps1 > log 2>&1` 会得到 0 字节日志**（脚本以 `exit` 结尾 ⇒ 宿主先退）；`node … > log` 正常。要逐条输出就别用 PS 重定向（第 246 轮）。
23. **`phoneSafe` 标记已被移除**（第 247 轮）：它原本挂在已退役的 `PL-G` 上、在网页端什么都不显示；**没有真机测过之前不许给任何档加回这个标记**（`tests/unit/profile-picker-warning.test.mjs` 会红）。
24. **「按构造就会偶发」的断言要写成聚合形式**（第 260 轮 D87）：对 1 字节随机数据要求「密文 ≠ 明文」有 **1/256** 的概率相等 ⇒ 逐样本断言要么加长度前提（len ≥ 8）、要么整轮跑完再聚合断言一次。
25. **`check-dist` 的两个数字不是一回事**：顶层 `13 pass / 0 fail` 与 `G9 CHECK: all 15 assertions pass` 分别数的是顶层 record 与 G9 块内的断言 ⇒ **改文档里的数字之前先量**（第 262 轮我先改错、量完当场改回）。
26. **`hidden` 属性会被 class 里的 `display` 压掉**（第 261 轮 D91）：只写 `hidden` 而 CSS 给了 `display:flex` 之类 ⇒ 元素照样显示 ⇒ 现在 `web/app.css` 用 `[hidden] { display: none !important; }` 兜底；判「藏没藏」要**量渲染后的可见性**，不是读属性。
27. **门限别并发跑**：第 258 轮有一次与重活并发的 `verify --gate all` 打印了 `GATE FAILURE` 且没能复现（**D88 OPEN**）⇒ 判决类的跑单独安排、并把**整段输出**留住（别只留自己筛过的那几行）。
32. **文档里写的命令，要原样跑一遍才算数**（D95 与 D97 是**同一个形状**的第①、②例）：① `send --format tiff` 的提示叫你打一个不存在的 `page-000.png` ✗；② 验收包 README 叫你用 `scans-module-6/5/4`，而一键核对工具只认 `module-6/5/4`，**照文档做的人只会看到 `0/3`** ✗。两次都是**照着自己写的命令跑**才发现的 ✓ ⇒ **新增或改动工具时，把 README / 手册里那条命令原样执行一遍** ✓（改动工具的输出/参数时尤其要 ✓）。
31. **解码是同步的 ⇒ 页面在那几十秒里是冻结的**（第 272 轮实测）：喂一张 6200×4650 的手机量级照片，接收页主线程被占满 **31.8 s**，期间任何脚本都跑不了（我的 `browser_evaluate` 全部 60 s 超时 ✗）⇒ 真机上同样「没进度、像卡死」✓ ⇒ **不要把它当 bug 去查**（分片 UI 是已知待决项 ✓）。
30. **测浏览器里的产物之前先清 Service Worker 缓存，并核实「被服务的那份 bundle 里有没有你这次的改动」**（第 272 轮差点因此写下错结论）：接收页是 PWA ✓，SW 会把上一版 `pskt-bundle.js` 预缓存起来 ⇒ **重新构建后页面仍然跑旧代码** ✗。第一次浏览器复验 D93 就是这样失败的（「试了 64 组候选」= 旧排序 ✗），清缓存并确认 bundle 里出现 `paperRank`（✓）后重测即通过 ✓。手势：`navigator.serviceWorker.getRegistrations()` 全 unregister + `caches.keys()` 全 delete + 带一个新查询串重新加载 ✓。
29. **别再指望用「px/格」单独预测手机成败**（第 268 轮实测）：把采样提到实测 **11.2 px/格**（新预设 `phone-full`，容差与 `phone40` 逐字相同）后三张照片**仍然全读不出**，失败在**对比度/角标识别**那一级；把眩光与曝光拉中性也救不回来 ⇒ px/格 是**必要非充分**条件。另外：**提高传感器分辨率本身不会提高 px/格**（模拟器里 `fill` 是**画面高度**占比）—— 第一版 `phone-full` 就是这么写错的（3200×2400 实测只有 ≈5.0 px/格，纸宽仅 1253 px）。
28. **不要在本沙箱里试着起第二个浏览器引擎**（第 266 轮试了三次）：Edge 后台起不来（立刻退出、无输出、无进程）、`--dump-dom` stdout 全空、加 `--no-sandbox` 后崩在 **`crashpad OpenProcess 存取被拒 (0x5)`** ⇒ 与 AGENTS §4 记的「受限沙箱里 Chrome/Edge 起不来」同源。插件里那个 `HeadlessChrome/152` 是**宿主**拉起来的，不代表你自己也能拉。⇒ 「第二个引擎」与 Safari 这两格只能用户做（Safari 在 Windows 上不存在）。
11. **`git push` 仍未做，而且原因已量清（第 263 轮）**：**不是沙箱**（工具报 `sandbox denied=false`），是 `credential.helper=manager` 在非交互环境里跑不了提示脚本 ⇒ `could not read Username for 'https://github.com'`，本机也没有 `~/.git-credentials` ⇒ **只有用户的 GitHub 凭据能推**。当年那个「MSYS signal pipe / Win32 error 5」是**另一种**症状（真要动沙箱时才是它）⇒ **不要**为此投机升级授权。

---

## E. 这一页怎么维护

- **每 10 轮**更新一次（首次第 242 轮建立、第 253 轮第一次按节奏更新、第 263 轮第二次、第 273 轮第三次、**第 283 轮第四次**；下一次第 **293** 轮）。更新只做三件事：把**新做成**的能力补进 A 节、把**新否掉**的做法补进 D 节、把**新出现**的「只有用户能验证」补进 C 节，并把 A 节末的**基线数字**按本轮实测刷新。
- 与 `docs/STATUS.md` / `docs/ACCEPTANCE.md` 冲突时，**以那两份为准**；本页不改写历史轮次块。
- **死代码复查每 5 轮**一次（第 242 轮 19 → 3、第 247/257 轮维持、第 252 轮 3 → 5 → 3、第 262 轮补上模块级与孤儿工具两条腿、**第 267 轮删掉被宽松正则藏住的 `glyphMask`、第 272 轮复核三角度全清**；第 277/282 轮维持、三角度全清**；下一次第 **287** 轮），口径见 D 节第 9 / 20 / 21 条；命令是 `node tools/find-dead-exports.mjs` 与 `node tools/find-dead-locals.mjs`。
- 动工顺序建议：`AGENTS.md` §0 → **本页** → `docs/HANDOVER.md` → `docs/STATUS.md` 最上面几块。
