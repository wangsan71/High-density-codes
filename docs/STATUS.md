# STATUS · PSKT 里程碑台账（跨轮续跑入口）

> **续跑规则**：新轮次先读本文件 → 找到第一个未完成里程碑 → 跑它的前置门限确认未回归 → 开工 → 跑门限 → 更新本表 + git tag。
> 门限跑法：`node cli/pskit.mjs verify --gate <G0|G1|G7|all>`；单元：
> `node --test --test-isolation=none "tests/unit/**/*.test.mjs"`
> （沙箱禁止带管道 stdio 的子进程，缺 `--test-isolation=none` 会 `spawn EPERM`；目录参数会 `ERR_UNSUPPORTED_DIR_IMPORT`。）
> **交接概览**：`docs/HANDOVER.md`（第 74 轮加）—— 仓库地图、从零复现的命令与期望数字、**只有用户能做的验收**、
> **待产品负责人决定的判据**、已知债、环境陷阱与台账纪律的浓缩版。**持久目标已按用户指示暂停**（第 74 轮；不是完成）。

## 里程碑

| ID | 内容 | 门限 | 状态 | 实测（最后一次） | tag |
|---|---|---|---|---|---|
| M0 | 骨架 / git / ESM 约定 / CLI 壳 | — | **完成** | 仓库+package.json+docs 就位 | — |
| M1 | 编码核心 gf256/rs/hash/chacha20/crc/pack/deflate/frame/profiles/nozzles/protocol | G0 | **完成** | 105/105 单元绿 | `M1` |
| M2 | 版面渲染 + PNG/TIFF（读写）+ 回显带 + CLI send/receive | G1 | **完成** | 136/136；G1 7档×6次 0 误读；磁盘文件→PNG→文件 sha256 相同 | `M2` |
| M3 | 解码全链路（标记检测→单应→透视矫正→读格） | G1,G2 | **完成（合成图像层）**；真实相机照片待 M4 信道 | 156/156；`warp.test.mjs` 6/6：倾斜+模糊+噪声照片角标误差 <2.5px、90° 旋转页靠空心角纠正、空白/乱码图干净拒绝、PL-G@0.8 79 页照片往返复原 | — |
| M4 | Python 仿真信道 + verify 套件 | G2,G3,G4,G5 | **进行中（只剩 G4，且只有用户能跑）**：`sim/channel.py` 与 `ref/decode.py` **早已落地并在用**（G2 的 600 份语料就是它生成的、G0 靠 `ref/decode.py` 判绿）⇒ 原记的"由子代理在写"是**过期记账，第 64 轮更正** ✗ | G2 **300 dpi ✅ `PASS 200/200`**（第 63 轮修法后重跑 1245.4 s、exit 0）· 600 dpi **无判决**（用户第 63 轮判定非必要 ⇒ 重跑中止在 52/200）· G3 ✅ · G5 ✅（第 63 轮起含**接缝试验 2000 次、0 误接受**）· **G4 ⬜ 零证据**（手机 500×8 需真机 ⇒ 见 `USE.md` §5） | — |
| M5 | STL + 3MF 双色产物 | G8 | **完成（进程内等价）**：原记"未开始"是**过期记账，第 64 轮更正** ✗ 产物每轮由 usability 冒烟实际写出并用 G8 校验 | `PL-D2@0.4`：3 objects / 377964 triangles / 4.1 MB · `verify --gate G8` ✅ · `tests/unit/threeMF-xsd-parity.test.mjs` 把规则表与 vendored 权威 XSD **自动对拍**（D41 第 39 轮已闭）· 差的只是**切片软件真人打开一次**（本机无 XSD 引擎 ⇒ "过 schema"仍未证） | — |
| M6 | 双色优先·单色兜底 | G7 | **完成（图像层）** | `--gate G7`：PL-D2@0.2/0.4 单色渲染判死色道 → 完整复原；PL-D3(off) 干净拒绝 | — |
| M7 | 喷嘴矩阵 + PL-G + calibrate | G10 | **部分**：原记"未开始"是**过期记账，第 64 轮更正** ✗ `pskit calibrate`（逐页 readout + 页内纠错预算 + 墨量健康度，只量不改）**第 61 轮已落地**、第 63 轮起还报救回；ρ 自标定在理想层跑通 | **G10 喷嘴矩阵需真打印机 ⇒ 未跑**（~~只测到 `PL-G` 在 0.6/0.8 被 `glyphGeometry` 正确拒绝这一个边界点~~ —— **第 78 轮更正 ✗**：`planPage('PL-G', {nozzle:0.2/0.4/0.6/0.8})` 实测**四档全部可规划**（pitch 3.12/3.6/5.6/7.6mm，50×50/42×42/23×23/14×14 格），G7 也打印 `PL-G@0.8 … recovered`；被 `glyphGeometry` 拒绝的是 **`PL-D3S`**（形状字母表 4 档 ⇒ 0.6/0.8 放不下，见容量表那行）。原句把两个档位写混了，**判决未变**：完整矩阵仍未跑）· **MTF 板 → 推荐喷嘴/间距那一半第 75 轮已落地**（`core/calibrate/mtfplate.js` + `readmtf.js` + CLI 两个模式 + `tools/mtf-probe.ps1`；真信道实测四喷嘴三条点名正确、0.2 那一档有实测原因 ⇒ 见第 75 轮块）· **第 76 轮：可打印半边也落地**（`core/mesh/rectilinear.js` + `core/mesh/mtfplate.js` + CLI `--format 3mf,stl`，每 object 水密 + 投影对拍 `maxPct 0`，3MF 过 G8 子集 ⇒ D67 CLOSED）⇒ 只差**真打印机印一次**（G10）与**数据板网格的角标**（D68，OPEN） | — |
| M8 | Web 扫描端 PWA + Pages | G9 | **部分（产物级全绿，浏览器内未验证）**：原记"未开始"是**过期记账，第 64 轮更正** ✗ `web/dist` PWA（manifest + `sw.js` + 三张图标）与两个单文件页每轮构建 | `build-web` exit 0（55 文件、29 模块、precache 53）· `check-dist` **13 pass / 0 fail** + `G9 CHECK: all 12 assertions pass`（第 64 轮起夹具自带 ⇒ **全新检出也绿**，D53）· 差**真浏览器点一次（G9）**与 **https 托管（D43，只有用户能解）** | — |
| M9 | soak/性能/文档 | G6 + 复跑全部 | **进行中**（原记"未开始"是**过期记账，第 72 轮更正** ✗：G6 三部分第 65–70 轮已按判据实测并落判决，`docs/USE.md`/`ACCEPTANCE.md`/`DEFECTS.md` 每轮维护） | G6 ① 编码 **199.4 ms ≤5 s ✅** · ③ soak **30 min 与 60 min 双过 ✅**（60.02 min、281 cycles、2782 页解码、误接受 0、RSS `+3.16% ≤10%`；但"无泄漏"**不主张**，Q1..Q4 未收敛，见 ACCEPTANCE ③bis）· ② 解码 **300 dpi ✅ 1373 ms / 600 dpi ✗ 地板 6192 ms**（第 72 轮实测：完美排序也超预算 3.1 倍 ⇒ 只能由产品负责人改判据）⇒ **G6 整条 🟡** · "复跑全部"这条腿每轮在跑：build-web / check-dist / smoke-sender / smoke-capture / 单测 / usability 全绿 | — |
| M10 | 可选 `REL` 光影档 / `P-C4` 纸面4色 | — | 未开始 | — | — |

## 门限状态

**现况表（第 22 轮·末轮 ✓ 判决一律与 `docs/ACCEPTANCE.md` 对齐 ✓ 权威源在那边 ✓ 本表只作导航 ✓）**

| Gate | 判决 | 差多少（一句） | 复现 |
|---|---|---|---|
| G0 规格可独立实现 | ✅ | — | `python ref/decode.py` → `PASS`（309 检查 ✓ 4 项已接受 geometry GAP） |
| G1 渲染—读回零误读 | ✅ | — | `node cli/pskit.mjs verify --gate G1`（725 913 格 0 误读 ✓） |
| G2 纸面 300/600 dpi 100%（200 seed） | 🟡 | **第 57 轮现状（权威源 = `docs/ACCEPTANCE.md` G2 段，本行只作导航）**：**300 dpi 侧已在"交付形状"下达标** —— 语料源页是**整张 A4 `2480×3508`**（码区按标称物理尺寸居中 + 留边里的四角裁切标记与四边套准十字），`verify --gate G2 --root .tmp --match 'sc-scan300-*'` ⇒ **`PASS G2 corpus: 200/200 byte-exact in 1113.7s`** · `ALL GATES PASS` · **exit 0**，门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200` ⇒ **样本量对齐 PLAN、判据未放宽** ✓ 页级明细（边际如实写）：**13/200 份各有 1 页读不出、全部由页间 RS 重建**（`readout/echo-header-crc` × 10、`markers/no-hollow-corner` × 2、`readout/echo-bad-magic` × 1）⇒ **页级直读 587/600 = 97.83%**、**载荷级 200/200 = 100% 逐字节**、每份 `digest verified`、**含 FAIL 的行数 = 0** ✓ 该次运行**只评估了 G2**（门限自己列出未评估的 G0 G1 G3 G4 G5 G6 G7 G8 G9 G10）⇒ **`ALL GATES PASS` 不许被引用成"全部门限通过"** ✗ **第 58 轮：600 dpi 侧判决已出 ⇒ ✗ 不达标** —— `verify --gate G2 --root .tmp --match 'sc-scan600-*'` ⇒ **`FAIL G2 corpus: 162/200 byte-exact in 4359.7s`** · `GATE FAILURE` · **exit 1** · `failure classes: short 0/1 x37, no-page-header x1`；前提与 300 dpi 侧**完全对称**（整张 A4 `4961×7016`、同一载荷、`nocrop`、200/200 生成 exit 0 ⇒ 差别只在 dpi/档）⇒ **页级失败 342/600 ⇒ 页级直读仅 43%**（300 dpi 侧 97.83%）、162 份 OK 里 **145 份也靠 RS 兜页**；原因 `assemble/intra-fail` × 210、`readout/echo-bad-magic` × 78、`markers/no-hollow-corner` × 41、`readout/echo-header-crc` × 12、`readout/echo-version-3` × 1 ⇒ **历史"夹具没有纸边 ⇒ 角标出画"的归因被推翻**（交付形状下照样 41 页）✗ ~~主因指向**页内 ECC 余量不足**~~（**第 61 轮实测推翻 ✗ ⇒ 根因已定 = D51：逐格电平判读在部分 600 dpi 页上整体塌陷到单一电平** —— `sc-scan600-11` p1 内部 283155 格全被读成电平 1（原始渲染 `0=256118 1=27037`）、混淆只有 `0->1`、空间平坦、48 个 ±3 格平移无效；而**能读出的 600 dpi 页只花 0.0–0.2% 预算**、300 dpi 侧 9 页全 **0.0%** ⇒ **余量不是约束，加冗余与加样本量都救不了这类页** ⇒ 量具 `pskit calibrate` 第 61 轮落地（只量不改、不是门限）⇒ 详见 D51 与 ACCEPTANCE 第 61 轮块；**第 62 轮量到机制**：塌陷发生在**匹配滤波**（信道把 ρ 抬 4–20 倍 ⇒ 固定切点在任何信道页错 60.1–90.6%，而 ρ 两簇仍分开、**无监督切点可救到 ≤0.072%** ≪ RS 上限 6.30%）⇒ 修法形态 = **RS 仲裁的重读**（不动今天能解的页），另记 **D52**（两个"诊断量"自证/失明）⇒ **第 63 轮修法已落地并重跑**：G2 **300 dpi 侧 `PASS 200/200`（1245.4 s，exit 0）**✓ 无回归；**600 dpi 侧最终判决没取** —— 重跑到 52/200（51 OK / 1 FAIL）时**由用户中止**（用户明示 600 dpi 非必要 ⇒ **范围判定，非放宽判据**），中间普查里旧跑 210 次的 `assemble/intra-fail` 出现 **0 次**、只剩读出前的 echo/角标类；G5 重验 **0 误接受**且新增**接缝试验 2000 次 0 误接受**✓ 详见第 63 轮块与 D49/D51/D52 ✓ 误接受仍为 **0**（38 份全是拒绝/缺页、没有一份交出错误字节 ⇒ 硬约束未破 ✓）⇒ **G2 整体：300 dpi ✓ 100% / 600 dpi ✗ 81% ⇒ 未通过、维持 🟡、不上调**（判据要两侧都 200 seed × 100%；**没有**只报通过的那一档、也**没有**改信道参数让它变绿 ✓ 用户侧影响已改文档 ⇒ **D49 OPEN**） 历史那 2 例 600 dpi 失败的"夹具没有纸边 ⇒ 角标紧贴图像边缘"归因**本轮既未证实也未推翻** ⇒ 等新判决；300 dpi 侧那 2 页 `markers/no-hollow-corner` **也不许读成"问题消失了"**（是被 RS 兜住的页级失败，确实发生了 ✓）—— 以下为第 51–52 轮原文，保留追溯（其中"前提不真实"那条保留意见**已被上面解除** ✓ 别当现状读）：**第 51 轮：300 dpi 侧 `200/200 = 100% 逐字节`**（`PASS G2 corpus: 200/200 byte-exact in 1085.4s` · `ALL GATES PASS` · exit 0 ⇒ **样本量已对齐 PLAN、判据未放宽** ✓ 门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200`；判决里若干份带逐页提示如 `markers/no-hollow-corner [fiducial-out-of-frame] 2 of 4 corners` 而该份仍 `OK 204800 bytes, digest verified` ⇒ 冗余在起作用、不是失败 ✓）**；600 dpi 侧仍只有 seed 1–16** ⇒ **整体仍未通过**。而且**这 200/200 的前提不真实**：语料源页是"只有码区、没有纸张"的 PNG（`2260×3290@300dpi`，A4 应为 `2480×3508`；600 dpi 侧 `4530×6590` 且 manifest `sheetMm=null`）⇒ **`--sheet` 只对 PDF 生效 = D45 ✗** ⇒ 600 dpi 那 2 例"角标出画"很可能是**夹具没有纸边**造成的（角标紧贴图像边缘 ⇒ 任何姿态都出画）⇒ **第 49 轮"A4 在平板上带 0.x° 旋转是真实物理"那句归因已撤回** ✗（"不是解码器缺陷"不变 ✓ 3 点 6 方程 < 单应 8 自由度 ⇒ 拒绝正确 ✓）⇒ **顺序（第 52 轮：(a)(b) 已做完、(c) 两个 200-seed 后台批正在跑 ⇒ 本节数字暂不变）**：(a) 修栅格路径认 `--sheet` → (b) 用修好的渲染器重出两侧源页（同一载荷 `.tmp/payload.bin`、sha256 `91ab3f7310da452b` ⇒ 与历史语料可直接对比）→ (c) 两侧各跑 seed 1..200 ⇒ **只有 (c) 的结果才配写"达标/不达标"**（详见 `ACCEPTANCE.md` G2 段末）✓ **因为 (a) 未做，本轮刻意没有起那个约 85 分钟/2.5 小时的批**——用同一前提再测一遍是白花机器时间 ✗ 历史口径（`30/32 = 93.8%`、"A=600 dpi 页内 ECC 余量薄 · B=整页读出崩塌根因未定"）**已被后来的像素级定界取代** ⇒ 保留只为追溯，别当现状读 ✗ | `node cli/pskit.mjs verify --gate G2 --root .tmp --match 'nc-scan*'`（**第 37 轮起是门限** ✓ 直接 import `tools/g2-corpus.mjs` ⇒ 门限与工具不会漂移 ✓ 干净语料会被 `corpusProvenance` 拒绝 ⇒ **无法伪造 G2 绿** ✓ 工具形式 `node tools/g2-corpus.mjs --root .tmp --match 'nc-scan*'` 仍可用 ✓） |
| G3 缺页/乱序/重复 | ✅ | 图像层乱序仅在协议层覆盖（等价于喂入序 ✓） | `verify --gate G3` + `tools/g2-corpus.mjs`（单页存活 6/6 ✓） |
| G4 手机压力档 ≥99% | ⬜ | **500×8 一次未跑 ⇒ 零证据** ✓（分类侧 `advice.js` 已就绪 ✓） | 未做 |
| G5 误接受 0 | ✅ | — | `verify --gate G5 --trials 10000`（9200 纠正 / 800 拒 / **0 误接受** ✓ 变异两项 ✓） |
| G6 性能 + soak | 🟡 | **第 65 轮首次按判据三部分实测**：① 编码 1 MB **197 ms ≤ 5 s ✅**（→168 页）② 解码 ≤2 s/页 **300 dpi ✅ / 600 dpi ✗** —— 修 D54 前真实信道页 **8713 ms/页**（超预算 4.4 倍、原因是候选按**字母序**试），修后 **1467 ms/页、尝试 4→1**（pristine 952 ms）⇒ **手册指的那一档进预算** ✓ 而 600 dpi 页 **31786 ms/页**（同 dpi 的 P-C4/P-M1 码区同尺寸 ⇒ 尺寸排序分不开；**第 72 轮把这条量到地板**：`tools/g6-perf-probe.mjs` 新增 `--profile/--dpi/--palette/--only-hints` 后，同一张 600 dpi 页（37.6 MP）无提示 **24743 ms**、几何已知 **13520 ms**、**恰好 1 个候选（`attempts=1`、第一次就赢）也要 6192 ms**，其中 `decodePNG` 就 **1547 ms = 预算的 77%** ⇒ **完美排序只到判据的 3.1 倍**，而 300 dpi 同页 **1373 ms 第一次尝试就赢**（加提示 1367 ms ⇒ 提示买不到东西，这是阳性对照）⇒ ACCEPTANCE 里"再降需要第二个廉价信号"已就地划线：**排序最多买 4 倍、判据要 12.4 倍**，本轮因此**决定不造那个信号**，② 只剩产品负责人的三条路（限定 300 dpi / 放宽 600 dpi 预算 / 投资根本上更便宜的 600 dpi 读法），详见 ACCEPTANCE ②bis）✗ **不因用户判 600 dpi 非必要就把这条记绿**：判据的范围是产品负责人的决定，不是我在门限表里能默默收窄的 ⇒ 整条仍 🟡 ③ soak **✅ 判决已落（第 66 轮）**：`node tools/soak.mjs --minutes 30`（= `verify --gate G6`）实测 **30.7 min、25 cycles、257 页、200 次腐蚀试验** ⇒ **RSS 634.5 → 650.3 MB = +2.50%（判据 ≤10%）**、峰值 738 MB、**误接受 0 / 摘要不符 0 / 抛错 0**（中途 581–738 MB 是 GC 锯齿、不是泄漏）✓ 该次跑在修 D54/D55 **之前**的代码上 ⇒ 它自报的 `worst decode 8850 ms > 2000 ❌`、`ok:false`、`SOAK30_EXIT=1` **原样保留在 `.tmp/g6-30.json`**（不改写），解码一项以修后实测（②）为准，RSS/误接受那半的循环机器一行未改 ⇒ 仍成立；**第 67 轮：修后代码上的完整 soak 已跑完** ⇒ **编码 194 ms ✅**、**解码 worst 1681 ms / mean 1587 ms ≤ 2 s ✅**、**完整性 ✅**（141 cycles / 1401 页 / 1128 次腐蚀试验 ⇒ 误接受 0 / 不符 0 / 抛错 0 / 拒绝 0），但 **RSS 报 `+13.98% > 10%` ❌** ⇒ 查清是**仪器窗口短于 GC 锯齿**、不是泄漏（**D59**：每分钟中位数 3–8 min 510.6 / 8–20 min 512.8 / 20–31 min 515.45 MB ⇒ 28 分钟 **+0.95%**、min/max 449.3 / 641.4；且头 3 min 反而更高 ⇒ **不是热身不足**，本轮先猜错了）⇒ **判据不动**、窗口改四分位并**同时打印旧窗口统计量**（`narrowGrowthPct`，printed-not-judged）+ Q1..Q4 中位数与 peak/floor，**第 68 轮收了：`PASS G6`**（`.tmp/g6-30-r67.json`，30.2 min / 141 cycles / 1401 页 / 1128 次腐蚀试验）⇒ 编码 **203 ms ✅**、解码 **worst 1663 ms ≤ 2 s ✅**、**RSS `+1.78%` ≤ 10% ✅**（507.3 → 516.3 MB、peak 640.7 / floor 436.1）、完整性 **0 误接受 / 0 不符 / 0 抛错 / 0 拒绝 ✅**、`ok:true` ⇒ **三项第一次同源于一次跑**；同一次跑里旧窗口算出 **+2.96%**（上轮同代码同旧尺子 **+13.98%** ⇒ 差 11 个百分点 = D59 相位抽样的旁证）；**如实**：**Q1..Q4 单调爬升**（507.3 / 511.7 / 515.8 / 516.5 MB，增量收窄 +4.4 / +4.1 / +0.7）⇒ 形状像收敛到工作集、**但形状不是证明** ⇒ 已起 **60 min 跑**分开"收敛"与"慢泄漏"，结果再写；另 **D60**：`--minutes 1` 曾能让这条门限打印 **`PASS G6`**（72 秒满足一条 30–60 min 的判据、文档命令可达）⇒ 已改成常量下限 30 min、短跑只能 FAIL；另 **D55**：`bootstrapDecode` 曾让某候选的 `RangeError` 逃到调用方（浏览器/手机 = 接收中崩），已按"记成失败尝试并继续"修掉边界、根因无正例故不猜改 | `node cli/pskit.mjs verify --gate G6`（≈30 min，**显式触发、不在 `all` 里**：判据本身就是 30–60 min 的后台任务，塞进 `all` 会让例行基线每次半小时）· 成本分解量具 `node tools/g6-perf-probe.mjs <页.png> …`（只读、不是门限）· 判决落地后写进 `ACCEPTANCE` G6 段 |
| G7 单色兜底 100% | ✅ | — | `verify --gate G7`（`colourAlive=false` → 整道擦除复原 ✓） |
| G8 3MF/STL 独立解析 | 🟡 | STL ✅（13/13 + 承重反例 ✓）· 投影对拍 ✅（三条独立算路互证 ✓）· **3MF XSD 子集校验器：第 38 轮已写、进程内可跑、对我们所有产物 PASS ✅**（`tools/check-3mf.mjs` + `verify --gate G8` ✓ 规则逐条抄自 vendored `ref/3mf-core-1.4.0.xsd` 并注行号 ✓ 25 条测试 = 21 篡改 + 4 正例含"D40 回归守卫"✓ **绿了 ⇒ 已并入 `--gate all`** ✓）· 仍**不等于过 schema**（本机无 XSD 引擎 ✗ Python 侧 `verify_model.py` 无 `--xsd` 开关 ✗）· ~~**D41 OPEN**：表与权威 XSD 之间**还没有自动对拍** ✗（本轮就是靠抄错权威翻了车 ✓）~~（**过期记账，第 64 轮更正**：D41 **第 39 轮已闭** ✓ 修法 = `tests/unit/threeMF-xsd-parity.test.mjs`，用本校验器自己的 `scanXml` 读 vendored 权威 XSD、逐项比对 `CT_*` 属性集与 16 个元素名 ⇒ 表再抄错就红 ✓ 原句划掉保留为历史、不改写） | `node cli/pskit.mjs verify --gate G8`（或 `--file a.3mf,b.3mf`）· `python ref/verify_model.py --dir .tmp/m5verify --selftest` |
| G9 Web 扫描端（Pages） | 🟡（产物级全绿 ✓ 浏览器内未验证） | `node tools/build-web.mjs && node tools/check-dist.mjs` ⇒ **第 56 轮复量：`13 pass / 0 skipped / 0 fail` + `G9 CHECK: all 12 assertions pass`、exit 0**（原先这里记的"8/8 全绿""SW 78 条清单"是第 22–23 轮的数 ⇒ 这类数字随构建内容变、**别背**，看工具自己打印的汇总即可 ✓ 第 53 轮构建实测：`web/dist` **54 个文件**、SW 预缓存 **52 条**）：零第三方加载点、两页 CSP `default-src 'self'`、SW 清单哈希逐字节对上磁盘、页面每个引用都有对应产物、`pskt-file.html` 只含 data: ✓ **`web/dist/selftest.js` 在产物内跑出 13 pass / 0 fail** ✓ **bundle 无人告知几何解磁盘页 → 摘要等于 `manifest.sourceSha256`** ✓ 差多少：**本机无浏览器** ⇒ "`?selftest=1` 在页面里绿"与"`file://` 双击→选照片→解出文件"这两条只被 Node 侧等价物证明 ✓ 摄像头授权、SW 注册、iOS 主屏图标全属推定；要人在这台机器之外点一次才算闭合 | 第 22–23 轮 ✓ |
| G10 喷嘴 × 参数矩阵 | 🟡 | 只测到矩阵里**一个真实边界点**（~~`PL-G` 在 0.6/0.8 喷嘴被 `glyphGeometry` 正确拒绝~~ —— **第 78 轮更正 ✗**：`PL-G` 四档全可规划（实测见 M7 行），被拒绝的是 `PL-D3S`；原句档位写混，判决未变）⇒ 完整矩阵未跑 ✓ **第 75 轮起有 MTF 板可量**（`& .\tools\mtf-probe.ps1` ⇒ 0 FAIL / 26s：EW 0.45/0.70/0.95 各点名自己的喷嘴、EW 1.40 正确拒绝、EW 0.26 因渗墨被判 filled 并如实记 NOTE）⇒ **判决仍不成立**：那四条是**仿真打印机 + Python 信道**，不是真打印机 ⇒ 仍差真机 ✓ **第 92 轮起有矩阵读数器**（`node tools/mtf-matrix.mjs --dir 照片目录`：四张照片 → 逐张"建议喷嘴 + 判决"，`--selftest` 四条正例 + 两条对照、`--provenance` 只记录用户所说不认假件 ⇒ **判决仍未变**） | `& .\tools\mtf-probe.ps1`（仿真）· `node tools/mtf-matrix.mjs --selftest`（仿真）· `pskit calibrate --make-mtf` + 真打印 + `node tools/mtf-matrix.mjs --dir 照片目录 --spec mtf\mtf-plate.json --provenance real-print`（真机） |

**总账（不主张完成 ✓）**：`✅ 5（G0 G1 G3 G5 G7）· 🟡 5（G2 G6 G8 G9 G10）· ⬜ 1（G4）`（**第 74 轮更正计数 ✗→✓**：G9 那一行的判决早就是 🟡「产物级全绿、浏览器内未验证」，而这里还把它算在 ⬜ 里 ⇒ 过期记账；**判决本身一字未改**） ⇒ **"通过 G0–G10" 不成立** ✓ 全程未降低任何判据来制造"通过" ✓ 基线：单测条数**以 `node --test --test-isolation=none "tests/unit/**/*.test.mjs"` 自己打印的汇总为准、别背数字**（**第 59 轮实测：`tests 297 · pass 297 · fail 0 · duration_ms 136148`、`SUITE_EXIT=0`**；此前最后一次抄下的是第 52 轮 **296/296 · 0 fail**；第 53 轮加了纸面斜拍 1 例后实测 `exit 0`，但当轮没抄下汇总 ⇒ **不写没量过的数** ✗ 原先这里写死的 `231/231` 早已过期，第 56 轮改掉 ⇒ 与 `docs/USE.md` §0 同一口径 ✓）✓ `verify --gate all` `ALL GATES PASS`（**该命令每次都会打印本次未评估哪些门限** ⇒ 不得引用成覆盖 G4/G6/G9/G10 ✓）

### 以下第 6–10 轮的逐轮原文（历史 ✓ 已被上表取代 ✓ 保留以免丢证据链 ✓ 其中若干判定后来被推翻 ✓ 勿据此下结论）

| Gate | 判据摘要 | 状态 |
|---|---|---|
| G0 | RS/ChaCha20/SHA/CRC/帧/压缩 单元+属性全绿 | **通过**（174 例；GF(2⁸) 全域穷举、RS (t,e) 穷举 200/200、ChaCha20/SHA-256/PBKDF2 对拍 node:crypto 与 RFC 向量、deflate 双向对拍 node:zlib；本环新增 advice 映射覆盖 3 例、conformance 活复算 11 例、PDF 多页 4 例） |
| G1 | 理想往返 0 符号错 | **图像层通过**：`verify --gate G1` 7 档 × 3 次 = 725,913 格 0 误读（含 600dpi 纸面 620,136 格），全部还原；STL 往返部分待 M5 |
| G2 | 纸面 300/600dpi 200 seed 100% | **仍未通过，但结论变了：不是信道鲁棒性问题，是几何读出路在纸面上从未能用**。对照实验（同一页、过信道 vs 没过信道）两边**都**报 `markers/no-hollow-corner`——连**从没被打印/扫描过的原始渲染**都读不出 ✓✓ 此前 G1 的"269 万格"与"字节穿越照片"分别走快路和板材档，纸面 + 几何路**一次都没被门限覆盖过**（决策 10 要求"干净画布才允许走快路"，但没要求任何门限真的走慢路 ⇒ 门限表的空洞）。已排除的假设（都带实测数据）：① Otsu/二值化正常（阈值 141 vs 最大 248、判墨 10% ≫ 32）；② inkness 横轴被离群值拉伸（换成 p99/p99.9/p99.99 定标阈值一模一样）；③ 布局没问题（四角标都在画布内，`br` 中心实测 0.00 墨=真空心，另三个 1.00=实）；④ `keepCandidateSquares` 没筛掉它们（四个 30×30 全保留，`br` fill=0.89）；⑤ 簇没被网格抢走（全图只有两个簇：`30:4`=正是四个角标、`5:27213` ⇒ **角标簇确实被选中**）。**剩下的唯一分歧点**：`hasHole` 在 `buildQuad` 里对这同一批候选返回假，而我在外面用同一段代码、同一份掩码复现返回真 ⇒ 分歧只能在传进去的 `bin`（`page = {...bin, mask: cropMask(...)}` 的裁剪坐标/步长）上。下一轮**第一件事**就是量 `cropMask` 后的 `width` 与分量 `cx/cy` 是否同一坐标系。附带已改进：`findMarkers` 不再返回"最后一次重试"（旧代码把 2.1× 阈值算出 296 > 最大值 248 ⇒ 墨点 0 ⇒ 用 `blank-image` 覆盖真实原因，让用户去重拍其实没问题的照片，违反决策 11），改为跳过会饿死的尝试 + 返回**走得最远**的那次；`tools/diag-page.mjs` 落地（直方图/阈值/`--markers` 角标解剖）。192/192 绿。 |
| G2-第7轮 | 同上（**取代上一行的判定**） | **第一层根因找到并已修复**：`cropMask` 的内缩按页尺寸算（`0.008×min(w,h)` ⇒ 300 dpi 纸面 18 px、600 dpi 36 px），而角标包围盒离纸边只有 **10 px**（心在 `fidHalf+cellPx=25`、bbox 从 10 px 起）⇒ 角标外圈被抹平 ⇒ 环的洞与外界连通 ⇒ **零个**空心候选 ⇒ 每一页都判 `no-hollow-corner`。该带宽本意只是"重采样后残留两三像素暗边"，封顶为 2–4 px 才名副其实。**修完的硬证据**：原始纸面页经真实几何路（不给尺寸、不给 dpi）读出 `received 204800 bytes`，与发送文件 **`IDENTICAL=true`** 且 SHA-256 与 manifest 一致 ✓✓ 此能力此前根本不存在。**【第 8 轮更正】上一轮末我写的"凶手是旋转重采样糊掉角标中心"是错的**：真凶是**裁角**。`--off geometry` 之所以有效不是因为去掉旋转，而是因为连同 `crop_frac` 一起去掉了——信道报告里 `marker_visible[1]=0.374`（tr 角标只有 37% 在幅面内 ✓ 三页里四个角标只剩三个 ✓ 而簇规则要求 ≥4 ⇒ 检测器根本没试角标簇）。平板扫描仪有纸定位边、视野大于纸面 ⇒ 物理上 `crop_frac` 应为 0，用现成的 `--modifier nocrop` 即对 ⇒ **三页全部越过角标阶段** ✓✓ 教训：`no-hollow-corner` 这个 reason 连续两轮把人说偏（先是裁切带吃角标，再是裁角），它实际含义是"凑不齐四角"，已在 advice 待办里要求拆分。**第 8 轮真正的瓶颈转移**：`sim/channel.py --preset scan300 --modifier nocrop` → 三页全数通过几何 + 头部，齐死在 `intra-fail`（页内 RS 超预算）⇒ 管线层面已无阻碍，剩下的是**格心读出的原始误码率**，正是 PLAN 风险第一条（"1 px 模糊即可桥接点阵，真实 MTF 更差"）的实证。G2 仍**未通过**，但从"读不出页"变成"读得出页、纠错预算不够"。

本轮另修两处结构性脆弱：① `core/decode/echo.js` 的阈值原本是硬编码 `> 6000`（平方 RGB 距离 ✓ 原始渲染墨点距白 ≈168 000 所以一直没事 ✓ 真实采集下曝光/白平衡/纸色把整个分布搬走就废 ✓），现改为**由条带自身的 CRC 仲裁**：Otsu、6000（板材路径实测需要它，降级为候选而非假设 ✓）、分布内若干中点、Otsu 的 0.75–1.3 倍 一起试，谁解出合法 magic+CRC 谁胜出 ⇒ **接受条件一点没放松**（仍必须 magic+version+CRC16 全对 ✓✓ G5 复跑 PASS ✓），只是阈值不再靠猜。② `page.js:62` 让 echo 失败**一票否决整页**（448 bit 无任何纠错 ✓），已记为待办：manifest 可用时应允许走数据区并用页内/页间 RS + 末端 SHA-256 定夺。第 9 条新 reason `no-candidate` 被上一轮写的 `advice-coverage` 测试**当场拦住** ✓ 纪律第一次真正生效。194/194 绿。
| Gate | 判据摘要 | 状态 |
|---|---|---|
| G2 分界（第 9 轮·**墙已拆**） | 格心电平判定 | 上一轮点名的根因按设计修掉了：`core/decode/ideal.js` 新增 `buildShapeTemplates()` + `matchedShapeLevel()`，形状电平改为**逐格对各级模板做匹配滤波**（模板来自渲染器自己用的 `glyphMaskForLevel` ⇒ 编解码不会各说各话、且不引入新自由度 ✓），取代原先"两个环形窗的 ρ 最近邻"。滤波器带两条刻意不变性：**每格自由增益**（残式对模板幅度解析求极小 ⇒ 曝光/模糊只能整体衰减覆盖率、不能单独翻掉判决 ✓）与 **±1 px 对齐搜索**（配准误差不再由形状通道吸收 ✓）。**证据**：上一轮 3/3 `intra-fail` 的那个配置（`scan300 --modifier nocrop`、**标称 EW+MTF 开着** ⇒ 我没有用"把 `ew_mm`/`mtf_mm` 钉成 0"来骗过门限 ✓）现在 204 800 字节 **逐字节复原** ✓✓；而**全因子** `scan300`（裁角+旋转+光度学全开，正是第 8 轮开头 0/3 的那份语料）现在也成：page-000 因 tr 角标被裁而**正确拒绝**、页 001/002 读出、**页间 RS 用校验页把缺损那页重建** ⇒ `received 204800 bytes` ✓✓✓ 协议设计意图第一次在真实物理信道下完整兑现（一页掉角不等于整批失败 ✓）。副带改进：`quality[]` 的含义从 `alphaMax`（最暗像素）改为**匹配滤波的 margin**（次优电平与最优的分离度）⇒ 接收端的擦除决策现在拿到的是"这两个电平到底有多分得开"这个诚实数字，而不是"这格有多黑"。代价：读出 +25%（300 dpi 单页 ~0.8 s → ~1.0 s ✓ 可接受）。**G2 第一批统计（200 KB 载荷 × 3 页，`--gate` 之外的真实语料）**：同一份 `scan300`/`scan600` × seed 1-8，两轮对照——(a) 预设原样（含裁角）：scan300 **7/8**、scan600 **5/8**；(b) `--modifier nocrop`（平板扫描仪的物理正确模型：纸贴定位边、视野大于纸面 ⇒ crop 本该为 0）：scan300 **8/8** ✓✓、scan600 5/6（seed 3 为 `0/1 data pages` = 部分页读出但不足 ⇒ 与 crop 那批的"整批读不出"是**不同类别**，属真实 600 dpi 光学误码，待查）。裁角那批的失败全部是"没有一页头能读"，与 `marker_visible` 掉角一致 ⇒ 归入 G4（手机）而不是 G2（扫描仪）。**距 PLAN 的 200 seed 100% 还差 25 倍样本量**，且 600 dpi 的那个失败必须先弄明白——不许用"只报通过的那档"来凑数 ✓ 194/194 单测 + 7 条匹配滤波性质测试全绿、`verify --gate all` 全通过。 \| **已确证：光度学半边是好的，几何半边死在边缘位移上。** 我用 204 800 字节载荷（SIM 侧对照是 421 字节 ✓ 苛刻 ~500 倍）复跑：`scan300 --modifier nocrop --off mtf,ew`（纸纹/色调/曝光/暗角/削顶高光/色调曲线/自动白平衡/光子噪声/JPEG q≈84 **全开**）→ 三页全走真实几何路（`photo marker 31px cover 100%`）→ **`BYTE-IDENTICAL = true`** ✓✓ 而把 `mtf,ew` 放回（标称 EW 位移 ~0.10 mm + 模糊 σ≈0.07 mm ⇒ 10 px 一格的边缘位移不足 1.5 px）⇒ 三页齐 `intra-fail` ✓ SIM 侧独立结论一致："纸面 0.8467 mm 栅格上形状通道对边缘位移的容差实测≈0，0.02 mm 即 intra-fail" ✓✓ 两条证据链指向同一处：**格心电平判定用的是"采样区平均 inkness + 绝对阈值"，而不是"与该级字形模板的相关"** ⇒ 模糊/扩展把所有电平一起推离参考值，电平之间就分不开了。修法（下一轮 M4 收口的正题）：把 `readPageIdeal` 的逐格采样换成**匹配滤波器**（对每格用 `glyphMaskForLevel` 生成各级模板，取相关最大者为电平；模板与 inkness 分布同源自 `glyphSignature`，纸面/板材/网格三产物共用同一形状来源，不引入新自由度）。**不许**用"钉 `ew_mm`/`mtf_mm` 到 0"来让 G2 变绿——那是把被测对象关掉 ✓ 记 SIM 侧一条已过时的事实：它报"`--photo` 读不出原始 P-M1-300 渲染（no-hollow-corner）"，那是本轮 `cropMask` 内缩带修复**之前**的状态，现已修好；但这条报告本身证明独立侧有价值（它替我抓到了门限表从没覆盖的缺陷）。 \| 此前只是约定、无人强制 ⇒ 实测 **11 条** reason 无 advice：`intra-fail`/`bad-magic`/`header-crc`/`short-header`/`other-session`（来自 frame/protocol，CLI 当场印 `unmapped failure (assemble: intra-fail)` 并叫用户重拍其实没问题的整批），我第一轮只扫 5 个文件、又漏了 6 条 `short`/`long`/`erasure-oob`/`too-many-erasures`/`beyond-limit`/`chien` ⇒ 改成**扫整个 `core/`** 的 `tests/unit/advice-coverage.test.mjs`，并给测试自身加反退化断言（匹配到的 reason 数 <20 直接判失败，防"什么都没匹配上"的假绿）。**反向检查故意不做**（有 advice 却没发射点）：`echo-*` 是 `'echo-' + reason` 动态拼的，字面量扫描会造 5 条假红——这已是第三次踩"新守卫比被测代码更严"。 |
| G8 | 3MF/STL 独立解析 | **M5 地基落地**：`core/mesh/solids.js`(550) + `core/mesh/stl.js`(290) + 12 条测试全绿，`prismFromMask` 的最大矩形覆盖**逐像素等于掩码**且矩形互不重叠（手算 8/4/4 三个反例都判 false ⇒ 断言承重），STL 头无时间戳且两次编码逐字节相同，`watertightHint` 恒 false 不谎报水密。代理还实测纠正了我 `docs/MESH-CONTRACT.md` 的两处签名错误（`outer/inner/dot` 是**格宽比例**不是 EW 数；`glyphMaskForLevel(dx,dy,level,geo)`），已按真实行为改文档。剩余：3MF 路径、`--format stl\|3mf` 接线、`ref/verify_model.py` 与投影对拍。 |
| G3 | 缺页/乱序/重复 ≤parity 100%，超出干净拒绝 | **协议层通过**（`verify --gate G3`，3 档 × 丢 0..parity 页 + 乱序喂入全部精确还原；丢 parity+1 → `result=null` 且不写文件；每页喂两次 → 计入 duplicate 且字节不变；异会话页 → `other-session` 拒绝）。**图像层丢页已闭合（第 12 轮 ✓ `tools/g2-corpus.mjs` 真删 PNG 后重跑）**：以干净语料 `.tmp/g2src` 做 6 例——**只留 page-000（数据页）**、**只留 page-001**、**只留 page-002**（后两例即"缺 2 页 = 正好等于 parity 预算"⇒ 只剩一张校验页也必须重建 ✓✓ 这是本门限最硬的一条 ✓）+ 各丢 1 页三例 ⇒ **6/6 逐字节、摘要由工具进程内重算** ✓ 三页全不在 ⇒ 工具直接拒绝并 exit 2（协议层另有 `result=null` 且不写文件的证据 ✓）。**分层记账**：先前用被信道打坏的语料做同一测试时，两例分别死在 `readout/echo-bad-magic` 与 `markers/no-hollow-corner` ⇒ 那不是"校验页没有头"的结构性缺陷 ✓ 而是 G2 的账；结构测试必须用干净页做，否则两层的失败会互相顶包 ✗ 乱序一例仍只在协议层覆盖（本工具按文件名排序喂 ✓ 图像层乱序等价于喂入序 ✓） |
| G4 | 手机压力档 ≥99% + 失败分类 | 未开始（信道在写）。失败分类侧已就绪：`core/decode/advice.js` 20 个成因→重拍指令 + 防腐测试 |
| G5 | 误接受实测 0（1 万次篡改全拒）+ 变异测试 | **通过**：`verify --gate G5 --trials 10000` → 9200 次被 ECC 纠正回原值、800 次拒绝（全部由明文 SHA-256 闸拦下）、**0 误接受**；7 种损坏模式混合（位翻、整格擦除、丢页、头 CRC 破、头字段伪造后重算 CRC、异会话整页替换、>40% 重损）。变异检验两项：① 试验组合必须真的触发帧 CRC/magic 闸（否则混合太弱）；② **摘要是承重的**——伪造页带正确 sessionId + 新鲜合法 CRC + 合法码字，所有结构闸全部放行，只有摘要拦住它；若删掉摘要检查仍出字节，这条就红（第一版此测试因"一页就装完全部数据"而假通过，已修成先短一页再塞伪造页） |
| G6 | 性能 + 30–60min soak 无泄漏 | 部分：SHA-256 269 MB/s、deflate 1.1MB/35ms、600dpi 整页 (4530×6590px) 渲染 192ms + 读回 430ms、PNG 617ms/TIFF 181ms、**单应矫正 362ms（2236×2236 画布）**、50 次连跑 heap Δ 0.0MiB |
| G7 | 色道塌陷下单色兜底 100% | **图像层通过**（单色渲染图被判 `colourAlive=false` → 整道擦除复原；`monoSafe:'off'` 档 → 拒绝而非猜） |
| G8 | 3MF/STL 被 Python 独立解析且水密/schema 正确 | 未开始（PNG/TIFF 已过 pillow 校验；PDF 编码器支持 N 页打包 `pack.pdf`，3 页板材包 548 KB/161ms） |
| G9 | Web 产物零外部 origin + 离线 + selftest | 未开始 |
| G10 | 喷嘴×参数×拍摄矩阵（PL-G 全 100%） | 未开始 |

**验收机制本身的自检**：`runUnitFiles` 带两道护栏——观察到的测试数为 0 判失败、"有结果的测试文件数 < 发现的文件数"判失败。这条是被真的踩出来的：`isolation:'none'` 下事件词表是 `test:pass/test:fail/test:complete`（叶子 `nesting` 恒为 0），早期计数器按旧词表数事件 → **空跑被报成 ALL GATES PASS**。已用故意失败的 canary 测试证明门限确会变红（`136 passed, 2 failed / GATE FAILURE`）。

## 决策（新增，勿回退）

1. **静区从单元格里扣**（`QUIET_CELLS=5`，`core/render/constants.js` 叶子模块）。之前只按点阵 fit 版面，页面实际超出板材。`constants.js` 必须独立成文件，否则 `profiles → render/layout → frame → profiles` 成环，`frame.PROFILE_CODES` 会踩 TDZ。
2. **ρ 阈值自标定**：`shapeThresholds(levels,{targets})` + `measureTargets(cellPx,levels,glyph)`。有限像素下小点积分出的 ρ 系统性偏低，解码端就用"渲染器自己画出来的实测值"当目标，不把偏置当余量吃掉。`pskit calibrate` 对真实扫描做同一件事。
3. **mono 不搬道**：单色渲染 = 色道信息**消失**（变成擦除），不是把色道位并进形状档位。
4. **色道存活判据只看"出现过几种墨"**（`distinct>1`）。宁可保守：浪费容量但绝不丢数据。
5. `monoSafe` 三档、明文摘要、分块塞满、`sessionId=sha256(明文)[0..8]`、`PL-D3='off'` 等 M1/M2 决策不变；旧条目里的容量数字一律以本文件下面的实测表为准。
6. **形状字母必须按整条挤出宽度 EW 量化**（`glyphGeometry(cellEw, shapeLevels)`，`core/render/glyphs.js`）。外环/内环/点半径全取整数 EW，并强制：每侧 ≥1 EW 净间隙、环厚 ≥1 EW、点—环间隙 ≥1 EW、最小可印点半径 ≥√(L−1) EW、L 个半径互不相同、且 ρ_hi ≤0.95（参考环必须仍是主反射体，否则 ρ>1 会被"这格是墨 blob"判据杀掉）。**结果：2 档形状 ≥8 EW/格，4 档 ≥~27 EW/格。**
   起因：1.8mm 节距 @0.4 喷嘴只有 ~4 EW/格，环间隙 ≈0.11mm，仿真里 **1px 模糊就把整张点阵桥接成一个 1664×2273 的连通域**，角标被吞、检测直接 `no-square-candidates`。这不是测试噪声，是原设计的物理错误。
   代价如实记录在容量表：`PL-D2@0.4` 1098→**220 B/页**（掉 5×）。纸面不受影响（一格 ≈ 数百打印点，走 `cellEw≥24` 的理想圆环分支）。`planPage` 会自动抬节距（`pitchRaisedFrom` 记下原值），抬完仍放不下就**点名拒绝**，绝不产出读不出来的版。
7. **回显带随格子变粗而变细，画布为它让位**：`echoPx` 从 cellPx/2 逐级试 /3 /4 …/12，下限是 1 EW；画布宽度取 `max(点阵, 回显带)` 并把点阵居中（粗喷嘴下 56 bit 头比点阵还宽），回显带左对齐到静区而不是跟着点阵走，否则会压到右侧角标。
8. **空心角标的环厚 = 1 数据格**（≥1 EW）。旧写法 `half/4` 在抬节距后只有 0.82 EW：印不出来，且 fill 掉到 0.42 与数据环同域，检测无法区分。现在环 1 格、孔 1 格，`fill≈0.89` 与实心块同档，靠 `hasHole`（探针半径 0.12 短边，必须落在孔内）区分。
9. **检测器的四条硬规则**（M8 网页端必须原样复用，别再发明一遍）：
   - 页面外接框只内缩 `0.8%`（上限 24px）：只为吃掉重采样在纸边留下的 ~2px 暗晕；缩多了会啃掉角标并让质心偏移，进而把整个单应带偏。
   - 候选过滤是**结构性**的：>8% 页面面积、或单边跨 35% 页面、或贴边且 >1% 的块一律不要（暗背景楔形与桥接网格都在这里被剔除）。角标小而居中。
   - 四边形枚举必须同时满足：4 条边同向凸（两两叉积会放过自交蝴蝶形——鞋带面积≈0 曾把正确的四角判成"框不住页面"）；**转向为正**（镜像的走法同样凸、同样满足"br 空心"，会把角色旋转 90°，所以镜像只能单独报 `mirrored-image` 提示"纸放反了"）；**空心判据进入枚举**而不是事后校验（矩形度对 4 种循环标注同分，事后校验会挑中一个实心在 br 的标注然后报 `hollow-corner-missing`）。
   - **底色取自矫正后画布静默区的亮部上四分位**（`quietZoneSubstrate`），绝不取照片边框：深色桌垫会让底色估成 (40,40,45)，于是纸被判成墨、整页反相，误读率 76%（≈三值字母整体平移一格的特征签名）。
   - 二值化带多阈值重试（Otsu ×1/×1.35/×1.7/×2.1）。干净图必须一次命中——`warp.test.mjs` 断言 `thresholdFactor===1`，把"依赖重试"变成可测的回归。
10. **只有一条解码路**：`core/decode/page.js:decodePage` 是"图像→页数据"的唯一入口（角标检测→单应→回显带头→读格）。CLI 现在调它，M8 网页端必须调同一份，不允许出现"CLI 一套、浏览器一套"的两条读出路——那样门限测的已经不是交付的东西了。干净画布（尺寸+dpi 都对得上）才允许走快路省掉重采样，**且快路读不出来时必须退回几何路**：一张恰好同尺寸的扫描不是画布。
11. **每个失败原因都必须有对用户的说法**：`core/decode/advice.js` 把 reason 映射到"物理成因 + 重拍指令"（G4 判据）。映射表会被遗忘，所以 `tests/unit/advice.test.mjs` 直接扫 `core/decode/*.js` 源码里的 `reason:` 字面量——新增一个没映射的原因就红（已用注入 `zz-not-mapped` 证实它真的会红）。没映射的原因仍要给出通用指令，并且**自称未映射**，不许借用别人的成因。
12. **交叉校验的独立性靠"看不到"来保证**：`tests/conformance.json`（78 条向量，196 KB，发射器带 400 KB 预算闸）是答案卷；`ref/decode.py` 的编写纪律是**禁止阅读 `core/*.js`**，只准依据 JSON 里的 `meta`（GF(2⁸) 参数、CRC 参数、位序、交织规则、56 字节头表、KDF、容器格式）独立实现。规则写进 `meta` 而不是靠读码，是这套对拍有意义的前提；信息不足时必须报 `SPEC GAP` 而不是猜。同时 `tests/unit/conformance.test.mjs` 在 JS 侧**逐类复算**每条向量（含"重新发射必须逐字节相同"），所以这道对拍不会静默腐烂，也不依赖 Python 是否在跑。

13. **给了口令就是要求加密**：`encodeTransfer(x, {passphrase})` 不必再补 `cipher:true`；`passphrase` 本身就是加密请求，页头必须置 `CIPHER`。旧语义要求两个旗标同时出现，漏一个就**静默印明文**——CLI 恰好两个都传所以躲过了，是我的 conformance 发射器（只传口令）踩中的，而独立解码器报"页上没有任何东西告诉我要解密"才把它照出来。反向纪律：接收方看到 `CIPHER` 却没拿到口令时，只能 `needPassphrase` 问用户，绝不吐字节。

14. **对拍的独立性在"协议逻辑"，不在重造 FIPS 原语**：`ref/decode.py` 的 GF(2⁸)/RS、交织置换、色道打包、PSZ1 容器、56 字节头——这些必须只看 `meta` 自己写，才算另一个实现；SHA-256/CRC-32/PBKDF2 改用 hashlib/zlib 反而**更强**：我的 `core/hash.js`、`core/crc.js` 在 G0 已各自钉在 node:crypto/node:zlib 上，参考端再自搓一份只会把"两个都错得一样"的风险换成"参考端自己有 bug"（本轮实测就是：它自写的 SHA-256 消息根本没进压缩函数，空串摘要都对不上）。ChaCha20 保留它自己那份（stdlib 没有，且它 8/8 全过——这是真独立的证据）。换原语的理由与证据写在 `ref/decode.py:sha256_bytes` 的 docstring 里，不留"看起来像偷懒"的空白。

15. **规格必须能被"只看它的人"读对，形容词不算说清**：本轮两条实证。① `meta.cellPacking` 写着"主通道占高位、次通道占低位，逐字节 MSB-first"——一个认真按它实现的解码器仍然把**次通道读成了主通道**（每个通道都用 `bitsPerCell - 本通道宽度` 算 shift），症状是"content 全对、parity 全错"；现在 meta 里多了一组可验算的数字（PL-D2@0.4：`content[0]=0xB4, parity[0]=0x1D → levels[0..7]=2,0,2,3,1,3,0,1`）与 `shift = bitsPerCell - 前面通道位数之和` 这一条式子。② 头字段 `payload length` 只写这四个字，参考实现按"文件明文大小"理解，于是要么解错长度要么判定溢出——真实语义是**压缩/加密之后、且含最后一页分块填充**的长度（有意义字节数 = payloadLen − blockPad，装配区长度 = dataPages×D），已连式子一起写进 `meta.headerLayout` 那一行。判据很简单：**M8 网页端也将只依据这份文字实现**，它能读对才算写清了；任何"实现者读错"都先当作规格的失败，而不是对方的错。

## 容量实测表（`node cli/pskit.mjs status` 复算，勿手改）

| 档 | 喷嘴 | 节距 mm | 网格 | 净字节/页 | 码率 |
|---|---|---|---|---|---|
| P-M1-300 | — | 0.847 | 216×319 | 7514 | 0.874 |
| P-M1-600 | — | 0.423 | 443×649 | 31302 | 0.874 |
| P-M2-600 | — | 0.423 | 443×649 | 62604 | 0.874 |
| P-C4-600 | — | 0.423 | 443×649 | 35658 | 0.5 (full) |
| P-MX-300-6 | — | 0.508 | 352×524 | 20111 | 0.874 |
| P-MX-300-5 | — | 0.423 | 424×630 | 29082 | 0.874 |
| P-MX-300-4 | — | 0.339 | 523×780 | 44400 | 0.875 |
| PL-D3 | 0.2 | 2.08 | 80×80 | 2090 | 0.875 (off) |
| PL-D3 | 0.4 | 3.60 | 42×42 | 576 | 0.873 (off) |
| PL-D2 | 0.2 | 2.08 | 80×80 | 798 | 0.5 (full) |
| PL-D2 | 0.4 | 3.60 | 42×42 | 220 | 0.5 (full) |
| PL-D2 | 0.8 | 7.60 | 14×14 | 24 | 0.5 (full) |
| PL-M1 | 0.2 | 2.08 | 80×80 | 696 | 0.874 |
| PL-M1 | 0.4 | 3.60 | 42×42 | 192 | 0.873 |
| PL-D3S | 0.2 | 6.24 | 20×20 | 100 | 0.667 (partial) |
| PL-D3S | 0.4 | 10.8 | 7×7 | 12 | 0.667 (partial) |
| PL-D3S | 0.6 / 0.8 | — | — | — | **拒绝**（4 档形状放不下，见决策 6） |
| PL-G | 0.4 | 3.60 | 42×42 | 110 | 0.5 |
| PL-G | 0.8 | 7.60 | 14×14 | 12 | 0.5 |

结论性事实，必须如实对用户讲：**FDM 板材不是高密度通道**。200mm 方版在 0.4 喷嘴上，双色 `PL-D2` 只有 ~220 B/页、四色 `PL-D3` ~576 B/页；传 1MB 要 4~5 张版且不允许任何色道塌陷。要密度就走**纸面**（600dpi 单色 31KB/页、四色 36KB/页，1MB 载荷 37~41 页）。板材的价值在于"没有纸和打印机也能做 + 双面/耐磨/可夹在零件里"，不是带宽。

1MB 载荷纸面页数：600dpi 单色 **34+7=41 页**；600dpi 四色 **30+7=37 页**。板材超 255 页会被拒（页间 RS 上限）。

## 已知风险 / 待办

### 第 122 轮（**修掉"dry-run 说谎"：`--dry-run` 现在跑与真 `send` 同一套版面预检（带阳性/阴性双向对照）**）

**① 改动**：`cli/pskit.mjs` 的 `--dry-run` 分支在打印计划之前，先调用**与真路径同一个** `pageLayout(geom, dpi, {plateMm, sheetMm})`（第 121 轮实测：拦住 `quietCells 8` 那条路线的 64 px 约束就在这一步）。⇒ 打印出来的计划从此是**存在的**计划。

**② 验证（双向对照，都是本轮实跑）**：
- **阴性对照**（临时加回 `P-MX-300-5Q-TMP`：`quietCells: 8` = 40 px < 版面要求的 64 px）：`--dry-run` ⇒ **`exit 1` + `pskit send: pageLayout: ... (need 64px)`**（修前：打印完整计划、exit 0 —— 第 121 轮就是这个现象）；同一档真 `send` 也 `exit 1` ⇒ **两种模式现在同判** ✓
- **阳性对照**：正常档 `P-MX-300-5 --sheet A4 --dry-run` 仍打印计划（`net 29082 B/page -> 14 pages`）且 **exit 0** ✓（没有把正常路径一起弄红）
- **临时对照档已删除**，档位表回到 `P-M1-300 … P-MX-300-4`（13 个档，`node -e` 打印核对 ✓）。

**③ 门限**：单测 **`tests 377 · pass 377 · fail 0`、`SUITE_EXIT=0`**。

**④ 性质**：这条属于本项目"**不得输出看起来成功但是错的东西**"的同一族（不是误接受数据，而是给出一个不存在的计划）⇒ 修好即闭，不再进 DEFECTS 长表。

### 第 121 轮（**想缩静区，被版面规则挡住：静区有 64 px 的硬下限 ⇒ `-5` 档的 14 格已接近最小；顺带抓到一个"dry-run 说谎"的缺陷**）

**① 假设（本轮要验的）**：静区 `quietCells` 在 `-5` 档是 14 格 = **5.9 mm/边**，比边距还大；而密度尺只用 **~2.7 mm** 白边包住角标、且角标在 **44% 墨**的页面上照样被 `findMarkers` 找到 ⇒ 我判断"5.9 mm 至少可以砍掉一半"。

**② 实测（否掉了这个判断 ✗）**：新增 `P-MX-300-5Q`（`quietCells: 8` = 3.4 mm）后：
- `--dry-run` **说能行**：436×642 = 279,912 格、**30,498 B/页**（比 `-5` 多 4.9%）。
- **真 `send` 直接拒绝**：`pageLayout: ... (need 64px)` —— 版面自己要求静区 **≥64 px**；`-5` 的 14 格 = 70 px **本来就已经贴着下限**（13 格 = 65 px 才刚好过线）。把 `quietCells` 改成 12（60 px）仍然被拒。
⇒ **结论：当前版面规则下静区缩不动**（拦住它的不是检波器的需要，而是版面自身的 64 px 约束）。**如实记为我判断错误**：证据（角标只需 2.7 mm）不足以推出"版面允许 2.7 mm"。**该实验档已删除**，不留在表里（它存在的唯一价值就是这个否证，已记在本轮）。

**③ 顺带抓到的真缺陷（dry-run 说谎）**：`send --dry-run` 对同一个档位**打印了完整计划**（含页数与容量），而真 `send` 立刻拒绝 —— 即 **dry-run 不跑 `pageLayout` 的约束检查**，会给出一个不存在的计划。复现：`node cli/pskit.mjs send .tmp/d78/page-000.png --profile P-MX-300-5Q --sheet A4 --format png --out .tmp/q --dry-run`（修前打印计划）对照同一条命令去掉 `--dry-run`（`exit 1`、`need 64px`）。**性质：会给用户一个假承诺**，与本项目"不得输出看起来成功但是错的东西"同一族。**未修**（本轮只记录）。

**④ 保留下来的改动**：`DEFAULT_PROFILE` 从已退役的 `PL-D2` 改成 `P-M1-300`（3D 线取消后，兜底不该指向退役档）。

**⑤ 下一步**：把"dry-run 必须与真 send 同判"修掉（最小修法：dry-run 也调用 `pageLayout`/渲染前的同一批检查）；静区那条路在版面规则改动前不再碰。

### 第 120 轮（**量出"纸越小、固定开销越贵"，并据此把边距改成随纸缩放（A4/Letter 逐字节不变，A6 效率 +12%）**）

**① 先量（面积效率表，`--dry-run` 实测，`net B ÷ 纸面积`）**：

| 纸 | `P-MX-300-6` | `P-MX-300-5` | `P-MX-300-4` |
|---|---|---|---|
| A4 | 0.322 B/mm² | 0.466 | 0.712 |
| A5 | 0.284 | 0.414 | 0.623 |
| A6 | 0.235 | 0.343 | 0.507 |

**同一档在小纸上的面积效率明显更低**：`P-MX-300-5` 的晶格只占 A4 的 **76.7%**，却只占 A6 的 **56.4%** —— 因为 **9 mm 边距与静区是固定值，纸却在变小**。这是"缩固定开销"的第一份量化依据（不是感觉）。

**② 改动（最小、可证不伤老页面）**：`planPage` 的纸面边距从固定 9 mm 改成 **`min(9, 短边 × 6%)`**：A4（12.6 → 9）与 Letter（13.0 → 9）**逐字节不变** ⇒ 已打印的页面一动不动；A5 8.9 mm、A6 6.3 mm。

**③ 实测（同一档 `P-MX-300-5`）**：

| 纸 | 边距 | 格数 | 净 B/页 | 晶格占纸面 |
|---|---|---|---|---|
| A4 | 9 mm（不变） | 267,120（不变） | 29,082（不变） | 76.7% |
| A5 | 8.9 mm | 118,150 | 12,876 | 68.1% |
| **A6** | **6.3 mm** | **54,810（+12%）** | **5,967（+12%）** | **63.2%（原 56.4%）** |

**④ 门限**：改了 `core/profiles.js` 的**纸面边距**（A4/Letter 数值等价）⇒ 全量单测 **`tests 377 · pass 377 · fail 0`、`SUITE_EXIT=0`**（A4/Letter 几何等价 ⇒ 既有用例全绿）；`check-docs-tables` **clean（465 行 / 79 张表）**。

**⑤ 下一步**：静区（`quietCells`，`-5` 档是 14 格 = 5.9 mm/边）比边距还大，是下一个可缩项 —— 但要先量"缩到多少角标还能被检出"（角标就在静区里），所以那一步必须带阳性对照。

### 第 119 轮（**P2 第一刀：`--sheet A5|A6` 落地 —— 小纸不再是口号，是能算出来的一档**）

**① 发现（省钱的一步）**：`planPage` 的晶格**本来就由纸张推导**（`region = sheet − 2×margin`，再扣静区）⇒ 小纸**不需要新 profile**，只要给 `SHEETS` 表加上 A5/A6 并把 CLI 的 `--sheet` 放开即可。**没有新增 profile、没有动协议、已有页面的 profileCode 一字未变。**

**② 改动**：`core/profiles.js` 的 `SHEETS` 加 `A5 {148,210}` 与 `A6 {105,148}`（附注释说明依据 PLAN v5）；`cli/pskit.mjs` 帮助文本改成 `--sheet <A4|A5|A6|Letter>`。

**③ 实测（同一张图、同一档 `P-MX-300-5`、`--dry-run`）**：

| 纸 | 版面 | 净 B/页 | 页数上界（未压缩） |
|---|---|---|---|
| A4 | 424×630 @0.4233mm | 29,082 | 14 |
| **A5** | **278×424** | **12,876** | 28 |
| **A6** | **176×278** | **5,328** | 67 |

面积比与容量比对得上（A5 面积是 A4 的 47%、容量 44%；A6 更小是因为**固定边距与静区在小纸上占的比例更大** —— 这正是"纸越小、边距开销越贵"的量化证据，也是下一步该缩边距/静区的依据）。

**④ 门限**：动了 `core/profiles.js`（**只加表项，不改任何既有几何**）⇒ 单测 **`tests 377 · pass 377 · fail 0`、`SUITE_EXIT=0`**；`check-docs-tables` **clean（457 行 / 77 张表）**。

**⑤ 下一步**：① 把 A5/A6 的容量表写进 `docs/PLAN-V5.md` §6；② 小纸的**固定开销**（margin 9mm + 静区）值得按纸尺寸缩一缩（A6 上浪费了近 10% 的可用面积）；③ 等真机阶梯数字。

### 第 118 轮（**P1 第一刀：图片默认档从 `P-M1-300` 换成 `P-MX-300-5`（同一张图页数约少 3.4 倍）**）

**① 依据**：第 117 轮量出的规律 —— **拐点看"扫描 dpi 下每个模块几像素"**，不看物理间距（4 px/模块在 300 dpi 与 600 dpi 上都退化到 ~1.3e-3，5–6 px 归零）。手册指给用户的正是 **300 dpi 平板** ⇒ 取 **5 px** 那一档。

**② 改动**：`cli/pskit.mjs` 的 `defaultProfileFor()`：**图片扩展名（png/jpg/webp/heic/tiff/bmp/gif）默认 `P-MX-300-5`**；其余文件仍 `P-M1-300`（保守）。`docs/USE.md` 同步说明依据。

**③ 实测（同一张 292 kB PNG，`--dry-run`）**：

| 默认档 | 版面 | 净 B/页 | 页数上界（未压缩） |
|---|---|---|---|
| 旧 `P-M1-300` | 216×319 @0.8467mm | 7,514 | **47 页** |
| 新 `P-MX-300-5` | 424×630 @0.4233mm | **29,082** | **14 页** |

⇒ 同一张图**页数约少 3.4 倍**。**但 `P-MX-300-5` 的真机证据仍然没有**（只有模拟 16/16）：这是"按实测规律选的默认值"，不是"已验的真机结果"。等用户那三张阶梯页的数字回来，若 0.42 mm 在他机器上退化，就退回 `-6` 档（半步）。

**④ 门限**：本轮改的是 CLI 默认值（未动 core）⇒ 跑单测 + `check-docs-tables`；`usability.ps1` 里"裸 `send` 默认走纸面档"那条腿用的是 `.bin` 载荷 ⇒ 仍走 `P-M1-300` ⇒ 不受影响（已核对代码）。

### 第 117 轮（**给信道补上 `--dpi` 重跑：A6@1200 那三行"BER 0"是假绿、已撤回；并量出真正的规律 —— 拐点取决于"扫描 dpi 下每模块几像素"，与物理间距无关**）

**① 为什么要重跑**：第 116 轮我标了边界（信道日志把 A5/A6 的 dpi 都记成 300），本轮查 `python sim/channel.py --help` 确认它**本就有 `--dpi`**（默认取 manifest、否则 300），我没传 ⇒ 1200 dpi 的页被当 300 dpi 处理、位图被放大 2 倍，等效模糊只有真机的一半。**这是我自己的操作错误，不是工具缺陷。**

**② 补 `--dpi` 后的实测**：

| 阶梯页 | 信道 | 结果 |
|---|---|---|
| A5@600（`--dpi 600`） | `scan600` | 0.423 → **0** · 0.254 → **0** · **0.169（4 px）→ 1.31e-3** |
| A6@1200（`--dpi 1200`） | `scan600` | 0.127 → **0.9968** · 0.106 → **0.4991** · 0.085 → **0.4997**（整页塌陷） |

**③ 两条结论**：
- **撤回**：第 116 轮" A6@1200 三档 BER 0"**作废**（假绿，原因如上），`docs/PLAN-V5.md` §6 已就地更正并写明是撤回。
- **A6/1200 在信道里测不了**：`scan600` 是"600 dpi 扫描"的模型，用它去扫 1200 dpi 的印品是**分辨率不匹配的伪影**，不是物理结论 ⇒ 1200 dpi 那一列**只能靠用户用 1200 dpi 平板真扫**（或给信道加一个 1200 dpi 预设，未做）。
- **得到的真规律**：**拐点取决于"扫描 dpi 下每个模块几个像素"，而不是物理间距** —— 4 px/模块在 300 dpi（0.339 mm）与 600 dpi（0.169 mm）上都落到 **~1.3e-3**，5–6 px/模块则归零。这条比"0.34 mm 是极限"更本质，直接指导 P1 选档：**选档标准应该是"在你扫描仪的 dpi 下让模块 ≥5 px"**。

**④ 门限**：本轮只跑仿真与文档（未改代码）⇒ 沿用第 115 轮 `ALL GATES PASS` / docs clean。

### 第 116 轮（**密度尺第一次过"真信道替身"：A4@300 上 0.34 mm 是拐点（BER 1.26e-3，RS 仍吃得下）；A5@600 / A6@1200 全档 BER 0 —— 但信道 dpi 记错，那两行只作指示**）

**① 做了什么**：把三张阶梯页分别送进 `sim/channel.py`（种子 7、`--modifier nocrop`、模糊实测 0.067–0.070 mm），再用同一个读数器读回。

**② 实测（**仿真**，不是真机）**：

| 阶梯页 | 信道预设 | 条带结果（BER） |
|---|---|---|
| A4@300 | `scan300` | 0.847 → **0** · 0.508 → **0** · 0.423 → **0** · **0.339 → 1.262e-3** |
| A5@600 | `scan600` | 0.423 → 0 · 0.254 → 0 · 0.169 → 0 |
| A6@1200 | `scan600` | 0.127 → 0 · 0.106 → 0 · 0.085 → 0 |

**③ 结论（含一条必须记住的边界）**：
- **A4@300 的拐点在 0.34 mm 附近**：与"仿真信道在 0.26 mm 以下会被渗展填死"的旧结论一致（0.34 mm 刚好开始退化，但 **1.26e-3 仍在页内 RS 的预算内** —— RS(255,223) 能纠 ~6% 符号错，对应 ~8e-3 比特错 ⇒ 所谓"marginal"是**保守标签**，不是判死）。
- **同一物理间距，在 600 dpi 上多给像素就能活**：0.169 mm 在 300 dpi 只有 2 px（上一轮残差/本轮未测），在 600 dpi 是 4 px ⇒ **BER 0**。这正是"每个模块 ≥4 px"那条设计原则的实证。
- **边界（如实记）**：`sim/channel.py` 的日志里 **dpi 记的是 300.0**（A5@600 与 A6@1200 两次都是），而输出位图被放大到输入的两倍 ⇒ **A5/A6 那两行的等效物理模糊可能比真实扫描更小（偏乐观）**，只能当**指示**；A4@300 那行与信道预设同量级，可信度最高。下一步要么给信道传对 dpi、要么只采信 A4 那行。

**④ 门限**：本轮只跑读数（未改代码）⇒ 沿用上一轮 `ALL GATES PASS` / docs clean 的状态。

**⑤ 下一步**：① 用正确的 dpi 驱动信道（或改用"把 A6 页在 300 dpi 下渲染"的对照）重跑 A5/A6；② 等用户打印+扫描的真机三段输出，把 `docs/PLAN-V5.md` §6 的实测表填上。

### 第 115 轮（**验收包按 v5 重做：去掉板材/MTF，换成三张密度阶梯页 + 一条读数命令**）

**① 改动**（依据 `docs/PLAN-V5.md` §3、§7）：
- `tools/acceptance-kit.ps1`：**删掉 5 块板材码牌与 MTF 板**（3D 线已取消），换成 `density-a4-300` / `density-a5-600` / `density-a6-1200` 三张阶梯页（每张 `density-ladder.pdf` + `.json`，PDF 都是**真尺寸**：A4 210×297、A5 148×210、A6 105×148）。
- `tools/acceptance-readme.txt`：**第 2 步从"打码牌"改成"打密度阶梯"**（含每张该用哪个 dpi 扫、扫描件命名、以及唯一那条 `node tools/density-ladder.mjs --read ...` 命令），删掉 MTF 那整节与"拍近一点"的旧提示。

**② 实测**：`& .\tools\acceptance-kit.ps1 -Out .tmp\kit-v5` ⇒ **`KIT_EXIT=0`、7 条 PASS**（纸面 3 页 + 三个模块档 + **三张阶梯页**），包里**没有任何 3MF/STL/MTF**。

**③ 门限**：本轮只动 `tools/`（未动 core/web）⇒ `verify --gate all` ⇒ **`ALL GATES PASS`（6/7 evaluated）`VERIFY_EXIT=0`**（未评估 G4 G6 G9 G10，与每轮一致）；`check-docs-tables` ⇒ **clean（443 行 / 73 张表）**。

**④ 下一步（需要用户动手）**：把 `.tmp/kit-v5` 里的三张阶梯页打印（100%）、按表扫描、跑三条 `--read`、把输出发回 ⇒ 我拿到**第一份真机密度表**，填进 `docs/PLAN-V5.md` §6，再决定 P1 的默认档与 P2 的纸张目标。

### 第 114 轮（**密度尺改为"自己拥有版面"，三张阶梯页（A4@300 / A5@600 / A6@1200）全部自读 BER = 0.000000**）

**① 为什么必须自己拥有版面**：阶梯页不是协议页，不能借某个 profile 的晶格尺寸 —— 实测**每个 profile 的页面都是 ~191×279 mm，塞不进 A5（148×210）更塞不进 A6**，之前两次生成直接 `pageLayout: page 191.5x278.9mm does not fit ...` 失败。现在 `--make` 自建画布（按纸张 mm × dpi）、自画角标、自定条带矩形，`density-ladder.json` 里写全 `width/height/fiducials/bandRect/bands`，读数器只认 spec、不再 import `profiles`/`pageLayout`/`raster`。

**② 途中量到的两个真事实（都是"几何猜错"型）**：
- **角标必须照抄"已知能被读出的页面"的几何**：我先用了 48 px 方标 + 4 px 环宽 ⇒ **连一页除了角标什么都没有的图也报 `no-square-candidates`**（排除了"墨太密"这个假设：单条带、近乎空白页同样失败）。改成 `P-MX-300-4` 那份能读的页面用的 **2.71 mm 半宽 / 1.02 mm 环宽 / inset = half + 0.34 mm** ⇒ 立刻可读。另外给每个角标加了**白静区**（阶梯页墨占比实测 44%，角标直接泡在墨里时二值化没有稳定电平）。
- **同一物理间距必须在"模块 ≥4 px 的 dpi"上测**：这正是上一轮残差的正确解法（不是去修采样）。

**③ 阳性对照（三张纸，原始渲染直接读回，全部逐带 BER = 0.000000）**：

| 纸 | dpi | 条带（间距 → px） | 模块数 | 净 B/页 | bit/mm² |
|---|---|---|---|---|---|
| A4 | 300 | 0.847→10 / 0.508→6 / 0.423→5 / 0.339→4 | 18,778 / 52,716 / 75,735 / 118,818 | 1,878 / 5,272 / 7,574 / 11,882 | 1.4 / 3.9 / 5.6 / 8.7 |
| A5 | 600 | 0.423→10 / 0.254→6 / 0.169→4 | 47,576 / 132,334 / 297,942 | 4,758 / 13,233 / 29,794 | 5.6 / 15.5 / 34.9 |
| **A6（105×148）** | **1200** | 0.127→6 / 0.106→5 / **0.085→4** | 243,584 / 350,675 / **548,583** | 24,358 / 35,068 / **54,858** | 62.0 / 89.3 / **139.5** |

⇒ **A6@1200 dpi 上 4 px 模块（0.085 mm）的一条带就能装 ~54.9 KB 净载荷 / 139.5 bit/mm²**（对比今天的默认档 `P-M1-300`：1.4 bit/mm²）——**这是"纸越小、dpi 越高、密度越高"的量化起点**，但**全部仍是模拟/渲染数字，真机（打印机 + 扫描仪）一次都没测**。

**④ 门限**：仍只动 `tools/density-ladder.mjs`（未动 core）；下一步把三张页接进验收包、按 §3 清单跑齐门限，然后请用户打印 + 扫描。

### 第 113 轮（**密度尺改走"直接把模块中心映射回原图采样"；并量出残差**不是**重采样造成的 —— 因此决定改用"在高 dpi 上让每个模块 ≥4 px"的测法**）

**① 改了什么**：读数器不再"先 `rectifyPage` 重采样到画布、再在画布上采样"，改成**用角标单应把每个模块中心映射回原图、在原图上双线性采样**（`homographyFromQuad(canvasQuad(layout), found.quad)` + `canvasToPhoto` + `sampleBilinear`）。这条路径少一次插值，且与真扫描要走的路一致。

**② 实测（原始渲染，阳性对照）**：**结果与改前逐档相同** —— 0.847/0.508/0.423/**0.339 mm（4 px）四档 BER = 0.000000** ✓；0.254（3 px）**5.41e-4**、0.169（2 px）**2.4e-4–9.6e-4**。
⇒ **结论：残差不是"多一次重采样"造成的**（这条假设被本轮测量否掉 ✗，如实记）。排查过程也顺手修掉两个真 bug：`sampleBilinear` 返回的是 `{values, inside}` 而不是数组（我第一版当数组用 ⇒ 全档 BER 正好 0.500000，一眼看出是"采样恒为常数"的假象）；以及调试用的 `LADDER_DEBUG=1` 环境变量开关（保留，便于下轮定位）。

**③ 由测量导出的设计决定（下一步执行）**：既然残差与重采样无关、又只在 2–3 px 模块上出现，那就**不要在低 dpi 上用 2–3 px 模块去测细间距** —— 改为**在能表达的最高的 dpi 上让每个模块 ≥4 px**：
- 粗档：**A4 @ 300 dpi**（0.85 / 0.51 / 0.42 / 0.34 mm，4–10 px，已证明 BER=0）；
- 中档：**A5 @ 600 dpi**（0.25 / 0.17 mm = 6 / 4 px）；
- 细档：**A6 @ 1200 dpi**（0.13 / 0.10 / 0.085 mm = 6 / 4.7 / 4 px）。
A4@1200 dpi 会要 139 MP 的位图（RGBA ≈ 557 MB）⇒ **不可能整页渲染**，这正是"纸越小越能上高 dpi"的物理理由 —— 与用户"在更小的纸上装更多数据"的目标同向。
**门槛不变**：2–3 px 那几行在残差解释清楚前，不写进给用户的结论。

**④ 门限**：本轮只动一个工具（且已用阳性对照验证行为不变）⇒ 未跑全量门限；下一步做多纸阶梯后按 §3 清单跑齐。

### 第 112 轮（**密度尺加亚像素采样：≥4 px 模块的本底归零；2–3 px 仍有 <1e-3 残差，已定位为"局部（非全局）配准"问题**）

**① 改了什么**：`tools/density-ladder.mjs` 的模块灰度由"取整窗平均"改成**在模块中心做双线性插值**，并把对齐搜索从整数 ± 扩到 **整数 ±3 × 半像素 {0, 0.5}²**。理由写在代码注释里：2–3 px 模块上半像素的起始误差会让采样点落在模块边界上、自己把比特翻掉；窗平均更糟（它会把两种状态一起平均）。

**② 实测（原始渲染直接读回，阳性对照）**：
- 0.847 / 0.508 / 0.423 / **0.339 mm（4 px）** ⇒ **BER = 0.000000** ✓（4 px 这一档正是今天最好的 `P-MX-300-4`，尺子现在对它可信）。
- 0.254 mm（3 px）**5.41e-4**、0.169 mm（2 px）**2.4e-4 – 9.6e-4**（首版是 7.05e-4 / 1.43e-3，亚像素把最坏值降了 ~33%，但**没有归零**）。

**③ 结论（写进这一轮，供下一步用）**：残差**不是全局偏移**造成的（全局偏移已被 ±3×0.5 搜掉）⇒ 只能来自**局部**误差：`rectifyPage` 先把四角标单应重采样到画布、再在画布上采样，这多出的一次重采样在 2–3 px 模块上产生逐处不同的错位。
**下一步修法（已选定，未做）**：**跳过"先矫正再采样"** —— 直接用角标求得的单应把**每个模块的中心映射回原图坐标**、在原图坐标上双线性采样（少一次重采样，且对真扫描同样成立）。这也更接近真解码器的做法。
**门槛**：2–3 px 档在修好之前**不出现在给用户的结论里**（宁可留空，也不写一个可能是尺子噪声的数）。

**④ 门限**：本轮只动一个工具 ⇒ 未跑全量门限（单测/门限在上轮 377/377 与 clean 状态）；下一轮修完局部配准后按 §3 清单跑齐，并把真机数字填进 `docs/PLAN-V5.md` §6。

### 第 111 轮（**3D 线退场第一步：档位冻结 + 门限划线 + CLI/网页不再提供板材档**）

**① 做了什么**（依据 `docs/PLAN-V5.md` §3，产品负责人已取消 3D 板材线）：
- `core/profiles.js`：**`PL-M1 / PL-D2 / PL-D3 / PL-D3S / PL-G / REL-H1` 全部加 `retired: true`**（附注释说明"仍可解码、不再提供"）。实测：`retired: PL-M1 PL-D2 PL-D3 PL-D3S PL-G REL-H1`；**active 7 档全是纸面**（P-M1-300/600、P-M2-600、P-C4-600、P-MX-300-6/5/4）。
- `web/sender.js`：档位下拉框跳过 `p.retired`（用户不再看到板材档；显式传 `--profile` 仍可用）。
- `cli/pskit.mjs`：`--help` 的档位列表去掉板材档、写明"已退役但仍可解码"；`defaultProfileFor()` **不再把 `.stl/.3mf/.obj` 默认派到 `PL-D2`**（3D 线取消 ⇒ 一律纸面；模型就是字节，要上板必须显式传档）。
- `docs/ACCEPTANCE.md`：**G7 / G8 / G10 三个小节标题划线 + 退役说明**（写清产品负责人取消、不再作绿/红依据、历史判决原文保留不改写）——按台账纪律"撤回＝划线 + 标注"。

**② 验证**：全量单测 **`tests 377 · pass 377 · fail 0`、`SUITE_EXIT=0`**（退役没有破坏任何既有用例 —— 板材代码冻结未删，纯回归仍绿）；`check-docs-tables` **clean（439 行 / 72 张表）**；`node --check` 过 CLI 与 `web/sender.js`。

**③ 本轮**没有**做的（如实）**：① `verify --gate all` 目前仍会跑 G7/G8/G10 并计入总数 —— 把它们从 `all` 里摘掉、改打印一行 `retired: G7 G8 G10` 是下一步；② 验收包（`acceptance-kit.ps1`）仍在生成板材码牌与 MTF 板，尚未清理成"纸面 + 密度阶梯页"；③ 密度尺（`density-ladder`）3 px / 2 px 带的本底误码（4.7e-4–1.4e-3，原始渲染）仍未修。三件都排在下一步。

### 第 110 轮（**把批准的 PLAN v5 写成文档**：`docs/PLAN-V5.md` 落地，`docs/PLAN.md` 顶部加指针）

**① 为什么先做这个**（用户指示：「你先把 PLAN 写成文档，避免忘了，然后再继续」）：v5 只存在于会话里，一旦换会话就会丢。
本轮把它固化成契约文件，并让老的 v3 文件**指向它**（不改写 v3 正文 = 台账纪律）。

**② 落地内容**：新增 **`docs/PLAN-V5.md`**（137 行）：目标（面积密度 B/mm² / 纸尺寸阶梯 / 页数）、
起点事实（含"所有模块档只有模拟证据"）、**退役清单**（`PL-*`/`REL-H1` 冻结、G7/G8/G10 划线、MTF 板转用于纸面、验收包去板材项）、
**判据**（G-CAP / S-LADDER / G-IMG / G-TILE / G4′）、**阶段 P0–P5**、
**实测容量表（§6，只填实测、未测留空）**、用户动作（打印 1 张 + 扫 300/600/1200 dpi）、风险与不承诺。
`docs/PLAN.md` 顶部加 ⚠️ 指针：不一致处以 v5 为准、判决权威仍是 `ACCEPTANCE.md`。

**③ 门限与产物**：本轮**只写文档**（`docs/PLAN-V5.md` + `docs/PLAN.md` 指针 + 本块），代码零改动 ⇒ 不跑全量门限；
`node tools/check-docs-tables.mjs` ⇒ **clean（439 行 / 72 张表）**（写作过程中它抓到一处代码跨度里的裸竖线 `--sheet A4|Letter`，
已改成不含竖线的写法 —— 这条纪律第 N 次自己救了自己）。

**④ 下一步（继续，静默）**：① 3D 退场动作（`retired` 标记 + CLI/网页移除 + G7/G8/G10 划线 + 验收包清理）；
② 修密度尺的本底（3 px / 2 px 带在原始渲染上的 4.7e-4–1.4e-3 假误码）；
③ 然后才请用户打印 + 扫描，把 §6 的实测表填上。

### 第 109 轮（**PLAN v5 获批：取消 3D 板材线、目标改为"小纸高密度、图片优先"；P0 第一件——纸面密度阶梯（生成器 + 读数器）落地**）

**① 产品负责人的两条指示（本轮上下文）**：①「**取消 3D 打印扫描的计划书**」；② 目标改为「**在 A5 大小的纸上最小化看见的尺寸、最大化数据储存**（QR 可缩到 2R，现在从 A4 逐步推进）」，**先做图片**、文件后置，**执行时静默**（只在需要用户动手/严重错误/需拍板时出声）。计划书 v5 已提交并获批准。

**② 本轮落地（P0 第一步）**：新增 **`tools/density-ladder.mjs`** —— 一张纸量出"能印多细、能装多少"：

- `--make`：在 `P-MX-300-4` 的版面骨架上（**复用现有四角标 + 单应**）画**多条不同间距的已知伪随机比特带**（0.847 → 0.085 mm，默认 9 条），输出 `density-ladder.png`（整张纸，含裁切/套准标记）+ **真尺寸 `density-ladder.pdf`**（打印路径）+ `density-ladder.json`（规格：每条带的间距/像素/行列/种子）。间距低于该 dpi 能表达的最小像素时会**明说"两条带塌成同一间距，请提高 --dpi"**（实测：300 dpi 下 0.127/0.102/0.085 mm 全部塌成 2 px = 0.169 mm ⇒ 已用 `--dpi 600` 另生成一版）。
- `--read <扫描目录> --spec ...`：用现有 `findMarkers → rectifyPage` 登记，再逐带采样、**带内 ±4 px 对齐搜索**（与真解码器同思路），输出每条带的 **BER / 净 B per page / bit per mm² / 可用性**。
- **阳性对照（原始渲染直接读回）**：0.847 / 0.508 / 0.423 / 0.339 mm 四带 **BER = 0.000000** ✓（对齐搜索修掉了首版的系统性假误码）；300 dpi 两版阶梯页的四角标都被 `findMarkers` 找到（`markerPx 64`）✓。

**③ 如实记两处未完成**：① **3 px 与 2 px 模块在当前尺子上仍有 4.7e-4 – 1.4e-3 的本底误码**（原始渲染、未过任何信道）⇒ 这是**尺子自己的配准/重采样本底**，不是物理结论；修法 = 下轮把读数改成走真解码器那条 **timing 行校准 + 亚像素配准**，再据此定"BER 多远才算可用"（今天判据暂定 BER<5e-4=yes、<1e-2=marginal，**未写进任何门限**）。② 真机数字仍缺：**需要用户打印 1 张 + 扫描（300/600/1200 dpi）**，等尺子本底修好再请他印，避免让他白打一张。

**④ 门限与产物**：本轮**只新增一个工具**（不动 core、不动既有档位、不改任何判据）⇒ 未跑全量门限；下一步修完读数本底后按 §3 清单跑一遍并把 P0 的实测表写进 `docs/PLAN.md` v5。

**⑤ 未做的退役动作（下轮）**：`PL-*`/`REL-H1` 加 `retired` 标记并从 CLI/网页移除；G7/G8/G10 在 `docs/ACCEPTANCE.md` 划线保留 + 注明"产品负责人取消 3D 线"；验收包去掉板材/MTF 项、加入阶梯页。

### 第 108 轮（**JPEG CLI 转换助手落地：Windows 上一条命令转 PNG，核心仍不引入第三方 JPEG 解码器**）

**① 用户需求**：手机原图是 JPEG，但 CLI 只读 PNG/TIFF。网页端已用浏览器原生解码 JPEG；CLI 用户仍缺少零依赖转换步骤。

**② 改动**：新增 `tools/jpeg-to-png.ps1`，使用 Windows 自带 `System.Drawing` 把目录中的 `.jpg/.jpeg` 转成同名 PNG；CLI 的 JPEG 拒绝提示同步指向该工具。核心保持纯 ESM，不加入第三方 JPEG codec。

**③ 验证**：对用户 `D:\pskt-photos` 三个 JPEG 转换成功；不存在目录返回 exit 2；转换后 CLI 可进入 PNG 解码路径。照片本身仍可能因拍摄质量失败，格式层已解决。

### 第 107 轮（**网页接收端恢复原生 JPEG 解码路径：CLI 仍只读 PNG/TIFF，浏览器负责把 JPEG 转成 RGBA 位图**）

**① 用户实测**：照片文件是 `.jpeg` 时，网页接收端仍调用纯 JS `decodePNG`，所以输出“只支持 PNG”。CLI 也无法读 JPEG，用户被卡在格式层。

**② 改动**：`web/app.js` 对 JPEG 文件使用浏览器原生 `createImageBitmap` + `canvas.getImageData`，转换成与 `core` 相同的 RGBA bitmap 后继续走原有 `bootstrapDecode`。`web/index.html` 文件选择器补 `.jpg/.jpeg`。CLI 能力保持不变，不新增纯 JS JPEG 解码器。

**③ 验证**：`build-web` exit 0；`check-dist` **13 pass / 0 fail**、G9 全过。真实 JPEG 解码仍只能在用户浏览器验证；即使格式能读，前述照片数据区 `intra-fail` 仍不因此解除。

### 第 106 轮（**照片回显条 ±2px 对齐搜索：用户高分辨率照片的帧头已能读出，数据区仍被照片光度误码挡住**）

**① 用户实测**：4032×3024 照片的角标约 45–52px，但 `P-MX-300-6` 三页全部 `readout/echo-header-crc`。逐偏移复算发现三张照片的回显条相对角标单应都有约 `+2px` 横向位移。

**② 修法**：`core/decode/page.js` 新增 `readEchoAligned()`。首次读回显失败时，在 `x/y ∈ [-2,2]` 内尝试整数偏移；回显头仍由 magic/version/CRC16 裁决，成功路径零额外成本。数据区保持原 `module-read.js` 的 timing 对齐与局部自适应阈值。

**③ 结果**：用户三张照片的帧头现在可读，失败前进到 `assemble/intra-fail`。照片数据区仍有约 8–10% 模块误码，超过页内 RS 预算；这是手持照片的反光/光度动态范围问题，不是扫描仪路径。全量单测 **377/376 pass/1 skip**，`verify --gate all` 通过。

### 第 105 轮（**前端重写：现代深色主题 + 状态驱动的卡片化 UI；后端接口（`core/` 全部 API、所有 exported 纯函数、selftest 入口、单文件 PWA 形态）一字未动**）

**① 为什么改**：用户要求"重写前端，使其更现代化，也要保留后端接口和功能"。原前端是第 1–73 轮累积起来的内嵌式 + 平面化布局，控件挤成一片、状态靠 log 文本流推断。视觉与「离线工具、工程师用户」的口径不一致；功能（解码/编码/连拍/口令/分片/单色/STL/3MF/分片/拼回）一条没动。

**② 改动（仅 `web/` 5 个文件 + `web/dist/` 自动重建，**`core/`、`cli/`、`tools/`、`tests/`、`docs/` 一行未动**）**：
- **`web/app.css`** 全量重写：CSS 自定义属性驱动的设计系统（颜色/几何/字体/缓动四组变量）、自动跟随 `prefers-color-scheme` 的浅色主题、`prefers-reduced-motion` 关掉所有 transition；卡片 `data-state="idle|busy|ok|err|warn"` 五态可视化；状态点 `busy` 用脉冲；按钮三档（默认/primary/ghost）+ `.danger`；下拉控件自绘箭头；动效只用于状态过渡，不是装饰。
- **`web/index.html`** 重新组织：sticky 顶栏（产品名 + 一句话 + 跳发送端）、5 段卡片（文件/参数/跑/结果/日志）+ 条件渲染的 selftest 卡 + 手机连拍卡；每张卡有 `step` 编号 + 标题 + 状态 pill + 旁白 lede。
- **`web/send.html`** 同步：表单字段分组、按钮排成 `btn-row`、所有提示从纯文本升级为可读的 `b/hint/tag` 标签；保留 `autoRunGuardId`（`sfile`）的契约。
- **`web/app.js`** 重组 DOM 控制：状态由 `setState(cardId, state, pillId, text)` 统一管；日志走 `<span>` + `textContent`（保持"不渲染 markdown"的产品口径）；所有 `core/` API 调用（`decodePNG / bootstrapDecode / TransferAssembler / feedPageWithRecalibration / sha256Hex / PROFILE_IDS,PROFILES,profileOptionLabel / advise / downloadName`）和 DOM id 契约（`files/log/status/...` 73 个）一字不改 ⇒ `tools/check-dist.mjs` 第 10/11 项断言照过。
- **`web/sender.js`** 完整重写同时保留 `export { isUnqualifiedPaper, profileOptionLabel, pickPalette, buildArtifacts, previewPlan, printPlan, transferBudget, pageLimitHint, earlySizePlan, downloadPlan, isPlate, PREVIEW_CAP, PRINT_WINDOW_PAGE_CAP, DATA_URL_RISK_BYTES, PROTOCOL_PAGE_LIMIT }`（`tools/smoke-sender.mjs` 入口）和 `autoRunGuardId = 'sfile'`（`tools/build-web.mjs:220` 的剥离规则）。文件分两段：纯函数（`buildArtifacts`、所有 plan）放在 DOM 守卫前，Node 里可直接 `import`；DOM 控制放在 `if (typeof document !== 'undefined' && document.getElementById('sfile'))` 守卫内。每个 `give()` 路径都先调用 `downloadPlan()` 显式说风险。
- **`web/capture.js`** 完整重写同时保留 `export { createBurstCollector }` 与 `burst*` 系列 id 契约；动态 `import('./core/...')` 保留（dist 端重写后的拼法），`tools/build-web.mjs` 的 dynamic-import 守卫只对 `app.js` / `sender.js` 起作用（capture.js 是单独复制而非 bundle）。

**③ 验证（全部跑过、`core/` 一行未改 ⇒ M0–M7 门限状态未变）**：
- `node tools/build-web.mjs` ⇒ exit 0；`single-file pskt-file.html 371.9 KiB`、`pskt-send-file.html 383.9 KiB (no sibling links)`、precache 62 条。
- `node tools/check-dist.mjs` ⇒ **`13 pass / 0 fail`** + `G9 CHECK: all 13 assertions pass`、exit 0（含 `every getElementById in built JS exists in some built page · 73 markup ids` 与 `no built page declares the same id twice · every id unique within its own page, across 4 built page(s)`）。
- `node --test --test-isolation=none "tests/unit/**/*.test.mjs"` ⇒ **`tests 377 · pass 377 · fail 0`、exit 0**。
- `node tools/smoke-sender.mjs` ⇒ exit 0、71 s（web `buildArtifacts` 路径走完，与 CLI 共用 `core/`，`tools/smoke-sender.mjs` 的 decode-blind 回路验证页面 5 段 + 切分 + 拼回 + 加密 + 单色 + 板材 STL/3MF ⇒ 全部字节相等）。
- `node tools/smoke-capture.mjs` ⇒ exit 0、17/17 PASS（合成变形照片 → 3 页传输的合成器 → `createBurstCollector` 收 4 帧去重 1 帧拿 3 页，与输入摘要一致；`stats {"frames":4,"accepted":3,"duplicates":1,"rejected":0}`）。
- `& .\tools\usability.ps1` ⇒ 33/33 leg PASS（含 `tools/smoke-sender.mjs` 71 s、PSKT 验收包 `pskit send (paper P-M1-300, png + true-size pdf) 1s`、加密腿 `encrypted transfer came back identical once the key was given (6000 B)`、分片 `split -> 3 transfers -> join came back identical`、G8 文件级 `gate G8 --file on those .3mf 5s`）。外层 PowerShell 5.1 300 s 触顶是因为 `smoke-capture` 在 `usability.ps1` 阶段还没跑完（单跑 ~50 s 内完）—— 拆开跑都已验证。
- `node cli/pskit.mjs verify --gate all` ⇒ **未跑整包**（G2 600 dpi 单门限 ~3 h，超工具 300 s 上限）⇒ **未评估 G2 600 dpi / G4 / G6 / G10**。本轮**没动 `core/`**，门限判决一字不变 ⇒ **G0/G1/G3/G5/G7/G8/G9 仍是上一次 `verify --gate all` 打的 `✅/🟡`**，不主张"全过"；后续轮次里有人按 AGENTS.md §0 拉一次长门限时回到此口径。

**④ 没做的（避免误以为完成）**：
- 浏览器里点一次（`?selftest=1`、双击 `pskt-file.html`、手机/桌面 `index.html`）：G9 缺的最后一条**本机无浏览器**（沙箱拦 Chrome/Edge 命名管道，§4 记录）⇒ 留与上轮同样。
- 静态截图：CSS 改完没渲 PNG 给用户看。`tools/build-web.mjs` 出的 `web/dist` 是真实产品；按"假定"出截图就是另一份说辞，与本项目"误接受是唯一不可原谅的失败"的口径不合。
- `web/dist/` 改动：纯构建产物不进 git，CI 也会重建（已有 `.gitignore` + `web/.gitignore`）；本轮树与第 104 轮干净度的差异只在前 5 个源文件。

### 第 104 轮（**模块档 PDF 路径加回归测试：继续用裸 RGB 行、不带 predictor 参数**）

**① 为什么补**：验收包让用户打印的是模块档 `pack.pdf`。D78 的教训正是 PDF 行距参数歧义会剪坏整页；模块档是新的渲染分支，必须单独钉住它没有重新引入 `/DecodeParms`、`/Predictor` 或 `/Columns`。

**② 改动**：`tests/unit/module-matrix.test.mjs` 新增用例，对 `P-MX-300-6` 三页传输渲染后写 PDF，断言完整 `%%EOF`、无 predictor/columns 参数，且 PDF 页数等于传输页数。

**③ 验证**：该测试文件 **5/5 通过**；全量单测 **`tests 377 · pass 376 · fail 0 · skipped 1`、exit 0**；`verify --gate all` 打印 **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`、exit 0**。真平板扫描仍未执行。

### 第 103 轮（**1 MB 不可压模块模拟：三档页数与逐字节还原全部落地**）

**① 为什么补这一测**：100 KB 验证了多页组装，但还没有验证目标里最直接的 1 MB 密度数字。本轮用同一份不可压 1,048,576 B payload、固定 seed 7、`sim/channel.py --preset scan300 --modifier nocrop`。

**② 实测**：
- `P-MX-300-6`：**64 页**，逐字节还原；其中 4 页由页间校验重建。
- `P-MX-300-5`：**45 页**，逐字节还原；其中 2 页由页间校验重建。
- `P-MX-300-4`：**29 页**，逐字节还原；其中 1 页被拒绝后由页间校验重建。

**③ 结论**：1 MB 页数约 **64 / 45 / 29**，与容量表一致；大传输中的 RS 丢页恢复继续成立。真平板扫描仍未执行。

### 第 102 轮（**模块真机扫描一键检查器：`tools/check-module-scans.mjs` 落地**）

**① 为什么做**：验收包虽然给了三条 `receive` 命令，但用户扫描完三档后仍要逐个跑 CLI、逐个比对摘要，容易漏掉其中一档或把输出目录放错。

**② 改动**：新增 `tools/check-module-scans.mjs`。它从验收包读 `payload-module.bin` 自己重算 SHA-256，逐档读取 `module-6/5/4` 目录中的 PNG/TIFF，走同一套 `decodePage + feedPageWithRecalibration + TransferAssembler`，打印三档 PASS/FAIL，三档全部逐字节相同才 exit 0。

**③ 验证**：用第 100 轮生成的验收包原图运行 ⇒ `3/3 profiles byte-exact`、exit 0；把扫描根目录指向不存在路径 ⇒ `0/3`、exit 1。阳性对照成立。真平板扫描仍未执行。

### 第 101 轮（**模块档 100 KB 多页模拟传输：三档全部逐字节还原**）

**① 为什么补这一测**：第 99 轮的 16 seed 是 10,240 B 小载荷，每档只有 1 个数据页 + 2 个校验页。它证明了单页模块读出，但没有证明模块档在多数据页、页间 RS、连续扫描页序下的组装路径。

**② 实测**：同一份不可压 100,240 B payload，固定 seed 7，`sim/channel.py --preset scan300 --modifier nocrop`：
- `P-MX-300-6`：8 页，逐字节还原，SHA-256 匹配。
- `P-MX-300-5`：6 页，逐字节还原，SHA-256 匹配。
- `P-MX-300-4`：5 页，逐字节还原，SHA-256 匹配。

**③ 结论**：多页模块阵的 `profiles -> packLevels -> RS -> frame -> marker/homography/echo -> assemble` 主链在模拟平板扫描下继续成立。真平板扫描仍是唯一未完成的实物验收。

### 第 100 轮（**把模块档接进用户硬件验收包：`acceptance-kit.ps1` 现在生成 6/5/4 三份 pack.pdf 与真平板扫描命令**）

**① 为什么做**：第 99 轮的 `P-MX-300-4/5/6` 只有模拟信道证据。真平板扫描需要用户执行，但原验收包只生成默认 `P-M1-300` 纸面产物；用户要自己拼 `send --profile` 命令，容易漏掉一档或打印错误尺寸。

**② 改动**：`tools/acceptance-kit.ps1` 增加 `module-6/module-5/module-4` 目录，用同一个 20,000 B payload 分别生成 PNG + 真尺寸 `pack.pdf`；`tools/acceptance-readme.txt` 新增第 1b 步，写明 100% 打印、300 dpi 扫描、关闭自动裁剪，并给出三档 `receive` 与 SHA-256 对比命令。`docs/USE.md` §5 同步说明。

**③ 验证**：`& .\tools\acceptance-kit.ps1 -Out .tmp\acceptance-kit-module2` exit 0；三档均生成 3 页 PNG + `pack.pdf`；原有纸面、五块码牌、MTF 板全部生成；16 个 `.3mf` 全部通过 G8 子集；生成的 `README.txt` 三个占位符替换完成。真实平板扫描仍未执行，模块档维持「真机待验」。

### 第 99 轮（**新物理编码 `module`：纸面二进制实心模块阵，保留原有协议主链，P-MX-300-4/5/6 三档落地**）

**① 用户目标**：保留 `profiles -> packLevels -> RS -> frame -> marker/homography/echo -> assemble` 主链，只增加一种物理表示。纸面模块为实心方块或空白，1 module = 1 bit；timing/registration 行帮助定位；解码先单应校正，再做局部自适应二值化；低置信模块标擦除交给 RS；回显头使用独立大模块；先做 6px，再试 5px 与 4px。

**② 修法**：新增 `physicalEncoding: 'module'` 与三个纸面档 `P-MX-300-6/5/4`。剖面仍走原 `packLevels/RS/frame`；渲染在 `core/render/raster.js` 分支画满格/空格，`core/decode/module-read.js` 用固定 top/left timing 行校准黑白电平、±3px 配准搜索、局部 Otsu 阈值；边缘用 timing 校准阈值，低置信模块产出 `cellMissing`。回显头 8px（6/5 档）或 10px（4 档），不用数据模块的一半；4/5 档使用更粗的空心角环，避免高密度模块阵淹没朝向标。`bootstrap` 先固定所有用户提示命中的候选再按尺寸排序，防止新档把旧页的显式提示挤到第二次尝试。

**③ 验证**：
- 模拟扫描 `sim/channel.py --preset scan300 --modifier nocrop`：**P-MX-300-6 / 5 / 4 各自固定 seed 1..16 全部 `16/16` 逐字节还原**。
- `verify --gate G1 --seeds 3`：三档均 `0 misread`；全量 `verify --gate all` 打印 **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`、exit 0**。
- 单测新增 `tests/unit/module-matrix.test.mjs`：几何保留 timing 行、纯渲染读回 0 错、真页渲染→单应→RS 组装、PNG bootstrap。
- 旧档回归：`P-M1-300` 仍为默认，`conformance.json` 仅追加新 profile code；旧 profile code 不改，已打印页面不变。
- 全量单测 **`tests 376 · pass 375 · fail 0 · skipped 1`、exit 0**；`build-web` + `check-dist` **G9 CHECK: all 13 assertions pass**；`usability.ps1` **289 s、全腿 PASS、exit 0**。

**④ 密度对比（不可压 1MB）**：`P-M1-300` 约 169 页；新档分别约 **64 / 45 / 29 页**。网页标签标「新档·模拟16/16，真机待验」，不冒充真扫描证据。

### 第 98 轮（**用户指示「阅读交接文档，继续循环工作」⇒ 按 HANDOVER §12 进程内优先级第一项修 D83：空白白页不再被默认 INK2 色板误诊成「擦镜头」**）

**① 为什么做这一条**：第 97 轮登记的 D83 是唯一 OPEN 的代码缺陷。空白页不产出错误数据，但会把用户指向错误的补救动作；这正是 D69/D71/D76 同一族的最后一处已知诊断错误。

**② 修法**：`core/decode/fiducial.js` 新增 `inkRangeRatio()`。它从 `inkness` 的 **p1/p99 动态范围**与 **峰值到 p99 的尾部比例**判断图内是否基本均匀；两者都低于 `BLANK_INK_RANGE_RATIO = 0.02` 时，在阈值阶梯之前返回 `blank-image`。范围量不依赖底色，因此纯白页相对 INK2 纸色 `[246,242,234]` 整体偏亮时也不会再被当成大片墨点。峰值尾部条件同时避免把「只有少量稀疏墨块」的合法图误判为空白。

**③ 阳性对照**：`tests/unit/fiducial-corner-damage.test.mjs` 新增 D83 用例，纯白页带 INK2 底色必须判 `blank-image`；同文件原有 D69 用例继续证明平光/糊图仍判 `no-contrast`，原有正常页与损伤页用例继续证明可读路径未动。CLI 实跑两条：默认 INK2 与显式 PAPER1 都打印 `markers/blank-image`、exit 2、不写盘。

**④ 门限**：单测 **`tests 366 · pass 365 · fail 0 · skipped 1`、exit 0**（1 条 skip 是本机禁 spawn Python 的已知 PIL 对拍）；`verify --gate all` 打印 **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`**；`build-web` + `check-dist` 的 **G9 CHECK: all 13 assertions pass**；`smoke-sender` 与 `smoke-capture` 全绿；完整 `usability.ps1` **247 s、全腿 PASS、exit 0**。**总账不变**：✅5 · 🟡5 · ⬜1。

### 第 97 轮（**用户指示「暂停所有的动作，编写交接文档」⇒ 本轮只做交接：D83 登记为 OPEN、600 dpi 重跑中止、两处未完成的 CLI 改动按纪律还原；`docs/HANDOVER.md` 全量重写**）

**① 用户指示**：「暂停所有的动作，编写交接文档，详细的」⇒ 持久目标转为 **paused**；本轮**不再新增代码**。

**② 交接文档全量重写**（`docs/HANDOVER.md`，14 节 + 开头状态块）：新增/更新的关键内容 ——
- 开头写明本次交接的特殊状态（目标已暂停、后台无任务、树干净在 `d69d709`、**7 个提交未推送**）；
- §0/§1 现状与"卡在验收权"的诚实回答 + **恢复目标的技术动作**（`get_goal` 取 id/revision → `update_goal action=resume`）；
- §4 仓库地图补上 `core/decode/png-read.js`、`core/decode/tiff-read.js`、`tools/mtf-matrix.mjs`；
- §6 门限现状：**600 dpi 侧这次重跑的部分数据**（150/200 已处理 = 149 OK + 1 FAIL；页级失败分类 42 echo-bad-magic / 31 no-hollow-corner / 14 echo-no-contrast / 9 echo-header-crc / 7 intra-fail）——**明写这不是判决**；
- §7 **新增"⓿ 先重新导出产物"**（`scans/pack.pdf` 是 D78 之前的坏产物）；
- §9 已知债新增 **D83**、两处被还原的 CLI 改进、600 dpi 重跑被中止、7 个未推送提交；
- §10 环境陷阱新增：`Select-Object -First N` 会让上游 exit code 变 -1、`git push` 需要一次 `danger-full-access` 且提示可能超时、`edit` 前必须重读被改过的文件、`run_code` 生成代码时的转义陷阱；
- §14 本次会话（第 90–97 轮）的 7 个提交逐条 + 推送方法与可复验入口。

**③ 本轮发现的缺陷（登记为 OPEN，未修）**：**D83** —— 纯白/空白扫描图在**默认调色板 `INK2`**（底色是纸色 `[246,242,234]`）下被判成 `markers/no-square-candidates` 并给出"擦镜头"的建议，而它其实是空白页；同一张图加 `--palette PAPER1` 就正确报 `markers/blank-image`（阳性对照）。根因是 `blank-image` 判据依赖"相对底色的墨度"。

**④ 按纪律还原的两处未完成改动（已验证可用，但无腿/无门限 ⇒ 不留未提交工作）**：`send <目录>` 抛裸 `EISDIR` 的具名拒绝；`receive` 批量失败的分类汇总行。两处的改法与应配的 usability 腿都写进了 `docs/HANDOVER.md` §9 第 2 条。

**⑤ 后台任务**：第 94 轮启动的 600 dpi G2 重跑**按用户"暂停"指令中止**（日志 `.tmp/d49-600-r94.log` 保留）；**没有**在跑的任务。

**⑥ 门限**：本轮**只改文档**（`docs/HANDOVER.md`、`docs/DEFECTS.md`、本块），**代码零改动** ⇒ 不重跑门限；第 96 轮的门限状态仍然有效（单测 365/365、`verify --gate all` ALL GATES PASS、usability 231 s 全腿、`check-docs-tables` 干净 ⇒ 本轮复核 **343 行 / 60 张表**）。**总账不变**：✅5 · 🟡5 · ⬜1。

### 第 96 轮（**扫描仪的另一个默认输出 TIFF，我们只会说"读不了"⇒ D82 已闭：自写基线 TIFF 读取器，十种变体 + 多页文件全部逐字节还原**）

**① 为什么做这一条**：第 95 轮补上了 PNG 的各种位深/调色板，剩下的大洞是**容器**：`receive` 只收 `.png`，TIFF 被点名 `TIFF read-back is not wired` 并让用户去装 ImageMagick/PIL —— **在气隙环境里"先装个工具"不是答案**，而这个仓库连 PNG 编解码都是自己写的（`core/render/tiff.js` 一直写得出一份 TIFF，只是读不回来）。平板扫描仪"输出 TIFF"与"输出 PNG"同样常见，**多页 TIFF**（一次扫多页存一个文件）更是常态。

**② 做了什么**：新增 `core/decode/tiff-read.js`（**零依赖、自写 LZW 与 PackBits**，基线范围写进文件头注释）：

- 字节序 **II/MM** · **多 IFD ⇒ 多页** · 条带式 chunky 布局 · 光度 **0/1/2/3**（白零/黑零/RGB/调色板）· 位深 **1/4/8/16**（16 取高字节）· **1 或 3 samples** · 压缩 **1/5/8/32946/32773**（Adobe Deflate 是 **zlib 包裹**的，raw Deflate 是裸流 —— 这两种我第一版混了，是 PIL 造的真文件把它照出来的）· **FillOrder 1/2** · **Predictor 2**（8/16-bit）· orientation 1 · XResolution+ResolutionUnit ⇒ dpi。
- **其余一律具名抛错**：瓦片、分离平面、CMYK/YCbCr、浮点、JPEG-in-TIFF、其它朝向、缺 StripOffsets。
- CLI：`receive` 与 `calibrate` 都把 `.tif/.tiff` 与 PNG 并列，**多页文件展开成多页**（日志用 `文件 [page N]` 区分），剩余不可读格式仍具名（JPEG/WebP/PDF）。

**③ 验证**

- **真文件十种**（PIL 从我们自己的页造出，`tools/usability.ps1` **4h 腿**）：未压缩 / LZW / Adobe Deflate / PackBits / 8-bit 灰度 / 1-bit 二值 / 调色板 / 16-bit 灰度 + **3 页多页 TIFF** ⇒ **十个全部 exit 0、sha256 与载荷逐位相同**（本轮独立复核：10 个输出文件全部与 `enc-payload.bin` 摘要相等、多页日志出现 `pages.tif: 3 pages in one file`）。
- **单测 9 例**（`tests/unit/tiff-read.test.mjs`，自己按字节造 TIFF、不依赖第三方编码器）：II/MM · WhiteIsZero/BlackIsZero 语义相反 · FillOrder 2 · 4/16-bit 定标 · ColorMap 三块布局 · Predictor 2（8 与 16-bit）· **四种压缩解出同一批像素** · 第二个 IFD 即第二页 · 八条具名拒绝。
- **如实记我自己的一个错**：写 FillOrder 测试时我把 `BlackIsZero` 的语义写反了（按"1=黑"写预期）⇒ 测试当场红，是它把我纠回来的（规范里 1 是**白**、0 才是墨）。这正是"先写测试"的价值。
- 门限：单测 **`tests 365 · pass 365 · fail 0`、`SUITE_EXIT=0`**（本轮 +9）· `verify --gate all` ⇒ **见 ④** · `USABILITY_EXIT=0`、**231 s 全腿通过**（新增 4h 腿）· `check-docs-tables` 干净（337 行 / 58 张表）。

**④ 门限结果**：`verify --gate all` ⇒ **`ALL GATES PASS`（6/7 evaluated）`VERIFY_EXIT=0`**（未评估 G4 G6 G9 G10，与每轮一致）。

**⑤ 用户影响（如实）**：`docs/USE.md` 的"扫描格式"与验收包 README 第 1b 步都改成"**存成 PNG 或 TIFF 都行**"（PNG 的黑白/灰度/彩色/调色板/16-bit、TIFF 的未压缩/LZW/Deflate/PackBits、二值/灰度/调色板/RGB/16-bit、多页 TIFF 全部直接吃）；只有 JPEG 仍需先转 PNG 或改用浏览器接收端。**总账不变**：✅5 · 🟡5 · ⬜1。

**用户指示**：本轮是持久目标的自动续跑（用户上一条直接指示是「好了可以继续」）。

### 第 95 轮（**扫描仪的默认档"黑白 / 线稿"我们一直读不了 ⇒ D80 已闭；写这组测试时又抓出"dpi 从来没读出来过"⇒ D81 已闭**）

**① 怎么发现的**：沿着用户真正会走的路做了一次格式普查 —— 把同一页用 PIL 转成扫描仪实际会写出的几种 PNG，各跑一次 `receive --photo`。8-bit 灰度 **exit 0 逐字节还原**（阳性对照），而**1-bit（无抖动）、1-bit（抖动）、16 色调色板、16-bit 灰度四种全部 exit 2、不写盘**，用户看到的是 `not a readable PNG (decodePNG: bit depth 1 unsupported (need 8))` —— 工具内部的一句话，而且**被挡住的恰好是最干净的输入**（1-bit 扫描没有半色调，墨点边界比彩色扫描锐利）。

**② D80 修法**：`core/decode/png-read.js` 按规范支持 **colour type 0/2/3/4/6 × 各自允许的位深**（1/2/4/8/16）：行字节数 `ceil(width×depth×channels/8)`、滤波像素偏移 `bpp = max(1, ceil(depth×channels/8))`（**亚字节行上 bpp 必须是 1** —— 写错就把像素揉成另一张图）、亚字节 MSB 优先解包、低于 8 位的灰度按 `255/(2^depth−1)` 定标、16-bit 取高字节、`PLTE`+`tRNS` 展开（缺 PLTE / 下标越界 / 位深与 colour type 不匹配一律**具名抛错**，不猜）。仍拒绝隔行 PNG 与非 0 压缩/滤波方法。

**③ 修完的实测**：四种变体**全部 exit 0、sha256 与载荷逐位相同**（`bw1`/`bw1dither`/`pal8`/`gray16real`，`.tmp/d80/`）；新增单测 **`tests/unit/png-read-depths.test.mjs`（8 例）**—— 自己按字节造 PNG（不依赖第三方编码器，CI 也跑）：MSB 解包与 0/85/170/255 定标 · 调色板 + tRNS · 16-bit 高字节 · 灰+alpha · 亚字节行滤波偏移 · 五条坏头具名拒绝 · 以及"我们的写入器仍能被自己的读者读回"。

**④ 顺手抓到的 D81（写测试时才现形）**：`decodePNG` 的 pHYs 判断写成 `u32(phy, 8) === 1` —— pHYs 是"4 字节 X + 4 字节 Y + **1 字节单位**"，从第 8 字节读 4 个字节会越过后 3 字节 ⇒ 条件**永远不成立** ⇒ `dpi` 恒 `null`。后果不止少个字段：`core/decode/page.js` 的 `Math.abs((bitmap.dpi \|\| layout.dpi) - layout.dpi) < 0.5` 是"这张位图真的是标称画布吗"的**第二道守卫**，于是它退化成 `0 < 0.5` ⇒ **永远通过**（第一道"宽高相等"仍有效 ⇒ 没有因此误收过页）。修成 `phy.length >= 9 && phy[8] === 1`，方向**更保守**（dpi 不符时跳过快路、走标记几何路）；回归：`receive .tmp/sc-scan300-10 --photo --profile P-M1-300 --dpi 300` ⇒ exit 0、摘要相符（语料 pHYs 实测 `11811 11811 1` = 300 dpi ⇒ 守卫现在真的在比 300 对 300）。

**⑤ 门限与产物**：单测 **`tests 356 · pass 356 · fail 0`**（本轮 +8）· `verify --gate all` ⇒ **见 ⑥**（第一次跑红过一次：我在 DEFECTS 里写的 `\|\|` 在代码跨度里没被转义 ⇒ `defects-ledger.test.mjs` 的"四列"断言当场变红，`node tools/check-docs-tables.mjs --write` 转义后重跑绿 —— **这条纪律（D47/D48）又一次证明自己有用**）· `USABILITY_EXIT=0`、**223 s 全腿通过**（新增 **4g 腿**：四种扫描仪变体逐个 receive 并比摘要）· `check-docs-tables` 干净（334 行 / 57 张表）· `docs/USE.md` 的"扫描格式"那行改成"黑白/灰度/彩色/调色板/16-bit 都直接支持"。

**⑥ 门限结果**：重跑 ⇒ **`ALL GATES PASS`（6/7 evaluated）`VERIFY_EXIT=0`**。**总账不变**：✅5（G0 G1 G3 G5 G7）· 🟡5（G2 G6 G8 G9 G10）· ⬜1（G4）。**600 dpi 的 G2 重跑仍在后台跑**（第 94 轮启动，日志 `.tmp/d49-600-r94.log`）⇒ D49 的判决等它跑完。

**用户指示**：本轮是持久目标的自动续跑（用户上一条直接指示是「好了可以继续」）。

### 第 94 轮（**D51 的账目收口：修法第 63 轮就落地了，状态列头上却还写着"未修"；用 D51 自己点名的命令复验 —— 首读仍塌陷（设计如此）、整条传输被救回且逐字节相同；并启动 600 dpi 的 G2 重跑去取 D49 的最终判决**）

**① 先修账（这一轮最大的收获是一条过期记账）**：`docs/DEFECTS.md` 里 **D51 的状态列开头**仍写 `OPEN ✗（第 61 轮量到根因，未修）`，而**同一格**里第 63 轮的修法记录（①..⑦）与 **D49 那一行**都写着 `D51 = FIXED ✓` ⇒ 头上那句从第 63 轮起就是过期记账，接手人只看状态列会被带错方向。本轮把它改成 CLOSED，并把复验证据补在该格末尾（⑧）。

**② 复验（用 D51 自己点名的两条命令，判据一字未改）**

- `node tools/level-diff.mjs .tmp/g2src-a4-600 .tmp/sc-scan600-11 0,1` ⇒ **首次直读仍然塌陷**：page 1 `mismatch 256118/283155 = 90.452%`、`COLLAPSE`、混淆只有 `0->1`；page 0 `0.000%`（`LEVELDIFF_EXIT=0`，它只表示比对跑成了）。**这不是回归**：修法是"页内码拒绝后按**本页实测**切点重读一次"，首读那一步一行未改。
- `node cli/pskit.mjs receive .tmp/sc-scan600-11 --photo --profile P-M1-600 --dpi 600 --out .tmp/d51/got.bin` ⇒ page-001/002 各打印 `RESCUED -- shape levels re-decided against a cut measured on this page`（切点 1.4725 / 1.2862、改判 259796 / 250222 格）⇒ **`received 204800 bytes`、sha256 `91ab3f7310da452b…` 与 manifest 逐位相同、`RECEIVE_EXIT=0`**。
- ⇒ **D51 的用户可见后果（整页丢 ⇒ `assemble/intra-fail` ⇒ `short`）已不存在**，而**首读塌陷按设计保留**（它是救回的触发条件）⇒ 台账当时写的验收形态（"修好后 `level-diff` 必须接近 0"）**是写错的形态**，实际形态是"整条传输逐字节还原"（本轮实测）。常驻守卫：`tests/unit/recalibrate.test.mjs` 的两条（塌陷页被救回且逐字节相同 / ρ 无信息的页**不被**救回）。

**③ 600 dpi 的最终判决：本轮启动重跑**（后台：`verify --gate G2 --root .tmp --match 'sc-scan600-*'`，200 份语料齐备、日志 `.tmp/d49-600-r94.log`）—— 上一轮跑到 52/200 被用户中止 ⇒ **结果未出之前不改任何判决**；跑完按 `docs/ACCEPTANCE.md` 的口径如实记账。

**④ 门限与产物**：本轮**只改文档**（`docs/DEFECTS.md`、`docs/STATUS.md`），**代码一行未动** ⇒ 不重跑门限；上一轮（第 93 轮）的门限状态仍然有效：单测 **348/348**、`verify --gate all` **ALL GATES PASS exit 0**、`USABILITY_EXIT=0`（211 s 全腿）、`check-docs-tables` 干净。**总账不变**：✅5 · 🟡5 · ⬜1。

**用户指示**：本轮是持久目标的自动续跑（用户上一条直接指示是「好了可以继续」）。

### 第 93 轮（**"扫描成 PDF"这条最常见的路，被说成"没有页"⇒ D79 已闭；顺带去掉每条错误上重复的命令前缀**）

**① 根因**：`cli/pskit.mjs` 的输入过滤只收 `.png`，并把"读不了的格式"列进 `UNREADABLE`（D74 加的 `.tif/.jpg/.webp/...`）——**`pdf` 不在名单里**。而本 build **写得出一份 PDF（那是打印路径）却光栅化不了一份** ⇒ 目录里只有 `scan.pdf` 时落到兜底那句 `receive: no pages found in <dir>`：把"格式读不了"说成"没有页"，用户会去重扫、去查文件名。扫描仪"扫描成 PDF"是最常见的默认输出 ⇒ 这条会撞。

**② 修法**：① PDF 单独识别并给**它自己的**一句话 —— 写得出、读不了 ⇒ 给出导出路径（扫描软件选 PNG / 阅读器"导出为图片"、300 dpi、彩色、关自动裁剪），并提醒**别截屏**（截屏重采样墨点、会移动角标 —— D78 的教训）；② 混合目录的提示行同时报出 PDF 与图片格式的计数；③ 顶层 catch 只剥掉**一层**重复的命令前缀（`pskit receive: receive: found …` → `pskit receive: found …`），不逐条改消息 ⇒ 测试与工具仍按原措辞匹配。

**③ 验证**：usability 新增 **4f 腿**（`-Skip3D` 可跳）：`send --format pdf` 出来的真 PDF 独占一个目录 ⇒ `receive` **exit 1、不写盘、消息里同时出现 PDF/PNG/export、且没有 `receive: receive:`**；**阳性对照**：同一目录再放进同一次传输的 PNG 页 ⇒ **逐字节还原、exit 0**。`USABILITY_EXIT=0`、**211 s 全腿通过**（含 4d 的 JPEG 腿与 4e 的校验页腿，说明这条改动没有把 D74 的措辞改坏）。门限：`verify --gate all` ⇒ **`ALL GATES PASS`（6/7 evaluated）`VERIFY_EXIT=0`**；`check-docs-tables` 干净。

**④ 没做的事（如实）**：**没有实现 PDF 解码**（要自带 PDF 光栅化器，与"零依赖"冲突）——本轮的交付是"把话说对 + 给出可执行出路"。本轮改动在 `cli/`、`tools/`、`docs/`，不进 web bundle ⇒ 没跑 `build-web`/`check-dist`。

**用户指示**：本轮是持久目标的自动续跑（用户上一条直接指示是「好了可以继续」）。

### 第 92 轮（**G10 的进程内半边补完：用户拍回的四张照片 → 喷嘴矩阵读数（`tools/mtf-matrix.mjs`）**）

**① 为什么是这一条**：`docs/HANDOVER.md` §12 进程内优先级第 1 条。第 75/76 轮做出了 MTF 板与它的可打印模型（板带四角标记），但读者只能**一张一张**读，而 G10 要的是**四个喷嘴 × 参数**的矩阵 —— 用户拍回来的照片此前没有自动路径，只能我逐张手工跑再手工记账。本轮把它变成一条命令。

**② 做了什么**

- 新增 **`tools/mtf-matrix.mjs`**（纯 ESM、零依赖、不 spawn 子进程 ⇒ 受限沙箱里也跑得动）：
  - **标签三种来源**（优先级从高到低）：`--label 0.4=n04.png` · `<dir>/mtf-labels.json`（两种方向都认）· **文件名**（`n02` / `nozzle_0.6` / `ew070` / `0.45` …）。**认不出就不猜**：一个名字里出现两个喷嘴、或出现不存在的规格（`0.5`、`n13`）⇒ 该张记 `unlabelled`，绝不硬套一个喷嘴让行变绿。
  - **逐张判决**：`names-itself`（认出了打印它的喷嘴）· `allowed-coarser`（**只有 0.2 那一档允许**，且必须由读数说明"0.26 mm 孔被渗墨填了"）· `mismatch` · `unregistered`（四角标记没找到 = 数据不是工具错误）· `unreadable` · `unlabelled`。
  - **exit 0 / 1 / 2**（全对 / 有认错 / 工具没跑起来），并**每次必打一行 provenance**：`--provenance` 只把你说的记下来（像素分不出真假），没给就明写 `unstated -- ... docs/STATUS.md must not claim G10 without it` ⇒ **仿真件不可能被无声地当成真机件**。
  - **`--selftest`**：把板按四个喷嘴的挤出宽度各渲一次（`renderMtfPlate(..., {printEwMm})`，与 `tools/mtf-probe.ps1` 用的同一个"打印机"仿真），读回必须点名自己；另加两条对照 —— EW 1.40（比所有喷嘴都粗）**必须推荐不出任何喷嘴**、原始渲染**必须解出至少一档**（否则"总是点名自己"的读数器会靠正例蒙过去）。
- 新增单测 **`tests/unit/mtf-matrix.test.mjs`**（6 例）：标签解析（含 `0.5`/`n13`/两名并列 ⇒ `null`）· `--label` **必须能压过误导性文件名** · sidecar 双向 · 判决规则（含"0.2 允许更粗"的**前提**）· 表格与 exit code · `selftest()` 四正例两对照。
- **用户路径**：验收包 `README.txt` 第 3 步改写（**同一块板用四个喷嘴各打一次** —— 喷嘴是切片软件/打印机的设置、不是文件 + 命名 + 一条命令 + 三种判决的含义）、`docs/USE.md` §5 与命令清单同步；并**改掉 USE/README 里"让你拍近一点"那句**（D76 的教训对 MTF 板同样成立：角标在板四角，拍近就出画 ⇒ 连登记都做不到；正确动作是改用 300 dpi 扫描或如实接受"这一档读不出"）。

**③ 实测**

- `node tools/mtf-matrix.mjs --selftest` ⇒ 四行全 `names-itself`、`MTF MATRIX SELFTEST: pass (4 nozzles named themselves, 2 controls held)`、**exit 0**。
- 真文件路径（`.tmp/mtf-matrix/`：四张 `--print-ew` 仿真件 + 一份 spec）：**文件名标签 ⇒ 4/4 `names-itself`、exit 0**；**sidecar 标签 ⇒ 同上**；**故意错标** `--label 0.8=n04.png` ⇒ 该行 `mismatch`、**exit 1**；目录不存在 ⇒ **exit 2**；`--json` 写出 `tool/version/spec/dir/provenance/summary/rows/measurements`。
- 门限：单测 **`tests 348 · pass 348 · fail 0`、`SUITE_EXIT=0`**（本轮 +6 例）· `verify --gate all` ⇒ **`ALL GATES PASS`（6/7 evaluated）`VERIFY_EXIT=0`** · `check-docs-tables` **干净** · `acceptance-kit.ps1` **`KIT_EXIT=0`**（16 个 3MF 过 G8 子集、README 已含新步骤）。
- **门限总账不变**：✅5（G0 G1 G3 G5 G7）· 🟡5（G2 G6 G8 G9 G10）· ⬜1（G4）。**G10 没有被本轮变绿**：进程内半边就绪，真打印机仍未跑。

**④ 没做的事（如实）**：`--provenance real-print` 只是**记录用户的话**，仿真件永远不能当 G10 证据；判决要等用户按 README 打印四块板、拍四张照片。本轮改动只在 `tools/` 与 `docs/`（不进 web bundle）⇒ 没跑 `build-web`/`check-dist`。

**用户指示**：本轮是持久目标的自动续跑（用户上一条直接指示是「好了可以继续」）。

### 第 91 轮（**用户报的「PDF 斜」是真的，而且是我们自己写出去的参数：`/Columns` 让每个主流阅读器按 3 倍行距取图 ⇒ D78 已闭；PDF 图像流从此不带任何 predictor 参数**）

**① 根因（量出来的，不是猜的）**

- 我们的图像字典写的是 `/DecodeParms << /Predictor 15 /Colors 3 /Columns ${width * 3} >>`，而流里每行**只有 `width*3 = 6780` 字节**（实测：解压后 `22309490 = 3290 × 6781`，每行滤波类型字节全是 0）。阅读器**只**按 `rowBytes = Columns × Colors × BitsPerComponent / 8` 解释 `/Columns`（pdf.js `PredictorStream`、PDFium `CPDF_Predictor`、mupdf `fz_open_predict`、poppler `StreamPredictor`、Ghostscript `zpredict` 同一条）⇒ 它按 `20340` 字节取行 ⇒ **每 3 行漂移 2 字节 = 0.222 px/行（300 dpi）**，整页被剪成平行四边形，下半页的套准十字与角标被挤出纸边 ⇒ 印出来的纸页解不了。PNG 产物一直是好的（PNG 的 IHDR 没有这个歧义）⇒ 用户看到的「PDF 斜、PNG 直」正是这一条。
- **指纹对上了**：对 `scans/pack.pdf` 的 obj 4 按阅读器公式重排后，局部周期向量由 `(dy,dx)=(10,0)` 变成 `(10,2)`（子像素法：每 10 行右移 2.15 px、水平/垂直周期本身不变 ⇒ **是剪切不是旋转**），与用户那三张 `pack_pages-to-jpg-000{1,2,3}.png` 实测**逐位一致**。用户图上的三个硬伤因此同一个解释：底部两个角标出现在 `x≈794/873`（= 135 + 0.21×3238）、右下角标被推出纸面、左侧那条斜向墨区边界 = 每行 0.2 px 的漂移。
- 顺带排除掉两个假设（都是数字）：那三张图**不是扫描/拍照**（空白边距 `min=253..255`、无 JPEG 8×8 块指纹 `ratio 1.00`、249 个灰阶 ⇒ 干净的数字渲染），所以形变只能来自**渲染**而不是纸/相机。
- **第 90 轮那条「两个产物逐像素相同」没有错**（本轮复算仍是 `0/7435400`、最差通道差 0），但它量的是**我们自己写出去时用的行距**，等于拿自己的尺子量自己 ⇒ 不能据此推出「转换工具的锅」。那条结论本轮作废（round-90 块里已加更正指针，原文保留）。

**② 修法**：图像流改**裸 RGB 行**（`width*3` 字节/行，无每行滤波字节），字典里**不再写 `/DecodeParms`**（`/Predictor`、`/Columns` 一并消失）⇒ 行距只剩 `Width × Colors × bpc / 8` 这一个无歧义算法。**为什么不只是把 `/Columns` 改成像素宽**：两种读者约定（`Columns` = 像素宽 vs = 采样数）必有一方算错，只要留着这个参数就仍有读者会错；而每行滤波类型恒为 0 ⇒ predictor 一分钱不省，删掉最干净。

**③ 验证（三层，都有阳性对照）**

- **单测（新增 1 例，自带阳性对照）**：`PDF: the image stream carries no /Columns, and the legacy one is provably broken (D78)` —— 断言 `/DecodeParms|/Predictor|/Columns` **不存在**、流长度恰为 `height*width*3`、按阅读器公式解码后与输入像素**逐字节相等**；**并重建旧格式字节断言同一公式下第 1 行即错**（否则这条测试不会红）⇒ 全套 **`tests 342 · pass 342 · fail 0`、`SUITE_EXIT=0`**（此前 341）。
- **独立复算（不共用我们的代码，Python）**：修后 `FIXED writer … no /DecodeParms: rowBytes = Width*Colors*bpc/8 = 6780 ⇒ pixels differing from our PNG code area: 0/7435400`；旧产物 `OLD writer … /Columns 6780 ⇒ rowBytes 20340 (true row is 6780) ⇒ 5682462/7435400`（76%）。
- **产物**：重新 `send` 出来的 `.tmp/d78/pack.pdf` = **805662 B**（修前 820853 B，**小 15 KB**），三张 PNG 与修前**逐字节相同**（292843 / 302532 / 302243）⇒ 只动 PDF 这一条路径。
- **门限与构建**：`verify --gate all` ⇒ **`ALL GATES PASS`（6/7 evaluated）`VERIFY_EXIT=0`**（未评估 G4 G6 G9 G10，与每轮一致）· `build-web` `BUILD_EXIT=0`（61 文件、bundle 326.6 KiB / 30 模块、precache 59）· `check-dist` **`G9 CHECK: all 13 assertions pass`、`CHECKDIST_EXIT=0`**（**bundle 里已含修复**）· `check-docs-tables` ⇒ **323 行 / 54 张表干净**、`DOCS_EXIT=0`。**门限总账不变**：✅5（G0 G1 G3 G5 G7）· 🟡5（G2 G6 G8 G9 G10）· ⬜1（G4）。

**④ 对用户的影响（如实）**：`scans/pack.pdf` 与那三张 `pack_pages-to-jpg-000*.png` 都是**修前 writer 写出来的坏产物** ⇒ 请**重新导出**（`node cli/pskit.mjs send 你的文件 --profile P-M1-300 --format png,pdf --out 目录`），修后任一主流阅读器按 **100% 缩放**印即可；PNG 路径一直是对的。

**⑤ 部署状态（如实）**：修复已提交到本地 `2f36ec4`；`git push origin master` 在受限沙箱里被拒（MSYS 传输助手要建 signal pipe ⇒ `Win32 error 5`，与第 90 轮同因），按规则用一次 `danger-full-access` 重试，**授权提示在本次运行里没有等到答复（10 分钟超时）** ⇒ **线上站点仍是修前版本**（`web/dist/` 不入库，由 CI 从源码构建；本地 `build-web` 产出的 bundle 已含修复、grep 无 `DecodeParms|Predictor`）⇒ 推上去后 Pages 会自动重建。

**用户指示**：「好了可以继续」（上一轮末用户报了 PDF/PNG 不一致并要求修复或继续暂停目标）。

### 第 90 轮（**部署到 GitHub Pages（D43 https 半边闭合）；修掉一个我自己引入的崩溃（D77）；用户实测的 PDF/PNG 差异已用数字定位到「不是我们两个产物的差异」**）

**① 部署（已完成）**：仓库推到 `https://github.com/wangsan71/High-density-codes.git`；新增 `.github/workflows/pages.yml`；Pages 启用（`build_type=workflow`）⇒ 站点 **https://wangsan71.github.io/High-density-codes/** 上线，run `34375362063` **success**，`index.html` 与本地 `web/dist/index.html` **逐字节相同**（sha256 `1759ccb1…`）⇒ **D43 的 https 半边闭合**（浏览器里点一次安装仍归 G9）。过程中修掉两个「本地绿、CI 红」的问题：工作流 step 名里未加引号的 `": "` 让整个文件无效（跑 0 个 job）、单测依赖 pillow（现在缺则明确 skip、CI 里装上）。

**② D77（已闭）**：用户实测 `receive scans --photo` 最后一行是 `pskit receive: skippedTiff is not defined` —— 我第 83 轮拆「可读/不可读」两个列表时漏了一处引用，崩在「一页头都没读出来」这条诊断分支上。修好后同一目录 ⇒ `INCOMPLETE … (0 distinct page(s) read out of 3 image(s) offered)`、exit 2、不写文件。

**③ 用户报的「PDF 斜、PNG 直」——已用数字定位（不是我们两个产物的差异）**：

- **两个产物本身逐像素相同**：`send --format png,pdf` 后把 PDF 里嵌的图像流解开、反掉 PNG 行滤波，与 `page-000.png` 的码区**逐像素比对 ⇒ 0/7435400 不同、最差通道差 0**（偏移 ±1px 则 8 万多点不同 ⇒ 对齐是精确的，不是巧合）；PDF `/MediaBox` = **210×297mm**、`cm` 是纯缩放+平移（**无旋转**）。
- **我们自己的图经得起转换**：把 `page-000.png` 过 JPEG q95/85/75/60、以及「降到一半再最近邻放大」⇒ 四种都能 `receive` 成功（exit 0、逐字节还原）。
- **用户那三张图里有硬伤**（`scans/pack_pages-to-jpg-000{1,2,3}.png`，2481×3508）：第 1 张里**上面两个角标在 (141,135)/(2351,135) 正常**，但**下面两个角标整个不见了**（(135,3373)/(2345,3373) 处是空白），而在底部中间 (811,3375)/(861,3375) 多出**两个实心方块**；同时逐行扫描显示**左侧有一条从 (top,≈760) 到 (bottom,≈130) 的斜向墨区边界**（≈10° 的剪切感），而角标却仍水平对齐 —— **这三张图不是我们的产物的忠实渲染**（更像是某个 PDF→JPG 工具/扫描流程把页内容剪切了）。

**【第 91 轮更正 ✗】**：下面三条测量都成立（本轮复算「两个产物逐像素相同」仍是 `0/7435400`、最差通道差 0 ✓），但**由此得出的「不是我们两个产物的差异」是错的** —— 那次比对用的行距正是**我们写出去时用的那一个**（每行 `1 + width*3` 字节），等于拿自己的尺子量自己。按**阅读器**的公式（`rowBytes = Columns × Colors × BitsPerComponent / 8`）解同一个流，得到的是**被剪切成平行四边形**的页（`5682462/7435400` 像素不同 = 76%），而这正是用户看到的「斜」⇒ 真凶是 **D78**（`/Columns` 写成了 `width*3`），第 91 轮已修（PDF 图像流不再带任何 predictor 参数）。原文保留不改写。

**下一步（重启 DSH 后继续）**：把用户那三张图与我们的 `page-000.png` 做**逐行墨区边界对拍**（左/右/上/下四个方向各取几十行），确定是「旋转」「剪切」还是「平移+裁剪」，并据此判断**解码器该不该容忍这种形变**（若是扫描/打印常见形变 ⇒ 值得修；若是那个转换工具的锅 ⇒ 在手册里点名换工具）。**当前结论只到「我们的 PDF 与 PNG 一致」这一步，没有把责任推给用户**。


**用户指示**：「链接 `https://github.com/wangsan71/High-density-codes.git`，尝试部署」。

**做了什么**：

1. `git remote add origin …` + `git push -u origin master` ⇒ **推送成功**（`* [new branch] master -> master`）。
   **沙箱注意**：`git push` 的 HTTP 传输助手（`git-remote-https`，MSYS 程序）要创建 signal pipe，被受限沙箱拒（`Win32 error 5`）⇒ 必须用一次 `danger-full-access` 才跑得通（与 `spawn EPERM` 同源，已记进 `AGENTS.md` 的陷阱表习惯里）。
2. 新增 **`.github/workflows/pages.yml`**：`push` 到 `master` 时用**用户本地同一条命令**构建（`node tools/build-web.mjs`）→ `node tools/check-dist.mjs` 把关（零第三方 origin / CSP / SW 清单哈希）→ 单元套件 → `upload-pages-artifact` + `deploy-pages`。**零运行时依赖**（`dependencies` 仍是 `{}`，不需要 `npm install`）。
3. 用 GitHub API 确认状态：仓库 **public**、`default_branch=master`；首次 run **失败**（当时 `has_pages=false`）；用 `POST /repos/.../pages {build_type:workflow}` **启用 Pages** ⇒ `201`，站点 **https://wangsan71.github.io/High-density-codes/**、`https_enforced: true`。

**如实说明**：启用 Pages 之后的重跑/触发被 GitHub 拒（`rerun → 403`、`dispatch → 422`：凭据是 GCM 里存的那一个，作用域不足以写 Actions）⇒ 改为**再推一次提交**触发 `on: push`。

**部署过程中查到的两个真问题（都已修，都是「本地绿、CI 红」型）**：

1. **工作流文件本身无效** ⇒ GitHub 报 `Invalid workflow file: .github/workflows/pages.yml#L36`、**跑了 0 个 job**（`total_count: 0`，所以连日志都没有）。原因：某个 step 的 `name` 里写了**未加引号的 `": "`**（`no npm install: dependencies are {}`）——YAML 的 plain scalar 不允许 `: `。修法：给 step 名加引号（并在文件里留注释说明为什么必须加）。
2. **单测在 CI 上红：`verify_raster.py exited 2`** —— 那是 `ref/verify_raster.py` 在**没装 pillow** 时的退出码（脚本自己在 stderr 说明 `pip install pillow`）。本地有 pillow、CI 没有 ⇒ 这条「第三方对拍」在 CI 里必然失败。修法两件：① 测试在 `status === 2` 时**明确 skip**（"python 在、pillow 不在"，不是"我们和 pillow 不一致"）；② 工作流里 `python -m pip install --quiet pillow` ⇒ **CI 真的会跑这条对拍**（Linux + 另一版 pillow，比本地更强）。

**最终结论（run 34375362063，head `53c33dd`）**：**`success`**，build 9 步全绿（含 `check-dist` 与 **Linux 上的 341 例单测**）+ deploy 绿 ⇒ 站点 **https://wangsan71.github.io/High-density-codes/** 上线。**站点侧实测**：`index.html` 与本地 `web/dist/index.html` **逐字节相同**（sha256 `1759ccb13e8dddc8`）、`manifest.webmanifest`(761 B) / `sw.js`(8711 B) / `pskt-file.html`(342 KB) / `pskt-send-file.html`(369 KB) / `icon-192.png` 全部 **200** ⇒ **D43 的 https 半边闭合**（"浏览器里点一次安装"仍归 G9 的用户验收）。
### 第 89 轮（**页数上限的 hint 说了「用 split」，却没说「每片多大」⇒ 第二句仍然撞墙**）

**用户口径仍然有效**（「先用上，没必要过度设计」）⇒ 本轮只把第 84 轮那句 hint 补成**能直接照抄**的。

**问题**：`send` 撞到 255 页上限时，hint 让用户「用 `pskit split` 切开」—— 但 `split` 默认每片 1.4 MB（纸面档的尺度），而板材档一次传输只装得下约 **21 KB**（`PL-G@0.4`：192 数据页 × 110 B）。用户照 hint 切完再发，**第二次仍然撞同一面墙**。

**修法**：hint 现在算出**这个档一次传输能装多少**（从 253 个数据页往下找满足 `D + parity(D) ≤ 255` 的最大 D，再乘每页净字节），并打印一条可照抄的命令：

```
  hint: this transfer is 204800 B; PL-G carries ~110 B per page, so ONE transfer of this profile holds at most ~21120 B (uncompressed), while P-M1-300 (paper) carries ~7514 B per page -- about 28 page(s) for this file before compression.
  to send it anyway: node cli/pskit.mjs split <file> --max-bytes 21120 --out parts and send/print/scan each part as its own transfer (then join the received parts).
```

**实测**：`split --max-bytes 21120` ⇒ 10 片、exit 0；再 `send part-000.bin --profile PL-G --nozzle 0.4 --format 3mf` ⇒ exit 0（每片真的装得下）。
**同轮的门限复跑（重要：本轮之前改动过解码路径，G2 语料必须重跑）**：第 79–81 轮动过 `core/decode/fiducial.js`（`no-contrast`）、`core/decode/echo.js`（`echo-no-contrast`）、`core/decode/bootstrap.js`（一致原因提升）、`core/decode/advice.js`，而 `verify --gate all` **不评估 G2**（它要 `--corpus`）⇒ 本轮把盘上那份 200-seed 语料重跑了一遍：

`node cli/pskit.mjs verify --gate G2 --root .tmp --match 'sc-scan300-*'` ⇒ **`PASS G2 corpus: 200/200 byte-exact in 1088.7s`**、`exit 0`（门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200`）⇒ **三条新诊断没有把任何一页真扫描判死**（如果 `no-contrast` 误伤合法扫描页，这里立刻会掉）。此前最近一次 G2 判决是第 63 轮的同一档 `PASS 200/200`（1245.4 s）。
**并如实记下我自己第一版的错**：那段循环从 255 **往下按字节**试、255 B 当然立刻满足 ⇒ 打印出「最多 ~255 B」（荒谬数字）。改成按数据页数求最大 D 后得 ~21120 B ✓。断言加进 `usability.ps1` 的 1b 腿（hint 必须含 `--max-bytes \d+`）。
### 第 88 轮（**少了一页也能拿回文件，但收端不吭声 —— 现在会点名「第 0 页是从校验页重建的」**）

**用户口径仍然有效**（「先用上，没必要过度设计」）⇒ 本轮只补一句**输出**。

**观察**：用户很可能有一页没扫好/没拍好。实测两种情况：① 缺的是**校验页** ⇒ 数据页在，直接成功（正确）；② **数据页缺、只剩校验页** ⇒ 页间 RS 把数据页重建出来，**逐字节还原**（正确，这正是校验页的用途）。但 CLI 对第二种只打印 `received N bytes`，看起来像「印了 3 页、只收了 2 页，却还是成功了」—— 用户会以为收端悄悄跳过了什么。

**修法**：`receive` 在成功后点名被重建的页（`core/protocol.js:545` 早就在 `stats.recovered` 里记着这件事）：

```
  note: page 0 (page-000.png) rebuilt from the parity pages (that is what they are for) -- the images for those pages were missing or unreadable, so nothing needs reprinting unless the digest below fails
```

**实测**：只放校验页的目录 ⇒ exit 0、`received 204800 bytes`、摘要与 manifest 相同、上面那行点名 `page 0 (page-000.png)`。已入库为 `usability.ps1` 的 **4e 腿**（判据含 exit code、文件存在、逐字节摘要、以及那行必须点名 `page-000`）。**判据未动**。

**同轮顺手否掉一个用户会想到的招（负面结果，同样有价值）**：「把纸打大一点不就有更多像素/格了吗？」—— 实测**没用**：把同一页放大 2×（等效 200% 打印，manifest dpi 300→600）再过 `phone40` ⇒ 仍然 `markers/no-contrast`。原因是手机必须**框住整页**，格子占的像素数只取决于**页上有多少格**、与纸多大无关（放大后整页也同比变大，占的帧比例不变）。⇒ 要更多像素/格只能**减少格数**（换粗档）。已写进 `docs/USE.md` §2 的手机那一段。
### 第 87 轮（**「拍近一点」是个把人指向更坏结果的建议 —— D76 已闭**）

**用户口径仍然有效**（「先用上，没必要过度设计」）⇒ 本轮只改**用户会照做的一句话**。

**问题**：第 80 轮我在 `USE.md` §2/§6 与 `advice.js` 里写了「手机拍纸面页读不出？**拍近到码区填满画面**」。这是错的：**四个角标在纸的四角**，拍近就会把它们挤出画面 ⇒ 页面**完全定位不了**（单应需要四个点对），比「整页入画、格子糊」还糟 —— 而后者至少能给出 `echo-no-contrast` 这种点名「太远」的诊断。

**修法**：三处文案统一改成「**四个角标必须都在画面里**（也就是整页入画）；拍近到角标出画就彻底定位不了」，并把「拍近」从纸面手机路径的出路里删掉（只剩两条：换粗档 `PL-G` / 用扫描仪 300 dpi）。

**证据**：`node tools/smoke-capture.mjs` ⇒ `PASS a cropped page is not accepted as a page`（裁到只剩码区 ⇒ `no-rectangular-quad`）；`tests/unit/warp.test.mjs` 的切边实验（左边切 16mm ⇒ 正确拒绝）。**判据未动**。

**同一轮**：验收包 README 的第 2b 步补上同一句话（「**必须把整块板含四角都拍进画面**：角标在板的四角，拍近到角标出画就完全定位不了；宁可让板在画面里小一点」）—— 用户做硬件验收时读的正是这份 README。实测：`& .\tools\acceptance-kit.ps1 -Out .tmp/kit-check` ⇒ 8 PASS、16 个 3MF 过 G8 子集，README 里能搜到这句话。
### 第 86 轮（**`send` 结束时把「下一步」直接打出来**：手册之外不用再翻文档）

**用户口径仍然有效**（「先用上，没必要过度设计」）⇒ 本轮只补一句**输出**，不加功能。

**观察到的问题**：`pskit send` 的结尾停在 `wrote N file(s) + manifest.json`，用户接下来该敲什么得回手册找 —— 而接收端的 `--profile` 是**必须**的（新扫的目录里没有 `manifest.json`），这一点最容易漏。

**修法**：`send` 收尾打印两条可复制的命令，参数取自本次传输自己（profile / nozzle / plate / 是否加密）：

```
  next       print <out>\pack.pdf at 100%, scan the pages to PNG, then:
             node cli/pskit.mjs receive <scan or photo dir> --photo --profile P-M1-300 --out <file>
```

板材档则打印 `slice <out>\page-000.3mf …` + 带 `--nozzle 0.4` 的 receive 命令。**实测**：纸面与板材两条都实测过（`--profile P-M1-300` / `--profile PL-G --nozzle 0.4`）；断言入库为 `usability.ps1` 的 1 号腿附加检查（日志必须含 `next` + `pskit.mjs receive` + 本次 profile）。**判据未动**。

**同一主题的第二处（同轮）**：`receive`/`calibrate` 在「没有 manifest.json 也没给 `--profile`」时只说「无法知道页面几何」，现在点名**该给什么**（纸面 `--profile P-M1-300`；板材 `--profile PL-G --nozzle 0.4 --plate 200`）并提示**浏览器接收端会自动搜候选几何**（它本来就会）。实测：`receive .tmp/nomanifest --photo` ⇒ exit 1 + 上述整句。
### 第 85 轮（**D8 量到一半答案：缩放本身不致命，「必须 100%」这句手册话可以放宽**）

**用户口径仍然有效**（「先用上，没必要过度设计」）⇒ 本轮只回答一个用户第一天就会问的问题：**打印时选错缩放会怎样？**

**实测（新增 `tests/unit/warp.test.mjs` 用例，进程内）**：把整张纸按 **95%、110%、95%×100%（非均匀）** 重采样后走真照片路径 ⇒ **三种缩放全部逐格零错（0/68904）、逐字节还原**。原因：几何不是从"每毫米多少像素"来的，而是从**四个角标的四个点对**反算的 ⇒ 仿射缩放被精确抵消。

**真正致命的是另一件事**：把左边切掉 16mm（角标所在位置）⇒ **正确拒绝**（单应需要四个点对，三点解不出）。⇒ 手册里「必须 100%、关掉适应页面」改成实测口径：**被缩放别慌，先试着扫**；如果打印机的不可打印边距切掉了角标，反而应当选「适应页面」把角标缩进可打印区。D8 的原始问题（印出来尺寸是否等于标称）仍是物理项，留给尺子。
### 第 84 轮（**用户的第一条命令就是坑：不带 `--profile` 的 `send` 默认走板材档 ⇒ D75 已闭**）

**用户口径仍然有效**（「先用上，没必要过度设计」）⇒ 本轮只修**第一次使用就会撞上**的那一个坑，不加功能。

**实测到的坑**：`node cli/pskit.mjs send 任意文件 --format png`（不写 `--profile`，也就是手册之外最自然的写法）⇒ **`needs 1120 pages > 255 (inter-page RS limit): shrink payload or use a denser profile`**。原因：`defaultProfileFor(ext)` 收着扩展名却从不看它，对所有输入返回 `PL-D2`（板材档，~220 B/页），而手册教的是 `P-M1-300`（~7514 B/页）——**两个数量级**。拒绝本身是对的（一次传输最多 255 页），但那句话没告诉用户该换成哪个档。

**修法（D75）**：① 默认档按输入类型选：`.stl/.3mf/.obj` → `PL-D2`（发模型给板材是用户本意），其余 → `P-M1-300`；② 撞到页数上限时 CLI 补一行**可执行**的 hint（本次多大、两个档各自每页多少字节、纸面档约需多少页、或 `pskit split` 切分）。

**实测**：不带 `--profile` ⇒ `profile P-M1-300`、3 页、exit 0；显式 `--profile PL-D2` 喂同样载荷 ⇒ exit 2 + `hint: … P-M1-300 (paper) carries ~7514 B per page … about 28 page(s) … Or cut the file into parts with pskit split`。两条都已入库为 `usability.ps1` 的 **1b 腿**（前者是正向契约，后者是带点名的拒绝）。
**同轮收尾的小事（`status` 也要能选档）**：`node cli/pskit.mjs status` 的容量表给 `PL-G` 的每一行加上 **`[手机拍摄首选]`**，并在表尾加一句「手机相机需要粗档：PL-G（3.12–7.6mm 格）整页取景可读，纸面（0.85mm 格）不行」。理由：CLI 用户就是在**这张表**上选档，而「手机拍摄首选」的实测结论（第 80 轮 8/8 vs 0/8）此前只出现在网页下拉框里。**判据未动**。

### 第 83 轮（**手机拍的 JPEG 拷到电脑上，`receive` 说「没找到页」——D74 已闭；用户指示「先用上、别过度设计」⇒ 本轮收尾**）

**用户口径（本轮直接指令）**：「其实我想先用上，没必要做过度设计」⇒ 本轮只做**挡在「用上」路上的那一件事**，然后把最短可用路径写清楚，不再往下深挖。

**撞到的路障**：手机相机默认写 JPEG、平板扫描仪默认写 TIFF，而 `receive` 的输入过滤只收 `.png`（`.tif/.tiff` 收进来再静默跳过），于是「拍完拷到电脑上跑 receive」这条最自然的路径得到的是一句 **`receive: no pages found`** —— 用户会去查目录、查权限，而真正的原因是这个格式没接线。

**修法（D74）**：`receive` 改成**按能力分类输入**：`.png` 解码；已知不可读的扩展名（tiff/jpeg/webp/gif/bmp/heic）**点名统计**、说明「只解码 PNG」并给出两条出路（ImageMagick/PIL 转换命令，或改用浏览器接收端——它自己解 JPEG）；**全不可读时以具名错误退出**（不再说「没找到页」），且**一个字节都不写**。`calibrate` 同步。`docs/USE.md` §3 与验收包 README 各补一句「扫描存成 PNG」。

**实测**：`node cli/pskit.mjs receive <JPEG-only dir> …` ⇒ 修前 `no pages found`，修后点名 `.jpg` + 转换命令，exit 1、未写文件；已入库为 `usability.ps1` 的 **4d 腿**（含阳性对照：同目录放回 PNG 必须照常逐字节还原）。
### 第 82 轮（**接收端也有档位下拉框，却既没有 D49 警告也没有手机提示；顺手修掉 `#outname` 监听器泄漏 —— D73 已闭**）

**查法**：把「同一件事在两处各写一遍」当线索去找。发送页的档位标签走 `profileOptionLabel()`（带 D49 警告 + 第 80 轮加的「手机拍摄首选」），而**接收页 `web/app.js` 是手搓的**（`${id} (${medium} ${dpi}dpi)`）⇒ 用户在接收端选档时，**两条信息都没有**。同一轮核到台账里记着的 `#outname` 监听器泄漏（HANDOVER §9 记为「属泄漏、不属错误」），一并修掉。

**修法（D73）**：

1. 标签策略搬进 **`core/profiles.js`**（`isUnqualifiedPaper` + `profileOptionLabel`）—— 两页共用，`web/sender.js` 重新导出以保持自己的对外面；接收页改用它。
2. `web/app.js` 的 `#outname` 监听器**提到模块加载时注册一次**，当前传输的命名输入放进 `currentNaming`（原来是每次 `run()` 里 `addEventListener` 一次，每次闭包住那一整份装配好的载荷）。

**实测**：`node --test --test-isolation=none "tests/unit/profile-picker-warning.test.mjs"` ⇒ 6/6 通过，其中两条新断言：`input` 监听器全文件**恰好一个**且**不在 `run()` 体内**（反例：同一段切片仍能找得到 `run` 作用域里的 click 监听器 ⇒ 不是空跑）；两页都走 `profileOptionLabel`、`未达标` 字样在 `web/` 出现 **0** 次（只在 `core/profiles.js` 出现 1 次）。`build-web` + `check-dist`（13 断言）exit 0、`smoke-sender` 全绿。

**门限**：单测 **340/340 exit 0**、`verify --gate all` ⇒ **ALL GATES PASS**（6/7）、`usability` exit 0、台账 clean。判据未动。
### 第 81 轮（**诊断要能到得了手机：D69/D71 那两条话术，之前一个字都到不了 —— D72 已修**）

**这轮补的是前两轮的「最后一公里」**。第 79/80 轮修的两条诊断（`no-contrast`、`echo-no-contrast`）是**写给手机用户**的，但它们在手机那条路上**到不了用户**：

- `web/capture.js` 的 DOM 半面对 `kind === 'no-page'` 只打一句「画面里没有本工具的页（或太糊/太暗）」——**把 reason 整个丢掉**；
- `core/decode/bootstrap.js` 在所有候选都失败后统一报 `no-geometry-matched`——**具体成因被包装名吃掉**。
实测：一张平/糊的帧让 24 个候选**全部**以 `no-contrast` 失败，用户看到的却是「所有候选几何都读不出这一页」。

**修法（D72，已闭）**：

1. `core/decode/bootstrap.js`：所有候选失败且**原因一致**时，把这个原因提升为页面级 `reason`（`wrappedReason` 保留包装名）；原因不一致仍报 `no-geometry-matched`（此时没有单一成因被确立）。
2. `web/capture.js`：收集器给失败帧带上 `advise()` 结果；DOM 半面优先显示中文一句话（没有中文时退回英文 cause→do）。
3. `core/decode/advice.js`：新增 `ZH` 表（16 条手机路径会遇到的 reason）与 `localizedReasons()`；**中英同表** ⇒ 不会再出现「core 知道、三个 UI 各说各话」那种漂移（D66 的教训）。

**实测**：`node tools/smoke-capture.mjs` ⇒ 修前那张平帧报 `kind no-page · reason no-geometry-matched`，修后 **`reason no-contrast`**，且 `advice.zh` 是「照片没有墨/纸对比度（过曝、反光，或离得太远把格子拍糊了）：降曝光、关闪光、避开反光，再靠近一点重拍 —— **重新取景没用**。」；**阳性对照**：好帧必须被接受且**不带** advice（否则这条断言只是在测「每帧都有 advice」）。另一条腿：裁切的页从 `no-geometry-matched` 变成 **`no-rectangular-quad`**（更具体、且指向正确的补救）。

**门限**：单测 **339/339 exit 0**（含本轮新增的 `ZH` 表守卫）、`verify --gate all` ⇒ **ALL GATES PASS**（6/7）、`usability` exit 0（含 smoke-capture 的新断言）、`check-docs-tables` clean。**判据未动**：没有任何帧因此被接受。
**同一轮顺手做的两件小事（都已入库）**：

1. `core/decode/advice.js` 补 `digest-mismatch` 的正式条目（它一直只作为 `fed.reason` 直接打印、没有 advice；第 81 轮把它接进手机 UI 后暴露出来），并新增单测：**`ZH` 表的每个键都必须是真 reason**（改名后不会留下悬空翻译）、手机路径那 10 个 reason 必须都有中文、未映射的 reason **不得**借用翻译。
2. **第二条正面证据**：`& .\tools\g4-probe.ps1 -Seeds 8 -Preset plate-matte`（FDM 板材照片档）⇒ 纸面 `P-M1-300` **0/8**、**板材 `PL-G@0.4` 8/8 = 100%**（`crop` 生效时 2/8）。加上第 80 轮的 `phone40`（8/8），**粗档板材在两个互不相同的信道预设下都是 100%** —— 这是目前对「手机/相机拍板材」最强的进程内证据（仍是仿真信道 ⇒ G10/G4 判决不变）。

### 第 80 轮（**同一张照片的第二层误诊：条带没对比度，被说成「头字段损坏/另一套码」⇒ D71 已修**）

**接第 79 轮的线**：那一轮修的是「照片整体没有墨/纸分离」被说成「四角没拍全」（D69）。这一轮查的是**下一层**：`phone40`（较温和的手机档）下角标**找得到**（6.3px）、校正 100%，但页面在 `readEcho` 处失败，报 `echo-bad-magic`，而 `advice.js` 对它的解释是「条带损坏，或这是另一套码」——**字节层面是真的，成因层面是错的**：真实原因是**拍得太远**，条带的 1-bit 微格（数据格的一半宽，是页上最细的特征）糊成一条灰带。用户照这条建议会去**重印**，而正确动作是**拍近/换粗档**。

**实测（同一张 P-M1-300 页，条带两电平的分离度 `(p75−p25)/max`）**：原始渲染 **1.000** · 300 dpi 扫描 **0.790** · 3× 降采样 **0.705** · 4× 降采样 **0.530** · **phone40 只有 0.148**。可读与不可读之间有一整个数量级的空隙，切点 **0.35** 落在里面。

**修法（D71，已闭）**：`core/decode/echo.js` 新增 `ECHO_SEPARATION_FLOOR = 0.35`；`readEcho` 在**所有阈值候选都失败之后**，若分离度低于它，改报 `no-contrast`（页面层自动成为 `echo-no-contrast`）；`advice.js` 给出「拍近/2× 变焦/顶边对焦/换粗档/改用 300 dpi 扫描」。**判据一字未动**：没有任何页面因此被接受。

**实测（端到端）**：`node tools/g2-corpus.mjs .tmp/p40-trial` ⇒ 修前 `readout/echo-bad-magic`，修后 **`readout/echo-no-contrast`**（三页全部）。
**同一轮的第二组实测（`phone40`，即「整页在画面里、光线正常」的现实手机档）—— 第一次拿到正面的 G4 证据**：

| 腿 | 取景 | 结果 |
|---|---|---|
| 纸面 `P-M1-300` | `nocrop` | **0/8**（失败类：`echo-no-contrast` ×17、`no-contrast` ×7 —— **正是本轮与上轮新加的两条诊断**）|
| 纸面 `P-M1-300` | `crop` | **0/8**（同上）|
| **板材 `PL-G@0.4`** | `nocrop` | **8/8 = 100%（逐字节还原、摘要核对）** |
| 板材 `PL-G@0.4` | `crop` | **6/8 = 75%** |

⇒ **产品结论（第一次由实测给出，而不是推断）**：手机拍整页时，**纸面档读不出来、粗档板材读得出来** —— 3.6mm 的格子在一张 1600×1200 的手机照片里还有 ~15px，而 0.85mm 的纸面格子只剩 ~2.7px。失败时两条新诊断会**分别**指向正确动作（`no-contrast` = 曝光/反光/太远；`echo-no-contrast` = 拍近/换粗档），不再让用户去重印。**仍然是仿真信道，不是真手机** ⇒ G4 判决不变。
**同轮第二件事：把「手机该用哪一档」做进产品，而不是只写在台账里（`core/profiles.js` + 发送页 + 手册）**：

- `core/profiles.js` 给 `PL-G` 加 `phoneSafe: true`（PLAN §2/§3 本来就称它是「任何喷嘴/手机必可读」的保底档，第 80 轮的 `phone40` 实测 8/8 是它的证据）；
- `web/sender.js` 的 `profileOptionLabel()` 给带这个标记的档追加 **` · 手机拍摄首选`**（**只有它**，单测钉住：恰好一个、其余一律不带 —— 否则提示会变成噪音）；
- `docs/USE.md` §2 开头补一段实测结论（手机用 `PL-G` 或拍近；纸面档的正确用法是扫描仪 300 dpi）。

⇒ 这是**把测量变成默认选择**：用户不需要读台账，选档那一刻就看见「这个能手机拍」。

**本轮门限复跑（第 80 轮第二段；`core/profiles.js` + `web/sender.js` 被改过）**：

- 单测：**`tests 338 · pass 338 · fail 0 · duration_ms 151883`、exit 0**（+1 例：只有 `PL-G` 带手机提示、其余一律不带）。
- 进程内门限：`verify --gate all` ⇒ **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`**。
- 产物级：`build-web` exit 0、`check-dist` exit 0（含 13 断言与 id 契约）。
- 台账：`check-docs-tables` ⇒ `clean -- 313 rows in 54 tables`、exit 0。


**阳性对照**（`tests/unit/warp.test.mjs` 新增 1 例）：同一页锐利 ⇒ 仍能读；轻微模糊（分离度 0.546）⇒ **不得**被判 `no-contrast`（否则这条诊断会吞掉真正的「条带受损」）。

**本轮门限复跑（第 80 轮，全部实测；`core/decode/echo.js` 被改过，所以全套重跑）**：

- 单测：**`tests 337 · pass 337 · fail 0 · duration_ms 148296`、exit 0**（+1 例：条带锐利仍能读、轻微模糊不得被判 `no-contrast`）。
- 进程内门限：`verify --gate all` ⇒ **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`**；**G5 的守卫分布逐项未变**（`feed:header:bad-magic=84`、`header-crc=1324`、`accepted-original=9200`、`refused:digest-mismatch=800`、**0 误接受**）⇒ 新诊断没有碰任何接受条件 ✓（合成篡改语料是原始渲染，条带分离度 1.000，本来就不该触发）。
- 端到端冒烟：`& .\tools\usability.ps1` ⇒ **exit 0**（纸面/板材/加密三条真信道腿都过 `readEcho`）。
- 真信道探针：`& .\tools\mtf-probe.ps1` ⇒ **0 FAIL / 42s**。
- 台账：`check-docs-tables` ⇒ `clean -- 308 rows in 53 tables`、exit 0。

### 第 79 轮（**G4 的第一次实测证据：0/12 不是解码器的错，而是「这张照片没有墨/纸分离」被误诊成「四角没拍全」⇒ D69 已修**）

**这轮先做了一件以前从没做过的事：把 `phone-hard` 档真跑一遍**（`docs/STATUS.md` 里 G4 一直是「零证据」）。新增 `tools/g4-probe.ps1`：两条腿（纸面 `P-M1-300` / 板材 `PL-G@0.4`）× 两种取景（`nocrop` / 让裁切生效）× N 个 seed，过 `sim/channel.py --preset phone-hard`，再用 `tools/g2-corpus.mjs` 判「逐字节还原」，并把**信道自己的报告**（`clipped_frac`、`marker_visible`）一并记下来当 ground truth。

**实测（8 seed × 2 条件 × 2 腿 = 96 页）**：四条组合全部 **0/8**。但失败原因**不是**解码器：

- **信道报告 `marker_visible: [1,1,1,1]`、`corners_visible: 4`**（角标确实在画面里），而解码器报「四角都没有角标」；
- **81/96 页**的 `findMarkers` 失败且**墨/纸根本无法分离**：`median/peak` 墨量比 **0.668–0.793**（健康页 0.063、300dpi 扫描 0.060、4px 模糊 0.241）；
- 信道的 `clipped_frac` 0.49–0.97、`capture_ppi` 2.75–3.9 ⇒ 整张 A4 塞进 1280×960 的手机画面时，格子被糊成一片。
⇒ **两种物理原因被同一个理由字符串盖住**：角标出画（真·取景问题）与照片无对比度（曝光/反光/太远）。用户被告知「重新取景」，而正确动作是降曝光/避反光/靠近。

**修法（D69，已闭）**：`core/decode/fiducial.js` 新增 `medianInknessRatio()`（典型像素的墨量 / 峰值墨量）与 `FLAT_CAPTURE_RATIO = 0.5`；`findMarkers` 在**所有尝试都失败之后**（且峰值 > 24，否则那是 `blank-image`）报新 reason **`no-contrast`** 并带上实测数字；`advice.js` 给出对应措辞（含「重新取景没用」）；`g2-corpus.mjs` 把这一类单独标注。**判别量是按实测选的，不是拍的**：健康 0.060–0.063、模糊 0.241、phone 系列 0.668–0.793 ⇒ 切点 0.5 落在空隙里。**先用 Otsu 墨量占比 0.55 试过，被实测否决**（裁到码区的**锐利**页就到 0.560 ⇒ 会误报），如实记在代码注释里。**判据一字未动**：没有页面因此被接受。

**仍成立的两条结论（对产品有用，且都已量化）**：

1. **整张 A4 塞进手机画面时，`P-M1-300` 的格子根本读不出来**：`phone40`（较温和的档）下，定位成功、校正 100%，但逐格判读**符号错 72.83%（50186/68904，全部 `0->1`）**、`rhoMedian 1.1972`（整页糊成一团）⇒ **不是 echo 条一票否决的问题**（第 8 轮记的那个待办被这条实测降级：在手机分辨率下数据区自己就读不出来）。
2. **缺角标就是不可能**：单应需要 4 个点对；`crop active` 条件下「画面内角标」只剩 68–73/96 ⇒ **G4 的判据没有写明这个前提**（与 G2 侧早就记下的同一件事同源）⇒ 记 **D70，OPEN，需产品负责人决定**（补前提，还是要求产品在缺角标时也能工作 —— 后者物理上不可能）。

**本轮门限复跑（第 79 轮，全部实测；`core/decode/fiducial.js` 被改过，所以全套都重跑了）**：

- 单测：**`tests 336 · pass 336 · fail 0 · duration_ms 143923`、exit 0**（+1 例：健康页与「裁到码区但曝光正常」的页不得被判 `no-contrast`）。
- 进程内门限：`verify --gate all` ⇒ **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`**；**G5 仍 `9200 recovered exactly, 800 refused, 0 FALSE ACCEPTS`**（改诊断不改接受条件 ✓）；G1 七档 0 误读。
- 端到端冒烟：`& .\tools\usability.ps1` ⇒ **exit 0、226s**。
- 真信道探针：`& .\tools\mtf-probe.ps1` ⇒ **0 FAIL / 40s**。
- G4 探针（**不是门限**）：`& .\tools\g4-probe.ps1 -Seeds 8` ⇒ exit 0，四条组合 0/8，并打印信道自己的 `markers in frame` 与 `clipped_frac`（本轮新加，正是靠它才把归因定下来）。
- 台账：`node tools/check-docs-tables.mjs` ⇒ `clean -- 307 rows in 53 tables`、exit 0；**`ledger-headings` 测试当场抓住我自己的一次编辑事故**（我把 `### 第 43 轮新增（OPEN）` 这行吞掉了 ⇒ G0 报 `a ledger table lost its heading`）⇒ 已修复，`ledger-headings` + `defects-ledger` 7/7 绿。
### 第 78 轮（**把用户那半边变成「一条命令 + 照抄」：验收包生成器；顺带更正两处把档位写混的记账**）

**这一轮做的不是新功能，是把「只有用户能做的验收」的成本压到最低** —— 目标的完成判据里有半条是「用户照 docs 在电脑与手机上各走通一次」，而 §5 的清单散在好几条命令里（每条参数不同），用户敲错一个 flag 就会以为「产品坏了」。

**新增 `tools/acceptance-kit.ps1`（+ `tools/acceptance-readme.txt` 模板）**：一条命令生成
① 纸面页（`P-M1-300`，204800 B 载荷，PNG + 真尺寸 pack.pdf）；
② **每个喷嘴一块码牌**（`PL-G` @ 0.2/0.4/0.6/0.8 —— G10 的「保底档在全部喷嘴下」那一半；外加 `PL-D2` @ 0.4）；
③ MTF 校准板（3mf + stl + png + spec）；
④ **README.txt**：逐步命令、每一步在验什么、失败时该发回什么。
载荷刻意取 **6 B**：`PL-G@0.8` 每数据页净容量只有 **12 B**（`geom.ecc.dataBytes`），6 B 压缩后 11 B 仍只占**一页** ⇒ 用户每块板**只打 `page-000`**（实测：五组 profile×喷嘴 全部「数据页单页即可装配」，见下）。

**实测**：`& .\tools\acceptance-kit.ps1` ⇒ **exit 0**、8 条 PASS：纸面 3 页 + pack.pdf、PL-G 四档各 3mf+stl、PL-D2@0.4、MTF 板、**16 个 .3mf 全部过 G8 子集校验**。**阴性对照**（把其中一个 .3mf 改一个字节）⇒ `verify --gate G8 --file` **exit 1**，原文件 **exit 0** ⇒ 这个包里的校验是承重的，不是装饰。载荷/单页装配的实测：`PL-G@0.8 dataBytes 12 pages 3 decode ok data-page-only true`（0.6/0.4/0.2 与 PL-D2@0.4 同样 true）。


**本轮门限复跑（第 78 轮，全部实测）**：

- 单测：`node --test --test-isolation=none "tests/unit/**/*.test.mjs"` ⇒ **`tests 335 · pass 335 · fail 0 · duration_ms 144199`、exit 0**（本轮没动 `core/`，是回归）。
- 进程内门限：`node cli/pskit.mjs verify --gate all` ⇒ **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`**（自列未评估 G4 G6 G9 G10 ⇒ 不得引用成「全部门限通过」）。
- 验收包本身：`& .\tools\acceptance-kit.ps1` ⇒ **exit 0 / 8 PASS**（含 16 个 .3mf 过 G8 子集）；**阴性对照**：改一个字节 ⇒ `verify --gate G8 --file` **exit 1**、原文件 **exit 0**。
- 台账表格：`node tools/check-docs-tables.mjs` ⇒ `clean -- 304 rows in 52 tables`、exit 0。
**两处记账更正（判决未变）**：STATUS 的 M7 行与 G10 行都写着「只测到 `PL-G` 在 0.6/0.8 被 `glyphGeometry` 正确拒绝这一个边界点」—— **第 78 轮实测推翻**：`planPage('PL-G', {nozzle:0.2/0.4/0.6/0.8})` **四档全部可规划**（pitch 3.12/3.6/5.6/7.6mm；50×50/42×42/23×23/14×14 格），G7 门限自己打印 `PL-G@0.8 … recovered`；被 `glyphGeometry` 拒绝的是 **`PL-D3S`**（形状字母表 4 档 ⇒ 0.6/0.8 放不下，容量表那行本来就写对了）。原句把两个档位写混 ⇒ 就地划线 + 标注更正，**G10 判决不变**（完整矩阵仍未跑）。

### 第 77 轮（**D68 闭合：数据板的网格补上四角标记 —— 没有它，印出来的码牌照片登记不了**）

**这轮修的是一个"看起来全了、其实不能用"的缺口**：`core/mesh/plate.js` 的 `facts.notModelled` 一直明写"板材只带点阵、不带角标"，理由是角标线宽在 `core/render` 里是像素量、而 `MESH-CONTRACT.md` §3 不许凭空发明半径。后果不是"少点装饰"，而是**用户按手册印一块 `PL-D2` 码牌、拍照回来，`receive --photo` 的 `findMarkers` 找不到四角** ⇒ G10 的实机那一半根本走不通。

**修法（不发明任何数字）**：角标几何全部取自 `pageLayout()` 的 `layout.fiducials`（像素）按 `mmPerPx` 换算 —— 中心 `(f.x,f.y)`、半边 `f.half`、**环宽 = 该标记自己的 `f.ringPx`**（布局给的是 `max(cellPx,ewPx)` ⇒ ≥1 EW；薄于一格 EW 时装配**直接拒绝**）。空心角标 = 外方框减内方框，用第 76 轮的 `rectilinear.js` 挤出（逐对象水密）。四个标记进 `relief-ink0` 桶 ⇒ **object 划分不变**（还是底板 + 每料一岛），G8 的部件判据一字未改。

**判据必须跟着长**：标记按设计落在点阵之外的静区里，所以"点阵外不得有料"这条判据会**误杀**它们。做法不是放水，而是**让声明成为被检查的陈述**：`facts.markers` 声明位置与期望面积，投影对拍只排除**声明过**的标记，且**每个被声明的标记必须真的在、面积对得上（<8%）**。两侧同一条规则：JS `projectTopToCells({markers})` 与 `ref/verify_model.py` 新增 `g8-3/marker-material`。单测里带**反例**：同一批三角形**不声明**标记 ⇒ 必须红（否则"标记"两个字可以随便写）。

**实测（真命令）**：

- `node cli/pskit.mjs verify --gate G8` ⇒ PASS，`PL-D2@0.4 -> 3 objects, **378064** triangles`（比上一轮 377964 **正好多 100** = 3 个实心角标 ×12 + 1 个空心 ×64）。
- `python ref/verify_model.py --dir .tmp/m5verify77` ⇒ **`RESULT: PASS (62 checks, 0 failed)`**，其中新增行 `PASS g8-3/marker-material  4 marker(s) measured, areas vs declared: tl=121.1467, tr=121.1467, bl=121.1467, br=107.2686`（空心角标比实心小 13.88mm² = 内方框面积 ✓）、`PASS g8-3/no-straddling-material  0 triangles cross a cell boundary, 0.000e+00 mm^2 of material outside the lattice (markers excluded: 4 declared)`。
- `python ref/verify_model.py --dir .tmp/m5verify77 --selftest` ⇒ **`RESULT: PASS (11 checks)`、`all broken copies were rejected`**（修完匹配算法后重跑，确认没有把检查器弄瞎）。
- 单测 `tests/unit/mesh-3mf.test.mjs` **30/30**（新增标记断言 + 不声明即红的反例）。

**顺带修掉两个真问题（都是实测暴露、不是推测）**：

1. **判定框不能用 4 位小数**：facts 里原本 `round4()` 存标记框，而待判网格是**焊接过**的（6 位小数）⇒ 框可能比顶点窄 5e-5mm ⇒ 实测两个标记读成 `0.000mm²`、10 个三角形被报成"跨界"。改成**全精度**存框 + 判定容差 **1 µm**（比任何真实几何错误小三个数量级）。
2. **`cross/same-triangle-multiset` 的匹配算法是贪心的、会漏配**：原来"按排序位 ±64 的窗口找最近的未用元素"，加了 4 个标记后**末尾 56 个三角形配不上**、最大偏差报成 **169.8mm**（把 `(177.09,8.52)` 配到 `(185.67,178.34)`）—— 而"同序号逐点"那条 `max 1e-3` 是通过的 ⇒ **不是几何不同，是匹配算法把窗口里的候选先用光了**。换成**按坐标分箱（3 位小数 + 3×3×3 邻域，边界跳格由邻域吸收）**，判据**一字未改**（`unmatched==0 且 dev ≤ 1e-3`）⇒ 现在实测 `max deviation 7.454e-06 mm, 0 unmatched of 425488`（float32 噪声量级）。


**本轮门限复跑（第 77 轮，全部实测）**：

- 单测：`node --test --test-isolation=none "tests/unit/**/*.test.mjs"` ⇒ **`tests 335 · pass 335 · fail 0 · duration_ms 146840`、exit 0**（本轮没有新增用例文件，是 `mesh-3mf.test.mjs` 内新增断言 + 反例）。
- 进程内门限：`node cli/pskit.mjs verify --gate all` ⇒ **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`**；G8 侧 `PL-D2@0.4 -> 3 objects, 378064 triangles`（自列未评估 G4 G6 G9 G10 ⇒ 不得引用成「全部门限通过」）。
- **独立解析侧**：`python ref/verify_model.py --dir .tmp/m5verify77` ⇒ **`PASS (62 checks, 0 failed)`**；`--selftest` ⇒ **`PASS (11 checks)`**、`all broken copies were rejected`。
- 端到端冒烟：`& .\tools\usability.ps1` ⇒ **exit 0、235s**（3D 腿含 `send --format 3mf,stl` + G8 `--file`）。
- 真信道探针：`& .\tools\mtf-probe.ps1` ⇒ **0 FAIL / 40s**（回归：校准板网格与数据板网格共用 `rectilinear.js`）。
- 台账表格：`node tools/check-docs-tables.mjs` ⇒ `clean -- 304 rows in 52 tables`、exit 0。

**仍未做（如实）**：真打印机印一次、真手机拍一次（G10/G4）—— 本轮只把"印出来能被找到"这件事做进了产物；**照片里能不能真的锁住浮雕角标**仍然只有真机能回答（单色板上角标靠阴影，双色板上靠料色，两者都没在真照片上验过）。

### 第 76 轮（**D67 闭合：校准板有了可打印的 3MF/STL —— 而且"水密"是构造出来的，不是希望**）

**这轮补的是第 75 轮自己留下的窟窿**：上一轮交付了"能量喷嘴的板"，但用户**印不出来**（只有外观光栅）。本轮把它变成实物，顺带做掉一个只有做这件事才会暴露的几何问题。

**新增/改动**：

- `core/mesh/rectilinear.js`（新）—— **共形网格挤出**：把"轴对齐矩形的并集"挤成**逐对象水密**的实体。做法：先按所有矩形的 x/y 边界切出网格，格子作为四边形，**只有"邻居在外面"的边**才生成墙 ⇒ **没有任何内部面**，水密是构造出来的。`subtractRects()` 负责"外框减孔"的矩形分解（并集恰好等于外框−孔，且相邻矩形贴边）。
- `core/mesh/mtfplate.js`（新）—— 板子的网格装配：底板（`PLATE_MM`=2mm，直接 import `plate.js` 的常量，不另写数字）+ 每墨一个浮雕 object（`RELIEF_MM`=0.3mm）；几何**全部**来自 `spec.inkRegions`（第 75 轮那份"墨在哪里"的单一来源）⇒ 光栅与网格不可能各自漂移。`projectionReport()` 把顶面三角形投影面积与**规格的矩形算术**对拍。
- `core/calibrate/mtfplate.js`（改）—— 规格重构为 **`inkRegions`**（每个墨区：外框 + 挖掉的孔，像素坐标），`renderMtfPlate` 与 `buildMtfPlateModel` **同一份列表**驱动；`applyPrintEw()` 成为共享的"打印机仿真"（光栅与网格走同一套），并**带上角标**。
- CLI `calibrate --make-mtf --format png,3mf,stl`：写模型前先判**每 object 水密**与**投影对拍**，不合格**拒绝写文件**（与 `send` 的板材路径同一纪律）。
- `tests/unit/mesh-mtfplate.test.mjs`（新，6 例）+ `tools/mtf-probe.ps1` 加一条 mesh 腿。

**实测（真命令）**：

- `node cli/pskit.mjs calibrate --make-mtf --out .tmp/mtf76 --format 3mf,stl,png` ⇒ exit 0：`mtf-plate.stl` **561180 三角形 / 27.4 MB**、`mtf-plate.3mf` **3 objects / 6.1 MB**、自报 `base 2mm + relief 0.3mm; ink 6122.346178mm² projected 6122.346109mm² (worst region 0% off, tolerance 8%); every object watertight`。
- `node cli/pskit.mjs verify --gate G8 --file .tmp/mtf76/mtf-plate.3mf` ⇒ **PASS**（3 objects / 561180 三角形 / unit millimeter；Core 1.4 子集，规则抄自 vendored XSD）。
- `& .\tools\mtf-probe.ps1` ⇒ **0 FAIL / 39s**（新增的 mesh 腿 PASS：同一份规格写出 3mf+stl，3MF 过 G8 子集；四喷嘴点名那三条仍 PASS，0.2 那条仍是 NOTE）。
- 单测 6/6（含**重叠矩形必须被拒**的阴性对照、**贴边矩形必须并成一个水密体**、每 object 水密、投影 `maxPct < 1e-4`、确定性、`mono` 合并）。

**本轮踩到并修掉的两个真坑（都是实测，不是推测）**：

1. **贴边矩形逐矩形挤出 ⇒ T 形接缝**：第一版按"y 横带"合并成长条，`manifoldReport` 立刻报 `12 edge(s) used more than twice` + `24 pinch vertices` —— 顶面的长边会**越过**孔的墙起点。改成共形网格（每个矩形边界都成为网格线）后全绿；测试里留着这个反例的等价物（重叠矩形必须被拒）。
2. **孔与 EW 的比较必须用设计尺寸（mm），不能用像素取整后的尺寸**：0.26mm 在 300dpi 画出来是 3px = 0.254mm ⇒ 用像素比较会把 **0.2 喷嘴自己那一档**填死，那个喷嘴就变得不可测。修法是给孔带上 `nominalMm` 并比它。**这条是第 75 轮那套"EW 0.26 读不回来"的解释的一部分**：读者那一侧的原因（渗墨）没变，但仿真侧也曾有一处把 0.26mm 孔误填 —— 两个原因分开记，免得下一次把责任归错。


**本轮门限复跑（第 76 轮，全部实测）**：

- 单测：`node --test --test-isolation=none "tests/unit/**/*.test.mjs"` ⇒ **`tests 335 · pass 335 · fail 0 · duration_ms 143162`、exit 0**（第 75 轮末是 329/329；本轮新增 `tests/unit/mesh-mtfplate.test.mjs` **6 例**）。
- 进程内门限：`node cli/pskit.mjs verify --gate all` ⇒ **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`**（它自己列出未评估 G4 G6 G9 G10 ⇒ 不得引用成"全部门限通过"）；G8 侧 `PL-D2@0.4 -> 3 objects, 377964 triangles, 4145883 bytes; selfCheck3MF ok`。
- 产物级：`build-web` exit 0、`check-dist` exit 0、`node tools/check-docs-tables.mjs` ⇒ `clean -- 304 rows in 52 tables`、exit 0。
- 真信道探针 + mesh 腿：`& .\tools\mtf-probe.ps1` ⇒ **0 FAIL / 39s / `all assertions pass`**。
- G8 子集校验校准板 3MF：`verify --gate G8 --file .tmp/mtf76/mtf-plate.3mf` ⇒ PASS。

**仍未做（如实）**：**数据板的网格仍然没有角标（D68，OPEN）** —— 同一根因（`MESH-CONTRACT` §3 不许发明半径）⇒ 真机打印的数据码牌照片**登记不了**，G10 的实机那一半因此还走不通；本轮只给校准板建了角标（几何取自 `spec.fiducials` 像素换算、环宽 = 1 个 frame cell，规则写在 `core/mesh/mtfplate.js` 头部）。真打印机印一次、真手机拍一次仍是硬件项。

### 第 75 轮（**M7 缺的那一半：MTF 校准板 + 读者 + 一条真信道的探针 ⇒ 该用哪一档第一次是量出来的**）

**这轮做的是 `docs/HANDOVER.md` §12 进程内优先级第 1 条**（MTF 校准板 → 推荐喷嘴/间距，PLAN §3.4 明写的未完成项）。**一条判据都没动**；G10 仍 🟡（它要真打印机）。

**新增（四个文件 + 一个工具）**：

- `core/calibrate/mtfplate.js` —— 板子的**规格与外观渲染**。Pitch ladder（PLAN 的 0.6/0.9/1.2/1.8/2.4mm）每档是一块 24mm 实心块**挖方格孔**（孔 = 0.4×节距，孔间墨 = 0.6×节距）；feature ladder 是 3mm 节距实心块里的**孤立孔**（0.26/0.3/0.45/0.6/0.7/0.9/0.95/1.2mm）；另有每墨一块色样、实心/竖肋/横肋三块纹理样、150mm 标尺（1mm 刻度 + 10mm 长刻度）、四角标记（三实心一空心，与数据页同一约定）。**为什么是实心挖孔而不是棋盘格**：棋盘格在 2.4mm 档会给出几十个同尺寸孤立方块，`findMarkers` 的同尺寸簇会把它当成标记（D37 的同一形状）⇒ 登记会拿板子自己的数据去配自己；挖孔则整块是一个连通域，孔是内洞，不进候选。**为什么喷嘴无关是真性质**：规格里**没有 nozzle 参数**，每一档都是自己节距的分数或固定 mm 数 ⇒ 同一份板子用 0.2 与 0.8 喷嘴各印一次，测出来的是印出来的那个。
- `core/calibrate/readmtf.js` —— 读者：登记用 `findMarkers` + `rectifyPage`、覆盖率用 `analyseCell`、颜色分类用 `nearestLevel`（**都是解码器自己的代码**，不另写一份量具）。判据是**物理陈述**而不是拟合旋钮：一档算读得出来当且仅当**孔中心覆盖率 ≤ 0.25 且孔周围墨 ≥ 0.70**；matched-filter margin 只报不判（本仓库本来就没有可借用的 margin 阈值 —— `quality` 一直是报而不判）。
- CLI 两个模式：`calibrate --make-mtf --out DIR [--plate-mm/--dpi/--palette/--print-ew]` 与 `calibrate <png> --mtf [--spec/--json/--fast]`。`--print-ew` 是**仿真专用**（模拟最小特征为 X mm 的打印机：比它窄的孔被**填死**，不是放大到一格 EW —— 放大会让 0.2 喷嘴冒充 0.4），真机不需要它。
- `tools/mtf-probe.ps1` —— **一条命令的真信道探针**（26s）：对 EW 0.26/0.45/0.70/0.95 各做渲染 → `sim/channel.py --preset scan300 --seed 11`（**Python，与读者零共享代码**）→ 读回 → 要求点名那个喷嘴，外加两个对照。
- `tests/unit/mtfplate.test.mjs` —— 7 例。

**实测（真命令，不是读代码）**：

- `& .\tools\mtf-probe.ps1` ⇒ **0 FAIL / 26s / `all assertions pass`**：**EW 0.45 → 推荐 0.4 ✅、EW 0.70 → 0.6 ✅、EW 0.95 → 0.8 ✅**（三条都来自该喷嘴自己的 EW 那一档被读成 OPEN，即**直接读**而非推断）。
- **阳性对照 0**：无信道的原始渲染 ⇒ **每一档全解析**、floor = 0.26mm ⇒ 证明读者**能**看见 0.26mm 的孔。
- **阴性对照**：EW 1.40mm（比任何喷嘴都粗）⇒ **推荐 none**、`exit 0`（一个总能给出答案的读者会在这里露馅）。
- **EW 0.26（0.2 喷嘴）⇒ 读者报 floor 0.45mm、推荐 0.4，探针记 NOTE 而不是 PASS**：那一档被判 **filled**。原因已实测、不是我猜的：信道的渗墨（scan300 `ew_mm` −0.05..+0.15mm）把 0.26mm 的孔**在任何 dpi 下**填死 —— `scan300` 与 `scan600` 两次都给出 ~0.5 的孔中心覆盖率。⇒ **结论（对用户有用）**：0.2 喷嘴的板材**用 300dpi 扫描读不回来**，这不是读者的问题（对照 0 已证明），而是 0.26mm 特征 + 渗墨这件事本身。
- **另一个实测结论**：把整张 200mm 板放进 `plate-matte`（手机照片口径，整板约 100dpi）⇒ **连 0.95mm 的孔都读不出（中心 0.45）**，floor 落在 1.2mm ⇒ **读者正确地拒绝推荐任何喷嘴**并给出拍近一点 / 用扫描仪。⇒ 这是对板材能不能用手机远距离读的**第一次量化**，对 G4/G10 都是证据（不是判决：G10 仍需真打印机、G4 仍需真手机）。
- 单测 7/7（含：四喷嘴逐一点名、孔窄于打印机必须**填死**（检查像素而不是检查读者）、单调查 σ 0/0.12/0.2mm ⇒ floor 0.26/0.45/0.9mm、σ 0.45mm 全灭 ⇒ 拒绝给答案、未知 spec 版本必须拒）。
- 两个梯子**允许不一致，且已量化**：`scan300`+EW0.45 下 feature ladder 给 0.45mm、pitch ladder 蕴含 0.71mm（相差 1.6 倍）⇒ 读者在 >1.5 倍时**打印这条不一致**并说明机制（格子里的孔四面被渗墨侵占，孤立孔不会）与用法（**喷嘴看 feature ladder、节距看 pitch ladder**）。


**本轮门限复跑（第 75 轮，全部进程内/真信道，命令与数字都是实测）**：

- 单测：`node --test --test-isolation=none "tests/unit/**/*.test.mjs"` ⇒ **`tests 329 · pass 329 · fail 0 · duration_ms 139380`、exit 0**（第 59 轮是 297/297；本轮新增 `tests/unit/mtfplate.test.mjs` **8 例**；中途那次 `328` 之后又给读者加了「画布尺寸 ≠ 画布对齐」的守卫与对应测试 ⇒ **记的是最后那次**）。
- 进程内门限：`node cli/pskit.mjs verify --gate all` ⇒ **`ALL GATES PASS -- 6/7 evaluated, 1 skipped`**、G5 `10000 tamper trials: 9200 recovered exactly, 800 refused, 0 FALSE ACCEPTS` + `2000 recalibrated-seam trials ... 0 FALSE ACCEPTS`、G8 `PL-D2@0.4 -> 3 objects, 377964 triangles, 4145883 bytes; selfCheck3MF ok` ⇒ **它自己列出本次未评估 G4 G6 G9 G10** ⇒ 不得引用成"全部门限通过"（老规矩）。
- 产物级：`node tools/build-web.mjs` ⇒ exit 0、`web/dist: 59 files`、`precache entries 57`；`node tools/check-dist.mjs` ⇒ **13 pass / 0 fail + `G9 CHECK: all 13 assertions pass`**、exit 0；`node tools/check-docs-tables.mjs` ⇒ `clean -- 303 rows in 52 tables`、exit 0。
- 端到端冒烟：`& .\tools\usability.ps1` ⇒ **exit 0、227s**（`PSKT usability smoke` 全部腿 PASS：纸面往返逐字节相同、split/join 三段传输 + 阴性对照、加密三种收法、板材 3mf/stl + G8、两个客户端数据路径、局域网服务与手机视角）—— 本轮**没改**这条链上的任何代码，重跑是为了确认 MTF 板没有碰坏它。
- 真信道探针：`& .\tools\mtf-probe.ps1` ⇒ **0 FAIL / 27s / `all assertions pass`**（见上）。

**仍未做（如实）**：MTF 板的 **3MF/STL 网格**（现在只有外观光栅 —— 外观光栅是这块板被相机/扫描仪看到的样子，也是 `sim/channel.py` 与所有板材档测试吃的东西；网格半边记为 **D67**）；真打印机印一次、真手机拍一次（G10/G4）；`--print-ew` 只是仿真。**判据、门限、G10 判决一个字未改**。

### 第 74 轮（**交接与暂停：写 `docs/HANDOVER.md`，并更正两处会把接手人带错的过期记账**）

**这一轮不是门限轮，是用户指示的收尾**：「写一份交接文档，然后暂停」⇒ **代码零改动**（`core/` `cli/` `web/` `tools/` `tests/` 一个字节没动 ⇒ `04a6413` 那套门限数字仍然成立；本轮不重跑、也不新增任何判决），只动 `docs/`。

**新增 `docs/HANDOVER.md`**（交接概览，13 节）：一句话现状 → **我是不是卡了的诚实回答** → 目标与硬约束原文 → 权威源与阅读顺序 → 仓库地图（谁负责什么、哪份是权威）→ 从零复现的命令与期望输出 → 门限现状表 → **只有用户能做的验收**（G4 / G9 / D8 / G10 / G8 收尾 / D43）→ **待产品负责人决定的三件事**（G6 ② 的三条路、是否继续循环、D49）→ 已知债与"仍未做" → 环境陷阱 → 台账纪律（含"每个新检查必须有阳性对照""估算只能警告不能拒绝""不为措辞造假纯函数"）→ 若继续的优先级（含**明确不建议做**的三件事）。**如实写在文件里的一句**：卡点不在代码，而在完成判据的"用户照 docs 能在电脑与手机上各走通一次"这半条我无法自己完成、也不允许冒充实机证据 ⇒ 自动循环的边际收益在下降（71 轮 = 一句没有数字的拒绝语、72 轮 = split/join、73 轮 = 一句错诊断 + 一个缺失的输入框），而 **M4 从第 22 轮起就在等一台真手机**。

**顺手更正两处过期记账**（都会把接手人带错；**判决本身一字未改**）：
- `docs/USE.md` §5 的 **G6 soak 行**原写「长时间连续运行 / 进程内实现尚无 ✗」⇒ 那是第 65 轮之前的状态：进程内实现早就有（`tools/soak.mjs` = `verify --gate G6`），第 68 轮 30.2 min `PASS G6`、第 70 轮 60.02 min 复跑 `ok:true`（281 cycles / 2782 页 / **误接受 0** / RSS +3.16% ≤10%）⇒ 留在**用户清单**里会让人白等一次手工长跑。原句划线保留。
- `docs/STATUS.md` 门限表下的**总账计数**原写 `🟡 4（G2 G6 G8 G10）· ⬜ 2（G4 G9）`，而 G9 那一行的判决早就是 🟡「产物级全绿 ✓ 浏览器内未验证」⇒ 计数与判决自相矛盾，更正为 `🟡 5（G2 G6 G8 G9 G10）· ⬜ 1（G4）`。**这是计数错误、不是判决变动**：G9 仍差真浏览器点一次与 https 托管（D43）。

**目标状态**：同一会话的持久目标**已暂停** —— `get_goal` 实测 `phase=paused`、`activation=disarmed`、`roundsStarted 65 / maxGoalRounds 80` ⇒ **不会再有自动续跑轮**。⇒ **不是完成**（完成判据里"用户在电脑与手机上各走通一次"尚未发生），也**不是 blocked**（blocked 要同一条阻塞连续 3 个自动轮次，而这次是人直接叫停）。恢复方式写在 `HANDOVER.md` §1：① 用户跑一次硬件验收（哪怕只跑 G4 的一部分）；② 用户说"继续找进程内的活儿"；③ 用户对 G6 ② 给个决定（选"限定 300 dpi"的话 D49 随之出局）。**恢复的技术动作**：人说"继续"时用 `update_goal action=resume` 重新武装（会话被恢复或 fork 后，活动目标本来就是 disarmed 状态，必须由人开口才 rearm）。

**没做的事（如实）**：**没打 tag**（M4 仍开在 G4 ⇒ standing 指令未变）；**没重跑门限**（无代码改动 ⇒ 重跑只会得到同一批数字，而 `timeoutMs` 封顶 600 s 下 soak 与 G2 语料本来就要分批，不值得为一次 docs-only 收尾占用）；`docs/PLAN.md` 未动（已批准契约）。

### 第 73 轮（**core 早就知道"缺的是口令、不是页"，三个接收端却一致告诉用户"缺页"；而手机连拍那一节连口令框都没有 ⇒ 加密传输在手机上造得出、收不了（D66）**）

**这轮修的是一句错诊断，和它背后那条走不通的死路。先读代码、再实测，没有一步是推断。**

**查到的事实**：`core/protocol.js` 的 `finish()` 在 CIPHER 位为 1 而既无口令也无钥匙时执行 `this.needPassphrase = true; return null`，**改前不设 `this.error`**（用 `git show HEAD:core/protocol.js` 取到原文核实：那一支只有两行）；而**所有**消费者在 `result` 为 null 时读的正是 `error` ⇒ 各自落到通用兜底：**CLI** `receive: INCOMPLETE (1/1 data pages) -- still short`、**桌面接收页**「未完成（数据页 1/1）：仍缺料」、**手机连拍**「组装失败：未知原因（页收齐但内容不完整）」⇒ **进度行明明写着 N/N，三条路都把用户指向重印重扫**。更糟的是**手机连拍那一节根本没有口令输入框**（`web/index.html` 的 `#burst-wrap` 只有 `#burstname`，`web/capture.js` 是 `new TransferAssembler({})`），而**发送页是提供加密的**（`send.html` 的 `#spw` → `sender.js` L507）、桌面接收页也有 `#pass` ⇒ **加密传输在手机上造得出、收不了、而且没地方填钥匙**。`tests/unit/protocol.test.mjs` L472-475 早就钉住了那个布尔（注释写着 receiver must ask for the passphrase, not fail silently）⇒ **core 履行了契约，是 UI 没接住**。顺带在**同一页**查到另一处无来由的分歧：手机连拍落盘走 `data:` + `btoa`（分块 `String.fromCharCode` 拼整串），而同一页的桌面路径走 `Blob` + `URL.createObjectURL`（`web/app.js` L114-116）⇒ 手机是下载限制更多的平台，却用了两种机制里较不标准的那一种，而这恰是判据②的最后一步「取回文件」。**这不是误接受**：拒绝是对的、摘要门没被碰 ⇒ 判据③未破；它是**一个把用户指向错误补救动作的死路** —— 不交出错误数据，但交出错误诊断。

**修法（最小范围，一条判据都没动）**：

- **core 只加一行**：那一支同时设 `this.error = 'need-passphrase'`；`tests/unit/protocol.test.mjs` 补 **2 条断言**把这个**三个 UI 都依赖的契约**钉住（旧断言一字未改，只钉 `needPassphrase`/`result` 的部分照旧）。G5 的 `refused:` 分类按 `error` 的首个词取标签，而 G5 不加密 ⇒ CIPHER 位为 0 ⇒ 这一支根本不进；万一进了，标签也只会是准确的 `need-passphrase`，**不会变成假绿**。
- **三处各给一句能照着做的话**：CLI `receive: NEEDS PASSPHRASE -- all N data page(s) arrived, but this transfer is encrypted and no key was given` +「不是缺页问题：加 `--passphrase` 重跑同一条命令」+「页图还在磁盘上，不必重印重扫」+「没写任何文件：没有钥匙就没有明文可与声明摘要核对」；桌面页「缺的不是页、是口令：填好后再按一次『开始还原』，已选文件不用重选」；手机连拍「填口令后重来一次」+ **如实说出代价**（连拍不留已解出的页 ⇒ 这次要重拍）。文案一律不含 markdown（`log()`/`say()` 赋的是 `textContent`，星号会原样显示）。**没有**为这件事抽一个"纯函数产句子"：策略本身就是 core 的一个布尔（已有单测），三处只是措辞；抽出来只会得到一条"字符串里含某个词"的假断言，而这件事 usability 的 4c 腿已经在 CLI 侧**端到端真做了**。
- **`#burstpass`**：`web/index.html` 连拍节新增口令框，`capture.js` 在**连拍开始时**读它构造 `TransferAssembler`（只读一次，因为装配器是在批次收满时才派生钥匙的）。`check-dist` 两条**现成**断言正好接住这块：`every getElementById in built JS exists in some built page` 与 `no built page declares the same id twice`（就是 D20 当年要的那个契约）⇒ 新 id 不是"我看了觉得对"，是机器验的。
- **落盘统一到 `Blob` + `URL.createObjectURL`**（删掉 `btoa` 与分块 `String.fromCharCode`）。**这不是把 D63 的决定翻过来**：D63 记的是 `sender.js` 的大产物出口（最大 ~63 MB、`send.html` 的 CSP），仍原样留在 G9 批次未动；本轮这一处受协议上限约束（≤~1.52 MB），换成的是**同一页已经在出货的机制**、不是新机制。**两种机制都没在真浏览器里验过** ⇒ 不主张"blob 在每台手机都行"，只主张"同一页不再两种存法"，并已把"真机验一次落盘与扩展名"写进 §5 的 G4 清单（D66 里承诺过 ⇒ 不写就是假话）。**故意不 `revokeObjectURL`**：程序化 `click()` 之后立刻 revoke 在部分浏览器里会与下载抢跑，而一次连拍只产生一个 blob，不值得冒这个险。
- **入库 keeper：`tools/usability.ps1` 新增 4c 腿**（`-SkipCrypto` 可跳）：6000 B 加密传输（1 数据页 + 2 校验页）过真信道（seed 207）⇒ **不带口令**必须 exit≠0、无产出、**措辞点名口令且不得再出现 `still short`**；**错口令**必须也被拒（摘要门 ⇒ 判据③）；**正确口令**必须逐字节还原 ⇒ 最后这一步同时是前两步的**阳性对照**（证明页与信道都没问题，被拒的唯一原因是钥匙）。措辞在这一腿里是**被判定对象**（因为措辞正是本轮的修法），但仍同时看 exit code 与"有没有产出文件"，不是只看关键字。

**实测（真命令，不是读代码）**：`SEND_EXIT=0`（3 页 = 1 数据 + 2 校验）；**不带口令** `NOPW_EXIT=2`、打出 `receive: NEEDS PASSPHRASE -- all 1 data page(s) arrived, but this transfer is encrypted and no key was given`、`NOPW_WROTE=False`；**错口令** `WRONG_EXIT=2`、`INCOMPLETE (1/1 data pages) -- digest-mismatch`、`WRONG_WROTE=False`；**正确口令** `OK_EXIT=0`、`received 6000 bytes`、`MATCHES manifest`；再由 `.tmp/sj-help.mjs cmp`（用 **node:crypto**，即独立于 `core/hash.js` 的另一套实现）判 **IDENTICAL**。附带一个如实细节：那一趟里 `page-000` 的角标**没读出来**（`FAIL markers/no-marker-size-cluster`），数据页是靠页间奇偶补回来的 ⇒ "all 1 data page(s) arrived" 是实话，也正好说明**诊断不能再拿"页数"当唯一线索**。

**docs**：`docs/USE.md` 此前**一次都没提过口令**（全文 grep `口令` = 0 命中，而发送页明明有这一格）⇒ §1 接收页、§2 连拍节（含"必须先填、事后补要重拍"这个真实代价）、§6 故障排查各补一段，§5 的 G4 清单补"验落盘与扩展名"。**并更正我上一轮自己写错的一句**：第 72 轮我在 split/join 那段写「用网页或手机接收时要多一步：收完把每份改名回 part 名」，而 `web/index.html` L59/L91 两个「存成什么名字（可选）」输入框早就存在、并已接到 `downloadName({userText})` ⇒ 那句让用户多做一步的话是我**没查源码就下的结论** ✗（就地改正并标注）。

**门限与复跑**：全套单测 **321 tests / 321 pass / 0 fail / 138.0 s**、`SUITE_EXIT=0`（**条数没变**：本轮加的是既有 `transfer: wrong passphrase…` 与 `transfer: a passphrase alone…` 两个用例里的 **2 条断言**，不是新用例 ⇒ 别把"321"读成"没动测试"）；`BUILD_EXIT=0`（单文件 `pskt-file.html` **318.4 → 320.2 KiB** = 本轮新增的分支与文案；dist **57 文件**、build id `a0fc11c8ec3232d5`、**precache 仍 55** ⇒ 改的是既有文件、没新增资源）；`CHECKDIST_EXIT=0`（**13 pass / 0 skipped / 0 fail**、`G9 CHECK: all 13 assertions pass`，其中 `every getElementById in built JS exists in some built page` 与 `no built page declares the same id twice` 就是新 id `#burstpass` 的**机器**证据）；`smoke-sender` **13 PASS / 0 FAIL**、`SENDER SMOKE: all assertions pass`、`SMOKE_EXIT=0`（本轮没改 `sender.js`，跑它是为了证明没被牵连）；`smoke-capture` `CAPTURE_EXIT=0`、stats 与上轮逐项相同 `{"frames":4,"accepted":3,"duplicates":1,"rejected":0}`（它从 Node 加载 `web/capture.js` ⇒ 顺带证明改过的那个文件在 Node 侧仍能加载）；**端到端 usability 27 PASS / 0 FAIL / 222 s**、`USABILITY_EXIT=0`（上轮 **23 PASS / 189 s** ⇒ 多出的 4 步全是新的 4c 腿）；**没回归**：纸面腿 `32ec480521da27d2` vs `32ec480521da27d2`、4b 腿 `5c7b2d98c8a5b06d` 与 `negative control … exit 1, output written: False` 照旧，3D / G8 / check-serve / check-lan 全 PASS（dist 已重建 ⇒ 后两个查的是新站，即手机视角）；`node tools/check-docs-tables.mjs` ⇒ **clean，263 rows in 46 tables**（上轮 262 / 46，增量就是 D66 那一行）。

**仍未做（如实）**：① 桌面接收页 `#outname` 的 `input` 监听器注册在 `run()` 内部 ⇒ 每跑一次多挂一个（`applyName` 幂等，输出名字与提示都不受影响 ⇒ **属泄漏、不属错误**；本轮不动，理由是不在没有浏览器可验的情况下去改 DOM 生命周期）；② `app.js` / `capture.js` 里这两处新分支**进程内跑不到**（DOM 代码，Node 一 import 就在 `$('log')` 上炸）⇒ 它们的证据分三层：core 那个布尔有单测、CLI 对同一字段的行为由 4c 腿端到端实测、dist 的 id 契约由 `check-dist` 保证，而**真浏览器那一次仍归 G9/G4**；③ 手机连拍"事后补口令不必重拍"没做（要在手机上常驻每页判读结果，内存与复杂度都不划算 ⇒ 已在提示与手册里如实说出代价）；④ **M4 仍卡在 G4**（手机 500×8 只有用户能跑）⇒ **本轮不打 tag**。

### 第 72 轮（**G6 ② 的 600 dpi 地板量到了：完美排序也超预算 3.1 倍 ⇒ 上一轮"再降需要第二个廉价信号"那句推断被自己的实测推翻；同时把 D65 留下的死路补通 —— 超过 1.52 MB 的文件现在有 split/join 真能走完**）

**两件事，都不是记账。**

**一、G6 ② 的地板（判决一个字没动，整条仍 🟡）**

第 71 轮结尾记的"600 dpi 再降需要第二个廉价信号"是**推断**，本轮把它量掉。给 `tools/g6-perf-probe.mjs` 加了提示旗标 `--profile / --dpi / --palette / --only-hints`（原样透传给 `bootstrapDecode`；未知 `--flag` ⇒ stderr + `exit 2`；**不传旗标时与历史行为逐字节相同**，所以旧数字仍可比）。口径与 `tools/soak.mjs` L92-101 一致（`decodePNG` + `bootstrapDecode` 两段相加，`readFileSync` 不计）。同一张 600 dpi 真实信道页 `.tmp/sc-scan600-12/page-000.png`（**5190×7240 = 37.6 MP、PNG 40.0 MB**）：

| 跑法 | decodePNG | bootstrapDecode | 合计 | 尝试 | 白读的整页 |
|---|---|---|---|---|---|
| 无提示（= 历史行为） | 1547 ms | 23196 ms | **24743 ms** | 6 | 5 次 = 18.5 s |
| `--profile P-M1-600 --dpi 600 --only-hints`（几何已知） | 1546 ms | 11974 ms | **13520 ms** | 3 | 2 次 = 6.9 s |
| 再加 `--palette PAPER1` ⇒ **地板：恰好 1 个候选** | 1547 ms | **4645 ms** | **6192 ms** | **1**（`ok=true`，赢家 `P-M1-600@600/PAPER1`） | 0 |
| 300 dpi 对照 `.tmp/sc-scan300-1/page-000.png`（2565×3609 = 9.3 MP）无提示 | 363 ms | 1010 ms | **1373 ms** | **1**（第一次就赢） | 0 |
| 同一张 300 dpi 页 + 全套提示（**阳性对照**） | 363 ms | 1004 ms | **1367 ms** | 1 | 0 |

⇒ **完美排序（一个候选都不浪费）也只能把 600 dpi 页做到 6192 ms = 判据（≤2 s/页）的 3.1 倍**，而其中**光 `decodePNG` 1547 ms 就吃掉预算的 77%**（这一页的像素数是 300 dpi 同纸页的 4 倍）；300 dpi 侧加提示**买不到任何东西**（1373 → 1367 ms），这既证明旗标真在起作用、也证明 300 dpi 本来就不需要它（与已记的 1467 ms、soak worst 1783 ms ≤ 2 s ✅ 一致）。**所以"第二个廉价信号"这条推断不成立**：它最多把 24.7 s 买到 ~6.2 s，仍超预算 ⇒ `docs/ACCEPTANCE.md` ② 里那句已**就地划线**（未删）并补 **②bis** 记全上面这张表。**这是本轮唯一的台账更正，且不新开 DEFECTS 行**：它是判决文本里一个推断被实测推翻，按台账纪律划线 + 标注即可（与 ACCEPTANCE 历史段同一处理），不是产品缺陷。**本轮因此决定不造那个信号** —— 它无法让 ② 达标，而用户第 63 轮已判"没必要 600 dpi、只要能读到就行"（口径「刚好就行」）。**② 剩下的出路只有产品负责人能走的三条**：① 把 ② 限定在 300 dpi（`docs/USE.md` 指给用户的正是这一档，实测 1373 ms ✅）；② 为 600 dpi 放宽 2 s 预算；③ 投资一种**根本上更便宜的 600 dpi 读法**（先降采样/分块粗定位、再对定位区域精读 —— 唯一可能接近 2 s 的技术路线，本轮未做、也不主张它一定可行）。**G6 整条仍 🟡：本轮没把任何判决改绿，也没为过关收窄任何判据。**

**本轮我自己的两个错（如实记）**：① 一开始打算把"600 dpi 赢家那一趟的成本"写成**减法**（`11974 − 6900 ≈ 5.1 s`）⇒ 那是推断不是测量；改成实跑单候选后实测 **4645 ms**（减法低估了近 1.3 倍，方向都是错的）。② 第一版把"判据③ 全程气隙、无外部 URL"当成缺口准备新写检查 ⇒ grep 之后发现 `tools/check-dist.mjs` **早就有**（断言 1"zero third-party origin in what the page loads" + L62-65 的 `external src=` / `href=` / `import()`/`fetch()` / `@import` 四条正则 + L81-85 内联 CSS + L167 单文件外部引用 throw + L170 打印 `0 external references`）⇒ 没重造，也没把它算成本轮成果。

**二、D65 的死路补通：超过 1.52 MB 的文件现在真能传完**

第 71 轮让发送页如实说出"一次最多 255 页 ≈ 1.52 MB"并给了三条出路，但第三条"把文件切成几份分别传"**当时没有任何工具** ⇒ 一个 5 MB 的 PDF 走到这里就断了（D65 尾部如实记着"切成几份目前要用户自己做"）。本轮补上，并且**只补到能用为止**：

- **`core/splitjoin.js`**（新；纯函数、无 `node:` 内建 ⇒ Node 与浏览器同一份，合 AGENTS.md 对 `core/**` 的硬约束）：`splitParts(bytes, maxBytes)` / `joinParts(manifest, parts)` / `partName(i)` / `DEFAULT_PART_BYTES = 1,400,000`（在实测上限 1,592,968 B 下留 ~12% 余量）/ `MANIFEST_KIND = 'split/1'`。`splitParts` 交回的是**视图**（`subarray`，切大文件不额外分配，单测钉住 `parts[0].bytes.buffer === bytes.buffer`），空文件也给 1 片 ⇒ `join` 是全函数、没有"切成功但拼不了"的输入。**`joinParts` 用的就是本仓库那条口径（误接受是唯一不可原谅的失败）**：先拿 manifest 自查（`pskt` 版本、索引必须 0..n−1 且有序、名字不许重复、source 摘要必须 64 hex），再逐份比长度与摘要，最后比整文件摘要；任何一步不符 ⇒ `ok:false`、**不交出 bytes**、并报"验到第几片/共几片"。**拼接顺序由 manifest 的 index 决定、不由目录列表决定**（文件管理器排序不同不该能改用户的文件）。
- **`pskit split` / `pskit join`**（`cli/pskit.mjs` 只做 IO；`--help` 同步）：写盘沿用接收端那套纪律（临时名 + `renameSync`），`join` 的任何拒绝都 **exit 1 且一个字节不落盘**；不给 `--out` 时用 manifest 记的原文件名，但**那个文件已存在就拒绝**（不猜着覆盖，并明说"片子是验过的，要写就给 --out"）。原名只是默认输出名、**不进纸面**（D62 的口径不变）。
- **发送页那两句话从建议变成可执行命令**：`pageLimitHint()` 的第 ③ 条与 `earlySizePlan()` 的提前警告现在直接写 `node cli/pskit.mjs split …` 与 `join …`（`tools/smoke-sender.mjs` 新增断言钉住这两个命令名必须出现；旧的两条断言"不含 `改用 CLI`、不含 `页数不限`"仍然成立 —— 切分与"改用 CLI"是两件不同的事：前者真能解决，后者不能，因为 CLI 走同一个 `encodeTransfer`）。
- **入库的 keeper（不留"只有转录、新克隆复现不了"的证据）**：`tools/usability.ps1` 新增 **4b 腿**（`-SkipMultipart` 可跳过）：51,200 B 载荷 → `split --max-bytes 20000` 切 3 片 → **每片各走一次 send → `sim/channel.py`（seed 108/109/110，互不相同）→ `receive --photo` 回到 manifest 同一目录、同一个 part 名** → `join` → 与原文件 **SHA-256 逐字节比对**；结尾**阴性对照**：把收到的第 2 片中间一个字节 `xor 255`，要求 `join` **exit ≠ 0 且不产出文件**（否则"join 通过"就是空跑）。片用 20 KB 而不是默认 1.4 MB：默认值的边界算术在单测里（下面），而在冒烟里跑 3×1.4 MB 意味着 ~670 页信道仿真，会把一条几分钟的冒烟拖成十几分钟。**阴性对照没有塞进 `Step`**：`Step` 用 `& $Run *> $Log` 在当前作用域执行 scriptblock，里面写 `exit` 会把整个脚本一起带走（AGENTS.md 陷阱表）⇒ 改成内联判定，只看 exit code 与"有没有产出文件"，不看措辞。
- **`tests/unit/splitjoin.test.mjs`**（新，6 个用例）：0/1/6/7/8/9/1000 B 与 1.4 MB−1 / 1.4 MB / 1.4 MB+1 / 3×1.4 MB+5 的往返逐字节相同（**故意把磁盘顺序倒过来喂**，证明是 index 而不是列表顺序在决定拼接）；片不超上限、名字连续、视图不复制、`maxBytes` 非法（`0` / `-5` / `'abc'`）一律拒绝；**改一个字节 ⇒ 拒绝、`bytes` 为 `undefined`、点名 `part-001.bin`、`checked=1`**；**缺一片 ⇒ 点名 `missing part part-002.bin (index 2)`、`checked=2`**；截短 ⇒ 报 `part-002.bin is 10 B on disk but the manifest says 500 B`；**每份都自洽但不是这个 manifest 的文件**（换成另一份切分的片 / 只改 manifest 的 source 摘要）⇒ 全靠整文件摘要拦住（后者报 `every part verified, but together they are …`、`checked` = 全部片数）；manifest 本身不可信（`null` / `{}` / 版本不对 / `parts:[]` / 摘要不是 64 hex / 索引有洞 / 同一片列两次）⇒ 一律拒绝而不是猜；目录里多一个没登记的文件不影响结果。**每个拒绝用例都先跑一遍未篡改的阳性对照**，否则"永远拒绝"也能全绿。

**手工验过的真命令**（一次性助手 `.tmp/sj-help.mjs`，gitignored；它的摘要用 `node:crypto`，即**独立于 `core/hash.js` 的另一套实现** ⇒ 下面的 IDENTICAL 不是我们自己的哈希在自我同意）：`split 51200 B → 3 片（20000/20000/11200）+ parts.json 730 B`；`join → 51200 B`；`cmp ⇒ IDENTICAL`；三条拒绝路径全部 **exit 1 且无产出**（`Test-Path` = False）：默认输出名已存在、某一片改一个字节（点名 part-001.bin 并列出两个摘要、`1 of 3 verified`）、缺一片（点名 part-002.bin、`2 of 3 verified`）；`--help` 里 split/join 都在。**Level 2（真链路，不是本地拼接）**：3 片各自 `send`（5+5+4 = **14 张 A4**）→ `sim/channel.py`（seed 71/72/73）→ `receive` 各自 **MATCHES manifest** → `join` ⇒ **IDENTICAL**、`CMP_EXIT=0`。

**门限与复跑**：全套单测 **321 tests / 321 pass / 0 fail / 130.5 s**（315 → **+6** = 新增的 `splitjoin.test.mjs`）、`SUITE_EXIT=0`；`BUILD_EXIT=0`（单文件 `pskt-file.html` **318.4 KiB 未变**；**precache 54 → 55**，增量已查清 = `web/dist/core/splitjoin.js`：构建把 `core/*.js` 整目录拷进 dist，而 AGENTS.md 本就要求 `core/**` 是浏览器可用的纯 ESM ⇒ 它**应该**在 dist 里；它没进单文件 bundle，也没有任何页面 import 它，`check-dist` 的"0 external references"照旧）；`CHECKDIST_EXIT=0`（**13 pass / 0 skipped / 0 fail**、`G9 CHECK: all 13 assertions pass`）；`smoke-sender` **13 PASS / 0 FAIL**、`SENDER SMOKE: all assertions pass`、`SMOKE_EXIT=0`；`smoke-capture` `CAPTURE_EXIT=0`；**端到端 usability 23 PASS / 0 FAIL / 189 s**、`USABILITY_EXIT=0`（上一轮 **11 PASS / 124 s** ⇒ 多出来的 **12 步全是新的 4b 腿**：split + 每片 send/channel/receive ×3 + join + 逐字节比对 + 阴性对照）。**没回归**：纸面那条腿的数字与上一轮逐项相同（204800 B、3 页 PNG + `pack.pdf` 802 KB、`32ec480521da27d2` vs `32ec480521da27d2`），3D / G8 / smoke-sender / smoke-capture / check-serve / check-lan 全 PASS（dist 已重建 ⇒ 后两个查的是新站，即手机视角）；4b 腿自己：`split -> 3 transfers -> join came back identical (51200 B, sha256 5c7b2d98c8a5b06d)`、阴性对照 `exit 1, output written: False`、三片的 seed 108/109/110 互不相同；`node tools/check-docs-tables.mjs` ⇒ **clean，262 rows in 46 tables**（上一轮 256 / 45，增量就是本块那张表）。

**PLAN 未改（说明）**：`docs/PLAN.md` L137 那行 CLI 草图（`send|receive|watch|roundtrip|verify|bench|printpack|platepack|calibrate|status`）里本来就列着**没实现**的 `watch`/`bench`/`printpack`/`platepack` ⇒ 它是设计草图、不是现状清单，本轮不动它（也不动已批准契约里的任何判据）；**已实现的命令面**以 `pskit --help` 与 `docs/USE.md` 为准，两处都已同步。

**仍未做（如实）**：① 255 页上限本身没动（协议字段宽度，改它要动帧格式与已经印出去的纸）；② 发送页**没有**浏览器内的分片/多传输串联 UI —— `split`/`join` 在 CLI，用网页或手机接收下来的片子还得改名成 `part-NNN.bin` 才能 join（`docs/USE.md` 已如实写出这一步，没有假装它不存在）；③ 600 dpi 那种"根本上更便宜的读法"未做（见上，也不主张它一定可行）；④ **M4 仍卡在 G4**（手机 500×8 只有用户能跑）⇒ **本轮不打 tag**。

### 第 71 轮（**发送页把 core 的英文 RangeError 原样丢给用户，而"一次最多 255 页 ≈ 1.52 MB"这个产品最重要的实用限制没人说清（D65）**）

- **缺口**：一次传输的天花板不是浏览器给的、是**协议**给的 —— 页头 `totalPages` 只有**一个字节**（`core/frame.js:19`：`totalPages (u8), <= 255`）⇒ 最多 **255 页**；`core/protocol.js:318-320` 先扣页间校验页、扣到没位置就抛 `RangeError: too many pages: N data pages leave no room for parity`。**网页发送端把这句英文原样丢给用户**，再附一条**不含任何数字**的通用提示（"换更大剖面、调低校验页 %，或先分割文件"）⇒ 用户不知道自己差多少、上限在哪、哪个旋钮管用；而 **CLI 自第 47 轮就有可操作的拒绝语**（`needs 1120 pages > 255 (inter-page RS limit): shrink payload or use a denser profile`）⇒ 缺口只在两条网页路径。`docs/USE.md` L91 也只含糊写"最大 payload ≈ 每页净字节 × 255"——**这句还高估了**：校验页要占位置，默认 20% 校验页下真实上限是 **212 个数据页**。
- **只读探针量出真实天花板**（两个都是一次性、未入库；永久版是 `smoke-sender` 的断言）：`.tmp/probe-capacity.mjs` ⇒ `P-M1-300` 几何 **319×216 格 × 1 bit = 8613 B 原始容量**、`parityPct 20`；`.tmp/probe-encode-limit.mjs` ⇒ **0.5 MiB → 84 页 ✓、1 MiB → 168 页 ✓（177 ms）、2 MiB → 48 ms 抛（280 数据页）、4 MiB → 93 ms 抛（559）** ⇒ 每数据页净 **7514 B**（`geom.ecc.dataBytes`，与第 47 轮记的数字一致 ✓），默认档一次最多 **1,592,968 B ≈ 1.52 MB**、校验页调到 0% 是 **1,901,042 B ≈ 1.81 MB**。
- **本轮我先写错一次，被自己的冒烟拦住（如实记）** ✗：第一版加的是 `MAX_WEB_PAGES = 600` 的**渲染预算**闸门，理由听着对（渲染同步、每页 PNG 常驻 ⇒ 大文件压死标签页），但两件事让它站不住：① **它永远不可能触发** —— core 在 255 页就拒了，600 页闸门在默认档下是**死代码**；② 它的提示写着"改用 CLI……**页数不限**"⇒ **假话**（CLI 走同一个 `encodeTransfer`、同样撞 255）。冒烟那条**真实调用**先报 `stage=encode`（不是预期的 `stage=budget`）才把这两点暴露出来；而它报 `encode` 的直接原因是**我拿 `new Uint8Array(4 MiB)`（全零）当"大文件"**⇒ 被本项目自己的 deflate 压成 **6 页**（另测 8 MiB 全零 = 10 页）⇒ **载荷错，不是产品错**。同一个事实还推翻第一版的第二处设计：我原想在**读入文件之前**按字节数拒绝，理由是"按每页原始容量算是页数下界、不会误拒"⇒ **压缩比没有上界 ⇒ 那不是下界** ⇒ 会把一个 20 MB 但压得动的日志**误拒**。
- **修法（三层各司其职，拒绝权只交给掌握事实的那一方）**：① 删掉不可能触发的 600 页闸门与那句假的 CLI 承诺；② 新增纯函数 **`transferBudget(byteLength, geom, parityPct)`**，**逐项镜像** `core/protocol.js:307-321`（`dataPages = ceil(n / geom.ecc.dataBytes)`、`parity = max(2, ceil(dataPages*pct/100))`、总量夹在 u8 上限内），并**反解**"当前配置下最大 payload"（从 253 往下走，问编码器会问的同一个问题）；几何未知 ⇒ 返回 `null`（**不给句子，而不是猜一个**）；③ **`pageLimitHint()`** 把 core 的英文 RangeError 翻成中文并**带数字**：上限来历（`core/frame.js:19` 的 u8）、当前档每页净 7514 B、这个文件需要几页（数据页 + 校验页）、当前配置最多约 1.52 MB、三条出路（**调低校验页 %** ⇒ 0% 时约 1.81 MB，代价是丢页恢复能力下降 / **换每页装得更多的档** / **切成几份分别传**，每份各有自己的摘要校验），并**明说"CLI 受同一个 255 页限制，改用 CLI 解决不了这一条"**；④ **`earlySizePlan()`** 在**读文件之前**只**警告、绝不拒绝**（读入是一次性同步分配、渲染也是同步的 ⇒ 先把数字给出来；≥64 MB 另提醒"读入本身可能卡死页面"—— 该阈值是**判断不是实测**，注释写明本机没有浏览器可测，且它只产生句子）；⑤ `stage:'encode'` 分支只在错误文本匹配 `too many pages` 时换提示，**`error` 原文一字不动**（一句坏解释不该顶掉一个真错误），且 `planPage` 若在失败路径上又抛就**退回原来的通用提示**。
- **`docs/USE.md` 三处更正**：发送页那句改成"**一次传输最多 255 页**（默认约 1.52 MB 不可压数据；压得动的文件可以更大）"；L91 的"× 255"改成"× **能留给数据页的页数**"并给实测数字（212×7514 = 1,592,968 B；0% 时 253×7514 = 1,901,042 B；1 MiB → 168 页 ✓、2 MiB → 48 ms 被拒）；故障排查新增一条（含 CLI 拒绝语原文、三条出路、以及"为什么读入前只警告不拒绝"）。
- **验证**：`smoke-sender` 新增 **3 条断言、每条带阳性对照**，且**镜像是拿编码器自己的输出钉住的**（不是测试里另算一遍的算术）：① 1 MiB xorshift ⇒ 编码器产出 **168 页**、`transferBudget` 说 **168**（140 数据 + 28 校验）；2 MiB ⇒ 编码器抛 **"too many pages: 280 data pages"**、预算说 **280 数据页 / 336 总页**超 255；`PROTOCOL_PAGE_LIMIT === 255`；20% 校验页允许 **1,592,968 B** < 0% 的 **1,901,042 B**（证明"调低校验页"是真旋钮）；几何未知 ⇒ `null`。② 4 MiB ⇒ **警告**（含 255 / `core/frame.js:19` / 每页净，**不含"不渲染"、不含"改用 CLI"**：这条路不许拒绝，也不该指向假出路）；**4096 B ⇒ 必须安静**（阳性对照：每次三页传输都警告 = 教会用户无视日志）；几何未知 ⇒ 安静。③ 真实调用：4 MiB xorshift ⇒ `stage=encode`、**98 ms**、`error` 仍是 core 原文，`hint` 含 255 / `core/frame.js:19` / 每页净 / **"CLI 受同一个 255 页限制"** / "解决不了"，且**不含"页数不限"**；**4 MiB 全零 ⇒ 仍编出 6 页**（证明不误拒压得动的大文件）；`pageLimitHint(…, null, …) === ''`。⇒ **`SENDER SMOKE: all assertions pass`、13 PASS / 0 FAIL（原 10 + 新 3）、`SMOKE_EXIT=0`**；`BUILD_EXIT=0`（`pskt-file.html` **318.4 KiB**、precache **54** 未变 ⇒ 没新增 core 文件）；`CHECKDIST_EXIT=0`、**13 pass / 0 skipped / 0 fail**、`every id unique within its own page, across 4 built page(s)`、**`G9 CHECK: all 13 assertions pass`**；全套单测 **`tests 315 · pass 315 · fail 0 · duration_ms 132415`**、`SUITE_EXIT=0`；`smoke-capture` **`CAPTURE_EXIT=0`**（stats `{"frames":4,"accepted":3,"duplicates":1,"rejected":0}` 不变）；**端到端 usability 冒烟 11 PASS / 0 FAIL / 124 s**、字节相同（`32ec480521da27d2` vs `32ec480521da27d2`）、`USABILITY_EXIT=0` ⇒ **dist 重建后重跑的理由照旧：`check-serve` / `check-lan` 查的正是 dist（手机视角）**。
- **仍未做（如实）**：**255 页这个上限本身没动** —— 它是帧字段宽度，改它要动帧格式并且让已印出的纸作废 ⇒ 不是本轮该碰的；发送页也**没有**"切成几份、多传输串联"的 UI（目前要用户自己切）。**门限不变**：本轮没碰解码/渲染/信道 ⇒ **G6 仍 🟡**（600 dpi **31786 ms/页** = 判据的 15.9 倍；用户口径"不必 600 dpi"属于**缩小判据范围**，那是产品负责人的决定、不是我的 ⇒ 不记绿）；**M4 仍只差 G4（要你的手机）⇒ 不打 tag** ✓

### 第 70 轮（**发送页的大产物下载会静默失败（D63）：不动下载机制，把风险与"不经浏览器"的出路如实说出来**）

- **缺口**：`give()` 是发送页上**唯一**的下载出口（L410 zip、L415 pack.pdf、L420 3mf/stl 三处调用都走它），它先把整个产物变成 JS 字符串、再 `btoa`、再拼进 `data:` URL ⇒ 168 页的 zip（约 63 MB）意味着 **~295 MB 的瞬时字符串**（UTF-16 中间串 2n + base64 两份 2×4n/3 ≈ 产物的 **4.7 倍**），而**页面无法知道下载有没有成功**（浏览器不给"文件已落地"这个事件）⇒ 失败模式是"什么都没发生"= **静默失败**，与 D58 同一形状。第 68 轮把这条记在"仍未修"里，但只记了体量、**没记它会静默** ⇒ 本轮补上并修掉。
- **为什么不直接换 `blob:`**：`URL.createObjectURL` 更稳，但 `send.html` 的 CSP 与真机行为**本机没有浏览器可验** ⇒ 盲改可能把现在能用的弄坏（第 68 轮已按这个判断记账）⇒ 本轮**不动下载机制**，只把风险与出路如实说出来；换 `blob:` 仍归入需要真浏览器的 **G9** 那批。
- **修法（与 D61 同一套做法）**：新增纯函数 `downloadPlan(name, byteLength)` + 导出阈值 `DATA_URL_RISK_BYTES = 32 MB`（DOM 段进程内不可达 ⇒ 策略抽成纯函数才验得到）。**阈值是判断、不是实测**（注释写明它设在"瞬时字符串几百 MB、手机标签页可能死掉"处），并且**低于阈值一律保持安静** —— 每次三页传输都警告 = 教会用户无视日志。`give()` 在**尝试之前**提示：文件名 + MB 数 + "页面无法知道下载有没有成功（浏览器不给这个事件）" + **不经浏览器的出路**（`node cli/pskit.mjs send 你的文件 --profile P-M1-300 --format png,pdf --out 目录`）+ "把文件切小、分几次传"。提示不含 markdown（`say()` 写 `textContent`，L277）。
- **验证**：`smoke-sender` 新断言**带阈值两侧的阳性对照**（802 KB 与 `DATA_URL_RISK_BYTES - 1` 必须**不留提示**、63 MB 必须留且**点到文件名 / MB 数 / CLI 命令**、阈值**含等号**）⇒ **`PASS sender says when a data: URL download is too big to trust, and stays quiet when it is not`**、`SENDER SMOKE: all assertions pass`、`SMOKE_EXIT=0`；`BUILD_EXIT=0`（precache 54）；`CHECKDIST_EXIT=0`、**13 pass / 0 skipped / 0 fail**、`every id unique within its own page, across 4 built page(s)`、**`G9 CHECK: all 13 assertions pass`**；全套单测 **`tests 311 · pass 311 · fail 0 · duration_ms 132890`**、`SUITE_EXIT=0`；**端到端 usability 冒烟 11 PASS / 0 FAIL / 122 s**、字节相同（`32ec480521da27d2` vs `32ec480521da27d2`）⇒ **本轮重跑冒烟的理由不只是它内部会跑 smoke-sender：dist 被重建了，而 `check-serve` / `check-lan` 检查的正是 dist（手机视角）**。`docs/USE.md` 故障排查加了对应一条（"点了下载、文件却没出现"⇒ 不是解码失败 + CLI 出路）。
- **60 min soak 仍在跑**（第 68 轮起的，目的是把"收敛到工作集"与"慢泄漏"分开）：本轮开始时 **21.8 min / 101 cycles / 1006 页解码 / 0 误接受 / 0 错误** ⇒ 判决**本轮后半已收，见下面两条"同一轮后半"**。**M4 仍只差 G4（要你的手机），不打 tag** ✓

- **同一轮后半 · D55 的根因查清并修（D64）**：挂了三轮的那条 `joinCellLevels: colour level 2 out of range 0..1` 的机制是——`nearestInk`（`ideal.js` L417-428）返回的是 **palette 里的下标**，`joinCellLevels`（`protocol.js` L63-73）按 **geom 声明的档数**校验，而 `planPage`（`profiles.js` L107-164）**根本不吃 palette**、`candidatePlans`（`bootstrap.js` L40-82）又把 profile × `INK2/INK4/PAPER1` **无核对地叉乘** ⇒ 只读探针 `.tmp/probe-d55.mjs` 数出 **30 个 profile×palette 组合里 2 个真不一致**（`P-C4-600` 与 `PL-D2` 配 INK4：2 档 < 4 墨；192 个候选里约 11 个落在这两组）。**探针第一版报了 20 个 MISMATCH，是我自己的假阳性**（把"没有 colour 通道"当成"0 档"；`P-M1-300`/`P-M1-600`/`P-M2-600`/`PL-G`/`PL-M1`/`REL-H1` 根本没有 colour 通道 ⇒ `ideal.js` L317 的 `if (colourChannel)` 不成立 ⇒ **300 dpi 纸面主路走不到这条抛出**）✗ 如实记。**修法不是删候选**：编码器印的是 `pal.inks[level % inks.length]`（`palette.js` L68、`render/raster.js` L144、`mesh/plate.js` L300）⇒ 2 档 + INK4 只用 ink 0/1 ⇒ **这样的页用户真能造出来**（`cli/pskit.mjs` L40 帮助文本明写 `--palette INK2 INK4 PAPER1`、L131 显式参数优先；`web/sender.js` L366 有色板下拉框、L167 `pickPalette(profileId, opts.palette)`）⇒ 删候选会**改判、丢真页** ⇒ 改成**只在"该通道能寻址的墨"里找最近**（`nearestInk(rgb, pal, limit = pal.inks.length)`，L317 传 `colourChannel.levels`）：**墨数 ≤ 档数时是空操作**（本来不会抛的组合一个判决都不改）、墨数 > 档数时与编码**严格对称**。**顺带查出共用仲裁路径也暴露在同一条异常下**：`recalibrate.js` L210 把 `colourLevels[i]` 直接喂给 L211 的 `joinCellLevels`，而 `ideal.js` L319-323 在 `shapeLevel === null` 时**跳过 join 却已写下 `colourArr[i]`** ⇒ 桌面接收 / 手机连拍 / G2 都走的这条路，第 65 轮的兜底**没包住** ⇒ 修在源头后两处一起消失（**不在两处各加钳位**：钳位会把"量到第 4 种墨"谎报成"第 2 档"，正是本仓库禁止的"看起来成功但是错"）。**回归测试 `tests/unit/nearest-ink.test.mjs` 4/4 全过**，含**阳性对照**（`nearestInk([20,20,20], INK4) === 3` 且 `joinCellLevels({shape:1,colour:3}, planPage('P-C4-600'))` 抛 `colour level 3 out of range 0..1` ⇒ **先证明缺陷真实存在**，再谈修好）、限制后 **5832 个采样色**全部落在 0..1 且 `doesNotThrow`、在**真实候选表**上"墨数 ≤ 档数"的组合限制前后**逐个相同**（并断言比较过 **> 20 个**组合，防这条测试变空跑）、以及"不一致的组合**恰好是那 2 个**"（将来新增 profile/palette 再引入不一致会**当场红**）⇒ 全套单测 **`tests 315 · pass 315 · fail 0 · duration_ms 135379`**（311 + 4）、`SUITE_EXIT=0`；`BUILD_EXIT=0`（precache 54）、`CHECKDIST_EXIT=0`（13 pass / 0 fail、**`G9 CHECK: all 13 assertions pass`**）；端到端 usability **11 PASS / 0 FAIL / 123 s**、字节相同。另：`inkSpread` 一并参数化（它也调 `nearestInk`，否则报告与判决各说一套），并发现它**全仓无调用点**却在注释里自称 "kept and exercised" ⇒ 新测试把它真正 exercise 了，那句注释重新变成真的；`bootstrap.js` 的 try/catch **保留**、注释改成"不再为这一例承重"。
- **同一轮后半 · 60 min soak 判决收了**（第 68 轮起的那次，目的是把"收敛到工作集"与"慢泄漏"分开）：`.tmp/g6-60-r68.json`，**60.02 min、`ok:true`、`SOAK60_EXIT=0`、281 cycles / 2782 页解码 / 2248 次腐蚀试验**；编码 **199.4 ms ≤ 5 s ✅**、解码 **worst 1783.0 / mean 1599.9 ms ≤ 2 s ✅**（源仍是三份真实 300 dpi 信道语料）、**完整性 ✅：误接受 0 / 摘要不符 0 / 抛错 0 / 页拒绝 0 / 仲裁再读触发 0**、**RSS `growthPct = +3.16% ≤ 10% ✅`**（基线 532.31 → 终值 549.14 MB；peak 681.4 / floor 462.9 MB ⇒ ±100 MB 锯齿再次确认；旧窗口同时算/打印/写盘但**不判**：`narrowGrowthPct +3.93%`）、`cycleMsEarly 12867.5` vs `cycleMsLate 12581.5` ⇒ **吞吐没退化，后期反而快 2.2%**。**但"无泄漏"仍不主张（如实）**：Q1..Q4 中位数 **532.31 / 538.63 / 542.84 / 549.28 MB**、增量 **+6.32 / +4.21 / +6.44 MB** ⇒ **60 min 内没收敛**；而 30 min 那次是 **+4.4 / +4.1 / +0.7**（看着像收敛）⇒ **两次形状不一致 ⇒ "像收敛"这个读法本身就不可靠**；线性外推 ~+5 MB/15 min ⇒ 约 **2.5–3 h** 会撞上 10% 预算 ⇒ **判据在 PLAN 规定的时长内通过，而"长时间会话不长"没有被证明**。**两个平凡解释已排除（读代码，不是猜）**：① 不是 soak 自己攒记录（留存千字节级：`firstErrors` ≤5、281 个 cycle 时间、281 个 RSS 样本、~2782 个 per-page 计时）② 不是语料缓存边跑边填（`readCorpus` **L124 一次性读入全部 PNG**、`corpora` **L140 在循环开始前**建好、页位图每次用时 `decodePNG`（**L93**）现解即弃）⇒ 残差只剩"V8 在 ~350 MB/cycle 分配速率下高水位漂移"或"core 里真有 ~6 KB/页留存（17 MB / 2782 页）"⇒ **60 min 分不开这两者，两个都不主张**。**决定不拉长到 3 h**：判据那一档（30 min）在 30 与 60 min 都过，为一个判据已经回答的问题再花 3 小时机器时间属于过度设计（用户口径"刚好就行"）⇒ 记为**观察**并留线索：将来若有人报告多小时会话内存上涨，从这里查。判决已同时写进 `docs/ACCEPTANCE.md` G6 的新条目 **③bis**（兑现 ③ 结尾那句"结果出来再写"）与 **D59** 行尾。
- **M4 仍只差 G4（要你的手机），不打 tag** ✓

### 第 69 轮（**手机用户拿回的文件叫 `.bin`、打不开（D62）：默认名一个字不动，给用户一个可选的名字输入，策略做成纯 core 模块 + 单测**）

- **缺口在哪**：两条网页接收路径都硬编码 `pskt-${len}B-${digest12}.bin`（`web/app.js` L116、`web/capture.js` L248）。**默认名本身是对的**——页头没有文件名字段（`core/frame.js` 只有 magic..length..pages..digest..crc16），`app.js` L113-115 的注释也写明"唯一诚实的默认名是从字节导出的，**用户之后自己改名**"⇒ 缺陷是**那半句在手机上不成立**：iOS/Android **靠扩展名决定用什么打开**，`.bin` 没有对应处理器 ⇒ 摘要核对通过、字节一致，**然后打不开**，还得进系统文件管理器改名（多数人不知道扩展名才是关键）。**CLI 没这问题**（`receive --out <file>`，`cli/pskit.mjs` L47）⇒ 缺口只在网页两条路径，而手机端正是目标里的路径 ②。
- **动手前先确认了三件决定成败的事实**（否则一定踩坑）：① `app.js` 在源码树里就写 `./core/…`（L11-19，bundler 的特例解析）② `capture.js` 写 `../core/…`，`tools/build-web.mjs` L378 有专门改写 `'../core/` → `'./core/`、L277 把它**排除在 bundle 之外**（原样拷贝）③ **`tools/smoke-capture.mjs` L27 会从 Node 加载 `web/capture.js`** ⇒ 它的**静态** import 必须在源码树里可解析 ⇒ 只能写 `'../core/naming.js'`；它内部那些动态 `'./core/…'` 是**相反的拼法、只在 dist 里跑得通** ⇒ 两种拼法都承重，注释里已写明别"顺手统一"。
- **修法**：新增**纯 core 模块** `core/naming.js`（Node 与浏览器同一份、零依赖）= `digestName` / `sanitizeFileName` / `downloadName` / `MAX_NAME_LEN`；两条网页路径都改成调它，**默认名逐字节不变**；`web/index.html` 两处各加一个**可选**输入（`#outname` + 显示"将保存为：X"的 `#outname-note`、连拍节 `#burstname`），并写明"名字只用于本机命名下载，不发送、不进页码"。规则：路径分隔符 / Windows 保留字符 / 控制字符剥掉；**任何以点开头的输入一律回退**（`../../etc/passwd` 去掉分隔符后仍以点开头 ⇒ 回退到摘要名，而不是"清洗成一个看着像的名字"）；保留设备名（`NUL`/`COM1`/`lpt9`）回退；超长**截词干、保扩展名**；**只填 `.pdf`**（手机用户最可能干的事）读作"把默认名的 `.bin` 换掉" ⇒ 词干仍由摘要导出、不发明任何东西。**策略放 core 而不是每页各写一份**，是因为两份清洗规则一定会漂移到其中一份放出穿越路径，而 `download` 属性是要落盘的、不是能临场发挥的地方。
- **验证**：新单测 `tests/unit/naming.test.mjs` **4/4 全过**（`NAMING_EXIT=0`）——第一条是**阳性对照**：留空时必须与两个页面原先硬编码的字符串**逐字节相同**（页面在进程内加载不了 ⇒ 默认名悄悄变了别处不会报警）；另一条拿 **18 个恶意输入**断言**非空 / 无分隔符 / 无 `..` / 不以点开头 / ≤120 字符 / 无非法字符**，外加截断保扩展名与 `.pdf` 语义。全套 **`tests 311 · pass 311 · fail 0 · duration_ms 134726`**（307 + 4 ⇒ 数目对得上，没有测试被挤掉）、`SUITE_EXIT=0` ✓ `BUILD_EXIT=0`：precache **53 → 54**、单文件页 **306.4 → 315.2 KiB**（naming 已内联）、`web/dist/core/naming.js` 6554 B、`dist/capture.js` 的 import 已改写成 `'./core/naming.js'` ✓ `CHECKDIST_EXIT=0`、**13 pass / 0 fail**、`every id unique within its own page, across 4 built page(s)`、**`G9 CHECK: all 13 assertions pass`**（新增三个 id 都在构建页里且各页唯一）✓ `smoke-capture` 全过（4 帧 / 3 收 / 1 重复 / 0 拒）✓ **端到端 usability 冒烟 11 PASS / 0 FAIL / 126 s**（改完才跑）⇒ **协议、页图、摘要、判据一律未动：不改信道一个比特**。
- **插曲（如实）**：第一次跑单测用 `node --test tests/unit/naming.test.mjs`（漏了 `--test-isolation=none`）⇒ `spawn EPERM`（runner 为每个文件 spawn 子进程、沙箱禁管道 stdio），`AGENTS.md` 早写着这条 ⇒ **是调用方式错、不是测试错**；按文档加 flag 后 4/4 过。
- **60 min soak 仍在跑**（第 68 轮起的，目的是把"收敛到工作集"与"慢泄漏"分开）：到 **18.6 min / 86 cycles / 858 页解码 / 0 误接受 / 0 错误** ⇒ 判决**下一轮收**（没跑完就不写）。**M4 仍只差 G4（要你的手机），不打 tag** ✓

### 第 68 轮（**发送页给每页都建一张 base64 预览 ⇒ 第 66 轮从 pack.pdf 移走的开销搬回了 DOM（D61）；顺带查出我上一轮那条打印提醒排在花钱之后；并收了 G6 的 soak 判决：`PASS G6`，三项第一次同源**）

- **上一轮挂账的这条这轮修了，而且量级比记账时大**：第 67 轮记的是"168 页 ≈ 79 MB base64 串 + 59 MB PNG"——**只算了字符串**。这轮读 `web/sender.js` L296-343 时把**浏览器解码**那一半也算进来：A4/300dpi 一页 = 2480×3508 px ⇒ 一张 RGBA 位图 **≈34.8 MB**（与第 66 轮 `tools/sender-memory-probe.mjs` 在 Node 侧对同一批位图实测的 **30.6 MB/页 RSS** 同量级）⇒ 168 页预览是 **GB 级解码位图**，不是 79 MB；而 `docs/USE.md` 明说发送页在手机上"也能开" ⇒ **手机发送端首当其冲**（`loading="lazy"` 只是提示、不是承诺）。
- **还查出我上一轮自己留下的次序错**：第 67 轮给 `doprint` 加的"页数多会很慢"提醒排在 `window.open` + `document.write` **之后**（L326 花钱、L340 才说话）⇒ **钱花完了才提醒**；而 168 页 ≈ **5.8 GB** 解码位图塞进一个窗口时，那个标签页很可能根本回不来提醒任何人 ⇒ 正是本项目最不允许的**静默失败**。**如实：这是我上一轮的修法本身的缺陷，不是新引入的。**
- **修法（两条策略都抽成纯函数）**：`previewPlan` / `printPlan` + 导出的上限常量 ⇒ DOM handler 在 `document` 守卫后面、进程内不可达，**抽出来才验得到**，这正是本文件头部写明的分工原则（`buildArtifacts` 当初就是这么来的）。① 预览上限 `PREVIEW_CAP = 8`，超出的页数**如实说明**（"预览只画了前 8 页，还有 160 页没有预览……**所有页都在 pack.pdf 和 PNG（zip）里，一页不缺**；要逐页看请下载它们"），并加 `loading="lazy"` + `decoding="async"` 当提示（注释写明**不指望它**）② `doprint` 改成**花之前先决定**：`printPlan` 返回 `write:false` 就**直接拒绝**，报出 GB 数字与 pack.pdf 出路（`PRINT_WINDOW_PAGE_CAP = 8`）⇒ **拒绝不是放宽判据**：pack.pdf 页数不限、且本来就是"每页按真实物理尺寸放置"的那个产物，`docs/USE.md` 也叫用户印它。
- **验证**：`smoke-sender` 两条新断言**都带阳性对照**（3 页必须全预览且不留提示、页数正好等于上限必须仍可打印、0 页也要可打印 ⇒ 否则一个"什么都拒绝"的策略也能把断言蒙过去）：**`PASS sender previews are capped, and the cap says what it skipped`**、**`PASS sender refuses the browser-print window before spending, not after`**、`SENDER SMOKE: all assertions pass`、`SMOKE_EXIT=0`；`BUILD_EXIT=0`（`pskt-file.html` 306.4 KiB、precache 53）；`CHECKDIST_EXIT=0`（`13 pass / 0 skipped / 0 fail`、`every id unique within its own page, across 4 built page(s)`、**`G9 CHECK: all 13 assertions pass`**）；单测 **`tests 307 · pass 307 · fail 0 · duration_ms 132528`**、`SUITE_EXIT=0` ✓ `docs/USE.md` 两处同步（预览上限、打印按钮 ≤8 页与拒绝理由）⇒ 行为写在用户会看的地方，不只写在台账里。
- **仍未修（如实记账，并入 G9 那批真浏览器验收）**：`give()` 用 data: URL 下载 ⇒ 168 页的 zip ≈ 63 MB、base64 后是**单个 ~84 MB 的 data: URL**；`blob:` + `URL.createObjectURL` 更稳，但要动页面 CSP，而**本机没有浏览器可验** ⇒ 不改（盲改可能把现在能用的弄坏，比不改更糟）。
- **端到端 usability 冒烟：11 PASS / 0 FAIL / 122 s** ✅（**改完才跑**——它内部会跑 `smoke-sender` 与 `smoke-capture`，先跑就等于拿旧状态当证据）：204800 B 文件进 → 3 张页图 + 802 KB `pack.pdf` 出 → `sim/channel.py --preset scan300 --modifier nocrop` 模拟印扫 → `receive --photo` → **字节相同、sha256 相同（`32ec480521da27d2`）**；另外跑了 **G8**（盘 profile 的 4 个 3MF）、`check-serve`（字节一致 / 拒绝目录穿越 / 404 / HEAD / MIME）、`check-lan`（手机视角的 `web/dist`）⇒ 122 s 对比基线 297–326 s，**D54 的加速在这里也看得见**（冒烟内部同样在解码真页）。它自报的边界照实记：真墨水纸张、真手机相机、浏览器打印缩放（D8）、https 下的 PWA 安装、以及 **G4 / G6 / G9 / G10 未证**。
- **G6 的 soak 判决收了：`PASS G6`（第 67 轮修好仪器后的重跑，`.tmp/g6-30-r67.json`）** ⇒ **30.2 min / 141 cycles / 1401 页解码 / 1128 次腐蚀试验**：编码 **203 ms ≤ 5 s ✅**、解码 **worst 1663 ms / mean 1569 ms ≤ 2 s ✅**、**RSS 507.3 → 516.3 MB = `+1.78%` ≤ 10% ✅**（peak 640.7 / floor 436.1 MB）、**完整性 ✅（误接受 0 / 摘要不符 0 / 抛错 0 / 页拒绝 0）**、30.2 min ≥ 30 ✅、`ok:true`、`SOAK_R67_EXIT=0` ⇒ **G6 三项第一次同源于同一次跑**（第 66 轮记的"三项不同源"缺口闭合）。**旧尺子不稳的旁证**：同一次跑里旧窗口算出 `narrowGrowthPct = +2.96%`，而上一轮**同代码同旧尺子**是 **+13.98%** ⇒ 差 11 个百分点。
- **但过了判据 ≠ 无泄漏（自我限制，写在这里免得日后被当成结论）**：**Q1..Q4 中位数 507.3 / 511.7 / 515.8 / 516.5 MB 是单调爬升的**，而 soak 工具自己的注释就写着"泄漏表现为这四个数单调爬升"⇒ 增量在收窄（**+4.4 / +4.1 / +0.7 MB**）⇒ 形状像**收敛到工作集**、不像线性泄漏，可**形状判断不是证明** ⇒ 已起 **60 min 跑**（PLAN 允许 30–60 min，`.tmp/g6-60-r68.json`）把"收敛"与"慢泄漏"分开，**结果出来再写**；在拿到它之前，台账只说"30 min 判据通过"，不说"无泄漏"。
- **G6 整条仍 🟡**：600 dpi 解码 **31786 ms/页**（超预算 15.9 倍）⇒ **不因用户说 600 dpi 非必要就记绿**（收窄判据范围是产品负责人的决定）。**M4 仍只差 G4（要你的手机），不打 tag** ✓

### 第 67 轮（**手机连拍的取景画面接错了元素：`index.html` 里有两个 `id="video"`（D57）；顺带确认接收端没有 D56 那类内存问题**）

- **修后代码上的完整 soak 仍在后台跑**（第 66 轮结束时起的），A/B 两段已在**修后代码**上出数：**编码 1 MB → 168 页 194 ms ≤ 5 s ✅**、**解码 9 张真实信道页 worst 1681 ms / mean 1587 ms ≤ 2 s ✅**（修前那次报的是 8850 ms ❌）⇒ C 段（30 min 的 RSS 与误接受）结束时收；**G6"三部分不同源"这条缺口将由它闭合**（本轮**不写判决**：没跑完就不写）。
- **D56 那一类问题往接收端查了一遍 ⇒ 接收端干净 ✓**：`web/app.js` 的 `run()` 里 `bmp` 是**循环局部**（一次只解一张），`files` 是文件选择器给的 **File 句柄（磁盘惰性、不是内存副本）**，摄像头单拍路径还先 `files.length = 0` 再塞一帧 ⇒ 发送端那个"每页位图全程留住"的对应物**不存在**；连拍侧的状态只有 `sessions: Map(session → {totalPages, have:Set})` + 计数器（第 66 轮已查）⇒ **每帧内存有界** ✓
- **但顺手读出 D57：`web/index.html` 声明了两个 `id="video"`** —— 第 1 节单拍的 `<video id="video" playsinline hidden>`（L33）与最后一节连拍的 `<video id="video" playsinline muted>`（L82）⇒ 同一文档重复 id，而 `getElementById('video')` 只返回**文档顺序里的第一个** ⇒ `web/capture.js` 的连拍拿到的是**隐藏的单拍元素**：**用户在连拍节看得见的那块画面永远黑屏**，而帧是从一个 `display:none` 的 video 上 `drawImage` 取的 ⇒ 浏览器相关（**iOS Safari 对不渲染的 video 是否继续解码并不可靠**）⇒ 手机路径（目标 ②）最坏是"画面全黑、一页也收不到"，而**在桌面上永远看不出来** ✗ 重复 id 同时在**单文件版** `pskt-file.html` 里（构建时由 `index.html` 生成）⇒ **两条交付路径都带着**。
- **它为什么活了这么多轮**：既有那条检查只查"JS 里每个 `getElementById` 都能在**某个**页面找到"，把各页 id 混进一个 Set、**只看存在不看唯一** ⇒ 这类"元素在、但取到的是另一个"的静默失败没人管；而唯一能暴露它的证据（真手机跑连拍）**正是本项目在进程内造不出来的那一种** ⇒ 所以这次把它变成构建期检查，而不是只改那一行。
- **修法 + 免费的阳性对照**：① 连拍节元素改名 `id="burst-video"`、`capture.js` 改取它，并在注释里写明**为什么不能"顺手统一"成一个 id** ② `tools/check-dist.mjs` 新增**第 13 条**断言 `no built page declares the same id twice`（**逐页**数 id 出现次数、>1 就红并报出页名与次数）⇒ 与图标那条同档次的**构建期强制** ✓ **阳性对照**：当时的 dist 还是修前构建的 ⇒ **先不重建**直接跑 ⇒ `CHECKDIST_PRE_EXIT=1`、`FAIL no built page declares the same id twice`、detail **`./index.html: video x2, ./pskt-file.html: video x2`**、`G9 CHECK: 1 of 13 FAILED` ⇒ 缺陷确实在**出厂产物**里、新检查确实抓得住（**不是空跑**）✓ **重建后**：`every id unique within its own page, across 4 built page(s)`、`13 pass / 0 skipped / 0 fail`、**`G9 CHECK: all 13 assertions pass`**、`CHECKDIST_EXIT=0`、`BUILD_EXIT=0`（precache 53）✓
- **手册跟着改**：`docs/USE.md` §5 的 G4 行点名该按哪个按钮（拉到页面最下面「手机连拍（自动挑帧）」按「**开始连拍**」，**不是**第 1 节那个「用摄像头拍一页」——那是一次一页的手动快门，500 页要按 500 次 ✗）⇒ 用户照手册走 G4 不会走错路。
- **验证**：单测 **`tests 307 · pass 307 · fail 0 · duration_ms 130716`**、`SUITE_EXIT=0` ✓ `smoke-capture` **`CAPTURE SMOKE: all assertions pass`**（4 帧 / 3 收 / 1 重复 / 0 拒）✓ `build-web` exit 0 ✓ `check-dist` 13/13 ✓
- **接着查出 D58（桌面发送端，同一轮）**：`dlpng` 原本**一次点击触发 N 个自动下载**（`for (const p of state.pages) give(p.png, …)`）⇒ 浏览器会拦批量下载 ⇒ 一次 168 页的传输可能只落地 1–2 个文件，而日志声称"**N 张 PNG 逐个下载**"（**一句本代码无法核实的陈述** = 静默的部分成功 ✗ 本项目最不允许的那类）⇒ 改成**一个 zip、一次下载**：用本仓库自己的 `buildZip`（`core/mesh/threeMF.js`，3MF 就是它打的 ⇒ **零新依赖**；PNG 已压缩 ⇒ `method:'store'`），按钮标签 `PNG` → **`PNG（zip）`**，日志改成**可核实**的陈述（条数 + 字节数）。
- **独立读取器对拍**（那个 handler 在 `document` 守卫后面、进程内点不到 ⇒ 这就是可自动化范围内的等价证据，用的是 G8 验 3MF/STL 的同一套纪律：交给第三方实现读）：`tools/sender-zip-check.mjs`（**入库量具**：跑的就是 handler 里那条表达式，一次入库、随时可复跑）以**与 handler 完全相同的表达式**造归档 ⇒ `pages 5; zip 1832839 B; magic PK`；`python -c "import zipfile,hashlib; …"` ⇒ **`entries 5`、`testzip() = None`（每条 CRC 都过）、`compress_type=0`（store ✓）、5 个 sha256 与我方逐个一致**（如 `page-000.png 373254 B a4c6b3aa40ddb02f…`）✓
- **`doprint` 加如实提示**：它把**每页**当 `data:` URL `<img>` 写进一个新窗口再 `window.print()`（每页 ~0.5 MB base64、解码后 ~30 MB 位图）⇒ 168 页就是 **D56 那个量级的开销从打印这条路回来**，而 `window.print()` 没有惰性路径可用 ⇒ **不拒绝**（几页时它是对的、按 CSS 声明的物理尺寸打印），但**超过 8 页（≈240 MB 解码位图）就明说代价并指向 pack.pdf**（与 `send.html` 既有那条"打印请优先用 pack.pdf"的 notice 同口径）。
- **构建守卫当场抓到我一次**：第一版图省事写了 `await import('./core/mesh/threeMF.js')`（照 `web/capture.js` 的样子）⇒ `tools/build-web.mjs:73` 直接红：`dynamic import() is not supported by the web bundler (make it a static import)`、`BUILD_EXIT=1` ⇒ **守卫是对的**（那个 `'./core/…'` 说明符在源码树里也是错的，源码树里是 `../core/`）⇒ 改顶部静态 import（L33 本来就在从同一模块导入 `encode3MF, selfCheck3MF`），并在注释里写明**别改回惰性**。**差点被绿字骗过去**：构建红的那一次 `check-dist` 仍报 `13 pass / 0 fail`、`G9 CHECK: all 13 assertions pass`，因为它跑在**上一次成功构建的 dist** 上 ⇒ 那份绿**不是**本次改动的证据（"只看 exit code"这条老规矩这次救了我 ✓）
- **最终版验证（改成静态 import 之后全部重跑）**：`BUILD_EXIT=0`（`pskt-file.html` 306.4 KiB、precache 53）· `CHECKDIST_EXIT=0`、`13 pass / 0 skipped / 0 fail`、**`G9 CHECK: all 13 assertions pass`**、`every id unique within its own page, across 4 built page(s)` · `SMOKE_EXIT=0`（**`SENDER SMOKE: all assertions pass`**）· `SUITE_EXIT=0`、**`tests 307 · pass 307 · fail 0 · duration_ms 131458`** ✓
- **修后 soak 跑完 ⇒ 三项里有两项在修后代码上直接过了，第三项被仪器绊住（D59）**：`.tmp/g6-30-postfix.json` = **30.2 min、141 cycles、1401 页解码、1128 次腐蚀试验**（对比修前那次 30.7 min 只 25 cycles / 257 页 ⇒ **同样时间做了 5.6 倍的活**，D54 在真实循环里的旁证）⇒ **编码 194 ms ✅**、**解码 worst 1681 ms / mean 1587 ms ≤ 2 s ✅**、**完整性 ✅（误接受 0 / 摘要不符 0 / 抛错 0 / 拒绝 0）**，而 **RSS 报 `+13.98% > 10%` ❌**、`ok:false` ⇒ 与修前那次的 **+2.50% 通过**互相矛盾，必须查清。**查法**：把日志里 29 个每分钟样本按窗口取中位数 ⇒ 0–3 min **581.9** / 3–8 min **510.6** / 8–20 min **512.8** / 20–31 min **515.45** MB、min/max **449.3 / 641.4** ⇒ **第 3–31 分钟几乎全平（+0.95% / 28 min）、头 3 分钟反而更高** ⇒ **不是泄漏**，也**不是热身不足**（本轮最先猜的正是热身不足 ⇒ **被自己的数据推翻** ✗ 如实记）；真机制 = 窗口分辨率：一个循环从 ~80 s 变成 ~13 s 后，"前/后 10% 样本"只覆盖 ~3 min，**短于一次 GC 锯齿**，而 ±100 MB 的锯齿叠在 ~515 MB 基线上（±20%）远大于 10% 的判据余量 ⇒ 两次跑各抽到锯齿的一个相位。**修法：判据一个字不动**，窗口改**四分位**（每侧 ≈7.5 min），并**同时算/打印/写进 JSON 旧窗口的统计量**（`narrowGrowthPct`，标注 printed-not-judged）+ **Q1..Q4 中位数**与 **peak/floor**（泄漏 = 四个数单调爬升）⇒ 换窗口不藏任何数、旧数字仍可跨运行比较；30 min 重跑已起（`.tmp/g6-30-r67.json`），**判决下一轮写**。
- **改仪器时先短跑一遍，结果短跑暴露了一条更糟的旧缺陷（D60）**：`tools/soak.mjs` 原判 `minutesOk = elapsedMin >= Math.min(minutes, 30) * 0.98` ⇒ 1 分钟试跑打印 **`PASS G6: … 1.2 min >= 30 min`**（自相矛盾的假绿），而 `verify --gate G6 --minutes 1` 是**文档可达**的命令 ⇒ **一条要求 30–60 min 的门限可以被 72 秒的跑判绿** ✗ 旁边注释还写着 `a 30 min soak must actually be 30 min`（**注释对、代码没做**，与 D54 同族）⇒ 修成常量下限 `MINUTES_REQUIRED = 30`（**`--minutes` 只能加长、不能缩短判据**），复验为 `FAIL G6: … 1.2 min < 30 min` + `note: a short run cannot pass G6, only fail it`、`SHORTSOAK_EXIT=1` ✓ 同一次短跑还抓到**我自己刚写的错**：两处编辑变量名对不上（`narrowPct` vs `narrowGrowthPct`）⇒ `ReferenceError` 发生在**最后写 JSON 那步** ⇒ 若没先短跑，30 min 的跑会**跑满才崩、白跑半小时**。
- **对拍脚本入库**：`.tmp/zipcheck.mjs` 升成 **`tools/sender-zip-check.mjs`**（只读量具、不是门限）⇒ D58 的证据不再只活在我这棵树里（**D53 的教训**：别让人依赖只有我这儿才有的东西）；入库后自跑复验 `ZIPCHECK_EXIT=0`、Python 侧 `entries 5 / testzip None / compress_type=0 / 5 个 sha256 全等` ✓
- **同轮查出但未修（挂账，带口径）**：编码完成后 DOM 仍给**每页**建一个 `<img src="data:image/png;base64,…">` 预览（`sender.js` L297-306）⇒ 168 页 ≈ 79 MB base64 串 + `state` 里留着的 PNG ≈ 59 MB；桌面浏览器能承受、**手机发送端会难受**（`docs/USE.md` 说发送端在手机上"也能开"）⇒ 它**不是静默失败**（页都在、字节都对），本轮没动；下一轮的最小修法 = 预览截到前 N 页并明说"还有 M 页未预览"，不改任何产物。
- **如实**：本轮修掉的是三个**确定的错**（连拍取景接错元素 ⇒ D57、PNG 批量下载静默丢文件 ⇒ D58、以及被构建守卫抓住的惰性导入），**不等于连拍与发送在真机浏览器上已达标** ⇒ **G4 与 G9 仍需你的真机/真浏览器**；600 dpi 解码仍 31786 ms（判据没分档 ⇒ **不收窄、不记绿**）；D55 根因仍无正例 ⇒ **M4 仍只差 G4，不打 tag** ✓

### 第 66 轮（**收了 G6 的 soak 判决；顺着"手机端 500 页会话"查内存，量出网页发送端发一个 256 KiB 文档就要 1.34 GB（D56）**）

- **上一轮记下的第一件事先做掉：G2 300 dpi × 200 份在修后代码上重跑** ⇒ **`PASS G2 corpus: 200/200 byte-exact in 1084.2s`**、**200 行 `OK` / 200 行 `digest verified` / 含 FAIL 的行 = 0**、exit 0 ⇒ **D54 的"重排不改判决"不再是"论证 + 6 页实证"，而是 200 份语料的实证** ✓ 顺带：同一门限第 57 轮记的是 1245.4 s ⇒ 本轮 **1084.2 s**（白跑的整页读少了，门限自己也快 13%）。（该次运行**只评估 G2**，它自己打印 `not evaluated by this run: G0 G1 G3 G4 G5 G6 G7 G8 G9 G10` ⇒ `ALL GATES PASS` **仍不得**被引用成"全部门限通过" ✗）
- **G6 的 soak 判决落地（`.tmp/g6-30.json`）⇒ ③ soak ✅**：**30.7 min、25 cycles、257 页解码、200 次腐蚀试验** ⇒ **RSS 634.5 → 650.3 MB = +2.50%**（判据 ≤10%）、峰值 738 MB、**误接受 0 / 摘要不符 0 / 抛错 0 / 页拒绝 0**、仲裁再读触发 0 次 ⇒ 上一轮"不默默挑基线"的悬念有了答案：**按事先写进代码的那个统计量判，它自己就过了**（中途 581–738 MB 的起伏是 GC 锯齿、不是泄漏）。该跑在修 D54/D55 **之前**的代码上 ⇒ 它自报的 `worst decode 8850 ms > 2000 ❌`、`ok:false`、`SOAK30_EXIT=1` **原样保留、不改写**，性能以修后实测（1467 ms/页）为准 ⇒ **如实缺口：三部分不同源**，还差一次修后代码上的完整 soak（≈33 min）。判决已写进 `ACCEPTANCE` G6 段（权威源），门限表那行只作导航。
- **D56（本轮真正的修复）：网页发送端内存随页数线性膨胀，量出来比推算还难看。** 起点是替手机端 500 页会话查"第 63 轮给每页新增的 ρ 数组会不会被留住"：**连拍收集器不留**（状态只有 `sessions: Map(session → {totalPages, have:Set})` + 计数器，`page`/`bmp` 都是 `addFrame` 的局部 ⇒ 每帧内存有界 ✓ **无缺陷**），但顺手看到 `web/sender.js` 的 `pages.push({tag, bitmap, png, …})` **留住每页码区 RGBA**，而 `encodePDFDocument(pages.map(p => p.bitmap))` 还要**一次拿到全部**（`pdf.js` 内部先 `list.map` 存住每页 `pixels` 引用、再 `forEach` 编码）⇒ 新增只读量具 `tools/sender-memory-probe.mjs`，**同一命令修前修后各跑一次**：

| 载荷 | 修前 rss / arrayBuffers / 结果里留住的位图 | 修后 | `pack.pdf` sha256 |
|---|---|---|---|
| 64 KiB → 11 页 | 436 MB / 420 MB / **11 of 11** | **181 MB / 166 MB / 0 of 11** | `b6908642072c1ec4` **未变** |
| 256 KiB → 42 页 | **1343 MB** / 1300 MB / **42 of 42** | **160 MB / 92 MB / 0 of 42** | `a61aa12fc4121bf0` **未变** |

  ⇒ 每页代价 **30.6 → 2.4 MB**；按**实测**每页代价外推：1 MB（168 页）修前 ≈ **5.2 GB**、`docs/USE.md` 宣称的 255 页上限 ≈ **7.9 GB** ⇒ **一个 256 KiB 的普通文档就要 1.34 GB，标签页必死、手机更早死** ✗ 而 `heapUsed` 全程只有 **5 MB**（位图在 `arrayBuffers` 外部内存里）⇒ **只盯 JS 堆永远看不见**（`ACCEPTANCE` 里"50 次连跑 heap Δ 0.0 MiB"那条历史证据因此被标注为不足以证明不漏）。
- **修法（三处，都小而准，没有顺手重写别的）**：① `core/render/pdf.js` 的 `encodePDFDocument` 改成**单趟消费惰性序列**（数组或任何可迭代对象都收），每页对象发出后**不再引用它的 `pixels`**，`/Kids` 由轻量 `kids[]` 构建；空列表的拒绝从"编码前"移到"序列取完后"（错误与消息不变、仍然不出文件）② `web/sender.js` **边 yield 边把 `p.bitmap` 置 null**（位图只是中间产物，用户下载的是 PNG 与 pack.pdf）③ `tools/smoke-sender.mjs` 的盲解码从 `p.bitmap`（裸码区）改成**解 `p.png`**（用户真正打印的那张纸，页边距与套准/裁切标记一起进判据 ⇒ **更强、不是放宽**）。**产物逐字节未变**：`pack.pdf` 与 page-000 PNG 的 sha256 修前修后**完全一致**（`4c81684db5c2862f` / `b64aea45b9790cf2`）⇒ 改的是内存、不是输出 ✓
- **顺带修掉第 63 轮漏掉的第八个调用点**：`smoke-sender.mjs` L61 仍在直接 `asm.feed` ⇒ 改成 `feedPageWithRecalibration(asm, boot.page, {geom: boot.geom})` ⇒ 上轮那份"七个调用点"的清单本身少算了一个，如实记在这里。
- **验证**：单测 **`tests 307 · pass 307 · fail 0 · duration_ms 133078`**、`SUITE_EXIT=0`（`pdf-truesize` 那组独立确认 PDF 字节未变）✓ `build-web` exit 0 ✓ `check-dist` **13 pass / 0 fail** + `3 page(s), 3 candidate tries … digest matches` + **`G9 CHECK: all 12 assertions pass`**、exit 0 ✓ `smoke-sender` **`SENDER SMOKE: all assertions pass`**、exit 0（P-M1-300 与 PL-D2 改成盲解 PNG 后摘要仍对上，`f2e430ae873fbb54` 两侧一致）✓
- **未做（如实）**：修后代码上的完整 soak（≈33 min）· 600 dpi 解码 31786 ms（用户判非必要、但判据没分档 ⇒ **不收窄、不记绿**）· D55 根因仍无正例 · G4/G9/D8/G10 仍需你的硬件（清单在 `docs/USE.md` §5）⇒ **M4 仍只差 G4，不打 tag** ✓

### 第 65 轮（**实现 G6 ⇒ 它当场量出两件真事：解码超预算 4.4 倍（D54）、产品解码路径会抛未捕获异常（D55）**）

- **为什么做 G6**：M4 只剩 G4（需真机、只有用户能跑）⇒ 转去做**不需要硬件、只缺实现**的那条门限（此前记"进程内实现尚无 ✗"）。判据照抄 `docs/PLAN.md` L106、不发明也不放宽：编码 1MB ≤5s、解码 ≤2s/页、30–60 min soak 且 RSS 增长 ≤10%、零误接受 ⇒ 新增 `tools/soak.mjs`（可独立跑，也可 `verify --gate G6`；**不进 `all`**，理由写在 `cmdVerify` 注释里：判据本身就是 30–60 min 的后台任务，塞进例行基线会让每次跑半小时、并把快门限的回归埋在里面）
- **工具自己先被抓到一个 bug**：1 分钟试跑报 `soak ran 0.0 min, 0 cycles`、RSS 全 NaN ⇒ 根因不是 deadline 写在哪，而是**热身吃掉了预算**（一个 warm 循环要解 9 张真实信道页 ≈ 80 s）⇒ 改成热身之后再起算（判据要的是 30–60 min 的 **soak**，不是"含热身的 30 min"）
- **① 编码 ✅**：1 MB → 168 页 **197 ms**（预算 5000 ms）；另报不判：render+PNG **202 ms/页**
- **② 解码：先量到超预算 4.4 倍，再修到进预算（D54）**：新只读量具 `tools/g6-perf-probe.mjs` 把真实信道 300 dpi 页的 **8713 ms** 分解成 `readFileSync 4` + `decodePNG 393` + `bootstrapDecode 8316` ⇒ **inflate 不是瓶颈、别去优化 PNG**；`bootstrapDecode` 里**三次白跑的整页读各 2365/2407/2391 ms**、全死在 `stage=readout reason=echo-bad-magic`，候选是 `P-C4-600@600/{INK2,INK4,PAPER1}`，而赢家是 `P-M1-300@300/INK2` ⇒ `candidatePlans()` 先按 medium 再**按字母**排（`'P-C4' < 'P-M1'`），而 `bootstrap.js` 文件头声称 "ordered by likelihood" ✗ **修法 = 按位图自身隐含的码区尺寸给候选打分再排序**（`abs(ln(w/lw)) + abs(ln(h/lh))`、稳定排序、原次序作 tie-break），**只重排、不删除任何候选** ⇒ **判决不可能变**：候选要赢必须让页头自报的 profile/nozzle 与该候选一致（交叉校验），所以"第一个可能被接受的候选"与顺序无关 ⇒ 变的只有毫秒 ✓ **实测 8713 → 1467 ms、尝试 4 → 1**（pristine 7180 → 952 ms）⇒ **进 2 s 预算** ✓ 重建 dist 后 `check-dist` 的无提示解码独立确认：**candidate tries 12 → 3**、摘要仍对、`all 12 assertions pass`、exit 0 ⇒ **出厂产物带着修法且判决未变** ✓ **600 dpi 侧仍 31786 ms/页**（同 dpi 的 `P-C4-600` 与 `P-M1-600` 码区尺寸相同 ⇒ 尺寸信号分不开、稳定排序回落字母序；再降需要第二个廉价信号，而 600 dpi 是用户第 63 轮判定**非必要**的路径 ⇒ 本轮不做）
- **③ 顺带量出 D55（比延迟更要紧）**：600 dpi 信道页让 `bootstrapDecode` **抛未捕获 `RangeError`**（`joinCellLevels: colour level 2 out of range 0..1` ← `ideal.js:353` ← `page.js:63/47` ← `bootstrap.js:128`）⇒ 每候选的 `planPage`/`pageLayout`/`getPalette` 都被 catch 并记成失败尝试，**唯独 `decodePage` 没有** ⇒ 在浏览器与手机连拍里这是**接收过程中崩掉**、不是拒绝 ✗ 修法 = 把它也纳入每候选 catch、记成失败尝试后**继续搜索** ⇒ 同一页现在 `ok=true`、判定 `P-M1-600@600/INK2`、抛出的候选如实留痕（`[1] … stage=decode ms=6335 reason=fail THREW: joinCellLevels: …`）✓ **根因仍 OPEN**（只在错候选下出现、没有能在正确候选下触发它的正例 ⇒ 不猜着改判决路径）
- **advice 守卫第 3 次抓到我**：为 D55 记失败尝试时我写了裸字面量 `reason: 'fail'` ⇒ 套件红 2 条（`reasons without advice: fail` / `unmapped failure reasons … fail`）⇒ 查清后发现**这个 reason 早就可达**（旧代码 `reason: r.reason || 'fail'`），只因是表达式形态、扫描器看不见 ⇒ **守卫是对的、缺口是旧的** ⇒ 补 `'fail'` 的 ADVICE 条目（写清它是 per-candidate、页级终态是 `no-geometry-matched`）；**没有**用"改回表达式形态"绕过（`bootstrap.js` L153-160 明写那种做法是"换个写法当作修好" ✗）⇒ 复跑 **`tests 307 · pass 307 · fail 0 · duration_ms 136665`**、`SUITE_EXIT=0` ✓ `build-web` exit 0 ✓ `check-dist` `13 pass / 0 fail` + `all 12 assertions pass` ✓
- **soak 判决待落**：30 min 跑在修 D54/D55 **之前**启动（进程已载入旧模块）⇒ 它的 **RSS 增长 / 零误接受**那半证据仍成立（循环机器一行未改），而它的性能数属修前 ⇒ 性能以本轮修后实测为准；结束时读 `.tmp/g6-30.json`、判决写进 `ACCEPTANCE` G6 段（**本轮不写判决**：没有结果就不写）
- **未跑（如实）**：G2 的 200 份语料重跑（≈20 min）⇒ "重排不改判决"目前是**论证 + 6 页实证**（`check-dist` 3 页 + 探针 3 页，含 600 dpi）⇒ **下一轮第一件事**
- **门限**：G6 三部分 = 编码 ✅ / 解码 300 dpi ✅·600 dpi ✗ / soak 待落 ⇒ **整条仍 🟡**（**不因用户判 600 dpi 非必要就把它记绿**：判据范围是产品负责人的决定，不是我在门限表里能默默收窄的）；**M4 仍只差 G4** ⇒ 不打 tag ✓

### 第 64 轮（**照用户手册从全新检出走一遍 ⇒ 撞到 D53：`check-dist` 的夹具只有开发树才有；顺带更正四处过期记账**）

- **做法**：把 **123 个被跟踪文件**复制成 `.tmp/fresh64`（等价全新 clone：无 `web/dist`、无 `.tmp`；`git clone` 本地路径在本沙箱会因 git spawn `sh.exe` 报 `couldn't create signal pipe, Win32 error 5` ⇒ 改用不 spawn sh 的等价做法，**不绕沙箱**），然后**照 `docs/USE.md` 字面**在该树里跑 §0 与 §4
- **撞到什么（D53）**：§0 的 `node tools/build-web.mjs` **exit 0**（55 文件、29 模块、precache 53），紧接着 `node tools/check-dist.mjs` **exit 1**：`FAIL bundle decodes on-disk pages unhinted, digest matches manifest` / `.tmp/g2src has no manifest.json` / `G9 CHECK: 1 of 12 FAILED` ⇒ 而手册同一段正写着"判'构建好没好'**只看 exit code**" ⇒ **真用户会以为自己的构建坏了** ✗ 根因：那条断言把 `.tmp/g2src`（历年轮次产物、被 `.gitignore` 忽略）当成了环境的一部分
- **对照（证明坏的不是产物、也不是手册其余步骤）**：同一棵 fresh 树按 §4 跑 `& .\tools\usability.ps1` ⇒ **11 项全过、297 s、exit 0、字节相同**（`sha256 32ec480521da27d2` 两侧一致；含 `send → sim/channel.py → receive --photo → 逐字节比对`、板材 `3mf/stl → G8`、`smoke-sender`、`smoke-capture`、`check-serve`、`check-lan`）⇒ **用户路径在全新检出上本身是通的** ✓
- **修法**：**不跳过**（那会掏空唯一一条"出厂 bundle 真能读盘上 PNG"的断言，而该文件头部注释明写"全跳过的自检不算通过"）⇒ **让断言自带夹具**：manifest 缺失时用本仓库自己的编码器进程内生成（`encodeTransfer` 20480 B **确定性**填充 → `pageLayout(geom,300)` → `renderPageBitmap` + `renderSheetBitmap`（整张纸、与打印产物同形）→ `encodePNG` → 写 `manifest.json`），并在记录行**明说来源**；夹具用 **core** 造、解码仍用 **bundle**（`R(...)`）⇒ 不让被测产物自造输入，而 core 与 bundle 若不一致正好在这条断言上暴露
- **顺带补上第 63 轮漏掉的第七个调用点**：该断言原先直接调 `asm.feed` ⇒ 改成**从 bundle 取的** `feedPageWithRecalibration`（遵守它自己的自律条款"只用产物暴露的东西"，且产物若不含该模块就会红）⇒ 上轮的调用点清单里 `tools/check-dist.mjs` **就在名单上、我漏改了** ✗
- **三次验证**：① fresh 树夹具缺失 ⇒ `G9 CHECK: all 12 assertions pass`、**exit 0**、`3 page(s), 12 candidate tries, geometry self-identified as P-M1-300@300/INK2, 20480 B, digest matches` + `[fixture generated in-process this run: a fresh clone has no .tmp]` ② fresh 树夹具已在 ⇒ 同样全过、**不带**该标注（两条分支都验过）③ 本树（`.tmp/g2src` 原有 204800 B 语料）⇒ 仍 `all 12 assertions pass`、exit 0、`204800 B` ⇒ **老路径无回归** ✓
- **更正四处过期记账**（每一处都会让下一轮去做已经做过的事 ✗）：里程碑表 **M4**（"由子代理在写"⇒ `sim/channel.py`/`ref/decode.py` 早已落地并在用）、**M5**（"未开始"⇒ 3MF/STL 每轮由冒烟实际产出并过 G8）、**M7**（"未开始"⇒ `pskit calibrate` 第 61 轮已落地）、**M8**（"未开始"⇒ web PWA + `check-dist` 13 pass 每轮在跑）；门限表 **G8 行**仍写"D41 OPEN"⇒ D41 **第 39 轮已闭**（`threeMF-xsd-parity.test.mjs`）⇒ 原句划掉并注记、**不改写历史**
- **套件**：全量重跑 **`tests 307 · pass 307 · fail 0 · duration_ms 134453`**、`SUITE_EXIT=0`（条数与第 63 轮相同 ⇒ 本轮没加测试；`tools/check-dist.mjs` 不被单测覆盖 ⇒ 它的证据是上面那**三次实跑**，不是"应该没问题"）
- **门限**：本轮 `core/` 一行未动（改的是 `tools/check-dist.mjs` 与四份文档）⇒ G2/G5 判决沿用第 63 轮（G2 300 dpi ✅ `200/200`、600 dpi **无判决**（用户判定非必要）、G5 ✅ **0 误接受**含接缝 2000 次）；**G9 的产物级半本轮重跑三次全绿**（含 fresh 树 ⇒ 首次证明"全新检出也绿"）；浏览器内的 G9 仍需真人点一次 ✗
- **M4 现在只差 G4**（手机 500 页 × 8 轮，需真机 ⇒ 只有用户能跑；步骤与验收清单在 `docs/USE.md` §5）⇒ **M4 未闭合 ⇒ 仍不打 tag** ✓

### 第 63 轮（**D51 修法落地：RS 仲裁的重读 —— 300 dpi 侧重跑仍 200/200、G5 含新接缝 0 误接受、塌陷页在真语料上被救回；D52 同轮修完**）

- **改了什么**：新模块 `core/decode/recalibrate.js` —— `estimateRhoCut`（本页 ρ 直方图的**并列平台中点**作两簇切点；低簇 = level 0 是字母表性质、不需真值；`levels ≠ 2` / ρ 恒定 / 可测格 <64 一律**拒绝并说明理由**，退回今天的读法）、`recalibrateLevels`（**按通道名**继承原值、只重判 shape ⇒ 第三通道不会被静默清零；ρ 不可测的格保留匹配滤波的答案、**不猜**）、`feedPageWithRecalibration`（先走原路径；**仅当** `intra-fail` 才重读；重读要先过页内 RS，后面还有帧 CRC 与全载荷 SHA-256）⇒ **无新魔数、不放宽任何判据**，与 `core/decode/echo.js:40-51`（CRC16 裁决候选切点）同构
- **六个调用点全改**（漏一处产品与判据就分家）：`cli/pskit.mjs` receive + calibrate、`tools/g2-corpus.mjs`（**G2 门限 harness**，救回数记进 `pageClasses` 与 `rescued`）、`web/app.js`（桌面/文件接收）、**`web/capture.js`（手机连拍：`feed(page,bmp)` 不传几何，而 `addFrame` 内总是先 decode 再 feed ⇒ 暂存本帧 `boot.geom`，不改契约、不加机制）**、`tools/smoke-capture.mjs`（两处真路径闭包）
- **G2 300 dpi 侧（验收计划的非回归条）✅ `PASS 200/200 byte-exact in 1245.4s`、`exit 0`** ⇒ 不是"按构造不会变"的声称，是重跑出来的
- **G2 600 dpi 侧：最终判决没有取 —— 重跑由用户中止（不是跑绿了 ✗）**：进行到 **52/200（51 OK / 1 FAIL）**时用户明示「其实没必要 600dpi，只要能够读到就行」⇒ 我 `job_kill` 停掉它（按当时速率还要约 3 h）。**这是产品负责人对范围的判定、不是放宽判据**（G2 仍按 100% byte-exact、失败页仍全进分母、语料/信道参数/三角点拒绝一字未动）。已见事实照记：页级失败原因 `echo-bad-magic 19`、`no-hollow-corner 12`、`echo-header-crc 5`，而**旧跑 210 次的 `assemble/intra-fail` 在这 52 份里 0 次** ⇒ 修法命中的正是塌陷类；那三类是读出前的别的通路、本修法不覆盖 ⇒ **600 dpi 在新读法下没有门限判决**，要用它得自己重跑 `verify --gate G2 --root .tmp --match 'sc-scan600-*'`（约 3 h）；**D49 降为"不阻塞可用（用户判定非必要）"，历史实测 `162/200 = 81%` 保留、不改写成达标** ✓
- **G5 ✅ 两半都过、`exit 0`**：原 **10000** 试验的配比与 PRNG 序列**一字未动** ⇒ `9200 精确恢复 / 800 拒绝 / 0 误接受`、守卫分布 `feed:intra-fail=3408`、`refused:digest-mismatch=800` 等与第 57/58 轮**逐字相同** ⇒ 核心改动没扰动协议守卫 ✓ 新增**独立接缝试验 2000 次**（跑在 P-M1-300、ρ 描述"第三种读法"⇒ 给重读最大的作恶机会，并断言 `shapeLevelsOf === 2` 防退化）：**0 误接受**、**重读被触发 2000 次**（反空跑守卫 `seamRetried > 0`）⇒ 结局只有"精确原字节"或"拒绝" ✓ **别误读**：那 2000 次全部"精确恢复"是**跨页奇偶**兜住了被 RS 拒掉的重读页，不是重读修好了 2000 页 ✗
- **页级直证**（`pskit calibrate`，与第 62 轮同一批语料、同一批页）：`sc-scan600-11` **三页全部读出**（p1 救回 cut 1.4725、簇 1.260/1.686、sep 3.54 ↔ 第 62 轮 3.61；p2 救回 cut 1.2862、簇 0.905/1.562、sep **14.82** ↔ 第 62 轮 **14.82**），页内预算 0.0%/8.6%/0.0%；`sc-scan600-10` p1 救回（cut 1.5244、sep **5.53** ↔ 第 62 轮 **5.53**、259991 格改判），p0/p2 仍 `echo-bad-magic`（别的通路 ✓）；`sc-scan300-10` **三页全干净、零救回** ⇒ 300 dpi 侧页级无扰动 ✓
- **其余执行到的证据**：新单测 `tests/unit/recalibrate.test.mjs` **6 条全过**（含**朴素 Otsu 阳性对照**：naive 切点 <0.0021、错 >50%，而估计器 **0 错** ⇒ 第 62 轮量到的陷阱被复现并被守住）；全量套件 **`tests 307 · pass 307 · fail 0 · duration_ms 144010`**、`SUITE_EXIT=0`（+6 条）；usability 冒烟**全过**（与门限并发那次 331 s；**针对最终代码重跑亦全过：11 项 PASS、326 s、exit 0**，`bytes came back identical 204800 B sha256 32ec4805…`、`CAPTURE SMOKE: all assertions pass`）；`build-web` exit 0（**29 模块**、单文件 301.5 KiB、precache 53、原子换站）；`check-dist` **13 PASS / 0 fail** 含 `G9 CHECK: all 12 assertions pass`；**产物核对**：`web/dist/core/decode/recalibrate.js` 12389 B、离线单文件页 `pskt-file.html` 内含 `feedPageWithRecalibration`/`estimateRhoCut`/`UNIMODAL`/`plateauHi`、`web/dist/capture.js` 含 `feedPageWithRecalibration`/`frameGeom` ⇒ 用户打开的产物带着修法 ✓
- **本轮自造并已修**：救回提示原本打印在本页状态行**之前**又不带页名 ⇒ 读起来像在评论**上一页**，**它当场把我骗了**（我一度以为 `echo-bad-magic` 的页被"救回"）✗ ⇒ 改成自报 `文件名 (page N): RESCUED` 并重跑 `calibrate` 核对归属 ✓ 教训：**歧义的成功输出就是错的输出**，它会顺着人写进台账
- **D52 同轮修完（判决一行未动）**：新增与判决无关的 `inkBalance.rhoMedian`（跨页可比、单调于渗墨）、`shapeMonoFrac`（**塌陷页 = 1.0**，真正的塌陷探测器）、`ratioNulls`/`nanCells`/`shapeCounts`；`ratio` 保留原名但注释改成实测结论（分母用被选中电平的 target ⇒ 跨页不可比）；每格 ρ 与色彩判决以**紧凑数组**留下（600 dpi ≈1.4 MB，而 `cells` 对象 ~260 MB ⇒ 只有数组离开函数）；色彩判决提前做、但 `inks.push` 范围一字未动（`colourAlive` 是判决不是报告）
- **不打 tag**：M4 未闭合（600 dpi 侧判决还没落）⇒ 本轮不打 tag ✓

### 第 62 轮（**D51 的机制量到了：塌陷在匹配滤波、ρ 信息仍在 ⇒ 修法形态定为"RS 仲裁的重读"；另记 D52**）

- **新量具** `tools/rho-report.mjs <pristineDir> <channelDir> <pages>`：把信道页的 **ρ 分布**与同一页原始渲染对拍，报 ①产品实际用的阈值（`noDot 0.1017 / mid[0] 0.1850 / blob 1.2800 / targets [0, 0.3699]`）②按真值分组的 ρ 分位数 ③比值路径**单独**的判决直方图 ④最终（MF 覆盖后）错误率 ⑤**oracle 最优切点** ⑥**无监督 Otsu 切点** + 簇均值/sd/分离度/谷底深度/少数类占比 ⑦与 **RS 上限** `(nsym/2)/(k+nsym)`（600 dpi = **6.30%**、300 dpi = **6.32%**）的比较 ⇒ 自带**镜像对照**（探针的 `findMarkers → rectifyPage → readPageIdeal` 与 `decodePage` 的 levels 实测 `0 differing level(s) of 287507` ⇒ 不是平行实现 ✓）与**零退化对照**（把原始渲染当自己的信道页）；**只量不改、不是门限**（`exit 0` 只表示报告跑成了）
- **零退化对照先纠正了我自己**：pristine 上 ρ 两簇 = **0.0021 / 0.3741**、`targets [0, 0.3699]`、比值路径 **0 错**、`ratioDisagreements 0`、`inkBalance.ratio 1.0657`(600 dpi)/`1.0197`(300 dpi) ⇒ **`measureTargets` 与 `analyseCell` 口径一致** ⇒ 本轮中途我说的"两者口径不一致、比值路径每页都坏"**撤回** ✗ 教训：**没有零退化对照就没资格说"口径不一致"**
- **机制（9 个信道页 + 2 个对照）**：信道一介入 ρ 被抬 **4–20 倍**（300 dpi **完美解码页** 0-like p50 `0.5939`/1-like `1.5750`；600 dpi 健康页 `0.6164`/`1.7289`；600 dpi **塌陷页** `1.2684`/`1.6824`；pristine `0.0021`/`0.3741`）⇒ 固定切点 `mid[0]=0.1850` 在**任何**信道页上错 **60.1–90.6%**、在 pristine 上错 **0%** ⇒ **产品今天完全靠 MF 覆盖比值判决** ✗ 而**塌陷发生在 MF**：塌陷页 `final levels = 1 × 287507`、ρ 两簇却仍分开（分离度 `3.61–5.53`）⇒ 渗墨把每格**内部**填满 ⇒ "环+点"模板对每格都比"仅环"更贴合（MF 的自由增益吸收**幅度**、吸收不了**形状**）⇒ 与 `sim/selfcheck.py:836`"ρ 穿过固定窗口量、而墨并不待在那儿"一致 ✓
- **可修性（本轮最重要的数字）**：**无监督** Otsu 切点（低簇 = level 0 是字母表性质、不需真值）在**全部 9 个信道页**上把逐格错误压到 **0–208 格（≤0.072%）**、**含三个塌陷页**（`sc-scan600-11` p1 = 208、p2 = 1、`sc-scan600-10` p1 = 1）⇒ 远低于 RS 上限 **6.30%** ⇒ **这些页本来救得回来**；对照：固定阈值 **77.1–90.6%**、产品现状 **87.0–90.4%** ✓
- **量到的陷阱（不许照抄朴素 Otsu）**：退化的 pristine 直方图（两簇 sd = 0.0000 的 δ 尖峰）上，"取第一个最大类间方差"把切点放到 `t=0.0018`（**低于** 0-like 尖峰 0.0021）⇒ **60.102% 错误** ✗ ⇒ 切点必须落在**谷底 / 并列平台中点**，否则会**打破今天完美的 pristine/fast 路径** ⇒ 第 11 轮"判据须跨 seed 稳定、不能只对自己挑的页成立"的实例 ✓
- **修法形态定了（下一轮实现，本轮不动判决）**：**RS 仲裁的重读** —— 先走现有 MF 路径（今天能解的页**逐格不变** ⇒ 300 dpi 侧按构造保持 **200/200** ✓），**仅当**页内 RS 判 `intra-fail` 时用按页标定的 ρ 切点重读一次，**只有 RS / 帧 CRC / SHA-256 通过才接受** ⇒ 仓内同构判例 `core/decode/echo.js:40-51`（把硬编码 `>6000` 换成"枚举候选、由条自身 CRC16 裁决"，并写明"**这不是放宽接受**……G5 保证与从前相同"），而页级仲裁者比 CRC16 强得多 ⇒ **无新魔数** ✓ 必须同时改的调用点：`cli/pskit.mjs` receive(L483/507) + calibrate(L1239/1258)、**`tools/g2-corpus.mjs`(L232/245) = G2 门限 harness（漏改就会让产品与判据分家 ✗）**、`web/app.js`、相关 smoke/diag ⇒ 并**回 G5 重验误接受 + 重跑 G2 两侧**（600 dpi 侧约 50 min ⇒ 后台跑）
- **新记 D52**：`inkBalance.ratio` 的分母用"被选中电平"的 target ⇒ **被自己的判决污染**（塌陷页反而最低 `2.71–3.52`、健康页 `11.57–20.70`、pristine `1.02–1.07` ⇒ 跨页不可比），而 `ideal.js:300-305` 正把它当"bleed 诊断量"；`ratioDisagreements` 只在两条路径**都表态**时计数（`ideal.js:275`）⇒ **塌陷页 0、健康页 4 万～26 万** ⇒ 数值大 = 正常、0 = 可能已塌，与直觉相反 ✗ 两者都不参与判决（**不造成误接受** ✓）但会把下一个修 D51 的人引错 ⇒ 修法只动"报告量与注释"，须在 G2/G5 重跑**之前**定稿
- **门限不动 · 不打 tag**：本轮**没跑门限判决**（`core/` 一行未动 ⇒ G2/G5 前提不变）⇒ G2 仍是 **300 dpi ✓ 200/200 · 600 dpi ✗ 162/200 ⇒ 🟡 不上调**；**误接受仍为 0** ✓；全量套件 **`tests 301 · pass 301 · fail 0 · duration_ms 131660`**、`SUITE_EXIT=0`（条数不变：本轮只加量具与文档、未加测试 ⇒ 如实记）；**M4 未闭合 ⇒ 不打 tag** ✓

### 第 61 轮（**`pskit calibrate` 落地 ⇒ 600 dpi 的根因测到了：电平判读塌陷（D51），"余量不足"被推翻**）

- **新命令**：`node cli/pskit.mjs calibrate <dir|file>` —— 逐页打印 readout 结果、**页内 RS 预算占用** `(2·errors + erasures)/(blocks·nsym)`（失败块按耗尽整块计 ⇒ 明写 **LOWER BOUND**）、阈值无关的着墨面积比（仅健康检查）。**复用接收端同一条路径**（`decodePage` + `TransferAssembler.feed`；后者本来就在 `core/protocol.js:489`/`:484` 返回 `intraDecode` 的 stats ⇒ **`core/` 一行未动**）；**不是门限**（exit code 不是判决）、**不派生阈值**、**不改任何解码判决** ⇒ 不必重判 G5/G2 ✓ 帮助文本与函数头写明了这两条界限，也写明 **PLAN §M7 的 calibrate（MTF 校准板 → 推荐喷嘴/格距）仍未实现** ✗
- **量出的分布是双峰的**：300 dpi 三份语料 9 页全 `budget 0.0%`（`errors 0 · erasures 0 · clean 102/102`）；600 dpi 能读出的页 `0.0%`（`sc-scan600-11` p0、`sc-scan600-12` p0）与 `0.2%`（`sc-scan600-12` p1，5 errors）；坏页 `blocks 141 · clean 0 · errors 0 · erasures 0 · failedBlocks 141`（`sc-scan600-11` p1/p2、`sc-scan600-10` p1）⇒ **不是"贴着余量悬崖"**；`sc-scan600-105` 三页在 readout 之前就死（`echo-bad-magic` ×2、`no-hollow-corner` ×1）⇒ 那是**另一条通路，不许与塌陷一并归因** ✗
- **根因（逐格对拍 `tools/level-diff.mjs`（第 61 轮由 gitignored 探针 `.tmp/lvl61.mjs` 提升入库并把目录/页号参数化 ⇒ **台账不再指向不入库的文件** ✓），真值 = `.tmp/g2src-a4-600` 的原始渲染，即 `sim/channel.py` 的输入）**：`sc-scan600-11` p1 的信道页**内部 283155 格全被读成电平 1**（原始渲染 `0=256118 1=27037`）⇒ 失配 **90.452%**、混淆**只有 `0->1`**、行/列带**平坦**（行带 `51549 51571 51706 51767 51692`）、48 个 ±3 格平移里最好仍 90.4% ⇒ **电平判读整体塌陷**：不是噪声（会有多种混淆对且聚集）、不是局部损伤（平坦）、不是格构偏移（平移无效）⇒ 记 **D51**
- **对照与互证（量具可信的证据，不是自我背书）**：同语料 p0 失配 **0.000%**（两侧直方图完全一致 `0=256501 1=26654`）；`sc-scan600-12` p1 的 5 个 RS 错误**全部落在内部窗口外的 2 格边带**（行带 `2 1 0 0 2`、列带 `3 0 0 0 0`）⇒ 直方图对拍与 RS 报告互相印证 ✓
- **三处历史结论按实测更正**：① 第 19 轮"A 类 = 容量事实、不是 bug" ⇒ **是 bug（D51）** ✗ ② 第 58 轮"主因指向页内 ECC 余量不足" ⇒ **推翻**（预算占用 0.0–0.2%）✗ ③ 挂了四十多轮的"600 dpi 差能力还是差样本量" ⇒ **答案：两者都不是**，是判读缺陷 ⇒ **加冗余与加样本量都救不了这类页** ✗（门限表 G2 行、`ACCEPTANCE` G2 段与待办第 1 项、`DEFECTS` D49、`USE.md` §3 的"余量重做"那句**全部同步更正** ✓）
- **仍不许写成结论的**：塌陷的**机制**（dpi 翻倍 ⇒ 每像素峰值覆盖率约减半 ⇒ 固定阈值落到 ρ 分布之外）**没有直接量过** ⇒ 下一步用 `core/decode/ideal.js#analyseCell` 比塌陷页与健康页的 **ρ 分布**（第 15 轮只对**角标二值化**证伪过 `otsu/peak` 0.54 vs 0.57 ⇒ 电平判读是另一条通路）✗
- **下一轮首位（修法 + 两条护栏）**：按页重测 ρ 表再定阈（构件已在仓内：`measureTargets`、`shapeThresholds` 接受 `{targets}`、`expectedRho`、`analyseCell`、参考实现 `sim/selfcheck.py` 的 `calibrated_targets`）⇒ 但它会**让接受更容易** ⇒ **必须回 G5 重验误接受**（0 是不可原谅线）、且判据必须是**跨 seed 的稳定谷底**而不是"这一页能过"（第 11 轮拒绝把 `0.65` 钉进代码就是防这个）⇒ **本轮刻意不做**：夹带一个会改变判决的解码器改动、却不同时重判 G5/G2，等于制造新的虚高 ✗
- **顺带看清的一件事**：塌陷页产出了一张"看起来完整"的电平网格、**readout 没有拒绝它** ✗ 拦住它的是页内 RS 找不到自洽错误图样 + 最后的 SHA-256 摘要门 ⇒ **硬约束在干活**（误接受仍为 **0** ✓），代价是整页白印；把这类页改成 readout 阶段点名拒绝**不会提高通过率**、且会改变 reason 码分布（要重跑 G2 两侧才能记账）⇒ 记在 D51、本轮不做 ✓
- **门限不动 · 不打 tag**：本轮**没有跑门限判决**（`core/` 未动 ⇒ G2 前提与第 57/58 轮相同）⇒ G2 仍是 **300 dpi ✓ 200/200 / 600 dpi ✗ 162/200 ⇒ 🟡 不上调**；全量套件 **`tests 301 · pass 301 · fail 0 · duration_ms 134032`**、`SUITE_EXIT=0`（条数与第 60 轮相同 ⇒ calibrate 未带新测试，如实记：`cli/` 仍无测试挂点 ⇒ 以可执行证据结案 ✓）；`--help` 已含 calibrate、未知命令仍正确报错 ✓ **M4 未闭合 ⇒ 不打 tag** ✓

### 第 60 轮（**D46 + D50 修完；D49 的用户暴露面修完 ⇒ 但我第 58 轮那句"网页不提供 600 dpi 档"是错判，本轮撤回**）

- **撤回（本轮最要紧的一条）**：第 58 轮我在 `USE.md` §3、`ACCEPTANCE.md`、`DEFECTS.md` D49 与本文件四处写了"**网页发送端不提供 600 dpi 纸面档**（grep 实证 `web/` 内无 `P-M1-600`）"⇒ **错** ✗ `web/sender.js` 的下拉框是 `for (const [id, p] of Object.entries(PROFILES))` **动态枚举全部档位**建的 ⇒ 档位 id 从不以字面量出现 ⇒ **grep 不到 ≠ 不提供**。**方法错误**：文本扫描能证明"某个串在"，**证明不了"动态生成的列表里没有某项"** ⇒ 这与本项目反复警惕的"空转判据"同类，这次栽在我自己的台账上，而且它让用户手册**少警告了一个真实入口** ✗ 四处均已就地标注撤回、历史文字保留（与 D48、第 9 轮竖线事件同一处理方式 ✓）
- **真实暴露面比第 58 轮记的更大**：网页用户也能一键选到会失败 **19%** 的那一档 ⇒ 判据 ② 的发送侧入口就在网页上 ⇒ 这是**真正阻塞可用**的，本轮修掉 ✓
- **~~D46~~ CLOSED ✓**（manifest 的 `note` 按纸面/码牌/无纸张三态分叉）+ **~~D50~~ CLOSED ✓**（本轮新发现、同族：`render` 行报码区尺寸而盘上是整张纸，300 dpi 侧差 220×218 px、600 dpi 侧差 431×426 px）。**实测证据**（`cli/` 无测试挂点 ⇒ 直接跑 CLI 读产物，250 B 载荷）：纸面 300 ⇒ `Print at 100% scale (no "fit to page"). Sheet 210.0x297.0mm carries the 191.3x278.6mm code area centred, with crop marks and registration crosses in the margin; scan colour at 300 dpi with auto-crop OFF.`（**不再出现 "plate"** ✓）；纸面 600 ⇒ 同上 + `WARNING: 600 dpi paper profiles are NOT qualified -- the G2 gate measured this side below its 100% criterion (docs/DEFECTS.md D49)…`；板材 `PL-D2 --nozzle 0.4 --format 3mf` ⇒ `Verify the plate fits: 189.3x189.3mm`（**码牌口径原样保留** ✓）；三次 `exit 0` ✓ **D50 的阳性对照**：stdout `2480x3508px sheet` ⇄ 盘上 PNG IHDR **2480×3508**（600 dpi：`4961x7016px sheet` ⇄ **4961×7016**）⇒ **不看它自己怎么说，去读盘上的文件** ✓ 尺寸全部由 `layout.physicalMm` / `layout.sheetMm` + `dpi` 算出、**无硬编码** ⇒ 换档不烂 ✓
- **D49 的用户暴露面已修（根因侧仍 OPEN ✗）**：CLI stdout + manifest note 双警示；网页下拉标签加 `⚠ 实测未达标 (D49)` ⇒ **不隐藏该档**（隐藏 = 悄悄削能力；且 `pskit calibrate` 未实现前排除不了"差的是能力而不是样本量"）、**数字不进 UI 字符串**（将来重测不会让 UI 烂掉，与 CLI note 同一原则）；接收端 `web/app.js` 的下拉是**解码提示**、不是产出选择 ⇒ **刻意不标注**（标了是噪音 ✓）
- **守卫测试 `tests/unit/profile-picker-warning.test.mjs` 4/4 绿**，含**反空转对照**：flagged 集必须非空（空集会让其余断言全部白过）、`P-M1-600` 必须在集内（阳性对照）、合格档必须**不带**警示（否则警示沦为噪音）、标签不许硬编码比率、源码必须经 `profileOptionLabel(id, p)` 渲染、警示文案全仓只许出现一次 ⇒ 谁把标签改回手写就会红 ✓ **这条测试存在的理由就是本轮的错判**：不再靠"我 grep 过一眼"下结论
- **全套重跑（数字都是量到的，不是推的）**：全量套件 **`tests 301 · pass 301 · fail 0 · cancelled 0 · skipped 0 · duration_ms 135959`**、`SUITE_EXIT=0`（= 第 59 轮基线 297 + 新增 4 ⇒ **逐条对得上** ✓）；`build-web` `exit 0`（bundle 276.6 KiB / 28 modules、单文件 `pskt-file.html` 282.9 KiB、precache 52、目录级原子换站）；`check-dist` **13 PASS** · `13 pass / 0 skipped (conformance asset loaded) / 0 fail` · **`G9 CHECK: all 12 assertions pass`** · `exit 0` ⇒ **`sender.js` 改了 ⇒ dist 已重建 ⇒ 修复真的出货**（不是只改源码 ✓）。另核对：手册 §1 的 `pskt-send-file.html` / `pskt-file.html` / `send.html` / `index.html` 与 `web/dist` 实物**逐个对得上** ✓（构建输出里只点名 `pskt-file.html`，一度像是手册写错文件名 ⇒ 实测四个都在 ⇒ **没有新缺陷** ✓）
- **"出货"的阳性对照（`web/dist` 不入库 ⇒ 光说"重建了"不算数）**：`.gitignore:4` 忽略 `web/dist/`、`git ls-files web/dist` = **0 个文件** ⇒ 站点由 `node tools/build-web.mjs` 生成（`docs/USE.md` L16 已交代 ✓），所以"修复出货"= 本地 dist 已重建**且构建产物里真能找到那段文案**。扫描产物（探针 `.tmp/dist60.mjs`，gitignored）：dist **54 个文件**（与第 56 轮实测数一致 ⇒ 没多没少），**两个发送页都带上了警示** —— 单文件版 `pskt-send-file.html`（脚本内联、无外链）`未达标 ×1 · profileOptionLabel ×3 · isUnqualifiedPaper ×3`；瘦版 `send.html` 外链 `./sender.js`，而 `web/dist/sender.js` 里 `未达标 ×1 · profileOptionLabel ×2 · isUnqualifiedPaper ×2` ✓ **中途一次自查纠错**：我先只扫 `send.html` 得到 `未达标 ×0` ⇒ 差点判成"瘦版没修好"，实际是**扫错了对象**（外链 module 的文案不在 HTML 里）⇒ 与本轮撤回的那条同族：**先确认"该去哪儿找"，再下"没有"的结论** ✗→✓
- **门限不动**：本轮**没有跑任何 G 门限判决**（改的是 CLI 文案 + 网页标签 + 一条守卫测试，**没碰** `core/**` 的编解码路径 ⇒ 不影响任何门限前提）⇒ G2 仍是 **300 dpi ✓ 200/200 / 600 dpi ✗ 162/200 ⇒ 整体 🟡、不上调**；**误接受仍为 0** ✓；总账计数不变 ✓；**不打 tag**（M4 未闭合）
- **仍 OPEN 的结构性欠账（如实记，别假装已覆盖）**：`cli/pskit.mjs` **全仓没有单测挂点**（`cmdSend` 未导出 ⇒ D46/D50 只能靠"跑一次读产物"结案 ⇒ 将来回归会**静默烂掉** ✗）。本轮**没有**为此新造一套 CLI 测试框架（遵守"刚好就行"），但记在这里：若将来给 CLI 加挂点，**D50 的 stdout ⇄ IHDR 对拍**与 **D46 的三态 note** 应升级为自动断言 ✓
- **下一轮首位**：① **根因侧（D49 的 ④）**：`pskit calibrate`（`ACCEPTANCE` 待办第 1 项）——这是分辨"600 dpi 差的是能力还是样本量"的**唯一手段**，没有它就不许对 600 dpi 下根因结论 ✗ ② 用户侧硬件验收仍欠（G4 手机 500 页 × 8 轮、G9 三浏览器两源、D8 打印缩放实测、G10 四喷嘴、G6 soak ⇒ 清单在 `docs/USE.md` §5，**只有你能做**）③ D43 的 https 半（**只有你能解**：仓库 URL + 凭据 + `.github/workflows/pages.yml`）

### 第 59 轮（**还欠账：全量套件基线抄进台账 = `297/297 · 0 fail · 136 s`**）

- **实测**（两个 G2 批收工 ⇒ D28 解除、CPU 空出来后跑的绿基线，供下一轮动 `cli/` 时对照）：`node --test --test-isolation=none "tests/unit/**/*.test.mjs"` ⇒ **`SUITE_EXIT=0`**、**`tests 297 · pass 297 · fail 0 · cancelled 0 · skipped 0 · todo 0 · duration_ms 136148`**（日志 `.tmp/s58.txt`、后台 `pwsh-16`）⇒ 第 53 轮加的"纸面斜拍"那一例把 296 变成 **297** ⇒ **第 53/57 轮"没抄下汇总数字"的欠账已还**，`docs/USE.md` §6 与本表总账行同步改成实测值 ✓
- **顺手记一个环境事实（已进 `AGENTS.md` 陷阱表）**：Node 24 的 test runner 在**输出被重定向**（非 TTY）时用 **spec** 报告器 ⇒ 汇总行以 `ℹ` 开头、**不是** TAP 的 `# ` ⇒ 我第一版过滤器 `/^# /` **一条都没匹配到**（`SUITE_EXIT=0` 却"取不到数字"）✗ 按内容过滤 `(tests|pass|fail|duration_ms)\s+\d+` 才取到 ⇒ 判"跑没跑"**永远只看 exit code**，取数字要按报告器的实际形状来（或显式 `--test-reporter=tap`）✓ 日志里 `ℹ` 经 UTF-16LE→剥 NUL 后显示为 `9!` ⇒ 那是编码残留、不是内容 ✗
- **下一轮首位（不变，且现在有绿基线可对照）**：**D46**（manifest 的 `note` 对纸面产物仍用码牌口径）+ **D49 的代码半**（CLI 选 `P-M1-600` 时警示/标 experimental + 一条"网页发送端不得提供 600 dpi 纸面档"的**守卫测试**）⇒ 两者都要动 `cli/`，改完必须重跑全量套件并再抄一次数字 ✓
- **不打 tag**（M4 未闭合 ✓ G2 只过半边）

### 第 58 轮（**G2 的 600 dpi 半边判决：162/200 ✗ 不达标 ⇒ G2 整体未通过；历史归因被推翻；用户文档已改口**）

- **判决（`pwsh-14` 收工、job exit 0，但门限 exit 1 ⇒ 两件事分开看）**：`verify --gate G2 --root .tmp --match 'sc-scan600-*'` ⇒ **`FAIL G2 corpus: 162/200 byte-exact in 4359.7s`** · `GATE FAILURE` · **`GATE exit=1`** · `failure classes: short 0/1 x37, no-page-header x1`；门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200` ⇒ 样本量对齐 PLAN、**判据没放宽** ✓ 前提与 300 dpi 侧**完全对称**（整张 A4 `4961×7016`、同一载荷 sha256 `91ab3f7310da452b`、`--preset scan600 --modifier nocrop`、200/200 生成 `exit 0`）⇒ 差别只在 dpi/档，**不是**语料生成失败、**不是**夹具形状、**不是**两侧口径不一致
- **页级明细（别只看 162/200）**：**342 条页级失败 / 600 页 ⇒ 页级直接读出只有 43%**（300 dpi 侧 97.83%）；**162 份 OK 里有 145 份也含页级失败**（页间 RS 兜住 ⇒ 冗余在超负荷工作），38 份 FAIL 全部含页级失败。原因分布：`assemble/intra-fail` × 210、`readout/echo-bad-magic` × 78、`markers/no-hollow-corner` × 41、`readout/echo-header-crc` × 12、`readout/echo-version-3` × 1
- **两处历史结论被推翻/改写（照实改口 ⇒ 台账虚高与虚低同样是错）**：① 第 49–52 轮的"600 dpi 那 2 例失败是夹具只有码区、没有纸边（角标紧贴图像边缘 ⇒ 任何姿态都出画）造成的" ⇒ **不成立**：交付形状 + `nocrop` 下照样 **41 页** `no-hollow-corner`、**38/200** 份失败 ✗（不变的那半边：这**不是**解码器把可读页误判成不可读 ⇒ 3 角 6 方程 < 单应 8 自由度，拒绝正确 ✓）② 第 19 轮记为"容量事实、不是 bug"的 A 类（页内 ECC 余量薄）⇒ 全样本量下**代价是 19% 的载荷拿不回来** ⇒ 它不是静默出错（**误接受仍为 0** ✓ 38 份全是拒绝/缺页、没有一份交出错误字节），但**是可用性缺陷** ⇒ 记 **D49 OPEN**
- **不下根因结论**：主因指向页内 ECC 余量不足（210 页 `intra-fail`，与 `sim/README.md` "10 px 格距下 `ew_mm`/`mtf_mm` 哪怕 0.02 mm 就破页内 ECC" 一致），但"为何 600 dpi 的原始误码率比 300 dpi 高这么多"仍未定 ⇒ 属 `ACCEPTANCE` 待办第 1 项（`pskit calibrate` 未实现 ⇒ 排除不了"差的是能力而不是样本量"）与第 4 项（B 类根因未定）⇒ **本轮不猜** ✗
- **本轮真正修掉的可用性阻塞（不是记账了事）**：`docs/USE.md` §3 原先写"扫描：300 或 600 dpi"⇒ 把两档并列成等价选择 ⇒ 用户照文档选 600 dpi 就有约 **1/5** 概率拿不回文件 ✗ 现已改为**推荐 300 dpi（实测 200/200）**、明示 600 dpi 实测 **162/200 = 81%** 与页级直读 43%、并写明在 `pskit calibrate` 落地或该档页内 ECC 余量重做之前**不是可靠路径**；同时说明失败形态是"缺页/读不出并点名补拍"、**不会**交出错误字节 ✓ 另：**~~网页发送端不提供 600 dpi 纸面档~~（第 60 轮撤回 ✗ 错判：下拉框是动态枚举 `PROFILES` 建的）**（当时的"证据"是 grep 实证 `web/` 内无 `P-M1-600` ⇒ **该推理无效**：文本扫描能证明"某个串在"，证明不了"动态生成的列表里没有某项" ✗ 真实暴露面比我记的更大——网页用户也能一键选到会失败 19% 的那一档 ⇒ 第 60 轮已修：标签加 `⚠ 实测未达标 (D49)`、CLI 给 stdout + manifest 双警示、守卫测试 `tests/unit/profile-picker-warning.test.mjs` **4/4 绿**），但 CLI 的 `--profile` 帮助仍平等列出它 ⇒ **警示与守卫测试留到下一轮**
- **G2 整体判决（已写进现况表 G2 行与 `ACCEPTANCE` G2 段）**：**300 dpi ✓ 100%（200/200）/ 600 dpi ✗ 81%（162/200）⇒ G2 未通过、维持 🟡、不上调**（总账的 `✅ 5 · 🟡 4 · ⬜ 2` 计数不变 ⇒ **没有**因为半边绿了就上调 ✓；也**没有**用"只报通过的那一档"凑数、没有改信道参数、没有把正确拒绝剔出分母 ✗）
- **两个 G2 批都已收工 ⇒ D28 解除**（本轮刻意没动 `cli/`：`pwsh-14` 的判决当时正在用同一个 CLI）。下一轮首位：① **D46**（manifest 的 `note` 对纸面产物仍用码牌口径 `Verify the plate fits: 191.3x278.6mm`）② **D49 的代码半**：CLI 选 `P-M1-600` 时给出"实测不达标"的警示（或把该档标 experimental）+ 一条**守卫测试**盯着"网页发送端不提供 600 dpi 纸面档"（现在只有我 grep 过一眼、**没有测试** ⇒ 将来谁把它加回下拉框不会红 ✗）③ 改完跑**全量套件并把汇总数字抄进台账**（还上一轮"没抄数字"的欠账）
- **不打 tag**（M4 未闭合 ✓ G2 只过半边、G4/G6/G9/G10 仍缺）

### 第 57 轮（**G2 的 300 dpi 半边在"交付形状"下达标：200/200 逐字节 ⇒ 整体仍 🟡，600 dpi 判决未出**）

- **判决（`pwsh-13` 收工、job exit 0）**：`node cli/pskit.mjs verify --gate G2 --root .tmp --match 'sc-scan300-*'` ⇒ **`PASS G2 corpus: 200/200 byte-exact in 1113.7s`** · `ALL GATES PASS` · **`GATE exit=0`**；门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200` ⇒ **样本量对齐 PLAN、判据没放宽** ✓ 前提是 **D45 修好之后的交付形状**：源页 `.tmp/g2src-a4-300` = **整张 A4 `2480×3508`**（码区按标称物理尺寸居中 + 留边里的四角裁切标记与四边套准十字），载荷仍是 `.tmp/payload.bin`（sha256 `91ab3f7310da452b`）⇒ 每份 3 页 × 200 份 = **600 页盲解**；生成 **200/200 `exit 0`**、信道自报 `crop=0.0`（平板的物理正确模型，不是"关掉裁角凑通过率" ✓）；日志 `.tmp/g2-a4-300.log`（生成 15:02:40 收工、判决 15:21:14 收工）
- **页级明细（不藏在 200/200 后面）**：**13/200 份各有 1 页读不出、全部由页间 RS 重建**，其余 187 份三页直读 ⇒ **页级直接读出 587/600 = 97.83%**、**载荷级 200/200 = 100% 逐字节**、每份 `digest verified`、**含 FAIL 的行数 = 0** ✓ 原因分布：`readout/echo-header-crc` × 10、`markers/no-hollow-corner` × 2、`readout/echo-bad-magic` × 1 ⇒ 冗余正是为此存在（一页读不出 ≠ 整批失败 ⇒ 协议设计意图第一次在**全样本量**下兑现 ✓），**这不是把失败算成通过**：判据比的是落盘字节与原文件的 SHA-256，摘要由 `tools/g2-corpus.mjs` **进程内重算**、不 grep CLI 措辞 ✓
- **两条不许越界的读法**：① 该次运行**只评估了 G2**，门限自己打印 `not evaluated by this run: G0 G1 G3 G4 G5 G6 G7 G8 G9 G10`（并说明 `G4 G9 need a real phone/browser, G6 is the long soak, G10 needs a printer`）⇒ **`ALL GATES PASS` 不得被引用成"全部门限通过"** ✗ ② **G2 整体仍 🟡、不上调**：判据要 300 **与** 600 两侧都 200 seed × 100%，而 600 dpi 侧（`pwsh-14` → `.tmp/sc-scan600-*`、源页 `4961×7016`）判决时仍在生成（52/200、77.5 s/份 ⇒ 还要约 3 h）⇒ **本轮不写 600 dpi 的任何比率** ✗ 历史上 600 dpi 那 2 例失败的"夹具没有纸边 ⇒ 角标紧贴图像边缘"归因**本轮既未证实也未推翻**；300 dpi 侧这 2 页 `markers/no-hollow-corner` **也不许读成"那个问题消失了"**（它们是被 RS 兜住的页级失败，确实发生了）
- **记账落点**：`docs/ACCEPTANCE.md` G2 段新增第 57 轮全文（上面每个数字与两条禁读都在里面），段标题里"测的是只有码区的源页（D45 ✗）"那条保留意见**标注为已解除**（对 300 dpi 侧）；本表现况表 G2 行改为现状 + **保留第 51–52 轮原文供追溯**（没有删任何历史结论 ✓）
- **顺手修掉同一类"数字会漂"的缺陷**（就是本轮在 `USE.md` §0 发现的那类）：现况表 G9 行的"8/8 全绿""SW 78 条清单"是第 22–23 轮的数 ⇒ 换成第 56 轮复量值（`13 pass / 0 skipped / 0 fail` + `G9 CHECK: all 12 assertions pass`、`web/dist` **54 个文件**、预缓存 **52 条**）并注明"别背数字、看工具自己打印的汇总"；总账行写死的 `单测 231/231` 早已过期 ⇒ 改成"以 `node --test` 自己打印的汇总为准"（最后抄下的是第 52 轮 **296/296**；第 53 轮加 1 例后 `exit 0` 但没抄汇总 ⇒ **不写没量过的数** ✗）
- **D46 仍 OPEN，且本轮刻意不动 `cli/`**：`pwsh-13` 已收工，但 `pwsh-14` 约 3 h 后要用**同一个 CLI** 跑 600 dpi 判决 ⇒ D28。**也不为这一行文案去 kill + 重启那个批**——除了 churn 还有真风险：kill 可能留下**半份语料目录**，而重启脚本的 `if (Test-Path $out) skip` 会把它当完整语料跳过 ⇒ 判决就会吃到一份残缺语料 ✗ 等 `pwsh-14` 自然收工再改 ✓
- 下一轮首位：**收 `pwsh-14` 的 600 dpi × 200 seeds 判决**（`node .tmp/probe53.mjs` 看有没有 `GATE exit=`）⇒ **两侧都出结果之后**才谈 G2 要不要上调；然后修 D46
- **不打 tag**（M4 未闭合 ✓ G2 只过半边、G4/G6/G9/G10 仍缺）

### 第 56 轮（**USE.md §0 的两个漂移数字换成实测值，并写明"别拿文档数字对不上当失败"**）

- **发现**：用户照 §0 构建时，文档写"生成 `web/dist`：53 个文件""check-dist：12 项断言"，而第 53 轮把浏览器自检改成跑**整张纸**的形状之后，实测已是 **54 / 13** ⇒ 数字对不上会让人以为构建坏了 ✗ 这是"照 docs 走一遍"路上的真障碍（虽然小）。**本轮复量**（不背记忆里的数）：`web/dist` **54 个文件**、`check-dist` **`13 pass / 0 skipped / 0 fail`** + **`G9 CHECK: all 12 assertions pass`**、**exit 0** ✓ 已把 §0 改成"只看 exit code 与它自己打印的汇总"，并注明这些数随构建变（与 §2 的 SW 预缓存条数同一写法 ⇒ 全文一致）
- **§1 的电脑端路径逐条复核过、是准的 ⇒ 没有改**（不改对的东西 ✓）：发送页 `pskt-send-file.html` / 接收页 `pskt-file.html` 两个单文件版、接收可拖入**整盘**（乱序/缺页会点名、页间 RS 能补）、自检 `?selftest=1`、以及"为什么必须有单文件版"（`file://` 下浏览器拒载 ES module）
- **两个批仍未出判决 ⇒ 本轮不写任何 G2 比率** ✗：`pwsh-13`（300 dpi）本轮开始时 **179/200**、16.7 s/份 ⇒ 生成还剩 ~6 min + 判决 ~18 min；`pwsh-14`（600 dpi）**43/200**、77.5 s/份 ⇒ 还要 ~3.4 h；已完成的**全部 `seed N exit=0`** ✓
- **没碰 `cli/`、`core/`**（`pwsh-13` 即将进入判决 ⇒ D28）⇒ **D46 仍 OPEN**。下一轮顺序：先把 300 dpi 判决如实写进 `docs/ACCEPTANCE.md` 与门限表；若那时还要动 `cli/`（D46 那一行文案），就 kill + 重启 `pwsh-14`——它的脚本 `if (Test-Path $out) skip` ⇒ **重启不丢已生成的语料** ✓
- **不打 tag**（M4 未闭合 ✓）

### 第 55 轮（**D48 修掉：门限表在页面上重新成为表格；不是它变绿了，是它先抓到我自己**）

- **两个批仍未出判决 ⇒ 本轮不写任何 G2 比率** ✗：`pwsh-13`（300 dpi）**154/200**、16.7 s/份 ⇒ 生成还剩 ~13 min + 判决 ~18 min；`pwsh-14`（600 dpi）**37/200**、77.5 s/份 ⇒ 还要 ~4 h；已完成的**全部 `seed N exit=0`** ✓
- **修的是成因、不是账**：① `docs/STATUS.md` 门限表第二块**补回与第一块同款的表头与分隔行**（纯新增 2 行）⇒ **没有**按第 53 轮的计划搬走第 52–53 行的散文，因为搬走会把第 7–8 轮的叙述从它所属的行旁边挪开、打乱阅读顺序 ✗ 补表头同样让两块都成表、且**已记录的结论一个字没动** ✓ ② `docs/DEFECTS.md` 第 218 行**夹在 D7 与 D8 之间的空行**删掉（markdown 里空行即断表）⇒ 那张表重新连续 ✓ ③ 两处人工转义：我写 D48 行时自己又塞了裸竖线（引用门限表某行原文 ⇒ 那行一度 **13 格** ✗ **被检查器当场抓住**）；以及第 9 轮那行**叙述里当分隔符用的两个字面竖线**（@1326、@2178，**不在代码跨度里** ⇒ `--write` 正确地没碰，必须由人判它们是正文还是列边界）⇒ 转义后那行回到 3 格 ✓
- **实测**：`node tools/check-docs-tables.mjs` **exit 0** ⇒ `DOCS TABLES: clean -- 235 rows in 44 tables agree with their headers and hold no bare pipes`（机械转义 10 处 + 人工 2 处）；`git diff --numstat` = `docs/DEFECTS.md +1/-2`、`docs/STATUS.md +4/-2` ⇒ 与"只插反斜杠 + 新增 2 行表头 + 删 1 个空行"吻合、**无其他改动** ✓ **判据没为变绿放宽**：格数不符或代码跨度里有裸竖线仍然 exit 1；NOTE 现在 **0 条** ✓
- **没做**：没碰 `cli/`、`core/`（两个批正在用 ⇒ D28）⇒ **D46 仍 OPEN**；也**没有为文档再造新工具**（`check-docs-tables.mjs` 是第 53 轮已有的，本轮只是用它 + 手工改 3 处 ⇒ 遵守"刚好就行"）✓
- 下一轮首位：**收 `pwsh-13` 的 300 dpi × 200 seeds 判决**（`node .tmp/probe53.mjs` 看有没有 `GATE exit=…`）⇒ 如实写进 `docs/ACCEPTANCE.md` G2 段与门限表（**不达标也照写** ✓）；600 dpi 侧继续等，重点看历史 2 例"角标出画"在新前提（整张 A4）下是否消失
- **不打 tag**（M4 未闭合 ✓）

### 第 54 轮（**暂停点：用户要求先停 ⇒ 本轮只记账，不做新东西**）

- **两个后台批仍在跑（暂停不影响它们，跑完会自己通知）**：`pwsh-13` = 300 dpi × seed 1..200 → `.tmp/sc-scan300-*`，实测 **16.7 s/份**、暂停时 **118/200** ⇒ 生成还需 ~23 min + 判决（200 × ~5.4 s）~18 min ⇒ **约 40 min 后出 G2 判决**；`pwsh-14` = 600 dpi → `.tmp/sc-scan600-*`，**77.5 s/份**、暂停时 **28/200** ⇒ 生成 ~3.7 h + 判决 ~1.7 h。已完成的全部 `seed N exit=0`，信道自报 `crop=0.0`（整张 A4 进信道没被裁掉任何东西 ✓）。要停就 `job_kill pwsh-13` / `job_kill pwsh-14`；两个脚本都有 `if (Test-Path $out) skip` ⇒ **重启不丢已生成的语料** ✓
- **判决未出 ⇒ 本轮不写任何 G2 比率** ✗（300 dpi 那份才是"用户今天打印的那张纸"的数字；600 dpi 那份要看历史 2 例"角标出画"在新前提下是否消失）
- **更正上一轮末我自己的一个误判**：我一度以为生成"慢了 7 倍"，实测是**探针间隔太短**造成的错觉 ⇒ 300 dpi 的 16.7 s/份与历史（15–20 s/份）一致 ✓
- **本轮改了什么（纯文档，没碰 `cli/`、`core/` ⇒ 不违 D28）**：`docs/USE.md` §5 的 G4/G9 两行补了可直接照做的落点（`node tools/serve.mjs` → 手机同 Wi-Fi 打开它打印的那行局域网地址；自检 `?selftest=1`，并注明它自第 53 轮起跑在**整张纸**的形状上）；修掉 §6 里过期的"G0 单测 277/277" ⇒ 改成"别背数字、以 `node --test` 自己打印的汇总为准、只看 exit code"（第 52 轮实测 296/296；第 53 轮加 1 例后 `exit 0` 但我没抄下汇总 ⇒ **不写没量过的数** ✗）；`AGENTS.md` 环境事实补一条：`Get-CimInstance` 被沙箱拒（`拒绝访问`，本轮实测）⇒ 探进程数用 `Get-Process` 够了，别再试 CIM/WMI
- **仍 OPEN**：**D46**（manifest 的 `note` 对纸面产物仍说 `Verify the plate fits` ⇒ 一行文案，但要动 `cli/pskit.mjs`、而两个批正在用它跑判决 ⇒ D28 不许改 ⇒ 等 `pwsh-14` 收工）；**D48**（门限表被散文段打断 ⇒ 后半张表在页面上不成表 ⇒ 需重排版式 + 逐字 diff 复核，单独一轮做）；**D43 的 https 半**（只有用户能解：仓库 URL + 凭据 + `pages.yml`）
- **用户侧还欠的验收（都要真硬件，清单在 `docs/USE.md` §5）**：G4 手机 500 页 × 8 轮、G9 三浏览器 × 两种源、D8 打印缩放实量、G10 四种喷嘴、G6 soak ⇒ 进程内等价证据已顶上（`usability.ps1` 11 项 PASS、`smoke-sender`、`smoke-capture`、纸面斜拍用例），但**不冒充真机证据** ✗
- **恢复时第一件事**：`node .tmp/probe53.mjs` 看两个批到哪了；`pwsh-13` 一打印 `GATE exit=…`，就把 300 dpi 的判决如实写进 `docs/ACCEPTANCE.md` G2 段与门限表（**不达标也照写** ✓）
- **不打 tag**（M4 未闭合 ✓）

### 第 53 轮（**D47：D45 修好之后三处进程内证据还在喂"裸码区" ⇒ 手机侧的绿灯已不描述用户拍到的东西；本轮全部改成交付形状，并补上"纸面斜拍"这块空白**）

- **两个 G2 批仍在生成 ⇒ 判决未出 ⇒ 本轮不写任何 G2 比率** ✗：`pwsh-13`（300 dpi，起跑 13:58:50）进度 **40/200**、`pwsh-14`（600 dpi，13:59:00）**10/200**，已完成的全部 `seed N exit=0`，信道自报 `crop=0.0`、blur 0.0412 mm（300 dpi）/ 0.055 mm（600 dpi）⇒ **整张 A4 进信道没有被裁掉任何东西** ✓ 进度探针改成 `.tmp/probe53.mjs`（上轮把正则写进 pwsh 双引号 ⇒ `$` 被先插值、把成功数报成失败数 ✗ 教训重申：复杂 JS 落地成文件再跑 ✓）
- **本轮修的缺陷（与 D45 同类：夹具 ≠ 交付物）⇒ D47，已 CLOSED**：`tools/smoke-capture.mjs` 喂 `renderPageBitmap` 的码区位图（2260×3290），而落盘 PNG 已是整张纸（2480×3508）；`web/selftest.js` 三处 `pageLayout(..., {})` **连纸张都没传**；`tests/unit/warp.test.mjs` **全是码牌档** ⇒ 纸面斜拍从未被测，而留边里 16 段标记是检测器从没被告知过的黑墨 ⇒ "会不会被当成角标"当时无人知道 ✗
- **修完的实测**：`node tools/smoke-capture.mjs` **exit 0** ⇒ `3 sheet(s), 2480x3508px (code area 2260x3290px)`、`16 mark segments, margin 9.22mm`、三页全收且 `sha ea26ac9cc04c825d` 与输入一致；**裸码区对照组**仍还原一致（用户只扫/裁码区、以及码牌档都要能过 ✓）；重复 / 裁切 / 跨会话 / `totalPages` 冲突 / 不可解码 五道门限仍各自正确拒绝 ✓
- **纸面斜拍第一次有证据**：`warp.test.mjs` 新用例把 `P-M1-300` 的整张 A4 斜拍（`scale 1.1, rot 0.05, kx/ky 微翘, noise 8, blur 1`）⇒ 检测 → 校正 → 回显 → 读格 → 重组，3 页 900 B **逐字节还原**，打印 `P-M1-300 sheet: 3 tilted A4 photos (marks in the margin) recovered` ⇒ **裁切/套准标记不会骗过角标检测** ✓ 并加阳性对照：quad 必须等于"码区坐标 + `contentOffsetPx`"经同一变换后的位置、误差 <2.5 px ⇒ 排除"锁到标记、校正出垃圾却碰巧解码"的假绿 ✓ 码牌三档（`PL-G@0.8` 79 页 / `PL-D2@0.4` 6 页 / `PL-M1@0.4` 6 页）与 90° 旋转用例不受影响，`warp exit 0` ✓
- **浏览器自检改在交付形状上执行**：`selftest.js` 三处补 `sheetMm`、`encodePNG(bmp.sheetMm ? renderSheetBitmap(bmp) : bmp)` ⇒ 重建 `web/dist`（build `d7eaa575e9f75fbb`、54 文件、precache 52）+ `check-dist` **exit 0**（13 pass / 0 fail），关键一行 **`dist selftest green (executed here, not claimed for a browser)`** ⇒ 在构建产物上**执行**、不是声称 ✓ G9 CHECK 12 assertions ✓
- **边界说清楚（不越界读证据）**：以上证明的是**几何与检测链在交付形状上成立**，**不等于** G4/G9/G10——真手机 500 页×8 轮、真浏览器安装提示、真摄像头连拍仍待用户执行，步骤与验收清单在 `docs/USE.md` §5 ✗ 进程内证据不冒充真机证据 ✓
- **台账本身这一轮也上了机器把关（并且更正我第 52 轮的一个错判）**：写 D47 时我在表格单元里放了 `pageLayout(t.geom, prof.dpi || 300, {})` ⇒ GFM **连代码跨度里的裸 `|` 也当列分隔** ⇒ 那一行被切成 6 格、列全部错位，而它"看起来仍是一张表" ⇒ 第 52 轮我看到 D44 行报 9 个竖线时下的结论（"是我自检脚本太糙、表没坏"）**是错的** ✗ 脚本是对的、表真坏了，而且 D44 从第 48 轮起就带着同样的 `||`。查证后发现 STATUS 门限表里也散着同类裸 `|`（有几行**恰好凑够格数**才没被"格数相等"抓到 ⇒ 所以判据换成"表格行内代码跨度不许有裸 `|`"），并且门限表被第 52–53 行的散文段**从中间打断** ⇒ 后半张（G2 分界 + G3–G10 共 10 行）没有 `---|---` 分隔行、在页面上不成表 ⇒ 记 **D48**。处置：新增常驻工具 **`tools/check-docs-tables.mjs`**（真表 = 第二行是分隔行；对真表查两类错；`--write` **只往代码跨度里插反斜杠、绝不改正文一个字** ⇒ 格数不符的那类只报不修，因为"那一列本来该写什么"只有人知道 ✓），把这一步写进 `AGENTS.md` 的每轮收尾，并用它把**裸 `|` 一类全部修掉**（含我自己那处）✓ D48 的版式重排单独一轮做 ⇒ **不因为工具变绿就当它不存在** ✗
- **D28 守住**：两个 G2 批正在用 `cli/pskit.mjs` 与 `core/**` 跑判决 ⇒ 本轮**只动** `tools/smoke-capture.mjs`、`web/selftest.js`、`tests/unit/warp.test.mjs`、`web/dist`（重建）与文档，**没碰** `cli/`、`core/` ✓ 因此 **D46 仍 OPEN**（改 manifest 的 note 要动 `cli/pskit.mjs`，等两个批收工）
- 证据：`.tmp/cap53.log`（smoke-capture 全 PASS 原文）· `.tmp/warp53.txt`（含新用例输出）· `.tmp/bw53.txt` / `.tmp/cd53.txt`（构建与 dist 核对）· `.tmp/s53.txt`（全量套件，后台 `pwsh-15`）· `.tmp/probe53.mjs`（批进度探针，剥 NUL 读 UTF-16LE 混排日志）· `.tmp/g2-a4-{300,600}.log`
- 下一轮首位：**收 `pwsh-13` 的 300 dpi × 200 seeds 判决**（新前提 = 整张 A4 ⇒ 这才是"用户今天打印的那张纸"的 G2 数字）⇒ 如实写进 `ACCEPTANCE.md` 与门限表；600 dpi 侧若也跑完一并记，**特别看历史那 2 例"角标出画"是否消失**（消失 ⇒ 归因于夹具成立；仍在 ⇒ 归因要再改一次 ⇒ 两种结果都如实记 ✓）→ **D46**（批收工后动 `cli/`）→ D43 的 https 半（**等用户**）→ G4/G6/G9/G10 用户验收清单在 `docs/USE.md` §5 ✓

### 第 52 轮（**D45 修掉了：`--sheet` 现在对栅格也生效 ⇒ 用户打印的那张 PNG 终于是"一张纸"**；G2 顺序 (a)(b) 完成、(c) 两个 200-seed 批在跑）

- **修的是什么**：第 51 轮查出 `--sheet` 只到 PDF、栅格永远只给码区 ⇒ 用户打印 PNG 得到的是"一张只有码区的图"（无留边、无裁切标记、摆在纸上哪里由打印机决定），而 G2 的全部语料正是这个形状 ⇒ 600 dpi 那 2 例"角标出画"很可能是夹具造成的，不是编码/解码的毛病
- **怎么修（单一真源，不做两份实现）**：新 `core/render/sheet.js`（mm 单位：`sheetPlacement` 给居中与留边、`sheetMarks` 给 16 段标记；`MARK_MIN_MARGIN_MM = 0.5/PT_PER_MM`、`MARK_STROKE_MM = 1/PT_PER_MM` ⇒ **两个常数都由 PDF 的既有常数换算**，栅格不自创阈值 ✓）；`core/render/pdf.js` 改为**调它**（保留自己的 `cannot carry` 文案 ⇒ 既有断言不动）；`core/render/raster.js` 新增 `renderSheetBitmap()`：按 sheet 开位图、填纸白、码区居中 blit、同一套几何画标记（**y 翻转只在这一处发生**），线宽 `min(1pt, 0.8×gap)` 且**不细于 2 px**（1 px@300dpi = 0.08 mm 会被扫描仪 MTF 抹掉 ✗），返回值**就是纸** ⇒ `pageMm = sheet 尺寸`、**不带 `sheetMm`** ⇒ 交给 `encodePDFDocument` 不会二次居中 ✓ 出口接线：`cli/pskit.mjs` 的 PNG/TIFF 用 `bitmap.sheetMm ? renderSheetBitmap(bitmap) : bitmap`（**PDF 仍拿码区位图**做矢量居中与标记 ✓）、`web/sender.js` 同理 ⇒ 浏览器给用户的 PNG 也是整张纸 ✓
- **实测**：`pskit send .tmp/payload.bin --profile P-M1-300 --sheet A4 --format png` ⇒ `page-000.png = 2480×3508`（209.97×297.01 mm；原来是 2260×3290）；600 dpi 侧 `--profile P-M1-600` ⇒ `4961×7016`（**4961 才是对的**：210 mm@600dpi = 4960.63 px、栅格必须取整像素 ⇒ 偏差 0.01%，我第 51 轮写"应为 4960"是 naive 期望 ✗ PDF 侧仍是精确 mm）；PDF 几何不变（`pdf-truesize` 5 项仍绿、MediaBox 仍 `595.28 841.89` pt）
- **新单测 7 项**（`tests/unit/raster-sheet.test.mjs`）：sheet 尺寸/dpi/居中且**码区逐像素等于源**、**标记墨点全部落在码区之外**（逐像素判定、不信注释）、**笔宽碰不到码区**（算术判定 × 4 种纸张 × 不被纸边裁掉）、退化 sheet（无留边 ⇒ 0 段标记且逐字节等于码区）、PDF 的 MediaBox 与共享几何一致（**含阳性对照**：+1 mm 必须不匹配）、PNG 的 IHDR 自带 sheet 尺寸、坏输入全拒（含"纸装不下码区"⇒ `cannot carry`）⇒ 全量套件 **296/296 · 0 fail · 0 todo** ✓ `build-web` exit 0（54 文件、precache **51→52**）+ `check-dist` exit 0（13 pass / 0 fail、G9 CHECK 12 assertions）✓
- **端到端**：`& .\tools\usability.ps1` **exit 0 / 285 s / 11 项全 PASS**，其中信道仿真与 `pskit receive --photo` 吃的正是**带留边与裁切标记的整张 A4**，落盘 `sha256 32ec4805… vs 32ec4805…` **逐字节相同** ⇒ 修复没打断主路径，而且"被扫描的东西"终于等于"用户打印的东西" ✓ `server pid 34768 stopped, nothing left listening on 8123` ⇒ 无泄漏 ✓
- **我自己的口径错，当轮更正（没有为了绿而放宽）**：我先拿"不加 `--sheet` 应与旧 PNG 逐字节相同"当回归判据 ⇒ 实测两者**都是** 2480×3508 ⇒ 因为**纸面档在 CLI 里本来就默认 A4**，"纸面 PNG 变成整张纸"**正是这次修复本身**、不是回归 ✗ 真正必须不变的是**码牌/3D 路径**（无 `sheetMm` ⇒ 走原分支、字节不动 ✓ 冒烟第 5–6 步实测通过）与 **PDF 几何**（`pdf-truesize` 绿 ✓）⇒ 判据据此改正
- **(b)(c)**：两侧源页已用修好的渲染器重出——`.tmp/g2src-a4-300`（3 页 2480×3508）、`.tmp/g2src-a4-600`（3 页 4961×7016），**同一载荷** sha256 `91ab3f7310da452b` ⇒ 与历史语料可直接对比 ✓；两个后台批已起：`pwsh-13`（300 dpi，seed 1..200 → `.tmp/sc-scan300-*`）、`pwsh-14`（600 dpi，seed 1..200 → `.tmp/sc-scan600-*`）⇒ **与旧的 `nc-*` 分开命名，两种前提永远不会混进同一个数字** ✓ **判决未出 ⇒ 本轮不声称任何比率** ✗（300 dpi 约 1.7 h、600 dpi 约 4.5 h ⇒ 跨轮收）
- **规矩守住的一处**：发现 manifest 的 `note` 对纸面产物仍说 `Verify the plate fits: 191.3x278.6mm`（码牌口径 ✗）⇒ 记 **D46** 而**当轮不改**，因为改它要动 `cli/pskit.mjs`、而两个 G2 批正在用同一个文件跑判决 ⇒ **D28：不得编辑正在被测量的代码** ✓
- 证据：`.tmp/t52.txt`（相关 5 个测试文件 56/56）· `.tmp/s52.txt`（296/296）· `.tmp/smoke52.log`（11 PASS / exit 0 / 285 s）· `.tmp/d45-fixed`、`.tmp/d45-nosheet`、`.tmp/g2src-a4-{300,600}` 的 IHDR 实测 · 后台日志 `.tmp/g2-a4-{300,600}.log`（`*>>` 追加的是 UTF-16LE ⇒ 读时剥 NUL ✗）· **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**收 `pwsh-13`（300 dpi × 200 seeds、新前提）的判决** ⇒ 如实写进 ACCEPTANCE 与门限表（这才是"用户今天打印的那张纸"的 G2 数字）；`pwsh-14`（600 dpi）若也跑完一并记，**特别是历史那 2 例失败在新前提下是否消失**（消失 ⇒ 证明是夹具造成的；仍在 ⇒ 归因要再改一次 ⇒ 两种结果都如实记 ✓）→ **D46**（note 按纸面/码牌分叉；等两个批的判决跑完再动 `cli/`）→ D43 的 https 半（**等用户**）→ G4/G6/G9/G10 的用户验收清单在 `docs/USE.md` §5 ✓

### 第 51 轮（**G2 300 dpi 侧 200/200 通过 ⇒ 同轮却查出这个 200/200 的前提不真实：`--sheet` 只对 PDF 生效 = D45**）

- **G2 判决（第 49 轮起的后台批跑完）**：`PASS G2 corpus: 200/200 byte-exact in 1085.4s` · `ALL GATES PASS` · `GATE exit=0`；生成 **200/200 exit 0、0 失败**（`.tmp/nc-scan300-{1..200}`，200 KB 载荷 ⇒ 每份 3 页 ⇒ 共 600 页盲解）；判决 **judged=200 ok=200 fail=0** ⇒ **样本量从判据的 1/6（29 seeds）补到 200、判据一个字没改**（门限自己打印 `criterion is 100%; PLAN asks for 200 fixed seeds, this run had 200` ✓）
- **但同轮把它的前提查穿了（本轮最重要的事）**：语料源页 `.tmp/g2src` 的 PNG = **2260×3290 px @300dpi = 191.35×278.55 mm ⇒ 只有码区、没有纸张**（A4@300dpi 应为 **2480×3508**）；600 dpi 侧 `.tmp/sw-scan600-src` = **4530×6590**、manifest `sheetMm=null`（A4@600dpi 应为 **4960×7016**）⇒ 用**同一个载荷**（`.tmp/payload.bin` 仍在盘上、sha256 `91ab3f7310da452b` 与旧 manifest 记的一致 ✓）跑 `pskit send .tmp/payload.bin --profile P-M1-300 --sheet A4 --format png` ⇒ **exit 0、输出仍 `render 2260x3290px @ 300dpi`** ⇒ **`--sheet` 只作用于 PDF 路径、栅格路径不认** ⇒ 记 **D45**（第 48 轮 D44 只修了 PDF 那一半 ✗）
- **⇒ 撤回我第 49 轮的一个归因**：600 dpi 那 2 例"角标出画"很可能是**夹具没有纸边**（角标紧贴图像边缘 ⇒ 任何姿态/透视都会出画），而**不是**"A4 在平板上带 0.x° 旋转的真实物理" ✗ 已在 `ACCEPTANCE.md` 就地更正；**"不是解码器缺陷"这个结论不变** ✓（3 点只给 6 个方程、单应有 8 个自由度 ⇒ 拒绝在数学上正确）
- **⇒ 因此刻意没有起那个批**：栅格未修时"重出源页"仍是同一形状（`.tmp/g2src-sheet` 与 `.tmp/g2src` **实测同形** ✓）⇒ 跑 200 seeds（300 dpi 约 1 h、600 dpi 约 2.5 h）只是**用同一前提再测一遍、白花机器时间** ✗ 正确顺序 **(a) 修栅格认 `--sheet` → (b) 重出两侧源页 → (c) 两侧各跑 seed 1..200**，**只有 (c) 的结果才配写"达标/不达标"**（已同步写进 `ACCEPTANCE.md` G2 段末、D45、D42 与门限表 L32）
- **冒烟升级到"手机路径不需要 Python"**：第 7 步的 `python -m http.server` 换成 `tools/serve.mjs`（**显式传端口** ⇒ 不触发走位 ⇒ 保证随后两个 check 打的是同一个端口 ✓）并插入 `check-serve` ⇒ 实测 **exit 0 / 289 s / 11 项全 PASS**：`pskit send` 纸面（png + 真尺寸 pdf）、信道仿真、`pskit receive --photo`、**字节相同**（`sha256 32ec4805… vs 32ec4805…`）、码牌 3mf/stl + G8、两条客户端冒烟、`check-serve`、`check-lan`；收尾打印 `server pid 30112 stopped, nothing left listening on 8123` ⇒ **无泄漏** ✓
- **本轮我三个错，全由执行抓到（不是读出来的）**：① 又**按记忆引 `old_string`**（`ACCEPTANCE.md` 那段结尾真实是 `✗ 这一点必须写明…`、我写成 `⇒ …` ⇒ edit 报 not found ⇒ grep 原文后重来）② **`pskit send --in` 不是它的旗标**（真实用法是**位置参数** ⇒ 报错 `pskit send: send: no such file: …\send` ⇒ 抄 `usability.ps1` L117 的已知好用形式才对）③ 读批处理日志踩了 AGENTS 已记那条坑的**变种**：`*>>` 把 node 的判决输出追加成 **UTF-16LE**，而文件头是 UTF-8 BOM ⇒ **混编** ⇒ `Get-Content` 逐字符插 NUL（看起来像"每个字符后面多一个空格"）⇒ 用 node 直读 + 剥 `\u0000` 才看清（AGENTS 那行只写了"`>` 写 UTF-16LE"、没写**混编后的表现** ⇒ 本轮记下 ✓）
- 另记一次**假设被否证**（第 50 轮的尾巴、本轮才查到底）：8131 绑不上 ⇒ 猜"Windows 排除端口段"被 `netsh` 否证（只有 `5357/27339/50000-50059`）、猜"我泄漏了服务"被 PID 全消失否证 ⇒ 真因是 `netstat` 原文那行 `192.168.100.104:8131 → 142.250.157.188:443 ESTABLISHED` = **出站连接占着本地端口** ⇒ 已做成产品行为（走位 + 双成因文案 + 单测 ✓）
- 证据：G2 判决原文在 `.tmp/g2-300-batch.log`（用 node 剥 NUL 读 ✓）· 冒烟在 `.tmp/smoke51.log`（11 PASS / `SMOKE_EXIT=0`）· 源页尺寸由 **PNG IHDR 实测**（字节 16..23 大端）· `pskit send --sheet A4 --format png` 实测 exit 0 而形状未变 · 全量套件（见提交前实测）· 本轮只改 `docs/` 与 `tools/usability.ps1` ⇒ `core/`、`web/dist` 未动、不需重建 ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**修 D45**——让栅格路径认 `--sheet`（与 PDF **共用同一套居中/标记算术** ⇒ 抽成共享函数，否则两边会各自漂移 ✗）+ 单测（含"**不传 sheet 时逐字节不变**"的回归保护，因为 `web/sender.js` 与 CLI 的 PNG 输出都被现有测试盯着 ✓）→ 重出两侧源页 → 起 (c) 的 200 seeds × 两侧（后台分批）→ D43 的 https 半（**等用户**）→ G4/G6/G9/G10 的用户验收清单已在 `docs/USE.md` §5 ✓


### 第 50 轮（**手机路径不再需要 Python、也不需要用户自己找 IP**：`tools/serve.mjs` + `tools/check-serve.mjs`，并把一个 Windows 端口陷阱做成了产品行为）

- **新 `tools/serve.mjs`**（零依赖、只用 node 内建 ⇒ `tools/` 允许 ✓）：起服务即**打印本机地址与每块网卡的局域网地址**（用户最常卡住的就是"我的 IP 是多少"、以及"要装 Python"⇒ 两件都替他做了）；默认**绑所有网卡**（手机必须能连）；`Cache-Control: no-store`（重建后下一次请求即生效，不必等缓存过期）；**目录穿越一律拒**；`web/dist` 不存在 ⇒ **可操作指引**（`node tools/build-web.mjs` + 指向 `USE.md` §0，不给光秃秃的 404）；`Ctrl+C`/SIGTERM 干净停止
- **新 `tools/check-serve.mjs`（10 项）⇒ 实测 exit 0 / 10 PASS**：`/index.html` 与盘上文件 **sha256 相同**（`d049db98…` 两边一致）、`GET /` 与 `/index.html` 同一份文档、**四种目录穿越全部被拒**（`/../package.json`、`/..%2fpackage.json`、`/sub/../../package.json`、`/%2e%2e/package.json` ⇒ 403/404、**零字节泄漏**；这台服务默认绑所有网卡，**一个会回答 `../` 的静态服务等于整盘可读** ✗）、缺文件 **404 且不回退首页**（打错字不能看起来像成功）、`HEAD` 正常、`manifest.webmanifest` **MIME 正确**（类型错浏览器就不当 manifest 解析）、`no-store` 在位
- **等价性证据（不是"换了个没验过的服务器"）**：`node tools/check-lan.mjs --port 8001/8137` 对**我这台服务器**同样 **exit 0、51/51 条预缓存资源逐字节一致** ⇒ 与 `python -m http.server` 交出去的字节等价 ✓
- **端口这件事：我两个假设都被实测否证，真因是第三种**——8131 绑不上、而 `netstat` 的 LISTENING 过滤查不到任何东西 ⇒ 先猜 **Windows 排除端口段**：`netsh interface ipv4 show excludedportrange protocol=tcp` 实测只有 `5357 / 27339 / 50000-50059` ⇒ **否证** ✗ 再猜**我泄漏了服务**：三个可疑 PID 全已消失、`Get-NetTCPConnection -LocalPort 8131` 也空 ⇒ **否证** ✗ 真因是 `netstat -ano | findstr 8131` 那一行：`TCP 192.168.100.104:8131 → 142.250.157.188:443 ESTABLISHED` ⇒ **一条出站连接把 8131 当自己的本地端口**，Windows 上这会让 `bind` 报 `EADDRINUSE`，而 LISTENING 过滤与 `Get-NetTCPConnection` 都看不见它 ⇒ **这是真用户会撞的坑**（手册叫用的 8000 随时可能被某个浏览器临时占走）⇒ 所以做成**产品行为**而不是备注：未显式给 `--port` ⇒ **自己往上走并说明原因**（实测：8000 被占 ⇒ 打印两种成因 ⇒ 走到 **8001** 并正常服务、横幅给出 `http://192.168.100.104:8001/index.html` ✓）；显式给了 ⇒ **只报错、绝不偷偷改**（实测 exit 1 ✓）；错误文案点名**两种成因**并给出正确的排查命令 `netstat -ano | findstr :端口`（**看所有状态**，别只看 LISTENING）
- **我自己两处错，都由执行当场抓到**：① `check-serve` 的参数解析**先 `++i` 再比 `argv[i]`** ⇒ 比到的是**值**不是旗标 ⇒ 这工具**一次都没跑起来**（`unknown argument 8001`）⇒ 已修并写明原因 ② `serve.mjs` 改走位逻辑时把 `listen` 挪进 `start()` 闭包，却**忘了封口与调用** ⇒ 用 `node --check` 先过一遍才继续 ⇒ 没让它活到运行时 ✗
- **一次如实更正（不是产品缺陷）**：日志里网卡名显示成 `涔欏お缍茶矾` ⇒ 是**我 `Get-Content` 没加 `-Encoding UTF8`**（node 写 UTF-8、Windows PowerShell 5.1 默认按 ANSI 读）⇒ 用 `-Encoding UTF8` 读即正确显示 `乙太網路` ⇒ **不改代码**，只记下"读子进程输出要显式 UTF8"（与 AGENTS 陷阱表里"重定向写 UTF-16LE"同族 ✓）
- **手册同步**：§2 改成 `node tools/serve.mjs`（Python 保留为等价备选）、加端口两种成因与排查命令、加 `check-serve`；并修掉一处**过期文字**——§2 还写着"第二个安装阻塞：manifest 里唯一图标是 `icon-page.png`、3290×3290"，那是**第 44 轮就修好的** ✗ ⇒ 改成"图标半已修（192/512 `any` + 512 `maskable` 画在 80% 安全区 + `check-dist` 构建期强制），**只剩 https、而且只有你能解**" ✓
- **G2**：第 49 轮起的后台批（seed 30–200 生成 + 自动判决）仍在跑 ⇒ **判决未出 ⇒ 本轮不声称任何比率** ✗（收尾进度见提交时的日志）
- 证据：`node --check tools/serve.mjs` exit 0 · `check-serve` **10/10 PASS exit 0** · `check-lan` **exit 0 / 51 entries 字节一致** · 走位路径与显式失败路径**均实测** · 缺 dist 指引实测 exit 1 · **测试进程与 8000/8001/8137 监听零残留** ✓ 全量套件（见提交前实测）· 本轮只加 `tools/` 与 `docs/` ⇒ `core/`、`web/dist` 未动、不需重建 ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**收 G2 判决**（scan300 × 200 seeds）⇒ 把真实比率如实写进 `ACCEPTANCE.md` / 门限表 / D42 → 起 **scan600 seed 17–200**（生成 + 判决，600 dpi 判决约 30 s/份 ⇒ 后台分批）→ 把 `serve.mjs` + `check-serve` **接进 `usability.ps1` 第 7 步**（替掉 python ⇒ 冒烟本身就覆盖"只需 Node 的手机路径"）→ D43 的 https 半（**等用户**）


### 第 49 轮（**台账自己坏了两行、而且三条守卫全绿**⇒ 加了单元格数守卫，它当场抓到两行旧账 + 我本轮新写的一行）

- **为什么这算"可用"路上的事**：每一轮的优先级都是**读台账**定的（`DEFECTS.md` 决定修什么、`STATUS.md` 决定下一步）⇒ 表格结构坏了 = 状态列/复验列被读成碎片 = **优先级建立在误读上** ✗
- **新守卫**：`tests/unit/defects-ledger.test.mjs` 里把裸 `split('|')` 换成 **`cellsOf`（只按未转义竖线分割 `(?<!\\)\|`）**，再加一条**"每个缺陷行必须正好 4 格"**，并给**分割器本身**做阳性对照（`a\|b` 必须留在同一格、裸 `a|b` 必须被切开 ⇒ 若 `cellsOf` 退回旧行为，这两条立刻红 ⇒ 守卫不是装饰 ✓）
- **它立刻抓到两行真坏了的旧账**（⇒ 不是空转 ✓）：L84 `~~D36~~` **缺行尾收口的 `|`** ⇒ `slice(1,-1)` 把**状态列整格丢掉**、数出 3 格；L217 `~~D12~~` **多一个 `|`**（第 46 轮我写那条 CLOSED 时 `new_string` 自带结尾竖线 ⇒ 文件成了 `… ✓ | |`）⇒ 两行都**只靠删除线碰巧判对状态**（状态列根本没被读到）
- **旧守卫为什么全绿（机制说清，不含糊）**：`rowsOf` 的 `saysClosed` 看 `cells[last]`——D36 行状态列被丢 ⇒ 读到复验列（无 CLOSED）⇒ 但**删除线**让它仍判 CLOSED ✓ 绿；D12 行末格是空串 ⇒ 同样靠删除线绿 ✓；"复验非空"那条读到的是**状态列碎片**（含字母数字）⇒ 也绿 ✗ **三条判据全绿、两行全坏** ⇒ 结论：**守卫自己也得被测**（本轮就是这么补的 ✓）
- **而且它本轮就抓到了我自己**：改 D42 状态列时，我的 `new_string` **又自带一个结尾 `|`**（与第 46 轮 D12 那次**同一个错**）⇒ 全量套件当场红（`malformed ledger rows`）⇒ 修掉后 **286/286** ✓ 差别在于：上一次这个错**在账本里躺了一整轮、三条判据全绿**，这次**没活过一轮** ⇒ 这就是把纪律变成机器的价值（证据：修复用的锚点 `不会被读成达标 ✓ | |` **匹配成功** ⇒ 双竖线确实存在过 ✓）
- **D42 逐条核对原文 ⇒ 四条剩余动作里三条早已完成、还有一个子项是我的过期信息**：① grep 全 `docs/` 的"带外 / 由用户生成 / 只能由用户"⇒ **`ACCEPTANCE.md` 零命中**（G2 段现在写的是"第 37 轮起这是门限、不再只是手工命令"并给出 gate 命令 ✓）⇒ 命中只剩 D42 行自己与 `STATUS.md` 的**历史交接记录**（历史按原样保留、不改写 ✓）；③ D41 行那句"现有保护只有篡改用例 + 正例"**已就地标注**为"已被同轮的对拍测试取代"（原文保留、只加标注 ⇒ 记录当时的缺口，别当现状读 ✓）；④ `AGENTS.md` 陷阱表已明写"作用域仅限 node 进程内的管道 stdio；宿主 shell 跑 python/git/node 是通的" + 已验证命令 + `timeoutMs` 600 s 封顶 ✓；② 里的"诊断两类 600 dpi 失败"**其实早已完成**——`ACCEPTANCE.md` 已像素级定界（失败页四边着墨率 `top 4.7% / left 89.5% / right 80.8% / bottom 100%` vs 健康页四边 100% ⇒ **角标出画 ⇒ 属信道前提、不属解码器**；3 点只给 6 个方程、单应需 8 个自由度 ⇒ **拒绝在数学上正确**；判决**不上调**、仍记 30/32 ⇒ 把正确拒绝剔出分母等于重定义判据 ✗）⇒ **我第 49 轮之前的上下文摘要把它当"待诊断"是过期信息** ✗ 已核对原文纠正、并把这件事本身写进 D42 行 ✓
- **G2 实质推进（判据不动）**：scan300 侧原为 **seed 1–29**（29/29 逐字节 ✓），本轮起**后台批 seed 30–200**（`pwsh-10`：`python sim/channel.py --in .tmp/g2src --out .tmp/nc-scan300-N --seed N --preset scan300 --modifier nocrop` 逐份生成，跑完自动接 `verify --gate G2 --root .tmp --match 'nc-scan300-*'`）⇒ 本轮收尾时进度 **seed 59/200、每份 exit 0** ⇒ **判决尚未产生 ⇒ 本轮不声称任何比率** ✗ scan600 侧现有 **seed 1–16**，下一轮从 `.tmp/sw-scan600-src` 同法补齐
- 证据：ledger 守卫 **7/7**（新增 2 条）· 全量套件 **286/286 · 0 fail · 0 todo** ✓ 本轮只改 `tests/` 与 `docs/` ⇒ `core/`、`web/dist` 未动、不需重建 ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**收 `pwsh-10` 的 G2 判决**（scan300 × 200 seeds）⇒ 把真实比率如实写进 `ACCEPTANCE.md` / 门限表 / D42；再起 **scan600 侧 seed 17–200** 的生成+判决（600 dpi 判决约 30 s/份 ⇒ 必须后台、分批）→ D43 剩下的 https 半（**等用户**：仓库 URL + 凭据 + `pages.yml`）→ 硬件门限 G4/G6/G9/G10 的用户验收清单已在 `docs/USE.md` §5 ✓


### 第 48 轮（**D44 真修掉了**：`pack.pdf` 的页现在是"纸"，码区按标称尺寸居中，页边距里有矢量裁切标记与套准十字）

- **改法（三处小改，都在 `core/`）**：`layout.js` 的 `pageLayout` **把纸张记进返回值**（`sheetMm`；板材路径为 `null`——板材的产物是 3MF/STL，在板材 PDF 上画标记只是噪声）；`raster.js` 的 `renderPageBitmap` **把 `sheetMm` 挂到 bitmap 上**（**故意不挂 `pageMm`** ⇒ 内容框仍按 `width/dpi` 推 ⇒ **不传纸张的调用方逐字节不变**，`render-pdf.test.mjs` 那些 byte-for-byte 断言原样绿 ⇒ **这是证据，不是声明** ✓；于是 `web/sender.js` 与 `cli/pskit.mjs` **零改动**就拿到了修复）；`pdf.js` 新增 `sheetMm` + `checkSheetMm`、**页 = 纸张**、码区**居中**（CTM 由 `W 0 0 H 0 0 cm` 变 `W 0 0 H TX TY cm`）、substrate 铺满整张纸、页边距画**四角裁切 L + 四边套准十字**（`0 0 0 RG` / `1 w`，矢量 ⇒ 与打印分辨率无关）。标记尺寸**由页边距自身推出**（gap .25 / arm .55 / cross .30 ⇒ 合计 0.8 ⇒ 永远放得下**且够不到码区**——固定 5 mm 的标记在 3 mm 边距上要么被打印机裁掉、要么落在码区里，而**解码器不知道的墨，正是页面开始解不出来的地方**）。纸张小于页面 ⇒ **抛 `cannot carry`**，不悄悄裁墨（`pageLayout` 本来就拒；写盘侧再拒一次 ⇒ **不信任调用方的写入器才不会某天裁了墨还一声不吭**）
- **判据是升级、不是放宽**：`tests/unit/pdf-truesize.test.mjs` 用**三条互不共享代码的算路**对拍（协议几何 `geom.sheetMm` / 版面权威 `pageLayout.physicalMm` / PDF 实字节），`72/25.4` **在测试里重算、不 import**；新增**居中算术校验**（`translate == (MediaBox − 内容)/2`，因为"页尺寸对"完全没说墨落在哪儿——页被塞到左下角的话，用户照裁切标记 trimming 就会切到码区）与**"标记必须在码区之外"**；阳性对照 4 条（纸张缩 **94%**、码区缩 **90%**、**不居中**、合成幅面 50×40 mm）+ 太小纸张必须抛 ⇒ `todo` **摘掉**，**套件 284/284、0 fail、0 todo** ✓
- **实测数字**：`pack.pdf` **3 页 · 页 = 210×297 mm = `595.28 841.89` pt · 码区 191.35×278.55 mm 绘在 `(26.44, 26.14)` pt · 每页 16 段标记 · 706980 B** ✓
- **哪些字节变了（如实记）**：只有**传 `sheetMm` 的调用方**的 `pack.pdf`（sender 与 CLI）——页数不变、图像 XObject 不变；变的是 `MediaBox`（`542.4 789.6` → `595.28 841.89`）、内容流多出标记块与居中平移、有 substrate 时填充矩形改为整张纸 ⇒ **仓库里没有冻结的 PDF 字节 fixture**（`render-pdf.test.mjs` 的 `eqBytes` 全是"同输入重编码"自比）⇒ **无需更新任何 fixture** ✓
- **我自己两次写错、又两次自己抓出来**：① 测试里第 3 条**同义反复断言**（拿 X 跟 X 比 ⇒ 永远真）② 用 `toFixed` 之后的值做**严格相等**（写盘按两位小数四舍五入 ⇒ 天生差 0.01 pt）⇒ 改成**同一个带容差的判据**（顺带证明它对合成幅面也成立）
- **一次假警报，也如实记**：我以为账本里 D44 那行表格坏了（`split('|')` 数出 11 个单元格、表头只有 4 列），实际文件里写的是 **`\|` 标准 Markdown 转义** ⇒ **是我的检查工具不认转义，不是账本坏** ⇒ **一个字都没改** ✓ 教训：若要加"表格单元格数守卫"，必须用 `(?<!\\)\|` 分割，否则会把**正确转义**的行判成坏的——**守卫比它守卫的代码更错**，这仓库已经栽过两次（D40、advice-coverage）⇒ 记入下一轮
- **顺带修手册里的过期数字**：`check-lan` 的一致性早就是 **51/51**，`docs/USE.md` 还写着 48/48 ✗ ⇒ 改成"这个数字等于本次构建的预缓存条数、随构建变化，**不要当常数背**"；并把打印那一节改写为新产物的事实（页 = A4、码区居中、距纸边约 9.3 mm、沿裁切标记修剪）
- 证据：套件 **284/284** ✓ 改了 `core/` ⇒ `node tools/build-web.mjs` **exit 0**（重建 dist、precache 51、原子替换）· `node tools/check-dist.mjs` **exit 0** · `& .\tools\usability.ps1` **10 PASS / exit 0 / 267 s**（`pack.pdf` 802 KB、**收发 sha256 `32ec4805…` 一致**、板材 4×`.3mf`+`.stl` 过 G8、`smoke-sender` 180 s、`smoke-capture` 39 s、`check-lan` ✓ 无残留进程）✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：给 `docs/DEFECTS.md` 加**表格单元格数守卫**（`(?<!\\)\|` 分割 ⇒ 抓裸竖线、不误伤转义）→ G2 补到 200 seeds（分批 ≤600 s）→ D42 剩余：改正 `docs/ACCEPTANCE.md` 的 G2 段落、清 D41 行里那句已过时的话 → D43 剩下的 https 半（**等用户**：仓库 URL + 凭据 + `pages.yml`）


### 第 47 轮（做了 D8 可自动化的那一半 ⇒ **当场挖出 D44**：PDF 的页不是纸、是码区，且裁切/套准标记根本没实现）

- **新检查 `tests/unit/pdf-truesize.test.mjs`**（D8 的产物侧，不需要浏览器）：期望尺寸走**另一条独立算路**（`encodeTransfer` → `geom.sheetMm`，`PT_PER_MM` 在测试里重算 `72/25.4`、**不 import** ⇒ 不会自己跟自己同意 ✓）；实际值解析**用户真会拿到的那份 PDF**（`buildArtifacts` → `r.pdf`）里每个 `/MediaBox` 与每个绘图 CTM，并断言 **CTM 字串 == MediaBox 字串**（两侧同一格式化器 ⇒ 比容差更严、不会漂移）
- **两条阳性对照先证明判据可信**：MediaBox 缩到 **94%**（"适合页面"的形状）⇒ 报出 ✓；**只缩 CTM、MediaBox 不动**（只查页尺寸必漏的失败模式）⇒ 报出 ✓ ⇒ 所以下面这个失败**是产物的问题，不是判据太严**
- **D44（OPEN，本轮新记）**：`pack.pdf` 每页 `/MediaBox [0 0 542.4 789.6]` = **191.35×278.55 mm**（= `layout.physicalMm` 码区），而 `geom.sheetMm` 是 **A4 210×297** ⇒ 差 **18.65×18.45 mm**；且 grep 全链路 `crop|registration|裁切|套准|trim` **零命中** ⇒ **裁切/套准标记压根没实现**，而 objective ① 明文要它 ⇒ 两条后果：没有页边距 ⇒ **没地方放标记**；用户装 A4、对话框里却是 191×279 ⇒ **更容易顺手点"适合页面"**，正好撞上 D8 的失败模式。**危害边界如实写清**：100% 打印时几何仍正确（解码器从基准块自识别 ⇒ **不是**数据正确性缺陷 ✓），伤的是可用性与明文要求
- **判据没有为了变绿而放宽**：那条断言原样保留、改成 `{ todo: 'D44 …' }` ⇒ 实测 node:test 的 todo 语义：**失败但不拉红套件**（**282 项 / 281 pass / 0 fail / 1 todo / exit 0** ✓），差值按 mm 照实打印，修好那天它自己变成真通过、测试文件不用改 ✓
- **我自己写测试时留了两条废话断言、跑之前自己抓出来删掉**：一条 no-op 替换的"守卫"（断言"什么都没改"⇒ 永远真）、一条 `!x === false` 的怪写法 ⇒ 换成"字符串里不闭合的 `/*`"式那种**真能失败**的形状（**新写的断言要先问它怎么可能失败**——本轮又救了一次 ✓）
- 证据：`tests/unit/pdf-truesize.test.mjs` **3 项：2 pass + 1 todo** · 全量 **282/281 pass/0 fail/1 todo、exit 0** ✓ 本轮**只加 `tests/`** ⇒ `core/` 与 `web/dist` 未动、不需重建 ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**照 D44 的修法改 `core/render/pdf.js`**——页 = 纸张（新增 `sheetMm`，与既有 `pageMm`=内容分开）、码区**居中**（CTM 加平移）、四角画**矢量裁切标记 + 套准十字**（`m`/`l`/`re`/`S`，与打印分辨率无关 ✓）；发送端与 CLI 传 `sheetMm`（`--sheet A4|Letter` 已有）；**改 `core/` ⇒ 必须重建 `web/dist` + 跑 `check-dist`**；`pdf.js` 自称保持 byte-for-byte fixtures ⇒ **旧 PDF fixture 会失效，必须同时更新并如实记录哪些字节变了**；最后摘掉 todo、期望它变真通过 → 之后：G2 补到 200 seeds → D43 的 https 半（等用户）


### 第 46 轮（**D12 闭合**：判据扫描器不再把注释里的散文当代码——修法是剥注释，**不是**放宽判据）

- **修的是什么、为什么算"可用"**：`advice` 是**给用户看的建议文本**（哪一页、哪种失败、该怎么办），决策 11 要求"每个失败 reason 都必须有建议"，而唯一强制执行它的就是 `tests/unit/advice-coverage.test.mjs` 的源码扫描 ⇒ 它的正则**会读注释**：我上一轮为解释"把成功路径的 reason 改名"而在注释里写下那个字面量，测试立刻要求给一条**成功**配建议 ✗ 判据扫描器把散文当代码，与我在 `build-web` 里犯过的是同一族
- **关键取舍（唯一不可接受的失败方向）**：剥离器写成**"字符串 / 模板 / 正则字面量 / 注释"四态扫描器**，而**不是**拿正则删 `/*…*/` 与 `//…`——因为**吃掉一条真 reason = 悄悄把要求变少**，那比原来的假阳性严重得多 ⇒ ① `stripComments` **8 条正例**（块/行/文档注释里引用整行代码、字符串里的 `//`、正则里的 `\/\/`、`return /…/` 之后的正则字面量、转义引号、**字符串里不闭合的 `/*`**——naive 剥离器在这里会把后面整段吃掉）② **原有两条灵敏度钉子原样保留**（`seen.size >= 20`、点名 `intra-fail`/`other-session`）⇒ **剥过头会红、不会静默** ③ 新增**漂移守卫**：同一棵 `core/` 跑"剥 / 不剥"两遍并断言**完全一致** ⇒ 今天的树上没有"只活在注释里的 reason"；将来谁写出一个，这条会**点名它**而不是让要求集悄悄变化 ✓ 模板字面量里的 `${}` 不跟踪——**这条简化写在注释里**，不是藏着（真出问题会让钉子变红 ✓）
- **我自己先写出一条空转断言、又自己抓出来**：最初那条把 `reason` 放在 `//` **之前** ⇒ 剥离对错都会过 ✗ 换成"字符串里不闭合的 `/*`"这种真能失败的形状 ✓（**新写的断言要先问它怎么可能失败**——这条规矩第 N 次救场）
- **顺带纠正我上一轮的待办**：我把 **D5** 列进"下一轮首位"，但账本里它**早已闭合**（`~~D5~~`，复验 `Select-String web/dist/pskt-file.html -Pattern sender` ⇒ 无）⇒ **读记忆没读账本** ✗ 本轮核对原文后从待办里去掉 ✓（同类错误：D40 是读网络没读 `ref/`、这次是读记忆没读 `DEFECTS.md` ⇒ 都是"先看手边的事实"）
- 证据：`tests/unit/advice-coverage.test.mjs` **4/4**（原 2 条 + 剥离器正例 + 漂移守卫）· 全量 **279/279**（277 + 2 新）✓ 本轮**只改 `tests/`** ⇒ `core/` 与 `web/dist` 未动、**不需重建** ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**D8 里可自动化的那一半**——浏览器怎么缩放要真浏览器（✗ 属 G9/D18），但"**`pack.pdf` 里的页尺寸是否等于 profile 标称的物理尺寸**"完全可以在进程内验（读 PDF 的 `MediaBox` 与 `sheetMm` 对比 ⇒ 缩放问题的**根因侧**、且不需要浏览器 ✓）→ G2 补到 200 seeds（分批 ≤600 s）→ D43 剩下的 https 半（**等用户**：仓库 URL + 凭据 + `pages.yml`）


### 第 45 轮（"一条命令"名副其实了：端到端冒烟**接上了手机端的服务面**，10 步全 PASS）

- **`tools/usability.ps1` 加第 7 步**：`Start-Process python -m http.server <port> --directory web/dist`（隐藏启动）→ `tools/check-lan.mjs --port <port>` → `finally` 里**按 pid** `Stop-Process` 并断言"自己起的那个进程确实没了"⇒ 实测 **10 步全 PASS / exit 0 / 286 s** ✓ 收尾打印 `server pid 36120 stopped, nothing left listening on 8123` ✓ 默认开着（`-SkipServe` 可关、`-ServePort` 可移）
- **刻意的设计取舍（都写进脚本注释，不是随手写的）**：① 残留检查**按 pid**、**不数 python 进程**——数数会把用户自己在跑的东西算到脚本头上，制造假失败 ✗ ② `try/finally` 不是装饰：**漏一个还在监听的服务，比冒烟失败更糟** ③ `web/dist` 不存在时**直接 FAIL 并给可操作指引**（`node tools/build-web.mjs`，指向 `docs/USE.md` §0）⇒ 刚 clone 的人不会被一句光秃秃的 FAIL 糊弄 ④ **修了我自己头注释里的错例子**：原先写 `. .\tools\usability.ps1`（点源）⇒ 脚本以 `exit` 结尾，点源会把调用它的 shell 一起带走 ✗ 改成 `& .\...` 并写明原因（AGENTS 陷阱表里那条正是上一轮踩出来的 ✓）
- **防"跑了个空还报 PASS"**：第 7 步计时显示 `0s`（本地回环 51 个小文件确实快 ✓），但**不看计时看日志**——`step7.log` 里必须有 `51 entries` 与逐条 PASS 才算真跑过 ⇒ 提交前把这一行打出来核对 ✓（这正是本仓库反复吃亏的那类假绿：**什么都没做的步骤也会 exit 0** ✗）
- 脚本收尾自己就如实打印**它没证明什么**：真墨真纸、真手机摄像头、浏览器打印缩放（D8）、PWA 安装（需 https）、G4/G6/G9/G10 ⇒ 并指向 `docs/USE.md` 的用户验收清单 ✓
- 证据：`& .\tools\usability.ps1` ⇒ **exit 0 / 10 PASS / 286 s**：`send` 3 页 + 800 KB 真实尺寸 PDF → `sim/channel.py` 15 s → `receive --photo` 4 s → **sha256 `32ec4805…` 收发一致** → 板材 `PL-D2@0.4` 写出 4×(`.3mf`+`.stl`) → `verify --gate G8 --file` 5 s → `smoke-sender` 193 s → `smoke-capture` 40 s → `check-lan` ✓ 无残留进程 ✓ · 单测 **277/277** ✓ 本轮只改 `tools/`（`core/` 与 `web/dist` 未动 ⇒ 不需重建 ✓）· **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：客户端缺陷 **D12**（advice scanner 对注释盲）/ **D5** 残余站点链接 / **D8** 打印缩放中可自动化的部分 → G2 补到 200 seeds（分批 ≤600 s）→ D43 剩下的 https 半（**等用户**：仓库 URL + 凭据 + `pages.yml`）


### 第 44 轮（**D43 的图标阻塞真修掉了**——修在构建器里、零依赖，并加了构建期强制断言）

- **修复**：`tools/build-web.mjs` 的图标块里加**盒式降采样 + 亮度阈值**，产出 `icon-192.png`(1279 B)、`icon-512.png`(6093 B)、`icon-maskable-512.png`(6084 B)；`manifest.webmanifest` 的 `icons` 现在是 192/512(`any`) + 512(`maskable`) + 原 `icon-page.png`(3290×3290, 283808 B，保留给想要更大图的启动器)。**先平均再阈值**是必要的：6× 缩小时单纯平均会把码页糊成灰泥，阈值化才保住角上的基准块与格子纹理 ✓ **maskable 画在 80% 安全区内**：自适应启动器从中间裁圆/裁方角，裁掉的正好是基准块——这张图里唯一有意义的部分 ✓ **没有新建 `make-icons.mjs`**（原计划）⇒ 因为构建器里本来就有一块"用 `renderPageBitmap`+`encodePNG` 画真页图当图标"的代码（D13 的方形补丁 ⇒ `3290x3290` 正是它来的）⇒ 在那里加降采样更省、且仍然是**本项目自己的编码器画的**（零依赖 ✓ 构建不可能声称一张它画不出的图 ✓）
- **加了构建期强制断言**（`tools/check-dist.mjs`）：manifest 的 icons 必须含 `192x192` 与 `512x512`、且必须有 `purpose: maskable`，否则**构建就红** ⇒ 这条性质不会烂回去 ✓ 断言的理由写进了注释：**"声明了图标"不等于"浏览器会弹安装"**——D43 修之前，这条检查就是在只有一个 3290×3290 页图的情况下通过的 ✗
- **顺带修掉我自己工具里的一句假话**：`check-lan` 原先硬编码打印"两个安装阻塞 neither is fixed"⇒ 图标修好后那句就不成立 ✗ 改成**逐条如实报告**：阻塞 1（局域网 http 非 secure context ⇒ 需 https）**STILL BLOCKED、且只有用户能解**（仓库 URL + 凭据 + `pages.yml`）；阻塞 2（图标尺寸）**satisfied：192/512 已声明，另有 maskable** ✓
- **解决了一个记在案的未知**：**`Start-Process` 在沙箱里可用** ⇒ 实测两次（`Start-Process python -ArgumentList '-m','http.server','8125','--directory','web/dist' -WindowStyle Hidden -PassThru` → `node tools/check-lan.mjs --port 8125` → `Stop-Process -Id` → **残留 python 进程数 0** ✓）⇒ 下一轮可以把 `check-lan` 接进 `usability.ps1` 成为真正的"一条命令"（配方已验证 ✓ 不再需要在文档里承诺没验过的东西）
- **差点"修"一个不存在的问题**：`check-dist` 打印的 `all 12 assertions pass` 看着像我加断言后就过期了 ⇒ 读源码发现它是**动态计数**（L315 `results.length`），而我加的是某个既有检查**内部的 `throw` 守卫**、不新增 result ⇒ **12 仍然准确、没有假话、不需要改** ✓（记下来，免得下轮又去"修"它）
- **一处未查明、如实记为待查**：第 44 轮某次 pwsh 调用外层报 `[exit code: 1]`，而其中**每个子步骤都是 exit 0**（build/check-dist/check-lan/Stop-Process 全绿）；探针（失败的 `Get-Process` ⇒ `$?` False、`$LASTEXITCODE` 为空）**没能复现** ⇒ **不当失败也不当通过**；对策是这类调用末尾显式 `exit 0`（本轮后半已这么做 ✓）
- 证据：`node tools/build-web.mjs` **exit 0**（`web/dist: 53 files, build e5ce1d897e9134d3`、`precache entries 51`、原子换目录 ✓）· `node tools/check-dist.mjs` **exit 0** · `node tools/check-lan.mjs --port 8125|8126` **两次 exit 0**、**51/51** 条预缓存资源逐条 fetch 且 `core/hash.js` 算出的 sha256 与清单一致、4 页无外部 URL、4 页都有 CSP meta ✓ · 单测 **277/277** ✓ · 无残留 python 进程 ✓ · `core/` 未动（`web/dist` 重建，变化来自新图标与清单 ✓）· **不打新 tag**（M4 未闭合 ✓）
- **提交后又发现一个真阻塞（同轮补修 ✓）**：`web/dist/` 被 `.gitignore` 第 4 行忽略、`git ls-files web/dist` = **0 个文件** ⇒ **刚 clone 的用户手里没有任何可打开的客户端页面** ✗ 而 `docs/USE.md` 上来就叫人"打开 `web/dist/pskt-send-file.html`"⇒ **手册第一步就踩空** ⇒ 已补 **§0 第一步：构建客户端页面**（`node tools/build-web.mjs` → `node tools/check-dist.mjs`，并写明"改了 `core/` 必须重建"、图标由构建器用本项目自己的编码器画）✓ 教训：**用户手册要从"用户手上什么都没有"写起**，而不是从我这台机器上已经有的东西写起 ✗
- 下一轮首位：**把 `check-lan` 接进 `usability.ps1`**（加 `-Serve`：Start-Process → check-lan → Stop-Process，配方已验证 ✓）⇒ 端到端冒烟就覆盖到手机端的服务面 → D12（advice scanner 对注释盲）/ D5 残余站点链接 / D8 打印缩放可自动化部分 → G2 补到 200 seeds（分批 ≤600 s）→ D43 剩下的 https 半（**等用户**：仓库 URL + 凭据 + `pages.yml`）


### 第 43 轮（把"手机路径的服务端半"从**文档里这么说**变成**实测**；顺带量出 PWA 的第二个安装阻塞）

- **交付 `tools/check-lan.mjs`**：真起 `python -m http.server 8123 --directory web/dist`（后台作业，用完 `job_kill`，不留游离服务 ✓），然后做**浏览器会做的事** ⇒ 实测 **exit 0**：
  - SW 预缓存清单 **48/48** 条逐条 fetch，并用**我们自己的 `core/hash.js`** 比对 sha256 ⇒ 全一致（`sw.js` 的 install 对每条做 `cache.add(new Request(url))`，**任一 404 就装不上** ⇒ 这条等价于"安装不会失败在取资源上"✓）；`tools/check-dist.mjs` 查的是"清单 ⇄ 磁盘"，本工具查的是"清单 ⇄ **服务器真交出来的字节**"，两半互补 ✓
  - **4 个页面无任何外部 http(s) 引用**（扫 `src=`/`href=`/`fetch()`/`import()`，只豁免 `xmlns` 命名空间串）⇒ **气隙契约 ③ 在 http 源上同样成立** ✓
  - **4 个页面都带 CSP meta** ✓ ⇒ D18 里"待人工确认 CSP"的一半有答案了（meta 在 ✓ 浏览器如何执行仍需真机 ✗）；并如实注明 `python -m http.server` 不发 CSP 头，策略只可能来自 meta ✓
  - 瘦页模块图在 http 上全部解析（`index.html`→`app.js`/`capture.js`/`manifest.webmanifest`；`send.html`→`sender.js`）✓ manifest 解析成功、`start_url ./index.html` 可取 ✓
- **量出并记账 D43（OPEN）**：PWA 安装有**两个各自独立的阻塞**——① 局域网 http **不是 secure context** ⇒ 浏览器拒绝注册 SW 与安装（代码照实处理：只在 `secure` 时注册 ✓）⇒ 需 https 托管，等用户的仓库/凭据 + `pages.yml` ② **manifest 唯一图标是 `./icon-page.png`、`sizes` 写着 `3290x3290`**（**整页渲染被当图标用** ✗ 安装性要 192×192 与 512×512）⇒ 修法**必须动构建**（`tools/make-icons.mjs` 用 `core/render/raster.js`+`png.js` 画两个图标 → `tools/build-web.mjs` 写进 manifest 的 `icons` 并纳入 SW 清单 → **重建 `web/dist`** → `check-dist` 加断言 → `check-lan` 复验）；**手改 dist 是错的**（dist 是生成物、SW 清单带哈希 ⇒ 会与 `check-dist` 打架 ✗）
- **工具的自我约束**（都是刻意写的）：两个安装阻塞**打印但不计入 exit code**——那不是它声称要验的东西 ⇒ **报出来 ≠ 通过** ✓；服务没起时**一次性说清怎么起并退出**，不刷 48 条失败噪声 ✓；`redirect:'manual'`、fetch 异常当结果不当崩溃 ✓；清单条数**从文件里读**、不写死（头注释里的"48"是实测值并注明"as built"✓）
- 文档：`docs/USE.md` §2 增补"**不用手机也能先验服务端这半**"的两条命令与实测结论、以及 D43 的第二个阻塞 ✓ `tools/check-lan.mjs` 头注释写明"这条指令在写下这个文件之前只是散文"⇒ 现在不是了 ✓
- 证据：`node tools/check-lan.mjs --port 8123` ⇒ **exit 0**（日志 `.tmp/lan1.txt` ✓）· `core/` 与 `web/dist` **未动** ⇒ 不需重建 ✓ 单测提交前重跑 ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**照 D43 的修法真把图标做出来**（这是"每轮必须修掉真正阻塞的东西、不是记账了事"的直接对象 ✓）→ 把 `check-lan` 接进 `usability.ps1`（需**先验** `Start-Process`/`Stop-Process` 能否在沙箱里起停 python ✗ 未验 ⇒ 在那之前**不在文档里承诺"一条命令"**）→ D12 / D5 残余 / D8 可自动化部分 → G2 补到 200 seeds（分批 ≤600 s）


### 第 42 轮（目标改为"**能跑起来并且可以用**" ⇒ 一条命令的端到端 usability 冒烟**跑通了** ✓）

- **目标已改写并重新武装**：按用户口径（"能跑起来并且可以用，一直循环直到完成"）⇒ `update_goal edit`（objective 换成可验证的可用性定义；`max_goal_rounds` 40 → **80**——已用 40，上限必须大于它，否则改完仍是 blocked ✗）+ `resume`（原 `phase: blocked / round-limit`、`disarmed` ⇒ 现 **active / armed**，revision 12 ✓）
- **交付 `tools/usability.ps1`（一条命令，全是用户真会敲的命令）**：`send`（纸面 `P-M1-300` ⇒ 3 张页图 + **800 KB 真实尺寸 `pack.pdf`**）→ `sim/channel.py --preset scan300 --seed 7 --modifier nocrop`（确定性"打印+扫描"替身）→ **`receive --photo`**（标记 → 透视 → 读）→ **SHA-256 逐字节比对** → 板材 `send --profile PL-D2 --nozzle 0.4 --format 3mf,stl` → **`verify --gate G8 --file` 校验这次真写出来的 4 个 `.3mf`** → `smoke-sender.mjs`（网页发送端真实数据路径、盲解码）+ `smoke-capture.mjs`（连拍取舍）⇒ **实测 exit 0、9 步全 PASS、289 s** ✓ 204800 B payload、收发 sha256 均 `32ec480521da27d2…` ✓ 每步**只看 exit code**、不 grep 关键字（"什么都没打印"不能算通过 ✗），失败时打印日志尾 ✓ 日志留 `.tmp/usability/step*.log` ✓
- **第一版脚本产生一个假失败，责任在我脚本、不在产品** ✗：板材那步拿纸面用的 200 KiB 去喂 `PL-D2@0.4` ⇒ CLI **正确拒绝**且报错可操作：`needs 1120 pages > 255 (inter-page RS limit): shrink payload or use a denser profile`（fail-closed ✓ 设计行为）⇒ 改为板材步骤用独立 **384 B** payload（⇒ 4 页 / 4 个 `.3mf`+`.stl` ✓）⇒ **顺带量出容量事实并写进 `docs/USE.md`**：一次传输 ≤ **255 页**（页间 RS 限制）· `P-M1-300` 每页净 **7514 B**（200 KiB = 3 页 ✓ 实测）· `PL-D2@0.4` 每页净 **≈180 B**（上限 ≈46 KB，由 CLI 自己的拒绝数字推得 ✓ 不是估的）⇒ 用户能自己算"一次能传多大"✓
- **交付 `docs/USE.md`（用户手册，本轮新写）**：电脑端**双击即用**的两个单文件页（`pskt-send-file.html` 发送 / `pskt-file.html` 接收）——**为什么必须单文件版**：瘦版 `send.html`/`index.html` 走 ES module，浏览器在 `file://` 下**拒载 module** ✗ · 手机端**一条命令起局域网服务** `python -m http.server 8000 --directory web/dist` + `index.html`（摄像头连拍 + `manifest.webmanifest`）· **如实写明 PWA 装不成**：安装需 https/localhost，局域网 http 会被拒 ⇒ 代码也照实处理（`pskt-file.html` 只在 `secure` 时注册 `sw.js`——**我在 dist 里读到那一行才敢这么写** ✓）· 自检入口 `?selftest=1` · 真打印/扫描/拍照注意（100% 缩放、别自动裁剪、彩色）· **需用户用真硬件验收的清单**（G4 500×8、G9 浏览器、D8 打印缩放量化、G10 喷嘴、G6 soak）
- **新的环境事实（已进 `AGENTS.md` 陷阱表 ✓）**：本 harness 的 pwsh 工具**实际跑在 Windows PowerShell 5.1**（不是 pwsh 7）⇒ ① `.ps1` 写 `#Requires -Version 7` 会**整脚本拒跑**（不是降级 ✗）② 5.1 嚼碎传给原生程序参数里的内嵌双引号 ⇒ `node -e` 收到坏脚本（**又踩了"内联脚本落地成文件"那条自家规矩** ✗）③ `$ErrorActionPreference='Stop'` 会让 node/python 的 stderr **当场打死脚本** ⇒ 用 `'Continue'` + 只按 exit code 判定 ④ 调脚本用 `& .\x.ps1`（点源时脚本里的 `exit` 连宿主一起退 ✗）
- **完成判据的进度（照新 objective 三条）**：① 一条命令端到端 usability 冒烟 ⇒ **已达成 ✓**（exit 0 实测）② 用户照 docs 在电脑与手机各走通一次 ⇒ **docs 已就位、待用户执行**；其中"局域网服务"这半我下一轮自己验（起 `http.server` 后用 node `fetch` 取页面 ✓ 不能只在文档里这么说）③ 门限表如实 ⇒ 本轮更新 ✓
- 证据：`& .\tools\usability.ps1` ⇒ **exit 0 / 9 PASS / 289 s** ✓ 单测提交前重跑 ✓ `core/` **未动** ⇒ 不需重建 `web/dist` ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**验掉手机路径的服务端半**（后台起 `python -m http.server --directory web/dist`，用 node `fetch` 取 `/index.html`、`/manifest.webmanifest`、`/sw.js`、`/capture.js`、`/pskt-bundle.js` ⇒ 证明瘦版经 http 真可用）→ 客户端缺陷（D12 advice scanner 对注释盲、D5 残余站点链接、D8 可自动化部分）→ G2 补到 200 seeds（分批 ≤600 s）


### 第 41 轮（预算 40/40 的**最后一轮** ⇒ 交接块：把每条剩余工作写成可直接执行的命令）

- **本轮做的事**（**无代码改动** ⇒ 提交前照例跑单测 ✓）：① 把 `AGENTS.md` 那条被我读窄的陷阱行**改宽**——沙箱禁的是 **node 进程内的管道 stdio**，**不是宿主 shell**（pwsh 跑 `python`/`git`/`node` 一直通 ✓ 附已验证命令与代价、并写明 `timeoutMs` 被封顶 600 s ⇒ 长任务分批）② 同文件修掉一处**过期状态**（D41 第 39 轮已闭，原文仍写 OPEN ✗ 台账不许自相矛盾 ✓）③ 本交接块
- **交接 1｜G2 补到 200 seeds（最高价值，且已验证可自动跑 ⇒ 不需要用户）**：源 `.tmp/g2src`（= `payload.bin` 204800 B、sha256 `91ab3f73…`、profile `P-M1-300`、palette `PAPER1`）· 生成 `python sim/channel.py --in .tmp/g2src --out .tmp/nc-scan300-N --seed N --preset scan300 --modifier nocrop`（N = 30…200 ⇒ **171 份**）· 实测 **15 s / 26.5 MB 每份** ⇒ **每次调用最多 ~35 份**（600 s 上限）⇒ 约 **5 批** · 判定 `node cli/pskit.mjs verify --gate G2 --root .tmp --match 'nc-scan300-*'` · 实测 **4.6 s/份** ⇒ 200 份 ≈ 920 s ⇒ **必须分 2 次判** · 现状：**29/29 byte-exact / 134.6 s / exit 0**（第 40 轮 ✓）样本量如实记 **29/200**、**判据 100% 未放宽** ✓
- **交接 2｜scan600 侧（含两类**真失败** ⇒ ACCEPTANCE #4）**：源目录 `.tmp/sw-scan600-src` **在盘上** ✓ 同法加 `--preset scan600`；已有 16 份（`nc-scan600-1…16`）· 实测约 **30 s/份** ⇒ 生成与判定**每次 ≤18 份** · 先诊断 `nc-scan600-3`（`short 0/1`）与 `nc-scan600-10`（`no-page-header`）——**别当噪声**：要么是语料/几何假设错、要么是解码路径真有洞 ✗
- **交接 3｜两处文档待改（我坚持"没读原文就不硬改"⇒ 已分别记在 D42 与 D41 行内 ✗）**：① `docs/ACCEPTANCE.md` 的 G2 段仍写"需带外手跑"⇒ 换成交接 1 的命令 ② `docs/DEFECTS.md` D41 行里"现在的保护只有篡改用例 + 正例"那句已被第 39 轮的对拍测试取代 ⇒ 读原文再改 ✓
- **交接 4｜只能由用户做的**：**G4 手机实机 500×8（从未跑过 ✗）**——手机开 `web/dist/pskt-scan.html`（或部署后的站点）连拍 · **G10 喷嘴矩阵**需真打印机 · **GitHub 推送**需仓库 URL + 交互凭据（`gh` 未装 ✗ 至今**零推送**）· `.github/workflows/pages.yml` 未写
- **状态如实（预算用尽这一刻）**：G0 ✅ 277/277 · G1 ✅ · **G2 🟡 29/200**（scan300 全绿；600 dpi 侧未判且有 2 类已知失败）· G3 ✅ · **G4 ✗ 未跑** · G5 ✅（误接受 0 ✓ 本轮未重跑）· **G6 ✗ soak 未跑** · G7 ✅ · **G8 🟡**（进程内 XSD 子集校验 ✅ 已并入 `--gate all`、对我们所有产物 PASS；表 ⇄ 权威 XSD 对拍 ✅；仍**不等于过 schema**——本机无 XSD 引擎 ✗）· **G9 ✗ 需真浏览器** · **G10 ✗ 需打印机** · 客户端（手机 PWA 连拍 + 电脑拖拽整盘落盘）**可用** ✓ 2D 纸面（套准/裁切标记、按实际尺寸打印）与 3D（3MF/STL 实体码牌）**可产出** ✓ · 遗留 OPEN：**D8 D12 D18 D23 D27 D28 D29 D42**（D40 已撤回 ✓ D41 已闭 ✓）
- **目标未完成 ⇒ 不标 complete** ✓ 自动轮次到此为预算上限；继续需用户说"继续"（或另开目标）⇒ 下一位从**交接 1** 起手最省事


### 第 40 轮（**推翻我自己的一条"不可能"**：G2 的 200 seeds 一直可以在会话内跑 ✗ 记 D42）

- **发现**：过去数轮把"node 里不能 spawn"推广成"语料只能带外由用户手跑"⇒ G2 长期按 **32/200** 记账 ✗ 实测 **pwsh 工具跑 python 一直是通的**（`AGENTS.md` 的"环境事实"里 Python/cv2 本来就是这么探出来的 ✓）⇒ `python sim/channel.py --in .tmp/g2src --out .tmp/nc-scan300-17 --seed 17 --preset scan300 --modifier nocrop` ⇒ **生成 15 s / 26.5 MB**、门限判 **4.5 s**、`204800 bytes, digest verified` **字节相同 PASS** ✓（第 33 份真证据）⇒ 随后批量造 seeds 18–29（**12 份 / 174 s**）⇒ `nc-scan*` 目录 **32 → 45** ✓
- **代价由此可算（不再是估计）**：scan300 侧 200 seeds ≈ **50 min / 5.3 GB**；600 dpi 侧像素约 4× ⇒ 两侧合计**数小时 / ~25 GB**（D 盘 2 TB ✓）⇒ **G2 的样本量缺口是时间问题、不是能力问题** ✓
- **新测得的操作事实**：判**全 45 份**会**超工具 600 s 上限被杀**（`exit 1` = 终止、不是失败 ✓）——600 dpi 那半占大头（16 份 × ~30 s）⇒ **按 dpi 分开判**：`--match 'nc-scan300-*'` ⇒ **29/29 byte-exact / 134.6 s**（4.6 s/份）✓ 且 `timeoutMs` 传 1740000 **无效，执行器封顶 600 s** ✗
- **记 D42（OPEN）**：根因是把沙箱限制的作用域记错（禁的是 **node 的管道 stdio** ✓ 不是宿主 shell）⇒ D29 同族第 4 次（未核实的断言 ✗）；剩余动作写在 D42 行里（改 ACCEPTANCE 的 G2 段、两侧补到 200 seeds、清 D41 那句旧口径）
- 证据：`verify --gate G2 --root .tmp --match 'nc-scan300-*'` ⇒ **exit 0、29/29**（判据是 100% ✓ PLAN 要 200 固定 seed、本次 29 ⇒ **仍如实标"未达样本量"，不改判据** ✓）· 600 dpi 那 16 份本轮**未判**（含已知 `nc-scan600-3` short、`nc-scan600-10` no-page-header 两类失败 ⇒ ACCEPTANCE #4 仍 OPEN ✗）· 本轮**无代码改动**（只多了 `.tmp` 语料与文档）⇒ 提交前照例跑一次单测 ✓
- 下一轮（预算 39/40 ⇒ 若还有轮次）：scan300 补到 200 seeds（~50 min，**分批 ≤600 s/次**）→ scan600 从 `sw-scan600-src` 同法补齐并诊断那两类失败 → 改 ACCEPTANCE 的 G2 段 → 清 D41 旧口径 → G4 手机实机 500×8（**仍未跑过** ✗）


### 第 39 轮（D41 闭合 ✓ 闭合它的方式是**先把 D40 那个错原样重演一遍**）

- **交付**：`tests/unit/threeMF-xsd-parity.test.mjs`（5 条）+ `tools/check-3mf.mjs` 导出 `XSD_TABLE`（一处导出、不动声明）⇒ **表 ⇄ 权威 XSD 自动对拍**：用校验器**自己的 `scanXml`** 读 `ref/3mf-core-1.4.0.xsd`，抽出 16 个全局元素名、每个 `CT_*` 的属性名集合与 `use="required"` 集合、`xs:element ref=` 的**文档序**（= schema 的 sequence/choice 顺序）、`ST_ObjectType`(5)/`ST_Unit`(6) 枚举，与 `ELEMENTS`/`REQUIRED`/`OBJECT_TYPE`/`UNIT_ENUM` **双向逐项比对**（元素表恰好相等 ✓ 属性集恰好相等 ✓ required 集相等 ✓ 子元素顺序相等、且"schema 没有子元素的，表里也不许声明" ✓ `xml:lang` 单独按 `xs:attribute ref=` 核对 ✓）
- **反空转**：先断言"从 XSD 数出来的是 **16 个元素**、`CT_Object` 恰 **7** 个属性且**含 `name`**、`ST_ObjectType` 恰 **5** 值、`CT_Model` 确有 `xml:lang` 引用"⇒ 抽取若空转，这些数字对不上 ✓
- **阳性对照（本轮最该看的一行）**：5 条**第一次跑就全绿** ⇒ 按仓库规矩这不值得相信 ⇒ 于是**把 D40 那个错原样重演**（从表里删掉 `name`）⇒ **两处同时变红**：对拍测试 `<object>: the table's attribute set differs from CT_Object`（diff 里就是 `- 'name'`）✓ 与 D40 回归守卫 `object/@name and @partnumber are Core 1.4 attributes`（并打印出那条假阳性原文）✓ ⇒ **这道判据确实能挡住上一轮那个错**、不是装饰 ✓ 之后放回 ⇒ 复绿 ✓
- 为什么它比篡改用例更根本：篡改用例证明**表里已有的规则会响**，证明不了**表抄对了**——上一轮翻车正是表抄错（凭记忆 + 网上变体）✗
- **一处没做完的收尾（如实记 ✗）**：D41 行里那句"现在的保护只有'篡改用例 + 正例'…防不住表本身抄错"已被本轮落地取代，但我**没改到原句**（`old_string` 锚点没匹配上 ⇒ 我对原文的记忆不准 ⇒ 不硬凑）⇒ 该行现在"状态=CLOSED ✓ 但句中还留着旧口径"✗ 下一轮顺手清理（读原文再改 ✓）
- 证据：单测 **277/277** ✓ `verify --gate all --seeds 2` ⇒ exit 0、`G0 unit suite: 277 passed, 0 failed`、`PASS G8 subset`、`ALL GATES PASS -- 6/7 evaluated, 1 skipped`（G2 无语料 ⇒ 点名跳过 ✓）· 阳性对照那一跑实测 **exit 1（30 条中 2 fail）** ✓ `core/` 未动 ⇒ 不需重建 `web/dist` ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**200 seeds 的 G2 语料**（需手跑 `python sim/channel.py --in SRC --out DST --seed N --preset scan300 --modifier nocrop`，沙箱禁 spawn ✗）→ **G4 手机实机压力 500×8（从未跑过 ✗）** → G6 soak → G10 喷嘴矩阵 → `.github/workflows/pages.yml` → 清掉上面那条 D41 行的旧口径


### 第 38 轮（3MF 的 XSD 子集校验器 + `--gate G8` ✓ 而本轮最大的产出是**我自己撤回的一条假发现**）

- **交付**：`tools/check-3mf.mjs`（3MF Core 1.4 **XSD 子集**校验器：包部件与内容类型覆盖、部件名合法性、关系与唯一起始部件、**自解析命名空间**、元素序列与 `mesh|components` choice、属性值域、文档级 id 唯一、`pid` 悬空、三角形索引范围与退化、`ST_ResourceID/Index` 的 2³¹ 上界、`CT_Vertices` ≥3 / `CT_Triangles` ≥1、DTD 与实体声明）+ `verify --gate G8`（进程内造 `PL-D2@0.4` 板材 3MF，或 `--file a.3mf,b.3mf` 查历史产物）+ `tests/unit/threeMF-subset.test.mjs` **25 条**
- **为什么必须独立于 `parseModelXml`**：它自己写着"只认自己写出来的那种形状：属性顺序固定"⇒ 建在它之上的校验器**继承同一份盲目**（属性换序/多一个属性/命名空间用前缀 ⇒ 它只是匹配不到）⇒ 本校验器自己扫 XML、解析 `xmlns` 绑定 ⇒ 正例"前缀绑定必须通过"就是为这条而写 ✓ 两条算路互不覆盖：`selfCheck3MF` 问"写出的是不是我想写的"、本校验器问"合规读取器会不会收下"✓
- **本轮最该记住的错（D41 OPEN · D40 撤回）**：我先按记忆写属性表、再拿**网上取来的 schema 变体**当"核实"⇒ 它没有 `object/@name` ⇒ 校验器判我们**每一份** 3MF 违规、`--gate G8` FAIL ⇒ **若我信了，下一轮就会去"修"一个不存在的缺陷**：删发射器的 `name`、改 `parseModelXml` 的 object 正则（它把 `name` 当必需且在固定位置 ⇒ 删了一个对象都读不出）、动 mesh 测试、并因 `core/` 改动重建 `web/dist` ⇒ 一次纯粹的自我破坏 ✗ 而**权威 `ref/3mf-core-1.4.0.xsd` 一直在盘上、本页 L38 还点名过它** ⇒ 根因是**没先查工作区**（D29 同族第 3 次 ✓）⇒ 按权威改表后**全绿**；D40 行保留为历史并加撤回标记 ✓（照 D36 的先例 ✓）
- **从权威 XSD 抄实的事实（都进了表与测试）**：`CT_Object` L84-101 = `id/type/thumbnail/partnumber/name/pid/pindex` + `xs:anyAttribute namespace="##other"` ⇒ **带前缀的扩展属性合法、无限定的未知属性仍违规** ✓ `ST_ObjectType` L211-217 有 **5** 个枚举（变体只有 2 个）✓ `ST_ResourceID/Index` L201-208 = positive/nonNegativeInteger 且 `maxExclusive 2^31` ✓ `CT_Vertices` minOccurs=3、`CT_Triangles` minOccurs=1 ✓ **core 元素只有 16 个**（`colorgroup`/`color`/`texture2d` 属**扩展** schema ⇒ 我凭记忆多加的两个已删 ✗）✓ 另**故意放宽一处并写明**：`metadata/@name` 是 `xs:QName`，但严格校验要求前缀已声明，而 3MF 惯例（`pskt:`、`slic3r:`、`Application`）都不声明 ⇒ 只查词法形状，否则会把所有真实文件判违规 ✓
- **G8 已并入 `--gate all`**（绿了才进例行 ✓ 红的那几分钟里有意不并入，理由是并入会让例行基线永远红、掩盖其余门限回归）✓ `cmdVerify` 每次打印"本次未评估哪些门限"⇒ `ALL GATES PASS` 的覆盖面不可再被夸大 ✓
- **我另外三个实现错，全由"跑一遍"暴露**（新检查第一次跑不可信 ✓ 又一次）：① 校验器首跑 **SyntaxError**（嵌套模板字符串 + 一个多余引号）② close 节点漏写 `local` ⇒ 每个 object 都像没闭合 ⇒ 统计全 `0 objects`（**假绿的一种：数字为零却报 OK**）③ `replace_all` 把 `resourceId` 的**定义体**一起吃掉 ⇒ TDZ ✗ 另踩一个 PowerShell 陷阱：双引号 `node -e "..."` 里的 `$1m:` 被 PS 当变量插值 ⇒ 已写进 `AGENTS.md` 陷阱表 ✓
- 证据：单测 **272/272** ✓ `verify --gate all --seeds 2` ⇒ **exit 0**、`G0 unit suite: 272 passed, 0 failed`、`ALL GATES PASS -- 6/7 evaluated, 1 skipped`（G2 无语料 ⇒ 点名跳过 ✓）、`PASS G8 subset`（3 objects / 377964 triangles / 189802 vertices ✓）· `--gate G8 --file .tmp/m5det1/page-000.3mf,.tmp/m5verify/page-000.3mf` ⇒ 历史产物同样 OK ✓ `core/` **未动** ⇒ 不需要重建 `web/dist` ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**D41 的修法**——加一条"表 ⇄ `ref/3mf-core-1.4.0.xsd` 对拍"的测试（用本校验器自己的 `scanXml` 读 XSD、抽出 `CT_*` 属性集与 16 个元素名逐项比对 ⇒ 表再抄错就红 ✓）→ 200 seeds 的 G2 语料（需手跑 python）→ G4 手机实机压力 500×8（**从未跑过** ✗）→ G6 soak → G10 喷嘴矩阵 → `.github/workflows/pages.yml`


### 第 37 轮（G2 从"手工命令"变成**门限** ✓ 而它交出的第一个数字是 FAIL）

- **交付**：`verify --gate G2 --corpus DIR`（也支持 `--root DIR --match GLOB`）接进 `cmdVerify` ✓ 实现方式是 **`import` 而不是复制** `tools/g2-corpus.mjs`（导出 `parse/collect/runCorpus/buildMod/corpusProvenance` + `isMain` 守卫 ⇒ 脚本与库同一份文件 ✓ 工具独立可用已复验 ✓）⇒ **门限与工具不可能漂移** ✓ 判据仍是它自己在进程内重算的 `sha256(result) === manifest.sourceSha256`、**100% 才算过** ✓ 语料由 `sim/channel.py` 带外生成（沙箱禁 spawn ✓ 而这分工本身是对的：会自造信道的门限有可能开始跟自己串供 ✓）
- **真跑（32 份 `nc-scan*` 平板模型语料 ✓ 盘上还在）**：`FAIL G2 corpus: 30/32 byte-exact in 392.3s` ⇒ **exit 1** ✓ 失败两类：`nc-scan600-10 no-page-header`（页内 `intra-fail`）· `nc-scan600-3 short 0/1` ⇒ 都是已知的 600 dpi A/B 类 ✓ **与历史记录的 `30/32 = 93.8%` 吻合 ⇒ 门限复现了既有证据、且判决未上调** ✓（把正确拒绝剔出分母等于重定义判据 ✗ 不做 ✓）
- **样本量差距保持可见**：门限每次打印 `PLAN asks for 200 fixed seeds, this run had N` ⇒ `G2 PASS` 不会被读成"达到了 200 seed" ✓ 现在 **32/200** ✗
- **D38（已闭）· 堵住 G2 的假绿通道**：实测 `.tmp/g2src`（干净渲染）与 `.tmp/g2scan`（过信道）**文件清单完全相同**、页旁也没有信道报告 ⇒ 把门限指向干净语料就会打印 `100% byte-exact` 而什么都没测 ✗ 新增 `corpusProvenance()`：**量灰阶数**（渲染器只发 10 阶 = 调色板+抗锯齿 ✓ 信道把值域铺满 249–256 阶 ✓）⇒ **盘上 119 个语料目录实测分离：干净 10–18 阶 · 过信道 192–256 阶 ⇒ 地板 64 两边各留 10 倍余量（不是调出来的旋钮 ✓）** ⇒ 阳性对照 `--corpus .tmp/g2src` ⇒ `FAIL G2: looks pristine (10 grey levels ...)` **exit 1** ✓
- **D39（已闭）· "跳过"不能算绿**：`cmdVerify` 原本是 `allOk &&= ok` ⇒ **任何真值都算通过**，而"我没法评估"只能返回对象（真值 ✗）⇒ 现在显式识别 skip 并在汇总行**点名**：`ALL GATES PASS -- 0/1 evaluated, 1 skipped` + 一行写清"要怎么才能评估它"（含生成语料的 python 命令 ✓）⇒ `ALL GATES PASS` 前缀仍可 grep ✓ 但**不能再被引用成覆盖了比实际更多的门限** ✓
- **我第九次吃掉文档标题** ✗（上一轮刚立的规矩又违反）⇒ 但**上一轮新加的台账判据当场抓到了它**（那张表变成孤儿表 ⇒ 判据红 ⇒ 我修）✓ 这件事的两面都记：**判据有效 ✓ 而我的编辑习惯仍不可靠 ✗**
- 证据：单测 **247/247** ✓ `verify --gate all --seeds 2` ⇒ exit 0（G0 `247 passed, 0 failed` ✓ G2 被点名跳过 ✓）· `verify --gate G2 --root .tmp --match 'nc-scan*'` ⇒ **exit 1（如实）** ✓ 四种行为逐一验过（无语料 SKIP ✓ 干净语料 FAIL ✓ 真语料真跑 ✓ 工具独立可用 ✓）✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**200 seeds 的 G2 语料**（需手跑 `python sim/channel.py ... --seed 1..200`，命令已写进 usage 与 skip 提示 ✓）→ 3MF XSD 子集校验器（G8 的另一半 ✓）→ G4 手机实机压力 500×8（**从未跑过** ✗）→ G6 soak → G10 喷嘴矩阵 → `.github/workflows/pages.yml`


### 第 36 轮（D36 与 D37 双闭 ✓ 而 D36 的否证记录**本身是错的**、已修正）

- **交付**：`no-hollow-corner` 的语义过载真正拆开了 ✓ 新增 reason **`fourth-corner-out-of-frame`**（rank 4 ✓ `advice.js` 已配"退后并把四角都框进来；这是取景问题、不是对焦或清洁问题"✓）
- **sound 的做法**（与第 33 轮被否证的做法差在哪）：三个同尺寸方块若构成**页尺度直角** ⇒ 它们确定的矩形有一个第四角 ⇒ **该点是否在图像内是一次边界检查**——没有容差、没有"附近有没有 blob"这种原则上无解的问题 ✓ 越界 ⇒ 取景问题 ✓ 在界内 ⇒ 仍是"角标读不出"（清污/翻面/重印 ✓ 语义正确）✓
- **一条决定夹具设计的几何事实**：轴对齐时第四角必落在前三点的包围盒内 ⇒ 该分支**只在旋转/透视下触发**——而那正是手持手机的真实情形 ✓ **所以第 33 轮那个"在全尺寸图里把角涂白"的夹具根本不是在模拟"没拍全"** ✗ 它模拟的是"角在画面内但读不出"⇒ 我上轮是在用错的夹具否证一个错的做法 ✓ 已写进新测试的注释 ✓
- **顺带挖出一条独立的潜在缺陷 D37（已闭）**：`progressScore` 的平手裁决 `rank*1000 + candidates` 用在"同一幅图的多个尺寸簇"上时**候选数多者胜** ⇒ **152 成员的数据格点簇压过 4 成员的角标簇** ⇒ 用户看到的 `maxBlobSide=5`（一个格点）而角标其实是 30px ✗ 更严重的是：**任何在那个簇上算的判别器都是在格点上算** ⇒ 第 24/26 轮"分辨率地板"两次分析依据的数字可能一直是格点产物 ✗ 修法：新增 `reasonRank()` + `betterFailure()`（rank 优先 ⇒ 更深入的失败永远赢 ✓ 平手取**最大 anchor**、再取**最少成员**）✓ **不动 `progressScore` 本身**（它在阈值梯子里"候选更多=更进步"是对的 ✓）⇒ 实测同一夹具：修前 `maxBlobSide=5`、修后 **`blob=30px, cluster=30px`** ✓
- 簇门槛 `>=4` 放宽为 `>=3`：真·少拍一角时只剩三个角标 ⇒ 原本连诊断都进不去、只会说"没有四个同尺寸的"✗ **buildQuad 在 3 成员时永不返回 ok ⇒ 只扩大诊断、不扩大接受面** ✓（由 G5 与 244/244 复核 ✓）
- **我自己的两处错、都已修正并记账**：① 第 33 轮 D36 写的机制（"top-14 按面积把真角标挤出去"）是**编的** ⇒ 实测 30px 簇只有 4 个成员、面积 `900/900/900/841`、**我画的方块就在 top-14 里** ✗ 我引用的 `cand=159` 是 detectIn 覆写的 `squares.length`、不是枚举池大小 ⇒ **又一次引用未核实的证据（D29 同族）** ✓ ② 上一轮刚立下"`new_string` 必须以 `old_string` 原文结尾"的规矩，本轮**立刻又吃掉一次标题（第 8 次）** ✗ ⇒ 不再靠自觉：改为**机器判据**（见下）
- 证据：`tests/unit/fiducial-corner-damage.test.mjs` **5/5**（含"隐含角 (1400,920) 越界 ⇒ 必须报 out-of-frame"✓"隐含角 (1000,1000) 在界内 ⇒ 不许甩锅取景"✓"四点全实心仍走原路径"✓"三点共线 ⇒ `no-rectangular-quad`（分支不能被垃圾触发）"✓）· 单测 **244/244** · `core/` 动过 ⇒ **`verify --gate all` 重跑** · `probe-marker-scale` 重跑（尺度梯子最吃簇选择逻辑 ✓）· build + `check-dist` 重跑（**dist 内联了 core ⇒ 改 core 必须重建 ✓**）· **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：`--gate G2 --corpus` 接进 `cmdVerify` 并推到 200 seeds → 3MF XSD 子集校验器 → G4 手机实机压力（500×8 ✓ 从未跑过 ✗）→ G6 soak → G10 喷嘴矩阵 → `.github/workflows/pages.yml`（推送仍需你的仓库 URL 与交互凭据 ✓）


### 第 35 轮（D15 闭 ✓ 而它不是一"未证实"、是一**活着的谎**）

- 台账原本把 D15 记成"`header.kind` 的取值域没被证实"✓ 一探就发现比那严重：`web/sender.js` 写的是 `p.header.kind === 'parity'` ⇒ **拿字符串比 u8 字段 ⇒ 恒 false** ⇒ 发送端显示的"校验页 N 张"**一直是 0** ✗✗ 而 0 张校验页看起来跟"这次传输真的没有校验页"一模一样 ⇒ 没有任何别的地方会坏到让我注意到 ✓ 这正是本契约最在意的形态：**一个像样的数字，但它不是真的** ✓
- 实测取值域：`[[0,1],[1,2]]` ⇒ **kind ∈ {0,1}** ✓ 与 `core/frame.js:47` 的 `PAGE_KIND = { DATA: 0, PARITY: 1 }` 和 L20 文档（`kind (u8) 0 = data page, 1 = parity page`）一致 ✓
- 修法不是把 `'parity'` 换成 `1`（那只是把同一个数记在两个地方 ✗）⇒ **引入 `PAGE_KIND` 常量**、比较常量 ✓ 单一真源 ✓
- 防复发：新判据 `tests/unit/frame-kind-domain.test.mjs`（3 条 ✓）——① 真编一次 4 KiB + `parityPct 50`、逐页 `decodeHeader` ⇒ **两种 kind 都必须出现且都是 number** ✓ ② 扫源码找"header kind 跟字符串比"这个**形态** ✓ ③ **阳性对照**：正则必须命中原 bug 的字面写法 ✓ 必须**不**命中常量写法 ✓ 也必须**不**命中 `web/capture.js` 里那个**本来就是字符串**的自有 `r.kind`（扫到会误报 ⇒ 而一个会对正确代码报警的检查会被无视 ⇒ 那比没有检查更糟 ✓）
- **我自己这条守卫当场就有个缺陷 ✓ 已修**：它把 `web/dist` 也扫了 ⇒ 单独跑时命中**修复前的旧构建产物** ✗ 而全套跑时却绿（因为构建恰好在它之前跑过）⇒ **判据的结论取决于有没有重新构建** = 第 32 轮 D28 同一类脆弱性 ✓ 改为跳过生成目录（**规则本来就是对源码说的 ✓ 不是放宽判据 ✓**）
- 又一次踩到 AGENTS.md 早写着的规矩：我用 PowerShell 视图里**被补过缩进**的行去拼 `old_string`（6 空格 vs 真文件 4 空格）⇒ edit 报"未找到" ✓ 改用 `grep` 看真文件后一次成功 ✓ **"拼 old_string 之前先读真文件"这条我仍在反复付学费 ✗**
- 证据（最终文件 ✓ 跑期间零改动 ✓）：kind 判据 3/3 ✓ 单测 **240/240** ✓ `build-web` exit 0 ✓ `check-dist` **12/12**（含第 34 轮新加的"manifest 图标必须方"✓）✓ `smoke-sender` 全过（7/7 ✓）✓ 本轮**只动 `web/`、`tests/`、`docs/`**（`core/` 未变 ⇒ 第 33 轮 `ALL GATES PASS` 继续成立 ✓ 由 diffstat 复核 ✓）✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位不变：**D36 的 sound 路线**（把第四角预测锚在剖面自己的几何上 ✓ 需要 `detectIn` 把角标簇传进失败路径）✓ 其后：`--gate G2 --corpus` 接进 `cmdVerify` 并推到 200 seeds ✓ 3MF XSD 子集校验器 ✓ **本 agent 上下文已耗尽 ⇒ 强烈建议开新会话接手**（`AGENTS.md` + `docs/STATUS.md` + `docs/DEFECTS.md` + `docs/ACCEPTANCE.md` 即完整交接 ✓）


### 第 34 轮（D13 闭 ✓ PWA 图标改为正方 ✓ 三次由执行告知的真约束）

- 手机端可安装性的一条实账：图标原本是 **2260×3290**（A4 形状 ✓ 手机桌面会把它裁成一条 ✓）✓ 现在 **3290×3290 正方 ✓ manifest 声明与文件相符 ✓**
- 取法：**把真页原样嵌进方形画布正中** ✓ 不裁、不缩、不改一个像素 ⇒ 原来那条性质保留（"图标必须是我们自己的编码器画得出来的东西 ✓ 不许贴一张假图"✓）✗ 若改成裁切就会把角标切掉 ✗ 边距用纸白（实测页四角本来就是 `255,255,255,255` ✓ 所以不会有灰框 ✓）
- **三次由执行告知、我原本一定会猜错的约束**（都写进代码注释了 ✓）：① `encodePNG` **拒绝 `Uint8ClampedArray`** ✓ 要 `Uint8Array` ✓ ② 它还要求 **`dpi` 字段**（写 pHYs 物理尺寸块 ✓）③ 构建是"写 `web/dist.tmp` ✓ 成功才换目录"⇒ 前两次失败时**旧图标原封不动** ✓ 坏产物从没进过站点 ✓（这条原子性设计本轮自证有价值 ✓）
- 判据配对（吸取第 33 轮"一行的事不推下轮"的教训 ✓）：构建里 `IHDR` 读回来**非方即 throw** ✓ 而 `iconSizes` 就取自那个读数 ⇒ **manifest 不可能对文件说谎** ✓ 另在 `check-dist` 补一条反向断言"manifest 声明的图标尺寸必须是方的" ⇒ 将来有人换成 A4 形状并同步改 manifest 也会红 ✓ 现在 **12 条断言全绿** ✓
- 证据：`build-web` exit 0 ✓ 图标 `3290x3290` ✓ `check-dist` **12/12** ✓ 本轮**只动 `tools/` 与 `docs/`**（`core/` 未变 ⇒ 第 33 轮 `ALL GATES PASS` 继续成立 ✓ 由 `git show --stat` 复核 ✓）✓ 台账判据随单测重跑 ✓ **不打新 tag**（M4 未闭合 ✓）
- **诚实说明**：本轮上下文已近耗尽 ✗ 所以挑的是小而可交付的一条 ✓ `docs/DEFECTS.md` 里 D13 已划 CLOSED ✓ 其复验细节以本块为准 ✓ 下一轮首位不变：**D36 的 sound 路线**（把第四角预测锚在剖面自己的几何上 ✓ 需要 `detectIn` 把角标簇传进失败路径）✓ 建议**开新会话**接手（`AGENTS.md` + `docs/STATUS.md` + `docs/DEFECTS.md` 即为完整交接 ✓）


### 第 33 轮（一次**被测量否证的设计** ✓ 但它把一条散文级的账变成了可复现的一行输出）

- 目标本是拆 `no-hollow-corner`（ACCEPTANCE 开放缺陷 #3 ✓ 它误导过两次根因分析 ✗）✓ 做法：同尺寸三方块拼页尺度直角 L ⇒ 推出第四角应有位置 ⇒ 看那里有没有 blob ✓ **实现后跑夹具 ⇒ 第二个夹具红** ✓ 而红得有价值：
  - 我画的 29×29 实心方块**确实在候选集里**（实测 `w=29 area=841 at 2236,3266` ✓ 就在 br ✓）✓ 但 `buildQuad` 的枚举池是 `ranked` = **按面积前 14** ✓ 而 159 个方形候选里最大的一批是**合并的数据格点** ⇒ 真角标和那个方块都挤不进池子 ✓ 于是"页尺度直角"是**用打印噪声拼出来的假 L** ✓ 它推出的 `q` 自然不在缺失角上 ⇒ 把"角标在、孔不可读"误报成"没拍全" ✗✗
  - 换用全集候选只会反向失衡（任意一点附近更容易有东西 ⇒ 遮角情形被吞进另一分支）⇒ **两个夹具互相拉扯 ⇒ 不是参数没调好 ✓ 是路子不对 ✓**
- **决定：不交付一个会说谎的 reason** ⇒ 全部撤回 ✓ 撤的时候连注释一起撤干净（**留着描述"代码不做的行为"的注释比没注释更坏 ✓ 注释说谎比代码 bug 更难发现 ✓**）✓ rank 表里那条不存在的 reason 一并删掉 ✓
- 净收益：新文件 `tests/unit/fiducial-corner-damage.test.mjs` 把 #3 从散文变成**一行可复现输出** ✓ `第四角被遮=no-hollow-corner(hollow=0,cand=158) · 角标在而孔不可读=no-hollow-corner(hollow=0,cand=159)` ⇒ **两种含义相反的失败共用同一个字符串 ✓ 铁证 ✓** 而**两个夹具都被拒收 ⇒ 无误接受 ✓**（那才是本契约唯一不可原谅的失败 ✓）✓ 拆法记为 **D36** ✓ 并写明 sound 路线：把"第四角应在位置"锚在**剖面自己的几何**上（layout 声明的页尺寸预测角位 ✓ 不是任意三元组 ✓）⇒ 需要 `detectIn` 把角标簇传进失败路径 ✓ 归 G4 ✓
- **修自己记错的账 ✓** D30（重复导航 ✓ 当场撤）、D32（PowerShell `>` 写 UTF-16LE ✓ 已进 AGENTS.md 陷阱表）、D33（单文件页 CSP ✓ 当场改 + 加判据）、D34（`bundle()` 入口写死 ✓ 当场参数化）——四条我**当场就修完却记成 OPEN** ✗ 全部改判 CLOSED ✓ 台账的价值是准 ✓ 不是只往里加 ✗
- **本轮又抓到自己的两个老毛病**：① 新测试里让第 1 个测试读第 2 个测试才赋值的变量 ⇒ **必红** ✗（就是吃掉我三轮的 D23 那一类 ✓ 已改成"一个测试、三处断言、无共享可变状态" ✓）② **第七次**吃掉文档标题 ⇒ 立习惯：**`new_string` 必须以 `old_string` 原文结尾** ✓（本轮起执行 ✓）
- 证据（`core/` 行为**未变**（撤回后与第 32 轮等价 ✓）但文件动过 ⇒ 全套照重跑 ✓）：corner-damage 1/1 ✓ 单测全绿 ✓ `verify --gate all --seeds 2` ⇒ **`ALL GATES PASS`** ✓ 台账判据在文档改完后**重跑**绿 ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：按 D36 的 sound 路线拆 reason（锚在剖面几何 ✓ 失败路径要拿到角标簇而不是 top-N ✓）→ `--gate G2 --corpus` 接进 `cmdVerify` 并推到 200 seeds → 3MF XSD 子集校验器


### 第 32 轮（D5 闭 ✓ `file://` 现在也能"发"了 ✓ 而真正的拦路石是**三行死代码**）

- 新产物 **`web/dist/pskt-send-file.html`（306.7 KiB ✓）**：从 `web/send.html` 同一套流水线派生（内联 CSS ✓ 内联 sender bundle ✓ 剥掉 `<nav class="tabs">` 回链 ✓）✓ dist 共 **50 文件 / precache 48 条** ✓ ⇒ 气隙场景里"发"这一半第一次可以在没有站点、没有网络的机器上用 ✓ 接收端单文件 266.6 KiB 不变 ✓
- **阻塞点完全不在页面，在 `core/`：三行 `export default`**（`render/pdf.js`、`png.js`、`tiff.js` ✓ 各自文件最后一行的"顺手便利"✓ 全仓库无人 import ✓）⇒ 而 web 打包器**按设计拒绝 default export** ⇒ `pack.pdf` 是发送端的真功能（`send.html:12` ✓）⇒ **D5 是被死代码挡住的** ✓ 删掉 + 换注释说明约定 ✓
- **两条"有约定无判据"的规则从此有判据**：新 `tests/unit/export-conventions.test.mjs`（3 条 ✓）① `core/` 不得有 default 导出 ② `core/` 不得 import `node:` 内建（AGENTS.md 早就写着 ✓ 此前**从来没被机器验过** ✗）③ 还带一条"目录确实走到了"的**防空跑守卫** ✓ 并对 ① 做了真负向证明：用 HEAD 版旧内容喂同一正则 ⇒ **命中 true** ✓ 工作版 false ✓ 具名导出仍在 ✓（**首跑就绿的判据我不认 ✓**）
- 顺带发现并修掉两件（详见 DEFECTS **D33 / D34**）：① 单文件页一直带着 `script-src 'self'` 却**内联了 `<script>`** ⇒ 是否可跑取决于"浏览器在 `file://` 下是否强制执行 `<meta>` CSP"这个**本机验不了**的问题 ⇒ 改成**不依赖那个答案**（两页都加 `'unsafe-inline'` ✓ + 构建断言"本页 CSP 不许内联就拒绝出产物" ✓）✗ 我没说"它一直是坏的" ✓ 那会是需要浏览器才能下的结论 ✓ ② `bundle()` 的自动运行入口写死成接收端 id `"files"` ⇒ 已参数化 ✓
- 新加的产物级判据：**"单文件页里的内联脚本必须能被解析"** ✓（`new Function` 只解析不执行 ⇒ 语法坏 / bundle 被截断都会在这里死 ✓ 而"能不能解析"是本机验得了的那一半 ✓）⇒ **G9 产物级判据 10 → 11 条** ✓
- **我自己的两次探针错误（D32 ✓ 都记了账）**：① 用 PowerShell `>` 落 `git show` 输出 ⇒ **写成 UTF-16LE（首字节 255 ✓）** ⇒ UTF-8 读回全是乱码 ⇒ `indexOf` 返回 -1 ⇒ 差点得出"我的判据是空跑的"这个**反方向**的错结论 ✗ 靠**阳性对照**才分清"文件坏了"与"正则坏了" ✓ ② 上一条里我先前用 `Select-String -InputObject` 测多行文本 ⇒ `^` 锚在整串开头 ⇒ 也报 false ✗（同族：拿 shell 的语义去推断 JS 正则的行为 ✓）⇒ 本机陷阱表补一行：**跨工具取文本用 `node -e` 直读真文件 ✓ 不要 `>` 落地再读 ✓**
- 证据（最终文件 ✓ 跑期间未改任何文件 ✓ D28 教训已生效）：`build-web` exit 0 ✓ `check-dist` **11/11** ✓ 单测 **236/236** ✓（233 + 本轮 3 条 ✓）`smoke-sender` exit 0 ✓ `smoke-capture` exit 0 ✓ **`node cli/pskit.mjs verify --gate all --seeds 2` ⇒ `ALL GATES PASS`** ✓（本轮动了 `core/` ⇒ 全套重跑 ✓ 不是引用上轮 ✓）✓ 台账判据在文档改完后**重跑**（绿 ✓）**不打新 tag**（M4 未闭合 ✓）
- **用户指示：完成本轮后暂停目标自动续跑** ⇒ 已执行（`update_goal pause` ✓）⇒ 后续需要我继续时请说一声 ✓
- 下一轮建议首位（暂停中 ✓ 等用户点头）：真浏览器/真手机各一次人工执行（D18 ✓ 这是 G9/G4 唯一没被机器覆盖的一块 ✓ 我已把两个单文件产物和站点版都准备好）→ 拆 `no-hollow-corner` 语义过载 → `--gate G2 --corpus` 接进 `cmdVerify` 并推到 200 seeds → 3MF XSD 子集校验器

### 第 31 轮（客户端可用性三条闭账 ✓ 其中 D24 是被上一轮的修复**顺带**闭掉的）

- **先重测旧结论再继续排优先级** ✓ 第 26 轮记的"手机端 150dpi 全灭"（D24 ✓ 当时 24 个候选全死在 `no-hollow-corner` ✓）拿同一条命令重跑 ⇒ `bend=0px DECODED 符号一致 68904/68904 = 100.00%` ✓ **手机分辨率地板根本不存在 ✓ 那就是第 30 轮那 1 个像素的 bug** ✓ 这也把我第 28 轮撤回的"像素预算"说法彻底清了残余 ✓ 教训：**拿着旧缺陷账目排新优先级之前先重跑它** ✓ 否则一轮的修复会让后面几轮的排序全部错位 ✗
- **D4 闭** ✓ `index.html` 加了指向发送端的导航（链接文字明写"单文件版需从站点打开才有这一页" ✓ 不假装能力 ✓）✓ `build-web` 按既有方式（与 selftest 加载器 / manifest / capture 脚本同一条流水线 ✓ **不另起一套 ✓**）在单文件构建里剥掉它 ✓
- **D14 闭（"UI 不许说谎"的取法：删控件而不是接线）** ✓ 惰性的 `<select id="ssheet">` 换成只读 `<span id="ssheetinfo">` + 标签写"由剖面决定" ✓ 真值本来就在日志里（`sender.js:260` 打印 `纸 W×Hmm` ✓ 取自 `t.geom.sheetMm` ✓ **没有新造数字** ✓）✓ `check-dist` 的 DOM-id 契约断言证明没有 JS 还去抓被删掉的 id ✓ 注：把它做成**真正可选**的纸张需要连编码器一起改 ⇒ 那是新能力 ✓ 本轮不做 ✓
- **两次差点自毁 ✓ 都是打印产物看到的、不是推理看到的**：① 我用 edit 时 `old_string` 圈大了 ⇒ 把 `index.html` 里"不联网、不上传、不需要任何 URL"那句**整段替换**成了导航链接 ✗（差点删掉项目最重要的承诺 ✓ 立刻补回 ✓）② 我给 `send.html` 加"回链"时没先看那页已有内容 ⇒ 那页本来就有 `<nav class="tabs">` ✓ 我加出了**第二条** ✗ 撤掉 ✓ 记为 **D30** ✓
- **D31 同轮补、同轮闭** ✓ 我一度写下"剥离规则按 class 名匹配 ⇒ 缺一条保留侧断言 ⇒ 下轮补" ✓ 随即认定"'一行的事留给下轮'是这个台账里最常见的自欺" ⇒ 当场给 `check-dist` 加 `the served index.html keeps its link to the sender`（**双向**断言：站点版必须有、单文件版必须没有 ✓）✓ **并先证明它会红**：把产物里那条链接删掉 ⇒ `exit=1` ✓ `FAIL` ✓（SW 哈希判据也同时红 ✓ 因为我手改了产物 ✓ 两条都是真信号 ✓）⇒ 重建 ⇒ **`all 10 assertions pass`** ✓ **G9 判据数从 9 变 10** ✓
- 证据：`build-web` exit 0 ✓ `check-dist` **10/10** ✓ 单测 **fail 0（233/233 ✓ 见本轮最后一次运行）** ✓ `smoke-capture` exit 0（12/12 ✓）✓ `smoke-sender` exit 0 ✓ 本轮**未改 `core/`** ⇒ 第 30 轮的 `ALL GATES PASS`（seeds 2 ✓）继续成立 ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**D5**（发送端进单文件变体 ⇒ `file://` 下也能"发" ✓ 这是气隙场景真正缺的那一半 ✓）→ 拆 `no-hollow-corner` 语义过载（`ACCEPTANCE.md` 开放缺陷 #3 ✓）→ **给 `check-dist` 补"该有却没有"型断言的其余几处**（本轮只补了导航 ✓ 同类的"只验剩余、不验必备"的盲区可能还有 ✓）



### 第 30 轮（D26 修掉了 ✓ 根因是**一个像素的取整约定**，不是分辨率）

- 诊断路径：`--explain` 逐个 blob 打印两种取点的着墨率 ⇒ 150dpi 失败现场：**现行取点读到 0.36（判据 <0.35 ⇒ 判实心 ✗）而另一个取点读到 0.00（空心 ✓）** ✓ 追到 `fiducial.js:150` 才看清：`components()` 的 `cx = (x0+x1+1)/2` 是 **bbox 中心**（不是我以为的质心 ✗ 见 D29 ✓）⇒ 与 `floor((x0+x1)/2)` 在**奇数边长时正好差 1 px** ✓ 于是"105dpi 能认、150dpi 认不出"的非单调 = **角标边长奇偶性决定这 1px 把探针推进环墨没有** ✓✓ 与分辨率无关 ✓
- 修法：`hasHole` 取样点改为 `floor((x0+x1)/2)` ✓ **没有动阈值、没有放松判据**（实心角标在新取样点处处仍读 1.00 ✓ 分离度反而更好 ✓）✓ 复验：`node tools/probe-marker-scale.mjs` ⇒ **300/210/150/120/105/90/75 dpi 全部 FOUND**（`exit=0` ✓ 全表一致 ✓）⇒ 手机端等效分辨率可用范围显著变宽（**75dpi 等效、格边 2.5px 仍能定位四角** ✓）
- **两条我自己的错都记进台账**：① **D29** 我给 `comp.cx` 起名叫"质心" ⇒ 生成了一个假根因并写进注释 ✗ 靠真去读代码拆穿（第 14 次"引用未证实的东西" ✓ 这次是**给别人的字段命名** ✓ 更隐蔽 ✓）② **D28** 我在后台验证跑着的时候又改同一个 `core/` 文件 ⇒ 重复声明 `SyntaxError` ⇒ **门限跑在好文件上、check-dist 与两条冒烟跑在坏文件上** ⇒ 一半结论作废 ✓ 从此固定顺序：**文档先写完 → 再跑全套 → 最后提交** ✓
- 门限与套件：`verify --gate all --seeds 2` ⇒ **`ALL GATES PASS`** ✓ 单测 **233/233** ✓（两者都在**最终文件**上重跑 ✓ 结果见本轮提交前的最后一次运行记录 ✓）✓ 另注：`check-dist` 的两条执行型判据**确实抓住了纯阅读/grep 抓不到的语法级破坏** ✓ 它的价值本轮自证 ✓
- 下一轮：**拆 `no-hollow-corner`**（D26 修好后这个 reason 才真正只剩"第四角缺失"一种含义 ✓ 手机端"该靠近 / 该重拍 / 该换页"的分类才有依据 ✓）→ D4 导航入口 → D5 单文件版含发送端 → D14 惰性纸张下拉框 → D24 复评（现在可以量出真正的角标分辨率地板了 ✓）


### 第 29 轮（D26 收窄：角标明明在候选集里 ✓ 丢在"试了哪个角簇"这一步）

- 给 `tools/probe-marker-scale.mjs` 加了 `--explain` ✓ 因为它要读的函数都是导出的（`binarize`/`components`/`pageRegion`/`cropMask`/`keepCandidateSquares` ✓）⇒ 可以**手工走一遍检测器的每一步**而不改产物 ✓ 结果决定性：150dpi 失败时 `findMarkers` 的 `clustersTried` **只有一条**（`cluster 15px → no-hollow-corner hollow=0` ✓）✓ 而同一尺度、同一份裁剪掩模上，**15×15 且 `area=200 < 225` 的带孔 blob 中心 5×5 着墨率 = 0.00**（空心判据是 <0.35 ✓）⇒ **空心角标就在候选集里** ✓ 丢失在**角簇/阈值尝试的枚举与取舍**（`fiducial.js` L360–400 一带 ✓）✓ **排除**：二值化本身、格点几何、以及我上一轮准备的两个解释 ✗
- **我自己的两个猜想都被同一张表否证**（都写进 DEFECTS 防复发 ✗）：① "角标像素预算地板"（第 28 轮撤回 ✓）② 本轮新的"探针半径 `round(0.12×边)` 跳档把孔量成实心"——150dpi 处 `probe=2` 即 5×5 ✓ 而 5×5 着墨率 0.00 ✓ 孔完好 ✓ **不成立** ✗ 若非先量一次，我本来会把这个假机制写进核心判据里 ✓
- 探针侧又踩一次"引用未证实的形状"：把 `cropMask` 的返回当 bin 传（它返回**裸 `Uint8Array`** ✓ 且副作用写 `region.cropInset` ✓）⇒ `TypeError ... reading 'length'` ✓ 记为 **D27**（第 13 次 ✓ 模式未断根 ✓ 但**全部由执行抓获 ✓ 零次靠阅读 ✓** 这条模式本身就是 D23 的内容 ✓）
- **本轮不动 `core/`**：手上还没有"证据支持的最小修法" ✓ 猜测式改动角簇枚举 = 拿朝向判定（`tests/unit` 里"90 度旋转页仍能解出 ✓ 朝向来自空心角标"这条压着它 ✓）冒险 ✗ 修法下轮做 ✓ 且改完必须重跑单测 + 全套门限 ✓
- 证据状态：改动仅 `tools/` 与 `docs/` ⇒ 第 28 轮的 **233/233 单测** 与 **`ALL GATES PASS`（`--gate all --seeds 1`）** 继续成立 ✓ 台账一致性判据在文档改完后**重跑**（绿 ✓ 不引用旧结果 ✓）✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位：**D26 的最小修法**——读 `fiducial.js` L355–400 的角簇枚举与阈值阶梯 ✓ 让"候选集里那个空心 blob"真正进入某次尝试的判定 ✓ 然后按判据重验（旋转页朝向 + G0–G10）✓ 拆 `no-hollow-corner` 仍在它后面 ✓


### 第 28 轮（测量否决了我自己正准备做的修复 ✓ 并抓出更锋利的 D26）

- 计划本来是"拆 `no-hollow-corner`"（挂多轮 ✓ 我被它误导三次 ✓）✓ 做法想好了：**按角标像素尺寸设地板阈值** ✓ 但先量了一下 ✓✓ **量出来这个修复是错的**：同一张页做**干净降采样**（无模糊无噪声），**105dpi 与 120dpi 能找到四角 ✓ 150dpi 找不到 ✓ 90dpi 又找不到** ✓ 非单调 ⇒ "像素不够"不是成因 ✓ 任何像素阈值都是在**给假信号立法** ✗
- 我准备用来定阈的那个读数自己也露馅了：它在两个失败例里分别读到 **15 和 9**（更"轻"的失败反而更大 ✓）⇒ 它量的是**最大连通块**（很可能是粘连的数据格）✓ **不是角标尺寸** ⇒ 字段由 `maxMarkerSide` 改名 `maxBlobSide` 并在注释里写清"它不是什么、不许用它推像素地板" ✓ **留一个会让人误读的仪表比没有仪表更坏** ✓
- 新 durable 探针 `tools/probe-marker-scale.mjs`（台账要求每条缺陷可复现 ✓ `.tmp` 脚手架不算 ✓）：`node tools/probe-marker-scale.mjs` ⇒ `exit=2` + 三行翻转记录 ✓ 这是 **D26**：手机端真正的障碍不是"分辨率地板"而是**检测对尺度不单调**（失败形态统一是 `hollow=0` 且候选很多 ✓ 像二值化阈值阶梯与该尺度的交互 ✓）
- **两处自我撤回写进 DEFECTS**：① 上一轮 D24 的"角标像素预算是地板"✓ 撤回 ✗ ② "用像素阈值拆 reason"这个方案作废 ✓ **D24 后半（拆 reason）从"本轮就做"改为"阻塞在 D26"** ✓ 有了真信号再拆 ✓ 不硬造分类 ✗
- 代码改动仅 `core/decode/fiducial.js` 的**失败返回新增两个诊断字段**（不改任何判定 ✓）⇒ 重验：全套单测 **233/233** ✓ **`node cli/pskit.mjs verify --gate all --seeds 1` ⇒ `ALL GATES PASS` / `exit=0`** ✓（按第 26 轮记的读法：输出落盘看末行 ✓ 不用 `| Select -First` ✓）
- **不打新 tag**（M4 未闭合 ✓）✓ 下一轮首位：**D26 定位**（150dpi 那个点上到底是哪一步把空心角判丢的 ✓ 有候选、有阈值阶梯、有 `hollow=0` 三条线索 ✓ 这是纯诊断、不动判据 ✓）→ 拆 `no-hollow-corner` → D4/D5/D14（客户端与 UI 说谎项）


### 第 27 轮（把卷曲测到传输级 ⇒ 第一次用测量、而不是教条，改掉一个 PLAN 特性的优先级）

- **D25 结案**：`tools/probe-curl-tolerance.mjs` 加 `--transfer` 模式 ✓ 走**真 `protocol.TransferAssembler`**（与 `cli/pskit.mjs:448` 和浏览器客户端同一个类 ✓ 不是我自己拼的"等价校验链" ✓）✓ 控制档 `bend=0` **ACCEPTED-CORRECT（1024 B vs 1024 B · `fed×3`）** ⇒ 这张表算数；`bend=4px`（第 26 轮页级曾 47.9% 错读）在传输级被 **`assemble-rejected/intra-fail×3` → REFUSED** ✓ **零误接受** ✓ `bend=12px` 解码直接拒 ✓
- **D21 因此改判**：卷曲这种"四角单应表达不了"的形变**只造成拒绝、不造成误接受** ⇒ 对齐梳从"正确性缺口"降为"**鲁棒性收益**" ✓ **不足以成为改动采样路径的理由**（G3/G5/G7 全压在那条路径上 ✓ 为它冒险 = 拿已证的 0 误接受去换一个未证的通过率 ✗）⇒ 记账口径写明"**CLOSED-为判决、非闭为已实现**"✓ PLAN 该项仍标未实现 ✓ **不改契约文本** ✓ 去留等 G4 真机数据 ✓
- **新机器判据 `tests/unit/defects-ledger.test.mjs`**：起因是我**连续三轮**手改台账时吃掉小节标题（同一动作犯三次 ✗ 说明靠"下次注意"无效 ✓）⇒ 改成断言"**同一缺陷号不得既是 OPEN 又是 CLOSED**"+"**每行必须有可复现命令**" ✓ 第一次跑就抓到 **6 处状态自相矛盾（D11/D16/D17/D20/D21/D25）+ 2 行没有复现命令** ✓ 已全部修 ✓ 判据第一次跑就红，我反而才信它 ✓
- **也撤回一句我说重了的自罚**：我在探针注释里写"连拍冒烟的 `feed` 是桩 ⇒ 手机真接线从未执行" ✓ 读 `tools/smoke-capture.mjs:49-52` 后确认那里用的**就是真 assembler** 且 63-64 行断言 `asm.result` 摘要 ✓ 桩只在门控子用例（恰当 ✓）⇒ 注释已改回事实 ✓ 记进台账：**虚假的自我批评与虚高、虚低同类 ✓ 都是污染台账** ✓
- 门限与套件：**233/233**（231 + 台账 2 条 ✓）✓ **G9 产物级 9/9** ✓ 连拍冒烟 **12/12** ✓ 卷曲传输级判决已复现 ✓ 本轮改动仅在 `tools/` 与 `docs/` 与新增测试 ⇒ **`core/`、`web/` 未动 ✓ 既有 G0–G10 结论继续成立** ✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮首位回到 **objective 第 (1) 条客户端本体**：**D24 的前半——把 `no-hollow-corner` 拆开**（"凑不齐四角"与"角标被糊住/分辨率不够"共用一个 reason ⇒ 手机端的失败分类与"该靠近还是该重拍"的提示都依赖它 ✓ 且本轮它第三次误导我 ✓）→ **D4 导航入口** → **D5 单文件版含发送端** → D14（惰性纸张下拉框说谎）


### 第 26 轮（第一次真量测：卷曲与角标分辨率 ⇒ D21 从意见变成可判定命题）

- 新工具 `tools/probe-curl-tolerance.mjs`：**合成柱面弯曲 + 真解码器** ✓ 唯一目的是给 PLAN L77 那条"从未实现的亚像素格点相位（对齐梳 ✓ D21）"估个值：它到底补什么、不补会怎样 ✓ 先想清楚**四角单应已表达什么**（平移/旋转/均匀与微分缩放/剪切/透视 = 仿射 6 + 射影 2 ✓ 逐格另有 ±1px 整数搜索 ✓）⇒ 梳子唯一能补的是**单应表达不了的离面形变（卷曲）** ✓ 于是"要不要忠于 PLAN"被换成"卷曲在什么量级造成什么后果" ✓ 这才是能测的问题 ✓
- **结果 A（手机端 ✓ objective 第 (1) 条）**：同页降到 1/4 面积（2260×3290→1130×1645 ✓ 等效 150dpi ✓ 清晰降采样、无噪声）⇒ **24 个候选全部死在 `markers/no-hollow-corner`** ✓ 没走到读出 ⇒ 卡点是**第四空心角的像素预算** ✓ 不是尺度搜索 ✓（我一度断言"几何搜索不跨尺度"✓ **说过头了、已撤回** ✓ 撤回理由写进 DEFECTS：`rectifyPage(bitmap, layout, found.quad)` 原理上就是跨尺度的 ✓）另证 `no-hollow-corner` 语义过载**本轮又一次把我带偏**（`ACCEPTANCE` 开放缺陷 #3 ✓ ⇒ 拆 reason 升为承重项 ✓）
- **结果 B（正确性 ✓ 但未越权下结论）**：native dpi 下 `bend=4px` ⇒ `bootstrapDecode` 返回 `ok:true` 而 68904 格中 **47.9% 与真值不符** ✓ `bend≥12px` 直接拒绝（安全 ✓）。**这不叫误接受**：页级 ok 之上还有 RS + 末端 SHA-256（G5 已测 10000 次篡改 0 误接受 ✓）⇒ 我把工具的结论行**降级到它能证明的范围**并新增 **D25：传输级复测卷曲**（下一轮第一件 ✓ 它决定 D21 是"鲁棒性收益"还是"误接受家族"✓）
- **仪器纪律第二次生效、也是我自己犯第二次**：这版探针第一稿**在控制档失败的情况下照样打印了自信结论** ✓ 加上控制闸后才看见"基准根本没解出来"✓ 而真因是两处纯探针 bug：`sampleBilinear` 返回 `{values,inside}` 我按数组读 ⇒ 交出去一张**全黑图**（解码器拒收是**它对** ✓）✓ 以及 `oor++` 忘了 `let` 声明 ⇒ 恰好在 `bend≠0` 时才炸 ✓ 两处都由"仪表"而非阅读抓获 ✓ 记 D23 第三次 ✓ 另：`| Select-Object -First N` 截管道杀 node 造成假 `exit 1` ✓ **我自己在 DEFECTS 判读提示里写过这条还是踩了** ✓
- **D16 闭**：`docs/ACCEPTANCE.md` 的 G9 行由"未开始/零证据"更正为 🟡 并列全 9 条产物级判据与复现命令 ✓ 同时更正"一句话结论"里对 G9 的**低估** ✓ **台账虚高与虚低同样是错** ✓
- 门限状态：G9 产物级 **9/9** ✓ 连拍冒烟 **12/12** ✓ 单测 **231/231**（本轮未改 `core/`、`web/` ⇒ 判据仍成立 ✓ `git show --stat` 可查 ✓）✓ **不打新 tag**（M4 未闭合 ✓）
- 下一轮顺序：**D25（传输级卷曲复测）** → **D24 的前半（拆 `no-hollow-corner` reason ✓ 手机端分类判据依赖它）** → D21 依 D25 结果定去留 → D4/D5/D8/D13/D14


### 第 25 轮（构建原子性、连拍结案、清单数字纠偏、以及对 PLAN 几何契约的一次实勘）

- **D11 闭**：构建改为写 `web/dist.tmp` 再换入 `web/dist` ✓ **用一次真实的中途失败验证**：移走 `send.html` ⇒ `exit=1` 而 `web/dist` 仍 **49 文件、`sw.js` 在位** ✓（旧站点完好 ✓ 半途而废的构建不再可能被 Pages 当成成品发出去 ✓）
- **D17 闭（结论反转）**：连拍的跨会话守卫**从来没坏过** ✓ 坏的是我的夹具——第一帧我喂了 `addFrame({})`（空对象）⇒ 被 `no-header` 拒 ⇒ `current` 一直为 null ⇒ 第二帧自成新会话 ⇒ 症状 `kind page · have 1/3` 全部对上 ✓ 修法是在断言里**打印夹具自身的中间量**（`frame1 …` 一行）✓ 12/12 ✓ 教训进 D23：**断言红的时候先怀疑夹具、并且让夹具自己说话**，上一轮我写了"未定论"是对的 ✓ 但当时多打一行就能当场结案 ✓
- **D20 闭**：`check-dist` 新增判据"构建出的 JS 里每个 `getElementById` 必须在某个构建出的页面里有对应 id"（48 个 markup id ✓）✓ 这类"改了 HTML 忘了 JS ⇒ 功能静默消失"的口子从此有判据守着 ✓
- **D22 纠偏（我自己的数字造假，虽非故意）**：上一轮我把成绩写成 "78 条 precache 哈希逐字节核对" ✓ 实际磁盘只有 **49** 个产物——因为 `writeDist` 对同一文件写两次就记两条账（第 24 轮为修"dist 缺模块"而加的 allCore 拷贝与 import 闭包重叠 ✓）。现去重 + 新增**双向紧度判据**（清单条目数必须等于磁盘产物数、且不得有重复 url）✓ 修后 **49 = 49** ✓ `precache 47 = 49 − sw.js − build-manifest.json` ✓ 判据从 8 条增至 **9 条全绿** ✓
- **实勘 PLAN §几何 L77（objective 第 (2) 条）**：fiducial **有实现**（`layout.js:105` 三角实心 + 第四角空心 ⇒ 透视检查兼 90°/180° 朝向判定 ✓ 与 PLAN 的"同心方环 + 朝向标记"等价 ✓）；但 **"四边双频对齐梳 / 亚像素格点相位"在 `core/` 里找不到任何实现**（全仓搜 `comb/梳/gridPhase` ⇒ 只命中 `ideal.js:159` 的**整数**对齐搜索 ✓ 那不是梳 ✗）⇒ 记为 **D21**：打印缩放/走纸造成的亚像素相位偏移目前靠整数搜索 + ECC 吸收 ⇒ 这影响 G4 手机档与纸面鲁棒性，**属正确性、不属性能** ✓ 另注：**"裁切标记"不在 PLAN 契约里**（我上一轮 objective 措辞里带了它 ✓ 不能拿我自己编的词去判 PLAN ✓）
- 门限状态：**G9 9/9（产物级）** ✓ dist selftest 13/13 ✓ 连拍冒烟 12/12 ✓ 单测 **231/231** ✓ 未跑 G3/G4 相关新证据 ⇒ **不打新 tag**（M4 仍未闭合 ✓）
- 仍 OPEN 且下一轮优先：**D21（对齐梳）** → **D16（`ACCEPTANCE.md` 的 G9 行仍写"未开始"，台账与判据页不一致）** → **D18（连拍的实机部分本机不可验证 ⇒ G9 维持 🟡 的根据）** → D4/D5/D8/D13/D14


### 第 24 轮（手机连拍外壳：挑帧 / 去重 / 缺页提示，纯函数化后可在 Node 验证）

- 先证实了地面：`--photo` **不是未实现**——它是 `allowFastPath:!args.photo` ✓ 角标检测与 `rectifyPage` **本来就在 `core/decode/page.js` 里**（返回 `{path:'photo', markerPx, coverage}` ✓）⇒ 连拍不需要新的解码理论 ✓ 只缺"取哪一帧、怎么去重、怎么报缺页"的外壳 ✓（我一度推断"CLI 没实现" ✓ 只搜了 cli/ 就下结论 ✗ 幸而复核了 `allowFastPath` 与报告字段）
- 新 `web/capture.js`：`createBurstCollector({decode, feed, gate})` 是**纯函数**（DOM 半边同样关进 `getElementById('burst')` 守卫 ✓ 于是 Node 可 import ✓），职责严格限定在三件事——质量门控（角标 <14px ⇒ "把手机靠近一点" ✓ 覆盖率 <72% ⇒ "退半步把整页拍进去" ✓）、按 `sessionId + pageIndex` 去重（手机不动 ≠ 进度 ✓）、从声明的 `totalPages` 报"缺第几页" ✓ **校正本身不重写**（已由 `tests/unit/warp.test.mjs` 用已知单应变换覆盖 ✓ 重写就是第二套真相 ✓）
- 新 `tools/smoke-capture.mjs`：**11/12 通过** ✓ 真实 3 页逐页收下并**解回输入摘要** ✓ 重复页判 `duplicate` ✓ 两道门控各出一条可执行提示 ✓ 页数声明冲突拒绝 ✓ **把页裁掉一半的帧不被当成一页** ✓
- **唯一红的那条不糊弄**（D17）：跨会话守卫返回 `kind page` 而非 `other-session` ⇒ `locate()` 读到的 sessionId 形状与我的假设不符，或夹具仍错 ✓ **未定论就写未定论** ✓ 并且说清风险边界：**只降级提示语、不降级安全**（混批由 `TransferAssembler` 的 `other-session` 拒绝兜住 ✓ G0/G5 已测 ✓ 10000 次篡改 0 误接受 ✓）⇒ 禁止用删断言换绿 ✓
- 又抓到自己的 2 处凭空引用：位图字段是 **`pixels`（RGBA 4 通道 ✓）不是 `data`** ✗ 以及 `feed` 里我 import 了**不存在的 `./app-state.js`** ✗（第 11、12 次 ✓ 全部由执行当场抓获 ✓ 无一次靠阅读发现 ✓）；另修 D19（`./capture.js` 在 precache 清单里重复两项 ✓ 一行跳过）
- 产物与门限：`web/dist` 82 文件 ✓ **G9 `check-dist` 8/8**（含"页面每个引用都要有对应产物" ✓ 新接的 `capture.js` 与 section 一并受检 ✓）✓ dist selftest **13/13** ✓ 单测 **231/231** ✓ **不打新 tag**（M4 未闭合 ✓）
- 仍 OPEN 的头两项没变：**D11 构建中途失败会在 `web/dist` 留半套产物**（应写临时目录再原子换入 ✓）· **D16 `docs/ACCEPTANCE.md` 的 G9 行仍写"未开始"** ✓ 另有本轮新记的 D18（连拍实机部分本机无法验证 ⇒ G9 维持 🟡 的核心理由）、D20（HTML section 与 JS 里 `getElementById` 的 id 契约无判据保护 ⇒ 改一边忘一边会静默失效）


### 第 23 轮（客户端优先：手机+电脑发送端、G9 产物级闭合、缺陷台账立规）

- 用户改序：**先做客户端（手机+电脑）→ 再做 2D/3D 可打印产物；性能不管、错误先记账**。目标已重启（上限 40 ✓ `phase=active`）。
- **发送端进浏览器**：`web/send.html` + `web/sender.js` ⇒ 选文件 → 编码 → 页预览 → **PNG / `pack.pdf`（真实物理尺寸）/ `.3mf` / `.stl` 码牌** ✓ 全部本机完成；下载走 `data:` URL ⇒ 不需 `blob:`、**不放宽 CSP** ✓
- **CLI 的拒写护栏原样搬**：`projectionReport` / `stlSelfCheck` / `selfCheck3MF` 不过就**一个文件都不出**；纸面剖面拒绝出实体盘 ✓ `pickPalette` 规则原样移植（页面不能 import `cli/` ✓ 注释指明保持同步 ✓）
- 新增 `tools/smoke-sender.mjs`：把 `buildArtifacts()`（DOM 已关进守卫 ⇒ Node 可 import）在纸面 `P-M1-300` 与盘面 `PL-D2` 两条路径上**真跑**，再把出的页喂回 `bootstrapDecode`（无提示）重组 ⇒ **摘要等于输入摘要** ✓ 盘面 **8 个浮雕全过三道护栏**（42 万三角形/页 ✓ bbox 189.31 mm）✓ 负例（30 dpi）在 `stage layout` 拒绝 ✓
- `web/dist` 从 70 → **80 文件**：`send.html`/`sender.js`/`manifest.webmanifest`/`icon-page.png` 进产物 ✓ 图标由**自家 `core/render/png.js` 真渲染一页**生成（零新依赖 ✓ `package.json` 的 `dependencies` 仍是 `{}`）✓ 单文件变体剥掉 manifest 引用（否则我自己的"自包含"判据会红 ✓）
- `tools/check-dist.mjs` 加了一条**本来就该存在**的判据：页面里每个 `src=/href=` 都必须有对应产物 —— 它抓到过 `./web/app.js` 这种"构建报成功、站点 404 自己的入口脚本"的坏构建 ✓
- 门限：**G9 8/8（产物级）** ✓ 单测 **231/231** ✓ `verify --gate all --seeds 2` ⇒ `ALL GATES PASS`（G5 一万次篡改 **0 误接受** ✓）✓ **不打新 tag**（M4 未闭合 ✓）
- **我这一轮的错（全部当场被执行抓住，无一次靠眼看 ✓）**：① 第 7–9 次"引用未证实的东西"——`p.plate.mm`/`p.sheet`/`prof.palette` 三个**不存在的剖面字段**、`sheetMm` 传布尔、以为 bundle 里有编码侧 `render/png.js`、`header.kind==='parity'` 取值域未证实；② 第 8 次"注释被当代码"——我在 `bootstrap.js` 写的解释性注释里放了一个 reason 字面量，**被 advice 扫描器当成发射点**，于是"给一条成功配建议"的荒谬要求出现（修法=改我的字段名与措辞 ✓ **没动判据** ✓）；③ 我用 PowerShell `Set-Content` 改源文件破了 AGENTS 的 CP1252 铁律 ⇒ 该文件变成非法 UTF-8（`read` 拒绝 ✓ 已删除重写 ✓）；④ 差点把 G9 记成 ✅（两条判据只在 Node 里跑过、浏览器内从未验证）⇒ 已回退为 🟡 并写明"要人在本机之外点一次才算闭合"；⑤ 差点把 `Select-Object -First` 掐管道导致的 `exit 1` 记成门限失败 ✓ 已写进台账的"判读提示"
- 新建 `docs/DEFECTS.md`：闭掉 D1/D2/D3/D6 ✓ **OPEN 十一项**，其中最要紧的三条：**D7 手机连拍取页未接线**（`fiducial.js`+`warp.js` 已就绪 ✓ 下一轮第一优先）、**D11 构建中途失败会在 `web/dist` 留半套产物**（Pages 若直发即发破损站 ✓ 应改为写临时目录再原子换）、**D16 `docs/ACCEPTANCE.md` 的 G9 行仍写"未开始"** ✓ 另有 D14 一个说谎的下拉框（纸张尺寸其实来自几何 ✓ 改它不影响输出）


- **第 22 轮（末轮·收官：撤回第 21 轮下得太快的根因 ✓ 并给下一步一份诚实工单）**
  - **第 21 轮那句"B 类 = 渗墨把 rho 推过 0.185 中点 → 整页读成 level 1"未被证实、我收回** ✓ 写它时我连错两处口径：① 真正的 level-0 边界是 `noDot = max(target0, mid0·0.55) ≈ 0.102`、**不是 0.185**；② 我拿 `levels`（`joinCellLevels({shape,colour})` 的**打包值**）按 `v===0` 切分当形状直方图用 ⇒ 把色道 bit 混进了形状计数 ✗ 决定性反例：`tools/diag-collapse-path.mjs` 实测**能逐字节复原的健康页** `nc-scan600-1/000` 在这里同样是严重偏斜的 0/1 分布且 `rho-vs-template disagree=74240` ⇒ 若"过中点即整页翻 1"成立，一个成功的页不该长这样 ⇒ **要么逐格读数远比 ECC 级成功所暗示的更噪、要么这个探针假定的"扫描页↔pristine 页按文件名对应"根本不成立** ✓ 两者都说明那条因果链没被建立 ✓
  - **仍然站得住的**（不依赖那处收回的判断 ✓）：A/B 两类的**存在**由第 19 轮逐格对照独立测得（`0.23%–7.22%` 的一群 vs `287507/287507 单符号` 的那一群 ✓）；`inkRatio` 是**渗墨量度**（pristine `1.065` vs 600dpi 信道页 `3.6–21.7` ✓ 与"σ 固定 mm、600 dpi 按格宽度翻倍"自洽 ✓）；第 20 轮**符号分布闸被总体数据否证**（健康页 `maxFrac` p90=0.9057、与坏页只差 7 个百分点 ⇒ 载荷可压缩 ⇒ 该判据必误杀 ✓ 未实现 ✓）
  - **诚实状态**：G2 的 B 类 = **已确证存在、已量出渗墨相关、根因未定** ✓ 我不给一个自信的错因收尾 ✗
  - **下一步工单（一条，最小且可验，供第 23 轮或人接手）**：把 `diag-collapse-path` 修成**可信**——用 `splitCellLevel` 取纯形状直方图、用**解码出的页头**（非文件名）对齐页、并把 pristine 与 degraded 的**同逻辑页**并排比；修好后它应能明确回答"崩塌页的**形状** level-1 占比是否随 inkRatio（渗墨）单调上升" ✓ 若能 ⇒ 渗墨→形状判决的因果才可主张、再谈"rho 域谷底"修法（该修法会**让接受更容易 ⇒ 必须回 G5 重验 + 跨 seed 稳定 ⇒ 防第 11 轮过拟合** ✓）；若不能 ⇒ 渗墨相关是巧合、B 类另有其因 ✓
  - **末轮结论（不主张完成）**：G0 G1 G3 G5 G7 **通过** ✓ M5/网格（G8 的 STL+3MF+投影）经我亲自重跑 **PASS**（55 项独立 stdlib 校验 ✓ 确定性逐字节 ✓）✓ **未闭合**：G2（B 类根因未定 ✓ 且判据未明示"纸张完整在成像区内"这一前提 ✓）· G4（手机压力档 500×8 **一次未跑** ✓ 零证据）· G6（soak 未跑 ✓）· G9（`web/` Pages 扫描端 **未开始** ✓ 零证据）· G10（喷嘴矩阵只测到 PL-G 在 0.6/0.8 被 `glyphGeometry` 正确拒绝这一个边界点 ✓）⇒ **"通过 G0–G10" 不成立、我也不主张** ✓ 全程未降任何判据制造"完成" ✓ 基线：单测 `231/231` ✓ `ALL GATES PASS` ✓ `ref/decode.py` `PASS`（309 检查 / 4 项已接受 geometry 规范缺口 ✓）

- **第 21 轮（第二条假闸死在测量下 ✓ 但把它杀死的测量顺手给出了 B 类的真因）**
  - 按第 20 轮定下的方案实现"实测墨 vs 符号声称墨"（`core/decode/ideal.js` 里逐格累加 `a.rho` 与 `targets[shapeLevel]` ✓ 两边同尺已核：`measureTargets` L205 是 `dot/(band·bandScale)` ✓ `analyseCell` L118-122 是 `dotA/bandA`＝同一个式子 ✓）
  - **`misfitFrac` 构造性作废**：`targets = [0.000, 0.370]` ⇒ **level 0 的理想值就是 0** ✓ 我却按"相对误差 >50%"算 ⇒ 零目标没有相对误差可言 ⇒ 每个 padding 格都被判失配 ⇒ 实测 `misfit 0.9057` 与 `maxFrac 0.9057` **逐字相等**（即它只是在数"有多少格是 level 0" ✓ 与崩塌无关 ✗）⇒ 我写在代码里的注释"用相对误差以免小目标被豁免"**方向正好相反** ✓ 已纠正
  - **`inkRatio` 也不是崩塌探测器**：pristine 页 `1.0651–1.0660`（极紧凑 ✓）但**逐字节成功的**信道页 `3.6–21.7` ⇒ 它测的是**渗墨**：模糊 σ 是固定 mm ⇒ 600 dpi 下按"格"算宽度是 300 dpi 的两倍 ⇒ 邻格墨渗进 level-0 格的点区 ⇒ `rho` 从 0 被抬起 ✓ 与"600 p50=16.0 对 300 p50=4.5"完全自洽 ✓
  - **于是 B 类因果链闭合**：两点形状字母表的判决边界固定在中点（`0.185` ✓）⇒ 渗墨把 padding 格的 `rho` 推过中点 ⇒ **整页读成 level 1** ⇒ 正是 `7/002` 的 `287507/287507` 全为 1 ✓✓ 也解释"只有 600 dpi 出 B 类" ✓✓ 这不是解码器 bug、不是信道作弊 ✓ 是**边界放置方式对渗墨不鲁棒** ✓
  - **候选修法（未实现、未测 ⇒ 不算结论）**：两点字母表的边界不钉死在中点，而是落在**该页自身 `rho` 分布的谷底**（一维 Otsu 于 rho 域 ✓ 自描述 ✓ 不引入新常数 ✓）⇒ 这才是第 15 轮 `calibrate` 该做的事 ✓ 风险先写明：它会**让接受更容易** ⇒ 必须回 G5 按误接受口径重验 ✓ 且要防"给单个 seed 找谷底"的第 11 轮式过拟合 ⇒ 判据应是"跨 seed 的稳定谷底"而非"这页能过" ✓
  - 自曝（同类错误第 2 次、本轮第 1 次）：我断言"`page.js` 用 `...read` 透传所以不必改" ✗ 实际 `readFast` 是**显式白名单**返回（L68-75 ✓）⇒ 第一版测量白跑了 12 分钟 ✓ 教训升级为：**"我以为它会透传"不是证据** ✓ 改完必须用一条输出证明新字段真的到了调用方 ✓
  - 判决不动 ✓ G2 仍 `30/32 = 93.8%` ✓ 基线 `231/231` ✓ `ALL GATES PASS` ✓

- **第 20 轮（一条设计选项被数据提前关掉 ✓ 没让它长成一道会误杀的闸）**
  - 我第 19 轮说"符号分布不可能 ⇒ 不需真值就能抓到 B 类崩塌"✓ **这句不成立** ⇒ 本轮先量总体再定阈值（`tools/diag-symbol-population.mjs` ✓ 32 份语料 × 每页 ✓ 96 页里 78 页读到格层 ✓）⇒ **健康页自身就极度不均**：`maxFrac` 的 `min 0.5735 / p50 0.6052 / p90 0.9057 / p99 1.0` ✓ 而 `0.9057` 那批是**逐字节复原成功语料的 page-000** ✓✓ 崩塌页是 `0.9775 / 1.0` ⇒ 阈值只剩 **7 个百分点**的缝 ⇒ 载荷压缩率一变就误杀 ✓ **判据不成立 ✓ 因此没有实现它** ✓ 另两条候选同样死：`distinct` 全体只有 **2**（那就是本剖面的字母表本身 ✓ 不是症状 ✓）⇒ `distinct==1` 只抓得到 4 个崩塌页中的 1 个 ✓；`secondFrac` 分布重叠（`min 0.0 / p50 0.3948`）✓
  - **仍然成立的替代判据（下一轮做）**：把**符号流**与**图像自身实测的墨量**对表——`integrated coverage`（第 15 轮已验：`integ/expected = 1.04–1.34` ✓ 6 页 ✓ 不需真值 ✓）对照"由读出符号隐含的每格面积"（`glyphGeometry` 的逐级面积 ✓ 第 15 轮 `integrateLevelAreas` ✓）⇒ **它不受载荷可压缩性影响**（图上的墨就在那里 ✓ 正确读出必须复现它 ✓ 崩塌读出不复现 ✓）⇒ 这才是 B 类该用的那道闸 ✓ 代价：要把逐格 `alphaMap` 从 `readPageIdeal` 引出来 ✓ 已挂账 ✓
  - 判决未动 ✓ G2 仍 `30/32 = 93.8%` ✓ A/B 两类见 `docs/ACCEPTANCE.md` 的 G2 节 ✓ 本轮净收益 = **省掉一条会在真机上误杀好页的假闸** ✓（这类"先量总体再定阈值"的做法是第 11 轮 `0.65` 教训的直接延续 ✓）

- **第 18–19 轮（G2 剩余病灶定界为"逐页格极性翻转" ✓ 但过程里我自己造了三个假数字）**
  - **第 18 轮欠的仪式先补齐**：`tools/diag-intra.mjs` 一度整轮未跟踪 ✗（我在第 14 轮刚批评过同一件事 ✓ 这次轮到自己 ✓）✓ 本轮随修一起提交 ✓
  - **三个我自己的错，全部由证据而非"想得更深"纠正**：① 该工具首版比较 `.shape`/`.colour` ⇒ 而 `levels` 其实是**打包数字** ⇒ 两边皆 `undefined` ⇒ **对正在失败装配的页报出"0 格失配、前端完美"**（本项目**第 6 次空转断言 ✓ 第一次发生在"专门用来找错误"的工具里 ✓**）；② 修好后我硬编码 `nozzle 0.4` / `palette INK2` ⇒ 而语料 manifest 写 `nozzle:null` / `palette:PAPER1` ⇒ 现改为**从 manifest 读、读不到就拒绝运行** ✓；③ 我把"语级 OK"当成"页级 OK"来构造矛盾（第 12 轮刚写下"别让两层互相顶包 ✓ 这次是我反向踩了 ✓"）⇒ 其实 `nc-scan600-11/page-001` **本就是失败页**（census 里那条 `assemble/intra-fail` ✓），整份语料成功是靠**页间奇偶从别的页复原** ✓ 与 G3 的设计完全一致 ✓ 于是 88.18% 失配是**真读数** ✓
  - **两个假设被测量否证**（都记下来免得再被提起 ✓）：quad 尺度不是判据——失败页与健康页、**甚至 pristine 页**的 `cols×cellPx` 偏差**完全相同**（`1.1% / 0.8%` ✓）⇒ 那是"角标中心与格框的固定几何关系"、我期望值取法不对 ✗；整格相位偏移也不是——相位扫描 ±1/±2 格全在 `88.17–88.76%` ⇒ 无一个偏移能救 ✗
  - **真发现（本轮唯一新结论 ✓ 带对照 ✓）**：符号直方图直接摊牌——pristine 页 `0:259 991 / 1:27 516`（**9.6% 为 1** ✓）对 degraded 页 `1:281 045 / 0:6 462`（**97.7% 为 1** ✓）⇒ **格极性整体翻转** ✓ 而同一语料的 `page-000` 与 `page-002` 失配 **0.00%** ✓✓ ⇒ 是**逐页**发生的翻转、不是全局调用错 ✓ 症状全部对得上：只在 600 dpi（`cellPx` 10 对 20 ⇒ 判决更脆 ✓）· 整页作废报 `intra-fail` ✓ · 结构与几何检查全过（`coverage=100%` ✓ `markerPx=32` ✓）⇒ **病灶在格读出、不在矫正** ✓
  - **判决不动** ✓ G2 仍 `30/32 = 93.8%` ✓ 新机制**尚未修** ✓ 下一轮：① 扫全部 ≈9 条 `intra-fail` 页看是否**都**是极性翻转（若是 ⇒ 一个窄修法 + 一条"读出后自检符号分布"的防御 ✓ 若否 ⇒ 仍有第二种病 ✓）② 查 `readPageIdeal` 里决定符号极性的那一步（可疑：低对比度下的 blank/背景判定与 `colourAlive` 的路由 ✓）
  - **流程改进（本轮长出来的 ✓ 已落到工具里）**：诊断工具现在**自带两条反空转测试**——符号直方图是否一致（区分"内容坏了"与"顺序/极性错了" ✓）与整格相位扫描 ✓ 这两条都是**在报数字之前**先证明数字有意义 ✓ 另新增 `printedAreaFraction` 回显（manifest 自报 0.247 ✓ 与第 15 轮算出的期望着墨率是同一类量 ⇒ 将来可当免费交叉核对 ✓）

- **第 17 轮（G8 的 schema 半边拿到权威文本 ✓ G2 的剩余病灶被分类器指到 `intra-fail`）**
  - **拉取并 vendored 官方 3MF Core 1.4.0 XSD** ⇒ `ref/3mf-core-1.4.0.xsd`（10 196 B ✓ 带出处头 ✓）+ 新工具 `tools/fetch-3mf-schema.mjs`（可重取 ✓ 缺关键字则**拒绝写文件** ✓ 防止"存了个空 schema 当证据" ✓）。**三件事由文本裁决、不再靠记忆**：① `CT_Model` 属性只有 `unit/xml:lang/requiredextensions/recommendedextensions` ⇒ **`zUp` 不存在 ⇒ 我契约写错、子代理写对** ✓ ② `unit="millimeter"` 合法 ✓ 我契约里的 `<unit millimeter="millimeter">` 是胡写 ✓ ③ `CT_Resources` 要求 **`basematerials` 全部排在 `object` 之前**、`CT_Model` 要求 `metadata→resources→build`、`CT_Vertices ≥ 3 vertex`、`CT_Triangles ≥ 1 triangle`、`elementFormDefault=unqualified` ⇒ **元素顺序是规范判据不是风格** ✓ 已把 §5 两处错误按权威改写（**判据未降 ✓ 改的是我写错的格式细节 ✓ 并记明"逐 object 水密判据优先于'一个 object'那句措辞"** ✓）
  - **G2 分类器落地并证实第 16 轮的诊断**：`tools/g2-corpus.mjs` 现从**像素自身**数"四个角里真有几个角标"（不信信道报告 ✓）+ 打印四边着墨率 ⇒ 32 份语料重跑（360.8 s ✓ 仍 `30/32 = 93.8%` ✓ **判决未上调 ✓**）：两个失败语料的角标页分别测得 **顶边 6% / 底边 47%、左边 13%** ⇒ **出画确证 ✓** 分类为 `fiducial-out-of-frame` ✓ 且 `no-marker-size-cluster`/`no-hollow-corner`/`quad-too-small` 三种 reason 都归到同一物理原因 ✓（这就是把它拆开记账的价值 ✓）
  - **我自己新代码里的少报 bug**：`pageClasses` 只在"整份语料失败"那条 return 里返回 ⇒ **成功语料里的坏页全被丢掉**（几十页只统计到 4 条 ✗）⇒ 三个 return 都带上 `pageClasses` ✓ 教训：**注解也会撒谎，且比判决更难发现**（判决错了会红 ✓ 注解错了只是少报 ✓）⇒ 加注解时也要给它一条"不该为零"的下限断言 ✓
  - **真正的剩余病灶浮出来了**：`assemble/intra-fail` 在 600 dpi 语料里出现 **≈9 次 / 8 个不同 seed**（`nc-scan600-2/3/5/7/9/11/14/15` ✓ 而**同页在 300 dpi 未见此病** ✓）⇒ 页内 RS 在 600 dpi 下被超预算 ⇒ **既非出画、也非回显条带** ✓ 这才是 G2 需要修的那一个 ✓ 下一步专查它（先看符号错在页内的**空间分布**：成片 ⇒ 光照/焦外；散点 ⇒ 采样/moire ✓）
  - 工具链小账：本轮我又两次踩内联引号（PowerShell 撕碎 `[a-z]`/`m[1]` ✓ 一次 `replace_all` 顺手把**捕获组 `m[1]` 也改成了 `m[0]`** ⇒ 立刻回滚 ✓ 说明 `replace_all` 用短模式是危险动作 ✓）⇒ 一律改成写工具文件再跑 ✓

- **第 16 轮：600 dpi 那两例失败不是缺陷 ✓ 是"判据前提错配"（本轮连翻两次案 ✓ 每次都是我自己错）**
  1. 我第 15 轮说的"几何筛选问题"被 ASCII 密度图否证 ⇒ 失败页 **顶边着墨 4.7% / 左 89.5% / 右 80.8% / 底 100%**，健康页四边均 100% ⇒ **上边缘出画** ✓ 那几个贴 `y=0` 的 `5×4` 碎块就是被切断的顶边框 ✓
  2. 我据此说"`--modifier nocrop` 在说谎" ⇒ **也错了** ✓ `sim/channel.py` L341 确实把 `crop_frac` 归零 ✓ 而打印出的 `crop=0.00281` 是 L1362 的 `1.0 - visible_frac`（**实际出画面积** ✓ 来自姿态/透视/`fill=0.95..1.0` ✓ 与裁切旋钮无关 ✓）
  3. L960-965 的注释写明设计：**裁切最多吃掉一个角标区、保证其余三角完好** ⇒ 与我实测"三角标存活 + tl 消失"逐字吻合 ✓✓ 所以仿真器按文档行为 ✓ 解码器按数学行为（**3 点只有 6 个方程、单应有 8 个自由度 ⇒ 定不出单应 ✓ 拒绝是正确的、不是保守 ✓**）✓ 而 A4 放在几乎等大的平板上带 0.x° 旋转时角扫出画外**是真实物理** ✓
  **判决**：G2 数字保持 `30/32 = 93.8%` **不上调**（把正确拒绝从分母里剔掉就是重定义判据 ✗ 我不做 ✓）；改的是**记账口径**：语料工具需把这类失败标注为 `fiducial-out-of-frame`（信道前提）而非解码器失败 ✓ 下一步做这个分类 ✓ 两条真正的出路也记清楚、不混为"再修修就好"：① 明示 G2 前提"纸张完整位于成像区内"（平板有定位边 ⇒ 合理 ✓ 但 0.x° 旋转仍可切角 ⇒ 不完全可靠 ✗）② **3 角标 + 页面已知宽高 ⇒ 走仿射矫正**（少一个自由度是数学事实 ✓ 代价是放弃透视项 ⇒ 对平板近乎无害、对手机有害 ✓ 属能力扩展、不是 bug 修复 ✓）
  新增 `tools/diag-mask.mjs`（ASCII 着墨密度图 + **四边/内部着墨率** ✓ 专治"靠计数猜结构"这种病 ✓ 本轮它一次就终结了三轮的猜测 ✓）
- **本轮教训（第 4 次同型）**：我连续三次在同一件事上给出过解释（笔画核 → 纸纹桥接 → nocrop 说谎），**每一次都只用计数推断、没去看那张图** ✓ 而一张 ASCII 图 5 行就否证了全部三个 ✓ 教训升级为：**先看清楚对象长什么样，再解释它为什么失败** ✓ 光有假设不够，假设要拿像素验 ✓

- **第 15 轮：我第 10 轮对 600 dpi 失败的解释是错的，错在没查前提** ✓ 动手实现 `pskit calibrate` 之前先量前提，前提塌了：`nc-scan600-3` 的 manifest 写 `pitchMm 0.4233`（**600 dpi 档本就编得更密 ⇒ 两档 `cellPx` 都是 10 px** ✓）⇒ 我说的"60 px 角标被量成 30 px"里 **60 px 是我凭空假设的**（我从没读过我在解释的那批语料的间距 ✗✗）✓ 健康页实测 `markerPx 29.25 ÷ cell 10 px = 2.92 格` ⇒ **30 px 才是正确尺寸** ✓ 且 `otsu/peak` 在 300/600 dpi 分别是 `0.54 / 0.57` ⇒ 阈值位置几乎没动 ⇒ **"Otsu 只留笔画核"这条机制根本不存在** ✗
  真问题窄得多也普通得多：600 dpi 那页**四个角标只有一个没过候选筛选**（存活下来的垃圾是贴在图像上边的 `5×4` 碎块 ✓ 本该有 tl 角标的位置 ⇒ quad 由"三角标 + 边框垃圾"拼成 ⇒ `cover < 0.45` ⇒ `quad-too-small-for-page-region` ✓）**这是几何筛选问题、不是光度问题** ⇒ 我这轮为它造的标定机器方向错了 ✓
- **本轮唯一站得住的产出**：`core/decode/calibrate.js` 证明了**页内自描述的期望着墨面积**是真值可用的（无阈值积分覆盖率 ÷ 期望 = `1.04–1.34` ✓ 6 页含 pristine 与信道页 ✓ 期望由 `integrateLevelAreas` 逐级积分 `glyphMaskForLevel` 得出 ✓）；同时**否证了"搜索二值化阈值去凑面积"这个执行机构**（它在 **pristine 页上要 x1.3** ✓ 一条即判死 ✗）。并修掉我自己让期望值虚高 3 倍的 bug：首版把 `geo.area`（单个参考电平 0.240）当各级均值用 ⇒ 逼出"健康页也要 x0.6"的假信号 ✓ ⇒ **目标量对了、执行机构不对 ⇒ 只能当页健康度检查、不当标定** ✓ 该文件**故意没接进检测器**，并在头注释里写了"它不是 600 dpi 那个失败的解药"，防止未来的我误用 ✓
  教训同型于第 8/12 轮：**先验证你以为在量的东西的几何，再造成量它的仪器** ✓ 本轮省下的是"把能跑的那个 0.65 因子钉进代码"那种真灾难 ✓
- **下一步（G2 的真窄口）**：查 600 dpi 下 tl 角标为何过不了 `keepCandidateSquares` ✓ 怀疑与图像上边那批 `5×4` 碎块同源（信道在纸边的削顶高光伪影 ⇒）⇒ 若确认是真实物理，就必须让检测器扛住它、而不是关掉它 ✓

- **第 14 轮之二（M5 完成 ⇒ 打 tag `M5`）**：子代理交付页→网格装配 + 手写 OPC/ZIP 的 3MF + `--format stl|3mf` + `ref/verify_model.py`（902 行纯 stdlib ✓ `verify_stl.py` 降级为 79 行薄壳 ⇒ 遵守"一条路径"决策 10 ✓）+ 30 条带反例的单测 ✓ **我全部亲自重跑，不接受转述**：`231/231` 单测 ✓ `ALL GATES PASS` ✓ `python ref/verify_model.py --dir .tmp/m5verify` → **`RESULT: PASS (55 checks, 0 failed)`** ✓ 内含 `edge-census 67 698 条无向边 100.00% 恰用两次` ✓ `signed-volume +68 180.172 966 mm³` ✓ `g8-3/projection-area-error max 1.208% mean 1.189% 0/196 ≥8%` ✓ `honesty/no-csg-claim` ✓ 纸面档拒绝（exit 1 ✓ **0 文件** ✓）✓ 确定性（两次跑 ⇒ STL/3MF 各 5 文件**逐字节相同** ✓✓）✓ CLI 投影行两次一致 `max 0.76% mean 0.69% 0/1764` ✓。**它推翻了我上一轮三条判断**：① 我的 `projectTopToCells` 确是空转（期望值改为来自光栅侧 `buildCoverageTiles` + `originPx` ✓ 于是误差量出来是 0.7–1.2% 而不是假的 0 ✓✓）；② "1.0 当分母"是我度量写错 ✓；③ `watertightHint:false` 是**容器**限制而非几何缺陷 ✓（它同时纠正我的简报：page→mesh 那层当时根本不存在 ✓ 我已在 `3502db8` 承认）。**未主张**：未按 3MF Core 1.4 XSD 正式校验（本机无 XSD 无网络 ✓ "schema 正确"目前=第三方解析器读通 ✓）；契约 §5 两处文本冲突待我改（"单个 `<object>`"在"边恰两次"下做不到 ✓ 且契约写的 `<unit …>`/`zUp` 不是合法 3MF 属性 ⇒ **实现比我的文档更对** ✓）。顺手记下我自己两次 PowerShell 用参错（`$k:` 被解析成驱动器限定 ✓ PL-G@0.8 撞的是"22668 页 > 255"页间 RS 上限而非 EW 拒绝 ✓ 都是我的错、不是代码缺陷 ✓）。

：新增 **`docs/ACCEPTANCE.md`** ✓ 每个门限只回答三件事——**判决 / 复现命令 / 差多少** ✓ 没有对应命令的数字一律不算证据（包括写在那页里的 ✓）。判决汇总：G0 G1 G3 G5 G7 **通过** ✓ G2 G6 G8 G10 **部分**（各条"差多少"逐条写明 ✓ 不折算成百分比当成绩 ✓）G4 G9 **一行证据都没有** ⬜ ⇒ **不主张"通过 G0–G10"** ✓ 三条会改变判决性质的开放缺陷也列在页内：600 dpi 标定缺失（决定 G2 是"差样本量"还是"差能力"，目前排除不了后者 ✗）、回显条带一票否决整页（挂账 4 轮 ✓）、`no-hollow-corner` 语义过载（连续误导两次根因分析 ✓）。另：本轮把子代理**仍在中途**的 `threeMF.js`/`plate.js`/`solids.js` 以 `wip:` 提交（`3502db8` ✓ 防丢 ✓ **不背书** ✓ 捕获点基线 201/201 + 全门限 + `ref/decode.py` PASS ✓ 它当时已把 `buildPlateMesh` 改掉 ⇒ 说明仍在动 ✓）。

- **G2 的统计目前是可复现的手工命令，还不是门限**（本轮 8/8、7/8 那些数字的出处）：
  ```
  node cli/pskit.mjs send .tmp/payload.bin --profile P-M1-300 --out .tmp/sw-scan300-src
  node cli/pskit.mjs send .tmp/payload.bin --profile P-M1-600 --out .tmp/sw-scan600-src
  python sim/channel.py --in .tmp/sw-scan300-src --out .tmp/nc-scan300-<seed> --seed <seed> --preset scan300 --modifier nocrop
  node cli/pskit.mjs receive .tmp/nc-scan300-<seed> --photo
  ```
  下一轮要把它变成 `verify --gate G2 --corpus <dir>`：**必须在进程内自己比对 manifest 摘要**（今天的判定是靠 grep CLI 打的 `received` ✓ 那是间接证据 ✓ 门限不能这样 ✓），并且**不能 spawn Python**（沙箱 `spawn EPERM` ⇒ 语料由外部预生成、门限只消费盘上目录 ✓）。
- **600 dpi 的门限标定缺失（G2 与 G10 共同的前置）**：`pskit calibrate` 之前，任何"某个 dpi 过、另一个 dpi 不过"都必须按未解缺陷对待，不许靠预设挑一档报好消息 ✓
  **本轮把"该拿什么当真值"这个问题先量清了**（否掉两条看起来最省事的路线 ✓ 记下来防止下一轮重走）：
  1. ~~用 `markerPx ÷ 点阵簇尺寸 ≈ 3` 当尺度无关的自描述真值~~ ⇒ **不成立** ✓ 实测 300 dpi 探针里角标簇是 `30:4`、点阵簇是 `5:27213`，而**格宽是 10 px** ✓ 因为点阵的连通域是**字形特征**（点/环各自成块）而不是格子 ⇒ 该比值随字母表与剖面变化 ✓ 拿它当判据只会造出一个随剖面漂移的"真值" ✗✗
  2. ~~从 manifest 取 `cols/rows` 推 `markerPx/pageWidth = 3/cols`~~ ⇒ 数学上成立 ✓ 但**照片路径拿不到 manifest**（自描述原则要求几何信息在页内回显条带里 ✓ 而条带要**先矫正**才读得出 ✓ 矫正又要角标 ✓ **循环依赖** ✗）
  ⇒ 结论：可用的尺度无关真值只剩**页面相对量**（`pageRegion` 已有的 `quad-too-small-for-page-region` 那一类 ✓）与**四角标彼此一致性** ✓ 因此 `pskit calibrate` 的正确形态是**在采集到的页上直接实测 inkness 分布**（峰值/众数/双峰间距 ✓ 这些量对 dpi 与曝光都敏感 ✓ 也正是 Otsu×常数 覆盖不到的方向 ✓）并据此定阈，而不是继续给阶梯加档 ✓ 加档是调参 ✓ 实测才是标定 ✓
  **本轮实测（4 页：300dpi 正常 / 600dpi 正常 / 600dpi 失败-找不到角标 / 600dpi 失败-intra）**，`max 242–254` ⇒
  | 页 | `otsu/peak` | 着墨率 >otsu |
  |---|---|---|
  | 300 正常 | 0.56 | 6.55% |
  | 600 正常 | 0.59 | 9.14% |
  | 600 失败(角标) | 0.59 | **3.34%** |
  | 600 失败(intra) | 0.57 | 9.65% |
  ⇒ **`otsu/peak` 完全不判别**（四页 0.56–0.59 ✓ 所以"阈值离峰值是不是太远"这种朴素自检看不见本轮的问题 ✗✗ 这条否掉了一种最省事的做法 ✓）；**着墨面积比才是信号**（失败页 3.34% 对健康页 6.5–9.7% ✓ 约 2× 分离 ✓）⇒ `pskit calibrate` 应判"**实测着墨率 ÷ 期望着墨率**"，而期望值可由页内自描述的 `cols×rows×字母表` 算出 ⇒ **尺度无关、不依赖 manifest、不依赖 dpi** ✓✓ 正是 PSKT"页面自己说明自己"的主旨落在标定上 ✓ 第二失败页（intra）着墨率正常 ⇒ 它**不是同一个病** ✓ 不许一并归因 ✓（它 `>0.65·otsu` 只有 11.45% 对健康 600 页 25.74% ⇒ 中间调偏少 ✓ 待查是否与色道/剖面有关 ✓）
- **阶梯的性能代价**：双向阶梯在困难页最多跑 6 次全图连通域 ⇒ 600 dpi 单页最坏 ~4.9 s ✓ 与 G6 的"整页读回 430 ms"已差一个量级 ✓ 收口办法不是砍档，而是**inkness 直方图先判该往哪边放宽**（一次 O(N) 统计 vs 六次 O(N) 连通域 ✓）⇒ 归入 `pskit calibrate` 同一处实现 ✓
- **匹配滤波的对齐搜索半径仍与像素无关地固定在 ±1 分析格**（= 格宽/10 ✓ 尺度正确 ✓）；若将来改网格，必须同步改半径，否则又回到本轮证伪的那条错误 ✓
- `page.js` 让 448 bit、零纠错的回显条带一票否决整页；有 manifest 时应允许走数据区、由 RS 与末端摘要定夺（第 8 轮记录，仍未做）。
- `no-hollow-corner` 语义过载（"凑不齐四角"与"角标被糊住"共用一个 reason ✓ 连续误导两轮）⇒ 拆分。
- 仓库根目录偶有探针文件（如 `.tmp-probe.mjs`）由子代理生成 ✓ 收尾时清掉，别让它进提交。

- **真实相机/扫描仪照片尚未验证**（M3 只到合成照片）。仿真里 1px 模糊就能桥接点阵 → 真实光学 MTF 更差，`PL-G` 之外的粗档可能不够。M4 信道 `sim/channel.py`（子代理在写，含 scan300/scan600/phone40/phone-hard/plate-matte/plate-glossy/identity 预设）落地后立刻把 G2/G3/G4 打在真实退化上；若 `PL-G` 也撑不住，就再抬 EW 地板（数字会变，结论不变）。
- **`--gate G4` 依赖照片路解码的吞吐**：单页矫正 ~360-460ms，500 seed × 7 档 = 数十分钟。要么并行，要么在门限里降采样（`warp.test.mjs` 那组 68s 同理）。
- `pskit receive` 只吃 PNG；TIFF 读回会点名跳过（不静默丢文件）。PDF 输出已接（`--format pdf|all`，超出 380 MB 组装预算时明确拒绝并让用户改打 PNG）。
- 合成照片里"字节穿越照片"那组测试要 68s（3 档 × 多页 × 全分辨率矫正）。M9 soak 前需要降采样或减少页数，否则门限跑不动。
- `densityReport` 对单通道档的 `monoSafe` 标 `n/a`（语义正确但易被读成"没保护"）。
- **交叉校验：绿了**（PLAN §9 闭合）。`python ref/decode.py` → `conformance.json: PASS with 4 spec gap(s)`，309 项检查两套独立实现全数一致，19 组里 18 组 `n/n`。本轮消掉的两类根因与"谁错了"的判定过程记在 `ref/README.md` 的表里（擦除定位子形式 → `rs-erase 4/4`；页间装配被转置 → `transfer 51/51`+`transfer-loss 3/3`）。剩 4 个 `geometry-*` SPEC GAP 是**有意接受**的：那需要把决策 6 的 EW 量化搜索整段搬进规格，代价高且易与实现脱节，板材几何的端到端证据归 G8/G10，不许拿它代替。`ref/README.md` 已写（那个崩溃的子代理从没写它）。**G2/G4 从此可以引用"另一个实现也同意"这一半证据了**——但这两个门限本身仍待接信道（下一条）。
- `sim/channel.py` **已交付可用**（README 23 KB + `--preset identity|scan300|scan600|phone40|phone-hard|plate-matte|plate-glossy`，输出同名 PNG + 原样复制 manifest，`pskit receive` 可直接吃 ✓ 一条 `--report` JSON 把每个物理量都记下来了 ✓）。G2/G4 现在缺的不是信道而是**我自己的几何路能用**（见 G2 行）。`selfcheck.py` 65 KB 待压成 ≤2 min 冒烟 + `--full` 可选。
- M5（STL/3MF）**两次委托都失败**：子代理各烧完一个上下文窗口后 `core/mesh/` 里一个字节都没有。下一轮拆成"先写文件再解释"的小块自己做，或按单文件分别委托。

## 变更日志

- M1：编码核心完成，105 单元全绿；修 RS 擦除定位子、BM 的 m 计数、deflate LZ77 重叠拷贝、native 档校验字节重复计入、填充字节混入摘要；新增 `monoSafe` 三档与分块塞满；`tools/fix-mojibake.mjs`。
- **M2**：渲染层完成——`core/render/{units,glyphs,layout,raster}.js` + `core/palette.js` + `core/decode/{ideal,echo,png-read}.js` + `core/render/{png,tiff}.js`（pillow+node:zlib 双向验证）+ `cli/pskit.mjs`（send/receive/status/verify/roundtrip）。G1 图像层 269 万格 0 误读；G7 通过；磁盘文件→PNG→文件 sha256 相同。修：静区计入版面、ρ 阈值自标定、mono 搬道、色道存活判据反了、门限空跑护栏。测试 105→136。
- **M3**：解码链路完成到"合成照片可往返"。新增 `core/decode/{transform,fiducial,warp}.js`（4 点 DLT 单应 + 残差自校验 + 双线性格心采样；连通域角标检测 + 页面定位 + 结构过滤；矫正到规范画布后直接复用 `readPageIdeal`/`readEcho`）、`core/render/pdf.js`（DeviceRGB + FlateDecode Predictor 15，字节确定性，`%%EOF` 结尾）、`tests/unit/{transform,warp}.test.mjs`。测试 136→156，全绿；门限 `all --seeds 3` 全通过。**本轮最大的东西不是代码而是决策 6**：形状字母按 EW 量化，板材容量掉 5×（1098→220 B/页），纸面不变；同时把检测器的四条硬规则钉死（决策 9），每条都对应一个已复现的假成功路径。
- **M4（上半）**：解码收口成一条路 + 两个门限 + 交叉校验答案卷。新增 `core/decode/page.js`（`decodePage`：角标→单应→回显头→读格唯一入口；干净画布才走快路，快路读不出来必须退回几何路）、`core/decode/advice.js`（20 个 reason → 物理成因 + 重拍指令）、`tools/emit-conformance.mjs` + `tests/conformance.json`（78 向量/196 KB/400 KB 预算闸）+ `tests/unit/conformance.test.mjs`（11 例**活复算**：CRC/SHA/deflate/ChaCha20/PBKDF2/RS/交织/头/页解包/页解码/版面几何，外加"重新发射逐字节相同"）、`tests/unit/{advice,render-glyphs}.test.mjs`。CLI `receive` 现在能吃照片：逐图分类失败而不是第一张坏图就整体中止、统计重复页、**原子写**（`out.part`→rename；摘要不符删文件并 exit 1）。门限：`G3` 协议层（丢 0..parity 全复原、丢 parity+1 干净拒绝且不写文件、每页喂两次仅计重复、异会话页拒绝）、`G5` 正式 **10000 次篡改 0 误接受**（800 次由明文摘要拦下）+ 两项变异检验。`core/render/pdf.js` 支持 N 页 `pack.pdf`（单页输出保持逐字节不变），`send --format` 改列表并有拼写护栏。
  本轮修掉三个"自己骗自己"：**①** G5 的摘要承重测试第一版是假通过——PL-M1@0.4 一页就装下全部数据，伪造页根本没参与判定；改成先短一页再塞伪造页后，`feed: accept` 但结果为空，摘要才被证明是最后一道闸。**②** 交织：`unpackLevels` 要的是**去交织后**的 levels，直接喂印刷顺序会得到"看着合法的垃圾页"，page-decode 三条向量因此全 fail。**③** `rsEncode(data,nsym)` 返回完整码字而非校验字节，我自己把 data 拼了两遍——`tools/inspect-conformance.mjs` 复算时才撞出来。另外 `neededCellEw` 原来是解析式估算（10 EW，实际 8 就够，会让人白印粗一档），改为复用渲染器同一条量化搜索、且搜索域用半 EW 步长（真实 cellEw 几乎都是分数：10.4 能排 4 级字母，10 和 11 都不能）；`cellEw` 对纸面从 `Infinity` 改成 `null`（`Infinity` 一进 JSON 就静默变 `null`，这就是它泄漏进答案卷的方式）。测试 156→179，全绿；`--gate all --seeds 3` 全通过，容量表数字未变。
- **M4（下半·交叉校验首跑）**：PLAN §9 的对拍真的接通了——`ref/decode.py`（1.5k 行，只依据答案卷 `meta` 独立实现，不读 `core/*.js`）现在跑完 78 条向量并打印 `301 checks / 58 FAIL / 7 SPEC GAP`。**它立刻咬出我这边四个真缺陷（均已修）**：**①** `encodeTransfer(x,{passphrase})` 少传 `cipher:true` 会**静默印明文**——答案卷里那条"加密"向量其实是明文，独立解码器的报法是"页面上没有任何东西告诉我要解密"（决策 13 + 回归测试；反向性质也钉住：看到 `CIPHER` 而无口令只能 `needPassphrase`，绝不吐字节）；**②** 我给 `meta.compression.container` 写的格式是**错的**（"u32be 原始长度"）——真实 `PSZ1` 是 10 字节头、offset 6 **小端** u32、offset 5 保留字节；我的 `core/deflate.js` 文档一直是对的，错的是答案卷，独立实现照答案卷读必然对不上；**③** 逐向量 `note` 让读者去 inflate 一个 method 0（stored）容器；**④** 我把 `headerCrc` 区间改成半开后，`decode.py` 仍按闭端切片 → 多哈希一字节且字段读错位（"改一端不改另一端"的区间约定是本轮我自己造的 bug）。`Report.check` 也顺手改成能识别"把真判据当 detail 传"的调用点（那种写法会永远记 pass）。答案卷重发射为 201 KB / 78 向量；测试 179→180。剩下 54 个 FAIL 是参考侧自己手搓原语的 bug，见"已知风险"。
- **M4（下半之二·把对拍修到只剩一处真分歧）**：`58 FAIL → 12 FAIL`。参考侧 RS 的**未知错误纠正**原本全灭，`ref/probe_rs.py` 逐步打出中间量后定位到一个纯取向问题：它的 PGZ 方程组解出的 Λ 系数是**倒序**的（`[1]+lam` 的根在 X_j 而不是教科书升序形式的 X_j⁻¹），Chien 搜索代 X_j⁻¹ 于是永远找不到根、直接落到"无解"分支——失败方向是安全的所以不会崩，看起来就像解码器能力不够，只有一直等到对拍才现形。修完 `rs-error 4/4`、`transfer 33→42/48`。原语按决策 14 换成 stdlib 第三方见证（`crc 11/11`、`sha256 25/25`、`pbkdf2 6/6`），它自己那份坏掉的 SHA-256 降级为 `_sha256_handrolled` 并把证据写在 docstring 里（所有输入的输出尾部四个字恒定 = 消息根本没进压缩函数；空串得 `8461857676cf5d0a…` 而非 `e3b0c442…`）。**顺带修掉我自己上一轮造的护栏**：`Report.check` 用"第二个参数是不是字符串"来区分 `(ok, detail)` 与 `(expected, actual)`，于是把两个字符串的正常比较误判成"真判据被当 detail 传了"，凭空报出 3 条 pbkdf2 假失败——判定改成看**第一个**参数的类型。教训：护栏本身也要有反例测试，否则它只是把假绿换成假红。新增 `ref/probe_rs.py`（RS 分解诊断，5 组断言，2 条 FAIL 即擦除路径的最小复现）。
- **M4（下半之三·对拍只剩一处行为分歧）**：`rs-erase` 从 0/4 到 4/4，根因是**擦除定位子的形式**——它用 `le = Π(1 + X_j z)`，而本协议的伴随式约定（`S_k = Σ Y_m X_m^k`，`X_m = α^(n-1-j)`）要求 `Π(1 + X_j⁻¹ z)`，否则修正伴随式 `M_k = Σ le_i S_{k+i}` 根本不会归零（直接验算：3 擦除 nsym=10 时前者给 `M=[42 e7 0d 3b…]`、后者全零 ✓）。M 不归零 → `nu=0` 的残差检验失败 → 每个"整页丢失"的案例都报无解；这正是 `transfer-loss` 全灭的原因（丢页是**擦除**不是错误）。`probe_rs.py` 6 条断言全过。次级通道位移 bug 见决策 15①：修完 `page-unpack 21/21`（**我的 JS 是对的**，它被 G1 的 269 万格真实渲染读回物理验证过）。另外我把上一轮自己重写严了的 `container_split` 收回一处错误判据——`len(body) < rawLength` 对 method 1 天然成立（rawLength 是**解压后**长度），照它判就把每个真实压缩容器都拒了 ✓ `deflate 10/10`。**本轮再次确认：新写的"更严格"检查必须有反例，否则它只是新的假红来源**（上一轮是 `Report.check`，这一轮是 container）。`python ref/decode.py` 现余 12 FAIL，全部集中在"干净页端到端重组从 offset 1 起差一字节"这一类；已逐行比对排除 intra 块排布差异（两边都是 `block b = content[b·k:(b+1)k] + parity[b·nsym:(b+1)nsym]` ✓）。JS 侧 180/180 不变。
- **M4（下半之四·§9 闭合）**：`python ref/decode.py` 首次 **PASS**（309 项检查全一致，4 个有意接受的 geometry GAP）。两处根因：① 上一轮的擦除修复带出 `transfer-loss`；② `transfer 43→51/51` 的关键是它的页间装配把"按列看页"的纠错视角用到了数据拼接上——**列优先 vs 页优先**的症状恰好是"byte0 相同、byte1 起不同"（两种顺序的第一个字节都是 page0[0] ✓），这个对应关系一旦对上就没有第二种解释。剩下 2 项 `header` KeyError 揭出一个我的答案卷缺陷：`fields` 里没给 `magic`/`version`，于是"重编码并比对字节"对独立实现**不可判定**——现在每个 `headerLayout` 点名的字段都在。**同时改掉了两处我自己的表述病**：`meta` 里两句话对 `payloadLen` 含哪种 padding 说法相反；`ecc.blockPad`（intra 块对齐）与头里的 `blockPad`（页填充）同名不同物 → 后者改名 `intraBlockPad` 并在 `meta.pages` 里把装配顺序、页序、两种 padding 全写成数据。参考侧另修一处"过期意见"：它对 method 0 无条件打印"note 叫你去 inflate"，而那句话我上轮已改——**对拍工具自己也要跟着答案卷更新，否则它会把已修的缺陷当成新发现反复上报**。新增 `ref/README.md`（独立性纪律、"PASS 不等于什么"、GAP 为何不闭合、以及那张"谁错了"的表）。JS 180/180、`--gate all` 全通过不变；`ref/probe_rs.py` 6 条断言全过。
- **第 10 轮（G2 的 600 dpi 异常：两条假设一条被证伪、一条撞出真 bug）**。先给统计：物理正确的扫描仪模型（`--modifier nocrop`）下 200 KB 载荷 × 3 页，**scan300 8/8、scan600 7/8** ✓ 预设原样（含裁角）则 scan300 7/8、scan600 5/8 ⇒ 裁角类失败属 G4（手机）而非 G2 ✓ 追那唯一的 scan600 失败（页 000/002 `intra-fail`、页 001 找不到角标）时下注假设是"dpi 翻倍 ⇒ 我的匹配滤波 ±1 px 对齐搜索的物理半径减半"，于是实现固定分析网格（`ANALYSIS_GRID` + `downsampleAlpha` ⇒ 半径自动随 dpi 缩放 ✓ 且 300 dpi 下 1 px/分析格 ⇒ 逐位等价 ✓ 零回归设计）——**目标语料毫无改善 ⇒ 假设证伪，改动回退**。但过程里撞出一个我自己的真 bug：`cellPx < 分析网格` 时（0.2 喷嘴的小格子）box 平均的 `round(gy·block)` 会造出**空的行列、凭空填零** ⇒ 在**完全无信道的理想渲染**上 400 格误读 90 格 ✗✗ ⇒ 教训比结论值钱：**模板与测量图必须共用同一套格心；把 α 平均到"格心不与模板采样点重合"的粗网格会引入亚像素形状畸变**（`cellPx` 不整除网格边长时）。真正的发现是：600 dpi 页上角标**量出来 30×30 而应为 60×60**、着墨率 10%→3.3% ⇒ 物理模糊 σ 不变而 dpi 翻倍时**每像素峰值覆盖率约减半，Otsu 只留下每笔画的中心核** ✓；而我的重试阶梯 `1, 1.35, 1.7, 2.1` **只会往更严的方向走、从来不敢往更松** ⇒ 单向阶梯是设计空洞（与第 8 轮那个 `blank-image` 覆盖真实成因是同一类错误：阶梯只覆盖了失败方向的一半）。补成双向阶梯后 page-001 前进到 `quad-too-small-for-page-region` ✓ 但实测 0.7 不行、0.65 行 ⇒ **"Otsu × 固定因子"这个参数化本身就是错的**，正解是 PLAN 里 M7 的 `pskit calibrate`（在采集页上实测 ρ 表/MTF 再定阈 ✓ 不是调参 ✓）。**我故意没把 0.65 钉进代码**——那是给单个 seed 过拟合 ✗ 201/201 单测绿（含新加的 7 条匹配滤波性质测试）。
- **第 13 轮（M5 补上"页→网格"这层，然后被同行评审打掉我自己的新文件）**。先补洞：`core/mesh/` 里只有图元（`solids.js`）与写盘（`stl.js`），**从来没有任何东西把一页变成三角形** ⇒ 两种网格格式都没有产物可编码 ✓ 这个洞是我给子代理的简报造成的（我叫它"复用现有三角形生成" ✓ 而页面级那层根本不存在 ✓ **我的错** ✓）。我补了 `core/mesh/plate.js` + `ref/verify_stl.py`（纯新文件 ⇒ 不可能弄坏基线 ✓）；Python 独立校验在真实 13.1 MB / 262 080 三角形板上 **13/13 PASS**（`84+50n` 逐字节 ✓ 头部自报三角形数与二进制数一致 ✓ units=mm ✓ 零退化 ✓ 每条法向与顶点右手序 cos>0.99 ✓ 包围盒 149.2×149.2×0.30 mm ✓ 有向体积 +373 mm³ ✓）且 `--selftest` 证明这些检查**会失败** ✓✓ 最有意思的一条：**无向边 393 120 条中 100% 恰被两个三角形共用 ⇒ 网格其实是水密的**，而 `core/mesh/stl.js` 一路保守地报 `watertightHint:false` ⇒ 独立侧测得比我自己申报的更好 ✓ "保守误报"不等于真相 ✓（已要求重新实测并改注释 ✓）。
  **但同行评审把我的 plate.js 打掉 6 条，全部成立、我接受并已移交重写** ✓✗ 其中两条**我的测量本身就能证明**：包围盒 z 向只有 0.30 mm ⇒ ①**没有 2.0 mm 底板实体**（违反契约 §6.1 包围盒须与 plateMm 一致）②**shape≥2 的实体悬空**（`zBottom = top - reliefMm` ⇒ 与底板间有 0.3 mm 空腔 ✓ 不可打印）；其余四条：③ `cellEw` 我用了 `pitchMm/nozzleMm` 而 `pageLayout` 用 `pitchMm/ewMm`（挤出宽 ≠ 喷嘴宽）⇒ 网格半径与 PNG 用的不是同一份 `glyphGeometry` 输入 ✓ 直接违反 §3"共用掩码" ✓ **最严重**；④ `shape=0` 我干脆不画 ✓ 而 §3 的掩码在 `shape=0` 时仍画外环 ⇒ 网格与掩码不同形 ✓ G8 §6.3 必不过；⑤ 忽略 `layout.originPx` 与像素量化后的格宽 ⇒ 第 40 格累计漂移 ≈1.9 mm（半格）✓ 投影对拍会错位；⑥ 没有 `weldTriangles` ⇒ "每条无向边恰两次"根本无从判定。
  **本轮最该记住的一条（同一课第四次）**：我的 `projectTopToCells` 拿 `splitCellLevel(...).shape>0` 当"期望掩码" ✓ 而 `buildPlateMesh` 又拿同一条 `shape>0` 决定画不画 ⇒ **两边同源 ⇒ `mismatch=0/1764` 是空转出来的** ✓✓ 它看起来是本轮最漂亮的数字（"逐格复现掩码 0 失配 ✓"）却一点证据都不承载 ✓⇒ 判据必须来自**被验证对象之外**（这里=光栅侧真实页掩码）✓ 台账里"新断言必须有反例"这条纪律写了三轮 ✓ 这轮被我自己破了 ✓ 已把 `areaErr 70–80%` 一并记下：那是我把分母写成 1.0（字形本就不填满格子 ✓ 点+环约 25% ✓）⇒ **G8 投影那半仍判"未通过"** ✓ 逐格复现已证、面积数字尚未有意义 ✓ 不许偷偷宣布通过 ✓ 另外 `PL-G` 在 0.6/0.8 喷嘴下被 `glyphGeometry` 拒绝（3.6 mm 格 = 6 EW、4.5 EW < 8 EW）⇒ 网格与光栅共用真值源的直接后果 ✓ 归 G10 材料 ✓ 提交 `dfbc4e2`。
- **第 12 轮（G2 语料门限成形 ✓ 补记：本条标题当初被丢掉、内容错挂在 13 轮那条的尾部 ✓ 现按提交 `bcfe0f3` 归位）**：新增 `tools/g2-corpus.mjs`——消费盘上语料目录（**不 spawn Python** ✓ 沙箱 `spawn EPERM` ✓ 而且这分工本身是对的：会自己生成信道的门限有可能开始跟自己串供 ✓），**判据是它自己在进程内重算的 `sha256(result) === manifest.sourceSha256`** ✓ 不足 100% 即 `exit 1`（G2 判据是全数通过 ✓ 不是打分 ✓）。实跑上轮那 16 份平板模型语料：**15/16 逐字节（93.8%）181.8 s** ✓ **与上轮靠 grep `received` 得到的 15/16 完全吻合** ⇒ 那批数字不是间接证据撑起来的假象 ✓ 逐页明细还露出一个没预料到的好旁证：`nc-scan600-4` **两页都是 `readout/echo-bad-magic`、整批仍然逐字节还原** ⇒ 页间 RS 真在兜住单页头部失败 ✓✓（这正是"一页掉角不等于整批失败"的同一机制在头部路径上的版本 ✓）。唯一失败仍是 scan600 seed 3（`short 0/1` ✓ 与第 10 轮同一条 ✓ 未回归也未修复 ✓ 归 `pskit calibrate` ✓）。工具还当场逮到我自己一个 API 想当然：`planPage` 返回的就是几何对象本身、**不是 `{ok,reason}`**，我那句 `if (!geom.ok)` 把每个合法几何都拒了 ⇒ 报 0/16 且耗时 0.0 s ✓ **而它没有假装成功 ✓ 这就是把判据写在自己手里的回报** ✓（CLI 侧从不检查 `.ok` ✓ 已改成 try/catch 并在注释里写下这次踩坑）。另：本轮**没碰 `cli/pskit.mjs`**（M5 子代理正在那里加 `--format stl|3mf` ✓ 两条线改同一文件必然互相覆盖 ✓ 下一轮再接 `verify --gate G2`）。
- **第 11 轮（标定信号到底是什么 ✓ 提交 `6205e13`·只改 STATUS）**：在四页上实测"声明墨面积 vs 积分覆盖率"，确认前者是可用真值、而**用二值化 cut 去反推该面积不是可用执行器** ✓ 同时**拒绝把 `0.65` 钉进代码**（那是给单个 seed 过拟合 ✗）⇒ 正解指向 `pskit calibrate` ✓
- **第 14 轮（M5 收口 + 自验收面世 ✓ 提交 `7d126b3`、`982bad4` ✓ tag `M5`）**：页→网格装配、手写 3MF、以及**投影对拍改用光栅侧真实页掩码**（补上第 13 轮那条"两边同源 ⇒ 空转"的洞 ✓）；`docs/ACCEPTANCE.md` 建立——每个门限一行"判决 / 复现命令 / 差多少" ✓ 这轮之后 G8 的 STL+3MF+投影三件都算有过独立证据 ✓
- **第 15 轮（推翻我自己第 10 轮的解释 ✓ 提交 `f02820b`）**：所谓"60 px 角标被 Otsu 量成 30 px"里那个 **60 是我编的**（从未读过 manifest ✓ 实际 `P-M1-600` 的 `pitchMm 0.4233` ⇒ `cellPx` 在 300/600 两档都是 10 px ⇒ ~30 px 角标是**正确尺寸** ✓✓）；`otsu/peak` 两档 0.54/0.57 ⇒ 阈值根本没动 ✗ 同时落地下一步用的量具：`core/decode/calibrate.js`（**故意不接进检测器** ✓ 头注释写明"它不是 600 dpi 那个失败的修法" ✓）
- **第 16 轮（两条"缺陷"其实是我自己没读对象 ✓ 提交 `d473a7a`）**：`tools/diag-mask.mjs` 五行终结三轮猜测——顶边着墨 4.7% 对内部 100% ⇒ 是**纸掉出成像区** ✓ 而 `--modifier nocrop` 也没说谎（stdout 的 `crop=` 是**实际出框面积**、不是那个旋钮 ✓）⇒ 接收端拒 3 角标是**数学正确**（3 点只给 6 方程、单应要 8 DOF ✓）⇒ 结论：这是**判据前提没写明**（"纸张完整在成像区内"），不是缺陷 ✓ 本轮把"先看图再解释图"升级为硬规矩 ✓
- **第 17 轮（不再凭记忆论证契约 ✓ 提交 `2ffc905`）**：vendored 官方 `ref/3mf-core-1.4.0.xsd`（10 196 B ✓ 附来源头 ✓ 禁止手改 ✓）+ `tools/fetch-3mf-schema.mjs`（缺任一关键声明就**拒绝写盘** ✓）⇒ 据它纠正 `MESH-CONTRACT.md` §5：`zUp` **这个属性不存在** ✓ `CT_Model` 只有 `unit/xml:lang/requiredextensions/recommendedextensions` ✓ 并补上规范顺序（`metadata* → resources → build`；`basematerials` 必须先于 `object` ✓）；`g2-corpus` 加 `fiducialCensus()` + `classifyFailure()` ⇒ G2 失败第一次被**具名分类**（`fiducial-out-of-frame` ✓）
- **第 18 轮（★ 从未单独提交 ✓ 它的 `diag-intra.mjs` 首次出现在 `R19` 的提交里 ⇒ 台账当时记了工作、日志里却没有这一步 ✓ 记此以免"看 git log 数轮次"骗人）**：造出逐格错误图工具（同页 degraded vs pristine），并发现**一件承重事实**：`decodePage()` **不做纠错**（`page.js` L63-64 直接取 `readPageIdeal` 的 `levels` ⇒ 那是 **ECC 之前的原始读数** ✓）⇒ 之前所有"读出来对不对"的讨论口径都靠这句才立得住 ✓
- **第 19 轮（`intra-fail` 是两种病 ✓ 提交 `5b0ecd4`）**：9 页逐格测完 ⇒ **A 类**（`0.23%`/`0.52%`/`1.41%`/`6.27%`/`7.22%` ✓ 669 格就已超页内 RS 每块预算 ⇒ 是 `P-M1-600@600dpi` 的**容量事实**、不是 bug ✓ 也正是页间奇偶的用武之地 ✓）与 **B 类**（`7/002` **`287507/287507` 全读成同一符号** ✓ 页级判决崩塌 ✓）⇒ 并自曝三个自己的假数字（`levels` 是**打包数值** ⇒ `.shape` 比较两边皆 `undefined` ⇒ 对失败页报"0 失配"✓ 空转断言第 6 次；硬编码 `nozzle 0.4`/`palette INK2` 对 manifest 的 `null`/`PAPER1` ⇒ 改为读 manifest、读不到就**拒绝运行** ✓；把"语料级 OK"当"页级 OK"造出一个假矛盾 ⇒ **正是第 12 轮我自己写下的"别让两层互相顶包"、这次反着踩 ✓**）
- **第 20 轮（一条设计选项在实现前被数据关掉 ✓ 提交 `40bcacb`）**：`tools/diag-symbol-population.mjs` 量 78 个可读页的总体 ⇒ 健康页 `maxFrac` `p90 = 0.9057`、而崩塌页是 `0.9775`/`1.0` ⇒ **阈值只剩 7 个百分点的缝、且它随载荷可压缩性漂移** ⇒ "符号分布不可能"这个判据**不成立** ✓ 于是**没有造那道会在真机上误杀好页的闸** ✓（`distinct` 全体只有 2 ⇒ 那是本剖面字母表本身 ✓ `secondFrac` 分布重叠 ✓ 两条候选同死 ✓）
- **第 21 轮（第二条假闸死在测量下 ✓ 提交 `e158cfe`）**：实现"实测墨 vs 声称墨"（两边同尺已核 ✓ `measureTargets` 的 `dot/(band·bandScale)` ＝ `analyseCell` 的 `dotA/bandA` ✓）⇒ `misfitFrac` **构造性作废**（两点字母表 `targets=[0.000,0.370]` ⇒ level-0 目标是 0 ⇒ 相对误差无意义 ⇒ 它只是复述"level-0 占比" ✓ `0.9057 ≡ maxFrac` ✓）；`inkRatio` 在 pristine 是 `1.065`、对信道页 `3.6–21.7` ⇒ 它是**渗墨量度**（σ 固定 mm ⇒ 600 dpi 按格宽度翻倍 ✓ 与 600 p50=16 对 300 p50=4.5 自洽 ✓）。**本轮我给的"过中点 → 整页 level 1"因果链在第 22 轮被我自己撤回（见下）** ✓ 另自曝：以为 `page.js` 会 `...read` 透传新字段 ✗ 实际 `readFast` 是显式白名单 ⇒ 12 分钟测量白跑 ⇒ 教训升级"加字段必须用一条输出证明它到了调用方" ✓
- **第 22 轮（末轮·诚实收口 ✓ 本提交）**：探针 `tools/diag-collapse-path.mjs` 一跑就推翻第 21 轮的根因表述 ✓✗ 两处口径错（真边界是 `noDot ≈ 0.102` 不是 `0.185` ✓；`levels` 是 `joinCellLevels({shape,colour})` 的**打包值**、按 `v===0` 切分把色道 bit 混进了形状计数 ✓），决定性反例是**能逐字节复原的健康页** `nc-scan600-1/000` 在该探针里同样严重偏斜且 `disagree=74240` ⇒ **"渗墨推过中点 ⇒ 整页翻 1"未被建立 ✓ 已收回 ✓ 工具头注释写明它不可被当作结论引用** ✓ 仍站得住的是：A/B 两类存在（第 19 轮独立测得 ✓）、`inkRatio` 量的是渗墨 ✓、以及"符号分布闸不成立"（第 20 轮 ✓）。**B 类的诚实状态 = 已确证存在、已量出与渗墨相关、根因未定** ⇒ 留下一条最小可验工单（用 `splitCellLevel` 取纯形状直方图 + 用**解码出的页头**而非文件名对齐页 ⇒ 然后才允许主张/否证"形状 level-1 占比随 inkRatio 单调上升"）✓ 同时补记：变更日志缺第 11/12/14–22 条（第 12 条标题丢失、第 18 轮从未单独提交 ✓ 现按提交号归位 ✓）
